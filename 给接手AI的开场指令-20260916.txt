# 给接手 AI 的开场指令（2026-09-16）

> 用法：把下面【版本 A】整段复制，粘给接手的 AI，作为**第一条消息**发出去。
> 前提：那个 AI 能读写本机文件（能打开 `D:\下载的文件\学习工作台`）。
> 如果它只能在网页里聊天、碰不到本地文件，用【版本 C】。

---

## 【版本 A】完整版（推荐 · 直接整段复制）

```
接手「星途」（原「学习工作台」）项目第二批的收尾工作。

【第一步 · 必做】
先完整读这个文件，它包含全部现状、未完成项的完整任务书、部署流程和回滚点：
D:\下载的文件\学习工作台\交接文档-20260916c-第二批执行现状与对接.md
配套读：D:\下载的文件\学习工作台\更新日志-20260916-第二批.md

【铁律 · 违反会整页白屏，本项目已因此出过两次全站事故】
1. 唯一可写代码树 = D:\下载的文件\学习工作台。
   绝对不要碰 C:\Users\ATM\WorkBuddy\Worktrees\... —— 那是废弃副本（无 .git、缺文件），改了等于全部白干。
2. 页面要跑在安卓 APK 的老 WebView 里，内核约 Chrome 50~58，上限 ES2017。以下语法一律禁止：
   ?.   ??   .replaceAll(   Object.fromEntries   .at(   顶层 await
   正则后行断言 (?<= (?<!    对象展开 {...obj}    对象剩余解构    指数 **    可选 catch 绑定 catch {}
   注意：node --check 抓不到这些，必须用 Grep 逐项确认 0 命中。
3. 禁止用 var/const/let 重复声明别的文件已存在的全局名 —— 浏览器会判该文件 SyntaxError 并整文件拒绝执行
   （这是历史上"AI 功能集体失效"的真凶）。新增全局一律写成：
   if (typeof window.xxx !== 'function') window.xxx = function () { ... };
4. 禁原生 alert/confirm/prompt，用页面内 toast（window.xtToast）或页面内区块。
5. 改任何 assets/*.js 或 CSS 后必须 bump 引用它的 ?v= 版本戳；但项目约定「波内任务不 bump、波末单点统一」，
   目标新戳是 20260916M。补裸路径戳要用单行内正则，严禁跨行正则（跨行会吞掉 --> 和标签 = 整页白屏）。
6. app.js（被 31 个页面引用）、api.js（30 个）、common.css（全站）是共享文件 ——
   同一时刻只允许一条线改，并行写必然后写覆盖先写。
7. 本项目禁 git 操作（push 需动态代理）；文件重命名用 os.rename，不要用 git mv。

【本机环境坑 · 会让你白浪费大量时间】
- bash 缺 coreutils：ls / grep / head / tail / wc / dirname 全部不存在。
  → 一律写 python 脚本文件再执行（不要用 python -c 单行，正则里的引号会被 shell 吃掉导致 SyntaxError），
    或用 Read/Grep/Glob 工具；命令一律写绝对路径。
- Windows 中文路径 + python 控制台：print 中文会 GBK 崩溃 → 结果一律写 UTF-8 文件，再用 Read 工具读。
- python = C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe
- node   = C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe
- 换行符必须保持：根 HTML / app.js / api.js / common.css 是 CRLF；
  assets/ai-service.js、assets/ai-page.js、assets/ppt-works.js 是 LF。改混了会造成整文件 diff。

【现在要做的第一件事】
修 D:\下载的文件\学习工作台\更多.html 的白屏级结构破损。
对接文档 §4.1 里已给出逐行精确方案（删除 L200–L352，保留 L198-199 的 <script>(function(){
与 L353-437 的函数体 + })();</script>）。按那个方案做，注意：
- 只做定向删除，禁止整文件重写（Write 覆盖）—— 会冲掉需求 B 已改好的文案；
- 文件是 CRLF；
- 改完必须跑 node tools/qa/escheck_es2017.js，输出必须是 DONE 0（当前是 DONE 3）。
这一步不做完，不要部署。

【工作方式 · 用户明确要求，最高优先级】
命令/脚本各跑一次即判定，不要反复回跑、不要多轮回读自审。发现问题直接给结论 + 建议，
不要为了"再确认一下"多跑几轮。
```

---

## 【版本 B】精简版（只想让它先看懂项目）

```
读 D:\下载的文件\学习工作台\交接文档-20260916c-第二批执行现状与对接.md，
严格按里面的「铁律」和「未完成工作」章节执行。重点三条：
① 唯一可写代码树是 D:\下载的文件\学习工作台，不要碰 C:\Users\ATM\WorkBuddy\Worktrees\...
② 老 WebView 内核 Chrome 50~58 上限 ES2017，禁 ?. ?? .replaceAll( {…obj} 等语法
③ app.js / api.js / common.css 是共享文件，同一时刻只允许一条线改
先读完文档，然后告诉我你打算按什么顺序做。
```

---

## 【版本 C】如果那个 AI 访问不到本地文件

网页版聊天 AI 只能在对话里看内容，**必须把文档内容一起贴过去**。顺序：

1. **先贴** `交接文档-20260916c-第二批执行现状与对接.md` 的全文（35 KB，最重要）
2. 再贴 `AI模型测试报告与修复需求-20260916.md` 和 `需求-首页AI小助手重构-20260916.md`、`需求-AI页与首页等4项修复-20260916.md`（按要做的需求挑）
3. 然后发这句：

```
以上是星途项目的对接文档和需求原文。请先通读，然后：
① 复述你理解的三条最关键的铁律；
② 列出交接文档里标记为「未完成」的项，按你建议的执行顺序排一下；
③ 指出文档里任何你认为矛盾、含糊或信息不足的地方。
先不要写代码。
```

**注意**：这种情况下它**不能真正改代码**，只能产出方案或补丁文本 —— 真正落盘还得交给能读写文件的 AI。

---

## 附：发给接手 AI 时，优先让它读的文件清单

| 优先级 | 文件（都在 `D:\下载的文件\学习工作台\`） | 用途 |
|---|---|---|
| ★★★ | `交接文档-20260916c-第二批执行现状与对接.md` | **总纲**：现状、未完成项任务书、部署流程、回滚点 |
| ★★★ | `更新日志-20260916-第二批.md` | 本批已发生的全部变更（谁改了什么） |
| ★★ | `交接文档-20260916b-第二批需求执行手册.md` | 上一份交接手册（原始需求 A~D 的要求与验收） |
| ★★ | `架构-批次九-全线改造-波次计划-20260915.md` | 批次九 23 项 7 波的依赖分析（Wave 3~7 共 10 项未开工） |
| ★ | `用户需求与决策总账.md` | 用户全部需求与拍板记录（**已拍板项照办，不要再问**） |
| ★ | `计划书-20260916-第二批执行（已归档）.md` | 本批开工前的波次编排与文件独占矩阵 |
| 按需 | `AI模型测试报告与修复需求-20260916.md` | 需求 D 依据（⚠️ 其第 61 行给出的修法本身有误） |
