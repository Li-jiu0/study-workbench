# 星途 · 更新日志（第三批续作 · 2026-09-16 14:24~17:46）

> 本文记录**第三批 429 打停之后的续作**：接手核查 → 四项 UI 需求落地 → 生产推送 → 用户反馈热修。
> 上游文档：`更新日志-20260916-第三批.md`（记录 429 打停时的落盘现场）
> 代码树：`D:\下载的文件\学习工作台` ｜ **线上版本：`20260916O`（已全站统一）**

---

## 一、总体结论（先看这五条）

| # | 事实 | 数据 |
|---|---|---|
| 1 | **线上已从 `20260916k` 连续推进到 `20260916O`** | 两次推送：`N`（四项 UI 需求）→ `O`（hero 标签热修） |
| 2 | **用户 5 条「只记录不执行」需求，4 条已落地上线** | 详见 §三；剩 1 条（导入题库跳转）仍纯记录 |
| 3 | **修复了上游遗留的生产行尾事故** | 39 个 HTML 曾被静默改成 LF，本次随 `N` 推回 CRLF，线上实测 `loneLF=0` |
| 4 | **全站 `escheck` 仍为 `DONE 0`** | 无解析级破损 |
| 5 | **本批全部改动已验证并上线** | 每项均有真实 jsdom 断言 + 生产独立复核 |

> 🔴 **接手者最需要知道的一条**：线上现在是 **`20260916O`**，且**行尾已是干净的 CRLF**。
> 不要再按旧文档（说线上是 `k`）去 plan。

---

## 二、本批实际落盘的 7 个文件（mtime + md5 实测）

| 文件 | 字节 | mtime | md5 (前8) | 线 | 状态 |
|---|---|---|---|---|---|
| `assets/app.js` | 7,080,634 | 17:21:41 | `6720b119` | ① ② | ✅ 已验收 + 已上线 |
| `assets/common.css` | 123,987 | 17:06:06 | `ce5d6d21` | ② | ✅ 已验收 + 已上线 |
| `assets/company-lib.js` | 29,126 | 17:38:41 | `cc62b298` | ① ③ | ✅ 已验收 + 已上线 |
| `assets/voiceplayer.js` | 68,697 | 17:08:47 | `b93769da` | ④ | ✅ 已验收 + 已上线 |
| `万能金句库.html` | 14,094 | 17:43:03 | `6a28ec01` | ② | ✅ 已验收 + 已上线 |
| `商务礼仪.html` | 13,995 | 17:43:03 | `dbc66deb` | ② | ✅ 已验收 + 已上线 |
| `企业定向库.html` | 22,727 | 17:43:03 | `5e952221` | ① ③ | ✅ 已验收 + 已上线 |

> 注：`assets/ai-page.js`（67,866 B / 15:35）与 `assets/ai-service.js`（42,170 B / 13:49）**本批未改动**，是上一批的产物。

### 2.1 新增的部署脚本（2 个）
| 脚本 | 用途 |
|---|---|
| `tools/deploy_update_20260916n.py` | 四项 UI 需求上线；**内置 3 道安全闸** |
| `tools/deploy_update_20260916o.py` | hero 标签热修（从 n 版复制，改 STAMP/OUT + 增删除断言） |

---

## 三、四项 UI 需求落地明细

### ✅ 需求 ①：企业定向库「删除分类标签」

**用户原文**：「企业定向库红色框住的哪些文字看见没，删除掉」
**用户决策**：选「**真删掉整行**」（而非收放）

**⚠️ 本需求踩坑两轮（重要教训）**：

| 轮次 | 我删了什么 | 结果 |
|---|---|---|
| 第 1 轮（错） | `#clTabs` 容器 + `CL_renderTabs()` 渲染的胶囊行 | **用户截图反馈「怎么还在」** |
| 第 2 轮（对） | `CL_renderHero()` 里的 `.cl-hero-tags` | ✅ 红框标签消失 |

**根因**：企业定向库页面有**两个来源**渲染外观几乎一样的分类标签 ——
- `CL_renderTabs()` → `#clTabs`（旧版，已废弃但代码还在）
- `CL_renderHero()` → `.cl-hero-tags`（**用户截图里真正的那排**）

**改法（两轮合并后的最终态）**：
- `assets/company-lib.js`
  - `CL_renderHero()`：删除 tags 数组构建 + `.cl-hero-tags`/`.cl-tag` 输出
  - 删除 `CL_renderTabs()` 及其调用、`CompanyLib.toggleTabs()`、`openTabs`/`closeTabs`、`state.tabsOpen`
  - **新增** `CL_renderCatFilter()`（数据驱动，读 `CL_cats()`，首项硬编码「全部分类」=`CAT_ALL`）+ `init()` 内绑定 `change` → `self.setCat()`
  - `setCat()` 补回写 `#clCatSel` 的 value（防状态双源不同步）
- `企业定向库.html`：删 `#clTabs` 容器、`.cl-tabs`/`.cl-tab` CSS、`.cl-hero-tags`/`.cl-tag` CSS；**新增** `<select id="clCatSel" class="form-input cl-cat-sel">`

**回归补位说明**：删掉胶囊后，「用户可见的分类筛选入口」也没了（`setCat` 变成零调用死函数）→ 补了数据驱动下拉。实测 9 个 option 与 8 条数据完全对应。

**验证**：真实 jsdom 加载 HTML+lib+data
```
数据驱动分类数: 8 → ["电网","两桶油","建筑央企","金融国企","通用方法","历年笔试形式","常见题型结构","时间分配"]
option: ["__all__","电网","两桶油","建筑央企","金融国企","通用方法","历年笔试形式","常见题型结构","时间分配"]  → 一致 OK
setCat('电网'|'金融国企'|'时间分配') → 下拉 value 同步 OK
setCat('__all__') → OK, selectedIndex=0
用户 change 路径选「建筑央企」→ OK
setKw('笔试') 后分类未被清掉 → 叠加筛选 OK
hero 内 cl-hero-tags/cl-tag/电网/两桶油/… 全不出现在  → OK
```

### ✅ 需求 ②：万能金句库 + 商务礼仪「分类条收放」

**用户原文**：「这个页面红色框住的按钮做一个收放的，要不要页面缩小显示就不舒适」+「这个红色框住的也做一下收放列表选择」
**用户决策**：**默认收起**（用户明确纠正了我先前的「默认展开」假设）

**统一契约（可复用）**：
- HTML：容器加 `data-xt-collapse`；开关 `<span class="type-filter-toggle" role="button" tabindex="0">展开<span class="xt-caret">▾</span></span>`
- CSS：态用 `.is-collapsed` / `.is-open`，规则**必须以 `[data-xt-collapse]` 前缀限定**
- JS：`xtInitCollapse()` / `xtToggleCollapse(box)` / `xtSyncCollapseToggleText(box)` + 自举 IIFE
- 幂等标记：`data-xt-collapse-bound="1"`

**关键技术绕坑**：容器本身带**行内** `style="display:flex"`，外部 CSS 改不动 → 改为**隐藏子元素**：
```css
[data-xt-collapse].is-collapsed > .type-filter:not(.active) { display: none; }
```
（`.type-filter` 子元素无行内 display，规则生效）

**旁路审计**：`.type-filter` 另被 `场景话术库.html`(7) / `行测刷题.html`(3) / `申论刷题.html`(1) 使用 → 因 `[data-xt-collapse]` 前缀限定，**零波及**。

**追加：键盘可达性**（我发现的缺口，已补）
- 问题：开关是 `<span role="button" tabindex="0">` 但只挂 `onclick` → Tab 聚焦后 Enter/Space 无反应（假可达）
- 改法：`xtInitCollapse()` 内补 `b.onkeydown`，Enter / `' '` / `'Spacebar'` 三键走既有 `xtToggleCollapse()`
- 验证（真实 jsdom 派发 KeyboardEvent）：
```
初始 is-collapsed/展开▾ → Enter→is-open/收起▾ → Enter→is-collapsed/展开▾
→ ' '→is-open → ' '→is-collapsed → Spacebar→is-open
→ 按 'a' 无变化 OK → 按 'Tab' 无变化 OK → 鼠标 click 仍正常交替
```

### ✅ 需求 ③/④：听说训练（听力精听 / 口语跟读）「两级列表收放」

**用户原文**：「红色框住的所有功能做一个收放列表」
**用户决策**：**默认收起**

**文件**：`assets/voiceplayer.js`
**CSS 位置**：L327-335（在 **L308** 起的 `var c =` 累加块内，注入于 L421）
**重写**：`renderSceneBar()` L428-470；handler `__catsToggle`/`__subsToggle` L893-907

**🔴 最关键的设计约束**：`__group` / `__scene` 切换时都会**重调 `renderSceneBar()` 整块重建** innerHTML →
**收放状态绝不能只存在 DOM class 上**（一重建就丢）→ **必须存在 `S` 对象里**（`S.catsOpen` / `S.subsOpen`）。

**自动收放语义**：
- `__scene` → `subsOpen=false`（选完条目自动收起）
- `__group` → `catsOpen=false` **且 `subsOpen=true`**（选完分类自动展开条目，让用户直接能选）
- 两级互斥

**验证**（真实 jsdom 四态，修正抽取范围 308→421 后）：
```
cats=收起/subs=收起 → bar1: sum=flex caps=0/5 OK ; bar2: sum=flex caps=0/3 OK
cats=展开/subs=展开 → bar1: sum=null bare=inline-block caps=5/5 OK ; bar2 同 OK
cats=展开/subs=收起 → bar1 OK ; bar2: sum=flex caps=0/3 OK
cats=收起/subs=展开 → bar1 OK ; bar2: bare=inline-block caps=3/3 OK
总判定: 全部 OK
```

---

## 四、🔴 生产事故与修复（本批最重的教训）

### 4.1 事故：39 个 HTML 行尾被静默改成 LF

**元凶**：`tools/deploy_update_20260916m.py` 的版本戳 bump 段：
```python
s = io.open(p, encoding='utf-8', errors='ignore').read()   # 默认 newline=None → \r\n 归一化为 \n
s2, n = re.subn(r'\?v=[0-9A-Za-z_\-\.]+', '?v=' + STAMP, s)
io.open(p, 'w', encoding='utf-8', newline='').write(s2)     # newline='' 不补回 \r
```
读时 `\r\n`→`\n`、写时又不还原 → **每行少 1 字节**，39 个 HTML 全站 CRLF 变 LF，**并已推上生产**。

**证据链**：
- 39 个 HTML mtime 全部 = `16:23:08~09`，与 `frontend_20260916M.tar.gz` 同一秒
- `tools/frontend_20260916k.tar.gz`（04:08，未被污染）内 HTML = **全 CRLF/loneLF=0**；M 包内 = **全 LF**
- 扫描 40 个历史部署脚本，**只有 m 版**用 `newline=''` → 我自己的错误，非项目惯例
- `设置.html` 少了 **2,421 字节 = 正好等于其行数**

**修复**：改为纯二进制读写
```python
with open(p, 'rb') as f:
    raw = f.read()
raw2, n = re.subn(rb'\?v=[0-9A-Za-z_\-\.]+', b'?v=' + STAMP.encode('ascii'), raw)
if n:
    with open(p, 'wb') as f:
        f.write(raw2)
```
本地 39 个 HTML 已复原 CRLF；随 `N` 版推送覆盖回生产；`O` 版推送后生产实测 47 个 HTML `loneLF!=0` = **0**。

**影响定级**：**纯外观，不影响功能** —— 两版内容逐字节等价、全页有 `<meta charset="UTF-8">`、HTML5 把 `\r\n`/`\r`/`\n` 一律视为换行。唯一代价是 diff 全红。

### 4.2 新增防御：部署脚本 3 道安全闸（`deploy_update_20260916n.py` / `o.py` 已实装）

缺任何一道，出错都会「静默推上生产」。每道不通过即 `sys.exit(N)` 中止：

| 闸 | 检查 | 不过 |
|---|---|---|
| **行尾闸** | bump 后逐 HTML 验 `count(b'\n') - count(b'\r\n') == 0` | `exit(3)` |
| **新增符号闸** | 从 tar 内**读文件内容**验 7 个新符号存在 | `exit(5)` |
| **已删符号闸** | 验应删符号已归零（O 版 8 项，含 `cl-hero-tags`/`cl-tag`） | `exit(6)` |

### 4.3 版本戳策略：**不要复用已服务过的值**

`M` 已推过生产 → 复用会让**已缓存客户端不再拉新** → 新功能拿不到。故换 `N`、再换 `O`。

---

## 五、两次生产推送记录

### 5.1 推送 `20260916N`（四项 UI 需求）
| 项 | 结果 |
|---|---|
| 上传 / 解包 | `exit=0` / `EXTRACT_OK` |
| HTTP 探活 | home · AI · im · setting · 企业定向库 全 200 |
| 新增符号 | `xtInitCollapse`=2 / `onkeydown`=2 / `data-xt-collapse`=6 / `CL_renderCatFilter`=2 / `catsOpen`=9 |
| 已删符号 | `tabsOpen`=0 / `CL_renderTabs`=0 |
| 行尾 | 生产 47 HTML `loneLF!=0` = **0** |
| 版本戳 | 569 处 100% `20260916N` |

### 5.2 推送 `20260916O`（hero 标签热修）
| 项 | 结果 |
|---|---|
| HTTP 探活 | 6 页全 200 |
| `cl-hero-tags` / `cl-tag` | company-lib.js = 0 / 0 ；企业定向库.html = 0 / 0 |
| 关键内容仍在 | `企业定向库`=5 / `goXingce`=2 / `clCatSel`=3 |
| 行尾 | 47 HTML `loneLF!=0` = **0** |
| 版本戳 | 569 处 100% `20260916O` |
| 客户端真实路径 | `curl assets/company-lib.js?v=20260916O` → `cl-hero-tags`=0 |

---

## 六、本批的自我纠错记录（**供接手者避坑，请勿重犯**）

| # | 我犯的错 | 后果 | 纠正 |
|---|---|---|---|
| 1 | 部署脚本用 text 模式 bump | 🔴 **39 HTML 行尾污染上生产** | 改二进制 + 加行尾闸 |
| 2 | 任务书里写了**不存在的选择器** `#clCatFilter` | 工程师查 0 命中，拒绝臆造（做对了） | 我认错，给真名字 |
| 3 | 让工程师删 `CL_cats()` 当"死代码" | 其实它有用 | 已让其恢复（数据驱动） |
| 4 | 判断「分类筛选能力丢失」**言过其实** | 实际只丢了**用户入口**，`setCat` 逻辑完好 | 补数据驱动下拉 |
| 5 | 自己的验证脚本错了 **3 次** | 产生**假 FAIL** | 修正 `var c` 起点（227→**308**）后全绿。**教训：探针报 FAIL 先怀疑探针** |
| 6 | **删错了标签容器**（删 `#clTabs` 而非 `.cl-hero-tags`） | 用户截图反馈「怎么还在」 | 重定位后修复并重推 |
| 7 | 我的 jsdom 断言把 `CAT_ALL` 写成中文「全部分类」 | 假 FAIL | 实测 `CAT_ALL = '__all__'` |

---

## 七、仍纯记录未执行的需求（**1 条**）

| ID | 需求原文 | 状态 |
|---|---|---|
| — | 「这个更多功能里的**导入题库**按钮跳转的页面不对是个bug」 | ⏸ **仅记录，未执行**。**2026-09-16 18:04 用户补全口径**：「导入题库应该跳转到一个页面，这个页面可以通过 word，excel 等等方式导入自定义题库」。实测：入口现为**页内全屏浮层**（非页面跳转），且 `assets/importer.js` **已支持 `.docx`/`.xlsx`/`.pptx`/`.txt`/`.csv`**，差距在**入口形态与文案**（两处文案均漏报 .pptx）。已登记为 **R62**（见 `用户需求与决策总账.md` §九），与 D-3 合并排查 |

---

## 八、版本戳与遗留物

### 8.1 版本戳
| 项 | 值 |
|---|---|
| 全站 `?v=` 总数 | **569 处** |
| 分布 | **100% `20260916O`**（无杂质戳） |
| 本地 HTML 数 | **39 个** |
| ⚠️ 生产 HTML 数 | **47 个**（多出 8 个**历史遗留旧页**，不上导航、不在 bump 清单内，属正常） |

### 8.2 行尾（本批修复后全绿）
| 类型 | 文件 |
|---|---|
| **CRLF** | 39 个根 HTML、`app.js`、`common.css`、`company-lib.js`、`voiceplayer.js`、`ai-config.js` |
| **LF** | `ai-page.js`、`ai-service.js` |
| 实测 | 39 HTML + 4 核心 JS/CSS 全部 `loneLF=0` |

### 8.3 根目录遗留临时文件
**115 个 `_*` 文件**（比上批的 40 个增加，含我本批的探针脚本）。
🔴 **用户铁律：文件清理永远最后一步，现在不要删。**

### 8.4 本批我新增的探针脚本（在**工作树** `main-833f6cf3`，非交付物）
`_vp_extract2.py`、`_vp_real3.js`、`_final_audit.py`、`_fp_check.py`、`_verify_13_14.py`、`_cl_verify.js`、`_cl_verify2.js`、`_a11y_verify.js`、`_hero_verify.js`、`_prod_pre.py`、`_prod_post.py`、`_prod_post2.py`、`_ship_check.py`

---

## 九、ES2017 终审（本批 7 个文件）

| 文件 | 结果 |
|---|---|
| `common.css` / `voiceplayer.js` / `company-lib.js` / 3 个 HTML | **CLEAN** |
| `app.js` | `**`×75 → **全部误报**（66 个 `/**` JSDoc 起始符 + 9 个 Markdown 加粗串） |
| `company-lib.js` | `**`×37 → **全部误报**（JSDoc + 注释内 Markdown 加粗） |

**结论：本批无真实禁用语法命中。**

---

*生成时间：2026-09-16 17:50 · 依据：本机 mtime/md5 实测 + 真实 jsdom 断言 + 生产 plink 直连复核。非文档转述。*
