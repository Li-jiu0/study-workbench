# 交接文档 · 星途学习工作台（2026-09-13 晚 · 交接给下一个 AI）

> 写给接手的 AI：这份文档假设你从零接手，读完即可安全开工。前序文档《交接文档-批次三.md》《用户需求与决策总账.md》《星途平台改进-需求清单-状态盘点-2026-09-13.md》仍然有效，本文是其增量 + 最新快照。

---

## 一、项目速览

- **是什么**：多页 HTML 学习 Web 应用「星途学习工作台」，32 个页面 + `assets/app.js`（约 6600 行）+ FastAPI 后端（`server/`），支持多用户登录。
- **主项目路径（铁律）**：`D:\下载的文件\学习工作台`。**严禁在 worktree 目录干活**（`C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\...` 只是镜像，已合并弃用；部署脚本有 ROOT 校验会拒绝 worktree 路径）。
- **线上状态（2026-09-13 20:00 快照）**：
  - 线上版本 **`20260913m`**，本地 commit `9c270e8`（m 批），已部署成功、探针全过。
  - USERS=7（基线 6，新增 1 正常）；服务 active；FEEDBACK_POST=200；TRACEBACK=0。
  - **服务器**：110.42.134.62，远端根 `/opt/study-workbench`，服务名 `study-workbench`。
- **Git/GitHub**：远端 `origin = https://github.com/Li-jiu0/study-workbench.git`，**已全部推送**：`34d3ea2..9c270e8 -> main`（l 批 + m 批均已上远端，2026-09-13 20:35 补推完成）。
- **git 代理坑（定案）**：会话环境变量 `HTTP_PROXY=127.0.0.1:57065` 的上游是死的（502）；本机真正可用的代理端口是 **7897**。git 联网操作一律显式指定：`git -c http.proxy=http://127.0.0.1:7897 push origin HEAD`。

## 二、铁律（违反 = 事故，接手前先背下来）

1. **APK 永久不打包**（用户拍板，除非用户主动提出，不得再问）。安卓端不追新前端。
2. **不删任何现有功能、保留原有 DOM**——例外仅限用户明确拍板的删除（如本日的「考试指南菜单」「无领导小组讨论菜单」）。
3. **词库/题库/文案内容全部自撰**，不得抄袭外部内容（版权红线）。
4. **未经用户明确要求不 push GitHub**。
5. **生产操作（删数据/改 nginx/重启服务/部署上线）需要用户点头**；其余改动按「按你的意见排序执行，中间不用再问」自主推进。
6. **`assets/icon-map.js` 单写者规则**：多线并行时只允许主理人（或指定唯一工程师）改它，其他成员只能引用。
7. **并行子代理改共享大文件（app.js 等）前必须重新 Read 盘上最新版，只做精确 Edit**（本日多次踩坑：并行 Edit 同一文件互相覆盖回滚）。

## 三、标准工作流（每批改动照此走，一步不省）

```
改（可多线并行，文件零重叠）
→ 单页/专项静态自检（node 断言脚本，日志落 tools/qa/*.log）
→ 版本戳 bump（?v=YYYYMMDDx 全站统一替换，改了 assets 必 bump）
→ 回归门：node tools/qa/qa_batch_0913h_final.js --with-c1   （exit 0 = 过）
→ 图标扫描：node tools/qa/icon_audit_0913j.js --strict      （残留=0、引用全注册）
→ 备用：node tools/verify_p0b_all.js                        （76 断言）
→ git add -A && git commit -F <UTF8 message 文件>
→ SW_DRY_RUN=1 python tools/deploy_update_20260913m.py      （dry-run，不联网）
→ python tools/deploy_update_20260913m.py                   （正式：上传+MD5 90/90+DB 备份+重启+探针）
→ git push origin HEAD（需用户要求或授权）
```

**部署脚本要点**（以 `tools/deploy_update_20260913m.py` 为模板，新批次复制后全局替换版本串）：
- 打包 90 文件（31 页面排除 `blog_wechat.html` + assets + server 2 文件），ASCII 包名 `deploy_20260913m.tar.gz` 中转。
- 远端全量 MD5 校验、DB 自动备份、`.env` 幂等追加（严禁覆盖整个 .env）。
- `FEEDBACK_ADMIN_KEY=xj20260913j-f7a3c921b6d84e05`（查看反馈：SSH `cat /opt/study-workbench/server/data/feedback.json`）。
- 探针：SITE/HEALTH 200、MD5、USERS（基线 6，>6 为正常增长）、TRACEBACK_10MIN=0、FEEDBACK_POST=200。新批次记得把探针版本串（CET_VER_K/HOME_VER_K 命名可顺延）和 manifest 名换成新戳。
- 凭据是惰性读取（正式部署时才需要），dry-run 不联网。

## 四、图标体系（全站规范）

- 写法：`<span class="nav-icon" data-icon="名称" data-icon-size="14"></span>`，由 `assets/icon-map.js` 渲染（lucide 风格内联 SVG，现注册 **96 个**；只准 path/rect/circle 图元，禁 mask/filter/symbol/use）。
- 动态 innerHTML 插入含 data-icon 的 DOM 后，**必须补调 `window.lucideAutoRender && window.lucideAutoRender()`**（参考 `assets/study-stats.js`、app.js renderLayouts 的既有做法）。
- 扫描器白名单（不判残留）：placeholder 属性文本、select/option 选项文本、聊天/AI 回复正文、用户头像字段、内容型 badge、🌙 主题按钮文案。
- 新页面/新图标缺注册时：查 `icon-map.js` → 没有就按 lucide 官方 SVG 补注册（`node --check` 验证）。

## 五、本机环境坑（都是实锤，别再试错）

- **bash 缺 coreutils**：无 `ls/tail/grep/sleep/dirname`。读写一律用文件工具（Read/Edit/Grep/Glob）+ node/python；长命令输出**重定向到 ASCII 名文件再 Read**。
- **PowerShell 中文路径不可靠**（输出吞/假失败）→ 用 managed python：`C:\Users\ATM\.workbuddy\binaries\python\versions\3.13.12\python.exe`。
- **git**：中文路径用 `git add -A`；commit message 走 `-F <UTF8文件>`（命令行内联中文易乱码）；长 git 操作后台跑。
- **并行 Edit 同一文件必然互相覆盖**：要么文件零重叠分区，要么串行；每次 Edit 前重读盘上最新版。
- 首页等页面的「学习概况」是 `assets/study-stats.js` 动态渲染的（五页共用）。

## 六、今日（2026-09-13）已完成的三批，供追溯

| 批次 | commit | 内容 |
|---|---|---|
| k（已推远端 34d3ea2） | 34d3ea2 | K1-K17 大批：弹窗→页面化、图标全站升级、评论嵌套/折叠、服务器 /api/feedback 上线 |
| l（**未推**） | a9d9ff0 | 删商务礼仪面试「无领导小组讨论」菜单卡；设置页 🧠→brain、🚀→rocket；voiceplayer.js 听说训练双 tab 选中态修复（K19） |
| m（**未推**） | 9c270e8 | 工具页/PPT版式库 emoji 全页升级；`.gs-input` 搜索框内联 SVG 放大镜（common.css 一处覆盖全站，placeholder 摘 🔍）；四级备考+央国企笔试「考试指南」卡删除、内容并入 `mock_exam.html` 双 tab（`switchMockTab`）；首页（学习工作台.html）任务卡「＋」自定义任务（localStorage 键 `xt_custom_tasks_v1`，按账号隔离）+完成勾选+✕删除；各模块进度卡两列网格重构；id=8 图形推理题干禁词「位置」→「号位」（app.js + exam-bank.json 双源）；旧 QA 断言归档（verify_p0b_all.js / j_kou_exam_check.js） |

## 七、待办与已知遗留（按优先级）

1. ~~push 两个本地 commit~~ ✅ 已完成（2026-09-13 20:35 经 7897 端口补推，远端已到 `9c270e8`）。
2. **云贴评论嵌套需 server 配合**：api.js 渲染已支持 replyTo 嵌套，但列表接口需返回 `parentId/replyTo` 字段，嵌套才能真正生效（当前只有本地贴生效）。
3. **`assets/mini.js` 有 mz-inline 死代码**（低优，不影响功能）。
4. **PPT案例拆解.html 的 PPT_CASES 卡片图标仍是 emoji**（🏢🍎🎓，渲染目标是 #caseList）——m 批未越权处理，用户提了再改。
5. 需求总账：30 条需求 20✅/3🟡/6❌，明细见《星途平台改进-需求清单-状态盘点-2026-09-13.md》。
6. 部署探针变量名（CET_VER_K/HOME_VER_K）只是历史命名，不影响功能，新批次可顺延改字母。

## 八、QA 工具索引（都在主项目 `tools/` 下）

| 命令 | 作用 |
|---|---|
| `node tools/qa/qa_batch_0913h_final.js --with-c1` | 回归门（7 个子脚本 + 硬断言防覆盖），exit 0 为过 |
| `node tools/qa/icon_audit_0913j.js --strict` | 全站图标盘库：图标容器 emoji 残留=0、data-icon 引用 100% 已注册 |
| `node tools/verify_p0b_all.js` | P0-B 专项 76 断言（含 q21 图形推理禁词扫描） |
| `python tools/deploy_update_20260913m.py`（SW_DRY_RUN=1 可 dry） | 部署 |
| `tools/qa/_deploy_0913m_manifest.txt` | 最近一次部署清单（90 条 MD5） |

## 九、给接手 AI 的三句话

1. 改任何东西之前先跑一遍回归门拿绿基线；收工前再跑一遍，红了你就是事故第一责任人。
2. 用户的截图需求要「全页审计」而不是只改红框——用户多次抱怨漏改；但 placeholder/option/正文/头像里的 emoji 是白名单，别越界。
3. 拿不准的先问用户，生产操作永远等点头；其余的事「按你的意见排序执行，中间不用再问」。
