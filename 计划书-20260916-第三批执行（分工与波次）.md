# 星途 · 第三批执行计划（分工与波次）

> 成文：2026-09-16 13:30 ｜ 主理人：齐活林（software-team-lead）
> 唯一可写代码树：`D:\下载的文件\学习工作台` ⛔ 禁碰 `C:\Users\ATM\WorkBuddy\Worktrees\...`
> 团队：`software-xingtu-batch3`（已创建，成员待开工时拉入）
> 本文合并三份来源：`交接文档-20260916c`（第二批） + `架构-批次九-全线改造-波次计划-20260915` + `任务清单-待办总览-20260915`

---

## 0. 开工前实测校正表（**与旧文档冲突时以本表为准**）

本次对代码树做了一次全量核验（文件存在性 + 关键符号计数 + 版本戳分布），**旧文档有 6 处已过时**：

| # | 项 | 旧文档说法 | 本次实测 | 结论 |
|---|---|---|---|---|
| 1 | `更多.html` 白屏破损 | `escheck` DONE 3 | **DONE 0**（已定向删除 L200–L352 + 补 L141 孤儿注释） | ✅ **已关闭** |
| 2 | `blog_wechat.html:325` 畸形戳 `20260915eaaa` | 🔴 R2 高风险 | **已不存在**，该页 `?v=` 全为 `20260916L` | ✅ **R2 关闭** |
| 3 | `app.js` blast radius | 31 页 | 根 HTML **38** 个；引 `app.js` **33** 页、`api.js` **30** 页 | 独占范围按 **33** 页算 |
| 4 | 全站版本戳 | 551 处 `20260916L` | **551 处，100% 为 `20260916L`** | 一致 ✓ |
| 5 | N9-23 每日一题卡 | 未开工 | **已做**：`面测.html` `ivQuizSubmit`×6 | ✅ 关闭 |
| 6 | N9-6 更多页填充 | 未开工 | **已做**：8 张卡，序列与验收表严格一致 | ✅ 关闭 |
| 7 | N9-16 联系管理员 | 未开工 | **已做**：`私聊.html` `acEntry`×16 | ✅ 关闭 |
| 8 | N9-17 长按菜单撤回 | 未开工 | **已做**：`im-msg-menu` 已在 | ✅ 关闭 |
| 9 | N9-5 工具页填充 | W1 标杆 | ❌ `工具.html` 仅 2 张内容卡（PPT版式/四级经验） | **仍需做** |
| 10 | N9-19 模拟面试重构 | W2 | ❌ `AI模拟面试.html` `callAI`=0 | **仍需做** |
| 11 | N9-3 `notify.js` | 未开工 | ❌ `assets/notify.js` 不存在 | 未做 |
| 12 | N9-22A 学习概括页 | 未开工 | ❌ `学习概括.html` 不存在 | 未做 |
| 13 | N9-12/13/14 图片资源 | 🔴 阻塞 | ❌ `assets/images/ppt-tpl`/`ppt-cases`/`ppt-tips` **三个目录都不存在**（`data-ppt-templates.js`/`tpl-preview.js`/`ppt-tips.js` 结构层已在） | **仍阻塞** |
| 14 | **C8 `fetchAiReply` 委托 `callAI`** | 决策 D2 已拍板 | ❌ **仍直连 `APP_AI_DEMO_CONFIG.apiUrl`**（旧后端演示路径） | **未做，且是 C 组前置** |
| 15 | C10~C21 页面级 AI | — | 全部目标页 `callAI`=0，只有 `aiFab` 悬浮助手统一入口 | **基本未做** |
| 16 | `common.css` 旧名注释 | 2 处 | **3 处**「学习博客」残留 | 比文档多 1 处 |
| 17 | `recentMap` 死值 | 2 条 | 仍在：`'社区.html':'blog'`、`'面试题库.html':'iv'` | 未做 |
| 18 | `app.js`「本地演示模式」 | 7 处 | **7 处** ✓ | 一致 |

> 处置：#1、#2、#5~#8 五项**从待办中剔除**，不重复派活。

---

## 1. 本批范围（三块）

| 块 | 来源 | 项数 | 优先级 |
|---|---|---|---|
| **A** | 第二批收尾（交接文档 §4） | 6 项 | **P0**（用户已等） |
| **B** | 批次九 Wave 3~7 | 10 项 + 4 收尾 | P1 |
| **C** | 全站 AI 升级 C-P1~P3（C10~C21）+ C8 | 13 项 | P2（大波，分 3 子波） |
| **D** | 更早遗留（09-15 待办总览） | 5 项 | P2/P3，择机插空 |

---

## 2. 团队编制与分工

| 角色 | 成员名（Agent ID） | 本批职责 |
|---|---|---|
| 主理人 · 交付总监 | `team-lead` | 编排、文件独占裁决、波末 bump 单点、部署、汇编 |
| 架构师 | `software-architect` | ① N9-19 六阶段状态机交互文档 ② N9-7 app.js 拆分方案（前置产出） |
| 工程师 ×4 | `software-engineer-任务一/二/三/四` | 四条并行线主刀 |
| QA 工程师 | `software-qa-engineer` | 每条线独立验收 + 波末全量回归 + 线上复检 |

**命名纪律**：`name` 与 `subagent_type` 均传 Agent ID，禁用中文名。

---

## 3. Wave 0 · 第二批收尾（4 条线并行，文件零重叠）

| 线 | 任务 | 独占文件（**严令不可越界**） | 验收口径 |
|---|---|---|---|
| **W0-A** | 核验 `app.js` +405 行小助手半成品（**只读**）→ 修 P2 `recentMap` | `assets/app.js` | 7 项功能逐条核验；`'blog'→'plaza'`、`'iv'→'interview'` |
| **W0-B** | **需求 D**：AI 400 修复 + 降级条件 + 15s 超时 + 429 跳过 + 流式守卫 | `assets/ai-service.js`（601 行 **LF**） | 按真实图片类型推 MIME；**400 不降级**；`:350`/`:420` 两处 `getReader` 都加守卫 |
| **W0-C** | **需求 C**：输入框双槽位外观统一 + 侧栏「模型」按钮 | `AI.html`(**CRLF**) + `assets/ai-page.js`(**LF**) | 375px + 1280px 双端出图；`.ai-model-panel` `z-index` ≥300 且先关抽屉 |
| **W0-D** | N9-5 工具页填充 + P3 `common.css` 注释 | `工具.html` + `assets/common.css` | 工具页无空白；只改注释不动 CSS 属性 |

**W0 收口**：QA 独立验收 → **波末单点** bump `20260916L` → `20260916M`（551 处 / 38 HTML）→ `SW_DRY_RUN=1` 预演 → 部署（⚠️ 生产操作，需用户点头）→ 线上复检。

---

## 3.5 🚀 首批「13 线并行」名册（用户要求马力全开 · 已做零重叠校验）

> 排期原则：**除 3 个共享文件外全部同时开**。共享文件的**持有线**按梯队串行接力，
> 其余线一律并行 —— 13 条线两两文件集合交集为空。

| 线 | 成员（Agent ID） | 任务 | 独占文件（严令不可越界） | 来源 |
|---|---|---|---|---|
| **L1** | `software-engineer-任务一` | 核验 `app.js` +405 行小助手半成品（只读）+ 修 P2 `recentMap` | `assets/app.js` ★ | A |
| **L2** | `software-engineer-任务二` | 需求 D：400 修复 + 降级条件 + 15s 超时 + 429 跳过 + 流式守卫 | `assets/ai-service.js` | A |
| **L3** | `software-engineer-任务三` | 需求 C：输入框双槽位外观统一 + 侧栏「模型」按钮 | `AI.html` + `assets/ai-page.js` | A |
| **L4** | `software-engineer-任务四` | N9-5 工具页填充 + P3 `common.css` 注释 + D-2 L68 滚动锁 | `工具.html` + `assets/common.css` ● | A/B/D |
| **L5** | `software-engineer-任务五` | N9-11 阅读理解布局修复 | `assets/cet-read.js` | B |
| **L6** | `software-engineer-任务六` | N9-18 听力精听全面改版（含 N9-10） | `assets/voiceplayer.js` + `英语.html` | B |
| **L7** | `software-engineer-任务七` | N9-19 模拟面试页面重构 | `AI模拟面试.html` + `assets/iv-prep.js` | B |
| **L8** | `software-engineer-任务八` | N9-14 PPT 训练课程改进（占位图先上） | `演示.html` + `assets/ppt-tips.js` + `assets/data-ppt-tips.js` | B |
| **L9** | `software-engineer-任务九` | N9-12 PPT 版式库升级（结构层 + 占位图） | `PPT版式库.html` + `assets/data-ppt-templates.js` + `assets/tpl-preview.js` | B |
| **L10** | `software-engineer-任务十` | C12 申论批改（AI 底座接线） | `申论刷题.html` | C |
| **L11** | `software-engineer-任务十一` | C18 错题本 AI 分析 | `错题本.html` | C |
| **L12** | `software-engineer-任务十二` | N9-4 个人中心作品集 + B1 feed-btn 挪位 | `个人中心.html` | B/D |
| **L13** | `software-engineer-任务十三` | N9-22A 新建 `学习概括.html` + N9-3 编写 `assets/notify.js`（注入留 W4） | 新建两文件 | B |

**零重叠校验**：13 组文件集合两两交集为空 ✓（`app.js` / `common.css` 各只有一条线持有）
**共享文件接力链**（首批之后按序开工，每段独占完成再交棒）：
`app.js`：**L1 → D-1 数量关系 → N9-1+2 → N9-24 → N9-13 → N9-22B → C8 → C20 → N9-7(split)**
`api.js`：**B2 资料卡重构 → N9-21 写发帖**
`common.css`：**L4 → N9-15**
`新页/独立页（后梯队）**：`万能金句库.html`+`表达.html`(N9-24/C16/C17)、`社区.html`+`blog_wechat.html`(N9-21)、`更多.html`(N9-22A 尾)、`行测刷题.html`(N9-8)

**⚠️ 429 防护**：上一批 **6 名成员全部被 429 打停**。首批 13 条**分两拨 spawn**（7 + 6），
观察无 429 再补第二拨；任一成员报 429 时，**当场以 `model: "lite"` 重拉该线**（UI 切模型对子代理无效，只能在 spawn 时指定）。


---

## 4. Wave 1~3 · 批次九共享文件波（Wave 3/4/5）

| 波 | 任务 | 独占文件 | 关键约束 |
|---|---|---|---|
| **W1** | N9-1+N9-2 题库筛选修复 | `assets/app.js` + `行测刷题.html` | app.js 本波独占 |
| | N9-15 我的文章布局 | `assets/common.css` + `blog_wechat.html` | 开工先解 F3（同名函数双份） |
| | N9-22A 学习概括页 | 新建 `学习概括.html` + `个人中心.html` + `更多.html` | 只做单文件段，28 页批量留 W4 |
| **W2** | N9-24 金句库升级 | `assets/app.js` + `万能金句库.html` + `表达.html` | app.js 本波独占；XHR 单路径，禁 fetch |
| | N9-12 PPT版式库 | `PPT版式库.html` + `data-ppt-templates.js` + `tpl-preview.js` | 🔴 占位图先上 |
| | N9-21 写发帖改进 | `社区.html` + `blog_wechat.html` + `assets/api.js` | 先解 F3 |
| **W3** | N9-13 PPT案例拆解 | `assets/app.js` + 新建 `data-ppt-cases.js` | app.js 本波独占；🔴 截图未到位先占位 |
| | N9-4 个人中心作品集 | `个人中心.html`（限页内） | 不碰 `renderProfileOverview` |
| | N9-8 筛选标签交互 | `行测刷题.html` + 其它筛选页 | 与 W1 跨波串行 |

---

## 5. Wave 4 · 全站 HTML 原子批量波（🔴 最高风险）

三件事**必须合并为一次原子批量**（否则全站被改三遍、后写覆盖先写）：

| 子任务 | 范围 | 文件边界 |
|---|---|---|
| W4-T0 | 批量脚本（先行，含三变换开关） | 仅 `tools/`，不改 HTML |
| W4-T1 | 第 1 批页面（约 18 页） | 独占页面清单，跑全部三变换 |
| W4-T2 | 第 2 批页面（其余约 18 页） | 同上，**与 T1 交集必须为空** |
| W4-T3 | N9-22B 收口：`app.js` `openBlogStats()` 改重定向 | 必须在 T1/T2 全完成后；**函数保留不删** |

- 三变换 = ① 全站页头返回（33 页）② 删底部导航「学习统计」（28 页）③ 注入 `assets/notify.js`（30+ 页）
- **豁免页**：`mock_exam_run.html`、`登录.html`
- 先 `SW_DRY_RUN=1` 出清单 → 人工过目 → tar 备份 → 执行 → 36 页全量实测 → 一次 bump → 一次部署

---

## 6. Wave 5 · 收尾波（串行）

1. **架构师先出拆分方案** → N9-7 `app.js` 拆分（硬约束：全局符号名不变、`INLINE_*_BANK` 仍在 `examInit` 之前、零现代语法、33 页 script 标签同步）
2. 统一 bump + 全项目禁用语法 Grep（`?.` `??` `replaceAll` `Object.fromEntries` `.at(` 顶层 await `(?<=` `(?<!` → 必须 0）
3. 部署 + 线上复检（`QA_ORIGIN=http://110.42.134.62`）
4. 文件清理（**用户铁律：最后一步**；`tools/` 下真实工具不删）

---

## 7. Wave 6 · 全站 AI 升级 C-P1~P3（大波，13 项）

> **前置**：C8 —— `app.js` 的 `fetchAiReply` 必须改为内部委托 `window.callAI`（保留签名）。实测**仍直连旧后端 `APP_AI_DEMO_CONFIG.apiUrl`**，不做这步后面全是两套底座。

| 子波 | 任务 | 目标页 |
|---|---|---|
| **C-P1 核心学习** | C10 面试真升级 | `AI模拟面试.html`（含 N9-19 重构）、`面测.html`、`面试题库.html` |
| | C11 行测/AI 讲解 | `行测刷题.html`、`央国企笔试`→`行测.html` |
| | C12 申论批改 | `申论刷题.html` |
| **C-P2 内容生产** | C13 四级/词汇 | `英语.html` |
| | C14 PPT 系列 | `演示.html`、`PPT版式库.html` |
| | C15 发帖辅助 | `社区.html`、`blog_wechat.html` |
| | C16 金句库生成 | `万能金句库.html` |
| | C17 高情商/话术 | `表达.html`、`商务礼仪.html` |
| **C-P3 分析增强** | C18 错题本 AI 分析 | `错题本.html` |
| | C19 学习数据诊断 | `学习工作台.html`、`个人中心.html` |
| | C20 悬浮助手上下文感知 | `assets/app.js` |
| | C21 拍题贯穿 | `assets/ai-service.js` + 各讲题入口 |

**准入硬门槛（决策 D8）**：不填 Key 返回真实模型内容 + 断网降级不白屏 + 结果就地渲染 → 真机点一遍才进下一子波。

---

## 8. Wave 7 · 更早遗留（择机插空）

| ID | 任务 | 文件 | 优先级 |
|---|---|---|---|
| B1 | 「我的动态」feed-btn 挪进资料卡操作区 | `个人中心.html`（页内） | P1 |
| B2 | 个人资料卡布局重构 | `assets/api.js` `_renderProfileOnline` | P1 |
| D-1 | 数量关系题库回滚重做 | `assets/app.js` + 题库数据 | **P0 遗留** |
| D-2 | `common.css` L68 全局滚动锁 | `assets/common.css` | P1 |
| D-3 | 「工具页导入题库跑到博客页」悬案 | — | ⏸ **等用户补截图，不空转** |

---

## 9. 全局风险与跨波约定

| # | 风险 | 缓解 |
|---|---|---|
| R1 | 版本戳 bump 重写 38 HTML（隐藏全局锁） | 波内**一律不 bump**，波末单点执行 |
| R2 | `app.js` 五连独占（关键路径） | N9-1+2 / N9-24 / N9-13 / N9-22B / N9-7 **严格串行**，靠波内其它线并行摊薄 |
| R3 | F3 同名函数双份（`app.js` vs `api.js`：`renderBlogStats`/`renderBlogMine`/`openBlogStats`） | N9-15 / N9-21 / N9-22 **开工第一步先判生效副本**，改死副本 = 白做且不报错 |
| R4 | W4 全站批量改 36 HTML | 合并原子批量 + dry-run 清单 + tar 备份 + 一次回归 |
| R5 | 老 WebView 语法（`node --check` **抓不到**） | 每波跑 `escheck_es2017.js` + Grep 禁用语法 = 0 |
| R6 | ADR-3 回归 | 每波 Grep 原生 `alert/confirm/prompt` = 0；保留原 DOM id 与函数名 |
| R7 | 并行 Agent 抢文件 | 派活时**显式列出独占文件清单** |

---

## 10. 需要用户拍板（3 件）

1. **开工范围**：先只做 Wave 0（第二批收尾 4 条线），还是 Wave 0 + W1 一起上？（W1 会开始动 `app.js`，风险上一个台阶）
2. **部署策略**：Wave 0 完成即部署，还是攒到 W1 再一起上？（线上目前仍是完好的 `20260916k`）
3. **D-1 数量关系题库回滚重做** 排期：插在 Wave 0 之后，还是等 C 组？（它是标注的 P0 遗留）

---

*主理人齐活林 · 2026-09-16 13:30。校正表数据均为本次对 `D:\下载的文件\学习工作台` 的实测（文件存在性 / 关键符号计数 / 版本戳分布），非文档转述。*
