# 交付报告 · A3 — N9-11 阅读理解布局修复 · 复核与验证收尾

- **任务编号**：A3（N9-11「三种题型在 `.cr-grid` 布局下右栏不再出现空卡片」）
- **结论**：**DONE — 零改动**。上一批落盘的改动经真实页面交互实测**已彻底修好**，无需再改。
- **验证日期**：2026-09-16
- **独占文件**：`D:\下载的文件\学习工作台\assets\cet-read.js`（35,497 B，纯 LF，mtime 2026-09-16 13:49:54）

---

## 一、根因分析

### 1.1 症状
三种题型（选词填空 cloze / 长篇匹配 match / 仔细阅读 careful）共用 `.cr-grid` 两栏布局，
在**题量为 0 / 字段缺失 / 自测组件未加载**时，右栏 `.cr-col-quiz` 只剩一块**只带头部的中空卡片**
（`.cr-quiz-card` 内仅有 `.cr-card-h`「答题卡 · 共 0 空」+ 一个空的 `.cr-card-nums`），
视觉上就是一块空白卡片。宽屏（`min-width:900px`）下 `.cr-grid` 强制两列，空白更显眼。

### 1.2 三条根因链
| # | 根因 | 机制 |
|---|---|---|
| R1 | `cardHtml(total)` 在 `total === 0` 时仍输出完整答题卡骨架 | 头部「答题卡 · 共 0 空」+ 空题号行 + 空进度条 → 中空卡片 |
| R2 | `renderCareful` 无条件调 `XTC.renderQuiz` | `XTC.renderQuiz` 在 `qs` 为空时内部执行 `slotEl.innerHTML = ''`（见 `xt-content.js`），右栏随之清空 |
| R3 | 三个渲染器直接取 `p.blanks.length` / `p.stats.length` / `p.paras.length` | 数据缺字段时抛 `TypeError`，**整份视图渲染中断**（宿主页连左侧文章一起丢，等同白屏） |

---

## 二、改动判定（行号证据）

上一批的改动**确实**针对上述三条根因，逐条落地：

### ① `cardHtml` 零题量空状态分支 — 修 R1
`assets/cet-read.js` **L257–L277**：

```js
function cardHtml(total, unit, guide) {
  var n = parseInt(total, 10);
  if (isNaN(n) || n < 0) n = 0;
  /* ★ n===0 时必须给「可见内容」… 这里直接渲染明确的空状态文案 */
  if (n === 0) {                                            // ← L263 新增分支
    return '<div class="cr-card-h">' + icon('check-circle', 15) + '答题卡</div>' +
      '<div class="cr-empty">本篇暂无题目数据，请先阅读左侧文章要点。</div>';   // L265
  }
  ...
}
```
> **证据**：零题量分支含 `.cr-empty` 可见空状态 = **true**；仍为中空卡片 = **false**（结构性等价断言，见 §3.4）。
> `.cr-empty` 样式定义在 **L146**：`padding:30px 0;text-align:center;…` → 明确可见文本，非空白。

### ② `renderCareful` 判空占位，绕过 `XTC.renderQuiz` 清空 — 修 R2
`assets/cet-read.js` **L556–L565**：
```js
/* ★ XTC.renderQuiz 在 quiz 为空时会执行 slotEl.innerHTML = ''（见 xt-content.js），
   此时右栏只剩答题卡一块，视觉上接近空卡片；这里先判空并显式占位。 */
var qHost = box.querySelector('#crQuizHost');
if (!qs.length) {                                                    // L559
  if (qHost) qHost.innerHTML = '<div class="cr-empty">本篇暂无题目，请先阅读左侧文章。</div>';  // L560
} else if (window.XTC && typeof window.XTC.renderQuiz === 'function') {
  window.XTC.renderQuiz(qHost, qs, opts);                            // L562
} else if (qHost) {
  qHost.innerHTML = '<div class="cr-empty">自测组件未加载（assets/xt-content.js 缺失）</div>';  // L564
}
```

### ③ 三个渲染器数据兜底 — 修 R3
| 渲染器 | 行号 | 兜底写法 |
|---|---|---|
| `renderCloze` | **L306–L307** | `var optsList = p.options || [];` / `var blanks = p.blanks || [];` |
| `renderMatch` | **L399–L400** | `var paras = p.paras || [];` / `var stats = p.stats || [];` |
| `renderCareful` | **L503** | `var qs = cur.qs || [];`（另 L482 对 `list` 判空） |

### ④ 三题型右栏结构补齐（`.cr-grid` 右栏 child 数对齐）
| 题型 | 左栏行 / 右栏行 | 右栏 child 数 |
|---|---|---|
| cloze | L330–L342 | `#crCard` + `#crRes` = **2** |
| match | L420–L432 | `#crCard` + `.cr-stats` 卡 + `#crRes` = **3** |
| careful | L509–L524 | `#crCard` + `#crQuizHost` + `#crQuizNote` = **3** |

> 与实测完全一致（见 §3.2：cloze rightChildren=2 / match=3 / careful=3）。

---

## 三、三题型实测结果

### 3.0 验证环境（关键：服务来源可信）
```
# 数据源 ORIGIN = http://127.0.0.1:8918
# 来源         = 本脚本自启的本地静态服务（端口 8918）   ← ✅ 非复用他人手工服务
# 是否本地     = 是（验的是本地工作区代码）
```
> 首次用 `QA_PORT=8911` 时命中噪声陷阱：banner 报「**复用已存在的本地静态服务（端口 8911）**」，
> 且关键内容多为 false。改到无占用的 **8917/8918/8919/8920** 后 banner 显示「本脚本自启」，
> 结果才可信。**结论：报告只采信自启服务的运行结果。**

脚本加载自检（19 个带 src 脚本，版本戳 `?v=20260916L`）：
```
含 cet-read.js    = assets/data-cet-read.js?v=20260916L | assets/cet-read.js?v=20260916L
含 xt-content.js  = assets/xt-content.js?v=20260916L
typeof XTC = object   typeof CETV2['cet-read'] = function   （真加载成功，非 404 噪声）
```

### 3.1 素材来源
`英语.html` → `XTC.registerView('CETV2','cet-read')` 注册的视图渲染器被真实调用，
tab 容器 `#crTabs` 渲染出 **4 个** `.xt-tab[data-xt-tab]`：`cloze / match / careful / tips`。

### 3.2 三题型切换实测（真实点击 + active 状态断言，防假 PASS）
| 题型 | tab 点击 | active 切换 | `#crBody` 题型特征 | 右栏 child | 右栏字符数 | 卡片数 | **空卡片数** | 判定 |
|---|---|---|---|---|---|---|---|---|
| 选词填空 | ✅ | `cloze → cloze` | `cloze`（含 `.cr-blank`） | 2 | 69 | 1 | **0** | ✅ PASS |
| 长篇匹配 | ✅ | `cloze → match` | `match`（含 `.cr-sel`） | 3 | 1069 | 2 | **0** | ✅ PASS |
| 仔细阅读 | ✅ | `match → careful` | `careful`（含 `.cr-col-quiz`） | 3 | 1109 | 1 | **0** | ✅ PASS |

原始输出摘录：
```
── 长篇匹配（tab id=match，clicked=true，active: cloze → match）──
      #crBody 题型特征 = match
  [长篇匹配] grids=1
       grid#0 cols=2 rightExists=true rightChildren=3 rightTextLen=1069 cards=2 emptyCards=0
         card#0{txt=65,emptyState=false,children=26} card#1{txt=1004,emptyState=false,children=151}
       判定: ✅ 右栏无空卡片

== 汇总 ==
  选词填空(cloze) : ✅ PASS（右栏无空卡片）
  长篇匹配(match) : ✅ PASS（右栏无空卡片）
  仔细阅读(careful) : ✅ PASS（右栏无空卡片）
```
> **断言定义**：右栏 `.cr-col-quiz` 存在（`rightExists`）且 `children > 0` 且
> 各 `.cr-quiz-card` 去空白后字符数 ≥ 6 且 `rightTextLen ≥ 6`。
> 三题型 `cols=2`（确认走的是两栏 `.cr-grid`，非窄屏单列退化）。

### 3.3 附加回归：仔细阅读子 tab 第1篇 → 第2篇
```
== 仔细阅读子 tab 切换（第1篇 → 第2篇）==
  子 tab 数 = 2
  点击 id=r4 → active=r4
  [仔细阅读·第2篇] grids=1
       grid#0 cols=2 rightExists=true rightChildren=3 rightTextLen=1078 cards=1 emptyCards=0
       判定: ✅ 右栏无空卡片
```
> `renderCareful` 重绘路径（L497 的 `XTC.tabs` 回调 → `renderCareful(host)`）同样无空卡片。

### 3.4 附加回归：零题量路径（直接对应 N9-11 根因）
```
零题量分支含 .cr-empty 可见空状态 = true
零题量分支仍是「只带头部的中空卡片」 = false
判定: ✅ 已消除中空卡片
```
源码实摘（`if (n === 0) {` 分支）：
```js
return '<div class="cr-card-h">' + icon('check-circle', 15) + '答题卡</div>' +
       '<div class="cr-empty">本篇暂无题目数据，请先阅读左侧文章要点。</div>';
```

### 3.5 运行时错误
```
JSDOM_ERR: Not implemented: navigation to another Document
```
> 该条是 **jsdom 对 `<a href>` 跳转的固有限制**，与本次改动无关（页面无真实脚本异常）。
> 页面文本长度正常（slot text len = 3742），无 `[CETV2] 渲染失败` 回退日志。

---

## 四、是否改动

**零改动。** 严格遵守「最小变更原则 / 不要为了改而改」。

| 文件 | 改动前 | 改动后 | mtime |
|---|---|---|---|
| `assets/cet-read.js` | **35,497 B** | **35,497 B**（未动） | 2026-09-16 13:49:54（未变） |

本次仅**新增只读 QA 探针**（不触碰站点代码树，不影响线上）：
- `tools/qa/a3_cetread_probe.js`（新增，三题型切换 + 断言）
- `tools/qa/a3_empty_probe.js`（新增，零题量分支等价断言）
- `tools/_a3_probe_out.txt` / `_a3_probe_out2.txt` / `_a3_empty_out.txt` / `_a3_escheck.log.txt`（输出）

> ⚠️ 注：`assets/` 下**不存在** `cet-read.js` 的 `.bak` 备份（仅 `cet-read.js` 一个文件），
> 说明上一批落盘时未留 bak（与铁律 8 不符）。**本次未改动，故不补**；
> 建议后续若要再动此文件，先补 `cet-read.js.bak-before-xxx`。

---

## 五、完工自检清单

| # | 检查项 | 命令 / 方法 | 结果 |
|---|---|---|---|
| 1 | `node --check` | `node --check assets/cet-read.js` | **rc=0** ✅ |
| 2 | ES2017 扫描 | `node tools/qa/escheck_es2017.js` | **`DONE 0`** ✅ |
| 3 | 禁用语法 Grep | `?.\|\?\?\|.replaceAll(\|Object.fromEntries\|.at(\|{...\|catch{\|(?<=\|(?<!` | **0 命中** ✅ |
| 4 | 原生弹窗 Grep | `alert(\|confirm(\|prompt(` | **0 命中** ✅ |
| 5 | 换行符 | `open(p,'rb')` 计数 | **CRLF=0 / 裸LF=703** → 纯 LF ✅ |
| 6 | 三题型切换实测 | `QA_PORT=8918 node tools/qa/a3_cetread_probe.js 英语.html` | **三题型右栏 emptyCards=0** ✅ |
| 7 | 服务来源可信 | banner | 「**本脚本自启的本地静态服务（端口 8918）**」✅ |
| 8 | 全局名冲突 | 顶层声明扫描 | 仅 IIFE 内 `VIEW_ID/KEY/STYLE_ID/LETTERS/state/timer/hooks/CSS/barEl`，**无全局污染**；`window` 仅挂 `CETV2/CET_READ_VIEW` ✅ |
| 9 | ES5 风格保守度 | 计数 | 箭头函数 **0**、模板字符串 **0** ✅ |

---

## 六、遗留问题 / 需裁决

1. **无功能性遗留缺陷**。N9-11 目标（三题型右栏无空卡片）**已达成且经真实交互验证**。
2. **待裁决 A｜缺 bak 备份**：`assets/cet-read.js` 无 `.bak`，违反铁律 8。本次零改动故未补。
   是否要求补一份 `cet-read.js.bak-20260916` 作为基线快照？
3. **待裁决 B｜版本戳**：文件头 `版本戳：20260915b`，但脚本引用 URL 为 `?v=20260916L`。
   属上一批遗留（波内不 bump，铁律 4）。**本次不动**。是否需下一波统一口径？
4. **观测｜空题量分支未被真实数据触发**：真实数据 4 篇 30 题，零题量分支只在数据缺失时生效，
   本次以「结构性等价断言 + 源码实摘」验证（§3.4）。若需运行期强证，需另置一份零题量
   `data-cet-read.js` 测试副本（超出本任务独占范围，未做）。
5. **观测｜`tips` tab 未纳入断言**：A3 目标只含三题型，`技巧速览` 不在范围内，未做空卡片断言。

---

## 附：复跑命令
```bash
# 语法 / 规范
node --check "D:/下载的文件/学习工作台/assets/cet-read.js"
node "D:/下载的文件/学习工作台/tools/qa/escheck_es2017.js"

# 三题型切换实测（必须用未被占用的端口，逼脚本自启服务）
QA_PORT=8918 node "D:/下载的文件/学习工作台/tools/qa/a3_cetread_probe.js" \
  "英语.html" "D:/下载的文件/学习工作台/tools/_a3_probe_out.txt"

# 零题量分支等价断言
QA_PORT=8920 node "D:/下载的文件/学习工作台/tools/qa/a3_empty_probe.js"
```
