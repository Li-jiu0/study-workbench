=== lat/lng 作为【导出字段】的所有位置（lat: / lng: 键） ===
L522   once({ ok: true, lat: c.latitude, lng: c.longitude });
L711   lat: r.lat,
L712   lng: r.lng,
L735   stepReverse({ ok: true, lat: o.testLat, lng: o.testLng });

=== lat/lng 作为【右侧值】传给函数的所有位置 ===
L478   function reverseGeocode(lat, lng, cb) {
L494   function locate(cb) {
L554   function nearby(lat, lng, cb) {
L557   reverseGeocode(lat, lng, function (g) {
L726   reverseGeocode(r.lat, r.lng, function (g) {
L740   locate(function (r) {
L1028  locate(function (r) {
L1032  reverseGeocode(r.lat, r.lng, function (g) {

=== JSON.stringify 全部命中 ===
L637   try { ls.setItem(LAST_KEY, JSON.stringify(loc)); } catch (e) {}
L809   try { ls.setItem(RECENT_KEY, JSON.stringify(out)); } catch (e) {}

=== textContent / innerHTML 赋值点里是否含坐标变量 ===
L888   style.textContent = STYLE;
L909   body.innerHTML = html;
L926   curlineEl.textContent = chosen;
L929   curlineEl.textContent = sub || '尚未选择位置';
L952   if (!hits.length) { listEl.innerHTML = '<div class="xtlp-empty">没找到「' + _escAttr(kw) + '」，换个关键词试试</div>'; return; }
L958   listEl.innerHTML = html2;
L981   listEl.innerHTML = html2;