# 星途学习工作台 UI 只读排查报告（2026-09-15）

## 一、滚动锁覆盖复查

**结论：通过。** 7 个已知页面均仍在页内覆盖 `html,body { overflow-y: auto }`，全部命中。

| 文件 | 行号 | 命中内容 |
|---|---|---|
| mock_exam.html | 28 | `html, body { height: auto; overflow-x: hidden; overflow-y: auto; ... }` |
| mock_exam_run.html | 28 | `body { height: auto; overflow-x: hidden; overflow-y: auto; ... }` |
| mock_exam_result.html | 25 | `body { height: auto; overflow-x: hidden; overflow-y: auto; ... }` |
| AI模拟面试.html | 11 | `html, body { height: auto; ... overflow-y: auto; ... }` |
| 学途.html | 12 | `body { height: auto; overflow-x: hidden; overflow-y: auto; ... }` |
| 好友申请.html | 11 | `body { height: auto; overflow-x: hidden; overflow-y: auto; ... }` |
| 登录.html | 21 | `html,body { height: auto; min-height: 100%; overflow-y: auto }` |

**新页疑似风险：** 重点检查的 9 个新页均使用统一的 `.app > .main > .page`（或 `.content`）外壳，
`common.css:445` `.main > .page { overflow-y: auto }` / `common.css:435` `.content { overflow-y: auto }` 已承载滚动，
无需在 `html/body` 再写覆盖：

- 时政热点.html、申论刷题.html、企业定向库.html、面试题库.html、万能金句库.html、PPT版式库.html、PPT案例拆解.html、场景话术库.html、四级词汇.html、行测刷题.html。

顺带检查：`PPT素材库.html`、`四级经验分享.html` 为独立样式页，未引入 `common.css`，不存在全局滚动锁。

---

## 二、ADR-3 弹窗违规复查

**结论：不通过，共 8 处违规。**

| 文件 | 行号 | 弹窗承载内容 | 违规原因 |
|---|---|---|---|
| 学习工作台.html | 307 | `#countdownModal` 添加倒计时（名称/日期/颜色/置顶） | 表单走遮罩弹窗，违反 ADR-3“表单必须走页面内布局” |
| 学习工作台.html | 341 | `#xtTaskModal` 添加自定义任务（任务名称/目标量） | 表单走遮罩弹窗 |
| 个人中心.html | 199 | `#profileEditModal` 编辑资料（头像/昵称/签名/性别/地区/学习偏好等） | 大表单走遮罩弹窗 |
| PPT训练.html | 274 | `#pptWorkModal` 添加作品（标题/类型/多图上传/设计说明/标签） | 表单走遮罩弹窗 |
| PPT训练.html | 320 | `#pptWorkView` 作品详情大图预览（标题/说明/标签/多图） | 详情走遮罩弹窗，违反 ADR-3“详情必须走页面内布局” |
| 设置.html | 819 | `#stPwdModal` 修改密码（当前/新/确认密码） | 表单走遮罩弹窗 |
| 私聊.html | 248 | `#imGroupSettingsModal` 群设置（成员管理/踢人/免打扰/退群/解散/改群名公告占位） | 列表+表单核心内容走遮罩弹窗 |
| 私聊.html | 300 | `#acPanel` 联系管理员会话浮层（消息列表+输入框） | 列表+作答（聊天）走遮罩弹窗 |

**合规示例（供对照）：**

- 好友申请.html：0 处弹窗/overlay，上轮改造效果保持。
- 申论刷题.html：`#slConfirmOv`（删除/交卷/重新开始二次确认）、`#slSheetOv`（加入积累本勾选）属 ADR-3 允许的轻交互。
- 私聊.html：`#imGroupModal`（发起群聊）为轻量多选；`#imAddFriendModal` 已改为页面内区块。
- mock_exam_run.html:731 `confirm-mask` 为交卷二次确认，合规。
- 个人中心.html:298 `#logoutConfirmModal` 为退出登录二次确认，合规。

**全站 `countdownModal` 休眠外壳说明：**
约 25 个页面保留 `#countdownModal` DOM，但仅 `学习工作台.html` 首页存在 `#countdownRow` 入口会真正触发；
其余页面为 `app.js` 顶层监听无守卫的兼容性保留，无用户入口，属低风险遗留。

---

## 三、图片引用断链

**结论：通过。**

- `assets/app.js` 中 `[IMG:文件名]` 去重后共 591 个唯一标记，与上一轮基线一致。
- 抽查前 60 个（均为 `2027数量关系（题本）_*`）全部命中，0 缺失。
- 全量校验缺失 6 个，均为注释/正则字面量误报：
  `文件名`、`xxx.png`、`文件`、`([^\`、`xxx`、`[^\`。
  与上一轮基线完全吻合。

---

## 四、窄屏（≤760px）溢出风险

**结论：通过，无明显硬溢出。**

| 文件 | @media 情况 | 观察 |
|---|---|---|
| 行测刷题.html | 无本地 `@media`，依赖 `common.css` 的 `max-width: 768px` | 布局多为 `flex-wrap: wrap`，无写死 `width > 100vw`；仅 `<select style="min-width:132px">` 在极端窄屏下可能贴近边缘，建议观察 |
| 申论刷题.html | `@media (max-width: 900px)`：`.sl-split` 改 column、`.sl-grid2` 改 1fr | 无写死大宽度；依赖 `common.css` 处理侧栏/底栏；整体收敛 |
| 时政热点.html | `@media (max-width: 900px)`：`.hn-dwrap` 改 column、`.hn-item` 换行、`.hn-ops` 宽度 100% | 筛选 `.hn-chips` 已 `flex-wrap: wrap`；无写死 `width > 100vw`；整体收敛 |

---

## 总体判定

| 检查项 | 结果 |
|---|---|
| 滚动锁覆盖 | 通过 |
| ADR-3 弹窗合规 | **不通过（8 处违规）** |
| 图片断链 | 通过 |
| 窄屏溢出 | 通过 |

**总体：有条件通过。本轮新改动未破坏滚动锁，但新增/遗留的表单、详情、聊天类弹窗仍违反 ADR-3，建议将 8 处违规改为页面内布局或页面内全屏面板。**
