# 学习概括页面整合需求（批次九 · N9-22，2026-09-15 · 终版＝合并方案）

> 登记来源：用户 2026-09-15 提出「这几个页面都有学习概括，要不要把这几个卡片都删除了，做一个学习概括页面进行全方面的各种数据实时分析的综合页面」，明确要求「记录一下」。
> 终版口径（用户 18:57 澄清截图后修正）：**把「学习概括」综合页 + 个人中心「本机数据」合并成 ONE 综合页；删除「本机数据」菜单与页面；新建页入口放到「更多」页；把「学习统计」功能并入该页并删除「学习统计」菜单。**
> 本文件为需求原文登记，**未开工**——用户铁律「不说开工不要自己执行」。
> ⚠️ N9-22 **取代 N9-20**：N9-20 原计划把「本机学习数据」页内改名「学习报告」，现改为「本机数据」整块删除并并入综合「学习概括」页，N9-20 仅存档。

---

## 0. 一句话定位

做 **ONE** 综合「学习概括」实时多维分析页，吸收原「本机数据」+「学习统计」两块能力；**删除**个人中心「本机数据」菜单与页面、**删除**全站「学习统计」菜单；新综合页的唯一入口挂在「更多」页。

---

## 1. 本次要删除 / 合并的散落资产（只读核实落点）

| 要处理的资产 | 落点（精确行号，已 Grep 核实） | 处理方式 |
|---|---|---|
| 个人中心「本机数据」菜单卡 | `个人中心.html` L106 `<div class="subpage-group-card" onclick="SubpageRouter.navigate('local-data')">` + L108 `<div class="sgc-title">本机数据</div>` | **删除整个 subpage-group-card（L106–109 区间）** |
| 个人中心「本机数据」子页 section | `个人中心.html` L269 `<section data-subpage="local-data">` + L274 `<div id="localStats">…</div>` | **删除整个 section（L269–276 区间）** |
| 个人中心填充逻辑引用 | `个人中心.html` L485 `var ls = document.getElementById('localStats');`；L823 `else if (key === 'prefs' || key === 'local-data')`；L782–793 `SubpageRouter.init({...})` 内 `local-data` 路由项 | **清理对应引用**（删 L485 取节点、L823 分支、SubpageRouter 路由登记项） |
| 「学习数据」卡（个人中心/档案） | `assets/api.js` L1297 / L1377「学习数据」卡 + L1315–1321「学习模块」入口 | 视综合页是否复用 `renderProfileOverview()` 而定：若综合页独立实现，则此处「学习数据」卡一并删；若复用则保留函数、仅删菜单卡 |
| 更多页「学习统计」功能卡 | `更多.html` L102–106 `<div class="morepage-card morepage-list-item" onclick="openBlogStats()">` + L104 `mpc-title">学习统计` | **改为「学习概括」入口卡**（改名 + 改 onclick 指向新综合页） |
| 更多页旧底部弹层「学习统计」 | `更多.html` L164–166 `<div class="bottom-more-item" onclick="openBlogStats()">` + `bm-label">学习统计` | **删除**或随 `#morePanel` 整体下线（legacy 兜底层） |
| 30+ 页底部导航「学习统计」按钮 | 各页 `.bm-label">学习统计` + `onclick="openBlogStats()"`（如 `行测刷题.html` L171 / `错题本.html` L150 / `四级词汇.html` L179 / `申论刷题.html` L238 / `商务礼仪.html` L137 …） | **删除菜单项**（见 3.4 待确认：删 vs 改指向新页） |
| 现有聚合入口 | `openBlogStats()`（`api.js`）→ 跳 `学习博客.html#stats`；`renderBlogStats()`（`api.js` L761）；`学习博客.html` `blogViewStats`（L306） | 综合页吸收其统计渲染逻辑，`openBlogStats()` 目标改为新综合页（或保留作兼容别名） |

---

## 2. 新建综合「学习概括」页（吸收两块能力）

### 2.1 承载位置（待确认，倾向 B）
- **A. 新建独立 HTML**（如 `学习概括.html`）：信息架构独立、不污染博客统计视图；需按部署 SOP 打包 + 加全站戳。
- **B. 复用 / 增强 `学习博客.html#stats`**（`blogViewStats` + `renderBlogStats`）：零新文件、改动最小；把「本机数据」的本地指标 + 行测各模块表并入该视图，改名「学习概括」。
- **倾向 A**：与用户「做一个学习概括页面」「把新建的学习概括页面添加到更多」措辞更贴合；且现有 `学习博客.html#stats` 语义是「广场发帖统计」，混进本机学习数据会概念打架。

### 2.2 页面内容（实时多维分析）
- **核心指标卡（P0）**：连续打卡天数 / 总学习时长 / 做题总数 / 正确率 —— 数据 `appData.stats`
- **各模块进度表（P0/P1）**：行测各模块正确率 + 题量 —— 数据 `EXAM_BANK` / `appData.examTypeProgress`
- **近 7 天学习时长柱状图（P1）**：—— 数据 `activityLog`（纯 CSS/SVG 柱状，禁第三方库）
- **最近学习记录（P1）**：—— 数据 `activityLog` 列表
- **各模块时长占比环形图（P2）**：—— 数据 `activityLog` / `appData`（纯 SVG 环形）
- **吸收「本机数据」原内容**：总学习时、刷题、错题、连续打卡 —— 全部 subsumed
- **吸收「学习统计」原内容**：学习概况 · 本周报告 —— subsumed
- 数据全本端、无新增后端；图表纯 CSS/SVG。

### 2.3 「更多」页入口（P0，必做）
- `更多.html` L102–106 的「学习统计」卡 → 改名「学习概括」并 `onclick` 指向新综合页（独立页用 `location.href='学习概括.html'`，复用博客则用 `openBlogStats()` / `gotoBlogStats()`）。
- 这是用户明确要求的「把新建的学习概括页面添加到更多功能页面」。

---

## 3. 与 N9-20 / 历史的关系

- **N9-20（已取代·仅存档）**：原方案 = 个人中心「本机学习数据」页内改名「学习报告」+ 增强。现整块删除并入综合页，故 N9-20 不再单独执行。
- **N9-22 终版 = 合并方案**：综合页 = 原「学习概括」设想 + 「本机数据」能力 + 「学习统计」能力，三位一体；入口集中在「更多」页，其余散落菜单全删。

---

## 4. 待确认（开工前）

1. **✅ 决策定稿（19:27）：综合页承载位置 = 独立新 HTML（方案 A）**
   理由：现有 `学习博客.html#stats` 语义是「广场发帖统计」，把本机学习数据混进去会概念打架；独立页信息架构干净，也更贴合用户「做一个学习概括页面」「添加到更多功能页面」的原话。落地位置：`更多.html` L102–106 改入口卡。

2. **✅ 决策定稿（19:27）：30+ 页底部导航「学习统计」按钮 = 删除（方案①），但 `openBlogStats()` 函数保留重定向**
   - **删除**：30+ 页底部导航里的「学习统计」`.bm-` 菜单项（`@마-` 节点），批量脚本执行 + 逐文件 Grep 残留检查 + jsdom 冒烟。
   - **保留并重定向**：`openBlogStats()` 函数**不删**，仅把跳转目标从 `学习博客.html#stats` 改为新综合「学习概括」页，作为**兼容别名**——防止任何漏网的引用触发 `ReferenceError` 导致白屏（这是本项最大风险点）。
   - **理由**：① 贴合用户原话「综合学习统计功能并删除学习统计这个菜单」——留着一个标题写「学习统计」的按钮就是没删干净；② 入口收敛到「更多」页一处，消除 30+ 页重复入口与口径不一；③ 虽跨 30+ 文件，但改的是删除同一重复节点，机械可控，配合既有批量脚本 + 逐文件验证 SOP 风险可管理。
   - **代价提示**：删除后各页底部导航会少一个格子（全局 UI 微调），且用户失去「任意页一键进统计」的触达。此代价用户已通过「删除学习统计菜单」的指令认可；实施后如需保留触达，可再议。
   - **配套**：`更多.html` L102–106 → 改名「学习概括」入口卡（指向新综合页）；L164–166 旧弹层「学习统计」→ 删除。`renderBlogStats()`（博客侧统计）保持不动，仅菜单入口收敛。
3. `#morePanel` 旧底部弹层（L161–179）是否整层下线，还是仅删其内「学习统计」项——需结合部署 SOP「底部弹层是否仍启用」确认。
4. `assets/api.js` 的 `renderProfileOverview()`（填 `#localStats`）在「本机数据」删除后是否弃用：若综合页独立实现则该函数可删；若复用则保留。

---

## 5. 约束（沿用全站铁律）

- 老 WebView 语法禁令：禁 `?.` / `??` / `replaceAll` / `Object.fromEntries` / `Array.at` / 顶层 await。
- ADR-3 合规：图表/预览全页内（无弹窗）；保留 `openBlogStats` / `renderBlogStats` 等 id 与函数名（若复用）。
- 改 `api.js`（`openBlogStats` / `renderBlogStats` / `renderProfileOverview`）后**必 bump 全站 `app.js` 版本戳**；新建独立页按部署 SOP 打包并加戳。
- 数据来源（已确认可复用）：`activityLog` + `EXAM_BANK` / `appData.examTypeProgress` + `appData.stats`（同 N9-20）。
- 删除 `个人中心.html` 内 `local-data` 子页与路由时，注意 `SubpageRouter.init({...})` 与 `onSubpageChange` 钩子里不要残留 `'local-data'` 分支（否则空引用报错）。

---

*文档结束。N9-22 终版＝合并方案，取代 N9-20；所有落点已只读核实，待用户「开工」后按第 4 节确认项定稿推进。*
