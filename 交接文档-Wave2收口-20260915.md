# 交接文档 · 批次九 Wave 2 收口（20260915 晚班）

> 接手者请通读本文档再动手。前情：Wave 1 收尾 + N9-25 补充包已上线（commit `a60c42f`，版本 `20260915f`），热修 1 删工具页考试倒计时已上线（commit `65a5673`）。当前处于 **Wave 2 四条线全部落地、QA 全绿、版本戳 bump 已完成、只差「热修 3 → 提交 → 部署 → 推送」** 的收口阶段。

---

## 0. 一句话现状

Wave 2 四条线（N9-19 面试重构 / N9-17 长按撤回 / N9-18 听力精听 / N9-14 PPT 双线）+ blog_wechat 版本戳修正 + 统一 bump（→ `20260915g`）**全部落在工作区、QA 全绿、未提交**。用户最新截图反馈「更多页收放列表取消，恢复直接显示功能菜单按钮」**尚未实施**。你要做的就是：热修 3 → 白名单提交 → 部署 → 线上复检 → GitHub 推送 → 更新任务执行文档。

---

## 1. 铁律（违反 = 线上事故，先背再动手）

1. **唯一可写代码树 = `D:\下载的文件\学习工作台`**。`C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-*` 是 git linked worktree，改了等于废品。
2. **老 WebView 语法禁令**（APK 旧内核 ParseError 白屏）：禁 `?.` `??` `.replaceAll(` `Object.fromEntries` `.at(` 顶层 await、正则后行断言 `(?<=` `(?<!`。`node --check` 抓不到，必须 Grep 逐项确认 0 命中。
3. **ADR-3**：禁原生 `alert(` / `confirm(` / `prompt(`；确认/弹窗走页内区块或轻量非模态 popover。
4. **版本戳 bump 是全局互斥锁**：改共享 JS/CSS 的收尾会重写 30+ HTML。波内任务一律不 bump，波末单点统一。**本次 bump 已由上一班完成（20260915c → 20260915g，7 处），不要再 bump**。
5. **绝不 `git add -A`**：工作区有 80+ 个 `??` 临时脚本/工具文件。提交一律走**显式白名单**。
6. **文件清理必须全批次最后一步**（P2，用户铁律，现在不做）。
7. **保留既有 DOM id 与全局函数名**（外部页面可能引用）。
8. **git push 必须走代理**，代理端口会变：先读 `HTTP_PROXY`/`https_proxy` 环境变量（WorkBuddy 自带代理，端口每次开机不同；注意自带代理可能 CONNECT 502 不可用），备选 `127.0.0.1:7897`（Clash，2026-09-15 晚实测可用）、`7890`、`10809`、`1080`。用 python socket 逐个探测，`ls-remote` rc=0 才 push。标准写法：`git -c http.proxy=P -c https.proxy=P -c http.version=HTTP/1.1 push origin main`。

### 环境坑（本机实测，全部踩过）

- **bash 缺 coreutils**：`ls / grep / head / tail / dirname` 全部 command not found。文件搜索用 Glob/Grep 工具，跑程序用绝对路径直接调。
- node = `C:\Users\ATM\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`；python = `C:\Users\ATM\.workbuddy\binaries\python\versions\3.13.12\python.exe`。
- **PowerShell 捕获 git 输出会乱码/吐空**；**python 控制台 print 中文会 GBK UnicodeEncodeError 崩溃**。一律 python `subprocess.run(capture_output=True)` + 脚本自写 UTF-8 文件（`open(..., 'w', encoding='utf-8')`）+ Read 工具读。
- `git diff --name-only` 输出**没有** ` M ` 状态前缀，别用 `l[3:]` 切片（会砍掉中文文件名前 3 字）。
- git 默认把中文路径转八进制 → 加 `-c core.quotepath=false`。
- 远端核实用 `ls-remote`，别信本地 tracking ref（本机 origin/main 有 `[gone]` 怪癖）。
- **用户偏好（最高优先级）：直接给结论，命令/脚本各跑一次即判定，不重复回跑验证。**

---

## 2. 当前 git 状态（2026-09-15 22:59 快照，bump 已含）

- HEAD = `65a5673`（热修 1：工具页移除「考试倒计时」区块）
- 近五个提交：`65a5673` → `a60c42f`（Wave1+N9-25, 20260915f）→ `789ec80`（20260915e）→ `05ef596`（20260915d）→ `f0db2a1`
- **工作区未提交改动（M，共 14 个）——这就是 Wave 2 提交白名单本体**：

| 文件 | 归属 | 内容 |
|---|---|---|
| `AI模拟面试.html` | W2-T1 / N9-19 | 模拟面试页重构，+1245 行（题目卡、展开块、报告区改版） |
| `assets/chat-local.js` | W2-T2 / N9-17 | 微信式长按消息菜单撤回（`imShowMsgMenu` / `imRecallEligible` / `imRecallDeny` 等），+264 行 |
| `私聊.html` | W2-T2 / N9-17 | 删桌面 hover 撤回按钮样式，加 `.im-msg-menu` 菜单样式 |
| `assets/voiceplayer.js` | W2-T3 / N9-18 | 听力精听 P0：`SCENE_GROUPS` 两级分类、卡片放大 70%/max820px、英文 22px、真·开关、进度条+查看全文、单 AI 助教入口。418→725 行 |
| `四级备考.html` | bump | L438 `voiceplayer.js?v=20260915g` |
| `学途.html` | bump | L175 `voiceplayer.js?v=20260915g` |
| `PPT训练.html` | W2-T4 + bump | 顶部导航去橙（P0-⑤，页内 style 覆盖）+ 4 个资产戳 → 20260915g |
| `assets/data-ppt-class.js` | W2-T4 course 线 | 四原则增量：对比图 11 组、`.dc-prog` 进度条、chip 按钮化、正文卡片化、关键句高亮 |
| `assets/design-class.js` | W2-T4 course 线 | 配套渲染器 |
| `assets/data-ppt-tips.js` | W2-T4 course2 线 | 技巧提升 5 专题 + `cases` 16 组，+456 行 |
| `assets/ppt-tips.js` | W2-T4 course2 线 | `PT_caseSvg` 等渲染器，+655 行 |
| `blog_wechat.html` | 版本戳修正 | 9 处：common/page-head/states/polish.css、icon-map/xt-toast/error-boundary.js → `20260915c`；app.js `20260915eaaa`→`20260915f`；api.js `20260913j`→`20260915f`。全站畸形戳普查 = 0 |
| `工具.html` | 注释整理 | 删倒计时区块的注释重写（本体已随 `65a5673` 上线，此次仅注释措辞） |
| `任务执行文档-20260915-全线自主执行.md` | 文档 | 已追加 Wave1 收尾 + 热修 1 章节 |

- `??` 未跟踪文件全是临时脚本/工具（`tools/qa/_*`、`tools/jx_patch/_*`、`_w2t1_*` 等）+ tar 包，**不提交、不删除**（清理是全批次最后一步）。

---

## 3. 待办（按优先级，逐步执行）

### P0 · 热修 3：撤掉 `更多.html` 的分组收放，恢复平铺直接显示

**用户原话（2026-09-15 22:59，附截图）：**「这个页面的收放列表也给取消了还可之前一样直接显示功能菜单按钮」（「还可」=「恢复」）。即：把 W3-T1（N9-25，已随 `a60c42f` 上线）给更多页加的分组折叠功能整块撤掉，所有功能卡片像之前一样直接平铺可见。

**实现已全部定位（可直接照做）：** 文件 `D:\下载的文件\学习工作台\更多.html`

1. **CSS**：删 L12-31 整个收放 `<style>` 块（`.mp-section-toggle` / `.mp-section-arrow` / `.mp-group-body` 三组类 + 注释）。
2. **HTML 三个分组头**，把可点击标题恢复成纯标题：
   - L127-130：`<div class="nav-section mp-section-toggle" id="mpHeadCommon" onclick="toggleMoreGroup('mpGroupCommon', this)" role="button" tabindex="0" aria-expanded="true" title="展开 / 收起"><span>常用</span><span class="mp-section-arrow">…chevron-down…</span></div>` → 恢复为 `<div class="nav-section">常用</div>`
   - L155-158：`mpHeadContent`（collapsed 态）→ `<div class="nav-section">内容直达</div>`
   - L187-190：`mpHeadSystem`（collapsed 态）→ `<div class="nav-section">系统</div>`
3. **三个分组体**：`<div class="mp-group-body" id="mpGroupCommon">`（L131）、`<div class="mp-group-body collapsed" id="mpGroupContent">`（L159）、`<div class="mp-group-body collapsed" id="mpGroupSystem">`（L193）——去掉包裹 div（卡片直接平铺回 `.morepage-list`），或至少删掉 `collapsed` 类与 id。**推荐连包裹 div 一起删干净**（收起态是 `display:none`，不删干净有些卡片永远看不见——这正是用户不满的原因）。
   - ⚠️ **L191-193 注释红线**：组内 `#adminEntryCard` 自带 `style="display:none"`，显隐由 `assets/admin.js` 控制，**那张卡自身的 style 和 id 一个都不能动**，只删外层收放容器。
   - L137-139 的「工具箱入口」卡（W3-T1 加的指向 `工具.html` 的入口）**保留**——用户只说撤收放，没说删这张卡。
4. **JS**：删收放脚本块（约 L255-298：`MP_GROUPS` 数组、`mpApplyGroupState` / `mpCollectState` / `mpWriteSavedState` / `mpReadSavedState`（如有）/ `toggleMoreGroup` 函数与末尾的 `restoreMoreGroups();` 调用）。localStorage 里的旧收放键（形如 `mp_group_state` 之类，删代码时顺手确认键名写进提交说明）可留可不管，没有代码读它就是死数据。
5. **注释**：仿照工具页热修的写法——写「2026-09-15 用户反馈：分组收放撤除，恢复直接平铺」，**刻意不写 id / 函数名本体**，避免污染全站自动化符号扫描。
6. **自检**：改完 Grep `mp-section|mp-group|toggleMoreGroup|mpHead|mpGroup` 应 0 命中（或仅存于说明注释中不含符号本体）；抽 `更多.html` 内联 script 跑 `node --check`；禁用语法 / ADR-3 Grep 0 命中。
7. **版本戳**：更多.html 的收放是页内自足改动（无 assets 引用变化），**不需要 bump**，直接进提交清单。

### P0 · 提交 Wave 2 整批（白名单，绝不 add -A）

白名单 = 上表 14 个 M 文件 + 热修 3 的 `更多.html` = **15 个文件**。仿照 `tools/commit_20260915f.py` 写 `tools/commit_20260915g.py`（python subprocess 白名单 add + commit，输出写 UTF-8 文件再 Read）。提交信息建议：

```
feat: 批次九 Wave2 四线交付 + 更多页收放撤除（20260915g）

- N9-19 AI模拟面试页重构（题目卡/展开块/报告区）
- N9-17 私聊消息撤回改微信式长按菜单
- N9-18 听力精听 P0（两级场景分类/大卡/开关/进度条）
- N9-14 PPT训练双线（四原则增强 + 技巧提升案例集）+ 顶栏去橙
- blog_wechat 版本戳修正 9 处（畸形戳清零）
- 更多页分组收放按用户反馈撤除，恢复直接平铺
- 波末统一 bump：6 个改动资产 ?v=20260915g（7 处引用）
```

### P0 · 部署

仿 `tools/deploy_update_20260915f.py` 写 `tools/deploy_update_20260915g.py`：tar 白名单 15 文件 → pscp 上传 → 远端先备份 `web_bak_20260915g.tar.gz` → 解压 → grep 校验落点（校验项建议：`更多.html` 中 `toggleMoreGroup` 0 命中、`nav-section">常用` 1 命中；`PPT训练.html` 中 `20260915g` ≥4 命中；`AI模拟面试.html` / `voiceplayer.js` / `chat-local.js` 关键新符号各 1 命中；`blog_wechat.html` 中 `20260913j` 0 命中）。服务器与路径参数照抄旧脚本。

### P0 · 线上复检 + GitHub 推送

- 复检仿 `tools/release_20260915f.py`：9~11 个页面 HTTP 200 + 新标记命中 + 线上 app.js 禁用语法仍为 0。
- 推送仿 `tools/push_20260915f.py`：python socket 逐端口探测（env `HTTP_PROXY` 端口 → 7897 → 7890 → 10809 → 1080），`ls-remote` rc=0 才 push，`git -c http.proxy=P -c https.proxy=P -c http.version=HTTP/1.1 push origin main`。2026-09-15 晚实测 7897 通、WorkBuddy 自带代理 502。

### P1 · 更新任务执行文档

`任务执行文档-20260915-全线自主执行.md` 追加「批次九 Wave 2 交付 + 热修 2/3」章节：四线内容摘要、bump 清单、热修 2（blog_wechat 戳）与热修 3（更多页收放撤除）、commit hash、部署与推送结果。

### P2 · 悬案与遗留（不阻塞本波收口）

- **「工具页的我的导入题库跑到博客页」**：代码侧查无实据——`#impLibs` 宿主仅在 `工具.html` L176，`blog_wechat.html` 与 `学习博客.html` 搜不到 importer/导入题库/我的导入任何字样。**需用户补截图才能定位**，别再空转排查。
- 用户截图当前模型读不了（图片 Content filtered）——涉及截图的需求让用户用文字描述。
- 数量关系题库回滚重做（P0 遗留）、`common.css` L68 全局滚动锁（P1）、文件清理（P2，必须最后一步）。
- Wave 3+ 备忘（原波次计划）：共享文件波 A/B/C（app.js / api.js / common.css 大手术，天然串行）、全站 HTML 原子批量波（N9-9 / N9-22B / N9-3）、收尾波（N9-7 app.js 拆分 + bump + 部署 + 清理）。

---

## 4. 关键事实表（新会话免重查）

| 事实 | 数值 |
|---|---|
| 统一 QA（`tools/qa/_w2_qa.py`，2026-09-15 晚） | 15 个脚本 node --check 全 OK；禁用语法 0；ADR-3 原生弹窗 0 |
| bump 执行结果（`tools/qa/_bump_out.txt`） | `20260915c→20260915g` 共 7 处：PPT训练.html×4、四级备考.html×1、学途.html×1、私聊.html×1；残留旧戳 0；畸形戳 0 |
| bump 覆盖资产 | data-ppt-class.js / design-class.js / data-ppt-tips.js / ppt-tips.js / voiceplayer.js / chat-local.js |
| 全站戳高频值 | `20260915c`×336（未动资产）、`20260915f`×60（app.js/api.js 等已上线资产）、新增 `20260915g`×7 |
| 子代理 wave2-interview | 改动已落地且被我统一 QA 覆盖（全绿），其最终自检报告未返回——**不必等它，直接提交** |
| AI模拟面试.html 行数 | 内联 script node --check OK（`_w2_qa_out.txt` 第 1 节） |
| 工具页删倒计时 | 已上线（`65a5673`）；工作区仅剩注释措辞重写 |
| 更多.html 收放 | W3-T1 所加、已上线、用户要求撤除——见 §3 热修 3 精确方案 |

## 5. 收口完成后

1. 向用户报告：commit hash、部署校验结果、推送结果、线上复检结论（一段话，别啰嗦）。
2. 工作日志：`.workbuddy/memory/2026-09-15.md` 追加一行（部署源树的日志在 worktree 那边的 memory 目录，路径见系统提示；若不可写就跳过，不影响交付）。
3. **不要做文件清理**（全批次最后一步，用户明令）。

---

## 6. 新增用户反馈（2026-09-15 23:13 截图，仅记录、未实施，排在 Wave 2 收口之后）

> 截图存档：`C:\Users\ATM\.workbuddy\clipboard-images\clipboard-2026-09-15T15-13-04-268Z-361459bf.png`（本次模型可正常读图）。
> 页面 = `个人中心.html` 资料子页（在线模式，api.js 渲染），用户数据：树枝 / 幻想买馍 / 吃 / 辽宁 / 考研 / 目标「成功抵达」/ @aaa。
> 用户原话：「这个页面布局有问题，我的动态放的位置不对，个人资料卡片布局和内容都需要更新升级」。

### 6-A 「我的动态」按钮位置不对（个人中心页，feed-btn）

- **现象**：资料卡正上方一个硕大的橙色渐变主按钮「我的动态」（RSS 图标），视觉上像页面标题，实际是可点按钮，点击跳 `动态.html?user=<uid>`。
- **代码定位**：`个人中心.html` L121-125 `#profileFeedBtnRow / #profileFeedBtn`（feed-btn，2026-09-15 所加，`btn-primary` 样式，挂在 `#profileBox` 之前）；文案脚本 L843-880（自视角「我的动态」/ 他人视角「查看 TA 的动态」）。
- **用户意图**：不该放在资料卡上方当"大标题"。建议收进资料卡操作区（与「编辑资料」同排，降为次按钮），或移回分组卡列表的「我的动态」卡内。
- **改动面**：仅 `个人中心.html` 页内（feed-btn 行 + 文案脚本），**不动 api.js → 不需要 bump**。优先级 P1。

### 6-B 个人资料卡布局与内容升级

- **现象（截图实测）**：
  1. 昵称/签名/简介/城市逐行竖排堆叠，无视觉层次；
  2. 头像被 flex-wrap 挤到信息文本中间偏左下，与文字区脱节；
  3. 「编辑资料」按钮孤立悬在卡片右侧中间；
  4. 「退出登录」红色危险按钮裸露在卡片左下，过于醒目易误触；
  5. 学习目标行橙色竖条与上方元素对不齐；加入时间贴底；整卡信息密度低、留白失衡。
- **根因**：`assets/api.js` `_renderProfileOnline()` L1357-1369——头像、信息区、两个按钮全塞在同一个 `display:flex;flex-wrap:wrap` 行内，窄屏下 wrap 后布局散架。本地版 `_renderProfileLocal()`（L1263+）同构写法，**升级时两个函数一起改**。
- **建议方向（待用户确认后实施）**：
  - 头部改「头像 + 昵称/签名 + @username」左右结构，头像固定尺寸不随内容换行；
  - 简介/城市/标签 chips 收进信息区，统一次级色与间距；
  - 操作区分层：主操作「编辑资料」置头部右侧；「查看动态」并入操作区；「退出登录」降级（卡片底部次级区/文本链接，现有确认弹窗保留）；
  - 学习目标、加入时间并入资料元信息行；
  - 保持既有 DOM id 与全局函数名（`editProfile/doLogout/closeProfileEditor/renderProfilePage` 等）不变。
- **改动面**：`assets/api.js`（共享资产，上线需 bump）+ 可能少量 `个人中心.html` 页内样式。**必须排进 Wave 2 收口之后的下一波**，别与 `20260915g` 混；老 WebView 语法禁令、ADR-3 照守。

---

*交接人：批次九晚班 team-lead · 2026-09-15 23:00 前后成文。QA/bump/定位数据均为当晚实测，可放心采信。*
