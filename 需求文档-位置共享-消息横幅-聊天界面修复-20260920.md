# 需求文档：实时位置双向互通 / 后台消息横幅 / 聊天界面显示 / 顶部黑边 修复清单

> 日期：2026-09-20
> 交付对象：workbuddy（实际改代码）
> 说明：以下问题均已在真机 + 源码中定位，给出**现象、根因（含文件:行号）、改法、验收标准**。请按文档改，不要自由发挥；改完逐项自测。本文只描述需求，不代表已改代码。
> 涉及端：H5 前端（根目录 `*.html`、`assets/*.js`、`assets/common.css`）、安卓壳（`android/java/com/study/workbench/*.java`、`android/AndroidManifest.xml`）、后端（`server/routers/liveloc.py`、部署进程）。

---

## 问题一：实时位置共享，双方只能看到自己，看不到对方

### 现象（真机复现）
A、B 两个好友互相开实时位置，各自地图上**只有自己的头像点，看不到对方**。期望对标微信：进入同一个共享，双方头像同时在地图上移动。

### 根因 1（交互层，必改）：当前是"单向直播"，接收方点"查看"并不会加入共享
- 发起方走 `assets/chat-local.js` 的 `imLiveStart()`（约 4980 行）：只调一次 `POST /api/live/start`，**只有发起人**自己上报坐标。
- 接收方聊天窗出现的横幅，对方态只有一个「查看」按钮（`chat-local.js` 约 4881-4885 行）；点查看只是 `imLiveGoView()` → 跳到 `live-location.html`（`imLiveViewUrl()` 约 4893-4919 行）。
- `live-location.html` 整页**只有轮询 `GET /api/live/state` 和（在已有自己会话时的）兜底 tick，从头到尾没有调用 `/api/live/start`**。其 `syncOwnTick()`（约 353-368 行）只有在 `sessions` 里已经存在"我自己的会话"时才会启动定位上报；接收方从没 start 过，所以永远不上报自己。
- 结果：发起方地图上只有自己（对方没加入）；接收方只围观、自己不上屏。**普通用户不知道还要自己再点一次"发起"，等于功能不可用。**

### 根因 2（后端/部署层，必须核实）：坐标只存进程内存，多 worker 下互相看不见
- `server/routers/liveloc.py`：坐标只放在进程内字典 `_LIVE`（32 行）和私聊双向索引 `_LIVE_BY_PEER`（35 行）。
- `GET /api/live/state`（289 行起）**先查本进程内存；只有本进程内存一条都没有（`if not pairs`，304-318 行）才回退数据库**。
- 一旦 uvicorn 跑了多个 worker，A 的 start/tick 落在 worker1、B 的落在 worker2（请求被轮询分发）：A 查 state 命中 worker1（自己在、列表非空）→ 不查库 → 只返回 A；B 同理只返回 B。DB 里虽有两行（30s 心跳落库），但"内存非空就不查库"，永远合并不到一起。
- 部署现状有矛盾，**必须先在服务器确认**：
  - `docs/HTTPS+APP跨端改造方案-2026-09-12.md:243` 的 systemd 写的是 `--workers 4`；`docs/review-r88f-服务端用量审查与修复.md:184` 也写"已确认多 worker 是真实部署形态"；
  - 但 `_SECURITY_REPORT_20260919.md:38` 又记录线上是"单进程"。
  - 请在服务器执行 `ps -ef | grep uvicorn | grep -v grep`，以实际进程为准。

### 改法
1. **交互改成微信式"点进即加入"**：接收方（私聊对方、群聊成员）点「查看」进入 `live-location.html` 时，若自己在该会话（该 peer 对 / 该 group）还没有 active 共享，自动调一次 `POST /api/live/start`（peerId/groupId 与当前页一致）并开始上报坐标；离开页面/点结束时调 `/stop`。
   - 可给「加入共享（默认）/ 仅查看」两个入口，但默认行为必须是"加入"，保证双方都上屏。
   - 复用现有 `imLiveArmUpload()` 的三态上报（原生桥优先、JS watchPosition 兜底），不要新写一套定位。
2. **后端消除多 worker 隐患，二选一**：
   - 最省事：启动命令/systemd 强制 `--workers 1`（本项目 WebSocket、内存限流、实时位置本就都按单进程设计）；
   - 彻底：把实时位置状态从进程内存迁到 Redis 或数据库，`/api/live/state` 始终合并查库，不依赖"本进程内存"。
3. 幂等与隐私不变：重复 start 仍走现有"先结束本人同目标旧会话"逻辑；坐标仍不落轨迹、state 不返回历史点。

### 验收标准
- A 发起共享，B **只点一次「查看」**，两人地图上都能看到彼此头像并实时移动（无需 B 再去发起）。
- 群聊中多个成员点查看，所有加入者头像都上屏，退出者头像消失。
- 反复进/出页面不产生重复会话、不重复落"开始共享"卡片。
- 后端无论单/多 worker（或重启 uvicorn 后），双方都能互相看到，不出现"各看各的"。
- 60 分钟到期、任一方结束，地图正确显示"共享已结束"。

---

## 问题二：退到后台 / 在其他 App 上方，不弹新消息横幅，只能看到"星途正在守护消息"常驻条

### 现象
App 在后台或停在别的应用上，好友发消息后**屏幕顶部不弹横幅通知**；下拉通知栏只看到常驻的"星途正在守护消息"守护条，看不到/不弹出"好友名：消息内容"。用户已开启通知总权限，仍无效。期望对标微信：不管在哪个应用上方都能顶部弹横幅。

### 先做判别（决定修哪一层，真机先验证）
让好友发一条，停在别的 App，下拉通知栏：
- **A 类**：通知栏里有"好友名：内容"那条，只是不弹顶部横幅 → 显示层问题（根因 1、2）。
- **B 类**：通知栏里只有守护条、没有消息那条，回到 App 才看到 → 后台取消息被系统限制（根因 3）。

### 根因 1（代码，必改）：消息通知构建缺少"弹横幅"要素
`android/java/com/study/workbench/MsgPollService.java` 的 `notifyMessage()`（371-417 行），构建通知时（397-413 行）只设了小图标、标题、正文、`setAutoCancel`、点击 PendingIntent，**缺少**：
- `setCategory(Notification.CATEGORY_MESSAGE)`（消息类，系统据此优先 heads-up）；
- `setPriority(Notification.PRIORITY_HIGH)`（兼容低版本 / 部分 ROM）；
- 消息渠道 `xt_msg_v2` 创建时（329-332 行）只传了 `IMPORTANCE_HIGH`，**没有显式 `enableVibration(true)` 和 `setSound(系统默认铃声)`**。原生安卓 HIGH 一般会弹，但很多国产 ROM 把"无声/无振动渠道"判为静默、不弹横幅。
- 另外消息通知用了**固定 id `MSG_NOTIFY_ID=101`**（81 行、413 行，注释写"后一条覆盖前一条不刷屏"）。Android 更新一条已存在的通知时默认**不重复弹横幅**，导致连续消息/不同会话常常不弹顶。

### 根因 2（国产 ROM，需引导用户开）
小米/红米、OPPO、vivo、华为等，除"允许通知"总开关外，**每个渠道还有独立的"悬浮通知/横幅通知""锁屏通知"开关，默认可能关闭**。代码无法替用户打开，只能引导到**具体渠道**设置页。
- 现有 `MainActivity.openAppNotificationSettings()`（约 1842 行）只跳到"应用通知总设置"，请补一个直达渠道的跳转：`Settings.ACTION_CHANNEL_NOTIFICATION_SETTINGS`，extra 带 `EXTRA_APP_PACKAGE` 和 `EXTRA_CHANNEL_ID = "xt_msg_v2"`，让用户一键到"消息通知"渠道把横幅/悬浮/锁屏/铃声/振动打开。
- 不需要"显示在其他应用上层/悬浮窗"权限，别引导错。
- App 内每次进消息相关页可检测：`NotificationManagerCompat.from(this).areNotificationsEnabled()` 以及 `getNotificationChannel("xt_msg_v2").importance < IMPORTANCE_HIGH`，命中就弹一次明显的引导（渠道一旦被降级，App 无法用代码提回，只能引导用户手动开）。

### 根因 3（架构差距，决定能否做到微信那样）
- 本项目后台取消息靠前台服务 + HTTP 轮询：亮屏 60s/次、灭屏 120s/次（`PollLogic.java:23/26`，服务 `MsgPollService.java`）。切到别的 App、尤其锁屏/Doze/国产省电策略下，后台网络与 CPU 会被延迟或暂停，消息不能实时取到（B 类）。
- 微信/QQ 接了**各厂商系统级推送通道**（FCM、小米/华为/OPPO/vivo/魅族 Push，或个推/极光聚合），消息由系统通道下发，**App 被杀也能弹**。本项目当前未接任何厂商推送 SDK，纯轮询在国产机上无法完全等同微信。
- 不改架构的缓解：引导用户把星途设为"允许自启动、电池无限制/不优化、最近任务锁定后台、关闭省电"（已有一次电池优化引导 `MainActivity.maybeGuideBatteryOnce()`，可结合渠道引导一起做）。

### 改法（按优先级）
- **P0（代码，先做）**：消息通知补 `CATEGORY_MESSAGE` + `PRIORITY_HIGH`；消息渠道显式开启振动 + 系统默认铃声；通知 id 改为**按会话 id 区分**（不同会话各自一条、各自弹横幅），或用消息唯一 id，避免固定 101 互相覆盖不弹。
- **P0/P1**：增加"渠道级通知设置"直达引导 + 未授权/渠道被降级时的 App 内提示。
- **P2（大改，单独排期，需主理人确认）**：接入厂商系统推送（建议先用聚合推送如个推/极光一次接入覆盖主流厂商，或按品牌逐个接），才能实现"被杀也能弹"。在未接推送前，文档/产品话术不要承诺微信级实时。

### 验收标准
- App 在后台、停在别的应用上，好友发消息后，**一个轮询周期内顶部弹出横幅**、有铃声/振动、点击直接进入对应会话。
- 两个不同会话先后发消息，各自独立弹横幅、通知栏各自一条，不互相覆盖。
- 通知权限或渠道横幅被关时，App 内有明确引导，且一键能跳到"消息通知"渠道设置页。
- 前台打开聊天时不重复弹（保持现有 `appForeground` 由 JS 提示的逻辑，`MainActivity.onResume/onPause` 2258/2264 行）。

---

## 问题三：手机聊天界面显示错乱（底部导航夹进键盘、输入框被盖、顶部双层头）

> 真机截图确认：键盘弹起时，输入框与系统键盘之间夹着全局底部导航"首页/互动/我的/AI/更多"；个别情况下整条输入栏被键盘盖住；聊天页顶部同时存在"通讯录外壳头 + 聊天头"两层。

### 根因
1. `私聊.html` 第 121-126 行手机端媒体查询里，进入聊天（body 加 `im-mobile` 类，JS 见 `assets/chat-local.js:1802/3818`，退出移除 1947 行）只做了：
   - `body.im-mobile .im-side{display:none}`、`body.im-mobile .im-chat{display:flex}`；
   - **没有隐藏全局底部导航 `.bottom-nav`，也没有隐藏通讯录外壳顶部栏**。
2. `.bottom-nav` 是 `position:fixed;bottom:0`（`assets/common.css:1355-1364`，手机端 `display:block` 见 1479 行）；而安卓键盘模式是 `adjustResize`（`AndroidManifest.xml:60`），键盘一弹，fixed 的导航条被顶到键盘上沿，正好夹在输入框和键盘之间。
3. `.main > .page` 手机端有固定 `padding-bottom:64px(导航)+20px+安全区`（`common.css:1486`）；即使隐藏导航，这块为导航预留的留白仍在，会导致输入框下方空一条。
4. 前端**没有任何键盘避让处理**（全项目搜不到 `visualViewport` / `interactive-widget`），部分输入法/手势导航下 WebView 的 `adjustResize` 收缩不彻底，输入栏 `.im-composer` 被键盘盖住（截图 4）。

### 改法
1. 手机端进入聊天（`body.im-mobile`）时：
   - 隐藏全局底部导航 `.bottom-nav`；
   - 隐藏通讯录外壳顶部栏（"通讯录/搜索聊天记录/铃铛/对话框/月亮"那一条），聊天窗全屏，只保留聊天对象头部（如"叶子"那条，含返回）；
   - 返回会话列表时恢复外壳顶栏与底部导航。
2. 聊天态把 `.page` 底部那块 `64px+20px` 占位清零，仅保留 `env(safe-area-inset-bottom)`，让输入框真正贴底。
3. 键盘避让（任选其一或组合，需真机验证）：
   - viewport meta 增加 `interactive-widget=resizes-content`；
   - 用 `window.visualViewport` 监听高度变化，动态把 `.im-composer`/聊天容器底部抬到键盘上沿；或布局改用 `100dvh`；
   - 保证弹键盘时输入框始终可见、不被盖、不与导航叠放，收键盘后布局复原。
4. 桌面端（>760px）双栏布局维持原样，不受影响。

### 验收标准
- 手机进入好友/群聊天：看不到底部 5 个 Tab，也看不到"通讯录"外壳顶栏，只有聊天头部 + 消息区 + 输入框。
- 弹键盘：输入框紧贴键盘上沿，中间不夹导航条，输入框不被遮挡；收键盘恢复正常。
- 返回会话列表：底部导航、通讯录顶栏正常恢复，可正常切换 Tab。
- 桌面浏览器双栏（左列表右聊天）显示不变。

---

## 问题四：App 顶部状态栏纯黑（黑边）

### 现象
App 顶部系统状态栏（时间/信号/电量那一条）纯黑，白色页面从状态栏下方才开始，没有沉浸式。**这是原生壳问题，H5/CSS 改不到状态栏那块黑底。**

### 改法（原生 + 前端两层都要做）
原生安卓壳（`MainActivity`）：
1. 开启边到边：`WindowCompat.setDecorFitsSystemWindows(getWindow(), false)`；
2. 状态栏透明：`getWindow().setStatusBarColor(Color.TRANSPARENT)`；
3. WebView 根布局 `match_parent` 铺满全屏，**不要**在根布局设 `fitsSystemWindows="true"`（一设内容就被顶下来露出黑条）；
4. 按明暗模式设置状态栏图标深浅（白底页用深色图标）：`WindowInsetsControllerCompat(...).isAppearanceLightStatusBars = true`。

前端：
1. viewport 保持 `viewport-fit=cover`（聊天页等已具备，统一核对所有页面）；
2. 顶部栏背景色延伸到状态栏之后，内容用 `padding-top:env(safe-area-inset-top)` 避让（是"背景铺满 + 内容避让"，不是整页下移）。

> 联动注意：做 edge-to-edge（`setDecorFitsSystemWindows(false)`）后，键盘 IME inset 不会再自动 resize，**必须同时处理问题三的键盘避让（WindowInsets ime / visualViewport）**，否则黑边修好后键盘遮挡会更明显，建议问题三、问题四一起改一起验。

### 验收标准
- 状态栏透明、顶部栏背景（白色/主题色）延伸到状态栏后面，时间/电量图标清晰可见、不遮挡页面标题；
- 深色模式下图标/文字反色正确；
- 各主要页面（首页、私聊、AI、设置）均无黑边、无内容被状态栏压住。

---

## 关键代码位置索引（便于定位，勿据记忆改，改前先读现状）

| 模块 | 文件:行号 |
|---|---|
| 实时位置-发起 | `assets/chat-local.js` `imLiveStart` ~4980、`imLiveArmUpload` ~5016 |
| 实时位置-查看跳转 | `assets/chat-local.js` `imLiveRender` ~4858、`imLiveViewUrl` ~4893、`imLiveGoView` ~4919 |
| 实时位置-地图页 | `live-location.html`（poll ~306、`syncOwnTick` ~353、watch 上报 ~370） |
| 实时位置-后端 | `server/routers/liveloc.py`（内存索引 32-37、`_pair` 44、`/start` 152、`/tick` 225、`/state` 289，内存优先/查库回退 304-318） |
| 后台通知-服务 | `android/java/com/study/workbench/MsgPollService.java`（渠道 316-345、前台判定 305、`notifyMessage` 371-417、固定 id 81/413、轮询 189-205） |
| 轮询间隔 | `android/java/com/study/workbench/PollLogic.java:23/26`（亮屏 60s / 灭屏 120s） |
| 通知权限/渠道引导 | `MainActivity.java`（onCreate 申请 128、渠道 1768、`showNotify` 1776、`requestNotifyPermission` 1819、`openAppNotificationSettings` 1842、前后台 2258/2264） |
| 聊天手机端样式 | `私聊.html:121-126`（body.im-mobile 媒体查询） |
| 底部导航 | `assets/common.css:1355-1364`（fixed）、`1479`（手机端显示）、`1486`（page 底部留白） |
| 键盘模式 | `android/AndroidManifest.xml:60`（adjustResize） |
| 顶部黑边 | `MainActivity` 状态栏设置 + 各页 viewport / safe-area |

## 建议实施顺序
1. 问题三 + 问题四一起做（同属外壳/insets，互相影响），真机验证键盘与状态栏。
2. 问题一根因 1（前端加入共享）+ 核实并修根因 2（worker），两账号真机互通验证。
3. 问题二先做 P0（通知要素 + 渠道引导），国产机逐项开权限验证；厂商系统推送（P2）单独评估排期。

> 改完请按每条"验收标准"真机自测，并在回复中逐条标注"已改/验证结果"；涉及服务器进程（worker 数）的改动，请同时给出 `ps -ef | grep uvicorn` 的实际结果。
