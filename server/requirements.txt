# 学习工作台 · 多人博客后端依赖
#
# 依赖锁定说明（2026-09-11）：以下为关键依赖锁定"主版本上界"，避免上游大版本升级
# 造成的破坏性变更。教训：fastapi 新版顶层不再 re-export 部分响应类，
# `from fastapi import <响应类>`（如 JSONResponse）会在 import 阶段抛 ImportError，
# 直接导致整个应用无法启动。响应类请一律从 `fastapi.responses` 导入。
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
SQLAlchemy>=2.0,<3.0
PyJWT>=2.8,<3.0
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27
