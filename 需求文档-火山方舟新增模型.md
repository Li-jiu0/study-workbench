# 火山方舟新增模型接入需求

> 在现有6个火山方舟模型基础上，新增12个模型（9个文本 + 3个图片生成），全部写入 `ai-config.js`。

---

## 一、新增文本对话模型（9个）

以下模型全部复用现有火山方舟 provider 配置：

- **端点**：`https://ark.cn-beijing.volces.com/api/v3/chat/completions`
- **Key**：已在 provider 中配置，无需重复填写

只需在 `builtinModels` 数组中追加：

| 配置ID | 显示名称 | 模型ID | 能力标签 | 速率 | 温度 | 最大token | 星级 | 降级到 |
|--------|---------|--------|---------|------|------|----------|------|--------|
| ark-v4-pro-old | DeepSeek-V4-Pro(旧) | deepseek-v4-pro-260425 | text, reasoning | 1x | 0.3 | 2000 | 4 | ark-v4-flash |
| ark-code-preview | Doubao-Code | doubao-seed-2-0-code-preview-260215 | text, code | 1x | 0.7 | 2000 | 4 | ark-doubao-mini |
| ark-lite-old | Doubao-Lite(旧) | doubao-seed-2-0-lite-260215 | text | 1x | 0.7 | 1000 | 3 | ark-doubao-mini |
| ark-lite | Doubao-Lite | doubao-seed-2-0-lite-260428 | text | 1x | 0.7 | 1000 | 4 | ark-doubao-mini |
| ark-turbo | Doubao-Turbo | doubao-seed-2-1-turbo-260628 | text | 1x | 0.7 | 1500 | 4 | ark-doubao-pro |
| ark-evolving | Doubao-Evolving | doubao-seed-evolving | text | 1x | 0.7 | 1500 | 4 | ark-doubao-pro |
| ark-glm-52 | GLM-5.2 | glm-5-2-260617 | text | 1x | 0.7 | 1200 | 4 | ark-glm-flash |
| ark-character-11 | Doubao-Character(11月) | doubao-seed-character-251128 | text | 1x | 0.7 | 1000 | 3 | ark-doubao-mini |
| ark-character-06 | Doubao-Character(06月) | doubao-seed-character-260628 | text | 1x | 0.7 | 1000 | 3 | ark-doubao-mini |

### 配置说明

- 所有模型的 `provider` 字段填 `"ark"`，复用现有 provider 的 apiUrl 和 apiKey
- `tag` 字段统一填 `"免费"`
- 能力标签用英文：`text`（文本）、`reasoning`（推理）、`code`（代码）
- 旧版模型名称后面加 `(旧)` 区分，Character 模型加月份区分
- 模型说明（`modelInfo`）同步补充，格式和现有火山方舟模型一致

---

## 二、新增图片生成模型（3个）

图片生成使用**不同的 API 端点**，需要单独处理：

| 配置ID | 显示名称 | 模型ID | 星级 | 出图速度 |
|--------|---------|--------|------|---------|
| ark-seedream-40 | Seedream-4.0 | doubao-seedream-4-0-20260415 | 4 | 约10秒 |
| ark-seedream-40-new | Seedream-4.0(新) | doubao-seedream-4-0-250828 | 4 | 约4秒，推荐 |
| ark-seedream-50-pro | Seedream-5.0-Pro | doubao-seedream-5-0-pro-260628 | 5 | 约20秒，质量最高 |

### 图片生成 API 配置要求

1. **端点**：`https://ark.cn-beijing.volces.com/api/v3/images/generations`

2. **请求体格式**：
   ```json
   {
     "model": "doubao-seedream-4-0-250828",
     "prompt": "用户输入的描述文字",
     "size": "1024x1024",
     "n": 1
   }
   ```

3. **响应格式**：返回图片 URL，在 `data[0].url` 字段

4. **在 `ai-config.js` 中的配置**：
   - 新建 provider `ark_image`，apiUrl 指向 `images/generations`
   - 模型的 `provider` 字段填 `"ark_image"`
   - `types` 字段填 `["image"]`
   - 增加 `apiType: "image"` 字段标记为图片生成模型

5. **AI 对话页调用逻辑**：
   - 当用户选中图片生成模型时，输入框 placeholder 变为"描述你想生成的图片..."
   - 发送请求调用图片生成端点，拿到图片 URL 后在对话中显示图片
   - 图片生成不支持多轮对话，每次生成都是独立的
   - 生成中显示"正在生成图片..."，超时设为 60 秒（图片生成较慢）

---

## 三、模型列表排序

新增模型插入到现有火山方舟模型后面，顺序建议：

1. 现有 6 个（v4-flash、v4-1-flash、v4-pro、doubao-mini、doubao-pro、glm-flash）
2. 新增文本模型（按推荐度排序：lite、turbo、evolving、glm-5.2、code-preview、v4-pro-old、lite-old、character×2）
3. 图片生成模型（放在最后，用分隔线与文本模型分开，标注"图片生成"）

---

## 四、其他要求

1. 模型选择下拉列表中，图片生成模型和文本模型用分隔线分开，图片模型加 🖼 图标
2. 模型设置页面的模型说明同步补充这 12 个模型的信息（特点、优势、使用场景）
3. 功能分类中，图片生成模型归入"图片生成"分类，code 模型归入"代码"分类
4. 快速/均衡/极致三个模式的模型池可以适当加入新模型（如 lite 加入快速模式，turbo 加入均衡模式）
5. 所有配置写在 `ai-config.js`，不写死在其他文件里
