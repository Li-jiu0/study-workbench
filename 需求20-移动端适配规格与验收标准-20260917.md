# 需求 20 · B 部分：APK 安装后「界面显示与布局」适配规格 + 验收标准

> 归属：R73 批次 · 需求 20 的 B 部分（A 部分「全项目代码审查与缺陷修复」另出问题清单）
> 制定：交付总监（主理人） · 日期：2026-09-17 · **版本 b（修正版）**
> 证据来源：对 `D:/下载的文件/学习工作台` 全部 **42 个根目录页面** 的静态扫描（脚本产物 `tools/qa/r73_page_profile.json`）

---

## ⚠️ 版本修正说明（覆盖前一版，重要）

用户于 2026-09-17 重新下发了需求 20 全文，**纠正了主理人此前的误读**：

| 对比项 | 前一版（错误） | 本版（正确） |
|---|---|---|
| 覆盖对象 | 主理人误把某个外部站点名当作需求原文，又自行「映射」回本项目页面 | 需求原文写的是**「项目里各种界面」**——**就是本项目自己的全部页面**，需求从未涉及任何外部站点 |
| 关注维度 | 只做「布局」适配（溢出 / 遮挡 / 断点） | **「界面显示文字显示」与「布局」并重** |
| 缺陷清单 | 由主理人自行归纳 | 按需求**原文逐字**列出五类必须消除的缺陷：**错位、串行、溢出、遮挡、字体或元素尺寸异常** |
| 判定断言 | A1–A8，全为几何 / 布局断言 | **新增「文字显示」专项断言 A9–A13**（窜行、逐字断行、字号层级、系统字体放大） |

修正后的需求 20 · B 部分原文（用户原话，逐字）：

> 同时，请确保应用打包为 APK 并安装到手机后，**项目里各种界面显示文字显示都与布局均能正确适配，不出现错位、串行，溢出、遮挡、字体或元素尺寸异常等问题**。请明确说明以下内容：需要覆盖的页面范围、适配的目标设备与屏幕尺寸、判定适配是否正常的标准，以及验证适配效果的具体方式。

**主理人自查（如实记录）**：上一版在需求已明写「项目里」的情况下，凭空引入了一个不相干的外部对象并据此「重新界定范围」，属**误读**。本版已彻底删除该表述，全文不再出现那个外部站点名，所有范围与断言均只针对本项目 42 个页面。

---

## 〇、结论摘要（先看这一段）

| 维度 | 客观结论 |
|---|---|
| 页面总数 | 42 个根目录 `.html`（APK 内可访问的全部页面） |
| viewport meta | **42/42 全部具备** ✅（移动端适配基础不存在缺失） |
| 真实硬宽度溢出（无 max/min 前缀的 `width:Npx` 且 ≥380px） | **0 处** ✅（此前担心的 768/900/1100/1323px 均为 `max-width`，属安全写法） |
| 零 `@media` 媒体查询的页面 | **22 页** ⚠️（从未做过窄屏适配，最大风险面） |
| 有 `@media` 但**无 ≤768px 窄屏断点**的页面 | **5 页** ⚠️（断点只覆盖桌面缩放，手机不生效） |
| 「有底部固定栏但静态扫描未发现 padding-bottom 补偿」的页面 | 原 **33 页** ⚠️ → **运行期实测已澄清**：`common.css` 在 ≤768px 已统一给 `padding-bottom:84px`（`calc(64+20)`），**遮挡担忧基本解除**（详见 §0.1） |
| 含 `white-space:nowrap` 且零 `@media` 的页面 | **5 页** ⚠️（窄屏下最易出现「文字撑破容器」与「串行」） |

**本项工作的重心（按需求原文的三个必查方向）**：
1. **布局错位 / 溢出 / 遮挡** → 22 个零断点页面补齐窄屏规则；33 个页面实测确认并消除底部栏遮挡；
2. **文字显示（错位、串行、字体或元素尺寸异常）** → 逐页检查 `nowrap`、flex 子项挤压、固定高度容器、中英混排、题目长文本换行（**这是前一版完全漏掉的一整个维度**）；
3. **可重复执行的自动化验收** → 新增文字断言 A9–A13，与 A1–A8 一起纳入 L2 多视口断言。

### 0.1 运行期实测修订（2026-09-17 · 由 L2 运行时断言链对 26 页 × 320/360/412 三视口跑出的真实基线）

> 工具链：本机 **Chrome 153 无头 + CDP**（Node 22 内置 WebSocket + 本地 http 服务 + 注入登录态绕过 auth 守卫 + 拦截守卫重定向）。这是**真实浏览器排版**结果，优先级高于 L1 静态扫描。

| 修订项 | L1 静态扫描的说法 | **运行期实测结论（以此为准）** |
|---|---|---|
| **`min-width:720/760/768`「全站最硬、必崩」** | §0 / §5.2 曾把 `mock_exam.html` / `mock_exam_result.html` / `mock_exam_run.html` / `企业定向库.html` 列为**最高危「必崩」候选** | ❌ **判定作废**。这些值是 **`@media (min-width:720/760/768px)` 的媒体查询条件（桌面向上）**，被扫描脚本误计入了 `min-width` 列表；它们**不是硬宽度**。三页在 320/360/412 下均 `ovh=no`。**全站真实硬 `min-width` 最大仅 220px**。 |
| **A1/A2 默认态基线** | 无法判定 | ✅ 26 页在 320/360/412 三视口下：`scrollWidth <= innerWidth+1` 全过；**可见元素 0 越界**。→ **默认态无横向溢出** |
| **D 组（底栏遮挡，33 页）** | 「待实测确认，可能真遮挡」 | ✅ **已在 26 页实测确认：无一例真遮挡**。所有含底栏的页面 `.content`/`.main>.page` 实测 `padding-bottom:84px`，来自 `common.css` 的 ≤768px 规则（`calc(64+20)`） |
| **`white-space:nowrap` 5 页（含 `nowrap` 且零 `@media`）** | 「窄屏下最易文字撑破」 | ⚠️ **部分成立**。逐处实测：AI模拟面试×3、面测×2、申论刷题×2、时政热点×4、企业定向库×2、学习概括×1、mock_exam_run×1（已 `ellipsis`）、blog_wechat×1（有意横滚）——**实数据下均未撑破**；按「只改会撑破的」做**定点收敛**，不做无差别改写 |
| **硬 `min-width` 各页下限** | 未分级 | 企业定向库 220/200、面测 160、申论 132/120、时政 200、学习概括 132/160、mock_result 140、mock_run 70/100/120/44 —— **全部 < 320，单条不构成溢出** |

**唯一一处「实数据复现的真实溢出」**：
- 页面 `企业定向库.html`，元素 **`.cl-table`**（详情表），在 **320 宽**下 `right=341 > 320`，`nOff=15`。
- **既定修法**：窄屏内 `.cl-table { display:block; overflow-x:auto; }` + 表头去掉 `nowrap`。

**对后续工作的影响**：
1. **重灾区从「模考三页」改判为「`企业定向库` `.cl-table` + 文字专项 A9–A13」**；模考三页降为常规「补齐窄屏断点」处理，不再按高危专项对待。
2. **L1 静态扫描的口径缺陷已登记**：扫描脚本把 `@media (min-width:…)` 的断点条件误计入「硬 `min-width` 元素」，**凡引用 L1 的 `min-width` 清单都必须以运行期实测复核为准**。
3. 底栏遮挡（原 D 组）**从「主要工作量」降为「常规复核项」**，节省出的精力全部投入文字专项。

---

## 一、需要覆盖的页面范围

### 1.1 范围界定
覆盖 **全部 42 个根目录页面**（= APK 内可访问的所有页面）。按重要性分三档：
**P0 必须逐页人工验收，P1 自动化全过 + 抽验，P2 自动化过即可。**

### 1.2 P0 — 核心链路页（12 页，必须逐页人工过）

| 页面 | 职责 | 本次扫描风险 |
|---|---|---|
| `学习工作台.html` | 首页 / 工作台 | 零窄屏外问题；属 D 组（底栏补偿待确认） |
| `AI.html` | AI 对话主入口 | **nowrap×9 + fixed×7**，全站 nowrap 最多；D 组 |
| `ai-settings.html` | 模型设置 | 有 640px 窄屏断点 ✅；含 `min-width:150/130` |
| `私聊.html` | 私信 / 互动（本批将改名为 `好友.html`） | **nowrap×6 + fixed×3 + min-width:240/190**；有 2 处窄屏断点；D 组 |
| `个人中心.html` | 个人资料与入口（本批升级） | 有 3 处窄屏断点 ✅；含 `min-width:150`；D 组 |
| `设置.html` | 设置中心（155KB，全站最大） | 有 2 处窄屏断点；含 `min-width:180/170`；D 组 |
| `社区.html` | 发帖 / 帖子流 | **零 @media + nowrap×3 + min-width:170**（E 组头号） |
| `动态.html` | 动态广场（本批改造为动态空间） | 有 1 处窄屏断点；D 组 |
| `登录.html` | 登录 / 注册 | 零 @media；`max-width:400` 安全；D 组无关 |
| `我的文件.html` | 文件与题库聚合 | 有 1 处窄屏断点；含 `min-width:150`；D 组 |
| `导入题库.html` | 题库导入（本批验收重点） | **零 @media**，且导入结果表格易溢出 |
| `错题本.html` | 错题与 AI 分析 | **零 @media + nowrap×1 + min-width:150** |

### 1.3 P1 — 功能页（24 页，自动化全过 + 抽验 8 页）

`AI模拟面试.html`（零 @media、nowrap×3）、`面测.html`（零 @media、nowrap×2、min-width:160）、`面试题库.html`（零 @media）、`行测.html`（零 @media）、`行测刷题.html`（零 @media、min-width:132）、`申论刷题.html`（有 @media 但无窄屏断点、min-width:200/132/120）、`四级词汇.html`（零 @media、min-width:180）、`四级经验分享.html`（零 @media、max-width:900）、`英语.html`（零 @media）、`时政热点.html`（无窄屏断点、nowrap×4、min-width:200）、`企业定向库.html`（无窄屏断点、min-width:760/220/200）、`场景话术库.html`（有窄屏断点、max-width:1323 安全）、`万能金句库.html`（零 @media）、`表达.html`（有窄屏断点）、`商务礼仪.html`（零 @media）、`PPT素材库.html`（有窄屏断点、max-width:1200 安全）、`PPT版式库.html`（零 @media）、`PPT案例拆解.html`（零 @media）、`学习概括.html`（有窄屏断点、min-width:160/132）、`学途.html`（零 @media、max-width:520）、`工具.html`（零 @media、max-width:760）、`更多.html`（零 @media，本批新增「赞助」入口）、`好友申请.html`（零 @media、max-width:560、nowrap×2）、`mock_exam.html` / `mock_exam_result.html` / `mock_exam_run.html`（**有 @media 但无窄屏断点；含 `min-width:720/768` —— 全站最硬的 min-width，模考三页是最高危的"必崩"候选**）

### 1.4 P2 — 边缘页（3 页，自动化过即可）

| 页面 | 说明 |
|---|---|
| `管理员.html` | 内部管理页，零 @media，用户面不直接触达 |
| `演示.html` | R72 已清空功能，仅保留跳转，零 @media |
| `blog_wechat.html` | **孤儿页**（全站仅 dev 脚本引用，不在导航中），零 @media、nowrap×1 |

---

## 二、适配的目标设备与屏幕尺寸

### 2.1 设备档位矩阵（5 档，全部必测）

| 档位 | 逻辑分辨率（dp） | 屏幕比例 | 代表机型 | Android 版本 | 角色 |
|---|---|---|---|---|---|
| **D1 兼容下限** | 360 × 640 | 16:9 | 红米 4/5 类老机型 | **8.0 (API 26)** | 最低支持基线，WebView 内核最老 |
| **D2 主流标准** | 360 × 800 | 20:9 | 红米 Note 系列 | 10 | **主判档**，问题最集中 |
| **D3 主流长屏** | 390 × 844 | 19.5:9 | Pixel 5 类 | 12 | 主流比例 |
| **D4 大屏长屏** | 412 × 915 | 20:9 | 主流 6.7 寸 | 13 / 14 | 通知权限与新系统行为 |
| **D5 极窄 / 大屏边界** | 320 × 568 / 600 × 960 | 16:9 / 16:10 | iPhone SE 类浏览器 / 小折叠展开 | — | 上下边界压力测试 |

### 2.2 其他必须覆盖的维度

| 维度 | 要求 |
|---|---|
| **屏幕比例** | 16:9、18:9、19.5:9、20:9、21:9（含长条屏）；分屏状态（高度约减半）须可滚动不崩 |
| **横竖屏** | **竖屏为唯一验收朝向**；横屏仅要求"可用"——不出现内容截断且无法滚动、关键按钮不可点 |
| **系统版本** | Android 8.0 / 10 / 12 / 13 / 14（13+ 需覆盖通知权限弹窗行为） |
| **WebView 内核** | Chrome 76（老设备下限）/ 100 / 最新；内核差异主要影响 `position:sticky`、`gap`、`dvh` 单位 |
| **系统字体缩放** | 默认 1.0 与 **1.3（大字模式）** 两档；1.3 下不得出现文字截断、按钮文字溢出、**行高重叠（串行）** |
| **系统深色模式** | 至少确认不出现"深色下文字不可见"（黑底黑字） |
| **刘海 / 挖孔屏** | 顶栏内容不得被摄像头区域遮挡（需 `safe-area-inset` 或足够上边距） |
| **浏览器（非 APK）** | Chrome for Android 移动版须覆盖；因手机浏览器侧无代理与原生桥，功能可降级但**布局与文字不得崩** |

### 2.3 明确不适用的目标
不做平板横屏优化、不做桌面浏览器适配（桌面端另有既有验收）、不做 iOS 适配。

---

## 三、判定适配是否正常的标准

### 3.0 需求原文五类缺陷 → 断言映射（先看这张，回答"判定标准"）

| 需求原文列出的缺陷 | 由哪些断言判定 |
|---|---|
| **错位** | A2（元素越界）、A9（文本行/元素重叠） |
| **串行** | **A9**（行间重叠 / 文本窜到其它元素位置）、**A10**（异常逐字断行） |
| **溢出** | A1（页面横向溢出）、A4（文本溢出容器）、A11（长串不可断） |
| **遮挡** | A3（底部遮挡）、A5（交互区被遮挡不可点） |
| **字体或元素尺寸异常** | A7（字号下限）、A12（字号层级）、A13（字体放大 1.3×）、A8（元素塌陷） |

### 3.1 断言 A1–A8（布局维度，可在浏览器/WebView 内程序化判定）

除注明外，**每个页面 × 每个视口都必须通过**。

| 编号 | 断言 | 判定式 | 容差 |
|---|---|---|---|
| **A1** | 无横向溢出 | `document.documentElement.scrollWidth <= window.innerWidth + 1` | ±1px |
| **A2** | 无元素越界（错位） | 所有可见元素的 `getBoundingClientRect().right <= innerWidth + 1` 且 `left >= -1` | ±1px；轮播/全屏浮层可白名单 |
| **A3** | 无底部遮挡 | 最后一个可交互元素底边 ≤ `innerHeight - 底栏高度 - 8`；且页面可滚动到末尾，末尾空白 ≥ 底栏高度 | 8px |
| **A4** | 文本不溢出容器 | 非 `text-overflow:ellipsis` 容器满足 `scrollWidth <= clientWidth + 1` | ±1px；单行省略号容器豁免 |
| **A5** | 交互区可点 | 所有 `button` / `a` / `[onclick]` 视口内可点区域 ≥ **32×32 CSS px**，且中心点 `elementFromPoint` 命中自身或其子元素（未被遮挡） | 32px |
| **A6** | 无载入跳动 | 首屏加载完成后 3 秒内水平滚动位置无变化、无横向滚动条闪现 | — |
| **A7** | 字号下限 | 正文 `computed font-size >= 12px`；辅助文字 ≥ 11px | 1px |
| **A8** | 无塌陷 | 有可见子元素的容器 `clientHeight >= 24px`；无 `height:0` 包裹可见内容 | 24px |

### 3.2 断言 A9–A13（**文字显示专项** · 本版新增）

> 这一组直接对应需求原文的「显示文字显示」「不出现错位、**串行**」「字体或元素尺寸异常」。

| 编号 | 断言 | 判定式 | 容差 |
|---|---|---|---|
| **A9** | **无「串行」**（文本行互相压盖 / 文本窜到其它元素位置） | ① 同一容器内相邻文本行：`lineBottom_i <= lineTop_{i+1} + 1`；② 任意两个**非父子关系**的可见文本节点，其 `getBoundingClientRect()` 交叠面积 ≤ 较小者面积的 **20%** | ±1px / 20%（设计意图叠层可白名单） |
| **A10** | **无异常逐字断行** | 元素 `clientWidth >= 2em`（≥2 个汉字宽）且文本长度 > 4 时，各行字符数**中位数 ≥ 2**（即不得出现"一行只挤下一个字"的竖排式断行） | — |
| **A11** | **长串可断，不撑破** | 含连续 ≥ 20 个非空白字符（URL / 英文 / 连续数字）的文本节点：其父容器 `scrollWidth <= clientWidth + 1`，或该节点声明了 `word-break:break-all` / `overflow-wrap:anywhere` / `word-break:break-word` | ±1px |
| **A12** | **字号层级合理** | 正文 ≥ 12px、辅助 ≥ 11px（同 A7）；子级标题字号 ≥ 其所属正文字号；同一 `class` 内字号极差 ≤ 2px（防止同类元素忽大忽小） | 1px |
| **A13** | **系统字体放大 1.3× 仍达标** | 将根字号置为 130% 后重跑 A1–A4 与 A9，须全过；且 `button` 文字不得溢出按钮自身边界 | — |

### 3.3 本项目「串行 / 文字异常」的高频根因（施工时必须逐条排查）

| 根因 | 典型表现 | 检查方式 |
|---|---|---|
| `white-space:nowrap` 出现在窄屏可见元素上 | 文字撑破容器（A1/A11 失败） | grep `nowrap` 逐处确认是否有窄屏解除 |
| flex 子项未设 `min-width:0` | 中文被挤成逐字断行（**A10 失败**） | 对 `display:flex` 容器的直接子元素检查 |
| 容器写死 `height:Npx` + 字号放大 | 文字溢出被裁 / 压到下一行（**A9/A13**） | grep 固定 `height` 且内含文本的元素 |
| `min-width:150/170/200/720` 等硬下限 | 320px 下卡片被挤压变形，文字串行 | grep `min-width` 逐处降级 |
| 中英混排无 `word-break` | 长英文单词撑破气泡 | 查聊天气泡、卡片标题 |
| `line-height` 写死 px 且字号随系统放大 | 行与行重叠（**A9**） | 检查固定 `line-height` 的元素 |
| 题目/选项长文本 | 选项文字与序号错位、跨行窜行 | 逐页检查题库类页面的选项渲染 |

**判定结论口径**：
- **通过** = A1–A13 全过（L2 自动化）+ L3 真机人工无「致命/严重」问题
- **有条件通过** = 存在偏差但已列入「已知偏差白名单」并注明原因与影响（如纯装饰元素）
- **不通过** = 任一断言失败且未白名单

---

## 四、验证适配效果的具体方式（三层验证）

### L1 · 静态扫描（已执行，作为基线）
- **做法**：扫描 42 页 HTML/CSS，统计 viewport meta、`@media` 数量与窄屏断点、硬宽度 / `max-width` / `min-width`、`white-space:nowrap`、`position:fixed`、底栏与 `padding-bottom` 补偿。
- **产物**：`tools/qa/r73_page_profile.json`
- **判定依据**：识别出 B/C/D/E 四组风险页面（见第〇节与 1.2–1.4 表）。
- **局限**：无法区分 `padding-bottom` 是否由 `common.css` 或 JS 补偿 → **D 组 33 页必须靠 L2/L3 定论**。

### L2 · 自动化多视口断言（本次批次执行）
- **工具**：Node + Puppeteer/Playwright（`tools/verifier` 下已有 node 依赖基础）；页面以本地静态服务加载。
- **视口集合（7 组）**：`320×568`、`360×640`、`360×800`、`390×844`、`412×915`、`600×960`、横屏 `800×360`。
- **每页每视口执行**：注入 **A1–A13 全部断言**脚本 → 输出 `页面 × 视口 × 断言` 的 CSV + **全页截图存档**。
- **额外一轮（文字专项）**：将根字号置为 **130%** 后重跑 A1–A4 + A9（=A13），单独出一份 CSV。
- **判定依据**：**A1–A13 100% 通过** 才判该页 L2 通过；失败项须给出失败元素的选择器、坐标与截图。
- **产物**：`tools/qa/r73/` 下 CSV + 截图目录 + 汇总报告。

### L3 · 真机人工验收（依赖用户设备，本批给出清单与判据）
- **测试设备（至少 2 台，理想 4 台）**：

  | 设备 | 要求 | 覆盖档位 |
  |---|---|---|
  | 手机 A | ≤ 360×800 宽，Android 10 | D1 / D2 |
  | 手机 B | ≥ 412×915，Android 13+ | D4（含通知权限） |
  | 手机 C（可选） | 320 宽小屏 | D5 |
  | 平板/折叠（可选） | 600×960 | D5 上边界 |

- **验证步骤（逐页）**：
  1. 安装 R73 APK，进入该页；
  2. 竖屏下从上滚到底：确认无横向滚动条、无内容被底栏遮住、**无文字截断、无文字窜行**；
  3. 触发该页主要交互（按钮/开关/弹层/列表项）：确认可点、无遮挡、**按钮文字不溢出**；
  4. 旋转到横屏再回竖屏：确认布局可恢复、无错位残留；
  5. **系统字体调到最大（1.3）：确认无截断、无行高重叠、无逐字挤行**，再调回；
  6. 对照该页「验收标准」逐项打勾，异常项录屏 + 记录机型/系统/复现步骤。
- **判定依据**：P0 12 页**逐项全过**；P1 抽验 8 页**无致命/严重**；P2 3 页无阻断性布局问题。

---

## 五、逐页适配要求与验收标准

> 通用验收项 = A1–A13（见第三节），下表不再重复，只列**该页专属**的适配要求与专属验收点。
> 「风险」列引用第〇节分组：B=零 @media，C=无窄屏断点，D=底栏补偿待确认，E=nowrap+零 @media。

### 5.1 P0 核心链路页

| 页面 | 风险 | 专属适配要求 | 专属验收点 |
|---|---|---|---|
| `学习工作台.html` | D | 首页卡片/轮播在 360 宽下横向排列改为可换行或横向滚动且不撑破页面；底部导航固定栏不遮挡最后一张卡片 | 5 张统计卡在 360×640 下**不被裁切**；滚到底最后一张卡完整可见；轮播滑动不触发页面横向滚动；统计数字（含百分号/单位）不逐字断行 |
| `AI.html` | B/D | 9 处 `nowrap` 逐一确认在 320 宽下不撑破；7 处 `fixed` 元素（输入区/悬浮球/底栏）互不遮挡，输入区不被底部导航覆盖 | 320×568 下输入框可完整看到并聚焦；悬浮球不压住发送按钮；对话流最后一条消息不被输入区遮挡；**长英文/代码块不被挤成逐字断行** |
| `ai-settings.html` | — | 沿用既有 640px 断点，**不得新增第二个窄屏断点**；排序弹窗在 360 宽下不溢出、居中、内容可滚动 | 360×640 下弹窗左右边距 ≥ 8px 且完整可见；弹窗内 4 个选项可滚动到「恢复默认」；模型行不再竖向挤压（该项已由需求 1 交付） |
| `私聊.html`（→`好友.html`） | B?/D | 会话列表 + 聊天区在窄屏下的切换形态（列表/详情）不得同时挤压；`min-width:240/190` 在 320 宽下须降级为可换行 | 320×568 下好友列表项标题不截断到不可辨识；消息气泡不超出屏宽；输入区不被底栏遮挡；**长消息 URL 可在气泡内断行** |
| `个人中心.html` | D | 卡片网格在 ≤360 宽下由多列降为**单列或双列**；`min-width:150` 卡片不得横向溢出（本批同时做需求 9/15 升级，图标网格小屏每行 3 个） | 360×640 下所有卡片完整可见、可点；滚动到底无内容被底栏遮挡；昵称/签名过长时单行省略而非窜行 |
| `设置.html` | D | 子页（SubpageRouter）切换后滚动位置归零；设置项行在窄屏下标签与控件分两行不错位（本批同时做需求 16 审计） | 每个子页（含隐私与安全）360×640 下无横向滚动；分段控件（seg）可点且不重叠；**长设置项说明文字不与右侧开关重叠** |
| `社区.html` | B/E | 补齐窄屏断点；3 处 `nowrap` 在 320 宽下改可换行；`min-width:170` 降级 | 320×568 下帖子卡片标题完整可读；发帖编辑器工具栏按钮全部可点；**帖子正文长段落正常折行不窜行** |
| `动态.html` | D | 本批将改造为动态空间（信息流 + 我的朋友圈 + 发布页），**新页面必须自带窄屏断点** | 三页在 360×640 下无横向滚动；九宫格图片区不溢出；发布页底部功能栏不遮挡输入框；**正文折叠/展开时文字不跳行** |
| `登录.html` | B | 补齐窄屏断点；`max-width:400` 表单在 320 宽下保持左右 ≥ 16px 边距（本批同时做需求 6 的找回密码弹层） | 320×568 下登录按钮完整可见；键盘弹起时按钮仍可点；**错误提示长文案不撑破容器** |
| `我的文件.html` | D | 模块下拉 + 文件列表在窄屏下不互相挤压；`min-width:150` 卡片降级为单列（本批同时做需求 13 导入联动） | 360×640 下列表项标题与操作按钮均可见可点；下拉展开不超出屏宽；**长文件名截断用省略号而非撑破** |
| `导入题库.html` | B | 补齐窄屏断点；**导入结果/预览表格**必须横向可滚动或改卡片化，不得撑破页面 | 360×640 下表格容器 `overflow-x:auto` 生效、页面本身无横向滚动；导入按钮与文件选择可点；**表头与单元格文字不重叠** |
| `错题本.html` | B/E | 补齐窄屏断点；`nowrap` 与 `min-width:150` 降级；AI 分析面板展开后不遮挡内容 | 360×640 下题目列表项可读；AI 分析面板内容可滚动、关闭按钮可点；**题目长选项不与序号错位** |

### 5.2 P1 功能页（24 页）

| 页面 | 风险 | 专属适配要求 | 专属验收点 |
|---|---|---|---|
| `mock_exam.html` | C（原判高危已作废） | 补齐 ≤768px 窄屏断点。**注：原判「`min-width:720` 必崩」已作废**——720 是 `@media (min-width:720px)` 的断点条件，非硬宽度；三视口实测 `ovh=no` | 360×640 下不出现整页横向滚动，试卷内容可完整阅读；选项可点；**题干长段落折行正常** |
| `mock_exam_run.html` | C（原判高危已作废） | 补齐 ≤768px 窄屏断点。768 同为 `@media` 断点条件（已作废），真实硬下限仅 70/100/120/44；另含 4 处 `fixed` | 360×640 下答题区、计时器、提交按钮三者不重叠且都可见；计时数字不换行 |
| `mock_exam_result.html` | C（原判高危已作废） | 补齐 ≤768px 窄屏断点。720 为 `@media` 断点条件（已作废），真实硬下限仅 140 | 360×640 下成绩数字与图表不溢出；返回按钮可点；百分比与标签不重叠 |
| `申论刷题.html` | C | 补齐窄屏断点；`min-width:200/132/120` 降级；编辑器工具栏可换行 | 360×640 下编辑器可输入、字数统计可见；工具栏按钮不重叠 |
| `时政热点.html` | C | 补齐窄屏断点；4 处 `nowrap` 改可换行；`min-width:200` 降级 | 360×640 下新闻卡片标题不截断；列表滚动流畅无跳动 |
| `企业定向库.html` | C | 补齐窄屏断点；真实硬下限 220/200（<320，不单独致溢）。**760 同属 `@media` 断点条件（原判已作废）**。⚠️ **本页是实测唯一真实溢出处**：详情表 `.cl-table` 在 320 宽下 `right=341>320` → 窄屏内改 `display:block; overflow-x:auto` + 表头去 `nowrap` | 360×640 下**无横向滚动**；企业卡片完整可见；**详情表可横向滚动而非撑破页面** |
| `AI模拟面试.html` | B/E | 补齐窄屏断点；`nowrap×3` 降级；面试对话区与输入区不互相挤压 | 360×640 下对话气泡不超屏宽；麦克风/发送按钮可点；**AI 长回复折行正常** |
| `面测.html` | B/E | 补齐窄屏断点；`min-width:160`、`nowrap×2` 降级 | 360×640 下阶段指示器不撑破页面；控制按钮全部可点 |
| `面试题库.html` | B | 补齐窄屏断点 | 360×640 下题目与答案区可滚动、无横向溢出；长答案折行正常 |
| `行测.html` / `行测刷题.html` | B | 补齐窄屏断点；`行测刷题` 的 `min-width:132` 降级 | 360×640 下题目文本可读；选项按钮整行可点；**图形/资料分析表格不撑破** |
| `四级词汇.html` | B | 补齐窄屏断点；`min-width:180` 降级 | 360×640 下单词卡不裁切；翻卡交互可点；**长英文单词可断行** |
| `四级经验分享.html` | B | 补齐窄屏断点；`max-width:900` 在窄屏降为 100% | 360×640 下正文可读、无横向滚动 |
| `英语.html` | B | 补齐窄屏断点 | 360×640 下各模块卡片完整可见 |
| `场景话术库.html` | — | 保留既有窄屏断点；`max-width:1323` 安全无需处理 | 320×568 下话术卡可读、复制按钮可点 |
| `万能金句库.html` / `商务礼仪.html` / `PPT版式库.html` / `PPT案例拆解.html` | B | 补齐窄屏断点 | 360×640 下卡片不重叠、无横向滚动；金句正文折行正常 |
| `PPT素材库.html` | — | 保留既有窄屏断点；`max-width:1200` 安全 | 360×640 下素材网格单列/双列自适应 |
| `表达.html` | — | 保留既有窄屏断点 | 360×640 下内容不溢出、控件可点 |
| `学习概括.html` | — | 保留既有窄屏断点；`min-width:160/132` 在 320 宽下降级 | 320×568 下图表/统计卡不溢出；**统计数字与单位不错位** |
| `学途.html` | B | 补齐窄屏断点；`max-width:520` 在窄屏降为 100% | 360×640 下无横向滚动 |
| `工具.html` | B | 补齐窄屏断点 | 360×640 下工具网格每个卡片可点且不重叠 |
| `更多.html` | B | 补齐窄屏断点；本批新增「赞助」入口，新页须自带窄屏断点 | 360×640 下入口列表项文案不截断、右侧箭头不被裁掉 |
| `好友申请.html` | B | 补齐窄屏断点；`nowrap×2` 降级；`max-width:560` 降为 100% | 360×640 下申请卡片按钮（同意/拒绝）**均可见可点** |

### 5.3 P2 边缘页（3 页）

| 页面 | 风险 | 专属适配要求 | 专属验收点 |
|---|---|---|---|
| `管理员.html` | B | 补齐窄屏断点（管理后台在手机上以可用为准） | 360×640 下关键操作按钮可点、无横向滚动 |
| `演示.html` | B | 补齐窄屏断点；只需保证跳转按钮可见可点 | 360×640 下中心按钮完整可见 |
| `blog_wechat.html` | B/E | 孤儿页（不在导航），仅要求不与构建/打包冲突 | 360×640 下不崩、无 JS 报错 |

### 5.4 本批新增页面（须一并纳入验收）

本批新建/改造出的页面，**自动继承全部 A1–A13 断言**，且各自须自带窄屏断点：

| 新页面 | 出处需求 | 额外验收点 |
|---|---|---|
| `朋友圈.html`（信息流） / `我的朋友圈.html`（或等效命名） | 需求 11 | 单列垂直流、九宫格不溢出、文字折叠展开不跳行、评论嵌套缩进不挤出屏 |
| `个人资料.html`（或等效页） | 需求 15 | 图标网格小屏每行 3 个、数字滚动动画不撑破卡片、昵称/签名过长单行省略 |
| `赞助.html` | 需求 21 | 收款码图片 `max-width:100%` 自适应、居中、桌面与移动均正常 |
| `好友.html`（由 `私聊.html` 改名） | 需求 14 | 改名后引用全通，布局与文字表现与改名前一致 |

---

## 六、执行与验收节奏（本批落地方式）

| 阶段 | 内容 | 产出 | 责任 |
|---|---|---|---|
| ① 静态扫描（**已完成**） | 42 页风险画像 | `tools/qa/r73_page_profile.json` | 主理人 |
| ② 补齐窄屏规则 | 22 个零断点页 + 5 个无窄屏断点页（含 `mock_exam*` 三页 `min-width` 解除） | 改动清单 + 行尾校验 | 任务八（主线） + 各页归属线 |
| ③ 消除底部栏遮挡 | **已在 26 页运行期实测确认无真遮挡**（`common.css` ≤768px 给 `padding-bottom:84px`）→ 降为**常规复核项**；剩余 16 页由各归属线复核 | 实测对照表 | 任务八 + 各归属线 |
| ④ **文字显示专项排查** | `nowrap`、flex 子项 `min-width:0`、固定 `height`、`min-width` 全量 grep + 逐页修 | 根因清单 + 改动 | 任务八 + 各页归属线 |
| ⑤ 自动化验收 | L2 七视口 × **A1–A13** 断言 + 1.3× 字体轮 + 截图。**运行时断言链已由任务八搭好**（Chrome 153 无头 + CDP + 本地 http + 注入登录态绕过 auth 守卫），QA 直接复用该链扩到 42 页 | CSV + 截图 + 汇总报告 | 独立 QA（严过关） |
| ⑥ 真机验收 | 按第一节 P0/P1/P2 清单逐页过 | 打勾清单 + 录屏 | **用户**（需真机，本批提供清单与判据） |

---

## 七、已知偏差白名单（需在 L2 报告中登记才可豁免）

| 场景 | 豁免理由 | 需登记内容 |
|---|---|---|
| 横向滚动轮播 / 全屏浮层 | A2 越界属设计意图 | 元素选择器 + 截图 |
| 单行省略号（`text-overflow:ellipsis`） | A4 截断属设计意图 | 元素选择器 |
| 舞台类全屏动画容器 | A8 高度由 JS 动态控制 | 元素选择器 |
| 设计意图的文字叠层（如封面标题压在图上） | A9 交叠属视觉设计 | 元素选择器 + 截图 |

---

*本规格为 R73 批次需求 20-B 的验收依据。A（代码审查与缺陷修复）部分的问题清单见同批次《代码审查问题清单》（前端 16 项 / 后端 23 项），两者共同构成需求 20 的完整交付。*
