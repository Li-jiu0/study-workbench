# 交接文档 · 2026-09-17g —— 第五批（R73）执行现状与对接

> 上一份：`交接文档-20260916f-第四批执行现状与对接.md`（其 §0.5 是 09-17 凌晨的收尾增补）。
> 本份覆盖 **R73 全批 + 当日夜间增补**，实测时点 **2026-09-17 20:41**。
> 配套：`更新日志-20260917-第五批-R73.md`（改动明细与踩坑全记录）、`给接手AI的开场指令-20260917g.txt`。

---

## §0 30 秒速览

- **代码层：全部落盘，无欠账。** 本批改了 **47 个根 HTML 中的 36 个 + 12 个 assets 文件 + 8 个 server 文件 + 4 个 android 文件**。
- **三件"最后一公里"没做**：**版本戳未打**（下一版 `20260917b`）、**未部署**到 `110.42.134.62`、**未 git 提交**。
- **APK 已重打**：`versionCode 20 / versionName 1.19`，91.8 MB，09-17 17:35:29。**真机验收未做。**
- **一个质量欠账**：任务二十三 的正式断言报告未产出（工装自身编码损坏），需重跑。
- **两个提交红线**：`git add 我的动态.html`（未跟踪）、`git rm --cached 学习工作台-安卓App.apk`（96 MB 二进制已被跟踪）。
- **一个必须填的配置**：服务端 `.env` 的 SMTP 凭证（不填则需求 6 邮箱功能在真机上只会报发送失败）。

---

## §1 行尾铁律（本批实测值，改文件前必查）

**根目录 `.html`（含 `ai-settings.html`）与 `assets/*.js`、`assets/*.css` = CRLF**，唯一例外：

| 文件 | 行尾 | 本批实测 |
|---|---|---|
| `assets/ai-service.js` | **LF** | 80609 B |
| `assets/ai-page.js` | **LF** | 90923 B |
| `assets/ai-settings.js` | **LF** | 99573 B |
| `ai-settings.html`（根目录） | **LF** | 34735 B |
| `assets/xt-profile.js` | CRLF | **CR=1648 / LF=1648**（已核） |
| `assets/xt-profile.css` | CRLF | 862 行 |
| `assets/api.js` | CRLF | CR=1682 |
| `assets/chat-local.js` | CRLF | CR=4177 |
| `assets/app.js` | CRLF | **CR=9642 / bareLF=0**（已核） |
| `assets/subpage-router.js` / `xt-moments.js` | CRLF | CR=249 / CR=931 |
| `好友申请.html` | CRLF | CR=181 |
| `server/**/*.py` | CRLF | — |
| `android/*.xml` / `*.java` | 各自现状，改前探测 | — |

**必须二进制读写**：`open(p,'rb')` → 字节级替换 → `open(p,'wb')`。禁止 text 模式（会把 CRLF 转 LF）。
**禁止用 Edit 工具对同一文件并发多处**（last-write-wins，只有最后一处生效）→ 逐条串行。

---

## §2 本批改动实况（含备份，可直接回滚）

### 2.1 前端资产（12 个）

| 文件 | 大小 | mtime | 行尾 | 本批改了什么 |
|---|---|---|---|---|
| `assets/app.js` | 7082874 B | 20:32:06 | CRLF | `PAGE_FILES`/`pageTitles` 补 `moments` 键 |
| `assets/api.js` | 112319 B | 19:40:52 | CRLF | `openUserHome` 改指新页；`renderProfilePage` 缺口1 拆三段 |
| `assets/chat-local.js` | 227473 B | 19:41:32 | CRLF | 新增 `imOpenPeerHome`；`imShowUserProfile` 403 回退；更正过时注释 |
| `assets/xt-profile.js` | 81382 B | 19:34:51 | CRLF | 16 个 TA 视角函数 + `boot()` 分叉 + 归并视觉/裁剪/改名 |
| `assets/xt-profile.css` | 26371 B | 19:10:15 | CRLF | 滚动豁免 + 裁剪舞台 340 + 视觉升级 + 560/400/340 三档 + 深色 token |
| `assets/xt-moments.js` | 45613 B | 19:41:05 | CRLF | `:901 openUser` 改指新页 |
| `assets/subpage-router.js` | 9743 B | 19:33:43 | CRLF | 面包屑保 `user=` |
| `assets/common.css` | 125165 B | 18:19:11 | CRLF | 6 处窄屏根治（768/480 断点内） |
| `assets/ai-service.js` | 80609 B | 19:06:23 | **LF** | `safeRespJson` 等非 JSON 容错 |
| `assets/ai-page.js` | 90923 B | 19:09:46 | **LF** | `parseJsonOrFriendly` |
| `assets/ai-settings.js` | 99573 B | 17:03:53 | **LF** | 与 ai-settings.html 配套 |
| `assets/importer.js` | 36115 B | 17:32:01 | CRLF | 需求 13 导入联动 |

### 2.2 根 HTML（36 个被改 + 2 个新建）

- **侧栏入口**：32 页各插 1 个「动态空间」项（20:32 前后，逐页 `data-page="moments"` = 1）
- **更多面板**：31 页各插 1 个入口（`AI.html` 确认无 `morePanel`）
- **`ai-settings.html`**：viewport + row-right 右贴齐 + 全启/全停 + 400/360 两档断点（19:03:00）
- **`学途.html`**：错题/待复习文案修正（18:23:45）
- **`登录.html`**：邮箱绑定入口（17:12:17）
- **`动态.html`**：入口卡片区 4 处文案 + `moOpenUser` 改指新页（19:41:23）
- **`好友申请.html`**：页内 shim + 头像 onclick（19:41:38）
- **`我的动态.html`**：**新建**（由 `我的朋友圈.html` 改名，19:24:51）
- **`个人资料.html`**：内联 JS 清零（19:10:49，2353 B）
- **`设置.html`**（163569 B）、`更多.html`（18699 B）、`社区.html`（42994 B）、`我的文件.html`（53944 B）、`导入题库.html`（22862 B）等

### 2.3 后端（8 个）

`server/routers/moments.py`(22946, 17:21:07) / `auth.py`(14380, 17:14:46) / `admin.py`(14587, 17:14:15) / `rate_limit.py`(4502, 17:13:54) / `mailer.py`(**新建**, 3665, 17:12:17) / `schemas.py`(13290) / `database.py`(28851) / `config.py`(5016)

### 2.4 安卓（4 个）

`AndroidManifest.xml`(3316, 20/1.19, 新权限) / `MsgPollService.java`(12239, 前台服务) / `MainActivity.java`(52648) / `build_apk.py`(11074)

### 2.5 备份位置

- 仓库外：`D:\下载的文件\_r73_bak_20260917\`（任务十 的原文件副本，不影响仓库，需要时可删）
- 各 `.bak-pre-rXX-20260917` 就地备份（**不要清理**，回滚要用）

---

## §3 接手必读的数据契约

**详见 `更新日志-20260917-第五批-R73.md` §七**，此处只列最易踩的 3 条：

1. **`PAGE_FILES` / `pageTitles` 必须成对登记**。新增可导航模块漏登记 → `navigateTo()` 只打 `console.warn('未知页面：xxx')` **原地不动**。这就是动态空间"上线不了"的元凶。
2. **TA 视角 URL = `个人资料.html?user=<服务端数字 uid>`**。必须 `/^\d+$/` 校验；判自己用 `GET /api/users/{uid}` 的 `isMe` 字段。**`xt-profile.js:396-413` 那个 6 位「用户ID」是本地哈希假 ID，不能当 uid 用。**
3. **`renderProfilePage` 同名重复声明三份**：`api.js:220`（无效）、`api.js:1327`（**生效**）、`app.js:8102`（无效）。**改错一份静默无效。**

---

## §4 执行状态与待办（**接手者即办清单**）

### 🔴 P0 — 阻断上线，必须做

| # | 事项 | 具体动作 | 验收标准 |
|---|---|---|---|
| 1 | **打版本戳** | 全站 bump 到 **`20260917b`**，**只 bump 本批内容真改过的资产** | 戳值 + 改动页数/处数；`\.js\.js\?v=` 扫 0；旧戳残留 0；行尾零变化；escheck DONE 0 |
| 2 | **填 SMTP 凭证** | 服务端 `.env` | 邮箱验证码能真发出去 |
| 3 | **git 提交** | ① `git add 我的动态.html`（**必须**，未跟踪）② `git rm --cached 学习工作台-安卓App.apk` ③ push 走代理 | `git status` 里新文件已被跟踪；远端无 96 MB 二进制 |
| 4 | **部署上线** | 推到 `110.42.134.62` `/opt/study-workbench/` | 用户点头后再做（生产操作先问用户） |

### 🟡 P1 — 质量验收欠账

| # | 事项 | 具体动作 |
|---|---|---|
| 5 | **APK 真机验收** | 装 `学习工作台-安卓App.apk`（20/1.19，91.8 MB，09-17 17:35:29），验证：连接服务器 / 好友私信 / 留言板 / 社区互动 / 语音 / 题库加载 / **后台通知**。异常时先查 `android/build_apk.py` 的打包断言与资源白名单，**不要瞎猜网络问题** |
| 6 | **重跑任务二十三 断言** | 工装 `tools\_r73_qa23_verify.js` 自身编码坏了（中文路径字面量被按 GBK 写出 → `ENOENT`）。改用 `tools\_r73_qa23_verify.py`（15440 B）+ 显式 `encoding='utf-8'`，产出 `tools\_r73_qa23_result.txt`。**`_r73_qa23_baseline.json`（5849 B）是有效的改动前基准，直接拿来比对**。重点断言：`app.js` moments=2 且落在两个区块内（**区间截取匹配，不要全文 grep**）；32 页计数=1；**插入位置断言**（必须落在「社区」项之后、下一个 `nav-section`/`nav-item` 之前，不跨 `</aside>`/`</nav>`/`</body>`/`id="morePanel"`）；E1 回归护栏（`PAGE_FILES` 原 20 键 + `pageTitles` 原键一个不丢、值不变） |
| 7 | **R67 遗留** | `AI_CONFIG.providerGroups` 第二层厂商模型展示名与设置页下拉文案对齐 |

### ⚪ P2 — 小尾巴

| # | 事项 |
|---|---|
| 8 | `assets/api.js:354-355` 空壳 `if`（两分支内容相同），无害待清 |
| 9 | `工具.html` 也有 `.morepage-list`，本批只给 `更多.html` 补了动态空间卡片 → **口径未裁定** |
| 10 | `用户需求与决策总账.md`（CRLF，98 KB）补记 **R68 / R70 / 本批 R73** |
| 11 | 用户侧：`ai.hub.camera` 端点已死需换；`ADMIN_PASSWORD`、6 个平台 AI Key 轮换 |

---

## §5 接手三步走

**第一步：读三份文件，不要凭记忆开工**
1. 本文件（§0 → §4 即办清单）
2. `更新日志-20260917-第五批-R73.md` —— **§七 数据契约、§八 踩坑、§九 未完成清单**
3. `用户需求与决策总账.md` —— 需求编号口径（本批新增备忘待补）

**第二步：先验证，再动手**

```powershell
# 落地情况一眼看清（这几条是主理人自己用过的核验命令）
$p="D:\下载的文件\学习工作台"
# app.js 路由
[System.IO.File]::ReadAllText("$p\assets\app.js") -match "moments: '动态.html'"
# 32 页侧栏计数（应全 = 1）
Get-ChildItem "$p\*.html" | ForEach-Object { $t=[System.IO.File]::ReadAllText($_.FullName); $n=([regex]::Matches($t,'data-page="moments"')).Count; if($n -gt 0){ "$($_.Name) = $n" } }
# 行尾
$b=[System.IO.File]::ReadAllBytes("$p\assets\xt-profile.js"); ($b|?{$_ -eq 13}).Count; ($b|?{$_ -eq 10}).Count
```

**第三步：按 P0 → P1 → P2 顺序推进。** 打戳/部署属于生产操作，**动之前先问用户**。

---

## §6 陷阱清单（本批新增，务必先读）

1. **`renderProfilePage` 三处同名声明，只有 `api.js:1327` 生效。** 改错静默无效。
2. **同一文件多处 Edit 并发 = last-write-wins**，只有最后一处生效。
3. **不要为了"加一个跳转函数"给页面挂 `assets/api.js`**：实测 `apiBoot` 会额外打 `/api/auth/me`、`/api/notifications`、`/api/chat/unread` 并起 30s/60s 两个轮询。3 行页内 shim 更正确。
4. **`git mv` 对未跟踪文件直接 exit 128** → 用「复制 + 删旧」回落，并显式 `git add` 新文件。
5. **PowerShell 工具 stdout 会被吞成空** → 一切结果先 `Out-File` 落盘再 Read。
6. **Node 脚本里的中文字符串字面量会因编码不一致而损坏**（曾产出乱码路径 → `ENOENT`）。含中文路径的批量脚本**优先用 python + 显式 `encoding='utf-8'`**，或把中文拆成字符数组拼接。
7. **`jsdom 30` 不导出 `ResourceLoader`** → 把页面真实源码内联后执行；`localStorage` 需 http origin（起临时本地静态服务）。本机 jsdom 在 `C:\Users\ATM\node_modules\jsdom`。
8. **`jsdom` 的 `location` 只读、无法拦截导航** → 用作用域遮蔽或 `Object.defineProperty` 抓目标 URL。副作用：`location.replace()` 不真跳，`isMe` 回落会暂留「加载中…」，**属测试环境产物，不是缺陷**。
9. **批量脚本按纯类名扫描会漏判**：`学习工作台.html` / `设置.html` 的 `.bottom-more-grid` 带 `data-page-node-id`。
10. **唯一可写代码树 = `D:\下载的文件\学习工作台`**。`C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-*` 是**停在 R70 的过期空壳**，本批已发生一次"跑错树导致整批结论作废"的事故。
11. **CSS 禁 `clamp()/min()/max()`**（老 Android WebView 不支持），一律固定值 + `@media` 分级。
12. **`assets/app.js` 第 665 行是一条约 2.7 MB 的超长行**，任何批量脚本都不要碰它。

---

## §7 需求台账指引

- 主台账文件：`用户需求与决策总账.md`（**CRLF**，98 KB，现止于 R67/638 行附近）
- 待补：**R68（移动端 AI 对话框布局 4 commit）/ R70（连通性检测 + APK 重打）/ 本批 R73（21 项需求 + 夜间 12 项增补）**
- 补记方式：二进制读写，先备份 `.bak-pre-r68-20260917`
- 本批规格文档：`需求20-移动端适配规格与验收标准-20260917.md`（32101 B，含 42 页逐页画像与三档验收标准）

---

## §8 文档编号关系（本目录）

| 文件 | 定位 |
|---|---|
| `更新日志-20260916-第四批-R64至R67.md` | 上一批改动明细（**§八 是 R68/R70 收尾实录**） |
| `交接文档-20260916f-第四批执行现状与对接.md` | 上一批交接（**§0.5 最有用**，§1~§9 是 22:25 快照，部分已过时） |
| `给接手AI的开场指令-20260916f.txt` / `-20260917a.txt` | 上一轮开场指令 |
| **`更新日志-20260917-第五批-R73.md`** | **本批改动明细（本份）** |
| **`交接文档-20260917g-第五批执行现状与对接.md`** | **本批交接（本份）** |
| **`给接手AI的开场指令-20260917g.txt`** | **本批开场指令** |
| `需求20-移动端适配规格与验收标准-20260917.md` | 需求 20-B 施工依据 |
| `需求文档-各平台模型状态更新.md` / `需求文档-火山方舟新增模型.md` | 用户提供的需求输入 |

---

## §9 收尾声明（本轮主理人 · 2026-09-17 20:41）

**代码层已收口，未做的是"发布链路"与"验收链路"，不是功能实现。**

已由本任主理人**亲自实测落盘**的（不是转述成员自述）：

- `assets/app.js` 7082874 B，`moments` = 2，两处分别在 `PAGE_FILES` / `pageTitles` 区块内，CRLF=9642 / bareLF=0
- 32 页侧栏 `data-page="moments"` **32/32**；31 页 `.bottom-more-grid` 内「动态空间」**31/31**
- 4 个动态页 mtime 未变（无误伤）
- `assets/xt-profile.js` 81382 B / CR=1648 / LF=1648；`boot()` 分叉原文正确
- `assets/api.js:1327-1350` 缺口1 拆判定原文正确；`需登录后查看` ×1；`个人资料.html?user=` ×3
- 活文件里 `个人中心.html?user=` 仅剩 **3 处注释**，无代码路径
- `我的动态.html` 11531 B 存在、`我的朋友圈.html` 不存在

**未由本人抽验、需接手者自行复核的**：需求 20-B 的 A1–A13 逐条验收、需求 20-A 审查报告全清单、需求 1/2/3/4/5/7/8/9/10/12/13/14/15/16/17/18/19/21 的逐条实现与自测结论 —— 这些**有落盘足迹（见 §2），但结论来自成员自述**。

**警告（沿用上一轮口径，本批依然成立）**：

- 不要把「开发自测通过」写成「已验证通过」。
- **APK 目前只有打包侧验证（manifest 版本号 + 体积 + apksigner），真机功能验收未做。**
- 不要把平台密钥说成"已保密"——6 个平台 Key **明文硬编码在 `assets/ai-config.js`**，前端直连架构下无法真保密；已交付的只是「UI 不回填 + 不复制进 localStorage + 清洗存量副本」。
- 不要顺手刷新未改动资源的版本戳（项目审计靠 mtime，无谓改动会被误判）。
- 不要清理任何 `.bak-pre-r*` 备份（回滚要用）。

**一句话：代码、APK 包全部就绪；先打 `20260917b` 戳 + 填 SMTP + `git add 我的动态.html` + 部署，再陪用户跑 APK 真机验收，最后补任务二十三 断言报告与台账。**
