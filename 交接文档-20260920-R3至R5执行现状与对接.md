# 交接文档 · R3–R5 执行现状与对接（2026-09-20）

> 面向下一位接手者（人或 AI）。看完这页即可接手，无需回溯对话。
> 前置：`交接文档-20260920-R2A至R2C执行现状与对接.md`
> 配套：`更新日志-R3至R5-20260920.md`（同目录）

---

## 一、一句话现状

**R3（5 条线：聊天界面/位置共享/横幅通知/设备统计/风控）、R3b（4 条线：观感稳定性/网络健壮性/长按引用+失败重发/安全收紧）、R4（4 条线：公共基建/死按钮/弹层压栈/图标防闪）三条线已随 v1.34 上线生产；R5（用户直接要求的「表情面板重做」）代码完成 + 主理人独立核验通过 + QA 独立复核在跑。本轮按用户要求「先出文档」，部署与 GitHub 同步均未执行。最高优先待办仍是密钥轮换。**

| 项 | 值 |
|---|---|
| 线上站点 | http://110.42.134.62 |
| 当前版本 | **1.34 / versionCode 35**（上一版 1.33 / 34） |
| 拟发版本 | **1.35 / versionCode 36**（R5 表情面板，**未执行**） |
| APK（线上） | `星途-1.34.apk` 87,244,449 B，签名证书 SHA-256 `2b95937e…`（与 1.33 同证书，可覆盖安装） |
| 代码仓库 | https://github.com/Li-jiu0/study-workbench ，远端 `main` = `be18396e` |
| 本地 HEAD | `526b391`（R4 批次提交，**未 push**） |
| 本地待提交 | **19 个文件**（R5 的 4 个 + R3/R3b/R4 遗留的 15 个，见第四节） |
| 更新日志 | `更新日志-R3至R5-20260920.md` |

### 发布状态

| 项目 | 状态 |
|---|---|
| R3 / R3b / R4 代码 | ✅ **已上线**（v1.34），但**未入库** |
| R5 表情面板代码 | ✅ 完成 + 主理人核验通过；QA 独立复核在跑 |
| R5 部署上线 | ⏸ **未执行**（用户要求先出文档） |
| 本地 git 提交 | ⏸ 未提交 |
| GitHub 同步 | ⏸ 未执行（计划走**增量同步分支**，不 force） |

---

## 二、🚨 安全待办（最高优先级，未变）

### 2.1 `server/.env` 的密钥早已随 GitHub 历史公开
- **事实**：`server/.env` 由初始提交加入、commit `848d642` 移除；两个提交**都已是远端祖先** → blob 早已在 GitHub 上。
- **后果**：全部模型供应商 Key + `JWT_SECRET` + `ADMIN_USERNAME` / `ADMIN_PASSWORD` 应视为**已泄漏**。
- **唯一补救是轮换**：强推 / `git-filter-repo` **删不掉 GitHub 上按 SHA 可达的旧对象**。
- **轮换注意**：`JWT_SECRET` 一换，**现有全部登录态立即失效** → 挑低峰期。
- **轮换方式**：在各自平台重新生成 → **只在服务器 `server/.env` 替换**（`pscp` 单文件上传后用远端 python 写回；**绝不用本地 `.env` 覆盖生产**，两边键不同：本地有 `ADMIN_TOKEN`，线上有 `ADMIN_PASSWORD` / `ADMIN_USERNAME`）。

### 2.2 `assets/ai-config.js` 的 Gemini Key 线上公开（活跃泄漏）
- `gemini` provider 的 `apiKey` 是真值且 `keyInQuery: true`；该文件是**公开静态资源**。
- **关键认知**：R131「前端零密钥」**并未覆盖 `needVPN` 的海外平台分支** —— 不要默认前端已无明文 key。
- **处置（待拍板）**：① 轮换 + 改后端中转（代价：重部署 + 重打 APK）；② 接受风险保持直连。

### 2.3 其他
- 远端 `refs/tags/1.0` 仍指向旧初始提交 → 旧对象持续可达；**是否删除待拍板**。
- 本地历史仍含明文密钥 → 每次 push 须走净化流程。
- **纪律**：provider key **只准进服务器 `.env`**，不进前端、不进 git。
- **被 GitHub 拦时绝不点 `unblock-secret` 链接放行** —— 那是公开密钥。

---

## 三、代码树与生产环境事实

- **唯一可写代码树**：`D:\下载的文件\学习工作台`（分支 `main`）。
  ⚠️ 会话工作区（`C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-cab2edc3`）可能停在旧提交 → **判断"某文件到底是什么版本"只认真源绝对路径的 md5**。
- **生产**：`110.42.134.62`
  - Nginx root：`/opt/study-workbench/web/`（前端在 **`/opt/study-workbench/web/assets/`**，不是 `/opt/.../assets/`，写错会得假失败）
  - 后端：`/opt/study-workbench/server/`；systemd `study-workbench`（uvicorn 单进程；改后端需 `systemctl restart study-workbench`）
  - `/api/` 反代 → `127.0.0.1:8000`；APK 静态目录 `/opt/study-workbench/web/static/apk/`
  - 备份目录 `/opt/study-workbench/backups/<批次>-<时间戳>/`
- **凭据取法**：从 `.gitignore` 忽略的 `upload_v23.ps1` 正则取（`pscp\.exe"\s+-pw\s+"([^"]+)"[^\n]*?root@([0-9.]+):`）→ **绝不 print 明文、绝不写进入库文件**；`tools/pscp.exe` / `tools/plink.exe` 写全路径；`-hostkey SHA256:i2DNTi3iEPauuTxvJEPlZ4jLf8K0LdDsaijQxArUU1M`。
- **禁止上线**的运行时数据：`server/.env`、`server/data.db`、`server/data/model_usage.json`、`model_quota.json`、`geo_place_count.json`、`geo_staticmap_count.json`、`feedback.json`。

---

## 四、⚠️ 本批的特殊性质：15 个「已上线但从未入库」的文件

**这是本次交接最重要的一条认知。**

`git status` 显示 **19 个文件**相对 `HEAD=526b391` 有未提交改动，但主理人用**段落级 hunk 归因**实测判定：

| 类别 | 文件 | R5 hunk 数 | 说明 |
|---|---|---|---|
| **R5 本轮真改** | `assets/chat-local.js` / `assets/common.css` / `assets/emoji/manifest.js` / `私聊.html` | 15 / 4 / 2 / 4 | 表情面板重做 |
| **历史遗留未入库** | 其余 **15 个** | **0** | 见下表 |

| 文件 | 新增/删除 | 归属批次 |
|---|---|---|
| `android/java/com/study/workbench/MainActivity.java` | +587 / −37 | R3-L1/L3/L5 + R3b-A/D |
| `android/java/com/study/workbench/MsgPollService.java` | +81 / −11 | R3-L3 |
| `assets/admin.css` | +125 / 0 | R3-L4 |
| `assets/admin.js` | +263 / −4 | R3-L4 |
| `assets/api.js` | +189 / −5 | R3b-B（15s 超时） |
| `assets/app.js` | +77 / −2 | R4-C（弹层压栈） |
| `assets/error-boundary.js` | +48 / −4 | R3b-A |
| `assets/icon-map.js` | +33 / −7 | R4-D（防闪烁） |
| `assets/xt-toast.js` | +26 / −3 | R4-A |
| `server/database.py` | +54 / 0 | R3b-C（引用四列） |
| `server/main.py` | +4 / 0 | R3-L4/L5 路由注册 |
| `server/routers/chat.py` | +98 / −5 | R3b-C |
| `server/routers/groups.py` | +24 / −3 | R3b-C |
| `server/routers/liveloc.py` | +17 / −7 | R3-L2（`silent` 入参） |
| `server/schemas.py` | +15 / 0 | R3b-C（`ReplyIn`） |

**结论**：这些改动**已经运行在生产上**（v1.34 部署时随全量差异一起上传），只是从未写进 git 历史。本次发版把它们一并入库，使仓库与生产第一次对齐。

> **接手建议**：`git status` 里的 ` M` 文件**不能**简单当成"本轮新改的"；**必须做 hunk 级归因**再判断归属。本次工装：`_r5_attrib3.py`（段落窗口 + 批次关键词表）。

---

## 五、R5 表情面板重做 —— 技术事实（供复核与接手）

### 5.1 被测文件与快照（字节级冻结）

| 文件 | R5 后 md5/size/lines | 前快照 `.bak-pre-emo-20260920` | 行尾 |
|---|---|---|---|
| `assets/chat-local.js` | `D1C92F48` / 353178 / 6410 | `3ABDFF12` / 337605 | CRLF（loneLF=0） |
| `assets/common.css` | `C983D9F5` / 157714 / 3348 | `0CA76CF4` / 154756 | CRLF（loneLF=0） |
| `assets/emoji/manifest.js` | `087D0626` / 6438 / 122 | `864830CA` / 2371 | CRLF（loneLF=0） |
| `私聊.html` | `1AA0954E` / 93053 / 1367 | `723C8AE0` / 91768 | **LF**（crlf=0） |

**发布前快照**：19 个文件均已冻结为 `<原名>.bak-r5pack-20260920`（md5 逐项 verify=True），回滚有精确字节级依据。

### 5.2 关键实现锚点（行号）

| 功能 | 文件:行 | 说明 |
|---|---|---|
| 面板 CSS（新） | `common.css:2156-2198` | `position:fixed;left:0;right:0;bottom:0` + `translateY(110%)→0` + `58vh/58dvh` 双声明；矮屏 `72vh`（L2198） |
| 遮罩 | `common.css:2163` | `.im-emoji-mask{position:fixed;inset:0;...z-index:210}` |
| 长按菜单 | `common.css:2193` | `.im-emoji-sticker-menu{position:fixed;z-index:212;...}` |
| 面板开关 | `chat-local.js:5084`（close）/ `:5096`（toggle） | `classList.add/remove('open')` |
| 返回键压栈 | `chat-local.js:5017`（`EMOJI_LAYER_ID`）/ `:5082`（push）/ `:5091`（pop） | 接 `assets/layer-stack.js` |
| 网格事件委托 | `chat-local.js:5162-5200+` | `emojiBindGrid`：500ms 长按 / 12px 移动取消 / `longFired` 屏蔽 click |
| 自定义表情渲染 | `chat-local.js:5150-5156` | `data-sticker` 索引；满 24 张隐藏「＋」 |
| **发送自定义表情** | `chat-local.js:5353-5378` | **`imSendSticker` → dataURL→Blob→File → 复用 `window.imSendImageFile`（L5373）** |
| 长按菜单定位 | `chat-local.js:5386-5410` | `getBoundingClientRect()` + 左右收边 + 上方不够翻下方 |
| 既有发图链路 | `chat-local.js:2468`（upload）/ `:2489`（post）/ `:2531`（local）/ `:2555`（file） | `MAX_IMAGE_BYTES = 5MB`（L2466） |
| 渲染（未改） | `chat-local.js:2155-2166` | 只做 `[emoji:code]` → Unicode char |

### 5.3 主理人独立核验结论（已亲验，非采信他人）

| 检查项 | 结果 |
|---|---|
| 面板从 absolute 小浮层 → fixed 全宽底部 | ✅ |
| 旧 `:hover` 收藏星已删除（`.im-emoji-fav:hover` = 0；仅留 `.im-emoji-item:hover` 的桌面端背景反馈） | ✅ |
| manifest 96 个 / 无重复 / `e001`–`e024` 未变 | ✅ |
| 收藏链路（`EMOJI_FAV_KEY` / 上限 24 / toast / 刷新） | ✅ |
| 自定义表情（`#imStickerInput` / 压缩 / localStorage ≤24） | ✅ |
| **发送走 `kind='image'` 上传通道，非文本通道** | ✅ **（最关键的一条，已消除"乱码"风险）** |
| 长按菜单视口边界收敛 | ✅ |
| 渲染零污染（`sticker:` 出现 0 次） | ✅ |
| 红线：`clamp()/min()/max()` 函数 0、`@supports` 0、`prompt/alert/confirm` 0、`?.`/`??` 0 | ✅ |
| 行尾守恒（三文件 CRLF、`私聊.html` LF，loneLF=0） | ✅ |
| 未越界（`MainActivity` / `AndroidManifest` / `version.json` / `xt-update.js` 未碰；`?v=` 未动） | ✅ |

### 5.4 🔴 本批最大的技术争议：`text-size-adjust` 可能无效

- **QA 独立复核**（W3C css-env-1 官方解释器 + MDN 兼容表）：Android WebView 字体放大走 **`systemFontScale` → `setTextZoom()`**；`text-size-adjust` 在 Android WebView **尚不生效**（crbug.com/419469463），且该属性 **WebView Android 54+** 才支持，而 `minSdk 21`（WebView ≈37）**不支持 → 声明可能被整条忽略**。
- **主理人核实**：`MainActivity.java` 的 `setTextZoom` **4 处全是注释**，代码**刻意不调用**，理由写在源码里：
  > 「若在此写死 `setTextZoom(100)` 反而会禁用系统字号跟随、破坏无障碍大字模式 —— 故保持默认跟随系统。」
- **⇒ 这是取舍，不是 bug**：保留无障碍大字模式（现状）↔ 全站尺寸绝对统一（需 `setTextZoom(100)`，牺牲无障碍）。
  **必须由用户拍板，不可静默修改。**
- QA 另建议：`text-size-adjust: 100%` → `none`（语义更精确，规范里 `none` 才是"禁用 inflation"）。**低成本，建议下次一并改。**

### 5.5 已知预期性失效

`tools/verifier/verify_increment_0911.js`（2026-09-11 产物）有两条断言 `panel.style.display === 'block'/'none'` —— 表情面板现改为 `class` 开关，**这两条必定失败**。**属预期性失效，非行为回归**，已被 `tools/qa/_r5_emoji_jsdom.js` 取代。建议单独一批更新该 legacy 断言。

---

## 六、架构红线（碰之前先读）

1. **站点是纯 HTTP，不上 HTTPS**（用户明确决策）→ Web 端 geolocation 恒被浏览器拒 → 精确位置、实时共享**只在 App（`file://`）成立**；**禁止用 IP 粗定位冒充**。
2. **App 是 `file://` 壳**：`MainActivity` 用 `web.loadUrl("file:///android_asset/学习工作台.html")` → **后端改动 App 用户立即生效；前端改动必须重打 APK**。
3. **`android/build_apk.py` 已是联网版**：有构建期硬断言「入口页必须含 `assets/api.js` + `assets/config.js`」。**新增核心 `assets/*.js` 必须同步登记进 `REQUIRED_ASSETS`**，否则「代码修好但包里没有」。
4. **模型解析链**：`body.modelId` → `resolve_model_name()` 查 `server/data/model_registry.json`（106 条）→ 失败用 `body.modelName` → 再失败回退 `.env` 默认（响应头 `X-Ai-Model-Fallback` 提示）。
5. **腾讯地图配额红线**：`geocoder/v1` 6000/日；`place/v1/*` **仅 200/日**（只走 `/api/geo/place` 代理，`GEO_PLACE_DAILY_CAP=150` 熔断；`place/v1/explore` 全站禁调）；Key 是 **IP 白名单制** → 探测必须落服务器。前端**绝不直连** `apis.map.qq.com`。
6. **限流桶**：`rate_limit("geo")` = 30/分/IP **且缓存命中也计数** → 新业务不要挂这个桶。
7. **CSS / 前端红线**：禁 `clamp()/min()/max()` **函数**（`min-height/max-height` 属性可用、`minmax()` 可用）；禁 `@supports`（全站 0，用"双声明"惯用法替代）；`vh` 在 fixed 层安全；**ES2017 上限**（`node tools/qa/escheck_es2017.js` 必须 `DONE 0`）；禁 `prompt/alert/confirm`。
8. **编辑纪律**：
   - 改前**二进制检测行尾并保持**。实测口径：**根 HTML 主流是 LF**（生产 60 个 html：LF 49 / CRLF 11）；`assets/common.css` / `chat-local.js` / `api.js` / `app.js` / `xt-moments.js` 为 **CRLF**；`assets/xt-update.js` / `xt-settings.js` 为 **LF**。
     > ⚠️ 旧 skill 表格写「其余根 HTML = CRLF」**与实测不符**，别照表判事故；**跨树搬文件一律以目标文件现有行尾为准**。
   - 改前备份 `.bak-pre-*`；不做无关重排。
   - **`?v=` 版本戳由 lead 统一 bump，只前进不回落**；戳若已推过生产**必须换新字母**。
   - **注释里不要写带版本号的资源路径**，否则正则可能跨行吞掉 `-->` 与标签 → 整页白屏（已踩两次）。

---

## 七、部署 SOP 摘要（完整版见 skill `study-workbench-deploy`）

1. **全量差量 + 依赖闭包**（别手工列清单）：本地全量 md5 vs 线上 `os.walk` md5 → DIFF / ONLY_LOCAL / ONLY_REMOTE 三张表；前端解析**所有** HTML 的 `assets/*` 引用求集合，减线上实际存在 → 差集即"部署后必 404"清单。
   > R94 事故：硬编码清单只推了 16 个文件，大量功能缺失。R4 实测靠闭包**营救出 `assets/layer-stack.js`**（被 6 页引用）。
2. **打包与上传**：`tarfile` 打 tar，`arcname` 写**生产侧相对路径**（`web/...`、`server/...`），`pscp` 传 `/tmp/`，远端 `tar -xzf ... -C /opt/study-workbench`。**中文文件名走 tar** 最稳。
3. **路径映射坑**：真源根即生产 `web/`；
   ```python
   def local_path(rel):
       return os.path.join(ROOT, rel[4:]) if rel.startswith("web/") else os.path.join(ROOT, rel)
   ```
   **只写 `os.path.join(ROOT, rel)` 会「找不到」→ 静默 0 文件入包**（首跑就踩过）。
4. **差异归零门禁**：**逐文件 `本地 md5 == 生产 md5`**（不能只数总数、不能只比 size）。R4 达 **2074/2074**。
5. **重启与健康检查**（后端变更）：`systemctl is-active`、`systemctl show -p NRestarts --value`、`/api/health`、目标新路由。
6. **发版（含 APK）**：四处 bump（`server/routers/version.json` + `android/AndroidManifest.xml` + `assets/xt-update.js` + APK 实体名）→ `python tools/qa/check_version_consistency.py` 必须 exit 0 → `python android/build_apk.py`（约 65s）→ 上传 APK/version.json/xt-update.js → **`version.json` 有 60s 进程缓存，等 66s 再验** `/api/app/version`。
   **版本号与 APK 必须一起动**（只推版本号 → App 提示更新但下载不到包）。
   工装：`python tools/bump_versions_safe.py --apply --version X --code Y`（默认 dry-run，二进制读写，幂等，失败回滚）。
7. **收尾**：备份路径与逐文件 md5 写报告；本地 `git commit`（**push 需用户单独拍板**）。

### 7.1 推 GitHub

```bash
# 1) 量化载荷
git rev-list --objects HEAD --not <remote_tip>   # 再用 cat-file --batch-check 求和
# 2) 净化副本（隔离 clone，本地仓库不动）
git clone --no-hardlinks --no-tags "D:/下载的文件/学习工作台" D:\_r2c_roundN
python -m git_filter_repo --force --no-gc \
  --invert-paths --paths-from-file _p.txt --replace-text _r.txt
# 3) 推送（Clash 7897！env 代理对本机 GitHub 已失效）
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 \
    -c http.version=HTTP/1.1 push --force-with-lease=main:<远端当前SHA> origin main
# 4) 成功判据只看远端 SHA == 本地 HEAD（"Everything up-to-date" 会与 RPC 错误同时出现 → 假成功）
```
- `_p.txt`（剔除）：`*.apk`、`tools/jx_patch/_work/`、`tools/frontend_*.tar.gz`、**`server/.env`**、`_lead_run*.log`。
- `_r.txt`（替换）：`AKLT…` / `sk-or-v1-…` / `sk-…` / `AIza…` / JWT 等固定规则，**外加从 GitHub 拦截报告点名文件里在脚本内部提取的 `literal:`**（曾漏掉 53 字符无前缀的 Gemini key）。
- 现成工装：`_r2c_loop.py`（解析报告 `path:` → 追加规则 → 重跑，最多 4 轮）。

> ⚠️ **R5 本次的 GitHub 策略已变更**：远端 tip `be18396e` 与本地 HEAD `526b391` 是**无关历史**（远端已被净化重写），**不能 force push**（会覆盖已净化的远端）。本轮改为**推送独立同步分支（fast-forward 增量）**，绝不 force。

---

## 八、验证工具与口径

| 工具 | 用途 | 要点 |
|---|---|---|
| `tools/qa/escheck_es2017.js` | 语法红线 | 在工作树根跑，必须 `DONE 0` |
| `tools/verifier`（jsdom） | 前端行为断言 | 必须 `runScripts:'outside-only'` 才能 `window.eval`；**不级联外部 CSS**，CSS 断言需直接解析文本 |
| `tools/qa/_r5_emoji_jsdom.js` | R5 表情面板行为测试 | 报告 `tools/qa/_r5_emoji_report.txt`；`JSDOM_ALL => PASS` |
| `tools/qa/page_check.js` | 页面渲染级校验 | **必须 `QA_PORT=89xx` 换端口**，逼它自启内置 Node 服务 |
| `tools/qa/check_version_consistency.py` | 版本号一致 | 应 exit 0 |
| 生产只读探针 | 拿 ground truth | 工装 `_r4_probe.sh` / `_r4_base.sh` / `_r4_leadverify.sh` + `_lead_remote.py`；**接手第一件事就是跑它** |
| 段落级 hunk 归因 | 判断改动归属哪一批 | `_r5_attrib3.py`（批次关键词表 + 20 行滑窗） |

### 8.1 本沙箱环境硬约束（必须先读，否则大量假失败）

- **bash 缺 coreutils**：`ls / cat / head / tail / wc / dirname / cp / env` 全部 `command not found`，**管道会返回 127 造成假失败**。
  → 一律用 **Write 工具写 python 脚本**（绝对路径），用
  `C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe <脚本>` 跑，输出落 `.txt` 再用 Read 读回。**绝不写 `| tail`**。
- node：`C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`；jsdom 用 `NODE_PATH=C:\Users\ATM\node_modules`。
- git：`D:\git\Git\cmd\git.exe`。
- **Python 用 `open(p,'rb')` → `re.subn` → `open(p,'wb')`**；文本模式会把 CRLF 静默改成 LF（历史事故：39 个页面被推上生产）。

---

## 九、本轮关键文件 md5（本地真源）

| 文件 | md5 | 说明 |
|---|---|---|
| `assets/chat-local.js` | `D1C92F48` | R5 面板逻辑（+396/−43） |
| `assets/common.css` | `C983D9F5` | R5 面板 CSS + tsa（+38/−8） |
| `assets/emoji/manifest.js` | `087D0626` | 24 → 96（+76/−1） |
| `私聊.html` | `1AA0954E` | 面板节点 + sticker input + ESC（+16/−3） |
| `assets/layer-stack.js` | `D86CD96C` | **新建**：弹层压栈基建（R4-A） |
| `assets/xt-toast.js` | `93686EF1` | R4-A 扩展 position/offset |
| `assets/app.js` | `D550352A` | R4-C 弹层压栈 |
| `assets/api.js` | `22532118` | R3b-B 15s 超时 |
| `assets/icon-map.js` | `4E8856E2` | R4-D 防闪烁 |
| `assets/error-boundary.js` | `FABF8528` | R3b-A JS 异常落盘 |
| `assets/admin.js` | `97E761F4` | R3-L4 设备统计 |
| `assets/admin.css` | `1A43369C` | R3-L4 |
| `server/database.py` | `FE0A0594` | R3b-C 引用四列 |
| `server/main.py` | `C8CBC2DA` | R3-L4/L5 路由注册 |
| `server/routers/chat.py` | `05B06922` | R3b-C `resolve_reply` |
| `server/routers/groups.py` | `853427AF` | R3b-C |
| `server/routers/liveloc.py` | `FB108F87` | R3-L2 `silent` |
| `server/schemas.py` | `2AA42965` | R3b-C `ReplyIn` |
| `server/routers/installed_apps.py` | `886ED710` | **新建**：R3-L5 风控 |
| `server/routers/admin_devices.py` | `708ECE08` | **新建**：R3-L4 设备统计 |
| `android/.../MainActivity.java` | `0D17D6E4` | R3-L1/L3/L5 + R3b-A/D |
| `android/.../MsgPollService.java` | `681A58E7` | R3-L3 |
| `android/.../PollLogic.java` | `85F498A4` | R3-L3 |
| `android/.../PollKeepAliveJobService.java` | `8623263F` | R3-L3 保活 |
| `android/.../CrashLogger.java` | `D667CCF6` | R3b-A |
| `android/.../LogFileProvider.java` | `F813B691` | R3b-A |

---

## 十、遗留与下一步

1. **🔴 安全**：见第二节（`server/.env` 轮换 + `ai-config.js` 的 Gemini key）。
2. **🔴 `text-size-adjust` 取舍待拍板**（见 5.4）——**保无障碍** 还是 **保尺寸统一**。
3. **🟡 R5 待落库**：4 个文件需 commit；15 个历史遗留文件需一并 commit（内容与生产一致，零风险）。
4. **🟡 R5 待发版**：`?v=` 戳 bump → 版本 1.35/36 → APK 重打 → 全量差异部署 → md5 零差异闸门 → commit → GitHub **增量分支**推送。
5. **🟡 真机验证（未做）**：表情面板 × 软键盘遮挡、长按收藏手感、自定义表情发送与接收、横幅通知三态 × 权限矩阵、图片捏合与保存到相册。
6. **🟡 远端 `refs/tags/1.0`**：是否删除待拍板。
7. **🟢 `verify_increment_0911.js` 两条 legacy 断言**已被机制变更淘汰，建议单独一批更新。
8. **🟢 小项**：`common.css` 的 `--z-toast` 重复定义；`server/.venv` 的 uvicorn 建议 force-reinstall；真源工作区约 500+ 项未跟踪临时物（**出清单后归档，不要顺手删**）。
9. **🧹 本批产出的工装**（真源根，可复用）：
   `_r5_leadverify.py` / `_r5_deep.py` / `_r5_attrib3.py` / `_r5_snapshot.py` / `_r5_doc_material.py`、
   `_r4_probe.sh` / `_r4_base.sh` / `_r4_leadverify.sh` / `_lead_remote.py`、
   `_r4_fulldiff.py`（等价）/ `_r4_deploy.sh` / `_r4_zerodiff.py` / `_r4_commit.py`、
   `tools/qa/_r5_emoji_jsdom.js`（+ 报告）。

---

## 十一、给接手者的一句话建议

**先跑生产只读探针拿 ground truth，再读第二节（安全）。**

本批最重要的两条经验，都不是读代码能得出的：
1. **`git status` 会骗人** —— 19 个"改动中"的文件里 **15 个早已在生产上跑着**，只是从未入库。**必须做 hunk 级归因**再判断归属，否则要么误判越界、要么漏记版本历史。
2. **一次"看起来对"的修复可能完全无效** —— `text-size-adjust` 在 `minSdk 21` 的 WebView 上大概率被忽略，而真正的开关（`setTextZoom`）被**刻意**关掉了（为了无障碍大字模式）。**要在注释里写因果链，先确认这条因果链在 Chromium 里真的成立。**
