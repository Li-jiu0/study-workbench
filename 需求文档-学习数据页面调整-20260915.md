# 学习数据页面调整需求（批次九 · N9-20，2026-09-15）

> 登记来源：用户 2026-09-15 提供「结合学习工作台项目、更实用更具体的学习数据页面方案」，明确要求「记录一下」。
> 本文件为需求原文登记，**未开工**——用户铁律「不说开工不要自己执行」。
> 改造落点（已只读核实）：`个人中心.html` L266–274「本机学习数据」section（`#localStats`，内容由 `assets/api.js` 的 `renderProfileOverview()` 填充）；`assets/api.js` L1297（本地档案「学习数据」卡）/ L1377（在线档案「学习数据」卡）/ L1315–1321（「学习模块」快捷入口，含 错题本 / 四级词汇 / 行测刷题 三个跳转按钮）。

---

## 0. 一句话定位

把「本机学习数据」这个冷冰冰、全是 0、还混着几个跳走按钮的板块，改成一页**真正有用的「学习报告」**——有空状态、有结构、有图表，让人一眼看懂自己学得怎么样。

---

## 1. 命名与基调调整

- **「本机学习数据」→「学习报告」**（P0）。用户觉得「本机学习数据」是技术口吻，对学习者不友好；统一改名为「学习报告」（标题在 `个人中心.html` L272 的 section 文案，以及 `assets/api.js` L1298 / L1378 的卡内「学习数据」标题一并改）。
- 板块定位从「设备内部统计」改为「我的阶段性学习成果汇报」。

---

## 2. 空状态（P0，必须有）

- 任何指标/列表/图表在**没有数据**时，**不允许显示 `0` 或空卡**，统一给友好的空状态占位：
  - 文案示例：「还没有学习记录，去刷几道题试试吧」「本周还没有学习时长」「行测还没做过题」。
  - 配一个浅色图标（`data-icon`，非 emoji）占位，不破坏版面留白。
- 目的：避免「打开一看全是 0」带来的挫败感与「像没加载出来」的错觉（与现有 `api.js` 里 `|| 0` 直接硬显示 0 的写法相反）。

---

## 3. 五个数据区块（核心交付）

| 区块 | 优先级 | 内容 | 数据来源（已核实可复用） |
|---|---|---|---|
| ① 核心指标卡片 | P0 | 连续打卡 / 总学习时长 / 做题总数 / 正确率 等 4 个核心数（沿用现 `profile-stat` 形态，但接空状态） | `appData.stats`（连打卡/总时长/总题数/正确率，已在 `api.js` L1300–1303 渲染） |
| ② 最近 7 天学习时长柱状图 | P1 | 近 7 天每日学习时长柱形，直观看学习节奏 | `activityLog`（已存在，记录每次学习动作/时长） |
| ③ 行测各模块正确率/题量表格 | P1 | 常识判断 / 判断推理 / 言语理解 / 资料分析 / 数量关系 等模块的正确率 + 做题量，表格呈现 | `EXAM_BANK`（行测各模块题库，`appData.examTypeProgress` 按题型累计，见 `api.js` L5075 一带的累计逻辑） |
| ④ 各模块学习时长环形图 | P2 | 各模块/各功能投入时长占比环形图 | `activityLog`（按模块/功能 tag 归类时长） |
| ⑤ 最近学习记录 | P1 | 最近若干条学习动作流水（做了什么、哪个模块、什么时候） | `activityLog`（最近学习记录，已是现有数据） |

> 说明：② 与 ④ 都依赖 `activityLog`，但 ② 是「按天」聚合、④ 是「按模块」聚合，二者维度不同，可同时做。③ 行测表格依赖行测做题累计（批次八已修「新题型累计漏统」bug，现 `examTypeProgress` 能正确统计常识判断等模块）。

---

## 4. 移除三个按钮（P0）

- 现状：`个人中心.html`「学习数据/学习模块」区里混了几个**跳走别页的按钮**（`assets/api.js` L1315–1321「学习模块」卡：错题本 / 四级词汇 / 行测刷题）。
- 用户要求移除：**错题本**、**四级词汇**、**偏好设置** 三个按钮（「行测刷题」入口是否保留待开工确认，建议保留作唯一快捷入口，或同样移除、让用户从侧边栏进）。
- 理由：学习报告页应该是「看数据」的地方，不该是「一堆跳走按钮」的杂货铺；跳转入口在侧边栏/底部导航已全覆盖。

---

## 5. 不做的事（明确排除，避免范围蔓延）

- ❌ AI 分析师（自动生成文字点评）——本期不做，纯数据可视化即可。
- ❌ 目标完成度（设目标 + 进度环）——无目标体系，不做。
- ❌ 能力雷达图（多维度能力画像）——与 N9-14 P2 的「PPT 设计能力雷达图」不是一回事，本页不做。
- ❌ 导出 PDF（把报告导出成文件）——用户明确不做。

---

## 6. 约束（沿用全站铁律）

- 老 WebView 语法禁令：禁 `?.` / `??` / `replaceAll` / `Object.fromEntries` / `Array.at` / 顶层 await；图表用纯 CSS 柱状/环形 或 轻量内联 SVG，**不引第三方图表库**。
- ADR-3 合规：图表/表格/空状态全部走页面内布局（本就页内，无弹窗），保留原 DOM id 与显隐函数名（`#localStats` / `renderProfileOverview` 等）。
- 改 `assets/api.js` 后**必须 bump 全站 `app.js` 版本戳**（见交接文档 6.4）。
- 数据全本端（localStorage + 已存在的 `activityLog` / `EXAM_BANK`），无后端新增接口。

---

## 7. 落地顺序建议（开工时）

1. P0：改名「学习报告」+ 空状态 + 移除 3 按钮（纯文案 + 删节点，风险最低）。
2. P1：① 核心指标卡接空状态 → ⑤ 最近学习记录 → ② 7 天柱状图 → ③ 行测表格。
3. P2：④ 各模块时长环形图。

---

*文档结束。改造落点、数据来源、约束均已只读核实，待用户「开工」后按 P0→P1→P2 推进。*
