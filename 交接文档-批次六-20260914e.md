# 交接文档 · 星途学习工作台（2026-09-14 · 批次六）

> 写给接手的 AI：假设你从零接手，读完本文即可安全开工。
> 前序文档**仍然有效**：《交接文档-批次五-20260914b.md》《用户需求与决策总账.md》《星途平台改进-需求清单-状态盘点-2026-09-13.md》。
> 本批专项方案：《星途平台-页面做真内容生产方案-2026-09-14.md》（执行依据）+《星途平台-页面名实问题与重构优化方案-2026-09-14.md》（诊断）+《docs/增量任务分解-R48-做真内容-2026-09-14.md》（架构与任务分解，**含实测校正，优先信它**）。
> 本文是以上全部文档的**增量 + 最新快照**。

---

## 🔴 二、用户明确要求"优先解决"的需求（**先做这个**）

> 用户 2026-09-14 15:52 原话：「把这些需求在交接文档里面标注优先解决」。
> 这两条是用户**口述提出、尚未动工**的需求，优先级高于其它一切待办。

| 编号 | 需求 | 现状 | 目标 |
|---|---|---|---|
| **R45** | 私聊页「联系管理员」入口改成和好友会话行**长得一样** | 是独立条 `.ac-entry`：浅绿底 + 盾牌图标 + 主色加粗文字，位于 `#imList` 会话列表**之外**、列表上方 | 改成与好友会话行 `.im-sess` 同款：**圆形头像 + 昵称 + 最近一条消息/时间**，融入 `#imList` 列表 |
| **R46** | 私聊/好友**会话列表每隔几秒闪一下** | **根因已定位**（见下） | 消除周期性闪烁 |
| **R48-2** | **第 2 批**：四级·阅读理解 + 四级·翻译专项 | 「阅读理解」是 10 道**策略选择题，一篇文章都没有**；「翻译专项」是 10 道**表达辨析四选一，连输入框都没有** | 阅读：**4 篇真文章 + 30 题（题题带解析）**，含选词填空 / 长篇匹配 / 仔细阅读三类；翻译：中文句 → **用户输入英文** → 参考译文 + 关键词命中评分（6 单句 + 2 段落） |
| **R48-3** | **第 3 批**：高情商·案例拆解库 + 面试·准备 + 面试·后 + PPT·技巧提升 | 案例库是情景选择题（**无案例**）；面试准备是 8 道四选一；面试后是 6 道复盘选择题；技巧提升只有 5 张卡片（**只有"看"没有"练"**） | 案例：**背景→冲突→处理→拆解要点** 8 个（各带验证题）；面试准备：STAR 框架 + 示范回答 + **自答对比** 8 题；面试后：输入复盘 → **自动生成改进清单**；技巧提升：**学练闭环** 5 技巧 × 2–3 题 + 实战任务 |

### R45 实施要点（方案已定，可直接动手）

- **涉及文件**：`私聊.html`（`.ac-entry` 的 DOM `L99-102` + CSS `L326-329`）、`assets/admin-contact.js`（入口交互与角标）、`assets/chat-local.js`（会话行模板 `renderChats` `L324-361` 作样式参照）
- **形状**：复用 `.im-sess` 结构 —— `<div class="im-av">头像</div>` + `<div class="im-si"><div class="im-n">昵称</div><div class="im-sub">最近消息</div></div>`
- **已拍板**（用户未反对，按此做）：
  - 头像用**固定盾牌图标头像**（管理员对普通用户隐形、无公开头像字段）
  - 位置**固定在会话列表首行**
- 🔴 **行为不可砍**：`.ac-badge` 未读角标（`admin-contact.js` 的 `refreshEntryBadge` / `renderEntryBadge`、退避轮询、打开面板即清零）是 R43/R44 的成果，**改样式时原样保留**。
- 顺手清遗留：`.ac-entry` 图标 span 内残留 emoji `🛡️`（见批次五文档 §七-2），改头像式时一并换掉。
- ⚠️ 注意：`chat-local.js` 的 `renderChats` 用 `box.innerHTML = ...` **整表重建**，把入口行并入列表时要考虑与 R46 的变化检测**一起改**，否则入口行也会跟着每 5 秒重建一次。

### R46 实施要点（根因已实测定位）

**根因链（已逐行核对，直接用）：**

| 环节 | 位置 | 说明 |
|---|---|---|
| ① 定时器 | `assets/chat-local.js:2495` | `setInterval(fn, 5000)` 未读消息轮询 |
| ② 无条件重渲染 | `chat-local.js:2566` | 回调末尾**无条件**执行 `renderList()` |
| ③ 整表重建 | `chat-local.js:355` | `renderChats()` 结尾 `box.innerHTML = groupHtml + chatHtml` → **整个列表 DOM 销毁重建** |
| ④ 表现 | — | 头像重绘 + `.im-sess` 过渡动画重放 = 每 5 秒闪一下 |

**修法（按性价比排序）：**
1. **快照比对 + 早退**（首选，直接消闪）：轮询回调内先比对 `未读数 / last / lastId / 置顶 / 免打扰`，**无变化直接 return**，不写 `innerHTML`
2. 给 5s 轮询补 `document.visibilityState !== 'visible'` 守卫 —— 注意：同一文件里 2.5s 会话轮询（`:2408`）与 30s 群/在线轮询（`:2414`）**都已有**这个守卫，**唯独 5s 这条没有**
3. `/api/friends/requests` 目前也是每 5 秒拉一次，可降到 30s 或与群列表轮询合并
4. 若仍有残余闪烁，再做**行级 diff**（只更新变化行的 badge 文本/摘要，不动节点）

**❌ 不建议**：把列表刷新改成 WebSocket 纯推送。项目 `/api/ws`（`server/wsmanager.py`）的架构定位是「**仅加速、不做主干**」，因为降级路径要求断网/离线时全站照常可用。

### R48 第 2 批实施要点（A1 阅读理解 / A2 翻译专项）

- **承载形态**：页内全屏视图 `四级备考.html#cetQuizView`（**不是弹窗**），与 A7/B2 在 PPT 页的做法同构。
- 🔴 **开工前必做**：先预埋 **`CETV2` 注册表**（架构文档标为 **T20**），与 `PPTV2` 同构；此后 A1/A2 只注册渲染函数，**不再碰宿主 HTML**。
- 🔴 **必须先查清的技术疑点**：`四级备考.html:532` 有一个 `mountMask()` —— 它把 `#mzMask` 挪进 slot，**很可能就是现成的"内联渲染"方案**。背景：`assets/mini.js` 的 `renderShell()` 是**无条件 `document.body.appendChild(m)`、且 `mz-mask` 带 `position:fixed` 内联样式**（物理上无法内联），所以 A1/A2 若直接沿用 mini.js 会退化成全屏弹窗。**先确认 mountMask 机制能否复用，再定渲染路径。**
- **数据**：阅读 4 篇 30 题、翻译 6 单句 + 2 段落 + 参考译文，全部**原创改编**（不抄真题原文）；原有 10 道策略题降级为「技巧速览」、10 道表达辨析降级为「表达积累」，**旧数据一行不删**。
- **交互新增**：文章阅读区 + 计时作答；翻译文本输入框 + 提交 + 关键词命中评分。

### R48 第 3 批实施要点（A3 案例 / A4 面试准备 / A5 面试后 / B1 技巧提升）

- **A3 案例拆解库**：8 个案例，四段式（背景 → 冲突 → 处理 → 拆解要点 + 可复用公式）；来源 = 现有 12 场景话术库提炼 6 个 + 原创 2 个；原 8 道情景题**降级为案例后的验证题**。
- **A4 面试准备**：8 题三件套（参考框架 STAR/PREP → 示范回答（可朗读）→ 用户自答输入区 + 自查清单）；可与 AI 模拟面试衔接。
- **A5 面试后**：输入式复盘（问了什么 / 答得好的 / 卡壳的 / 下次改进）→ 生成改进清单；原 6 道复盘题**保留为「复盘知识自测」**。
- **B1 技巧提升**：技巧速览（5 卡片保留）→ 技巧练习（每技巧 2–3 题）→ 实战任务（checklist）。
  🔴 **必办**：B1 落地时**必须同步改** `PPT训练.html` 的 `CATS['ppt-tips']` 入口文案（现为 tag「知识卡片」、desc「六个专题技巧，点击卡片标题展开或收起阅读」—— 且"六个"与数据里实际 5 条也对不上）。挂载点 **`PPTV2['ppt-tips']` 已预埋**，只注册渲染函数，**不必再动 HTML 结构**。
- **共用组件**：`assets/xt-content.js` 已提供 `hero / progressBar / tabs / sectionsHtml / renderQuiz / registerView / dispatchView` 等，**默认优先复用**；唯一例外与约束见《docs/增量任务分解-R48-做真内容-2026-09-14.md》**§7.2**。
- **铁律不变**：内容全原创 + 每题带解析 + 旧内容只增不删 + 新库用自己的二级键前缀 `xtc:lib:<库缩写>:`。

---

## 三、本批次（六）交付内容：R48 第 1 批「做真内容」

### 3.1 背景（一句话）

用户拍板：全站 9 个页面存在**名实不符**（名字承诺"阅读/翻译/拆解/训练/库/实战模板"，实际只是四选一选择题或弹窗文字），**不走改名路线，直接把内容做实**。

### 3.2 本批交付（第 1 批 5 个任务全部完成，QA **128/128 PASS**）

| 任务 | 页面 | 做成了什么 |
|---|---|---|
| **T00** 契约基建 | — | 新建 `assets/xt-content.js`；`PPT训练.html` 预埋挂载点 + `PPTV2` 注册表 + 代理分发 |
| **T10** A6 | 央国企·企业定向库 | **弹窗 → 独立页** `企业定向库.html`：8 类 × 四段式（企业名片/笔试考情/面试注意）+ **40 道自测题，题题带解析**；进度条 + 已学标记 + 收藏；入口从 `央国企笔试.html:104` 改跳新页 |
| **T11** A7 | PPT·实战模板库 | **文字清单 → 5 套真模板 × 5 帧 = 25 个纯 CSS 骨架预览** + 配色/字体规范 + 一键复制骨架 |
| **T12** B2 | PPT·设计基础 | **上来就考试 → 3 节课（约 4000 字）+ 10 组正反例 + 9 题随堂测**；原 8 题降为课后小测 |
| **T90** 版本收尾 | — | 全站统一版本戳 **`20260914e`** |

**新建文件**：`企业定向库.html`；`assets/` 下 `xt-content.js`、`company-lib.js`、`data-exam-company.js`、`data-ppt-templates.js`、`tpl-preview.js`、`data-ppt-class.js`、`design-class.js`
**改动文件**：`PPT训练.html`（挂载点/注册表/代理分发 + 3 处入口文案）、`央国企笔试.html`（L104 入口）
**禁改文件 git 校验干净**：`mini-exam.js` / `mini-ppt.js` / `mini-cet.js` / `app.js` —— **全程零改动**

### 3.3 架构三条（后续批次必须遵守）

- **ADR-1 数据外置 + 后置覆盖**：不改 `mini-*.js`，新数据文件在它们**之后**加载覆盖同名 key。旧数据一行不删，**回滚只需删一个 `<script>` 标签**。
- **ADR-2 注册表模式**：宿主 HTML 的挂载点由**唯一一条线一次预埋**，此后各任务只 `XTC.registerView('PPTV2', id, fn)` 注册渲染函数，**永不再碰宿主 HTML**（这是规避本项目历史上 3 次并行覆盖事故的关键）。
- **ADR-3 承载形态**：A6「库」类内容资产 → **独立页**；A7/B2 等模块内子功能 → **页内全屏视图**（`PPT训练.html#pptPanel`，`position:fixed;inset:0`，**不是弹窗**）。弹窗只允许承载三类：快捷查看 / 确认操作 / 轻量小测。

### 3.4 部署与推送结果（已完成）

| 项 | 结果 |
|---|---|
| 部署脚本 | `tools/deploy_update_20260914e.py`（由 `..._20260914d.py` 派生） |
| dry-run 清单 | **100 文件**（33 HTML + 39 assets + 26 data.json + 1 emoji + 1 根 data）；7 个新 js 与 `企业定向库.html` 均在包内；`blog_wechat.html` 已排除；`server/` 文件 0 |
| ROOT 校验 | 指向主项目 `D:\下载的文件\学习工作台`（**非 worktree**，铁律通过） |
| **全量 MD5** | **100 / 100 一致**（远端回读 108 条，0 不一致） |
| 服务 | `SERVICE=active`，`NRESTARTS=0` |
| 站点 / 接口 | `SITE=200`、`/api/health=200`、`企业定向库.html=200`、`PPT训练.html=200`；7 个新资源全部 200 |
| 内容探针 | `企业定向库.html` 含「企业定向库」×5；`PPT训练.html` 含 `PPTV2`×11、含 `20260914e`×6；**新资源旧版本号残留 = 0**；中文名以直读内容核对（非 grep） |
| DB | 部署前自动备份成功（200704 字节） |
| 日志扫描 | `journalctl -u study-workbench --since -10min` 扫 `traceback/exception/500` = **0** |
| **Git commit** | **`be755fb`**（10 文件，+4911 / −8，含中文名 `企业定向库.html`） |
| **Git push** | ✅ 成功，stderr：`102ab2c..be755fb  main -> main` |
| 凭据自查 | `git ls-files \| grep -E 'data\.db\|\.env$\|node_modules/\|deploy_.*\.tar\.gz'` 命中 = **空** |

**⚠️ 两个遗留异常（接手者必读）**

1. **`DB_USERS=11`（SOP 基线 6）—— 已只读排查，未做任何删改**。明细：

   | id | username | nickname | 创建时间 | 判定 |
   |---|---|---|---|---|
   | 1, 2, 3, 4, 6, 7 | good / asdf / goos / aaa / qqqqq / Sage | 馍 / 梦想成真 / hi / 树枝 / 一号 / Sage | 09-10 ~ 09-11 | ✅ **真实用户，即 SOP 基线 6** |
   | 10 | 管理员 | 管理员 | 09-14 02:36 | ✅ **合法**（需求 01 管理员超级账号，`is_admin=1`） |
   | 8 | 777 | 小明 | 09-13 15:23 | ⚠️ 疑似测试残留 |
   | 9 | 123yeyuehui | 灰灰 | 09-13 23:32 | ⚠️ 疑似测试残留 |
   | 11 | 3347158526 | 🍂 | 09-14 15:40 | ⚠️ 疑似测试残留（**也可能是用户本人测试**） |
   | 12 | good12 | 你好呀 | 09-14 15:42 | ⚠️ 疑似测试残留（同上） |

   → 多出的 5 个里 **id=10 是合法管理员**；**id=8/9/11/12 疑似测试残留**。
   🔴 **特别注意 id=11/12 创建于 09-14 15:40 / 15:42**（就在本批部署前后），**极可能是用户本人或其它会话在测试页面时注册的 —— 清理前必须先与用户确认**。
   （本批纯前端、未动库，这 11 行与本次改动无因果关系。）
   清理范式见 `study-workbench-deploy` 技能 §6.4：**必须先清 22 张关联表，最后才删 `users`**，删后核对回到基线。

2. **工作树存在未提交改动 → 线上与 git 不完全一致**（**已实测确认**）：
   - 已跟踪但未提交的改动共 6 个文件：`assets/admin-contact.js`、`assets/chat-local.js`、`私聊.html`（属 **batch d / R44-20260914c** 的私聊与管理员角色时序修复）、`tools/qa/_qa_batch_summary.json`、`tools/qa/e1_smoke_log.txt`（QA 产物）、`用户需求与决策总账.md`（台账）。
   - 线上实测已含 batch d 内容：`私聊.html` 含 `20260914d`×2、`chat-local.js` 含 `prevServerChats`×2、`admin-contact.js` 含「该功能暂未开放」×2。
   - 本次部署打包的是**工作树**且 MD5 100/100 一致 → **线上内容 = 工作树**；但 **git HEAD `be755fb` 只含本批 10 个文件**。
   → 🔴 **接手者注意：线上内容 ≠ git HEAD。batch d 已上线但未入库。** 如需回滚/追责，**以线上实际内容为准**；建议尽快把 batch d 那 3 个文件补一次 commit，让 git 与线上对齐，否则将来从 git 回滚会**丢掉这批已上线的修复**。

---

## 四、铁律（违反 = 事故）

1. **APK 永久不打包**（用户 2026-09-13 拍板；除非用户主动提出，不得再问）。
2. **不删任何现有功能、保留原有 DOM** —— 例外仅限用户明确拍板的删除。旧内容"降级保留"（如策略题→「技巧速览」）而非删除。
3. **内容资产禁止用弹窗承载**（本批立项的直接原因）。
4. **词库/题库/文案全部原创**，四级阅读/翻译按真题难度仿写并标注「原创改编」，**绝不抄真题原文**（版权红线）。
5. **未经用户明确要求不 push GitHub**；生产操作（删数据 / 改 nginx / 重启服务 / 部署上线）需用户点头。
6. **管理员对普通用户保持隐形**（硬需求，不可回退）：搜索/好友/在线列表/公开主页都不出现管理员；只有 `GET /api/admin/contact` 一条通道能让普通用户拿到管理员 id 并发私信。
7. **`assets/icon-map.js` 单写者规则**：多线并行时只允许唯一一条线改它。
8. **并行改共享大文件前必须重新 Read 最新版**，只做精确 Edit；文件零重叠分区。
9. **凭据红线**：服务器密码 / JWT / API Key 一律走环境变量或被 gitignore 的 `upload_v23.ps1`，**绝不入库**。
10. **`server/` 本地是旧副本** —— 绝不整目录上传，只传本次改动的 `.py`，且上传前必做三层差异预检。

---

## 五、标准工作流（每批照此走）

```
改（多线并行，文件零重叠）
→ node --check <改动的 js>
→ 页面配对自检（<!-- -->、<div>、<style>/<script> 计数平衡）
→ 版本戳 bump（改了 assets 必 bump；本批 = 20260914e）
→ 独立 QA 证伪回归（脚本写报告文件再读，不信 stdout）
→ git add <指定路径>（勿用 -A）→ git commit → commit 前自查凭据
→ SW_DRY_RUN=1 python tools/deploy_update_<stamp>.py   （本地清单+打包，不联网）
→ python tools/deploy_update_<stamp>.py                 （上传+全量 MD5+DB 备份+探针）
→ git push（用户明确要求时）
```

**部署脚本模板**：`tools/deploy_update_20260914e.py`。新批次复制后改 4 处即可：`STAMP`、`MANIFEST_PATH`、`PROBE_PY` 里的本批内容探针、`judge_probes()` 里对应的断言块。
**首件事：校验脚本里的 `ROOT` 指向主项目 `D:\下载的文件\学习工作台`，绝不能是 worktree 路径**（`...\main-8d0a1649` 之类，跑错会复活已删功能）。

---

## 六、需求台账（截至 2026-09-14）

| 编号 | 需求 | 状态 |
|---|---|---|
| **R45** | 私聊「联系管理员」改会话行样式 | 🔴 **待做（用户要求优先）** |
| **R46** | 会话列表每 5 秒闪动 | 🔴 **待做（用户要求优先）** |
| **R47** | 页面名实问题诊断与重构框架 | ✅ 已转入 R48（诊断部分继续有效；**"短期改名"路线已被用户否决**） |
| **R48** | 9 页「做真内容」 | 🟡 **第 1 批已交付**；第 2/3 批待做 |
| R31–R44 | 批次五 / 20260914b / 20260914c 的修复（今日任务删除、图标升级、管理员体验、角色时序等） | ✅ 已上线 |
| 原始 30 条需求 | 见《星途平台改进-需求清单-状态盘点-2026-09-13.md》 | 25✅ / 3🟡 / 1❌（**仅需求 20**） |

**⚠️ 编号避让**：R44 已被占用（2026-09-14c 批次的私聊/管理员角色时序 + 轮询退避修复，见 `chat-local.js` / `admin-contact.js` 注释）。新需求从 **R49** 起编。

---

## 七、遗留与待办

### 7.1 R48 后续批次（用户已拍板走"做真"，不要改成改名）

| 批次 | 内容 | 备注 |
|---|---|---|
| 🔴 **第 2 批（用户要求优先）** | A1 四级阅读（**4 篇文章 + 30 题带解析**）+ A2 翻译输入评分（6 单句 + 2 段落） | 内容量最大；**开工前需预埋 `CETV2` 注册表（T20）**；实施要点见 **§二** |
| 🔴 **第 3 批（用户要求优先）** | A3 案例拆解（8 个）+ A4/A5 面试做真 + B1 技巧提升 | B1 挂载点 `PPTV2['ppt-tips']` **已预埋**；实施要点见 **§二**，注意 B1 必改入口文案 |

### 7.2 🔴 第 3 批必办遗留（B1，勿忘）

`PPT训练.html` 的 `CATS['ppt-tips']`（PPT·技巧提升）**入口文案仍是旧的**：tag「知识卡片」、desc「六个专题技巧，点击卡片标题展开或收起阅读」。
实际数据在 `assets/mini-ppt.js` 的 `PPT['ppt-tips']`（**L33-34**，`mode:'info'`），尚未做真。
→ **B1 落地时必须同步改这两处文案**，否则又留一个「名实不符」，直接命中验收第 7 条。
> 注：文案里"六个"与数据里实际 5 条也对不上。

### 7.3 其它已知遗留

1. **`个人中心.html` 不解析 `?user=`** —— 站内所有 `openUserHome(uid)`「看 TA 主页」入口**实际展示的是登录者自己的资料页**。本批只在管理员用户列表旁路（改用页内资料卡），**动态、留言板、群成员头像等位置仍走老路径**。建议统一给 `个人中心.html` 补 `?user=` 支持。
2. **`个人中心.html`「我的学习偏好」文字竖排 Bug**（用户截图报障，**根因已定位、未修**）：
   - `assets/common.css:551` `.form-input { width: 100% }`
   - `个人中心.html:30` `.pf-pref-row .form-input { min-width:150px; flex-shrink:0 }` ← **只禁收缩、没把 flex-basis 归零**，`flex-basis:auto` 回退读 `width:100%` → 下拉框独占整行
   - `个人中心.html:27` `.pf-pref-info { flex:1; min-width:0 }` ← 被压成 0 宽，中文逐字折行
   - **修法（1 行）**：`.pf-pref-row .form-input { width:auto; flex:0 0 auto; min-width:150px; }`
   - 参考正确写法：`assets/polish.css:369` `.pe-addrow .form-input { flex: 1 }`
3. `私聊.html` `.ac-entry` 图标 span 内遗留 emoji `🛡️`（合并进 R45 一起清）。
4. 仓库根有 **21+ 个未跟踪文件**（`_r01_*`、`_r37_bak.txt`、`_r39.txt`、`tools/deploy_update_20260913m.py` 等）—— 清理前先确认无引用。
5. **需求 20**（同质化选择题库差异化或合并）是原始 30 条里**唯一 ❌**，与 R48 的 A1–A5 **主题高度重叠，建议合并处理**，不要拆成两批。

---

## 八、环境坑（全是实锤）

- **bash 缺 coreutils**：无 `ls / grep / tail / head / sleep / env / mkdir`。读写一律用 Read/Edit/Grep/Glob + node/python；长输出**重定向到 ASCII 名文件再 Read**。
- **`env` 命令不存在** → SOP 里 `env -u HTTP_PROXY ... git push` 直接 `rc=127`（**假失败**，实际没执行）。推 GitHub 用 Python 过滤环境变量：
  ```python
  env = {k:v for k, v in os.environ.items() if k.lower() not in ('http_proxy','https_proxy')}
  subprocess.run(['git','-c','http.proxy=http://127.0.0.1:7897','-c','https.proxy=http://127.0.0.1:7897','push','origin','main'], env=env, ...)
  ```
  可用代理端口 **7897**（会话里 `HTTP_PROXY=127.0.0.1:57065` 是死代理）。成功标志在 **stderr**：`<old>..<new>  main -> main`（stdout 为空是正常的）。
- **中文路径 `git add`**：Bash 下有**静默 no-op**风险；用 PowerShell `git add -- "中文路径"`，或 Python `subprocess.run(['git','add','--',f])` 后**核对 `git status --porcelain` 确实 staged** 再 commit。
- **中文文件名 pscp 上传**：会按本机 GBK 编码落成乱码，**进度条照样报 100% 但文件没生效**。必须 **ASCII 临时名上传 → 服务器侧 Python 按映射 `shutil.move` 到目标名 → `urlopen` 直读复核**。
- **并行 Edit 同一文件必然互相覆盖** → 文件零重叠分区或串行。
- **Edit 的 `old_string` 含 `</style>`/`</script>`/`</body>` 时，`new_string` 必须重新带上**，否则其后全部被当 raw-text → 白屏。
- **服务器无 `sqlite3` CLI**，查库用 `python3 -c` + sqlite3 模块。
- **PowerShell stdout 常被吞** → 需回读的输出用 bash `>` 重定向到项目内 ASCII 名文件再 Read。
- **`icon-map.js` 的 `[data-icon]` 只在 DOMContentLoaded 扫一次**（L806，**无 MutationObserver**）：JS 动态注入的图标会渲染成**空 span**，且图标名写错时在 **L795 被 `continue` 静默跳过、控制台零报错**。→ 每次 `innerHTML` 后补 `window.lucideAutoRender()`，或用 `window.lucideIcon(name, size)` 内联。
- **图标名必须查已注册名单**，别自己编（编了就静默消失）。已知无 `x` 键，要用 `x-circle` / `close` / `check`。

---

## 九、工具索引（主项目 `tools/` 下）

| 命令 / 文件 | 作用 |
|---|---|
| `python tools/deploy_update_20260914e.py`（`SW_DRY_RUN=1` 可 dry） | 部署（上传 + 全量 MD5 + DB 备份 + 探针） |
| `tools/qa/_deploy_0914e_manifest.txt` | 最近一次部署清单 |
| `tools/qa/qa_r48_batch1.js` | R48 第 1 批 jsdom 回归（**128 断言**，可重复跑；缺文件记 SKIP 不误报）。运行：`cd tools/verifier && node ../qa/qa_r48_batch1.js` |
| `tools/qa/_r48_batch1_result.txt` | 上述脚本的结果输出 |
| `tools/bump_versions_safe.py` | 安全批量换版本号（**必须先按行过滤**，裸正则跨行会吞注释） |
| `tools/verifier/` | jsdom 校验套件（`node_modules` 首次需 `npm ci`） |
| `tools/plink.exe` / `tools/pscp.exe` | 服务器操作（**不在 PATH，写全路径**，带 `-hostkey`） |

---

## 十、给接手 AI 的话

1. **先看第二节**，那是用户明确要求优先解决的两条；再看第四节铁律和第三节架构三条，然后就可以动手了。
2. **改任何东西前先读本文 + 《用户需求与决策总账》§三「已拍板」**——那里写了"照办、不要再问"的事项（例如**页面名实问题一律走「做真」、不走改名**，别再提议改名）。
3. **收工前跑一遍相关 QA 脚本**（`tools/qa/` 下找对应的），红了你就是事故第一责任人。
4. **用户的截图/口述需求要"全页审计"**，不要只改红框；但 placeholder/option/正文/头像里的 emoji 是白名单，别越界。
5. **用户偏好：直接给结论，不要反复验证** —— 命令各跑一次即判定，不做重复回跑、不做多轮回读自审。生产操作（部署/推送/删数据）永远等用户点头，其余按你的意见排序执行、中间不用再问。
6. **本沙箱会话不继承环境变量**（`SW_PASS` / `SW_HOST`），部署脚本缺失时会直接 `raise SystemExit` —— 从被 gitignore 的 `upload_v23.ps1` 正则提取，**绝不 print 明文、绝不写进入库文件**。

---

## 十一、本批落地记录（2026-09-14）

- **部署脚本**：`tools/deploy_update_20260914e.py`
- **版本戳**：`20260914e`
- **部署结果**：见 **§3.4**（100/100 全量 MD5 一致、四项校验全绿、内容探针命中、日志扫描 0 异常）
- **Git**：commit **`be755fb`**（10 文件，+4911/−8），已推送 `102ab2c..be755fb  main -> main`
- **两个遗留异常**：见 **§3.4** 末尾 —— ① `DB_USERS=11` 的逐账号清单与判定；② 线上与 git HEAD 不一致（batch d 已上线未入库）
- **本批未做的事（明确记录）**：未做 **R45 / R46**（用户要求在本文档中标为"优先"，留给接手者实施，方案见第二节）；未清理生产账号（超出授权）；未提交 batch d 的 3 个文件（非本批范围，见 §3.4）
- **团队**：`software-xt-content`（主理人 + 7 名成员），全程四条工程线并行 + 独立 QA 证伪回归
- 本批拦下的「静默失败」坑（供后续参考）：
  1. V2 代理时序 —— 会被后续赋值覆盖导致新功能静默失效，改为 `DOMContentLoaded` 内安装 + 超时兜底
  2. **localStorage 键名撞车** —— `xtc:learn:exam-company:*` 与 `xt-content.js` 通用契约 `xtc:learn:<bankId>:<itemId>` 同命名空间，会互相覆盖用户学习进度；已改独占前缀 `xtc:lib:ent:*`。**推广规则：每个新内容库必须用自己的二级前缀 `xtc:lib:<库缩写>:`**
  3. 入口文案没跟上（内容做真了但入口仍写「随堂小测 8 题」）—— 已改 3 处
  4. 图标静态扫描机制（见第八节）
  5. `MutationObserver` 监听 `body.childList` 会误关 V2 面板 → `__pptV2Active` 守卫
- **一条实测纠正**：`openPptPanel` **唯一定义在 `PPT训练.html` 页尾 IIFE 顶层**（T00 改造后约 L404、改造前 L394）；`assets/mini-ppt.js` 全项目 grep **零命中**。架构文档 §2 ADR-2 里那段 `mini-ppt.js` 代码是**示意伪代码**，**勿据此定位排工**。
