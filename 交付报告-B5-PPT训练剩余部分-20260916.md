# 交付报告 · B5｜N9-14 PPT 训练课程改进 —— 剩余部分（演示页侧）

- **任务编号**：B5 / 任务十三
- **执行人**：software-engineer-任务十三
- **日期**：2026-09-16
- **唯一可写代码树**：`D:\下载的文件\学习工作台`
- **状态**：**DONE**（数据侧已完备，未做无谓改动；页面侧补齐口径 + 复核注释）
- **收尾更新（20260916 追加）**：A5 线把渲染器 header 口径动态化后，页面侧静态 tag `16 组正反例` → **`27 组正反例`** 已对齐，详见 §十。

---

## 一、需求原文来源与范围界定

### 1.1 需求原文（已找到）

- **文件**：`D:\下载的文件\学习工作台\需求文档-PPT训练改进-综合-20260915.md`（5118 B，mtime 1789467425）
- **登记编号**：N9-14（该文档第 3~5 行自述：登记编号 N9-14，见 `交接文档-批次九-20260915.md` 第 10.8 节）
- **来源**：用户综合豆包 + GPT 观点（2026-09-15 18:15 提供）
- **改造对象原文点名**：
  > `PPT训练.html` + `assets/data-ppt-tips.js`（设计四原则课程数据）+ `assets/ppt-tips.js`（渲染器 `window.PPTV2['ppt-tips']`）

### 1.2 范围界定（为什么本任务只落在 `演示.html` + `data-ppt-tips.js`）

| 需求文档点名对象 | 实际承载者 | 本任务是否可写 |
|---|---|---|
| `assets/data-ppt-tips.js` | 数据侧，**本任务独占** | ✅ 可写 |
| `assets/ppt-tips.js` | 渲染层，**A5 线独占**（任务书明令「不要碰」） | ⛔ 只读参考 |
| `PPT训练.html` | **已不存在**（站内实测无此文件；本页 PPT 训练能力已迁至 `演示.html`） | ⛔ 不存在 |

**关键判定**：站内 `PPT训练.html` 仅存历史备份（`.bak-*`），当前线上无该文件；
「PPT 训练 / 技巧提升」的入口与承载页是 **`演示.html`**（`openPptPanel('ppt-tips')` → `#pptPanel`）。
故本任务的「演示页侧剩余部分」= `演示.html` + `data-ppt-tips.js`，与任务书独占清单一致。

### 1.3 需求文档中与本次相关的 P0 条目（原文摘要）

- P0-1｜每个原则配正反例对比图（「没有图等于白讲，这个必须加」）
- P0-2｜加「标记已学」按钮 + 阅读进度
- P0-3｜「课后小测」「正反例」做成可点击入口，不要死标签
- P0-4｜正文卡片化，不要大段文字

---

## 二、现状梳理

### 2.1 `data-ppt-tips.js`（42,681 B，LF 换行）

**数据结构**（ADR-1：后置覆盖 `window.MINI_BANK['ppt-tips']`）：

```
window.MINI_BANK['ppt-tips'] = {
  t, mode:'tips', v:2, credit, intro,
  meta: { unit:'个', total:5, quiz:13, tasks:6, cases:16, casesArt:11 },
  items: [  // 5 个专题
    { id:'tips-key|tips-align|tips-image|tips-anim|tips-flow',
      icon, name, legacyIcon, body, scene[],
      detail: { sum, groups:[{h, lines[]}] },     // 深入说明
      casesArt: [ { t, bad:{cap,art[]}, good:{cap,art[]}, note } ],  // 图元版正反例（r/c/l/t 四类图元，200×130 画布）
      practice: [ { q, o[], a, x } ],               // 练习（带解析）
      cases:    [ { k:'key|align|image|anim|flow', t, bad, good, note } ] }  // 文字模板版正反例
  ],
  tasks: [ { id:'tk-1'..'tk-6', t, d } ],           // 6 项实战任务
  guide: { icon, title, body, steps[] }
}
```

**实测条目数**（逐条解析，非估算）：

| 指标 | 值 |
|---|---|
| items（专题） | **5** |
| casesArt 图元组 | **11** |
| cases 文字组 | **16** |
| practice 练习题 | **13** |
| tasks 实战任务 | **6** |

> ⚠️ 与任务书「先读现状」表的差异：任务书未给出该文件条目基线；实测 11/16/13/6 **与文件内 `meta` 声明完全一致**，即**数据侧在上一批已全部完成**，本批无需补数据。

### 2.2 `演示.html`（52,359 B → 改后 53,240 B，CRLF 换行）

**消费方式**（DOM 契约）：

1. L282 `<div class="feature-card" onclick="openPptPanel('ppt-tips')">` — 入口卡片
2. L461-462 加载 `assets/data-ppt-tips.js` → `assets/ppt-tips.js`（**均在 L445 `mini-ppt.js` 之后**，ADR-1 顺序天然正确）
3. L603 `CATS['ppt-tips']` — 面板标题/描述元数据
4. `ppt-tips.js` 幂等执行 `window.PPTV2['ppt-tips'] = PT_render` 并经 `XTC.registerView('PPTV2', ...)` 注册；
   由 `演示.html` L753 的 PPtv2 分发器写入 **`#pptPanel` > `#pptPanelSlot`**（全屏页内视图，非 modal，符合 ADR-3）

**是否引用图片路径**：**否**。渲染器 `PT_CASE_IMG = {}`（空表，示例行处于 `/* */` 注释态），
`PT_figure()` 对空表**连 `<img>` 都不生成**，对比图全部由 `PT_art()` / `PT_caseSvg()` 现场绘成内联 SVG。

### 2.3 与 `ppt-tips.js` 的职责边界

| 层 | 文件 | 职责 |
|---|---|---|
| 渲染层（A5 独占，只读） | `assets/ppt-tips.js` | `PT_render` 主渲染 / 三个 tab / casesArt→SVG / 正反例画廊 / 小测判分 / 已学标记 / 进度条 / 占位图降级 |
| 数据层（本任务） | `assets/data-ppt-tips.js` | 纯数据：5 专题 + 11 组 casesArt + 16 组 cases + 13 题 + 6 任务 |
| 页面层（本任务） | `演示.html` | 入口卡片 / 加载序 / `CATS` 元数据 / PPtv2 挂载点 |

---

## 三、实现内容（含行号）

本次共 **3 处外科手术式改动**，全部位于 `演示.html`，**均为展示文案 / 注释，零 DOM 结构变更、零 JS 行为变更**。

| # | 位置（改后行号） | 改动 | 依据 |
|---|---|---|---|
| 1 | L285 `<div class="feature-desc">` | 「5 个专题速览，配 13 道带解析练习与 6 项实战任务」→ 补入「每专题配正反例对比图（内联 SVG 自绘，无外链图片）」与「学练闭环」 | N9-14 P0-1 正反例必加；入口卡片应如实反映能力 |
| 2 | L458-467 `<!-- ===== R48 第 3 批 ... -->` 注释块 | 追加 B5/N9-14 复核结论：加载序正确性（L445→L461）、PPtv2 分发链路、DOM 契约零改动、配图走内联 SVG 不引 `assets/images/ppt-tips/*` | 复核留痕，防后续并行线误加容器/误引图片 |
| 3 | L608-609 `CATS['ppt-tips']` | `tag` 补「16 组正反例」（与渲染器 `PT_setHeader` L852 口径对齐）；`desc` 补「每专题正反例画廊 + casesArt 代码自绘不外链 + 标记已学 + 阅读进度条」 | N9-14 P0-2/P0-3/P0-4 |

### 3.1 `data-ppt-tips.js`：**未做改动**（决策说明）

实测 5 专题 / 11 组 casesArt / 16 组 cases / 13 题 / 6 任务全部齐备，且 `meta` 声明准确、
渲染层 A5 线已确认 `PT_art()` 可消费。**「宁少而准，严禁臆造」** —— 强行补条目只会引入
同题材重复与口径分歧，故**保持字节零改动**（前后 SHA 级一致，size 42,681 不变）。

> 若主理人认为仍需为 `data-ppt-tips.js` 增补，请给出**具体缺口条目**（哪个专题还缺哪一组），
> 我再定向补数据，不盲目铺量。

---

## 四、占位图策略与替换锚点

### 4.1 策略（当前生效）

- **不引用任何 `assets/images/ppt-tips/*`**（该目录已实测**不存在**：`os.path.isdir('assets/images/ppt-tips') = False`）。
- 全部对比图由 **内联 SVG 代码自绘**：
  - `casesArt[]` 的 `r/c/l/t` 四类图元 → 渲染器 `PT_art()` 绘成 `viewBox="0 0 200 130"` 的 `<svg>`；
  - `cases[]` 的 `k(key|align|image|anim|flow)` → 渲染器 `PT_caseSvg()` 绘成 `viewBox="0 0 240 140"` 的 `<svg>`。
- 老 Android WebView（Chrome 50~58）与 `file://` 均可渲染，**零图片请求、零 404、不白屏**。

### 4.2 后续换真图的替换锚点（资源方无需改逻辑）

锚点在 **`assets/ppt-tips.js`（A5 线文件）**，本任务只登记、不修改：

| 锚点 | 位置 | 说明 |
|---|---|---|
| `PT_CASE_IMG` | `ppt-tips.js` **L86-91** | 空对象 `{}`，示例行处于 `/* */` 注释态。填入 key→url 即出 `<img>` |
| key 规则 | `ppt-tips.js` **L78** | `<技巧id> + '|' + <kind> + '|' + <组内序号> + '|' + <bad\|good>`；`kind` = `art`（casesArt，序号从 0）/ `tpl`（cases，按过滤后展示序） |
| 命名建议 | `ppt-tips.js` **L72-75** | 目录 `assets/images/ppt-tips/`；文件名 `<技巧id>_<序号>_bad.png` / `_good.png`（技巧 id：`tips-key`/`tips-align`/`tips-image`/`tips-anim`/`tips-flow`） |
| 一键试看 | `ppt-tips.js` L1384 | `PptTips.setImgMap({...})` 临时塞图；`PptTips.imgInfo()` 列出全部可回填 key |
| 404 兜底 | `PT_bindFigures()` | 图缺失（加载失败 / `naturalWidth=0`）自动摘掉 `<img>`、露出下层手绘 SVG 兜底 |

> **本页不新增任何占位图片文件**，故不存在需替换的本地占位图。

---

## 五、实测结果

### 5.1 页面体检（强制自启服务，可信头）

```
QA_PORT=8917 node tools/qa/page_check.js "演示.html" "技巧提升,正反例,技巧练习,实战任务,快捷键提效,标记已学"
```

输出头（**可信**）：

```
# 数据源 ORIGIN = http://127.0.0.1:8917
# 来源         = 本脚本自启的本地静态服务（端口 8917）     ← 任务书要求的可信头
# 是否本地     = 是（验的是本地工作区代码）
```

> 首次用 `QA_PORT=8911` 时输出头为「**复用**已存在的本地静态服务」→ 按任务书判为**不可信噪声**，
> 已换空闲端口 8917 重跑，拿到自启服务后才采信。

结果：

| 断言 | 结果 |
|---|---|
| 页面正常加载 | ✅ `html bytes = 44435`（= 落盘 44436 字符，UTF-8 下 53240B） |
| 关键内容渲染 | ✅ 「技巧提升 / 正反例 / 技巧练习 / 实战任务 / 快捷键提效 / 标记已学」6 词**静态HTML=true 且 运行时DOM=true** |
| 卡片/区块 | `.card=2`，`[class*=card]=29`，`button=16` |
| 运行时错误 | ✅ 仅 `JSDOM_ERR: Not implemented: navigation to another Document`（jsdom 对 `location` 跳转的**已知无害告警**，非页面脚本异常） |
| 无 404 资源请求 | ✅ 无 FETCH_FAIL / 无资源加载失败记录；渲染层空表 → 零图片请求 |

### 5.2 完工自检清单（逐项）

| # | 检查项 | 结果 |
|---|---|---|
| 1 | `node --check assets/data-ppt-tips.js` | ✅ **rc=0** |
| 2 | `node tools/qa/escheck_es2017.js` | ✅ **`DONE 0`** |
| 3 | `演示.html` 配对：`<!--`24/`-->`24；`<div`187/`</div>`187；`<script`25/`</script>`25 | ✅ **PASS**（`<style>`2/`</style>`1 为误报，见下） |
| 4 | 禁用语法 Grep = 0 | ✅ **PASS**（详见 5.3 误报说明） |
| 5 | 原生 `alert(`/`confirm(`/`prompt(` = 0 | ✅ **PASS**（两文件均 0） |
| 6 | 换行符未被改混 | ✅ `演示.html` CRLF=805 / bareLF=**0**；`data-ppt-tips.js` CRLF=0 / bareLF=794（**原样保持**） |
| 7 | 无对 `assets/images/ppt-tips*` 的引用 | ✅ **PASS**（0 处**活引用**；命中项均为注释/他线文件，详见 5.3） |
| 8 | 未越界改动 A5/B2 独占文件 | ✅ **PASS**（`ppt-tips.js` / `data-ppt-templates.js` mtime < 我的会话起点；`PPT版式库.html` / `tpl-preview.js` 的 mtime 变动来自**并行队友**，非本任务，已用内容比对排除） |
| 9 | 新增数据文件 ASCII 名 + UTF-8 无 BOM | ✅ `data-ppt-tips.js` 无 BOM、文件名 ASCII（本任务未新增数据文件） |
| 10 | 未删既有功能、保留既有 DOM id 与全局函数名 | ✅ 3 处改动全为文案/注释，`#pptPanel`/`#pptPanelSlot`/`openPptPanel`/`closePptPanel`/`CATS` 等**全部原样** |

### 5.3 检查器误报说明（逐条澄清，非缺陷）

1. **`<style>` 2 vs `</style>` 1** — 第 2 处 `<style` 位于 **L593 JS 注释文本**：「样式见 `<style>` 区」，
   不是标签。真实标签仅 L12 `<style>` / L113 `</style>`，**配对正确**。
2. **`**` 命中 4 处（演示.html 3 + data-ppt-tips.js 1）** — 全部落在 `/* */` 注释散文里的
   **Markdown 加粗**（`**之后**`、`**保留不清空**`、`**禁止再改本 HTML**`），任务书已明示「** 在引号/文本内是误报」。
   经剥离字符串字面量后复检，**无任何 `**` 幂运算符**。
3. **`assets/images/ppt-tips` 命中 6 处** —
   - 1 处在 `演示.html` L463：**我新写的说明注释**（「该目录不存在，故全程零图片请求」）；
   - 5 处在 `assets/ppt-tips.js`（A5 线）：L67/L72/L75 为注释，L88/L89 位于 **`/* */` 块注释内**的示例行。
   即 `PT_CASE_IMG` **实际为空**，**不存在活图片引用**。

---

## 六、未验证项

| # | 未验证项 | 原因 | 建议 |
|---|---|---|---|
| 1 | 老 Android WebView（Chrome 50~58）真机渲染 | 本机无真机/模拟器 | 打包 APK 后在真机点开「综合功能模块 → 技巧提升」目视确认 |
| 2 | casesArt 的 11 组 SVG 目视观感 | jsdom 无真实渲染管线 | 真机/桌面 Chrome 目视；图元坐标已按 200×130 画布约定 |
| 3 | 「标记已学」按钮点击后的 localStorage 持久化 | 需真实交互，page_check 为静态加载体检 | 真机点一夜；键为 `xtc:lib:pt:<id>:learned` |
| 4 | 正反例画廊翻页 / 小测判分交互 | 同上 | 真机走一遍 |
| 5 | 端口 8911 上那个**残留旧服务** | 非本任务范围 | 请主理人安排清理，避免后续 QA 误判 |

---

## 七、遗留问题 / 需要主理人裁决

1. **口径分歧（需裁决）**：「N 组正反例」存在两个数源 ——
   - 数据声明 `meta.cases = 16`（仅 `cases[]` 文字组）；
   - 实际画廊池 = `casesArt`(11) + `cases`(16) = **27 组**（`PT_casePool()` 合并，`PT_HIDE_DUP=true` 但数据无 `dup:1` 标记 → 无过滤）。
   渲染器 `PT_setHeader`（A5 线 L852）用 `meta.cases`=16 作为 tag。
   **本任务已将 `演示.html` 的 CATS tag 对齐为 16**，但**全站口径不统一**。
   → 建议：由 A5 线把 `PT_setHeader` 改为报实际池长度，或数据侧补 `dup:1` 标记。**本任务不改 A5 文件**。

2. **`data-ppt-tips.js` 零改动**：若团队期望该文件本批必须有产出，
   请指定**具体缺口**（专题 × 缺哪类内容），我按需定向补，不做无依据铺量。

3. **残留旧静态服务占 8911**：会导致 `QA_PORT=8911` 的 QA 结果「复用旧服务」不可信，
   建议清理并约定 QA 统一用自启端口。

---

## 八、改动文件清单

| 文件 | 前字节 | 后字节 | Δ | 改后 mtime | 换行 |
|---|---|---|---|---|---|
| `D:\下载的文件\学习工作台\演示.html` | 52,359 | **53,240** | +881 | 1789540449.307 | CRLF（保持） |
| `D:\下载的文件\学习工作台\assets\data-ppt-tips.js` | 42,681 | **42,681** | **0** | 1789481849.368（未变） | LF（保持） |

备份（命名含 `bak-pre-b5-20260916`）：
- `演示.html.bak-pre-b5-20260916`
- `assets/data-ppt-tips.js.bak-pre-b5-20260916`
- 无中文路径副本：`_bak-pre-b5-subst\演示.html`、`_bak-pre-b5-subst\data-ppt-tips.js`

未改动（越界保护，已核）：
`assets/ppt-tips.js`、`PPT版式库.html`、`assets/data-ppt-templates.js`、`assets/tpl-preview.js`

---

## 九、结论

**IS_PASS: YES** —— 页面侧展示口径已与 N9-14 需求原文对齐，
数据侧经实测确认**已完备**（5/11/16/13/6），占位图走内联 SVG **零 404**，
ES2017 合规（`DONE 0`）、语法合法（`node --check` rc=0）、换行符保持、未越界。

---

## 十、收尾更新（20260916 · 跨线口径对齐 16 → 27）

### 10.1 背景

A5 线修正 `assets/ppt-tips.js`：`PT_setHeader` 原硬编码 `meta.cases`（16），
现新增 `PT_poolCount(data)` —— **优先实时池长**（遍历 `data.items` 累加 `PT_casePool(it, PT_HIDE_DUP).length`），
数据未就绪才回退 `meta.cases + meta.casesArt`（16+11=27）。
A5 实测：`HEADER_REPORTED = 27` / `LIVE_POOL = 27` / `HEADER_MATCHES_POOL = true`。

→ 原 `演示.html` 静态 tag 仍为 `16`，与渲染器报出的 `27` 打架，本任务在页面侧跟进对齐。

### 10.2 改动（共 2 处，仅 `演示.html`，纯 CRLF）

| # | 位置（改后行号） | 改动前 | 改动后 |
|---|---|---|---|
| 1 | **L612** `CATS['ppt-tips'].tag` | `5 专题 · 13 道练习 · 16 组正反例 · 6 项实战任务` | `5 专题 · 13 道练习 · **27 组正反例** · 6 项实战任务` |
| 2 | **L464-468** 加载序注释块「配图策略」段 | 原写 `casesArt[]（16 组图元）`（本身有误：casesArt 实为 11 组） | 更正为 `casesArt[]（图元版 11 组）` + `cases[]（文字模板版 16 组）`，写明 `= 11 + 16 = 27 组` 即 header 报出的「27 组正反例」，并注明 `PT_poolCount()` 优先取实时池长、回退 `meta.cases + meta.casesArt` |

> 说明：第 2 处顺带修正原文一处**数据事实错误**（把 `casesArt` 组数误写成 16，实际 11）。
> 保留 11/16 分解式——它是「数据侧改动、口径自动一致」的证据；措辞已标注「图元版 / 文字模板版」，
> 不会被误读为画廊总数。

### 10.3 逐处排查的「遗漏处」（确认无需再改）

| 位置 | 是否含组数 | 处置 |
|---|---|---|
| L285 入口卡片 `feature-desc` | 否（仅「正反例对比图」泛述） | 无需改 |
| L610 `CATS['ppt-tips'].desc` | 否（仅「正反例 N 组」泛述） | 无需改 |
| L601-602 `CATS['ppt-design']` | 是（`10 组正反例`） | **另一类目**（设计基础，非技巧提升），A5 未动态化，**保持不动** |
| `data-ppt-tips.js` `meta.cases: 16` | 是 | 数据侧声明值，任务书禁止改该文件；渲染器已改「优先池长」，该字段仅作回退，**保持不动** |

### 10.4 自检结果（用户要求的 5 项）

| # | 检查项 | 结果 |
|---|---|---|
| 1 | `node tools/qa/escheck_es2017.js` | **PASS · `DONE 0`** |
| 2 | `演示.html` 配对：`<!--`24 / `-->`24；`<div`187 / `</div>`187；`<script`25 / `</script>`25 | **PASS** |
| 3 | 禁用语法 Grep = 0；原生 `alert(` / `confirm(` / `prompt(` = 0 | **PASS**（3 处 `**` 均在 `/* */` 注释散文的 Markdown 加粗内，非幂运算符） |
| 4 | 换行符仍纯 CRLF | **PASS · CRLF=808 / bareLF=0** |
| 5 | Grep 确认无「16 组正反例」残留 | **PASS · `16 组正反例` 精确命中 = 0**；`27 组正反例` 命中 = 2（L466 注释 + L612 tag） |

补充：`page_check.js`（`QA_PORT=8918`，可信头「本脚本自启的本地静态服务」）
→ `27 组正反例` **静态HTML=true 且 运行时DOM=true**；无 404；仅 jsdom 已知无害告警
（`Not implemented: navigation to another Document`）。

### 10.5 收尾改动文件清单

| 文件 | 前字节 | 后字节 | Δ | 改后 mtime | 换行 |
|---|---|---|---|---|---|
| `演示.html` | 53,240 | **53,584** | +344 | 1789540909.050 | CRLF（保持） |
| `assets/data-ppt-tips.js` | 42,681 | 42,681 | **0** | 1789481849.368（未变） | LF |
| `assets/ppt-tips.js`（A5 独占） | — | — | **未动** | 1789540691.116（A5 线所改，非我） | — |
