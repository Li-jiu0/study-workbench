# 星途平台改进 · 需求清单（30 条 · 状态盘点版）

> 整理时间：2026-09-13 ｜ 基于《星途平台改进-需求清单.md》原文 + 2026-09-12/13 日志 + 两份工作副本代码实测
> 状态口径：✅ 已完成 ｜ 🟡 部分完成 ｜ ❌ 未做 ｜ ⏸️ 用户撤回
> ⚠️ 副本状态更新（2026-09-13）：两份工作副本**已合并**（主项目 HEAD `8ef7f49`），并于 **20260913h**（commit `c1bb3f7`）上线；worktree 不再是独立线。下表状态已同步本批（20260913h）成果。

---

## 汇总

| 状态 | 数量 | 需求编号 |
|---|---|---|
| ✅ 已完成 | 25 | 01、02、03、04、05、06、07、08、09、10、11、12、15、16、18、19、21、22、23、24、25、26、27、28、30 |
| 🟡 部分完成 | 3 | 13、14、29 |
| ❌ 未做 | 1 | 20 |
| ⏸️ 用户撤回 | 1 | 17 |

---

## 分类索引（含状态）

| 类别 | 涉及需求 | 状态 |
|---|---|---|
| A. 账号与权限 | 01 | ✅ |
| B. 跨平台兼容 | 13 | 🟡 |
| C. 功能删减与模块合并 | 03、04、05、07、12 | 全部 ✅ |
| D. 页面布局与交互重构 | 08、09、10、11 | 全部 ✅ |
| E. 图标 / 视觉统一升级 | 02、03、04、05、06、07、09、10、11、12 | 全部 ✅（02 为基本完成） |
| F. 聊天富媒体能力 | 14 | 🟡 |
| G. 数据隔离与安全（P0 主） | 15、16、17、22、23 | 15✅ 16✅ 17⏸️ 22✅ 23✅ |
| H. 数据一致性与计数器 | 18 | ✅ |
| I. 功能库质量治理与去同质化 | 19、20、21、26、28 | 19/21/26/28 ✅，20 ❌ |
| J. 内容诚实性与入口修正 | 24、25 | 24✅ 25✅ |
| K. AI 与内容深度 | 27 | ✅ |
| L. 产品战略建议 | 29 | 🟡 |
| M. 首页数据真实性 | 30 | ✅ |

---

# 一、账号与权限

## 需求 01｜管理员超级账号（单向可见） — ✅ 已完成

- **需求**：管理员账号具备单向可见特性：可查看所有用户在线状态与资料、新用户注册后自动纳入观察范围、不参与普通好友可见性广播；用户端不显示管理员为好友，仅可主动发起私信。
- **状态说明**：✅ 本批完成。账号 `username=管理员` / `nickname=管理员`（非手机号，密码部署时走服务器环境变量）；users 加 `is_admin` 字段；新增 `/api/admin/*`（overview / users / online / users{id} / feedback / reply），非管理员一律 403。全接口对普通用户隐形（搜索/好友/在线/公开主页均不可见），私信放行（`can_message` = 好友 OR 任一方管理员）；用户反馈管理员可见可回复（合并两源）；前端 `管理员.html` 观察台 + 私聊「联系管理员」入口（`GET /api/admin/contact`）+ 设置页「我的反馈」。后端端到端自测 12/12 通过。

---

# 二、跨平台兼容

## 需求 13｜APP 端好友功能跨平台兼容 — 🟡 部分完成

- **需求**：排查修复 APK 好友功能失效根因，使 APP 与网页端互动/数据共享一致。检查项：`networkSecurityConfig` 明文 HTTP、`setDomStorageEnabled`、Cookie 跨域、WebSocket 可达、api.js 基址。
- **状态说明**：基础配置已具备——`AndroidManifest.xml` 有 `usesCleartextTraffic="true"`；`MainActivity` 有 `setDomStorageEnabled(true)` + `setMixedContentMode(COMPATIBILITY)`。批次五 APK 重打已修复 6 个缺失资源文件。**但端到端好友功能可用性未做真机验证**，需求 14 的图片/视频亦未做。

---

# 三、功能删减与模块合并（按用户决定直接删除）

> 用户已拍板直接删除；本批（20260913h）已全部执行并上线。

## 需求 03｜四级备考页面调整 — ✅ 已完成

- **删除**「写作提升」功能模块；页面内功能模块图标统一升级。
- **状态**：B1 已删除「写作提升」模块并升级图标，20260913h 上线。
- **实测**：`四级备考.html` 已无「写作提升」。

## 需求 04｜央国企笔试页面调整 — ✅ 已完成

- **删除**「数量关系」「判断推理」「资料分析」三个模块；删除「题型正确率」统计项（计数 bug 见需求 18）；保留「学习状况」；图标统一升级。
- **状态**：B2 已删除三模块 +「题型正确率」，保留「学习状况」，图标升级，20260913h 上线。
- **实测**：`央国企笔试.html` 数量关系/判断推理/资料分析/题型正确率均已移除。

## 需求 05｜高情商表达页面调整 — ✅ 已完成

- **删除**「情景选择实战卡片」；图标统一升级。注意：万能金句库/场景话术库是扎实功能，勿误删。
- **状态**：B3 已删除「情景选择实战」，万能金句库/场景话术库完整保留，图标升级，20260913h 上线。
- **实测**：`高情商表达.html` 已无「情景选择实战」；两库保留。

## 需求 07｜PPT 训练页面重构 — ✅ 已完成

- 新建「综合功能模块」入口；保留「版式库」「优秀案例拆解」为独立模块（豆包确认版式库为真 SVG）；其余模块合并归入综合模块；图标统一升级。
- **状态说明**：✅ 本批完成——新建「综合功能模块」入口收拢其余模块，「版式库」「优秀案例拆解」保留独立；页面图标已随本批升级。

## 需求 12｜工具页面调整 — ✅ 已完成

- 删除工具页「万能金句」「场景话术」重复入口（高情商页同名功能保留）；图标统一升级。
- **状态**：B4 已删除两重复入口，其余入口保留，图标升级，20260913h 上线。
- **实测**：`工具.html` 两入口已移除。

---

# 四、页面布局与交互重构

## 需求 08｜好友页面菜单优化与重排 — ✅ 已完成

- **需求**：优化「会话、好友、+好友、群聊、申请」按钮样式；重排为 **会话 → 好友 → 群聊 → 申请 → 加好友**（「+好友」改「加好友」并置末位）。
- **状态**：C1 好友页菜单已重排为 会话→好友→群聊→申请→加好友（「+好友」改「加好友」置末），20260913h 上线。

## 需求 09｜个人中心页面 Bug 修复与重构（P0） — ✅ 已完成

- **需求**：① 修复路由错乱（点「发帖统计」跳「个人资料」）；② 补全「学习偏好」「本机数据」空页；③ 重新组织信息层级；④ 图标升级。
- **验收标准**：每个入口点击后进入语义匹配页面，无空白页、无错误跳转。
- **状态**：A4 已独立验收路由（每个入口进入语义匹配页，无错乱/无空白页）；穿越英语死链已修复；空页此前已修复；20260913h 上线。

## 需求 10｜广场页面重构 — ✅ 已完成

- **需求**：删除顶端说明性卡片（含「广场·写发贴·分类标签·草稿归档·数据本地保存」字样）；「广场、我的文章、写帖子、统计」提升为顶端主导航；按钮与图标统一升级。
- **状态**：C2 广场页已完成顶部说明卡片删除 +「广场/我的文章/写帖子/统计」主导航提升 + 图标升级，20260913h 上线。

## 需求 11｜设置页面层级重构 — ✅ 已完成

- **需求**：①「帮助与反馈」提升为平级一级菜单；②「阅读与界面」「语音与朗读」提升为平级一级菜单；③ 图标按钮统一升级。
- **状态**：C3 设置页已把「帮助与反馈」「阅读与界面」「语音与朗读」三项提级为平级一级菜单 + 图标升级，20260913h 上线。

---

# 五、图标 / 视觉统一升级

## 需求 02｜图标统一升级（遗漏范围补全） — ✅ 基本完成

- **需求**：底部导航栏菜单图标及全站所有遗漏位置图标一并升级，确保全局统一。
- **状态说明**：侧边栏 23 个带侧栏页的 nav-icon 已全部换 `data-icon`（lucide 风格，批次五 T04）；**E1 本批完成 15 个页面共 149 处 emoji 图标（`.bn-icon`/`.bm-icon`/`.mpc-icon` + `.title-icon`）→ `data-icon`，20260913h 上线**。**尚有 23 处无对应注册图标（`assets/icon-map.js` 未收录），按规则保留 emoji，待后续补注册图标后再升级。**

## 需求 06｜商务礼仪及面试页面图标升级 — ✅ 已完成

- 商务礼仪及面试页面内所有图标统一升级美化。
- **状态说明**：✅ 本批完成——页面内 emoji 图标已统一换为 `data-icon`（lucide 风格，icon-map 补齐所需图标）。

> 03/04/05/06/07/09/10/11/12 的图标升级随各页面改造一并执行，现已全部完成。

---

# 六、聊天富媒体能力

## 需求 14｜私聊与群聊富媒体消息能力 — 🟡 部分完成

- **需求**：私聊与群聊均支持 ① 语音消息（录制发送可播放）② 图片消息（可点击预览大图）③ 视频消息（可在线播放）；网页端与 APP 端跨端一致。
- **状态说明**：**语音已实现**（2026-09-11 A7：`Message.kind=voice` + `/api/uploads/voice`）；**图片/视频未实现**；跨端一致性未验证。

---

# 七、数据隔离与安全

## 需求 15｜数据按账号隔离（P0） — ✅ 基本完成

- **需求**：① 所有 localStorage 业务键加账号前缀（`lsKey(name)` 封装）；② 退出登录不清除数据，靠前缀隔离；③ 登录后从后端拉取覆盖本地；④ 根治（中期）：学习记录迁移后端 study router 按 user_id 存储；⑤ 修正设置页「数据存于服务器」失实描述。
- **验收标准**：A 学习→退出→登 B→B 看不到 A 数据；再登 A 数据仍在。
- **状态说明**：`lsKey(name)` 前缀封装已落地，app.js 内 22 处使用；退出不清策略已按方案统一。**A2 已补充修复 api.js / study-stats.js 的业务键前缀化（此前遗漏），20260913h 上线**。**待办**：后端 study router 根治未做。

## 需求 16｜修复倒计时日期解析 Bug（P0） — ✅ 已完成

- **需求**：用 `new Date('YYYY-MM-DD')` 标准解析，禁手动字符串拼接；剩余天数 `Math.floor((target-now)/86400000)`；显示「还剩 XX 天」+ 日期 `2026-12-21`。
- **验收标准**：今天/明天/明年/闰年 2-29 等边界日期天数与显示均正确。
- **状态**：`countdownDays` 已用标准 `new Date` 解析（禁手动 `split('-')` 拼接），本批复核确认，20260913h 上线。

## 需求 17｜部署 HTTPS 与域名（P0 安全 / P1 排期） — ⏸️ 用户撤回

- **需求**：域名 + Let's Encrypt + Nginx 443 反代 + 80→443 跳转；前端/APP 基址切 https。
- **状态说明**：**用户 2026-09-12 拍板撤回**（批准 6 决策之一）；仅归档设计文档 `docs/HTTPS+APP跨端改造方案-2026-09-12.md`，未实施。

## 需求 22｜注册密码强度校验（P1） — ✅ 完成

- **需求**：注册/改密前后端双校验——≥8 位 + 必含字母与数字，弱密码拒绝并提示。
- **验收标准**：`123456`、`test`、`test123456` 被拒；`Abc12345` 通过。
- **状态说明**：后端 `check_password_strength`（≥8+字母+数字）已实现（P0-B T01）；主项目 `设置.html` 已对齐「≥8 位」提示（此前 prompt 版「至少 6 位」与后端不一致的缺口已补）。

## 需求 23｜修复注册昵称不生效（P1） — ✅ 完成

- **需求**：注册 nickname 入后端 users 表（非 localStorage）；登录返回 nickname；个人中心可 PATCH 改昵称。
- **验收标准**：注册填昵称→登录后个人中心/评论/私聊均显示该昵称；APP 端同样。
- **状态说明**：全链路存在——`登录.html` 注册表单有昵称输入（`#loginNickname`）→ `auth.py` 注册 `nickname.strip() or username` 入库 → 登录接口返回 nickname → `users.py` PATCH 改昵称 + `schemas.py` max_length=20。

---

# 八、数据一致性与计数器

## 需求 18｜全局修复计数器同步 Bug（P0/P1） — ✅ 已完成

- **需求**：① 答题后即时 `setItem` + 刷新 DOM（阅读「已对 0」不更新）；② 正确率 = 做对/已做 × 100（做对 1 题显示 100%）；③ 连续打卡用单一全局键 `study_workbench_streak`（带账号前缀），当天任一模块学习即 +1，跨天未学清零，首页与模块读同一数据；④ 全局排查更新链路。
- **验收标准**：各模块答对后计数器即时 +1；首页与模块连续打卡一致；正确率实时变化。
- **状态**：全局 streak 键、`lsKey` 前缀、正确率公式均已存在于 app.js；本批复核确认 T18①②（答题后即时 +1 / 首页与模块跨页一致），20260913h 上线。

---

# 九、功能库质量治理与去同质化

## 需求 19｜真题模考：网上搜集真题 + 真模考（P1） — ✅ 完成

- **需求**：① 真正实现模考（套卷 + 倒计时 + 自动判分 + 成绩归档）；② 真题从网上搜集添加（用户决定）；③ 原攻略折叠面板移至「考试指南」；④ 页面保留「真题模考」文案。
- **状态说明**：P0-B T03 已重做为三独立页（mock_exam / run / result）+ 9 种题型 + Timer + 答题卡 + 草稿 + 成绩归档；`data/mock-papers.js` 4 套真卷已做实；「真题模考」文案按用户决定保留。⚠️ 版权风险已按用户决定提示（非阻断）。

## 需求 20｜同质化选择题库差异化或合并（P1） — ❌ 未做

- **需求**（对保留项二选一）：① 差异化——数量加线段图、资料加原题标注、判断加推理路径、案例拆解改真逐句拆解；或 ② 合并精简——无特色换皮题并入「综合选择题库」。
- **状态说明**：手册 B3–B6 未开工（阅读/翻译/口语/案例/i 人/综合知识/企业库均未改造）。

## 需求 21｜行测图形推理重写题干（P1） — ✅ 完成

- **需求**：题干只留图形描述，规律入解析；题库改 JSON 加载便于更新。
- **状态说明**：P0-B T02 重写题干 + 分片 JSON（figure/define/analogy/logic/verbal/quant/data）；批次三 P1-5 进一步清洗 `exam-bank.json` id 2/8/41 与内置 60 题；泄题扫描 `scan_question_leak.py` 零命中。

## 需求 26｜商务礼仪「SVG 插画」实为 emoji 修正（P2） — ✅ 已完成

- **需求**：补真正 SVG 场景插画（8 张统一扁平风 400×300），或修正描述去掉「SVG 插画」。
- **状态说明**：✅ 本批按用户拍板走「改描述」路线——`商务礼仪面试.html:131`「8大核心场景，SVG插画+要点清单+错误警示」→「8大核心场景，场景图标+要点清单+错误警示」，与实际渲染一致，不再失实。

## 需求 28｜简略内容项补全（P2） — ✅ 已完成

- **需求**：i 人沟通专区（补「提前准备/文字辅助」工具）、企业定向库（补「历年笔试形式」详情）、i 人伙伴团（回复深度）按原始描述补全真实内容。
- **状态说明**：✅ 本批完成——
  - **企业定向库**（`assets/mini-exam.js` exam-company）：info 卡片 5→8 条，新增「历年笔试形式：怎么组织」「常见题型结构(通用)」「时间分配与做题顺序」；内容为通用规律表述并注明"以当年招聘公告和笔试通知为准"，未编造企业名/年份/真题。
  - **i人伙伴团**（`assets/i-partner.js`）：5 人格与 `replyStyle` 结构零改动；新增 10 场景话术库（拒绝/道歉/破冰/汇报/求助/表达不同意见/催进度/会议发言/接话闲聊/被夸回应，每条含方法 + 可直接照说的话术）、5 人格语气包装、`buildSystemPrompt`/`buildMessages`（人设+风格+场景话术+近 10 条上下文组装为 LLM 的 system+messages，含禁臆造硬约束）、`window.IPartnerLLM` 兼容钩子（未接 LLM 时行为与原先一致）。
  - **i人沟通专区**：`assets/mini-comm.js` comm-introvert 补「提前准备：开口前先写三行」「文字辅助：说不出就先打字」两条，并**镜像进 `高情商表达.html`「i人心法 · 沟通专区」**（466 行副标题坐实 + 473/474 行两张 `gq-mindset-card`，class/图标用法与既有 5 张一致），用户在界面上可见可用。
  - **自测**：`node --check` 三 js 全过；`tools/qa/_se5_smoke.js`（32 项断言）与 `tools/qa/_se5_gq_check.js`（jsdom 校验，24 项断言）全 PASS。

---

# 十、内容诚实性与入口修正

## 需求 24｜私聊去掉「演示模式」误导标签（P2） — ✅ 已完成

- **需求**：移除「演示模式」标签，或改「在线」状态显示。
- **状态**：随副本合并带入修复并上线（20260913h）——`assets/chat-local.js` 好友会话不再挂「演示模式」误导标签（见该文件内「需求24」注释）。
- **备注**：`私聊.html` 中另有 AI 助手面板副标题「本地演示模式 · 历史已保存」，属独立功能（该 AI 确为本地规则引擎、不联网），非本需求所指的好友会话标签，不影响本条判定。

## 需求 25｜群聊功能实现或移除入口（P2） — ✅ 完成

- **需求**（二选一）：① 实现群聊（建群/群消息/成员列表）；或 ② 移除入口。
- **状态说明**：**群聊已完整实现**——2026-09-11 批次一建群/群消息/成员列表 + 批次四群设置面板（T02）；2026-09-12 T03 好友页加「我的群聊」分组（`chat-local.js` 动态渲染）。需求 14 的群聊富媒体待补。

---

# 十一、AI 与内容深度

## 需求 27｜i 人伙伴团 AI 回复深度优化（P2） — ✅ 基本完成

- **需求**：优化 AI prompt 与上下文，回复针对用户输入给可操作沟通建议与话术示例。
- **状态说明**：`i-partner.js` 5 人格含 `replyStyle`（先共情再建议/结构化建议）+ 具体话术示例（三段式结构、万能回答公式等）。深度可随 B4 进一步打磨，功能层面已兑现。
  - ⚠️ **勘误（2026-09-14 实测）**：台账此前记「5 人格已接真 LLM」，但盘上 `i-partner.js` 的 `generateReply()` 实为本地模板关键词匹配，全仓无 LLM 调用点。需求 28 本轮已补齐 prompt/上下文（`buildSystemPrompt`/`buildMessages`，人设+replyStyle+场景话术库+近 10 条上下文）并留 `window.IPartnerLLM({partner, systemPrompt, messages}, cb)` 兼容钩子；将来接真 LLM 按此签名对接即自动生效，未接时维持本地模板回复不变。

---

# 十二、产品战略建议

## 需求 29｜做减法 + 建立学习闭环（战略） — 🟡 部分完成

- **建议**：① 做减法——保留约 15 个扎实功能库集中做深；② 建学习闭环——把词汇打卡的艾宾浩斯间隔重复/进度追踪/效果评估推广到所有学习模块（学-练-复-评）。
- **状态说明**：**做减法部分**——首页精简已做（批次五 T01）+ C 类删减（03/04/05/12）已随本批执行上线；**学习闭环未推广**（仅词汇打卡有艾宾浩斯）。

---

# 十三、首页数据真实性

## 需求 30｜清理首页硬编码假数据（P2） — ✅ 完成

- **需求**：清理首页所有硬编码假数据，进度/薄弱点/最近学习均从真实记录动态计算；无数据时显示空状态引导。
- **验收标准**：新账号首页进度为 0/空状态；学习后首页随真实记录变化；不同账号数据各自独立。
- **状态说明**：批次三完成——`computeModuleProgress` / `computeWeakPoints` 实时计算 + 空态三件套（`renderEmptyState` + `.empty-hint`）+ `activityLog` 环形日志（上限 200 条，三级回退：activityLog → 旧档日期 → 空态）。✅ 该批次已随副本合并并入主项目并于 20260913h 上线。

---

## 附：批次映射（执行建议）

| 批次 | 需求 | 现状 |
|---|---|---|
| 🔴 P0-A 数据安全与一致性 | 15、16、18、23、09 | 全部 ✅ 已上线 |
| 🟠 P0-B 诚实化与基础安全 | 19、22、17、21 | 19✅ 22✅ 21✅，17⏸️撤回 |
| 🟡 P1 内容填充与去同质化 | 20、28、26（+ B3–B6 手册模块） | 26/28 ✅ 已完成，20 ❌ 未开工 |
| 🟡 P2 聊天跨端专项 | 13、14、25 | 25✅，13/14 🟡 |
| 🟡 P2 标签与入口修正 | 24、27、30 | 24✅ 27✅（含 2026-09-14 LLM 接线勘误） 30✅ |
| 🔵 战略 | 29 | 🟡 |
| 横向批次 | 图标升级（02/06 等）、功能删减（03/04/05/07/12） | 02 ✅基本完成，06 ✅，03/04/05/07/12 全部 ✅ |

## 附：两份工作副本差异（已合并）

| 位置 | 内容 | 状态 |
|---|---|---|
| 主项目 `D:\下载的文件\学习工作台` | 已并入 worktree「批次三」成果，HEAD `8ef7f49` | 已提交 |
| 合并后本批 20260913h | B1-B4 删减 + C1-C3 重构 + E1 图标 + A2/A4/T16/T18 复核 + 引用驱动补 bump | commit `c1bb3f7`，已上线 |

> **副本已合一（主项目 HEAD `8ef7f49`）并于 20260913h（commit `c1bb3f7`）上线；worktree 不再是独立线。** 30/19/21/27 等 worktree 成果与 22/23/15 等主项目成果现已同源。
