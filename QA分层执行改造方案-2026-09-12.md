# QA 分层执行改造方案（星途平台）

> 目标：在**不降低缺陷检出率**的前提下，把当前「每次改动都跑全量回归」的 QA 流程改造成分层执行，将单次反馈从 5-15 分钟压缩到秒级，全量回归只在部署前跑一次。
> 适用范围：星途平台前端（`tools/verifier` jsdom 校验 + `tools/qa/*` 用例）+ 部署前验证。
> 编制时间：2026-09-12 16:20

---

## 一、问题陈述（为什么要改）

### 现状
每次代码改动后，都会启动一轮 jsdom 全量回归：

- `tools/qa/qa_t04t05_0912_*.js` 共 6 个脚本、**2627 条断言**
- 每个页面都要真加载并执行 `app.js`（≈360KB）、`api.js`、`icon-map.js` 等全部脚本
- 单轮耗时 **5-15 分钟**
- 输出为超长文本，全部回灌进对话上下文

### 痛点
1. **反馈太慢**：改一行代码，要等十几分钟才知道有没有问题
2. **时机错位**：全量回归被当成「改完就跑」的快筛用——**总计覆盖量没变，却发现得晚**
3. **上下文浪费**：2000+ 条 PASS 明细进入模型上下文，是纯消耗，且会挤压后续推理空间
4. **串行等待**：6 个脚本依次执行，本可并行
5. **重复劳动**：工程师已做 `node --check` + DOM 配平，QA 又做一遍

### 关键判断
> 质量保障**不依赖**「每次全量」，而依赖「**在正确的时机跑正确的测试**」。

---

## 二、改造原则（不容妥协的质量底线）

| 编号 | 原则 |
|---|---|
| P1 | **全量回归（L3）任何情况下不得跳过或降级**，部署前必须完整跑过一次 |
| P2 | **现有 2627 条断言一条都不许删除**，只重新编排执行时机 |
| P3 | L1 快筛必须**前置到工程师自测环节**，不能等到 QA 阶段 |
| P4 | 所有层的输出必须**落盘**，对话只接收「FAIL 行 + 汇总行」 |
| P5 | 改造后必须做**有效性自证**（见第六节），证明快筛不是空转 |
| P6 | 建立**基线快照**机制，未改动页面走哈希核验而非重跑 |

---

## 三、三层执行模型

### L1 · 快筛层（秒级，每次改完立刻跑）

**目标**：捕获该项目历史上最容易踩的、代价最高的几类错误。

**检查项（必须全部实现）**：

| # | 检查项 | 说明 | 历史教训 |
|---|---|---|---|
| 1 | `node --check` | 所有本次改动的 `.js` 文件语法校验 | 基础项 |
| 2 | HTML 结构配平 | 每个改动页面的 `div` / `style` / `script` 标签开闭配对 | Edit 的 `old_string` 含 `</style>` 时漏掉闭合 → 整页白屏 |
| 3 | **版本号令牌扫描** | ① 新版本号令牌存在；② **旧版本号令牌计数必须为 0** | 缓存坑；旧令牌残留 = 用户拿到旧资源 |
| 4 | **版本号损坏特征扫描** | 正则 `\.js\?v=...["][^>]` | 批量替换正则跨行吞注释 → 整页空白 |
| 5 | **localStorage 未前缀键残留扫描** | 扫描业务键（`study_workbench_data` / `study_workbench_tool_<工具>` / `study_workbench_recent` 等）是否仍有未加账号前缀的裸用 | 数据隔离（需求 15）回归风险 |
| 6 | 资源引用完整性 | 页面新增的 script/link 是否存在对应文件 | 防止引用不存在的资源 |

**输出**：`PASS/FAIL` + 失败详情（行号 + 期望/实际）。**耗时目标：< 10 秒**。

---

### L2 · 定向层（1-3 分钟，改完一个需求就跑）

**目标**：只跑本次改动所涉及页面的验收用例。

**实现方式**：
- 从现有 `qa_t04t05_0912_*.js` 中**按页面维度拆分**出可独立运行的用例文件
- 命名约定：`qa_<页面简称>_<日期>.js`，例如 `qa_profile_0912.js`（个人中心）、`qa_home_0912.js`（首页）
- 执行：`--target 个人中心` 只加载对应页面 + 对应用例
- 每个页面的用例保持完整（不做断言删减），只是**不再和其他页面捆绑执行**

**输出**：该页面全部断言结果。**耗时目标：单页 < 3 分钟**。

---

### L3 · 全量层（5-15 分钟，**部署前必跑一次**）

**目标**：完整回归，质量底线。

**实现方式**：
- 调度全部 `qa_*.js` 用例文件
- **并行执行**（`Promise.all` 或并行子进程），不要串行
- 结果与**基线快照**逐条比对，断言数不得少于 2627
- 全量输出落盘到 `tools/qa/_qa_full_<时间戳>.txt`

**输出**：汇总（总断言数 / PASS / FAIL / 失败清单）。**只有当失败清单非空时，才把失败详情回传对话。**

---

### 基线快照机制（L3 的加速器）

1. 每次 `--full` 通过后，生成基线文件 `tools/qa/_baseline.json`：
   ```json
   {
     "generated_at": "2026-09-12T16:20:00+08:00",
     "version": "20260912a",
     "pages": {
       "个人中心.html": { "sha256": "<文件内容哈希>", "assertions": 231 },
       "学习工作台.html": { "sha256": "<文件内容哈希>", "assertions": 512 }
     },
     "total_assertions": 2627
   }
   ```
2. 下次回归时：
   - 页面文件哈希**未变** → 只核验「断言数一致 + 版本号一致」，标记 `SKIPPED_UNCHANGED`（秒过）
   - 页面文件哈希**变了** → 跑该页完整用例
3. **汇总必须同时报告跳过的页面清单**，让人清楚知道哪些是哈希核验通过的、哪些是真跑的。

> ⚠️ 哈希核验**不等于**不检查——它能发现「意外变更」（比如脚本误改了不该动的页面），这本身就是一种有效检查。

---

## 四、交付物与接口约定

### 4.1 统一入口脚本 `tools/qa/qa_run.py`

```
用法：
  python tools/qa/qa_run.py --quick              # L1 快筛
  python tools/qa/qa_run.py --target <页面名>     # L2 定向
  python tools/qa/qa_run.py --full               # L3 全量（部署前）
  python tools/qa/qa_run.py --full --update-baseline   # 全量通过后更新基线

退出码：
  0 = 全部 PASS
  1 = 存在 FAIL
  2 = 脚本自身执行异常
```

### 4.2 输出约定

| 输出 | 位置 | 是否回传对话 |
|---|---|---|
| 完整明细 | `tools/qa/_*.txt`（已在 `.gitignore`） | ❌ 不 |
| FAIL 清单 | 落盘 + 对话摘要 | ✅ 是 |
| 汇总行 | 对话 | ✅ 是 |
| PASS 明细 | 仅落盘 | ❌ 不 |

**汇总行格式（固定）**：
```
[L1 QUICK] PASS=6 FAIL=0 | 耗时 7.2s | 检查项: syntax/dom/token/version-key/ls-prefix/assets
[L2 TARGET 个人中心] PASS=231 FAIL=2 | 耗时 96s | FAIL: 路由-点发帖统计应进统计页(实际:资料页)
[L3 FULL] PASS=2625 FAIL=2 | SKIPPED_UNCHANGED=18/24 | 耗时 8m12s | 断言总数 2627 (基线持平)
```

### 4.3 并行执行

6 个用例脚本之间无共享状态 → 全部并行启动。实现方式二选一：
- Node：`Promise.all([...].map(f => run(f)))`
- Python：`concurrent.futures.ProcessPoolExecutor` 或 `subprocess.Popen` 并发

---

## 五、执行时机矩阵

| 时机 | 执行层 | 责任人 |
|---|---|---|
| 工程师改完代码、提交自测 | **L1 快筛（必跑）** | 工程师 |
| 单个需求改完 | **L2 定向** | 工程师或 QA |
| 一批需求全改完 | **L3 全量** | QA |
| 部署前 | **L3 全量（必跑，不可跳过）** | QA |
| 部署后 | 线上探针四件套 | 部署方 |

> L1 前置到工程师环节是本次改造的**关键收益点**：问题在开发阶段秒级暴露，不再堆积到 QA 阶段才发现。

---

## 六、有效性自证（这是「有保障」的前提）

> 改造完成后**必须**做这一步，否则不能声称快筛可靠。

### 自证方法：故意构造失败，确认能被捕获

| 层 | 构造的故障 | 期望结果 |
|---|---|---|
| L1 | 把某个页面的版本号改回旧值（如 `20260912a` → `20260911i`） | L1 **必须**报 FAIL（旧令牌残留 > 0 / 令牌不一致） |
| L1 | 删掉某个页面的 `</style>` 闭合标签 | L1 **必须**报 FAIL（结构配平） |
| L1 | 在某个 js 里插入语法错误 | L1 **必须**报 FAIL（`node --check`） |
| L1 | 把 `study_workbench_data` 去掉账号前缀写回代码 | L1 **必须**报 FAIL（键前缀扫描） |
| L2 | 在对应页面用例里注入一个必然失败的断言 | L2 **必须**报 FAIL，且失败定位准确 |
| L3 | 复跑时确认断言总数 = 2627 | 数量不得减少 |

### 反向对照（证明检查非空转）

参考项目已有的成功先例：`zipalign` 校验曾用 `-c 5`（应通过）/ `-c 8`（应不通过）反向对照，证明检查确实在工作。

L1 同理：**构造一个「干净」状态必须全 PASS，构造一个「脏」状态必须报 FAIL**，两者都成立才算检查有效。

---

## 七、验收标准

改造完成的判定条件（全部满足）：

1. ✅ `qa_run.py` 三种模式均可正常运行，退出码语义正确
2. ✅ L1 耗时 < 10 秒；L2 单页 < 3 分钟；L3 与改造前同量级（并行后应更快）
3. ✅ L3 断言总数 **≥ 2627**，与基线持平，**一条未删**
4. ✅ 第六节「有效性自证」全部通过（每一类故障都被对应层捕获）
5. ✅ 输出全部落盘，对话只收到汇总行 + FAIL 清单
6. ✅ 基线快照机制工作正常，未改动页面正确标记 `SKIPPED_UNCHANGED`
7. ✅ 提供一份**前后对比报告**：改造前总耗时 / 改造后各层耗时、检出缺陷类型清单

---

## 八、实施顺序

| 步骤 | 内容 | 说明 |
|---|---|---|
| 1 | 拆分现有用例为按页面独立的 `qa_<页面>_<日期>.js` | 纯搬运，不改断言内容 |
| 2 | 实现 `qa_run.py` 三角色入口 | 先串行跑通，再加并行 |
| 3 | 实现 L1 六项检查 | 复用现有 `ed_scan_versions.py` / `check_icons_0912.js` 等脚本的能力 |
| 4 | 实现并行调度 | 6 个脚本并发 |
| 5 | 实现基线快照 | 生成 / 比对 / 报告跳过清单 |
| 6 | **执行有效性自证** | 第六节全部用例 |
| 7 | 输出前后对比报告 | 交付物 |

---

## 九、硬性约束（重申）

1. 🔴 **L3 全量回归不得跳过或降级** —— 这是质量底线
2. 🔴 **现有 2627 条断言一条不许删** —— 只重排执行时机
3. 🔴 **改造必须自证有效** —— 未通过第六节验证不得宣称完成
4. 🔴 **改造期间不得干扰正在运行的 QA 任务** —— 先等当前批次判定出结果
5. 🔴 遵循项目铁律：不写死、不删原功能、保留 DOM；改动落 `.gitignore` 已覆盖的 `_*.txt` 输出文件

---

## 十、预期收益

| 指标 | 改造前 | 改造后 |
|---|---|---|
| 单次反馈耗时 | 5-15 分钟 | **L1 < 10 秒** |
| 缺陷发现时机 | 部署前 | **开发中即时** |
| 全量回归频率 | 每次改动 | **部署前 1 次（覆盖量不变）** |
| 对话上下文占用 | 2000+ 行 PASS 明细 | **仅汇总行 + FAIL 清单** |
| 脚本执行方式 | 串行 | **并行** |
| 未改动页面 | 每次全跑 | **哈希核验 + 报告跳过清单** |
| 缺陷检出率 | 基线 | **不下降**（L3 完整保留） |

---

*编制：交付总监（Qi） · 2026-09-12 · 提交对象：软件开发团队执行方*
