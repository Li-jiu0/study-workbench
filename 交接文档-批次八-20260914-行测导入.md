# 交接文档-批次八-20260914：批次七上线复盘 + 行测5000题结构化导入

> 交接时间：2026-09-14 23:50 前后。前序文档：交接文档-批次七-20260914-R49-R61.md（worktree main-6599a366）。
> 本文档记录两件事：① 批次七 R49–R61 的上线结果与收尾；② 行测5000题剩余 4 本的结构化导入任务（进行中，本文写明精确续作方法）。

---

## 一、批次七 R49–R61：已上线、已推送（完结）

- **线上版本 `20260914f`**，部署于 2026-09-14 晚（脚本 `tools/deploy_update_20260914f.py`）。
- 部署验证全绿：116 文件 MD5 全一致、DB 备份 208896B、py_compile OK、服务重启后 active / NRestarts=0、站点与 /api/health 200、openapi 已注册 `/api/uploads/image` 与 `/api/groups/{gid}/transfer`、DB 22 表 / 11 用户（未减少）、日志 traceback=0。
- **GitHub**：`origin/main = c50758e`（`af0cbff` 批次七主体 137 文件 +29824/-333，外加 `.gitignore` 追加 `tencent_2025_interim/*.pdf`）。**两个 PDF 已按用户要求剔除**，规则已入库。
- 批次七遗留（非阻塞）：DB 11 用户超 SOP 基线 6（清理需用户点头）；QA 提过的 `__imConsumeForward` 返回完整 URL 而非裸 `#note=` 深链（如需短链改 私聊.html:603 附近）。

## 二、部署流程的硬经验（下次直接照做）

1. **同步清单必须含 server/**：纯前端同步会漏掉后端三件套，靠部署脚本 dry-run 的「本批必上文件硬断言」拦截（本次就是这样救回来的）。
2. **worktree 是唯一代码树**：`main-6599a366` 改完 → 同步部署源 `D:\下载的文件\学习工作台` → 部署。直接改部署源的改动会在下次同步时被抹掉（718 题差点丢过）。
3. **push 一律走 Clash 7897**：`git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push`。默认 env 代理 62083 小请求能通、大传输必断（502 / SSL unexpected eof）；直连被墙。
4. **PowerShell 捕获 git 输出不可靠（常吐空）**：用 node `execFile` 执行 git、结果写文件再 Read。中文路径直接传 `git -C` 没问题。
5. 本机 bash 缺 coreutils（ls/rm/grep 等 command not found）；python 用 `C:\Users\ATM\.workbuddy\binaries\python\versions\3.13.12\python.exe`，node 用 `C:\Users\ATM\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`。

## 三、行测5000题结构化导入（进行中，本文是续作指南）

### 3.1 目标与已完成

把《行测5000题【26年3月版】》OCR 结果结构化并导入工作台行测模块。OCR 根目录：
`D:\下载的文件\行测5000题【26年3月版】（最新27版）\_ocr_work\`

| 书目 | 状态 |
|---|---|
| 数量关系（题本） | ✅ **718 题已上线**（20260914f，app.js 内联） |
| 政治理论与常识判断（题本） | 🟡 **questions.json 已产出**（648KB，`常识判断_题本\`），**未经复核** |
| 资料分析（题本） | 🟡 适配脚本 `build_book_questions_analysis.py` 已建，json 未产出 |
| 言语理解（题本） | 🔴 无产出（子代理被暂停） |
| 判断推理（题本） | 🔴 无产出（子代理被暂停） |
| 5 本解析册 | 🔴 未开始（结构化后与题本二次关联，解析字段 `x` 现为占位） |

### 3.2 导入管线（已确认的架构，续作必读）

```
OCR .json --build_book_questions*.py--> .questions.json（原始结构化，stem是行数组，含出版页垃圾行）
          --[转换层：读 questions.json → 清洗 → {id,type,sub,diff,q,o[4],a,x,tip}]--> INLINE_XXX_BANK
          --自执行 IIFE--> app.js（worktree）
```

- **bank 字段格式**（照 `assets/app.js` L424-438 的 `INLINE_QUANT_BANK`）：`{"id":序号,"type":"模块名","sub":"子类","diff":1~3,"q":"题干","o":["A","B","C","D"],"a":正确索引,"x":"解析","tip":"提示"}`。
- **id 分配**：数量关系占了 187–904（718 题）。新模块各分配独立连续区间（建议：言语 905 起、常识 1905 起、判断 2905 起、资料 3905 起，各留 1000 余量），**绝不与已有 id 冲突**（`mergeExamBankQuestions(bank,false)` 同 id 会静默跳过）。
- **type 字段**：言语理解 / 常识判断 / 判断推理（sub 按图形推理、定义判断、类比推理、逻辑判断）/ 资料分析。
- 🔴 **注入位置铁律（R50 的教训）**：新 IIFE 必须插在 **worktree `assets/app.js` 的 L439 起**（数量关系 IIFE 之后、`examInit` 首次调用之前）。**绝不能追加到文件末尾**——末尾晚于 examInit 执行，首屏会只显示内置 60 题（R50 就是这个 bug）。
- 转换层参考：`_ocr_work\inject_safe.py`（数量关系当时用的注入脚本，**先读它再写转换**，弄清当时 x/tip 是怎么生成的——题本无解析，现有 x/tip 疑为 LLM 简解）。
- 切图：判断推理（图形题）与资料分析（表格材料）的 images\ 引用，需确认工作台渲染端如何解析 `[IMG:文件名]` 标记（数量关系的分数切图已有一套约定，照抄）；切图文件需同步部署源对应目录。

### 3.3 结构化关键逻辑（脚本已实现，复核时盯这四条）

1. OCR 页序可能乱，必须按 `page_no` 排序；
2. 答案速览是 4 列布局，必须按**全局 y 序**处理，不能按列；
3. **每个考点题号从 1 重编**，答案按考点分组存储；
4. 去重键 = section_key + qid；答案关联：精确匹配 → 考点+难度匹配 → fallback。

### 3.4 续作步骤（按序）

1. **复核常识判断**的 questions.json（抽样对照 pages\ PNG；重点查"政治理论"部分引号书名号是否切坏）；
2. **重启三条未完成的线**（见 3.5）；
3. 每本完成后：写转换脚本（或复用/改造 inject_safe.py）→ 注入 worktree app.js（L439 起）→ `node --check` → 部署源同步 → 浏览器验证题数与首屏显示 → 用户点头后部署（部署含重启？**纯前端不用重启**；app.js 改动 bump `?v=` 版本戳）；
4. 全部题本完成后做 5 本解析册 → 结构化 → 按「题干相似+考点+题号」关联到 `x` 字段。

### 3.5 子代理续作方式

团队 `software-xt-r4961` 的成员与任务书都还在，**优先 SendMessage 唤醒续作**（省重新交底）：

| 成员 | 任务 | 状态 |
|---|---|---|
| `data-common` | 常识判断结构化 | 已产出，可让它自检汇报或直接弃用 |
| `data-verbal` | 言语理解结构化 | 被暂停，SendMessage「继续原任务」即可 |
| `data-judge` | 判断推理结构化（含图形切图） | 同上 |
| `data-analysis` | 资料分析结构化（含表格切图） | 脚本已写一半，同上 |

若唤醒失败（429 等），重派时用 `general-purpose` 类型（**不要用 `software-engineer`，它没有 Bash 工具**）；模型按当时额度选（`lite` 最省，`reasoning` 429 风险低但贵）。429 是按模型分别计的滚动窗口限流，与积分余额无关（积分 3358 充裕）；UI 切模型救不了子代理，只能 spawn 时指定 `model`。

### 3.6 用户原指令要点（完整版以用户 2026-09-14 23:10 消息为准）

- 顺序建议：言语/常识（纯文字）→ 判断推理（图形切图，OCR 框坐标直切，不需要 DET）→ 资料分析（表格切图）→ 解析册关联；
- **每完成一本立即导入验证**，不攒批；
- file:// 下 fetch 分片失败 → 必须内联注入，不能只改 ext json；
- 注入不得修改 EXAM_BANK 数组本身（TDZ 坑）。

---

## 四、快速事实卡

- worktree：`C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-6599a366`（唯一代码树）
- 部署源：`D:\下载的文件\学习工作台`；部署脚本模板 `tools/deploy_update_20260914f.py`
- git：`origin/main = c50758e`，main 有 upstream（`git push` 即可，走 7897）
- 题库 OCR：`D:\下载的文件\行测5000题【26年3月版】（最新27版）\_ocr_work\`
- 数量关系内联 IIFE：worktree app.js **L424-438**；新模块插 L439 起
- id 占用：187–904（数量关系 718 题）；内置题 id < 187
