# 任务清单 · 待办总览（2026-09-15 23:25 盘点）

> 范围：Wave 2 收口遗留 + 个人中心反馈 + 全站 AI 升级 + 历史遗留。
> 原则：需要决策的点已由主理人按最优意见定掉（见 §五 决策记录），执行时照此文档走，不再回头确认。
> 代码树铁律照旧：唯一可写树 `D:\下载的文件\学习工作台`；老 WebView 语法禁令；ADR-3；bump 全局互斥；绝不 `git add -A`；文件清理永远最后一步。

---

## 一、A 组 · Wave 2 收口（P0，今晚主线，顺序执行）

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| A1 | 热修 3：`更多.html` 撤分组收放恢复平铺 | ⬜ 未做 | 方案已定位到行号（交接文档 §3），无需 bump |
| A2 | 提交 Wave 2 白名单（14 M + 更多.html = 15 文件） | ⬜ | 仿 `tools/commit_20260915f.py`，绝不 add -A |
| A3 | 部署（tar + pscp + 远端先备份） | ⬜ | 仿 `tools/deploy_update_20260915f.py` |
| A4 | 线上复检（9~11 页 HTTP 200 + 标记命中 + 禁用语法 0） | ⬜ | 仿 `tools/release_20260915f.py` |
| A5 | GitHub 推送（代理探测 → ls-remote rc=0 → push） | ⬜ | 仿 `tools/push_20260915f.py` |
| A6 | 任务执行文档追加 Wave 2 章节 | ⬜ | 与 A4 可并行 |

## 二、B 组 · 个人中心反馈（用户 23:13 截图，交接文档 §6）

| # | 任务 | 优先级 | 说明 |
|---|---|---|---|
| B1 | 「我的动态」feed-btn 挪位：从资料卡上方大橙钮收进资料卡操作区（次按钮） | P1 | 仅 `个人中心.html` 页内，**无需 bump**，可挂在 A 组后单独小提交 |
| B2 | 个人资料卡布局/内容重构（头像+信息左右结构、操作区分层、退出登录降级） | P1 | 改 `assets/api.js` `_renderProfileOnline/_renderProfileLocal`，**需 bump → 进 C 组 P0 同波** |

## 三、C 组 · 全站 AI 升级（大波，按 P0→P3 分批，每批真机验证真 AI 再进下一批）

### C-P0 底座与入口（含统一 bump + 部署）

| # | 任务 | 说明 |
|---|---|---|
| C1 | 新建 `assets/ai-config.js` | 照《问AI页面开发文档》10.2.3 原文：2 平台公共 Key、11 模型、fallback 链、关键词、推荐问题、rateLimit；**并入需求工单 7 个 funcType 分工表** |
| C2 | 新建 `assets/ai-service.js` | 统一 `callAI(funcType, messages, opts)`；SSE 流式；429 退避 2s 重试一次再降级；10 次/分钟限频；**`?.` 等禁用语法全部改写兼容写法** |
| C3 | 新建 `assets/ai-presets.js` | 离线兜底问答库（demoStyle 保留为最后一层，不删） |
| C4 | 新建 `AI.html`（DeepSeek 风格优化版，见决策 D1） | 欢迎页/对话页/历史侧栏/模型选择/自定义模型/拍题多模态 |
| C5 | 左主导航「首页」下方加「AI」一级入口 | sparkles 图标 icon-map.js 已有（L320）；导航渲染处确认在 app.js 还是页内 |
| C6 | 26 个页面底部第 4 Tab「工具」→「AI」 | 脚本原子批量替换；工具.html 去 active；设置.html 保 data-page-node-id；学习工作台.html 特殊结构 |
| C7 | 更多页删「内容直达」5 卡（在 A1 热修 3 落地后的平铺结构上删，工单里 `MP_GROUPS` 说法按过时处理） | 保留「工具箱」卡；5 个页面文件不删 |
| C8 | app.js 旧 `fetchAiReply` 内部改为委托 callAI（保签名兼容），不并存两套 Key/fetch | 见决策 D3 |
| C9 | 统一 bump + QA（node --check + 禁用语法 Grep + ADR-3）+ 部署 + 推送 | 波末单点 |

### C-P1 核心学习

| # | 任务 | 说明 |
|---|---|---|
| C10 | AI 模拟面试真升级（AI模拟面试/商务礼仪面试/面试题库） | interview 真出题不重样、按本次回答真点评、可追问、longtext 综合报告；**基于 Wave 2 新页面结构做** |
| C11 | 行测/央国企「AI 讲解」 | reasoning（数学）/general（言语常识）；五段结构；题干+选项+答案+用户选择全传；可展开讲解区 + 换个讲法/出同类题 + 拍题 |
| C12 | 申论批改 | 材料随题必传；采分点逐条对照；公文题评格式；大作文五维评分+逐段建议+范文片段；可修改后重批 |

### C-P2 内容生产

| # | 任务 | 说明 |
|---|---|---|
| C13 | 四级备考/词汇 | 精听句讲解、口语文本评（不假装听声音）、作文/翻译批改、单词例句助记 |
| C14 | PPT 系列 | 贴文字出分页大纲+文案+排版建议；版式库/案例库「AI 仿写」 |
| C15 | 发帖/广场辅助 | 润色/扩写/压缩/3 标题/自动标签/笔记结构化；结果回填编辑器；删技术细节文案 |
| C16 | 万能金句库 | 按主题实时生成 + 换一批 + 关键词定制 + 收藏进本地库 |
| C17 | 高情商/话术库/商务礼仪 | 场景+原话出多版回复（稳妥/幽默/正式）；情景题 AI 点评用真 AI 重做 |

### C-P3 分析增强

| # | 任务 | 说明 |
|---|---|---|
| C18 | 错题本 AI 分析 | 错因+考点+避坑+1~2 道变式题；模块级「最该补的 3 个考点」 |
| C19 | 学习数据 AI 诊断 | 读 localStorage 真实数据出个性化诊断与下周计划（首页/个人中心） |
| C20 | 悬浮助手上下文感知 | 带当前模块/当前题；意图跳转保留 |
| C21 | 拍题贯穿 | 所有讲题/批改入口统一走 vision（GLM-4.6V-Flash，base64 多模态格式） |

## 四、D 组 · 历史遗留与悬案

| # | 任务 | 优先级 | 说明 |
|---|---|---|---|
| D-1 | 数量关系题库回滚重做 | P0 遗留 | 排期待定，建议插 C-P0 之后 |
| D-2 | `common.css` L68 全局滚动锁 | P1 | 共享 CSS，跟 bump 波走 |
| D-3 | 「工具页我的导入题库跑到博客页」悬案 | — | 代码侧查无实据，**等用户补截图/文字描述**，不空转 |
| D-4 | Wave 3 备忘：app.js/api.js/common.css 大手术（共享文件波 A/B/C，天然串行）、全站 HTML 原子批量波（N9-9/N9-22B/N9-3）、收尾波（N9-7 app.js 拆分+bump+部署） | P2 | C 组 C8/C5 与共享文件波有交集，到时合并排 |
| D-5 | 文件清理（117 个 `??` 临时文件 + 备份目录） | P2 | **全批次最后一步，用户铁律** |

---

## 五、决策记录（主理人已拍板，执行照此，无需再确认）

- **D1 · AI.html 按 DeepSeek 当前布局风格优化**（用户明示参考 DeepSeek）：
  - 桌面端：历史侧栏**常驻**（260px，可一键收起为窄条），不做抽屉；对话区单列居中（max 768px）；移动端侧栏退回 85% 抽屉（原文档方案）。
  - 输入框参照 DeepSeek：大圆角（24px）、底部居中固定、按钮全部右置；**新增「深度思考」开关 chip**（开启 = 强制走 DeepSeek-R1-8B 低温，与「自动」路由互斥提示）。
  - AI 消息操作按钮（复制/重新生成）**常驻显示**（DeepSeek 风格），不做 hover 才出现；支持 Markdown 渲染 + 代码块等宽字体（讲题五段式需要）。
  - 全页无 emoji 无机器人头像，线性 SVG 图标；页面内只出现「AI」两字，文件名 `AI.html`（工单硬性要求，开发文档里 ai.html 的写法按笔误处理）。
  - 欢迎语保留工单文案「欢迎使用，随时开始吧」，推荐问题 4 卡从配置读、可按最近学习模块动态换。
- **D2 · 底座整合唯一化**：新建 `ai-config/ai-service/ai-presets` 三件套为**唯一** AI 底座；`app.js` 旧 `fetchAiReply/AI_PROVIDERS` 保留函数签名、内部改为委托 `callAI`，杜绝两套 Key 两套 fetch（工单明令）。
- **D3 · 语法兼容**：开发文档样例中的 `?.`（如 `choices?.[0]?.delta?.content`）一律改写兼容写法；`async/await` 允许（非顶层），SSE 读取放 async 函数内。
- **D4 · 时序**：A 组（Wave 2 收口）→ B1（顺手小提交）→ C-P0（含 B2 资料卡重构同波 bump）→ D-1 数量关系回滚 → C-P1（面试排头）→ C-P2 → C-P3 → D-2/D-4 择机 → D-5 清理收尾。
- **D5 · 公共 Key 保护**：rateLimit 10 次/分钟硬限 + 429 退避 2s 仅重试一次 + 智谱 1 并发前端排队，超限友好提示；预设问答与本地意图不受限。
- **D6 · 更多页**：A1 热修 3 先恢复平铺；C7 在平铺结构上直接删「内容直达」区块；工单中 `MP_GROUPS` 相关表述作废。
- **D7 · 不做项照工单**：不做语音识别/语音对话；不删任何现有功能与本地意图；不接收费模型；Key 不散写业务页；不做云同步（localStorage）。
- **D8 · 每批验收**：以「不填 Key 返回真实模型内容 + 断网降级不白屏 + 结果就地渲染」为准入下一批的硬门槛，真机点一遍。

---

*主理人齐活林 · 2026-09-15 23:25 成文。依据：交接文档-Wave2收口、需求工单、问AI页面开发文档、代码树实测。*
