# 需求文档：全站交互排查（第五批）

> 日期：2026-09-20
> 范围：全部 52 个 HTML 页面 + 
>
> `assets/`
>
>  交互脚本（app.js/chat-local.js/xt-moments.js/ai-page.js/api.js/voiceplayer.js/xt-region.js 等）+ 安卓壳 
>
> `MainActivity.java`
> 方式：交互信号脚本逐页扫描（表单、防重复提交、手势、分页、权限、媒体、前后台切换、二次确认）+ 关键链路人工读源码核对
> 原则：
>
> **只发现问题、不改业务代码**
>
> ；每条给 文件：行号、现象、改法、验收，供 workbuddy 直接动手
> 与前几批的关系：弹窗「安卓返回键先关弹层」见第四批 P2-3；聊天键盘遮挡 / 外壳隐藏见第一批；消息失败重发见第三批 U3、全局断网见 N2、外链跳浏览器 / 再按退出见 U1/U2，本篇不重复。



***

## 〇、先说结论

核心交互链路整体是扎实的：登录注册有防重复提交和完整校验、危险操作二次确认覆盖广、安卓权限被拒有引导、模考 / AI / 聊天发送都做了防连点、语音消息播放互斥、动态有下拉刷新和触底分页、离开考试 / 位置共享有保存草稿和停止逻辑（详见第三章）。

本批要处理的：



* **P2（真机高频功能受损，优先修）1 项**：App 内 WebView 选图**不支持多选、也没有拍照通路**—— 发朋友圈 / 动态一次只能选到 1 张图，聊天「拍照」可能调不起相机或拍完收不到图。

* **P3（交互一致性打磨）8 项**：搜索无防抖、语音播放的生命周期、关弹窗不收键盘、编辑资料未保存无提示、键盘键型 / 数字键盘、输入字数上限、弹层关闭方式不统一、好友申请列表分页；另列 2 个可选增强（非缺陷）。



***

## 一、P2 问题

### I1　App 内选图不支持「多选」和「拍照」：多图只收到 1 张，拍照可能拿不到图

**前端现状（都走&#x20;**`<input type=file>.click()`**，没有原生多选桥兜底）**



* 朋友圈 / 动态发布：`朋友圈发布.html:50` `<input type="file" id="xtmFileImg" accept="image/*" multiple>`、`:51` 视频 `xtmFileVid`；选图逻辑 `xt-moments.js:788/832/993/1122` 都是 `file.click()`。

* 我的文件多图上传：`我的文件.html:306` `<input … id="xfFiles" accept="image/*" multiple>`。

* 聊天：`私聊.html:246` 相册 `imImgInput`、`:248` `<input … id="imCameraInput" accept="image/*" capture="environment">`**（拍照）**、`:250` 文件、`:253` 录音 `capture`；触发在 `chat-local.js:2602/4947`。

* AI 附图：`AI.html:769` `aiFileInput`，触发 `ai-page.js:3318`；社区编辑器插图 `社区.html:271`、`blog_wechat.html:189`；头像 `个人中心.html:259`。

**安卓壳现状（**`MainActivity.java`**）**



* `onShowFileChooser`（`:1122-1157`）只构造了一个 `ACTION_GET_CONTENT` 单选 Intent：


  * **没有读&#x20;**`FileChooserParams.getMode()`**（**`MODE_OPEN_MULTIPLE`**），没有加&#x20;**`EXTRA_ALLOW_MULTIPLE`；

  * 图片分支（`:1134-1135`）只把 MIME 设成 `image/*`，**没有附加&#x20;**`ACTION_IMAGE_CAPTURE`**&#x20;拍照 Intent**（只有音频 `capture` 在 `:1145-1149` 附加了系统录音机）；

* `onActivityResult`（`:2743-2755`）只取 `data.getData()` **单个 Uri，没有遍历&#x20;**`data.getClipData()`。

* 工程里只有日志导出 `LogFileProvider`、APK 安装 `ApkFileProvider`，**没有图片拍照用的 FileProvider /&#x20;**`EXTRA_OUTPUT`。

**真机表现**



1. 朋友圈 / 动态「最多 9 图」、我的文件多图：在 App 内即便系统选择器里选了多张，H5 只收到第 1 张（多选被当成单选）。

2. 聊天点「拍照」：只弹普通文件选择，不一定有相机入口；部分机型即便能拍照，`ACTION_IMAGE_CAPTURE` 没给 `EXTRA_OUTPUT` 时返回的 `data.getData()` 为 null，**拍完收不到图**。

3. 取消选择这条已处理对（`:2745-2751` 取消时也 `onReceiveValue(null)` 释放了回调，不会卡死下一次选择）。

**改法（安卓壳，H5 基本不用动）**



1. `onShowFileChooser` 读取 `params.getMode()`，为 `MODE_OPEN_MULTIPLE` 时给 `ACTION_GET_CONTENT` 加 `Intent.EXTRA_ALLOW_MULTIPLE`。

2. `accept` 含 `image/*` 时，用 chooser 的 `EXTRA_INITIAL_INTENTS` 额外附加一个 `ACTION_IMAGE_CAPTURE` 拍照项；拍照通过 FileProvider 提供 `EXTRA_OUTPUT` 的目标 Uri（需新增一个开放缓存目录的 FileProvider，参照现有 `LogFileProvider` / `ApkFileProvider` 的自写方式，并在 Manifest 配 provider 与路径）。

3. `onActivityResult` 组装回传数组时：优先遍历 `data.getClipData()`（多选），其次 `data.getData()`（单选），拍照则回传事先给的 `EXTRA_OUTPUT` Uri；任何分支结束都要 `onReceiveValue(uris)`，取消回传 `null`。

4. 视频（`accept="video/*"`）同理可附加摄像录制，至少保证单选视频能回传。

**验收**



* 朋友圈 / 动态一次能选最多 9 张并全部正常发布、预览顺序正确；我的文件多图一次全部上传。

* 聊天点「拍照」直接调起相机，拍完即发；点相册可多选；录音仍能调起系统录音机。

* 中途取消后，再次点 + 号选图 / 拍照能正常弹出（无一次点击无反应）。

* 头像、AI 附图、社区插图等单选场景不受影响。



***

## 二、P3 问题（一致性 / 打磨）

### I2　内容库搜索框每敲一个字就全量检索 + 重绘，低端机卡顿，缺防抖



* 现状：万能金句库 `:101`、商务礼仪 `:101`、四级词汇 `:101`、场景话术库 `:168`、时政热点 `:210`、错题本 `:88`、面试题库 `:101`、申论刷题 `:220` 的搜索框都是 `oninput="globalSearch(this.value)"`，无防抖。

* `globalSearch`（`app.js:8595-8635`）每次按键都遍历全部博客发贴 + 模块索引、拼下拉 `innerHTML`，并在 `:8633` 调一次 `lucideAutoRender()`（图标重渲染）；`api.js:1012` 还有一份在线 globalSearch（在线场景更需要防抖）。

* 改法：对输入做 150～200ms 防抖（连续输入只在停顿后检索一次）；`lucideAutoRender` 限定只渲染下拉容器而非整页。

* 验收：低端机快速输入不卡顿、不掉帧，结果正确；清空输入立即收起下拉。

### I3　语音消息：开始录音不停止正在播放的语音；App 切后台语音继续外放



* 现状（做得好的部分）：语音播放是单例，点新语音会停旧的、再点同一条暂停、有进度条和结束复位（`chat-local.js:3340-3368`）。

* 缺口：

1. 开始录音（原生桥 `chat-local.js:2840`、网页录音 `:2809`）前没有先 `_va.pause()`，若上一条语音还在外放，**外放声音会被录进新语音 / 产生回声**。

2. `visibilitychange`（`:5831-5836`）切回前台只做补拉消息，**切到后台（hidden）不暂停语音播放**，按 Home 键 / 切其它应用后语音继续外放（隐私 + 反直觉，微信是切后台即停）。`pagehide`（`:5656`）只停了传输。

* 改法：录音启动入口统一先暂停当前语音并复位播放 UI；`visibilitychange` 为 hidden 时暂停语音播放（可参考 `live-location.html:577-579` 已有的 hidden 兜底写法）。

* 验收：播放语音中点录音，外放立即停；播放中按 Home 键 / 切应用，声音停；回到聊天页播放状态指示正确。

### I4　含输入框的弹窗关闭后不主动收起软键盘



* 现状：`closeAppModal`（`app.js:2846-2857`）只移除 `.active`、解锁背景滚动，**没有&#x20;**`document.activeElement.blur()`。意见反馈、重命名、编辑资料、AI 设置等弹窗内输入会弹起键盘，点遮罩 / × / ESC（监听在 `:2859-2871`）关闭后，焦点仍在已隐藏的输入框上，安卓 WebView 下**软键盘常悬停不收**。

* 改法：关闭弹窗前判断 `document.activeElement` 为 `input/textarea/contenteditable` 时调用 `.blur()`；该处理放进统一的关弹窗函数，各页自定义弹层也复用。

* 验收：任意弹窗输入弹起键盘后，点遮罩 / × / 返回键关闭，键盘同步收起。

### I5　「编辑资料」改了没保存就关弹层，内容静丢，无「放弃修改」提示



* 现状：`editProfile`（`api.js:472`）打开弹窗，只有点保存才提交（在线 `saveProfileEditor` `:505`、本地 `:1748`），直接关弹层（`:517` 移除 active / 遮罩 / 返回键）会把昵称、简介等改动静默丢弃。

* 对照：朋友圈发布在有内容时返回会弹「放弃本次编辑？」（`xt-moments.js:988-989`），资料编辑没有，体验不一致。

* 改法：打开时记录初始值，有关联改动且未保存时，关弹层走「放弃修改 / 继续编辑」确认（复用现有 confirmBox）；无改动直接关。

* 验收：改了昵称 / 简介直接关闭弹「放弃修改」确认，选放弃才丢弃、选继续回到弹窗；没改动时不打扰。

### I6　键盘右下角键型、数字键盘不统一



* 现状：私聊输入框、AI 输入框、朋友圈文案、群公告、各类弹窗 textarea 都没有 `enterkeyhint`，键盘右下角仍是默认回车，而不是「发送 / 完成」。数字类字段（如 AI 设置里的端口号、各类手机号 / 号码输入）也多未指定 `inputmode`，弹的是全键盘。

* 已做对的：登录页邮箱验证码 `inputmode="numeric"`（`登录.html:223`）。

* 改法：


  * 聊天 / AI 单行发送输入：`enterkeyhint="send"`（点击发送，配合现有发送逻辑；多行 textarea 如需回车换行可用 `"enter"` + 独立发送键，按现状选）；

  * 昵称、反馈、公告等普通填写：`enterkeyhint="done"`；

  * 纯数字字段（验证码、端口、手机号、QQ / 微信号中的数字项）：`inputmode="numeric"` / `"tel"`。

* 验收：各输入场景键盘右下角键型符合预期，数字字段直接弹数字键盘。

### I7　输入长度上限 / 字数提示不统一，部分文本域无上限



* 现状：多个公共弹窗 textarea（意见反馈、重命名、公告、简介等）以及私聊（3 处）、面测（3 处）、AI 设置（2 处）、社区 /blog 编辑器（2 处）的 textarea **没有 maxlength**（扫描逐页可见）。申论作答有实时字数统计 `slCount`（`申论刷题.html:803/820-821`，显示「字数 n / 上限」），练习场景不硬限制是合理的，保留。

* 风险：聊天超长消息、超长公告 / 昵称 / 简介可能被后端拒绝或撑坏气泡 / 排版，且用户没有字数提示。

* 改法：按后端约束给各文本域加合理 `maxlength`（昵称、群名、公告、简介、反馈、聊天消息等），聊天 / 动态等长文本给「n/N」字数提示；到达上限给出轻提示而不是静默截断。动态正文若已有上限请核对前后端一致。

* 验收：各输入达到上限有提示且不会发出后端拒收的超长内容；申论作答字数统计不受影响。

### I8　各页自定义弹层的关闭方式不统一（与第四批 P2-3 配套实施）



* 现状：公共 `.app-modal-mask` 已支持 ESC 关闭、点遮罩关闭（`app.js:2859-2871`）；但各页自定义遮罩 ——`.modal-overlay`、申论 `.sl-overlay`、时政 `.hn-overlay`、AI 设置 `.xt-modal-mask`、私聊 `.im-overlay`、登录 `.modal-mask` 等 —— 关闭方式不统一，有的只能点 × 或指定按钮，点遮罩 / 返回键不一定能关。

* 改法：结合第四批 P2-3 的「弹层统一压栈、返回键关闭」，一并统一三件套：① 返回键 / ESC 关弹层；② 点遮罩关闭（表单类弹层为防误触可保留二次确认，见 I5）；③ 右上角 ×；④ 关闭即收键盘（I4）。

* 验收：所有弹层关闭路径一致、可预期；不会出现某个弹层只能刷新页面才能关掉。

### I9　「好友申请」列表一次性全量加载，无分页（低频，可选）



* 现状：`好友申请.html:64/137` 一次渲染全部申请，无触底分页 / 加载更多。对照：动态有 IntersectionObserver + 触底 + 下拉刷新（`xt-moments.js`），聊天历史有分页（`chat-local.js` loadmore 相关）。

* 影响：自用 + 好友少，当前无感；申请很多时才有流量 / 性能问题。

* 改法（可选）：复用动态列表的分页模式，或至少做懒渲染。

* 验收：申请很多时滚动流畅、能加载更早的记录。

### I10　可选增强（非缺陷，按需排期）



* 会话 / 动态 / 文件 / 收藏列表支持**左滑快捷操作**（置顶 / 删除 / 收藏 / 取消好友），目前主要靠长按菜单。

* 首页、各内容库列表支持**下拉刷新**（动态已支持，可推广）。

* 数据列表的「加载失败点击重试」三态已在第四批 C7 提出，此处不重复。



***

## 三、已核查「做得好」的交互，请勿重复改



1. **登录 / 注册防重与校验**：点击提交立即 `disabled` + 文案变「登录中 / 注册中」，失败恢复、网络异常有离线兜底（`登录.html:569-630`）；回车在账号→密码→提交间流转（`:463-465`）；协议未勾选拦截（`:562-568`）；密码强度、两次一致、用户名 / 邮箱格式校验完整；输入框 maxlength、autocomplete、验证码 numeric 齐全。

2. **安卓权限被拒有闭环引导**：麦克风永久拒绝 → toast + 跳应用设置（`MainActivity.java:2704-2714`）；通知拒绝 → toast + 跳通知设置（`:2716-2730`）；定位拒绝 → toast + 引导手动选地区（`:2731-2738`）。H5 录音被拒有 toast（`ai-page.js:1241/1331`、`chat-local.js:3137`）；定位在 `xt-region.js` 被拒会降级 IP 定位 / 手动选地区（`:637-712 / :1646 / :1756`）。

3. **文件选择取消不卡死**：取消时也回调 `onReceiveValue(null)` 释放（`MainActivity.java:2745-2751`）—— 多选 / 拍照缺失见 I1。

4. **危险操作二次确认覆盖广**：个人资料 / 设置（xt-profile confirm 17 处）、动态（xt-moments 5）、聊天（chat-local 4）、AI 设置（3）、时政删除（hotnews 2）、3D / 视频生成（ai-cap 各 1）；朋友圈发布有「放弃编辑」确认；模考有 `beforeunload` 提示 + 主观题草稿持久化（`mock-engine.js:467-473`）；实时位置离开有 pagehide + beforeunload + visibilitychange 三重停止（`live-location.html:577-579`）。

5. **语音 / 音频**：语音消息播放单例互斥、再点暂停、进度条、结束复位（`chat-local.js:3340-3368`）；听说训练关闭时停止 TTS 并 `speechSynthesis.cancel()`（`voiceplayer.js:931`）。

6. **图片查看器**：支持双击 / 双指缩放、返回键 pushState 关闭（`img-viewer.js:405-476`）。

7. **高频按钮防连点**：模考引擎大量 disabled 锁定（mock-engine 47 处）、AI 面板（ai-page 14）、聊天（chat-local 19）、AI 服务（ai-service 4）、管理后台（admin 4）。

8. **列表分页 / 刷新**：动态 / 社区有下拉刷新 + 触底加载 + IntersectionObserver（xt-moments io=4 /loadmore=9 /pull=5）；聊天历史消息分页加载（chat-local loadmore 26 处）。

9. **前后台切换**：聊天切回前台立即补拉未读和当前会话（`chat-local.js:5829-5836`），不用干等轮询周期。



***

## 四、真机回归清单



1. 朋友圈 / 动态发 9 图、我的文件多图上传、聊天拍照即拍即发、相册多选、录音、取消后重选（I1，重点）。

2. 各内容库快速输入搜索，低端机不卡（I2）。

3. 语音播放中点录音、播放中按 Home 键 / 切应用（I3）。

4. 各弹窗输入后用 × / 遮罩 / 返回键关闭，键盘是否收起（I4、I8）。

5. 编辑资料改一半返回，是否弹放弃确认（I5）。

6. 各输入框键盘右下角键型、数字字段数字键盘（I6）；超长输入有提示（I7）。

7. 好友申请很多时滚动 / 分页（I9，可选）。



***

## 五、实施顺序建议



* **第一批（真机功能）**：I1 多选 + 拍照（安卓壳改造，含 FileProvider），这是唯一影响核心功能的项。

* **第二批（交互一致性，建议一起做）**：I4 关弹窗收键盘 + I8 弹层三件套（与第四批 P2-3 统一压栈一并实现）→ I5 编辑资料放弃确认 → I3 语音生命周期。

* **第三批（输入体验打磨）**：I2 搜索防抖 → I6 键型 / 数字键盘 → I7 字数上限 → I9 / I10 按需。