# 给接手 AI 的开场指令（2026-09-16 · 第三批交接）

> 用法：把下面【版本 A】整段复制，粘给接手的 AI，作为**第一条消息**发出去。
> 前提：那个 AI 能读写本机文件（能打开 `D:\下载的文件\学习工作台`）。

---

## 【版本 A】完整版（推荐 · 直接整段复制）

```
接手「星途」（原「学习工作台」）项目，继续做第三批的收尾工作。

【第一步 · 必做，不读完不要动代码】
先完整读这份「更新日志」，它的 §二 有一张 mtime 落盘实况表 —— 精确标出本批哪些文件真的改过、
哪些"看着像做了其实没做"：
D:\下载的文件\学习工作台\更新日志-20260916-第三批.md

再完整读这份「总纲」（现状、铁律、未完成项任务书、部署流程、回滚点）：
D:\下载的文件\学习工作台\交接文档-20260916d-第三批执行现状与对接.md

需要看波次排期/13线名册/接力链时读：
D:\下载的文件\学习工作台\计划书-20260916-第三批执行（分工与波次）.md

【铁律 · 违反会整页白屏，本项目已因此出过两次全站事故】
1. 唯一可写代码树 = D:\下载的文件\学习工作台。
   绝对不要碰 C:\Users\ATM\WorkBuddy\Worktrees\... —— 那是废弃副本，改了等于全部白干。
2. 页面要跑在安卓 APK 的老 WebView 里，内核约 Chrome 50~58，上限 ES2017。以下语法一律禁止：
   ?.   ??   .replaceAll(   Object.fromEntries   .at(   顶层 await
   正则后行断言 (?<= (?<!    对象展开 {...obj}    对象剩余解构    指数 **    可选 catch 绑定 catch {}
   注意：node --check 抓不到这些，必须用 Grep 逐项确认 0 命中。
   已验证的两个误报，别当 bug 改：** 在引号内是 markdown 字符串；... 在 [ 之后是合法数组展开。
3. 禁止用 var/const/let 重复声明别的文件已存在的全局名 —— 浏览器会判该文件 SyntaxError 并整文件拒绝执行
   （这是历史上「AI 功能集体失效」的真凶）。新增全局一律写成：
   if (typeof window.xxx !== 'function') window.xxx = function () { ... };
4. 禁原生 alert/confirm/prompt，用页面内 toast（window.xtToast）或页面内区块。
5. 改任何 assets/*.js 或 CSS 后需要 bump 引用它的 ?v= 版本戳；但项目约定「波内任务不 bump、波末单点统一」，
   目标新戳是 20260916M。补裸路径戳要用单行内正则，严禁跨行正则（跨行会吞掉 --> 和标签 = 整页白屏）。
   现状：全站 567 处全是 20260916L。另有一个历史遗漏：live_chat.html 仍是 20260911c，别漏。
6. app.js（被 33 个页面引用）、api.js（30 个）、common.css（全站）是共享文件 ——
   同一时刻只允许一条线改，并行写必然后写覆盖先写。
7. 本项目禁 git 写操作（push 需动态代理）。文件重命名用 os.rename，不要用 git mv。
   ⚠️ 判断「哪些文件被动过」只能用 mtime，不要用 git —— 本仓库 HEAD 很旧，
   git status 会列出几十个更早批次改的文件，极具误导性。本批开工时刻 = 2026-09-16 13:30。

【本机环境坑 · 会让你白浪费大量时间】
- bash 缺 coreutils：ls / grep / head / tail / wc / dirname / env / rm 全部不存在。
  → 一律写 python 脚本文件再执行（不要用 python -c 单行，正则里的引号会被 shell 吃掉导致 SyntaxError），
    或用 Read/Grep/Glob 工具；命令一律写绝对路径。
- Windows 中文路径 + python 控制台：print 中文会 GBK 崩溃 → 结果一律写 UTF-8 文件，再用 Read 工具读。
- len(str) 是字符数不是字节数 —— 量字节用 len(open(p,'rb').read())。
- python = C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe
- node   = C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe
- 换行符必须保持：根 HTML / app.js / api.js / common.css 是 CRLF；
  assets/ai-service.js、assets/ai-page.js、assets/ppt-works.js 是 LF。改混了会造成整文件 diff。
- 跑 QA 页面体检时必须显式指定没被占用的 QA_PORT（8911~8920），逼脚本自启内置 Node 服务；
  否则它会复用你手工起的 python -m http.server，把 assets/*.js?v= 全报成加载失败，
  看着像全站 js 崩了，其实是噪声。输出头必须显示「本脚本自启的本地静态服务」才可信。

【429 配额 · 影响你马上能不能开工】
本项目刚被 429 打停，配额 2026-09-17 13:32:39 UTC+8 重置。
- 配额耗尽期间：派子代理时在 spawn 时指定 model: "lite" 可立刻继续
- UI 右上角切模型对子代理无效 —— 子代理的 model 只能在拉起时指定，中途不可切换
- 子代理类型：工程师线用 software-engineer（有 Bash）；QA 线必须用 general-purpose
  （software-qa-engineer 没有 Bash，拉起后 6 秒即挂）

【先跑这三把闸确认基线，再决定做什么】
1) node tools/qa/escheck_es2017.js                      → 期望 DONE 0
2) python tools/qa/wave_check_20260916m.py "文件1,文件2"  → 逐文件 PASS/FAIL
3) python tools/qa/global_name_scan_20260916m.py         → 跨文件全局名冲突，门槛 FATAL=0 且 RISK=0
（完整命令见交接文档 §5）

【建议的接手顺序】
① 读更新日志 §二落盘实况表 → ② 跑三把闸 → ③ 收尾 assets/ai-service.js（AI 400 修复，最值钱）
→ ④ 收尾需求 C（AI 页输入框 + 模型按钮）→ ⑤ 并行启动本批零产出的 5 项
→ ⑥ 复核本批 12 个"落盘但未验证"的文件 → ⑦ 再进批次九 Wave 3~7
→ ⑧ 波末统一 bump → dry-run → 部署（部署需用户点头）→ 线上复检

【工作方式 · 用户明确要求，最高优先级】
命令/脚本各跑一次即判定，不要反复回跑、不要多轮回读自审。发现问题直接给结论 + 建议，
不要为了"再确认一下"多跑几轮。

【最后】
读完文档后，先复述你理解的三条最关键的铁律 + 你打算按什么顺序做，
并指出文档里任何你认为矛盾、含糊或信息不足的地方。先不要写代码。
```

---

## 【版本 B】精简版（只想让它先看懂项目）

```
读 D:\下载的文件\学习工作台\更新日志-20260916-第三批.md（先看 §二 落盘实况表），
再读 D:\下载的文件\学习工作台\交接文档-20260916d-第三批执行现状与对接.md，
严格按里面的「铁律」和「未完成工作」章节执行。重点四条：
① 唯一可写代码树是 D:\下载的文件\学习工作台，不要碰 C:\Users\ATM\WorkBuddy\Worktrees\...
② 老 WebView 内核 Chrome 50~58 上限 ES2017，禁 ?. ?? .replaceAll( {...obj} 等语法；
   node --check 抓不到，必须 Grep 逐项确认 0 命中
③ app.js(33页) / api.js(30页) / common.css(全站) 是共享文件，同一时刻只允许一条线改
④ 判断"哪些文件被动过"只能用 mtime，不能用 git（本仓库 HEAD 很旧，会误导）
先读完文档，然后告诉我你打算按什么顺序做。
```

---

## 【版本 C】如果那个 AI 访问不到本地文件

网页版聊天 AI 只能在对话里看内容，**必须把文档内容一起贴过去**。顺序：

1. **先贴** `更新日志-20260916-第三批.md` 的全文（最重要，含落盘实况表）
2. 再贴 `交接文档-20260916d-第三批执行现状与对接.md` 的全文
3. 按要做的需求挑着贴：`AI模型测试报告与修复需求-20260916.md`、`架构-批次九-全线改造-波次计划-20260915.md`
4. 然后发这句：

```
以上是星途项目的对接文档。请先通读，然后：
① 复述你理解的三条最关键的铁律；
② 列出文档里标记为「落盘但未验证」「零产出」的项，按你建议的执行顺序排一下；
③ 指出文档里任何你认为矛盾、含糊或信息不足的地方。
先不要写代码。
```

**注意**：这种情况下它**不能真正改代码**，只能产出方案或补丁文本 —— 真正落盘还得交给能读写文件的 AI。

---

## 附：优先让它读的文件清单（都在 `D:\下载的文件\学习工作台\`）

| 优先级 | 文件 | 用途 |
|---|---|---|
| ★★★ | `更新日志-20260916-第三批.md` | **先读**：本批 mtime 落盘实况 + 签名证据 + 未验证清单 |
| ★★★ | `交接文档-20260916d-第三批执行现状与对接.md` | **总纲**：现状、铁律、未完成项任务书、部署流程、回滚点 |
| ★★★ | `计划书-20260916-第三批执行（分工与波次）.md` | 13 线名册 + 接力链 + Wave 0~7 + 文件独占矩阵 |
| ★★ | `交接文档-20260916c-第二批执行现状与对接.md` | 上一份总纲（含 20 项实测校正表） |
| ★★ | `架构-批次九-全线改造-波次计划-20260915.md` | 批次九 23 项 7 波依赖分析 + 冲突矩阵 |
| ★ | `任务清单-待办总览-20260915.md` | A/B/C/D 四组待办 + 已拍板的 8 条决策 |
| ★ | `用户需求与决策总账.md` | 全部需求与拍板记录（**已拍板项照办，不要再问**） |
| 按需 | `AI模型测试报告与修复需求-20260916.md` | 需求 D 依据（⚠️ 第 61 行修法有误，见交接文档 §4.1） |
