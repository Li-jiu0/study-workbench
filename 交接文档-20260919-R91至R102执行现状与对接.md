# 交接文档 · 20260919 · R91-R102 执行现状与对接

> 交给下一批 AI。配合 `更新日志-20260919-R91至R102.md`（本批改动明细）阅读。
> 工作树：`D:\下载的文件\学习工作台`（**唯一可写**，⛔ 不碰 `C:\Users\ATM\WorkBuddy\...` worktree）。
> 生产：`110.42.134.62`，根 `/opt/study-workbench`，Nginx 80 反代 `/api/`→127.0.0.1:8000、`/static/`→web/static/，systemd 服务 `study-workbench`（uvicorn，远端 venv Python 3.14）。
> 凭据：`tools/plink.exe`/`pscp.exe` + `upload_v23.ps1`（root@110.42.134.62，HOSTKEY `SHA256:i2DNTi3iEPauuTxvJEPlZ4jLf8K0LdDsaijQxArUU1M`）。

---

## 一、当前版本状态（截至 2026-09-19 04:23）

| 项 | 状态 |
|---|---|
| 服务器 APK | `web/static/apk/星途-1.26.apk`（86,951,674B，md5 a6eb80ef...，versionCode 27/1.26） |
| 版本接口 | `GET /api/app/version` → 1.26/27/apkReady=true（60s 内存缓存） |
| 用户设备 | 已手动安装 1.25（versionCode 26，含 R96-R100 + 下载修复） |
| 测试中 | 用户正测试 1.25→1.26 App 内更新链路（系统下载器后台下载+完成自动弹安装）——**结果未回传** |
| 网页端 | 128 文件与本地对齐（R99 全量 + R100/R101/R102 增量）；xt-update.js CURRENT_VERSION=1.26 |
| geo 三接口 | 全通（R100 验证）；新腾讯 Key 配额已分配（IP 5000/逆地址 4000/行政区划 5000/日） |

## 二、项目铁律（继承+本批新增，全部实测教训）

1. ES2017 上限；权威检具 `node tools/qa/escheck_es2017.js` 必须 `DONE 0`；CSS 禁 clamp/min/max；禁 prompt/alert/confirm。
2. 行尾逐文件实测（CRLF/LF 混杂），二进制读写；**并行 Edit 同一文件会静默丢改**。
3. 本机 bash 无 coreutils；**stdout 捕获坏** → Python 写 `.txt` 再 Read。
4. **部署=全量差量+依赖闭包+差异归零门禁**（R95 教训，工装 `_r95_prod_diff.py`）。
5. **后端部署铁律**（R100 事故）：推 main.py 前解析 import 集合核对远端在位；探活 sleep≥20+重试循环；备份先于 pscp 覆盖；不碰 `server/.env`/`data.db`/`feedback.json`。
6. **发版三处版本必须同批同步**：`server/routers/version.json` + `android/AndroidManifest.xml` + `assets/xt-update.js CURRENT_VERSION`；且 APK 内嵌常量=该包自身版本号，**不能预写下一版**（R101 教训）。
7. **pscp/plink 中文远端路径 GBK 坑**：远端含中文路径会以 GBK 字节落盘（pscp 仍报 rc=0）。必须 ASCII 中转名 + 远端 `mv` 用 ANSI-C 转义（如 `星途=$'\xe6\x98\x9f\xe9\x80\x94'`）。诊断 `ls | cat -A`。
8. **腾讯位置服务红线**：周边 POI 只用 `geocoder/v1?get_poi=1` 的 `result.pois`，禁调 `place/v1/*`（仅 200/日）。抽验接口克制，一次成功即止（121=超额或 0 配额）。
9. APK 打包：`python android/build_apk.py`（工具链 `C:\Users\ATM\android-build`，JDK17+build-tools 34.0.0+android-35；keystore `android/workbench.keystore`，pass workbench123，alias workbench）。**签名必须一致否则覆盖安装失败**。产物固定名 `星途-安卓App.apk` → 上传时 ASCII 中转+mv 成正式名。

## 三、关键机制速查

- **geo 三接口契约**（前端写死，字段名不可改）：`/api/geo/ip`、`/api/geo/reverse?lat=&lng=`（pois `_distance` 带下划线）、`/api/geo/children?adcode=`（children 可能空数组→前端手输降级）。统一 HTTP 200+`{ok:bool}`。
- **检测更新链路**：`/api/app/version` 免登录（update.py，60s 缓存）；APK 物理位置 `<Root>/web/static/apk/<apkFileName>`；apkReady=文件存在性检查；apkUrl 按 host 动态拼。
- **App 内下载（R101b 新增）**：xt-update.js 检测 `window.AndroidBridge.downloadApk(url,fileName)` → 系统下载器（Download 公共目录+通知栏）；`setDownloadListener` 兜底；`ACTION_DOWNLOAD_COMPLETE` 广播自动弹安装（`ApkFileProvider` authority=`com.study.workbench.apkfile`）。
- **AndroidBridge 桥**：`window.AndroidBridge`（saveFile/fetchUrl/downloadApk 等）+ `AndroidTTS`；loadUrl 前注入 `assets/xt-android.js`。

## 四、遗留待办（按优先级）

1. **用户 1.26 链路测试结果未回传**——若报障，优先查：通知栏进度、退页中断、自动弹安装三环节（原生代码在 MainActivity.java `enqueueApkDownload`/`registerApkDoneReceiver`/`installApk`）。
2. **git push 被 GitHub secret scanning 拒**：历史提交 cc137ab 含 OpenRouter Key 6 处；01b2548 误用 `add -A` 混入 136 临时文件+2 APK+22 tar.gz。**等用户决策**（改写历史 or 置之不理）。本地 main 领先远端多提交。
3. 可选：IP 定位文案加「网络定位可能偏差到邻近城市」（R100 已提议未实施）。
4. 可选：geo/children 部分区县无第四级数据（腾讯 district 接口限制）→ 未来换民政部数据集。
5. 可选：视频 IndexedDB 永久化（24h TOS 过期）；xt-profile.js 死代码清理；tools/qa 历史脚本旧引用清理。
6. 低优先：`server/.env` 有 TENCENT_MAP_KEY=F34BZ-...（后端专用 Key，勿泄勿删）；旧 Key RRTBZ-... 仍在前端 xt-region.js 作 JSONP 降级。

## 五、工具与环境

- Python：`C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe`；Node：`C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`；jsdom 装在 `C:\Users\ATM\.workbuddy\binaries\node\workspace`。
- git push 必须走代理：第一优先环境变量 HTTP_PROXY/HTTPS_PROXY，备选 127.0.0.1:7897；`git -c http.proxy=P -c https.proxy=P -c http.version=HTTP/1.1 push origin main`。
- 记忆日志：`.workbuddy/memory/2026-09-18.md`、`2026-09-19.md`（R96-R102 细节完整，含事故复盘与坑）。
- skill：`study-workbench-deploy`（部署 SOP + §5.1 门禁）。
