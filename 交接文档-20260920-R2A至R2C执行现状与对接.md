# 交接文档 · R2A–R2C 执行现状与对接（2026-09-20）

> 面向下一位接手者（人或 AI）。看完这页即可接手，无需回溯对话。
> 前置：`交接文档-20260919b-R129至R130执行现状与对接.md`

---

## 一、一句话现状

**R2A（七项需求 + 双向位置共享 + 3D 修复 + 模型删除）、R2B（v1.33 发版，含重打联网版 APK）、R2C（GitHub 同步）三批全部完成：Web 全量已上线、APK 1.33 已发布且 `apkReady=true`、远端 `main` 已同步到 `be18396e` 并通过 secret scanning。唯一最高优先待办是密钥轮换（见第二节）。**

| 项 | 值 |
|---|---|
| 线上站点 | http://110.42.134.62 |
| 当前版本 | **1.33 / versionCode 34**（上一版 1.32 / 33） |
| APK | `星途-1.33.apk` 87,157,631 B，md5 `d6bd18f6…`，签名证书 SHA-256 `2b95937e…`（与 1.32 同证书，可覆盖安装） |
| 版本接口 | `/api/app/version` → `{status:"ok", version:"1.33", versionCode:34, apkReady:true, apkUrl:"http://110.42.134.62/static/apk/星途-1.33.apk"}` |
| 代码仓库 | https://github.com/Li-jiu0/study-workbench ，`main` = **`be18396e`**（168 提交 / 3717 文件） |
| 资产戳 | 全站 `20260922c`；`xt-update.js` 单独为 **`20260922d`** |
| 本地提交（已 push） | `410a292`、`9118132`、`ed547f6`、`8a2a749`（后者为密钥清理） |
| 更新日志 | `更新日志-R2A至R2C-20260920.md`（同目录） |

---

## 二、🚨 安全待办（最高优先级）

### 2.1 `server/.env` 的密钥早已随 GitHub 历史公开

- **事实**：`server/.env` 由**初始提交**加入、commit `848d642`（"移除硬编码凭据与数据库文件"）移除；经 `git merge-base --is-ancestor` 判定**两个提交都已是远端祖先** → 该 blob 早已在 GitHub 上。
- **后果**：其中**全部模型供应商 Key + `JWT_SECRET` + `ADMIN_USERNAME` / `ADMIN_PASSWORD`** 均应视为已泄漏。
- **唯一补救是轮换**：强推 / `git-filter-repo` **删不掉 GitHub 上按 SHA 可达的旧对象**（本批已把该文件从**新历史**中彻底删除，但旧对象不在我们控制范围内）。
- **轮换注意**：`JWT_SECRET` 一换，**现有全部登录态立即失效**（用户需重新登录）→ 建议挑低峰期并提前公告。
- **轮换方式**：在各自平台重新生成 → **只在服务器 `server/.env` 追加/替换**（`pscp` 单文件上传后用远端 python 写回；**绝不用本地 `.env` 覆盖生产**，两边键不同：本地有 `ADMIN_TOKEN`，线上有 `ADMIN_PASSWORD` / `ADMIN_USERNAME`）。

### 2.2 `assets/ai-config.js` 的 Gemini Key 线上公开（活跃泄漏）

- **事实**：`assets/ai-config.js` 的 `gemini` provider 里 `apiKey` 是真值且 `keyInQuery: true`；该文件是**公开静态资源**（`curl http://110.42.134.62/assets/ai-config.js` 即可读到）。
- **来源**：v1.31「Google Gemini 支持前端直连、本版已内置 Key」的**刻意设计**（Gemini 属 `needVPN` 海外平台，前端直连需要 key）。
- **关键认知**：**R131「前端零密钥、内置平台走服务端中转」并未覆盖 `needVPN` 的海外平台分支** —— 接手时不要默认前端已无明文 key。R129–R130 交接文档里点名的硅基流动明文 key 已被 R131 清掉（现 `siliconflow` 无 `apiKey`），**但 `gemini` 这条仍在**。
- **处置（待用户拍板，二选一）**：
  1. **推荐**：轮换 Gemini Key + 把 Gemini 调用也改走后端中转（`server/routers/ai.py` 已支持按 `model_registry.json` 的 `type` 分流与 `/v1/*` 端点派生）→ 前端彻底无 key；**代价**：需重新部署 + **重打 APK**（前端改动不重打包到不了 App 用户）。
  2. 保持前端直连：需接受「key 公开、可能被刷量/被封」的风险。

### 2.3 其他

- **远端 `refs/tags/1.0`** 仍指向旧初始提交 → 旧对象（含 `server/.env`）持续可达；**是否删除待拍板**。
- **本地 git 历史仍含明文密钥** → 今后每次 push 都须继续走净化流程（工装 `_r2c_loop.py`），否则会被 GitHub push protection 拦下。
- **纪律（不可再犯）**：任何 provider key **只准进服务器 `.env`**，不准进前端、不准进 git。
- **被拦时绝不点 `unblock-secret` 链接放行** —— 那等于公开密钥；唯一正确动作是把密钥从历史清掉再推。

---

## 三、代码树与生产环境事实

- **唯一可写代码树**：`D:\下载的文件\学习工作台`（分支 `main`）。
  ⚠️ 会话工作区（`C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-2ab398e3`）**本批实测与真源同步**（对 `assets/app.js` 做 `difflib` 对照，差异全部可归因本批），可放心搬文件；但**判断"某文件到底是什么版本"仍只认真源绝对路径的 md5**。历史上曾出现过停在旧提交的过期 worktree（见 20260919b 交接），故规则不变。
- **生产**：`110.42.134.62`
  - Nginx root：`/opt/study-workbench/web/`（前端资源在 **`/opt/study-workbench/web/assets/`**，不是 `/opt/.../assets/`，写错会得假失败）
  - 后端：`/opt/study-workbench/server/`；systemd 单元 `study-workbench`（uvicorn **单进程**；改后端需 `systemctl restart study-workbench`）
  - `/api/` 反代 → `127.0.0.1:8000`；APK 静态目录 `/opt/study-workbench/web/static/apk/`
  - 备份目录 `/opt/study-workbench/backups/<批次>-<时间戳>/`（本批：`r2a-20260920-144905`、`r2a-20260920-145013`、`r2b-20260920-150729`）
- **凭据取法**：从 `.gitignore` 忽略的 `upload_v23.ps1` 正则取（`pscp\.exe"\s+-pw\s+"([^"]+)"[^\n]*?root@([0-9.]+):`）→ **绝不 print 明文、绝不写进任何入库文件**；`tools/pscp.exe` / `tools/plink.exe` 写全路径；`-hostkey SHA256:i2DNTi3iEPauuTxvJEPlZ4jLf8K0LdDsaijQxArUU1M`。
- **禁止上线**的运行时数据：`server/.env`、`server/data.db`、`server/data/model_usage.json`、`model_quota.json`、`geo_place_count.json`、`geo_staticmap_count.json`、`feedback.json`。
  > 本批 `_r2a_fulldiff.py` / `_r2a_deploy.py` 已把上述项写进硬豁免名单。

---

## 四、架构红线（碰之前先读）

1. **站点是纯 HTTP，不上 HTTPS**（用户明确决策）。直接后果：浏览器安全策略恒拒 Web 端 geolocation → 「距你约 X 公里」、精确位置发送、实时共享等**只在 App（`file://` 安全上下文）成立**，**禁止用 IP 粗定位冒充**。
2. **App 是 `file://` 壳**：`MainActivity` 用 `web.loadUrl("file:///android_asset/学习工作台.html")`，页面与 assets **打进 APK** → **后端改动 App 用户立即生效；前端改动必须重打 APK 才能到达 App**，Web 部署对 App 无效。
   > 本批的「图片捏合/保存到相册」「横幅通知」都属这一类，**必须重装 APK 才生效**（用户已选择"联网版"，APK 已按联网版打出）。
3. **`android/build_apk.py` 已是联网版**：删 `api.js` 引用的正则已在 2026-09-17 移除，改回纯二进制复制，并有构建期硬断言「入口页必须含 `assets/api.js` + `assets/config.js`」（缺一即中止打包）。
   **新增核心 `assets/*.js` 必须同步登记进 `REQUIRED_ASSETS`** —— 否则「代码修好但包里没有」，只在真机暴露（本批已补 `img-viewer.js` / `xt-update.js`）。
4. **模型解析链**：`body.modelId` → `resolve_model_name(provider, quota_key)` 查 `server/data/model_registry.json`（现 106 条）→ 失败用 `body.modelName` → 再失败回退 `.env` 默认模型（响应头 `X-Ai-Model-Fallback` 提示）。**新增模型请补 registry，不要靠猜**。
5. **腾讯地图配额红线**：`geocoder/v1` 6000/日；`place/v1/*` **仅 200/日**，只允许走 `/api/geo/place` 代理且受 `GEO_PLACE_DAILY_CAP=150` 熔断（`place/v1/explore` 全站禁调）；`staticmap/v2` 受 `GEO_STATICMAP_DAILY_CAP`（缺省 3000）。Key 是 **IP 白名单制** → 本机直连恒返 `status=112`，探测必须落服务器。前端**绝不直连** `apis.map.qq.com`。
6. **限流桶**：`rate_limit("geo")` = 30/分/IP **且缓存命中也计数** → 运营商 NAT 下易 429；新业务不要挂这个桶。
7. **CSS / 前端红线**：禁 `clamp()/min()/max()` **函数**（老 WebView），但 `min-height/max-height` 属性可用；`vh` 在 fixed 层安全；**ES2017 上限**（`node tools/qa/escheck_es2017.js` 必须 `DONE 0`）；禁 `prompt/alert/confirm`。
8. **编辑纪律**：
   - 改前**二进制检测行尾并保持**。本批实测口径：**根 HTML 主流是 LF**（生产 60 个 html：LF 49 / CRLF 11），`assets/xt-update.js`、`assets/xt-settings.js` 等为 LF，`assets/api.js` / `app.js` / `chat-local.js` / `xt-moments.js` 为 CRLF。
     > ⚠️ 旧 skill §5.0 表格写「其余根 HTML = CRLF」**与实测不符**，别照表判事故；**跨树搬文件时一律以目标文件现有行尾为准**（本批工装 `_r2a_sync_main.py` 已实现）。
   - 改前备份 `.bak-pre-*`；不做无关重排。
   - **`?v=` 版本戳由 lead 统一 bump，只前进不回落**（内容变了必须前进，否则老用户命中旧缓存 = 白改）；**戳若已推过生产必须换新字母**。

---

## 五、部署 SOP 摘要（完整版见 skill `study-workbench-deploy` §5 与新增 §9–§11）

1. **全量差量 + 依赖闭包**（别手工列清单）：本地全量 md5 vs 线上 `os.walk` md5 → 三张表（DIFF / ONLY_LOCAL / ONLY_REMOTE）；
   前端解析**所有** HTML 的 `assets/*` 引用求集合，减线上实际存在 → 差集即"部署后必 404"清单。
2. **打包与上传**：`tarfile` 打 tar，`arcname` 写**生产侧相对路径**（`web/...`、`server/...`），`pscp` 传 `/tmp/`，远端 `tar -xzf ... -C /opt/study-workbench`。
   > **中文文件名走 tar 比「ASCII 临时名 + 远端改名」少一半步骤且已验证**（本批 99 个含中文名文件 md5 99/99 通过）。
3. **路径映射坑**：真源根即生产 `web/` 的映射（`D:\...\x.html → /opt/.../web/x.html`）。取文件要用 `local_path(rel)`：`web/x → ROOT/x`；**只写 `os.path.join(ROOT, rel)` 会「找不到」→ 静默 0 文件入包**（本批首跑就踩了）。
4. **差异归零门禁**：**逐文件 `本地 md5 == 生产 md5`**（不能只数总数、不能只比 size；生产侧用 `plink` 拉 md5 清单）。本批从 98 项降到 4 项豁免。
5. **重启与健康检查**（后端变更）：`systemctl is-active`、`systemctl show -p NRestarts --value`、`/api/health`、目标新路由。
6. **发版（含 APK）**：四处 bump（`server/routers/version.json` + `android/AndroidManifest.xml` + `assets/xt-update.js` + APK 实体名）→ `python tools/qa/check_version_consistency.py` 必须 exit 0 → `python android/build_apk.py`（约 65s）→ 上传 APK/version.json/xt-update.js → **`version.json` 有 60s 进程缓存，等 66s 再验** `/api/app/version`。
   **版本号与 APK 必须一起动**（只推版本号会让 App 提示更新却下载不到包）。
7. **收尾**：备份路径与逐文件 md5 写报告；本地 `git commit`（**push 需用户单独拍板** —— 且必须走下方净化流程）。

### 5.1 推 GitHub（本批新增，详见 skill §11）

```bash
# 1) 量化载荷
git rev-list --objects HEAD --not <remote_tip>   # 再用 cat-file --batch-check 求和
# 2) 净化副本（隔离 clone，本地仓库不动）
git clone --no-hardlinks --no-tags "D:/下载的文件/学习工作台" D:\_r2c_roundN
python -m git_filter_repo --force --no-gc \
  --invert-paths --paths-from-file _p.txt --replace-text _r.txt
# 3) 推送（Clash 7897！env 代理对本机 GitHub 已失效）
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 \
    -c http.version=HTTP/1.1 push --force-with-lease=main:<远端当前SHA> origin main
# 4) 成功判据只看远端 SHA == 本地 HEAD（"Everything up-to-date" 会与 RPC 错误同时出现 → 假成功）
```
- `_p.txt`（剔除）：`*.apk`、`tools/jx_patch/_work/`、`tools/frontend_*.tar.gz`、**`server/.env`**、`_lead_run*.log`。
- `_r.txt`（替换）：`AKLT…` / `sk-or-v1-…` / `sk-…` / `AIza…` / JWT / `apiKey: "<28+>"` 等**固定替换**规则，
  **外加从 GitHub 拦截报告点名的文件里在脚本内部提取的 `literal:`**（本批漏了 53 字符无前缀的 Gemini key，靠前缀正则抓不到）。
- 现成工装：`_r2c_loop.py`（自动收敛：解析 GitHub 报告的 `path:` → 追加规则 → 重跑，最多 4 轮）。

---

## 六、验证工具与口径

| 工具 | 用途 | 要点 |
|---|---|---|
| `tools/qa/escheck_es2017.js` | 语法红线 | 在工作树根跑，必须 `DONE 0` |
| `tools/verifier`（jsdom） | 前端行为断言 | 必须 `runScripts:'outside-only'` 才能 `window.eval` |
| `tools/qa/page_check.js` | 页面渲染级校验 | **必须 `QA_PORT=89xx` 换端口**，逼它自启内置 Node 服务；复用已存在的 Python 服务会报「全站 js 全挂」的假红 |
| `tools/qa/check_version_consistency.py` | 版本号五处一致 | 应 exit 0（本地 `web/static/apk/<新版本>.apk` 需就位，否则报 `apkReady=false` 预警） |
| 生产只读探针 | 拿 ground truth | `_r2a_prod_probe.py`；**接手第一件事就是跑它**，别假设生产干净 |
| 独立 QA 线 | 复核实施方自证 | 实施方与复核方**文件零重叠**，复核方只读 |

---

## 七、本轮改动文件与 md5（关键 24 个）

| 文件 | md5 | 说明 |
|---|---|---|
| `assets/img-viewer.js` | `7b10ca4e…` | **新建**：捏合/拖动/翻页/保存到相册 |
| `assets/api.js` | `82e9d611…` | `aiStream` 真实生效（关=整段渲染） |
| `assets/app.js` | `e0d79e85…` | `aiContext` 门控 + 删 6 无效键 + boot 注册提醒 |
| `assets/chat-local.js` | `80f77e61…` | 接入 ImgViewer（旧 `_IV` 留 fallback） |
| `assets/xt-moments.js` | `49dd6409…` | 接入 ImgViewer（`openList`） |
| `assets/xt-settings.js` | `f4723e69…` | 删 6 键 + 迁入 `applyStudyReminder` |
| `assets/xt-update.js` | `cb41f51b…` | `CURRENT_VERSION=1.33` + 暴露 `window.XT_VERSION` |
| `assets/ai-cap-3d.js` | `92cd81d6…` | 取值链补 `rj.modelUrl`（3D 字段错位） |
| `协议.html` | `f106ad18…` | **新建**：UA 九章 + 隐私八章 |
| `数据管理.html` | `b3a36db6…` | **新建**：微信存储空间风（27 个 `dm*` 函数） |
| `关于.html` | `d59878a0…` | 八区块重写 |
| `设置.html` | `77359a91…` | 删 3 section + 收敛「存储空间」入口 |
| `个人中心.html` | `d4c057e0…` | 新增数据管理入口 |
| `server/routers/liveloc.py` | `a9592b78…` | 双向共享（索引改 shareId 列表 + `sessions[]`） |
| `server/routers/ai.py` | `fe4542f1…` | `_task_view` 内容类型加固 |
| `server/data/model_registry.json` | `b616d40a…` | 删 `ark-hyper3d-gen2`（106 条） |
| `server/data/model_quota.json` | `42c42398…` | 删 `ark-hyper3d-gen2` |
| `server/routers/version.json` | `7443c2b3…` | 1.33 / 34 / `星途-1.33.apk` / notes 10 条 |
| `android/AndroidManifest.xml` | `c50eee6d…` | versionName 1.33 / versionCode 34 |
| `android/build_apk.py` | `7b471b0c…` | 白名单补 `img-viewer.js` + `xt-update.js` |
| `android/.../MainActivity.java` | `f9890c49…` | 通知权限引导闭环 + 存相册桥 + 电池优化引导 |
| `android/.../MsgPollService.java` | `172cf4cb…` | 渠道 `xt_msg_v2` + `scheduleKeepAlive` |
| `android/.../PollLogic.java` | `85f498a4…` | 熄屏轮询 300s → 120s |
| `android/.../PollKeepAliveJobService.java` | `8623263f…` | **新建**：JobService 15min 保活 |
| `web/static/apk/星途-1.33.apk` | `d6bd18f6…` | 87,157,631 B（`*.apk` 已 gitignore，不入库） |

---

## 八、遗留与下一步

1. **🔴 安全**：见第二节（`server/.env` 轮换 + `ai-config.js` 的 Gemini key）。
2. **🟡 真机验证（未做）**：横幅通知三态（前台 / 后台 / 进程被划掉）× 权限矩阵（已授权 / 拒一次 / 拒且不再询问）；图片捏合与「保存到相册」（需 1.33 APK 真机）。
3. **🟡 远端 `refs/tags/1.0`**：是否删除待拍板。
4. **🟡 本地历史治理**：当前本地历史仍含明文密钥；若不做，则每次 push 都要走 `_r2c_loop.py`。
5. **🟢 小项**：`common.css` 的 `.bottom-nav` 变量/`env()` 未加兜底 + 两处历史 `min()`（行为可接受）；`server/.venv` 的 uvicorn 安装损坏建议 force-reinstall；真源工作区约 500+ 项未跟踪临时物（建议单独一批**出清单**后归档，不要顺手删）。
6. **🧹 本批产出的工装**（真源根，可复用）：`_r2a_stamp.py` / `_r2a_sync_main.py` / `_r2a_prod_probe.py` / `_r2a_preflight.py` / `_r2a_srv_3layer.py` / `_r2a_fulldiff.py` / `_r2a_deploy.py` / `_r2a_postverify.py` / `_r2b_bump.py` / `_r2b_stamp.py` / `_r2c_loop.py`。

---

## 九、给接手者的一句话建议

**先跑 `_r2a_prod_probe.py` 拿生产 ground truth，再读第二节（安全）。**
本批所有关键结论都不是靠读代码得出的，而是靠「实测」：生产其实停在几天前的旧态（此前的"已修复"从未上线）、16 个页面尾部结构破损、`server/.env` 早已泄漏、`ai-config.js` 的 Gemini key 线上公开 —— 这些都是**只读实盘比对**发现的。**永远不要假设生产干净、也不要假设上一轮真的部署过。**
