# 交接文档：星途（原「学习工作台」）全站修复收口与 APK 打包

> 生成时间：2026-09-16 09:35
> 交接对象：新会话 / 新执行者
> 本文件目标：**让你在不读历史对话的前提下，能独立复验、继续开发、重新打包 APK**

---

## 0. 一句话现状

网页侧的「AI 不可用、全站按钮点不动、无法跳转、留言板/首页卡片不显示、设置页数据丢失、机器人球点不开拖不动」**六个问题已全部定位到根因并修复，已部署到线上 `20260916k`**。

你的任务顺序：**① 先复验线上是否真的恢复（§5）→ ② 处理未收口模块（§7.4）→ ③ 重新打包 APK（§7.2）→ ④ 打通数据同步（§7.3）**。

---

## 1. 环境与路径（必读）

| 项 | 值 |
|---|---|
| 唯一可写代码树 | `D:\下载的文件\学习工作台` |
| ⛔ 绝对不要碰 | `C:\Users\ATM\WorkBuddy\Worktrees\...`（git worktree 废品，没有意义） |
| 线上静态站 | `http://110.42.134.62/`（nginx，远端根 `/opt/study-workbench/web`） |
| 线上后端 | `http://110.42.134.62:8000`（FastAPI / uvicorn，远端根 `/opt/study-workbench`） |
| 部署凭据 | `tools/upload_v23.ps1`（部署脚本会自动解析，**不要打印或外传**） |
| node | `C:\Users\ATM\.workbuddy\binaries\node\versions\22.22.2-3\node.exe` |
| python | `C:\Users\ATM\.workbuddy\binaries\python\versions\3.13.12\python.exe` |
| jsdom + acorn | `D:\下载的文件\学习工作台\tools\verifier\node_modules\`（jsdom、acorn@8 已装好） |

**环境坑（会让你白白浪费大量时间）：**
- bash **缺 coreutils**：`ls` / `grep` / `head` / `tail` / `dirname` 全部不存在。→ 一律用 python 脚本，或用 Read / Grep / Glob 工具；命令一律写绝对路径。
- **Windows 中文路径 + python 控制台**：`print` 中文会 GBK 崩溃。→ 结果一律写 UTF-8 文件，再用 Read 工具读。
- 用 python 写 `-c` 单行命令时，含 `["']` 的正则极易被 shell 吃掉引号导致 `SyntaxError`。→ **正则类脚本写成 .py 文件再执行**（别硬写 `-c`）。

---

## 2. 铁律（违反 = APK 白屏。本轮已因此出过两次全站级事故）

### 2.1 老 WebView 语法禁令
站点跑在安卓 APK 的老 WebView 里（内核约 **Chrome 50~58**）。以下语法**一律禁止**：

```
?.        ??        .replaceAll(        Object.fromEntries
.at(      顶层 await       正则后行断言 (?<= (?<!
{...obj}  ← 对象展开（ES2018）★本轮全站事故元凶
const {a, ...rest} = x     ← 对象剩余
**        ← 指数运算符
catch {}  ← 可选 catch 绑定
```

**允许**：`let/const`、箭头函数、模板字符串、`class`、`async/await`（非顶层）、数组展开 `[...arr]`、`Math.max(1, ...arr)`。内核 ≥ ES2017。

### 2.2 顶层声明冲突禁令（AI 失效的真凶）
**禁止**在任何 js 里用 `var/const/let` 重复声明别的文件已存在的全局名。浏览器遇到重复声明会判该文件 **SyntaxError 并整文件拒绝执行**。
新增全局一律写成：

```js
if (typeof window.xxx !== 'function') window.xxx = function () { ... };
```

### 2.3 其它
- 禁原生 `alert/confirm/prompt`，用页面内 toast。
- 改任何 `assets/*.js` 或 HTML 后，**必须 bump 该文件的 `?v=` 版本戳**，否则用户设备永远加载旧文件（本轮踩过：ai-service.js 没带版本戳，用户测的一直是旧版）。
- 禁 git 操作（本机 git push 需动态代理，不值得折腾）。

---

## 3. 本轮已完成的修复（全部是根因级）

### 3.1 AI 永远走本地兜底 → 已修
- **根因**：`assets/app.js` 顶层有 `const AI_CONFIG = {...}`（老版演示配置），AI.html 先加载 app.js、后加载 `assets/ai-config.js`（`var AI_CONFIG = {...}`）→ **全局重复声明 SyntaxError** → `ai-config.js` 被浏览器**整文件拒绝执行** → AI 配置从未生效，只能吐「（网络不佳，以下为本地参考）」。
- **修复**：app.js 内 9 处 `AI_CONFIG` 改名 `APP_AI_DEMO_CONFIG`（备份：`assets/app.js.bak-before-aicfg-20260916`）。
- **复验**：jsdom 加载真实 AI.html 并模拟点击发送，实际回复「你好，我能帮助你学习。」（不再是兜底文案）。

### 3.2 全站按钮点不动 / 无法跳转 / 留言板·卡片·机器人全废 → 已修
- **根因**：app.js 有一处 **ES2018 对象展开**：`appData = { ...appData, ...JSON.parse(saved) }`（原 L1215）。老内核解析不了 → **整个 app.js 报废** → `navigateTo`、所有事件绑定、留言板渲染、卡片渲染、机器人球拖拽全没了。
- **修复**：改为 `Object.assign({}, appData, JSON.parse(saved))`。
- **这不是六个独立 bug，是一个死因的六种表现。** 记住这个判断：凡是"app.js 负责的东西集体失效"，先怀疑解析失败。

### 3.3 设置页数据丢失 / 开关点不动 → 已修
- **根因**：`loadAllSettings / getSetting / setSetting` 三个函数在 app.js 历史拆分过程中**被弄丢**（旧备份 `assets/app.js.backup_20260914:126-138` 里还在）。页面里 `initSettingsUI()` 是解析期同步执行的 IIFE，一加载就抛 `ReferenceError`。
- **修复**：新增 `assets/xt-settings.js`（全部挂 `window`、无顶层声明、纯 ES5），注入 `设置.html` 与 `个人中心.html`；`设置.html` 内联另加一份同逻辑兜底（双保险）。
- **复验**：jsdom 里两条 `ReferenceError` 归零；设置项正常回填（dailyNew=50、blogCat=cet）。

### 3.4 老内核运行时 API 缺失 → 已补
- 新增 `assets/xt-polyfill.js`（约 19KB，**纯 ES5**，acorn `ecmaVersion:5` 可解析）：补齐 `Object.assign/values/entries`、`Array.from / find / findIndex / includes`、`String.startsWith/endsWith/includes/padStart/padEnd/trimStart/trimEnd`、`Number.isNaN/isInteger`、`Element.closest/matches`、`Promise`、`window.fetch`（XHR 实现）、`TextDecoder/TextEncoder`。
- 每个 shim 都是「缺失才补」+ 独立 try/catch 隔离。
- 已注入**全部 37 个根目录 HTML 的第一个 script 之前**。

### 3.5 首页轮播卡偏大、窄屏不适配 → 已缩小
- `.home-carousel-card` padding `20px→14px`（窄屏 10px）、`card-title 16→14px`、`stat-num 22→18px`、`week-chart 高 80→58px`、目标完成率圆环 `70→54px`（改 `.home-goal-ring` + svg 100% 自适应）、圆点 `6→5px` / 激活条 `20→16px`。
- 新增 `@media (max-width: 480px)` 断点；轮播容器改自适应宽度。
- **行为不变**：`scrollToCard`、`syncCarouselDots`、scroll 监听逻辑未改。

### 3.6 机器人球点不开 / 拖不动 → 已加固
- 机器人球是 **app.js 动态创建并绑定事件**的（不在任何 HTML 里写死），所以它其实是 3.2 的连带损伤；3.2 修完应已恢复。
- 额外加固（eng-ball2）：
  1. 文件顶部新增自举闭包 `initAiFabModule`（自带状态，不依赖后部代码，前面任何初始化崩了也不影响它）；
  2. 拖拽同时支持 **mouse + touch** 两套事件（老 WebView 常无 PointerEvent / setPointerCapture）；
  3. DOM 缺失时轮询重试；位置持久化 + 双击复位；
  4. `.ai-fab` 的 CSS 用了 `calc(...env(safe-area-inset-bottom))`，**Chrome < 69 会让整条 `bottom` 声明失效、球掉到屏幕外** → 已加 `window.CSS.supports` 检测 + 纯 px 兜底（app.js 约 L85-92）。
- 同时给 `toggleAiPanel` 补了 `#aiPanel` 等元素判空、首页 `decorateHome/loadData/updateDate` 各自独立 try/catch + 可见兜底、留言板 `renderBoard` 空数据/异常渲染可见文案与重试按钮。

---

## 4. 部署批次（均已上线，远端已校验）

| 批次 | 内容 | 脚本 |
|---|---|---|
| `20260916i` | app.js（AI_CONFIG 改名 + 对象展开修复）+ AI 四件套 + 37 页 bump 版本戳 | `tools/deploy_update_20260916i.py` |
| `20260916j` | `xt-polyfill.js` + `xt-settings.js` + 37 页 bump | `tools/deploy_update_20260916j.py` |
| `20260916k` | app.js（机器人球 / 渲染健壮性）+ 37 页 bump | `tools/deploy_update_20260916k.py` ← **当前线上版本** |

线上已核实：`xt-polyfill.js`(19113B)、`xt-settings.js`(2922B)、`app.js`(7045231B) 均 HTTP 200。

**部署脚本用法**：复制 `deploy_update_20260916k.py`，改 3 处即可——
1. `STAMP = '20260916X'`（新版本戳）
2. `files = [...]`（要上传的 assets 文件列表）
3. 远端校验段的 grep 关键词

脚本会自动：bump 全部根 HTML 的 `?v=` → 打包 tar.gz → pscp 上传 → 远端解压 → curl 状态码 + grep 校验。

---

## 5. 复验方法（改完 / 部署后必须跑）

脚本已固化到 `tools/qa/`：

| 目的 | 脚本 | 说明 |
|---|---|---|
| **老内核解析级体检（最重要）** | `tools/qa/escheck_es2017.js` | 用 acorn 按 `ecmaVersion:2017` 严格解析全部 `assets/*.js` + 各 HTML 内联 script。输出「`(全部脚本 ES2017 解析通过)`」才算过；任何一条命中都是**老内核必崩**的致命点 |
| 单页运行时体检 | `tools/qa/page_check.js` | 用法：`node page_check.js "页面名.html" "关键词1,关键词2"`，输出运行时错误、关键内容是否渲染、卡片/按钮数量 |
| 多页导航体检 | `tools/qa/multi_check.js` | 无参，跑 8 个页面，输出 `navigateTo`/`callAI` 类型、底部导航项与 onclick、运行时错误 |
| AI 端到端（真页面） | `tools/qa/ai_jsdom_check.js` | jsdom 加载线上 AI.html → 输入 → 点发送 → 断言气泡内容**不含**「本地参考」 |
| 机器人球点击 / 拖动 | `tools/qa/ball_verify.js` | 断言球元素存在 + 模拟 touch 拖动后 `style.left/top` 变化 |
| 拉线上文件对比本地 | `tools/qa/fetch_srv_files.py` | 下载线上 AI 相关文件并与本地做 md5 对比，快速发现"线上还是旧的" |

**运行前注意**：这些脚本依赖 `tools/verifier/node_modules` 下的 jsdom / acorn，路径已硬编码，保持该目录不动即可。

**jsdom 会报 `Not implemented: navigation to another Document`** —— 这是 jsdom 自身的限制（它不支持真实页面跳转），**不是 bug**，看错误列表时请忽略这一条。

---

## 6. 部署脚本的坑（血泪清单）

1. **凭据必须是 `root@ip`**：正则若只抓到裸 IP，plink/pscp 会用本地用户名认证 → `Access denied`，报错信息是极具迷惑性的「password not accepted」。`tools/deploy_update_*.py` 已处理，自己写脚本时注意。
2. **远端备份命令必须容错**：`tar -czf 备份 <filelist>` 在文件不存在时会让 `&&` 链断掉，导致**根本不解压**（现象：上传成功但线上 grep 报 No such file）→ 备份命令后加 `|| echo BACKUP_SKIP_NEW`。
3. **部署后一定实测**：`curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1/xxx.html` + 对关键字符串 grep，不要只看上传 exit code。
4. **版本戳**：新上的 js 必须带 `?v=` 且 bump，否则用户拿到旧文件。
5. 别用 `sw` 当变量名（与 shell 冲突）。

---

## 7. 待办清单（按优先级）

### 7.1 复验线上（先做这个）
跑 §5 全部脚本，确认 `20260916k` 真的恢复。重点两页：`AI.html`、`学习工作台.html`。
⚠️ **提示用户强制刷新**（清缓存 / 重开），否则设备上仍是旧文件。

### 7.2 重新打包 APK（用户最关心，历史上每版都"连不上服务器"）
工程在 `android/`：`build_apk.py`、`build-apk.sh`、`build-apk.ps1`、`merge_apk.py`、`AndroidManifest.xml`、`res/`、`java/`、`workbench.keystore`。

**打包前必查（这些就是"单机静态、连不上服务器"的候选原因）：**
1. **打进 APK 的网页是不是旧快照** —— 打包前必须从项目根重新同步最新 `*.html` + `assets/`（本轮改了 37 页 + 5 个 js，旧快照必然缺）。
2. **file:// 环境下的 API 地址** —— `assets/config.js` 的 `window.STUDY_API_SERVER = 'http://110.42.134.62:8000'`；web 环境走同源（`STUDY_API_BASE = ''`）。确认 APK 环境下拿到的是绝对地址。
3. **Android 9+ 默认禁明文 HTTP** —— `AndroidManifest.xml` 需要 `android:usesCleartextTraffic="true"` 或 `networkSecurityConfig`（指向 http:// 的 8000 端口，缺这条一切网络请求静默失败）。
4. **`INTERNET` 权限**是否在 manifest 里。
5. **老内核语法**：打包前跑一遍 §5 的 `escheck_es2017.js`；确认 `xt-polyfill.js` 已注入。
6. 后端 CORS：确认 `/api/ai/chat` 等接口对 `file://`（Origin: null）放行。

### 7.3 私信 / 互动数据同步（用户抱怨"数据不同步"）
审计报告：`tools/sync_audit_20260916.txt`。
要点：把"只写本地 localStorage、从不上传服务器"的功能接到 `server/routers/` 已有接口上；后端已有但前端未调用的路由清单见该报告。

### 7.4 未收口的 worker 任务（⚠️ 不要相信任务状态，**必须重新读文件核验**）
这些 worker 曾被中断，其改动可能只做了一半：
- 全站底部导航统一 / 点击兜底（"点我的没反应"）
- 页头返回按钮全站注入
- PPT 相关页面标签行收放分类
- `高情商表达.html`、`场景话术库.html`、`管理员.html` 布局重排
- AI 页「配置自定义模型」弹窗重做
- 全站死链排查、全站点击无反应排查
- 全站 AI 底座引入（`ai-config/ai-presets/ai-service` 是否所有需要 AI 的页面都引入了 —— 注意**加载顺序必须固定、必须同步加载，禁 `defer/async/module`**）

### 7.5 收尾
统一 commit（如需要）。`.workbuddy` 目录不要删。

---

## 8. 给新会话的开场指令（可直接复制）

```
接手「星途」项目（原学习工作台），代码树 D:\下载的文件\学习工作台，线上 http://110.42.134.62。
先完整读 D:\下载的文件\学习工作台\交接文档-20260916-全站修复收口与APK.md，严格按其中的
「铁律」（§2）和「复验方法」（§5）执行，不要引入文档里禁止的老内核语法。

本轮任务顺序：
1) 跑 tools/qa 下的复验脚本，确认线上 20260916k 版本已恢复（AI 能真回答、设置页无 ReferenceError、
   首页留言板与卡片有渲染、机器人球能点能拖），把实测结果贴给我；
2) 重新打包 APK，重点解决"连不上服务器"（按 §7.2 的 6 个必查项逐条确认，缺工具自己装、装不了换路子，
   不用问我）；
3) 继续处理 §7.4 里未收口的模块，每改完一批就跑 escheck_es2017.js + page_check.js 自检并部署。

注意：bash 缺 coreutils，文件和统计一律用 python 脚本（结果写 UTF-8 文件再读）；改完 js 必须 bump
?v= 版本戳并部署，否则用户拿到旧文件。
```

---

## 9. 其它有用线索

- 历史交接文档（根目录）：`交接文档-批次九-20260915.md`、`任务执行文档-20260915-全线自主执行.md`、`用户需求与决策总账.md`、`运行说明.md`、`手机真机打包运行方案.md`、`安卓App安装说明.md`、`问AI页面开发文档.md`。
- 排查报告：`排查报告-20260915-HTML.md` / `-JS.md` / `-UI.md`、`tools/click_audit_20260916.txt`（22 万行全站点击审计）。
- 本轮新增文件：`assets/xt-polyfill.js`、`assets/xt-settings.js`；备份：`assets/app.js.bak-before-aicfg-20260916`。
- 关键诊断经验（务必传承）：
  1. **只测服务层会假绿**。曾用 vm 加载 ai-config/ai-service 得到"真模型回答"，但没加载 app.js，掩盖了全局声明冲突。→ 验证前端必须用 jsdom 加载**真实完整页面**。
  2. **"某个模块集体失效"优先怀疑该 js 解析失败**，而不是逐个按钮排查。用 acorn 按 ES2017 扫一遍最快。
  3. **重复全局声明**（`const` vs `var` 同名）是老内核上第二类"整文件报废"元凶。
