# 星途学习工作台 · 更新日志（R3 / R3b / R4 / R5）

- 更新日期：2026-09-20
- 当前版本：**1.34（versionCode 35）** ／ 上一版：1.33（versionCode 34）
- 覆盖范围：Web 端全量 + Android App（`星途-1.34.apk`）+ GitHub 仓库同步
- 线上地址：http://110.42.134.62 ／ 代码仓库：https://github.com/Li-jiu0/study-workbench
- 前置批次：`更新日志-R2A至R2C-20260920.md`

> **本批次的特殊性质（读之前先看这条）**
> 1. R3 / R3b / R4 三条线的代码**已随 v1.34 上线到生产**，但它们当时**只存在于工作区、从未提交进 git**。R5（表情面板）是本次新增的可见功能。
> 2. 因此本日志分两部分：**第一节的 1.1 / 1.2 是 v1.34 已上线的内容**；**R5 表情面板是「代码完成、待发版」**（拟以 v1.35 发布）。发布状态以文末「六、发布状态」为准。
> 3. 本批次完成后，版本历史第一次与线上状态完整闭合。

---

## 一、用户可见更新

### 1.1 v1.35（**待发布** · R5 表情面板，即下一版 App 更新提示）

| # | 更新内容 | 类型 |
|---|---|---|
| 1 | 私聊页表情改为**微信式底部弹出面板**，全宽、带遮罩、点遮罩/再点表情/「收起」/安卓返回键均可关闭 | 重做 |
| 2 | 表情库 **24 → 96 个**（笑脸 / 情绪 / 手势 / 爱心 / 学习 / 动物等分组） | 扩充 |
| 3 | 表情支持**长按收藏**，新增「收藏」与「最近」两个分组（旧版靠鼠标悬停，手机上没有悬停 = 根本用不了） | 新增 |
| 4 | 新增**自定义表情**：点「＋」从相册添加图片（自动压缩），可发送、可删除，最多 24 张 | 新增 |
| 5 | 安卓物理返回键优先关闭表情面板 / 各弹出层，不再一按就退出页面 | 修复 |

### 1.2 v1.34（**已上线** · App 更新提示即 `version.json` 的 notes）

| # | 更新内容 | 类型 |
|---|---|---|
| 6 | 修复手机上一行 5 个入口（会话/通讯录/群聊/申请/添加好友）宽度不足、右侧留白不占满 | 修复 |
| 7 | 修复部分页面底部菜单大小与其它页不一致（安卓系统自动放大字体所致） | 修复 |
| 8 | 优化 AI 配置项校验，配置异常时明确提示而不是静默失效 | 优化 |
| 9 | 聊天支持**长按消息引用/回复**（微信式，带最小快照，被引用消息删除/撤回后仍可显示） | 新增 |
| 10 | 发送失败的消息显示**失败态 + 点击重发**（图片、文本、语音、文件全覆盖） | 新增 |
| 11 | 聊天输入框支持**按住说话**：按下录音、松手发送、上滑取消，屏上实时提示 | 新增 |
| 12 | 网络请求统一 **15 秒超时**，不再无限转圈；超时给出明确文案并支持重试 | 修复 |
| 13 | 实时位置共享：**点进即加入**（不再需要对方重复确认），反复进出不刷屏 | 优化 |
| 14 | 后台消息横幅通知：**通知渠道升级 + JobScheduler 15 分钟兜底拉活 + 熄屏轮询加快** | 修复 |
| 15 | 通知权限被拒后**有引导闭环**（toast 提示 + 一键跳系统设置），不再永久静默 | 修复 |
| 16 | 设备统计后台（管理员）：设备总数/新增/活跃、按平台与版本分布、分页明细，**androidId 脱敏展示** | 新增 |
| 17 | 读取已安装应用列表用于**设备风控**（识别多开/模拟器/异常设备），登录页已声明用途 | 新增 |
| 18 | 新增**崩溃与 JS 异常落盘**（`logs/` 目录，可通过文件提供器导出），排查线上问题不再靠猜 | 新增 |
| 19 | 图标渲染防闪烁：图标未就绪前保持占位，不再出现"先空后跳" | 优化 |
| 20 | 全站弹层（右上角菜单 / 退出确认 / 各浮层）接入统一返回键压栈，**安卓返回键行为符合直觉** | 修复 |

> 第 6–8 项是 v1.34 的 `version.json` notes 原文；第 9–20 项是**同一版本已随包上线、但当时未列入更新提示**的内容（详见第三节 R3/R3b/R4 明细）。

---

## 二、逐项明细（现象 → 根因 → 改动 → 验证）

### R5-A 表情面板重做（本次核心，用户直接提出的需求）

**用户原话**：「点击表情的时候是展开显示的，能不能做成第二张图片一样的点击显示，就是和微信一样，还可以收藏表情，自定义添加表情的菜单」

- **现象**：点表情按钮后，在输入框右上方弹出一个小浮层（`width:300px`），手机上又小又挤；且"收藏"的星星只有 `:hover` 才显示 —— **触屏设备没有 hover，等于收藏功能完全不可用**。
- **根因**：
  1. 面板是 `position:absolute;bottom:60px;right:12px;width:300px`（贴输入框的挂件形态），不是底部面板；
  2. 收藏交互绑在 CSS `:hover` 伪类上（`common.css` 旧 `.im-emoji-fav:hover{opacity:.75}`），触屏永远不会命中。
- **改动**（严格限定 4 个文件）：
  - `assets/common.css`（+38/−8，5 hunk，全部集中在表情 CSS 段 L2156-2198）：
    - `.im-emoji-panel` → `position:fixed;left:0;right:0;bottom:0;height:58vh;max-height:58vh;border-radius:16px 16px 0 0`，`transform:translateY(110%)` → `.open{transform:translateY(0)}` 滑入动画，`max-width:520px;margin:0 auto` 兼顾大屏；降级双声明 `58vh` + `58dvh`；矮屏（`max-height:520px`）提到 `72vh`。
    - 新增 `.im-emoji-mask`（`position:fixed;inset:0;background:rgba(0,0,0,.35)`，`opacity/visibility` 过渡，`z-index:210`）、`.im-emoji-handle`（顶部拖拽条，`::before` 画 36×4 圆角条）、`.im-emoji-add`（虚线「＋」格）、`.im-emoji-sticker-menu`（长按菜单，`z-index:212`）。
      - 说明：`.im-emoji-fav` 的 `opacity:0` 与 `.im-emoji-item:hover .im-emoji-fav{opacity:.75}` **两条 :hover 规则均已删除**；唯一的 `:hover` 保留在 `.im-emoji-item` 本身（桌面端背景反馈），触屏收藏改由长按触发。
  - `assets/emoji/manifest.js`（+76/−1）：表情 **24 → 96**（`e001`…`e096`），**纯尾部追加**，`e001`–`e024` 的 `code` 与 `char` **一字未改**（历史消息靠它渲染）。
  - `assets/chat-local.js`（+396/−43，13 hunk，全部在 L5009 表情段 + `__IM_TEST__` 钩子块）：
    - `renderEmojiPanel(tab)` 重写，tabs = **最近 / 全部 / 收藏 / 自定义 / 收起**；事件委托 `emojiBindGrid` 统一处理点按与长按。
    - 长按 500ms 切换收藏（`touchstart` / `mousedown` + 定时器；移动超 12px 取消；触发后 `longFired` 屏蔽随后的 `click`），桌面 `contextmenu` 兜底，toast「已收藏 / 已取消收藏」。
    - 自定义表情：`imStickerPick()` → `#imStickerInput` → `imAddSticker()` → canvas 压缩（最长边 240px；PNG 先试，超 80KB 逐档降质 JPEG 0.8/0.6/0.45/0.32，仍超则 toast 拒绝）→ 存 `study_workbench_emoji_custom`（最多 24 张）。
    - **发送走既有图片链路**：`imSendSticker(idx)` 把 dataURL → `Blob` → `File` → 复用 `window.imSendImageFile`（在线 `POST /api/uploads/image` → `kind='image'`；离线回落 dataURL）。**没有新增后端接口、没有把 base64 当文本发送**。
    - 长按自定义表情 → 小菜单（发送 / 删除），定位用 `getBoundingClientRect()` 且**左右收边 + 上方不够则翻转到下方**。
  - `私聊.html`（+16/−3，4 hunk）：删除旧的内联 `style="display:none"` 浮层节点；新增 `#imStickerInput`；新增 `#imEmojiMask` + `#imEmojiPanel`（挪到与 `.im-plus-mask`/`.im-plus-menu` 同级，同类浮层集中）；ESC 同时关闭表情面板。
- **安卓返回键**：面板打开时 `xtLayerPush('imEmojiPanel')`，关闭时 `xtLayerPop` —— 复用 R4-A 已上线的 `assets/layer-stack.js`，未加载时 `try/catch` 静默降级。
- **验证**：
  - `node --check` 通过；`tools/qa/escheck_es2017.js` → `DONE 0`。
  - 行尾守恒（字节级）：`chat-local.js` CRLF 6056→6409、`common.css` CRLF 3317→3347、`manifest.js` CRLF 46→121，三文件 **loneLF 均为 0**；`私聊.html` loneLF 1353→1366、**crlf 恒为 0**。
  - 红线：`clamp(` / `min(` / `max(` **函数**命中 0（14 处命中全在注释行，7 处 `minmax(` 为合法值）、`@supports` = 0、`prompt/alert/confirm` = 0、`?.`/`??` = 0。
  - 结构：`<style>=2/</style>=2`、`<script>=29/</script>=29`、`<div>` 开 193 / 闭 193。
  - `manifest`：code 总数 96、无重复、`e001..e024` 全部存在。
  - 渲染零污染：`renderContent()` 未改，仍只做 `[emoji:code]` → `Unicode 字符`；`sticker:` 在 `chat-local.js` 出现 **0** 次。
  - jsdom 行为测试（`tools/qa/_r5_emoji_jsdom.js`）：面板 open/mask/tabs、收藏切换与收藏 tab 回填、长按触发且**不误插入**、自定义添加与上限、`imSendSticker` → `imSendImageFile` 链路、`renderContent` 输出、遮罩与「收起」关闭 —— **JSDOM_ALL => PASS**。

### R5-B 底部菜单最后一项归位（v1.34 主诉求之一）

- **现象（用户截图）**：手机上一行 5 个入口（会话 / 通讯录 / 群聊 / 申请 / 添加好友）**右侧留白、不占满**。
- **根因**：`.im-tab` 的 flex 值为 `flex:0 0 auto` —— **不伸缩**，5 个 tab 只占内容宽度，剩余空间全部留白。
- **改动**：`私聊.html` 手机媒体查询（`@media max-width:480px`）内改为 `flex:1 0 auto; min-width:56px; padding:0 6px; line-height:32px; height:34px; font-size:12px`（+7 行说明注释）。
- **验证**：`.im-tab` 在 L567 声明了 `box-sizing:border-box` 且 L579 未重置 → `min-width:56px` **已含 padding**；5 tab 最小总宽 = 5×56 + 4×8(gap) = **312px**，**320px 视口即可铺满**，375px 剩 63px 余量；最宽文案「添加好友」≈60px < 分配宽（≈67.6px @375）**不换行**；`overflow-x:auto` 兜底永不触发。

### R5-C 底部导航尺寸统一（Text Autosizing）

- **现象**：部分页面底部菜单的字号/图标明显比其它页大，观感不统一。
- **改动**：`assets/common.css` 的 `:root` 内**纯插入 29 行**（28 行说明注释 + 2 条声明，删除 0）：
  ```css
  -webkit-text-size-adjust: 100%;
  text-size-adjust: 100%;
  ```
- **⚠️ 本项存在技术争议，如实记录**：
  - QA 独立复核指出（W3C css-env-1 官方解释器 + MDN 兼容表）：Android WebView 的字体放大走的是 **`systemFontScale` → `setTextZoom()`** 通道，「`text-size-adjust` doesn't work in Android Webview yet（crbug.com/419469463）」；且该属性 **WebView Android 54+** 才支持，而本项目 `minSdk 21`（对应 WebView ≈37）**不支持 → 可能整条声明被忽略、回退到原行为**。
  - 即：**若真机放大来源是系统字体缩放，这条 CSS 改动可能无效**。
  - 唯一能真正切断系统字体缩放的是原生 `WebSettings.setTextZoom(100)`，但 `MainActivity.java` **刻意不调用**它，代码内注明理由：「若在此写死 `setTextZoom(100)` 反而会禁用系统字号跟随、破坏无障碍大字模式」。
  - **结论**：这是一次「保留无障碍大字模式」与「追求全站尺寸绝对统一」之间的取舍。当前选择**保留无障碍跟随**，CSS 声明作为「尽力而为」的对齐手段保留。**待真机实测后拍板**（见第五节遗留）。
- **验证**：`text-size-adjust` 全站改前 1 处（`ai-settings.html:65` 既有）→ 改后 3 处；`:root` 花括号 1136/1136 配平。

### R5-D AI 配置校验（C2）

- **现象**：AI 配置项异常时静默失效，用户看不到任何提示。
- **改动**：`assets/ai-page.js`（L616-624）与 `assets/ai-settings.js`（L224-239）的 `toast()` 改为**三层守卫**：`xtToast(state,msg,opts)` → `showToast(msg)` → 本页 `#aiToast`/`#setToast` 内联兜底，每层 `try/catch`。
- **验证**：`assets/xt-toast.js` 存在且两页均已引用；两文件 `git diff HEAD` 各 2 行，已随 v1.34 上线；在 `file://` 下不会因缺少某层而抛错（三层任一层可用即成功）。

### R5-E `设置.html` 注释准确性

- **改动**：L15 注释「本页 13 个子页」→「本页 12 个子页」。
- **验证**：`data-subpage` 标签数实测 = **12**，属性出现 12 次，与注释一致。

---

## 三、随 v1.34 一并上线的历史线（R3 / R3b / R4）

> 这几条线的代码**早于本次已进入生产**，此前从未提交 git。本次发版一并入库。

### 3.1 R3-L1 聊天界面错乱 + 状态栏黑边
- `私聊.html` viewport 增加 `interactive-widget=resizes-content`（软键盘正确压缩视口）；新增键盘避让规则；`MainActivity` 改为**沉浸式状态栏**（顶栏背景延伸到状态栏 + 内容避让）。

### 3.2 R3-L2 实时位置共享双向可见
- **根因**：`server/routers/liveloc.py` 的二级索引是**一对一映射** —— `_LIVE_BY_PEER[(min,max)] = share_id`，双方各自 `start` 时**后者直接覆盖前者**，表现为「只能单向看到对方」。
- **改动**：索引改为 **shareId 列表**（`append/remove`，空则 `pop` 防慢泄漏）；`_hydrate()` 只写 `active` 行；`live_start` 追加而非覆盖；`_end_existing()` 清干净本人同一目标的全部旧会话；`live_state` 返回 `sessions[]`（保留顶层旧字段作兼容层）；`hasFix = lat is not None and lng is not None` 修掉 `Number(null)===0` 造成的 **(0,0) 漂移**。
- **新增**：`silent` 入参 —— `live-location.html`「点进即加入」的隐式 start 不落系统卡片，避免聊天流刷屏。

### 3.3 R3-L3 后台消息横幅（P0）
- 通知渠道 `xt_msg` → **`xt_msg_v2`**（换新渠道 = 重置 `importance`，绕过历史偏低/被用户关掉的渠道）；通知 id 由固定 101 改为**基址递增**（Android 更新"已存在"的通知时默认不再弹 heads-up）。
- 新增 `PollKeepAliveJobService.java`（`JobService`，`setPeriodic(15min)` + `setPersisted(true)`）兜底拉活，`AndroidManifest.xml` 注册（`BIND_JOB_SERVICE`，**未加新权限**）。
- `MainActivity` 新增桥 `isNotifyGranted()` / `openNotifySettings()`；权限拒绝分支补 toast + 跳系统设置引导；新增 `maybeGuideBatteryOnce()` 一次性引导忽略电池优化。
- `PollLogic.java` 熄屏轮询 `300_000L → 120_000L`。
- **验证**：`javac` 真编译通过（`-source 8 -target 8`，aapt2 compile/link rc=0，10 个源文件 exit 0）。

### 3.4 R3-L4 设备统计后台
- 新增 `server/routers/admin_devices.py`（`/api/admin/devices` 与 `/api/admin/devices/stats`）+ `管理员.html` / `admin.js` / `admin.css` 前端。
- **隐私红线**：`androidId` **脱敏展示（只留前 4 后 4）**；普通用户访问返回 401/403；不采集 IMEI / MAC / 通讯录 / 短信。

### 3.5 R3-L5 已安装应用列表（风控）
- 新增 `server/routers/installed_apps.py`（`POST /api/user/installed-apps`）+ `MainActivity` 桥 `getInstalledAppsForRisk()` / `sha256Hex()` / `setRiskEnabled()`。
- 用途**仅限账号与设备安全风控**（识别多开 / 模拟器 / 异常设备）；登录页已补充「会读取哪些设备信息以及用途」说明。

### 3.6 R3b-A 观感 + 稳定性 + 原生交互
- `assets/error-boundary.js`：前端异常经原生桥追加写入 `logs/js-YYYYMMDD.log`（与原生崩溃同目录），自带兜底 + 去重限流（同 msg 10s 内只落盘一次）。
- 新增 `CrashLogger.java` / `LogFileProvider.java`，崩溃可导出。

### 3.7 R3b-B 网络健壮性
- `assets/api.js`：`api()` 增加 **AbortController 15 秒超时**（按路径可放宽），超时抛「网络超时，请检查网络后重试」；与 401 续期协同（abort 只作用于当次请求，不会与 401 分支混淆）。

### 3.8 R3b-C 长按引用 + 失败重发（本批最大一块）
- **后端**：`server/database.py` 新增四个可空列 `reply_to_id` / `reply_sender_name` / `reply_kind` / `reply_text`（**守卫式 ALTER 加列，存量库零破坏**，不做 DB 级 FK）；`server/schemas.py` 新增 `ReplyIn`（`toId` + 最小快照，`text` 限长 100）；`server/routers/chat.py` 新增 `resolve_reply()` 与 `_REPLY_PLACEHOLDER`（`[图片]`/`[语音]`/`[位置]`/`[文件]`/`[实时位置]` 占位）；`groups.py` 复用 `chat.py` 的工具（不反向 import，无循环依赖）。
- **前端**：微信式长按引用（发送前快照 + 清空草稿）；失败消息**失败态 + 点重发**，图片重发复用原 `File` 重走「上传 → 发送」。

### 3.9 R3b-D 安全收紧 + 交互细节
- 校正 `setAllowUniversalAccessFromFileURLs` 的注释认知（后端实为公网服务器，非局域网）。

### 3.10 R4-A 公共基建层
- 新增 **`assets/layer-stack.js`**（弹层压栈基建，公开 API：`xtLayerPush/Pop/IsTop/Depth/Register/Bind`）→ 解决「安卓返回键一按就退出页面」。
- `assets/xt-toast.js` 扩展：`opts.position`（top/bottom）+ `opts.offset`，接口向后兼容，各页内联 toast 统一转调。

### 3.11 R4-B / C / D
- R4-B：死按钮修复 + 底部安全区 + 图标热区。
- R4-C：高频弹层（右上角菜单 / 退出确认）接入返回键压栈。
- R4-D：图标防闪烁（`html:not(.icons-ready) [data-icon]{visibility:hidden}` + `finally` 加 `icons-ready`，**绝不永久隐藏**）。

---

## 四、上线记录

| 批次 | 内容 | 结果 |
|---|---|---|
| R4（= v1.34，**已上线**） | 底部菜单统一 + 5 入口铺满 + AI 配置校验 + 全量前端/后端 | 全量差异部署，**逐文件 md5 零差异 2074/2074** |
| R5（= 拟 v1.35，**待上线**） | 表情面板重做（4 文件）+ 19 个文件入库 | 代码完成；主理人独立核验通过；QA 独立复核在跑 |

**v1.34（R4）实测数据留档**：
- 版本 → 1.34 / 35；`/api/app/version` → `apkReady=true`，`apkUrl` 为绝对地址。
- 全量差异：`LOCAL 2074 / REMOTE 2089 / DIFF_OR_NEW 62`；依赖闭包缺失 **0**；**营救出新增资源 `assets/layer-stack.js`**（被 6 个页面引用，硬编码清单必漏）。
- tar 打包 62 文件上传；APK 重打 `87,244,449` 字节，签名证书 SHA-256 `2b95937e…` **未变**（可覆盖安装），badging 1.34 / 35。
- `version.json` / `xt-update.js` / APK 三方 md5 一致（本地 == `/tmp` == 已安装）。
- 行尾基线不变：LF 50 / CRLF 12 / MIXED 0。
- 用户数 16；`traceback|500` 近 10 分钟计数 0。

---

## 五、R5 待部署清单（预检事实）

> 本节是**计划**，非既成事实。发布状态见第六节。

| 目标 | 本地 | 生产 |
|---|---|---|
| `web/私聊.html` | `私聊.html`（`1AA0954E` / 93053 B / LF） | 待覆盖 |
| `web/assets/chat-local.js` | `assets/chat-local.js`（`D1C92F48` / 353178 B / CRLF） | 待覆盖 |
| `web/assets/common.css` | `assets/common.css`（`C983D9F5` / 157714 B / CRLF） | 待覆盖 |
| `web/assets/emoji/manifest.js` | `assets/emoji/manifest.js`（`087D0626` / 6438 B / CRLF） | 待覆盖 |

- 映射规则：真源根 = 生产 `web/`（`x.html → web/x.html`；`assets/a.js → web/assets/a.js`）。
- **需 bump 的 `?v=` 戳**：`私聊.html` 中引用的 `assets/chat-local.js`、`assets/common.css`、`assets/emoji/manifest.js` 三个戳必须换成新字母（内容变了戳不换 → 老用户命中旧缓存 = 白改）。
- **APK**：这 4 个文件全部打进 APK → **必须重打并上传 `星途-1.35.apk`**（Web 部署对 App 无效）。
- **版本号四处同步**：`server/routers/version.json`（1.35 / 36 / notes / changelog）+ `android/AndroidManifest.xml` + `assets/xt-update.js` 的 `CURRENT_VERSION` + APK 实体名。

---

## 六、发布状态（一句话）

| 项目 | 状态 |
|---|---|
| R3 / R3b / R4 代码 | ✅ **已上线**（v1.34），但**未入库** |
| R5 表情面板代码 | ✅ 完成 + 主理人核验通过；QA 独立复核在跑 |
| R5 部署上线 | ⏸ **未执行**（用户要求先出文档，部署待确认） |
| 本地 git 提交 | ⏸ 未提交（19 个文件待入库） |
| GitHub 同步 | ⏸ 未执行（远端 `be18396e`；本轮计划走**增量同步分支**，不 force） |

---

## 七、已知遗留（按优先级）

1. **🔴 `text-size-adjust` 修复可能对真机无效**（详见 R5-C 争议）——需真机实测后决定：保留无障碍大字模式（现状）或改 `setTextZoom(100)`（尺寸绝对统一但失去系统字号跟随）。QA 另建议把 `100%` 改为语义更精确的 `none`。
2. **🔴 `server/.env` 的密钥早已随 GitHub 历史公开** → **唯一补救是轮换**（`JWT_SECRET` 一换现有登录态全部失效，需择时）。详见 R2A–R2C 交接文档第二节。
3. **🔴 `assets/ai-config.js` 的 Gemini Key 仍是前端明文且线上公开**（`keyInQuery: true`）→ 待拍板：轮换 + 改后端中转（需重打 APK），或保持前端直连。
4. **🟡 远端 `refs/tags/1.0`** 仍指向旧初始提交 → 旧对象（含 `server/.env`）持续可达；是否删除待拍板。
5. **🟡 真机验证未做**：表情面板（键盘弹出时是否被遮挡）、长按收藏手感、自定义表情发送与接收、横幅通知三态 × 权限矩阵、图片捏合与「保存到相册」。
6. **🟢 `tools/verifier/verify_increment_0911.js` 中两条断言已被机制变更淘汰** —— 它断言 `panel.style.display === 'block'/'none'`，而表情面板现改为 `class` 开关（`.open`）。**属预期性失效，非行为回归**，已被新的 jsdom 测试取代；建议单独一批更新该 legacy 断言。
7. **🟢 本地 git 历史仍含明文密钥** → 每次 push 仍需走净化流程。
8. **🟢 真源工作区约 500+ 项未跟踪临时物** → 建议单独一批**出清单**后归档，**不要顺手删**。
