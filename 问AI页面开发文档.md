# 问 AI 页面开发文档

> 目标：做一个简洁、响应式、灵活配置的 AI 问答页面，所有免费模型内置，用户开箱即用，同时支持自定义模型。
> 核心原则：不写死（Key 可配、模型可配、参数可配、供应商可换），自动降级，任何情况都能用。



***

## 一、页面整体结构

### 1.1 页面由 5 部分组成



```
┌─────────────────────────────────────────────┐

│ ☰ 顶部导航栏                            ✚   │

├─────────────────────────────────────────────┤

│                                             │

│  【欢迎页】或【对话页】（根据状态切换）        │

│                                             │

├─────────────────────────────────────────────┤

│  输入框（欢迎页在中间，对话页固定底部）        │

└─────────────────────────────────────────────┘

侧边栏（点☰从左滑出）

模型选择弹窗（点"自动▾"弹出）

模型说明弹窗（侧边栏入口）

自定义模型配置弹窗（模型选择弹窗底部入口）
```

### 1.2 两种页面状态



| 状态  | 触发条件       | 显示内容               |
| --- | ---------- | ------------------ |
| 欢迎页 | 无对话 / 点新对话 | 居中欢迎语 + 输入框 + 推荐问题 |
| 对话页 | 发送第一条消息后   | 消息列表 + 底部输入框       |

### 1.3 在主站左侧导航加入口（重要）

问AI页面不是孤立页面，要在**项目主站的左侧主导航栏**（就是首页、四级备考、央国企笔试那一列）加一个"AI"入口，用户从任何页面都能一键进入。

**主站左侧导航新增菜单项：**

```
星途 logo
─────────
🏠 首页
✨ AI              ← 新增这个，放在首页正下方，一级入口
─────────
学习模块
  四级备考
  央国企笔试
  高情商表达
  商务礼仪面试
  PPT训练
─────────
创作
  广场
...（其余不变）
```

**具体要求：**

| 项目 | 要求 |
|---|---|
| 菜单名 | `AI`（就两个字，不要叫"星途AI""AI助手""智能问答"） |
| 位置 | 放在"首页"正下方，作为一级高频入口，不放进任何分组 |
| 图标 | 用线性SVG图标（星星或聊天气泡风格，和其他菜单图标统一），不要用emoji和机器人头像 |
| 选中态 | 停留在问AI页面时，该菜单项橙色高亮，和首页选中样式一致 |
| 点击行为 | 跳转到问AI页面（ai.html 或对应路由），不弹窗、不新开标签页，站内直接切换 |
| 返回 | 主站左侧导航在问AI页面保持可见，点其他菜单能正常切回各模块 |

**两种集成方式（按项目现有架构选一种）：**

- 方式A（独立页面）：新建 `ai.html`，点"AI"菜单跳转到这个页面，页面内复用主站左侧导航
- 方式B（单页切换）：和现有模块一样作为一个视图section，点"AI"菜单显示问AI视图、隐藏其他视图

**注意：** 无论哪种方式，主站左侧导航在问AI页面都要保留显示，不能让用户进了AI页面就找不到回首页的路。问AI页面自己的☰侧边栏（历史对话那个）是二级菜单，和主站左侧导航不冲突。



***

## 二、顶部导航栏

### 2.1 布局



```
┌─────────────────────────────────────────────┐

│ ☰                                        ✚  │

└─────────────────────────────────────────────┘
```



* **左侧 ☰ 菜单按钮**：点击从左侧滑出侧边栏

* **右侧 ✚ 新对话按钮**：点击保存当前对话并回到欢迎页

* **没有标题文字、没有 Logo、没有机器人头像**（极简风格）

### 2.2 交互



| 按钮 | 点击行为                                   |
| -- | -------------------------------------- |
| ☰  | 打开侧边栏，同时显示半透明遮罩                        |
| ✚  | 弹确认框 "开始新对话？当前对话会保存到历史记录"，确认后清空消息回到欢迎页 |

### 2.3 样式



* 高度：56px

* 背景：白色，底部 1px 浅灰边框

* 按钮：20px 图标，hover 浅灰背景圆角 8px

* 左右 padding：24px（桌面）/ 16px（移动端）



***

## 三、欢迎页

### 3.1 布局（桌面端）



```
┌─────────────────────────────────────────────┐

│                                             │

│                                             │

│              欢迎使用，随时开始吧             │  ← 28px加粗，居中

│                                             │

│      ┌───────────────────────────────┐      │

│      │ 问我任何学习问题...             │      │  ← 输入框

│      │                               │      │

│      │                    📎 自动▾ \\\[↑]│      │  ← 按钮全部靠右

│      └───────────────────────────────┘      │

│                                             │

│   ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐

│   │行测怎么  │ │帮我制定  │ │数量关系  │ │申论大作文│   │  ← 推荐问题

│   │复习？    │ │学习计划  │ │有什么技巧│ │怎么写？  │   │

│   └─────────┘ └─────────┘ └─────────┘ └─────────┘

│                                             │

└─────────────────────────────────────────────┘
```

### 3.2 欢迎语



* 文案："欢迎使用，随时开始吧"

* 样式：28px 加粗，深灰色 (#333)，居中

* 位置：垂直居中偏上，输入框上方 48px

* **没有 Logo、没有机器人头像、没有产品名**

### 3.3 输入框



| 元素   | 说明                                               |
| ---- | ------------------------------------------------ |
| 容器   | 最大宽度 720px，居中，白色背景，圆角 20px，1px 边框，轻阴影            |
| 文本域  | placeholder"问我任何学习问题..."，16px，无边框，自动增高（最高 120px） |
| 附件按钮 | 右下角，SVG 回形针图标，点击上传图片（拍题讲题用）                      |
| 模型选择 | 附件旁边，显示 "自动 ▾"，点击弹出模型选择菜单                        |
| 发送按钮 | 最右，圆形橙色 (#ff8c00)，白色↑图标，有内容时高亮，无内容时置灰            |
| 按钮对齐 | **所有按钮靠右对齐**，左侧不留快捷按钮                            |

### 3.4 推荐问题



* 4 个卡片，桌面端 4 列，平板 2 列，移动端单列

* 每个卡片：白色背景、1px 边框、圆角 12px、hover 上浮 + 蓝色边框

* 点击卡片直接发送该问题

* **推荐问题从配置读取，不写死**，可根据用户最近学习模块动态变化

推荐问题配置示例：



```
const SUGGEST\\\_QUESTIONS = \\\[

\&#x20; "行测怎么复习？",

\&#x20; "帮我制定学习计划",

\&#x20; "数量关系有什么技巧？",

\&#x20; "申论大作文怎么写？"

];

// 后续可根据用户最近学习的模块动态替换
```



***

## 四、对话页

### 4.1 布局



```
┌─────────────────────────────────────────────┐

│ ☰                                        ✚  │

├─────────────────────────────────────────────┤

│                                             │

│                          用户消息气泡（右）   │

│                                             │

│ AI头像 AI消息气泡（左）                      │

│                                             │

│                          用户消息气泡（右）   │

│                                             │

│ AI头像 AI消息气泡（左）                      │

│                                             │

├─────────────────────────────────────────────┤

│ ┌─────────────────────────────────────────┐ │

│ │ 输入消息...                              │ │

│ │                             📎 自动▾ \\\[↑]│ │

│ └─────────────────────────────────────────┘ │

└─────────────────────────────────────────────┘
```

### 4.2 消息气泡



| 元素   | 用户消息            | AI 消息           |
| ---- | --------------- | --------------- |
| 对齐   | 靠右              | 靠左              |
| 头像   | 右侧，橙色圆形，显示 "我"  | 左侧，蓝色圆形，显示 "AI" |
| 气泡背景 | 橙色 (#ff8c00)，白字 | 白色，深字，1px 灰边框   |
| 圆角   | 12px，右下角 4px    | 12px，左下角 4px    |
| 最大宽度 | 85%             | 85%             |
| 字号   | 15px，行高 1.6     | 15px，行高 1.6     |

### 4.3 AI 消息的操作按钮

AI 消息右下角，hover 时显示：



* **复制**：点击复制完整回答

* **重新生成**：点击重新调用 AI 回答

* 时间戳：间隔超过 5 分钟才显示，12px 灰色

### 4.4 "正在输入" 状态

AI 回复前显示三个跳动圆点动画：



```
AI头像  ● ● ●
```



* 三个圆点依次上下跳动（CSS animation）

* 收到第一个字后消失，开始流式输出

### 4.5 流式输出（打字机效果）



* AI 回答逐字显示，不是一次性蹦出来

* 用 SSE/stream 方式接收

* 自动滚动到最新内容

### 4.6 底部输入框



* 固定在页面底部

* 最大宽度 720px，居中

* 布局和欢迎页输入框一致（附件、模型选择、发送全部靠右）

* 回车发送，Shift + 回车换行



***

## 五、侧边栏

### 5.1 布局



```
┌──────────────────────┐

│ 菜单             ✕   │  ← 标题栏

├──────────────────────┤

│ ┌──────────────────┐ │

│ │  + 新建对话       │ │  ← 橙色按钮

│ └────────────────────┘

│ 历史对话             │  ← 分组标题（12px灰色）

│ ┌──────────────────┐ │

│ │ 行测怎么复习？     │ │  ← 历史记录项

│ │ 帮我制定学习计划   │ │

│ │ 数量关系技巧       │ │

│ └──────────────────┘ │

│                      │

│                      │

├──────────────────────┤

│ 📖 模型说明           │  ← SVG书本图标

│ ⚙️ AI设置            │  ← SVG齿轮图标

│ 🗑️ 清除所有对话       │  ← SVG垃圾桶图标，红色文字

└──────────────────────┘
```

### 5.2 尺寸和动画



* 宽度：300px

* 从左侧滑入，transition 0.3s

* 遮罩：半透明黑色 (rgba (0,0,0,0.4))，点击遮罩关闭

* z-index：侧边栏 100，遮罩 90

### 5.3 每个元素的功能



| 元素     | 功能                              |
| ------ | ------------------------------- |
| 新建对话   | 保存当前对话，回到欢迎页，关闭侧边栏              |
| 历史对话列表 | 显示所有对话，标题取第一条用户消息前 20 字，点击加载该对话 |
| 空状态    | 没有历史时显示 "暂无历史对话"（灰色居中）          |
| 模型说明   | 打开模型说明弹窗（见第七节）                  |
| AI 设置  | 打开 AI 设置（参数调整、自定义 Key 等，后续扩展）   |
| 清除所有对话 | 确认后清空所有历史记录                     |

### 5.4 历史对话存储



* 存在 localStorage，key：`ai_chat_history`

* 数据结构：



```
\\\[

\&#x20; {

\&#x20;   id: "chat\\\_时间戳",

\&#x20;   title: "第一条消息前20字",

\&#x20;   createdAt: 时间戳,

\&#x20;   messages: \\\[

\&#x20;     { role: "user", text: "用户消息" },

\&#x20;     { role: "ai", text: "AI回复" }

\&#x20;   ]

\&#x20; }

]
```



* 新对话排在最前面

* 最多保存 50 条历史，超出删除最旧的



***

## 六、模型选择弹窗

### 6.1 布局



```
┌─────────────────────────────────────┐

│ 选择模型                         ✕  │

├─────────────────────────────────────┤

│ ┌─────────────────────────────────┐ │

│ │ ☑ 自动（推荐）                  │ │  ← 蓝色背景高亮

│ └─────────────────────────────────┘ │

│ ─────────────────────────────────  │

│ ☐ GLM-4.7-Flash              免费  │

│ ☐ GLM-4.5-Flash              免费  │

│ ☐ GLM-4-Flash                免费  │

│ ☐ GLM-4.6V-Flash             免费  │

│ ☐ GLM-4V-Flash               免费  │

│ ☐ Qwen2.5-7B                 免费  │

│ ☐ Qwen3-8B                   免费  │

│ ☐ Qwen3.5-4B                 免费  │

│ ☐ DeepSeek-R1-8B             免费  │

│ ☐ GLM-4-9B                   免费  │

│ ☐ Hunyuan-MT-7B              免费  │

│ ─────────────────────────────────  │

│ ✏️ 配置自定义模型                    │

└─────────────────────────────────────┘
```

### 6.2 样式



| 元素      | 样式                                   |
| ------- | ------------------------------------ |
| 弹窗      | 白色，圆角 16px，最大宽度 400px，最大高度 80vh，超出滚动 |
| 标题      | 18px 加粗，右上角✕关闭                       |
| 自动（推荐）  | 选中时浅蓝背景 (#e8f0fe)，蓝色方形勾选框            |
| 模型项     | 高度 48px，左侧方形复选框，中间模型名，右侧绿色 "免费" 标签   |
| 免费标签    | 绿色文字 (#52c41a)，浅绿背景 (#f6ffed)，圆角 4px |
| 选中状态    | 浅蓝背景 + 蓝色勾选框显示✓                      |
| 配置自定义模型 | 底部分隔线下方，铅笔 SVG 图标 + 蓝色文字，点击弹配置表单     |

### 6.3 交互



* 点击模型项即切换，勾选框打✓，菜单自动收起

* 输入框旁的模型按钮文字更新为选中模型名（选 "自动" 显示 "自动"）

* 选择存在 localStorage，刷新后保持

* 点击遮罩或✕关闭

### 6.4 模型列表不写死

模型列表从配置文件 `ai-config.js` 读取，**不硬编码在页面里**，后续新增 / 删除模型只改配置：



```
const BUILTIN\\\_MODELS = \\\[

\&#x20; {

\&#x20;   id: "glm-4.7-flash",

\&#x20;   name: "GLM-4.7-Flash",

\&#x20;   model: "glm-4.7-flash",

\&#x20;   apiUrl: "https://open.bigmodel.cn/api/paas/v4/chat/completions",

\&#x20;   apiKey: "内置公共Key",  // 默认公共Key，用户可在设置里替换

\&#x20;   type: \\\["general", "math"],

\&#x20;   tag: "免费",

\&#x20;   group: "通用",

\&#x20;   fallback: "glm-4.5-flash"  // 拥堵时降级到哪个

\&#x20; },

\&#x20; // ... 其他模型

];
```



***

## 七、模型说明弹窗

### 7.1 入口

侧边栏底部 "📖 模型说明" 按钮，点击弹出。

### 7.2 布局



```
┌─────────────────────────────────────────┐

│ 模型说明                             ✕  │

├─────────────────────────────────────────┤

│ 💡 所有模型均为永久免费，默认选"自动"即可 │  ← 蓝色提示条

│ ┌─────────────────────────────────────┐ │

│ │ GLM-4.7-Flash                  免费  │ │

│ │ 平台：智谱AI                        │ │

│ │ 类型：通用文本    参数：30B          │ │

│ │ 推荐度：⭐⭐⭐⭐⭐ 日常问答首选       │ │

│ │ 优势：30B大参数，中文理解强...       │ │

│ │ 适用：日常问答、方法咨询、面试模拟   │ │

│ └─────────────────────────────────────┘ │

│ ... 每个模型一个卡片 ...                 │

└─────────────────────────────────────────┘
```

### 7.3 11 个免费模型完整说明

#### 1. GLM-4.7-Flash



* 平台：智谱 AI

* 类型：通用文本

* 参数：30B

* 推荐度：⭐⭐⭐⭐⭐ 日常问答首选

* 优势：智谱最新免费模型，30B 大参数，中文理解能力强，上下文 200K

* 适用：日常学习问答、方法咨询、文案生成、面试模拟

* model id：`glm-4.7-flash`

* apiUrl：`https://open.bigmodel.cn/api/paas/v4/chat/completions`

#### 2. GLM-4.5-Flash



* 平台：智谱 AI

* 类型：通用文本

* 推荐度：⭐⭐⭐⭐ 稳定备用

* 优势：上一代免费模型，稳定性好，响应快

* 适用：4.7 拥堵时自动降级，日常问答

* model id：`glm-4.5-flash`

#### 3. GLM-4-Flash



* 平台：智谱 AI

* 类型：通用文本

* 推荐度：⭐⭐⭐ 兜底备用

* 优势：最稳定，几乎不拥堵，效果稍弱

* 适用：其他模型都拥堵时的最终兜底

* model id：`glm-4-flash`

#### 4. GLM-4.6V-Flash



* 平台：智谱 AI

* 类型：多模态（图片 + 文本）

* 推荐度：⭐⭐⭐⭐⭐ 图片理解首选

* 优势：免费支持图片理解，识别题目截图、PPT 截图

* 适用：拍题讲题、图片识别、PPT 截图分析

* model id：`glm-4.6v-flash`

#### 5. GLM-4V-Flash



* 平台：智谱 AI

* 类型：视觉理解

* 推荐度：⭐⭐⭐ 图片备用

* 优势：老版视觉模型，稳定

* 适用：4.6V 拥堵时备用

* model id：`glm-4v-flash`

#### 6. Qwen2.5-7B



* 平台：硅基流动

* 类型：通用文本

* 参数：7B

* 推荐度：⭐⭐⭐⭐ 中文问答

* 优势：阿里通义千问开源模型，中文好，速度快

* 适用：日常问答、学习方法、简单文案

* model id：`Qwen/Qwen2.5-7B-Instruct`

* apiUrl：`https://api.siliconflow.cn/v1/chat/completions`

#### 7. Qwen3-8B



* 平台：硅基流动

* 类型：通用文本

* 参数：8B

* 推荐度：⭐⭐⭐⭐ 新版通用

* 优势：Qwen 第三代，推理更强，支持工具调用

* 适用：日常问答、逻辑推理、简单数学

* model id：`Qwen/Qwen3-8B`

#### 8. Qwen3.5-4B



* 平台：硅基流动

* 类型：通用文本

* 参数：4B

* 推荐度：⭐⭐ 极轻量

* 优势：小参数，响应极快

* 适用：极简单问答、分类，复杂问题不推荐

* model id：`Qwen/Qwen3.5-4B`

#### 9. DeepSeek-R1-8B



* 平台：硅基流动

* 类型：推理专用

* 参数：8B

* 推荐度：⭐⭐⭐⭐⭐ 数学推理首选

* 优势：专门优化逻辑推理和数学计算，解行测题比通用模型准

* 适用：数量关系、资料分析、判断推理

* model id：`deepseek-ai/DeepSeek-R1-0528-Qwen3-8B`

#### 10. GLM-4-9B



* 平台：硅基流动

* 类型：通用文本

* 参数：9B

* 推荐度：⭐⭐⭐ 通用备用

* 优势：智谱开源 9B，中文不错

* 适用：通用问答备用

* model id：`THUDM/GLM-4-9B-0414`

#### 11. Hunyuan-MT-7B



* 平台：硅基流动

* 类型：通用文本（多语言）

* 参数：7B

* 推荐度：⭐⭐⭐ 多语言

* 优势：腾讯混元开源，多语言强，翻译好

* 适用：英语翻译、四级备考、多语言问答

* model id：`tencent/Hunyuan-MT-7B`

### 7.4 说明内容不写死

模型说明数据存在 `ai-config.js` 里，和模型列表共用一份配置，新增模型时说明自动同步，不需要单独改页面。



***

## 八、自定义模型配置

### 8.1 入口

模型选择弹窗底部 "✏️ 配置自定义模型"，点击弹出配置表单。

### 8.2 表单布局



```
┌─────────────────────────────────────────┐

│ 配置自定义模型                       ✕  │

├─────────────────────────────────────────┤

│ 模型名称 \\\*                               │

│ \\\[我的DeepSeek                        ]  │

│                                         │

│ API 地址 \\\*                               │

│ \\\[https://api.siliconflow.cn/v1       ]  │

│                                         │

│ API Key \\\*                                │

│ \\\[sk-xxxxxxxxxxxxxxxx                 ]  │

│ 👁 显示/隐藏Key                          │

│                                         │

│ 模型 ID \\\*                                │

│ \\\[deepseek-ai/DeepSeek-V3.2           ]  │

│                                         │

│ 适用类型（多选）                          │

│ ☑通用  ☐推理  ☐图片                     │

│                                         │

│ \\\[测试连接]            \\\[取消] \\\[保存]      │

└─────────────────────────────────────────┘
```

### 8.3 字段说明



| 字段      | 必填 | 说明                               |
| ------- | -- | -------------------------------- |
| 模型名称    | 是  | 显示在模型列表里的名字，用户自定义                |
| API 地址  | 是  | 接口地址，默认填硅基流动地址，可改成任意 OpenAI 兼容地址 |
| API Key | 是  | 用户自己的 Key，密码框，可显示 / 隐藏           |
| 模型 ID   | 是  | 调用时传的 model 参数                   |
| 适用类型    | 否  | 多选：通用 / 推理 / 图片，用于 "自动" 模式判断     |

### 8.4 测试连接



* 点击 "测试连接" 发一条 "hi" 消息

* 成功：绿色提示 "连接成功"

* 失败：红色提示具体错误（Key 无效 / 地址错误 / 模型不存在）

### 8.5 保存后



* 自定义模型出现在模型选择弹窗里，带 "自定义" 标签

* 存在 localStorage，key：`ai_custom_models`

* 可以编辑和删除（自定义模型才有删除按钮，内置模型不能删）

### 8.6 灵活配置原则



* **不绑定任何一家供应商**：只要是 OpenAI 兼容格式的 API 都能接（硅基流动、智谱、DeepSeek 官方、通义等）

* **用户可以加多个自定义模型**

* **内置 Key 作为默认**，用户填了自己的 Key 优先用用户的



***

## 九、"自动" 模式的智能选模型逻辑

### 9.1 默认选 "自动（推荐）"

用户不需要懂模型，系统根据问题类型自动选最优模型。

### 9.2 判断逻辑



```
用户发送消息

\&#x20;  ↓

是否上传了图片？

\&#x20;  ├─ 是 → 用 GLM-4.6V-Flash（多模态）

\&#x20;  └─ 否

\&#x20;     ↓

\&#x20;  是否是数学/推理题？（关键词匹配）

\&#x20;  ├─ 是（数量关系/资料分析/计算/工程/利润/增长率等）

\&#x20;  │     → 用 DeepSeek-R1-8B（推理模型）

\&#x20;  └─ 否

\&#x20;     ↓

\&#x20;  是否是英语翻译/四级问题？

\&#x20;  ├─ 是 → 用 Hunyuan-MT-7B（多语言）

\&#x20;  └─ 否 → 用 GLM-4.7-Flash（通用首选）
```

### 9.3 数学题关键词（可配置，不写死）



```
const MATH\\\_KEYWORDS = \\\[

\&#x20; "计算", "工程", "利润", "增长率", "比例", "方程", "几何",

\&#x20; "数量关系", "资料分析", "速度", "路程", "浓度", "排列组合",

\&#x20; "概率", "整除", "余数", "最大公约数", "最小公倍数"

];
```

### 9.4 自动降级链

每个模型配置 `fallback` 字段，调用失败自动降级：



| 首选             | 降级 1          | 降级 2          | 最终          |
| -------------- | ------------- | ------------- | ----------- |
| GLM-4.7-Flash  | GLM-4.5-Flash | Qwen2.5-7B    | GLM-4-Flash |
| DeepSeek-R1-8B | GLM-4.7-Flash | GLM-4.5-Flash | 预设问答        |
| GLM-4.6V-Flash | GLM-4V-Flash  | -             | 提示图片服务暂不可用  |

降级时 toast 提示："当前模型繁忙，已自动切换到备用模型"。



***

## 十、API 调用架构（灵活不写死）

### 10.1 文件结构



```
assets/

├── ai-config.js       # 模型配置、关键词配置、推荐问题配置

├── ai-service.js      # 统一调用、自动选模型、降级、流式输出

├── ai-presets.js      # 预设问答库（离线兜底）

└── ai-page.js         # 问AI页面的交互逻辑
```

### 10.2 内置 API 配置（直接复制可用，已实测验证）

项目内置两个平台的公共 Key，用户打开就能用，不需要自己注册。以下配置全部实测通过。

#### 10.2.1 平台一：智谱 AI（5 个免费模型）



| 配置项      | 值                                                                                          |
| -------- | ------------------------------------------------------------------------------------------ |
| API 地址   | `https://open.bigmodel.cn/api/paas/v4/chat/completions`                                    |
| 内置公共 Key | `f5aa78850e7a4575a1c4d119b03c3019.Uvg73xeMhVdN7crs`                                        |
| 请求方式     | POST                                                                                       |
| 请求头      | `Content-Type: application/json`，`Authorization: Bearer 你的Key`                             |
| 免费模型     | glm-4.7-flash、glm-4.5-flash、glm-4-flash、glm-4.6v-flash、glm-4v-flash                        |
| 限制       | 单模型 1 并发，flash 系列永久免费                                                                      |
| Key 管理页  | [https://open.bigmodel.cn/usercenter/apikeys](https://open.bigmodel.cn/usercenter/apikeys) |

**请求体示例（纯文本）：**



```
fetch("https://open.bigmodel.cn/api/paas/v4/chat/completions", {

&#x20; method: "POST",

&#x20; headers: {

&#x20;   "Content-Type": "application/json",

&#x20;   "Authorization": "Bearer f5aa78850e7a4575a1c4d119b03c3019.Uvg73xeMhVdN7crs"

&#x20; },

&#x20; body: JSON.stringify({

&#x20;   model: "glm-4.7-flash",

&#x20;   messages: \[

&#x20;     { role: "system", content: "你是学习助手，回答简洁务实。" },

&#x20;     { role: "user", content: "行测怎么复习？" }

&#x20;   ],

&#x20;   temperature: 0.7,

&#x20;   max\_tokens: 1000,

&#x20;   stream: true   // true=流式逐字返回，false=一次性返回

&#x20; })

})
```

**请求体示例（图片 + 文本，多模态模型 glm-4.6v-flash/glm-4v-flash）：**



```
body: JSON.stringify({

&#x20; model: "glm-4.6v-flash",

&#x20; messages: \[

&#x20;   {

&#x20;     role: "user",

&#x20;     content: \[

&#x20;       { type: "image\_url", image\_url: { url: "data:image/jpeg;base64,图片base64" } },

&#x20;       { type: "text", text: "这道题怎么做？" }

&#x20;     ]

&#x20;   }

&#x20; ],

&#x20; stream: true

})
```

#### 10.2.2 平台二：硅基流动（6 个免费模型）



| 配置项      | 值                                                                                                                                      |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| API 地址   | `https://api.siliconflow.cn/v1/chat/completions`                                                                                       |
| 内置公共 Key | `sk-ftwbrdrlxnkzalwkjxrnnltktyuljktxhvacbxdgckdrdwxw`                                                                                  |
| 请求方式     | POST                                                                                                                                   |
| 请求头      | `Content-Type: application/json`，`Authorization: Bearer 你的Key`                                                                         |
| 免费模型     | Qwen/Qwen2.5-7B-Instruct、Qwen/Qwen3-8B、Qwen/Qwen3.5-4B、deepseek-ai/DeepSeek-R1-0528-Qwen3-8B、THUDM/GLM-4-9B-0414、tencent/Hunyuan-MT-7B |
| 限制       | 免费模型 30 RPM（每分钟 30 次），9B 以下永久免费                                                                                                        |
| Key 管理页  | [https://cloud.siliconflow.cn/account/ak](https://cloud.siliconflow.cn/account/ak)                                                     |

**请求体示例：**



```
fetch("https://api.siliconflow.cn/v1/chat/completions", {

&#x20; method: "POST",

&#x20; headers: {

&#x20;   "Content-Type": "application/json",

&#x20;   "Authorization": "Bearer sk-ftwbrdrlxnkzalwkjxrnnltktyuljktxhvacbxdgckdrdwxw"

&#x20; },

&#x20; body: JSON.stringify({

&#x20;   model: "deepseek-ai/DeepSeek-R1-0528-Qwen3-8B",

&#x20;   messages: \[

&#x20;     { role: "system", content: "你是学习助手。" },

&#x20;     { role: "user", content: "这道数量关系题怎么解？" }

&#x20;   ],

&#x20;   temperature: 0.1,   // 数学题用低温度更准确

&#x20;   max\_tokens: 1500,

&#x20;   stream: true

&#x20; })

})
```

#### 10.2.3 完整的 ai-config.js（直接创建这个文件）



```
// ========== AI模型配置文件 ==========

// 所有Key、模型、参数都在这里配置，业务代码不写死

// 新增/删除模型只改这个文件

const AI\_CONFIG = {

&#x20; // 两个平台的内置公共Key（用户没填自己的Key时用这个）

&#x20; providers: {

&#x20;   zhipu: {

&#x20;     name: "智谱AI",

&#x20;     apiUrl: "https://open.bigmodel.cn/api/paas/v4/chat/completions",

&#x20;     apiKey: "f5aa78850e7a4575a1c4d119b03c3019.Uvg73xeMhVdN7crs"

&#x20;   },

&#x20;   siliconflow: {

&#x20;     name: "硅基流动",

&#x20;     apiUrl: "https://api.siliconflow.cn/v1/chat/completions",

&#x20;     apiKey: "sk-ftwbrdrlxnkzalwkjxrnnltktyuljktxhvacbxdgckdrdwxw"

&#x20;   }

&#x20; },

&#x20; // 内置免费模型列表（11个，全部永久免费）

&#x20; builtinModels: \[

&#x20;   {

&#x20;     id: "auto",

&#x20;     name: "自动（推荐）",

&#x20;     provider: null,

&#x20;     model: null,

&#x20;     types: \["general", "math", "image", "translate"],

&#x20;     tag: null,

&#x20;     fallback: null

&#x20;   },

&#x20;   // —— 智谱5个 ——

&#x20;   {

&#x20;     id: "glm-4.7-flash",

&#x20;     name: "GLM-4.7-Flash",

&#x20;     provider: "zhipu",

&#x20;     model: "glm-4.7-flash",

&#x20;     types: \["general"],

&#x20;     tag: "免费",

&#x20;     temperature: 0.7,

&#x20;     maxTokens: 1000,

&#x20;     fallback: "glm-4.5-flash"

&#x20;   },

&#x20;   {

&#x20;     id: "glm-4.5-flash",

&#x20;     name: "GLM-4.5-Flash",

&#x20;     provider: "zhipu",

&#x20;     model: "glm-4.5-flash",

&#x20;     types: \["general"],

&#x20;     tag: "免费",

&#x20;     temperature: 0.7,

&#x20;     maxTokens: 1000,

&#x20;     fallback: "qwen2.5-7b"

&#x20;   },

&#x20;   {

&#x20;     id: "glm-4-flash",

&#x20;     name: "GLM-4-Flash",

&#x20;     provider: "zhipu",

&#x20;     model: "glm-4-flash",

&#x20;     types: \["general"],

&#x20;     tag: "免费",

&#x20;     temperature: 0.7,

&#x20;     maxTokens: 1000,

&#x20;     fallback: null  // 最终兜底

&#x20;   },

&#x20;   {

&#x20;     id: "glm-4.6v-flash",

&#x20;     name: "GLM-4.6V-Flash",

&#x20;     provider: "zhipu",

&#x20;     model: "glm-4.6v-flash",

&#x20;     types: \["image", "general"],

&#x20;     tag: "免费",

&#x20;     temperature: 0.5,

&#x20;     maxTokens: 1500,

&#x20;     fallback: "glm-4v-flash"

&#x20;   },

&#x20;   {

&#x20;     id: "glm-4v-flash",

&#x20;     name: "GLM-4V-Flash",

&#x20;     provider: "zhipu",

&#x20;     model: "glm-4v-flash",

&#x20;     types: \["image"],

&#x20;     tag: "免费",

&#x20;     temperature: 0.5,

&#x20;     maxTokens: 1500,

&#x20;     fallback: null

&#x20;   },

&#x20;   // —— 硅基6个 ——

&#x20;   {

&#x20;     id: "qwen2.5-7b",

&#x20;     name: "Qwen2.5-7B",

&#x20;     provider: "siliconflow",

&#x20;     model: "Qwen/Qwen2.5-7B-Instruct",

&#x20;     types: \["general"],

&#x20;     tag: "免费",

&#x20;     temperature: 0.7,

&#x20;     maxTokens: 1000,

&#x20;     fallback: "glm-4-flash"

&#x20;   },

&#x20;   {

&#x20;     id: "qwen3-8b",

&#x20;     name: "Qwen3-8B",

&#x20;     provider: "siliconflow",

&#x20;     model: "Qwen/Qwen3-8B",

&#x20;     types: \["general", "math"],

&#x20;     tag: "免费",

&#x20;     temperature: 0.5,

&#x20;     maxTokens: 1200,

&#x20;     fallback: "qwen2.5-7b"

&#x20;   },

&#x20;   {

&#x20;     id: "qwen3.5-4b",

&#x20;     name: "Qwen3.5-4B",

&#x20;     provider: "siliconflow",

&#x20;     model: "Qwen/Qwen3.5-4B",

&#x20;     types: \["general"],

&#x20;     tag: "免费",

&#x20;     temperature: 0.7,

&#x20;     maxTokens: 800,

&#x20;     fallback: "qwen2.5-7b"

&#x20;   },

&#x20;   {

&#x20;     id: "deepseek-r1-8b",

&#x20;     name: "DeepSeek-R1-8B",

&#x20;     provider: "siliconflow",

&#x20;     model: "deepseek-ai/DeepSeek-R1-0528-Qwen3-8B",

&#x20;     types: \["math", "reasoning"],

&#x20;     tag: "免费",

&#x20;     temperature: 0.1,

&#x20;     maxTokens: 2000,

&#x20;     fallback: "glm-4.7-flash"

&#x20;   },

&#x20;   {

&#x20;     id: "glm-4-9b",

&#x20;     name: "GLM-4-9B",

&#x20;     provider: "siliconflow",

&#x20;     model: "THUDM/GLM-4-9B-0414",

&#x20;     types: \["general"],

&#x20;     tag: "免费",

&#x20;     temperature: 0.7,

&#x20;     maxTokens: 1000,

&#x20;     fallback: "qwen2.5-7b"

&#x20;   },

&#x20;   {

&#x20;     id: "hunyuan-mt-7b",

&#x20;     name: "Hunyuan-MT-7B",

&#x20;     provider: "siliconflow",

&#x20;     model: "tencent/Hunyuan-MT-7B",

&#x20;     types: \["translate", "general"],

&#x20;     tag: "免费",

&#x20;     temperature: 0.5,

&#x20;     maxTokens: 1000,

&#x20;     fallback: "glm-4.7-flash"

&#x20;   }

&#x20; ],

&#x20; // 自动模式：问题类型 → 首选模型

&#x20; autoRoute: {

&#x20;   image: "glm-4.6v-flash",

&#x20;   math: "deepseek-r1-8b",

&#x20;   translate: "hunyuan-mt-7b",

&#x20;   general: "glm-4.7-flash"

&#x20; },

&#x20; // 数学题关键词（自动判断用推理模型）

&#x20; mathKeywords: \["计算","工程","利润","增长率","比例","方程","几何","数量关系",

&#x20;   "资料分析","速度","路程","浓度","排列组合","概率","整除","余数"],

&#x20; // 英语翻译关键词

&#x20; translateKeywords: \["翻译","英语","四级","六级","单词","语法","translate","english"],

&#x20; // 欢迎页推荐问题（可动态替换）

&#x20; suggestQuestions: \["行测怎么复习？","帮我制定学习计划","数量关系有什么技巧？","申论大作文怎么写？"],

&#x20; // 频率限制

&#x20; rateLimit: { maxCalls: 10, perSeconds: 60 }

};
```

#### 10.2.4 统一请求函数（ai-service.js 核心，直接参考）



```
// 调用模型，自动处理Key、流式、降级

async function requestModel(modelConfig, messages, onChunk) {

&#x20; // 1. 找平台配置

&#x20; const provider = AI\_CONFIG.providers\[modelConfig.provider];

&#x20; // 2. Key优先级：用户自己填的 > 内置公共Key

&#x20; const userKey = localStorage.getItem("ai\_user\_key\_" + modelConfig.provider);

&#x20; const apiKey = userKey || provider.apiKey;

&#x20; const resp = await fetch(provider.apiUrl, {

&#x20;   method: "POST",

&#x20;   headers: {

&#x20;     "Content-Type": "application/json",

&#x20;     "Authorization": "Bearer " + apiKey

&#x20;   },

&#x20;   body: JSON.stringify({

&#x20;     model: modelConfig.model,

&#x20;     messages: messages,

&#x20;     temperature: modelConfig.temperature || 0.7,

&#x20;     max\_tokens: modelConfig.maxTokens || 1000,

&#x20;     stream: true

&#x20;   })

&#x20; });

&#x20; if (!resp.ok) {

&#x20;   const err = await resp.json().catch(() => ({}));

&#x20;   throw new Error("API错误:" + resp.status + " " + (err.error?.message || ""));

&#x20; }

&#x20; // 流式读取SSE

&#x20; const reader = resp.body.getReader();

&#x20; const decoder = new TextDecoder();

&#x20; let fullText = "";

&#x20; while (true) {

&#x20;   const { done, value } = await reader.read();

&#x20;   if (done) break;

&#x20;   const chunk = decoder.decode(value);

&#x20;   // 解析 data: {...} 行，提取 delta.content

&#x20;   chunk.split("\n").forEach(line => {

&#x20;     if (line.startsWith("data: ") && !line.includes("\[DONE]")) {

&#x20;       try {

&#x20;         const json = JSON.parse(line.slice(6));

&#x20;         const delta = json.choices?.\[0]?.delta?.content || "";

&#x20;         if (delta) { fullText += delta; onChunk && onChunk(delta, fullText); }

&#x20;       } catch(e) {}

&#x20;     }

&#x20;   });

&#x20; }

&#x20; return fullText;

}

// 带自动降级的调用

async function callWithFallback(modelId, messages, onChunk) {

&#x20; let tried = new Set();

&#x20; let current = AI\_CONFIG.builtinModels.find(m => m.id === modelId);

&#x20; while (current && !tried.has(current.id)) {

&#x20;   tried.add(current.id);

&#x20;   try {

&#x20;     return await requestModel(current, messages, onChunk);

&#x20;   } catch (e) {

&#x20;     console.warn("模型失败，降级:", current.id, e.message);

&#x20;     if (!current.fallback) break;

&#x20;     current = AI\_CONFIG.builtinModels.find(m => m.id === current.fallback);

&#x20;     if (current) toast("当前模型繁忙，已自动切换到" + current.name);

&#x20;   }

&#x20; }

&#x20; // 所有模型都失败 → 预设兜底

&#x20; return getPresetAnswer(messages\[messages.length-1].content);

}
```

#### 10.2.5 响应格式说明

**流式返回（stream:true）**：SSE 格式，每行 `data: {json}`，结束行 `data: [DONE]`



```
data: {"choices":\[{"delta":{"content":"你"}}]}

data: {"choices":\[{"delta":{"content":"好"}}]}

data: \[DONE]
```

**非流式返回（stream:false）**：



```
{

&#x20; "choices": \[{ "message": { "role": "assistant", "content": "完整回复" } }],

&#x20; "usage": { "total\_tokens": 120 }

}
```

#### 10.2.6 常见错误码和处理



| 错误码         | 含义           | 处理方式                   |
| ----------- | ------------ | ---------------------- |
| 401         | Key 无效 / 过期  | 降级到另一个平台，提示用户可填自己的 Key |
| 429         | 请求太频繁 / 并发限制 | 等 2 秒重试一次，还失败就降级       |
| 1305（智谱）    | 模型拥堵         | 立即降级到 fallback 模型      |
| 余额不足        | 公共 Key 额度问题  | 降级到另一个平台的免费模型          |
| 网络错误 / CORS | 连不上          | 走预设问答兜底                |

#### 10.2.7 CORS 跨域说明



* 智谱 AI 和硅基流动的接口**都支持浏览器直接调用（CORS 已开放）**，纯前端项目可以直接 fetch，不需要后端代理

* 如果部署后遇到 CORS 问题，再用服务器做一层反向代理，但优先直接调

### 10.3 统一调用函数



```
/\\\*\\\*

\&#x20;\\\* 调用AI

\&#x20;\\\* @param {string} userMessage - 用户消息

\&#x20;\\\* @param {object} options - { image, history, onChunk, funcType }

\&#x20;\\\* @returns {string|void} AI回复（流式时通过onChunk回调）

\&#x20;\\\*/

async function callAI(userMessage, options = {})
```

### 10.4 调用流程



```
callAI被调用

\&#x20;  ↓

1\\. 先查预设问答库（关键词匹配，离线，不耗API）

\&#x20;  ↓ 匹配不到

2\\. 确定用哪个模型

\&#x20;  \\- 用户手动选了 → 用用户选的

\&#x20;  \\- 选了"自动" → 根据问题类型智能选

\&#x20;  ↓

3\\. 获取API Key

\&#x20;  \\- 用户填了自己的Key → 用用户的

\&#x20;  \\- 没填 → 用内置公共Key

\&#x20;  ↓

4\\. 调用API（支持流式）

\&#x20;  ↓ 失败

5\\. 按fallback链降级重试

\&#x20;  ↓ 全部失败

6\\. 返回预设兜底回复
```

### 10.5 Key 的优先级（不写死）



```
用户自定义Key（localStorage） > 内置公共Key（ai-config.js） > 预设问答
```

### 10.6 调用频率限制



* 每个用户每分钟最多 10 次 API 调用（防止公共 Key 被刷爆）

* 超限提示："提问太频繁了，休息一下吧"

* 预设问答不受限制



***

## 十一、响应式布局

### 11.1 断点



| 屏幕宽度         | 布局变化                          |
| ------------ | ----------------------------- |
| > 1024px     | 输入框最大 720px，推荐问题 4 列          |
| 768px-1024px | 输入框最大 560px，推荐问题 2 列          |
| ≤ 768px      | 输入框占满宽度（左右 16px 边距），推荐问题单列    |
| ≤ 480px      | 欢迎语 22px，输入框圆角 16px，侧边栏宽度 85% |

### 11.2 移动端适配



* 侧边栏宽度改为屏幕的 85%

* 模型选择弹窗宽度占满（左右 16px 边距）

* 模型项高度 48px（方便手指点击）

* 模型选择按钮空间不够时只显示 "自动▾"，不显示完整模型名

* 消息气泡最大宽度 90%



***

## 十二、完整交互流程

### 12.1 首次打开



1. 显示欢迎页："欢迎使用，随时开始吧"

2. 显示输入框和 4 个推荐问题

3. 默认模型 "自动（推荐）"

### 12.2 发送文字消息



1. 用户输入文字（或点推荐问题）

2. 点发送或回车

3. 切换到对话页，用户消息显示在右侧

4. AI 侧显示 "正在输入" 动画

5. 系统判断用哪个模型（自动模式）

6. 调用 API，开始流式输出，逐字显示

7. 回答完成，显示复制 / 重新生成按钮

8. 对话自动保存到历史记录

### 12.3 发送图片（拍题讲题）



1. 点📎附件按钮，选择图片

2. 图片缩略图显示在输入框上方，可删除

3. 输入问题（如 "这道题怎么做"）

4. 发送时自动切换到多模态模型（GLM-4.6V-Flash）

5. AI 识别图片内容并回答

### 12.4 切换模型



1. 点输入框旁 "自动 ▾"

2. 弹出模型选择菜单

3. 点选模型，菜单收起

4. 按钮文字更新，toast 提示 "已切换到 XXX"

5. 后续对话用新模型

### 12.5 查看历史对话



1. 点左上角☰

2. 侧边栏滑出，显示历史对话列表

3. 点某条历史，加载该对话的所有消息

4. 可以继续对话

### 12.6 新对话



1. 点右上角✚或侧边栏 "新建对话"

2. 确认后当前对话保存

3. 清空消息区，回到欢迎页

### 12.7 查看模型说明



1. 点☰打开侧边栏

2. 点 "模型说明"

3. 弹窗显示 11 个模型的详细介绍

### 12.8 添加自定义模型



1. 点 "自动▾" → "配置自定义模型"

2. 填写名称、API 地址、Key、模型 ID

3. 点 "测试连接" 验证

4. 保存后出现在模型列表里



***

## 十三、实现步骤

### 第 1 步：搭页面骨架（2 小时）

* 主站左侧导航加"AI"菜单项（首页下方），配跳转和选中高亮
* 顶部导航栏（☰和✚）
* 欢迎页（欢迎语 + 输入框 + 推荐问题）
* 对话页（消息列表 + 底部输入框）
* 两种状态切换
* 响应式布局

### 第 2 步：做侧边栏（2 小时）



* 侧边栏滑入滑出动画 + 遮罩

* 新建对话按钮

* 历史对话列表（存 localStorage）

* 底部三个按钮（模型说明、AI 设置、清除）

### 第 3 步：做模型选择和说明（2 小时）



* 模型选择弹窗（11 个免费模型）

* 模型说明弹窗（每个模型详细介绍）

* 自定义模型配置表单 + 测试连接

* 选择状态持久化

### 第 4 步：搭 AI 服务层（3 小时）



* ai-config.js（模型配置、关键词、推荐问题）

* ai-service.js（统一调用、自动选模型、降级、流式）

* ai-presets.js（预设问答兜底）

* Key 优先级逻辑

* 频率限制

### 第 5 步：接入真实 AI（2 小时）



* 文字对话接入

* 流式输出（打字机效果）

* 正在输入状态

* 图片上传 + 多模态调用

* 自动降级测试

### 第 6 步：完善交互（2 小时）



* 复制、重新生成

* 历史对话加载和继续

* toast 提示

* 错误处理

* 移动端适配测试

**总预估：1.5 天左右**



***

## 十四、验收标准

### 页面布局

* [ ] 主站左侧导航"首页"正下方有"AI"菜单项，点击能进入问AI页面
* [ ] 停留在问AI页面时，"AI"菜单项橙色高亮
* [ ] 问AI页面保留主站左侧导航，能正常切回其他模块，不会困在AI页面
* [ ] 顶部只有☰和✚，没有标题、Logo、机器人头像

* [ ] 欢迎页居中显示 "欢迎使用，随时开始吧"

* [ ] 输入框按钮（附件、模型选择、发送）全部靠右

* [ ] 推荐问题 4 个卡片，点击直接发送

* [ ] 对话页用户消息靠右橙色，AI 消息靠左白色

* [ ] AI 回复有 "正在输入" 动画和流式打字机效果

### 侧边栏



* [ ] 点☰从左滑出，点遮罩或✕关闭

* [ ] 新建对话能回到欢迎页

* [ ] 历史对话自动保存，点击能加载

* [ ] 模型说明按钮能打开说明弹窗

* [ ] 清除对话有确认，清除后列表清空

### 模型选择



* [ ] 点 "自动▾" 弹出 11 个免费模型

* [ ] 每个模型有方形复选框和绿色 "免费" 标签

* [ ] 选中后高亮打勾，菜单收起，按钮文字更新

* [ ] 刷新后保持上次选择

* [ ] 模型说明弹窗显示 11 个模型的完整介绍

* [ ] 能添加自定义模型，测试连接，保存后出现在列表

### AI 调用



* [ ] ai-config.js 已配置两个平台的内置公共 Key，不填自己的 Key 也能直接用

* [ ] 11 个免费模型逐个测试都能正常返回（智谱 5 个 + 硅基 6 个）

* [ ] 流式输出正常（逐字显示，不是一次性蹦出）

* [ ] 自动模式能根据问题类型选模型（数学题用推理模型，图片用多模态）

* [ ] 模型拥堵时自动降级到备用模型，并有 toast 提示

* [ ] 没网 / Key 失效时走预设问答兜底，不崩溃

* [ ] 用户自己填的 Key 优先于内置 Key

* [ ] 每分钟超 10 次有频率限制提示

* [ ] 图片上传后能调用多模态模型识别

* [ ] 智谱模型走智谱地址，硅基模型走硅基地址，不串台

### 响应式



* [ ] 窗口缩小到 768px 以下，推荐问题变单列

* [ ] 移动端侧边栏占 85% 宽度

* [ ] 移动端模型弹窗占满宽度

* [ ] 所有按钮在移动端能正常点击



***

## 十五、不做的（砍掉）



* ❌ 不做语音输入和语音对话（后续再加）

* ❌ 不做机器人头像、产品 Logo、产品名（极简风格）

* ❌ 不做输入框里的快捷功能按钮（学习计划 / 题目讲解等，已删除）

* ❌ 不做云端同步（历史记录存 localStorage）

* ❌ 不做对话分享导出（后续再加）

* ❌ 不内置任何收费模型（只内置 11 个永久免费模型）

* ❌ 不把 API Key 写死在业务代码里（统一在 ai-config.js 配置，可替换）

* ❌ 不绑定单一供应商（支持任意 OpenAI 兼容 API）



***

## 十六、关键提醒（给开发者）



1. **不要写死**：模型列表、Key、关键词、推荐问题全部从配置文件读取

2. **一定要降级**：首选模型→备用模型→预设问答，任何情况 AI 都不能崩

3. **预设问答优先**：常见问题走本地预设，省 API、速度快、离线可用

4. **自动模式是默认**：90% 用户不会手动选模型，自动选模型逻辑要可靠

5. **图标统一用 SVG**：不要用 emoji，保持精致统一

6. **流式输出必做**：长回答要逐字显示，不要让用户干等

7. **所有配置存 localStorage**：模型选择、自定义 Key、历史对话刷新不丢

8. **内置 Key 直接用**：10.2 节的两个公共 Key 已实测可用，直接写进 ai-config.js，用户打开就能对话，不需要自己注册

9. **两个平台地址不要搞混**：智谱模型走 [open.bigmodel.cn](https://open.bigmodel.cn)，硅基模型走 [api.siliconflow.cn](https://api.siliconflow.cn)，每个模型配置里的 provider 字段决定用哪个地址和 Key

10. **Key 安全**：内置公共 Key 会暴露在前端代码里，这是纯前端项目的正常取舍（靠频率限制和降级保护）；项目正式上线后如果 Key 被刷爆，去平台后台重置 Key 并更新 ai-config.js 即可，同时建议引导高频用户填自己的 Key