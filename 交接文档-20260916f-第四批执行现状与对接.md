# 交接文档 · 2026-09-16f —— 第四批（R64 → R67）执行现状与对接

> 面向**下一位接手 AI**。读完本文件 + `更新日志-20260916-第四批-R64至R67.md` + `用户需求与决策总账.md` 的 R63~R67 章节，即可无歧义续作。
> 本文件生成时间：2026-09-16 22:20 · **最后更新：2026-09-17 01:45（R68/R70 收尾后——接手者先读 §0.5，§1~§9 为 22:25 时点快照）**
> **收工状态**：用户于 22:23 指示「剩余任务不再推进」→ 本轮主理人已停止；**R66 独立 QA 未执行、R67 线1/线2 未开工，全部转交接手者**。
> **⚡ 2026-09-17 更新**：R66 独立回归、R67 线1/线2、R68、打戳 `20260916R`、部署、R70 两项修复（连通性检测 + APK 重打包）**均已完成**（commit `1abfc88` / `f34939d`）。§0 速览与 §4/§9 的「未做」类结论**已过时**，以 §0.5 为准；开场指令最新版 = `给接手AI的开场指令-20260917a.txt`。

> ⚡ **接手者请先看 `给接手AI的开场指令-20260916f.txt`**（可直接粘贴给 AI 的开场指令，含第一件事与派活口径）。

---

## §0 30 秒速览

| 项目 | 现值 |
|---|---|
| 唯一可写代码树 | `D:\下载的文件\学习工作台` ⛔ 绝不改 `C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-*`（废弃副本，缺文件、无完整 `.git`） |
| 根 HTML 数 | **40** 个 |
| 全站版本戳 | `20260916O` × 516 处 + `20260916Q` × 65 处（后者 = 4 个 AI 资产，R64/R65 打） |
| 版本戳 | ✅ **已打 `20260916R`**（66 处；Q 残留 0；O 516 处未动，实测 2026-09-17） |
| 是否部署上线 | ✅ **已部署**（`1abfc88` 批次 + R70 网页端修复均已上线） |
| 向后端 | 本批**未动** `server/` 任何文件 |
| 团队 | 会话团队 `software-r66-ai-polish`（3 工程师 + 1 QA，**已全部停止/paused**）。⚠️ 团队与子代理 transcript 绑定在**旧会话**上，新会话**不一定能唤醒**——唤醒失败就按 §5 口径重建，别依赖旧成员状态 |
| 任务清单（旧会话） | #12 QA-R66（in_progress，**无报告**）／#13 线1、#14 线2（**未开工**）／#15 线3（completed）／#16 QA-R67（未开工） |
| **结论（09-17 更新）** | **代码无欠账**：验证/打戳/部署/R70 均已完成；剩余 = APK 真机验收 + 2 个小项，见 §0.5 |

---

## §0.5 2026-09-17 收尾增补（**接手者从这节开始读**）

### 已完成（01:40 实测复核）
- R66 独立回归 + R67 线1（服务商分组下拉/删重复入口/检测连接）+ 线2（上下文用量新格式）—— commit `1abfc88`
- R68 移动端 AI 对话框布局 4 连修 —— commits `64c7945` / `f748547` / `d7d8f76` / `98cdac0`
- 打戳 `20260916R`（66 处，Q 残留 0）+ 部署上线
- R70-①：设置页补载 `assets/ai-service.js`，连通性检测恢复（根因/验证详见更新日志 §八）
- R70-②：`android/build_apk.py` 修复（不再剔除 api.js、二进制复制、白名单补全、打包硬断言），
  重打 **versionCode 18 / 87.3MB 完整包** —— commit `f34939d`

### 接手者即办清单（新）
1. 🔴 **APK 真机验收（唯一硬性遗留）**：用户手机安装 versionCode 18 新包，验证连接服务器、好友私信、
   留言板、社区互动、语音、题库加载。异常排查入口：`android/build_apk.py` 的断言/白名单 + 本文 §6。
2. 🟡 R67 遗留小项：`providerGroups` 第二层厂商模型展示名与设置页下拉文案对齐。
3. 🟡 台账补记：把 R68/R70 备忘补进 `用户需求与决策总账.md`（现止于 R67/638 行；CRLF，二进制读写，先备份 `.bak-pre-r68-20260917`）。
4. ⚪ git 卫生：APK（87MB）已进 `.gitignore` 但仍被跟踪；下次提交代码前先 `git rm --cached 学习工作台-安卓App.apk`。
   工作树 vs HEAD 有 ~44 M + 6 D 更早批次漂移（含 `server/` 3 文件），属常态，判断改动用 mtime/内容。

### R70 新增陷阱（并入 §6，编号顺延）
⑨ **script 清单插标签必须数闭合**：漏 `</script>` 会吞后续脚本、页面静默全空且无报错；
   改完核对 `count(<script) == count(</script>)`（`ai-settings.html` 现 9/9）。
⑩ **打包脚本禁用「剔除式」正则**：曾把每页 api.js 删掉产出无联网空壳；现已有打包期硬断言兜底，
   改 `build_apk.py` 时不得绕过断言。

---

## §1 行尾铁律（本批实测值，改文件前必查）

| 文件 | 行尾 | 当前行数 / 字节 |
|---|---|---|
| `ai-settings.html` | **LF** | 558 / 26540 |
| `assets/ai-settings.js` | **LF** | 2257 / 84697 |
| `assets/ai-page.js` | **LF** | 1725 / 84485 |
| `assets/ai-service.js` | **LF** | 1650 / 71592 |
| `AI.html` | **CRLF** | 930 / 53019 |
| `assets/ai-config.js` | **CRLF** | 720 / 28712 |
| `用户需求与决策总账.md` | **CRLF** | 639 / 76567 |
| 39 个其它根 HTML + `app.js`/`common.css`/`company-lib.js`/`voiceplayer.js` | **CRLF** | — |

**改文件正确姿势（本项目出过生产事故）**
```python
raw = open(p, 'rb').read()          # 二进制读
raw2, n = re.subn(...)              # 只替换目标片段
open(p, 'wb').write(raw2)           # 二进制写
# 自测：CRLF 文件 loneLF 必须 0；LF 文件 CRLF/loneCR 必须 0
```
⛔ 绝不用 `io.open(p, 'w', encoding='utf-8', newline='')`（曾把 39 个 HTML 静默转成 LF 并推上生产）。

---

## §2 本批改动实况（含备份，可直接回滚）

| 交付文件 | 改动内容（本批） | 备份 |
|---|---|---|
| `ai-settings.html` | R64 建 5Tab → R66 密钥/检测/记忆/表单（558 行） | `.bak-pre-r64-20260916`（281 行）、`.bak-pre-r66-20260916`（477 行） |
| `assets/ai-settings.js` | R64 建 → R66 **全量重写**（2257 行） | `.bak-pre-r64-20260916`（795 行）、`.bak-pre-r66-20260916`（1760 行） |
| `assets/ai-page.js` | R64/R65 模式行+记忆+MAX 联动 → R66 下拉瘦身/改名/移除记忆入口/限高/输入区+用量（1725 行） | `.bak-pre-r64-20260916`（1367 行）、`.bak-pre-r66-20260916`（1640 行） |
| `AI.html` | R66 入口改名/面板 CSS/输入行重构（930 行） | `.bak-pre-r66-20260916`（915 行） |
| `assets/ai-service.js` | R64 底座（findModel/catModels/健康检查）→ R65 模式链+MAX → R66 密钥清洗+临时配置（1650 行） | `.bak-pre-r64-20260916`（1207 行）、`.bak-pre-r66-20260916`（1538 行） |
| `assets/ai-config.js` | R64 stars/speed/能力化 → R65 modelModes → R67 providerGroups（720 行） | `.bak-pre-r64-20260916`（521 行）、`.bak-pre-r67-20260916`（533 行） |
| `用户需求与决策总账.md` | 追加 R64/R65/R66/R67 备忘（639 行） | `.bak-pre-r64-20260916`（518 行） |

**主理人独立抽验结论（不采信成员自述）**
- 备份均为真·改动前原样（含旧版本串、含旧 provider key 回落逻辑、`providerGroups` 0 命中）。
- R64 特性在新文件中全部存活：`order` 29/29、`drag` 15/15、`moveModel` 3/3、上移/下移 6/6，与备份计数逐一相等。
- 新 `ai-settings.js` / `ai-settings.html` 内 6 个平台密钥明文**0 命中**。
- `providerGroups` 单块（1 定义 + 1 注释）、段内 13 key 无重复、`node --check` 通过。
- 行尾：上述表格数值即最终态，零污染。

---

## §3 接手必读的数据契约（详见更新日志 §五）

- `ai_selected_model` / `ai_model_mode` / `ai_memory` / `ai_custom_models` / `ai_model_settings`（单键：`disabled` `order` `overrides` `catModels` `categories` `health` `stars`）
- `AI_CONFIG.modelDetails[id].stars|speed`、`AI_CONFIG.modelModes`、`AI_CONFIG.providerGroups`
- `window.aiHealthCheck(modelId, cfgOverride?)` → `{ok, ms, err}`，err ∈ `http_XXX`/`cors`/`network`/`timeout`/`empty`/`no_endpoint`/`no_key`；**不 recordCall、不烧限频**
- `window.aiGetModelSettings()` / `aiSaveModelSettings(obj)`（含平台密钥副本幂等清洗）

---

## §4 执行状态与待办（**接手者即办清单**）

> 本轮主理人于 2026-09-16 22:23 收到用户指示「剩余任务不再推进」，故**第 1、2 条以下全部留给接手者**。代码已落盘、有备份、有契约文档，接手即可继续，无需重做已完成部分。

1. 🔴 **第一件事：R66 独立回归（未做）**。R66 三线自测分别为 70/70、34/34、37/37，但**没有独立验证**——按项目惯例「开发自测 ≠ 通过」。被测 4 文件：`ai-settings.js`、`ai-settings.html`、`ai-page.js`、`AI.html`。必验项见 `更新日志` §三与本文 §6-③；重点是 **① 密钥专项（localStorage 全量 dump 不含平台密钥、编辑不回填、空保存不丢覆盖 key）② 因 `ai-settings.js` 被全量重写，必须逐项行为断言 R64/R65 特性未丢**。
2. 🔴 **第二件事：R67 线1 / 线2（未开工）**。设计上被 R66 QA 阻塞（**保归因**：QA 验的正是这两个文件，不能边验边改）。任务书要点：`用户需求与决策总账.md` 的 R67 备忘 §决策 1-4 + `更新日志` §四。
   - 线1 = `ai-settings.html` + `assets/ai-settings.js`：删 `#setAddCustom` 重复入口并同步空态文案（`ai-settings.js:1065`）、模型名称改**服务商分组下拉**（消费线3 已交付的 `AI_CONFIG.providerGroups`）、模型 ID 改下拉联动、切模型 ID 自动预填能力标签、API 格式默认隐藏（高级设置可展开，保住 Gemini）、底部「检测连接」。
   - 线2 = `assets/ai-page.js` + `AI.html`：上下文用量格式改 `0.0% · 0 / 1000.0K 上下文已使用`（百分比 1 位小数 + 上限 K 单位 1 位小数）+ hover 说明 + 阈值色沿用 + 排版紧凑。
3. ⏳ **打戳 `20260916R`**：R66/R67 全部验证通过后执行。规则见 §6-②（属性锚定 / 最小变更 / 捕获组不带后缀 / 独立终态复验 / 二进制读写 / 不复用已服务戳）。当前 4 个 AI 资产仍为 `?v=20260916Q`，**与新内容不符，属"待打戳"状态**。
4. ⏳ **部署上线**：需用户点头，流程见技能 `study-workbench-deploy`（3 道安全闸 + 中文名 ASCII 中转 + MD5 全量核对）。
5. 🧹 交付树仍有 ~96 个他批 `.bak-*`（本批新增的 r64/r66/r67 备份**不在清理范围**，回滚要用）。
6. 📌 小项：`providerGroups` 第二层厂商模型展示名（线3 按通用命名补）与线1 下拉文案对齐。

---

## §5 接手三步走

**第一步 · 2 分钟体检**
```bash
PY="C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe"
NODE="C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe"
cd "D:/下载的文件/学习工作台" && $NODE tools/qa/escheck_es2017.js      # 期望 DONE 0
```
再核：4 个 AI 资产行尾（表格 §1）、戳仍是 `20260916Q`、`用户需求与决策总账.md` 尾部有 R64~R67 备忘。

**第二步 · 唤醒或重建团队**
- 优先尝试唤醒旧会话团队 `software-r66-ai-polish` 的成员（`SendMessage` → `software-qa-engineer` / `software-engineer-任务一` / `software-engineer-任务二`）。
- ⚠️ **若唤醒失败（新会话常见）**：不要纠缠，直接按 §5 第三步口径**新建团队 + 新成员**，任务书用「总账 R67 备忘 + 更新日志 §四」现成内容改写，文件独占清单照抄，不要新建替代性"更大"的团队。
- 派活顺序建议：**先 QA 补验 R66 → 放行 R67 线1/线2 → 最终 QA（R66+R67 全覆盖）→ 打戳 `20260916R` →（用户点头后）部署**。R67 线3 的 `ai-config.js` 已交付且经主理人抽验，无需重做。

**第三步 · 派活口径（本项目验证有效，照抄）**
- 每条任务书必须自带：① 唯一可写树 + 独占文件清单 ② 行尾要求（逐文件写清 CRLF/LF）③ ES2017 禁令清单 ④ 顶层全局名守卫要求 ⑤ 不 bump 版本戳 ⑥ 备份命名 ⑦ 跨线契约（结构/签名一字不改）⑧ 自测清单（node --check / 行尾 / ES2017 Grep / 全局名交集 / jsdom 行为断言）⑨ 「直接给结论、不反复验证」的工作风格。
- 工程师线用 `subagent_type: software-engineer`（**有 Bash**）；**QA 线必须用 `general-purpose`**（`software-qa-engineer` 类型无 Bash，6 秒即挂）。
- 0 配额时 spawn 指定 `model: "lite"` 可立即恢复（UI 切模型对子代理无效）。
- 并行前先列**独占文件清单**，保证零重叠；共享文件（`app.js`/`api.js`/`common.css`）同一时刻只允许一条线改。

---

## §6 陷阱清单（本批新增，务必先读）

① **bash 缺 coreutils**：`ls/grep/tail/head/wc/dirname/cp/env` 全部不存在。所有统计/替换一律 **Write 落盘 `.py`/`.js` 再执行**；结果写 UTF-8 文件再 Read。
② **打戳三铁律**（R63/R66 各踩一次坑）：
   - **属性锚定**：正则必须锚 `(?:src|href)\s*=\s*["\']assets/…`，否则会污染注释与 JS 字符串（R63 曾把 `错题本.html` 的用户可见报错文案改成带版本号的）。
   - **最小变更**：只 bump 内容真改过的资产；仅新建页面才给未改动资源补戳（沿用现有戳）。
   - **捕获组不得带后缀**（R66 曾产出 65 处 `xxx.js.js?v=…`）；**终态扫描必须用与替换不同的独立 pattern**，并显式扫 `\.js\.js\?v=` 必须 0。
   - 二进制读写保行尾；不复用已服务过的戳（Q 已服务，下一版用 R）。
③ **全量重写高风险**：R66 线1 全量重写了 `ai-settings.js`。凡遇同类重写，必须「签名计数对照备份 + QA 逐项行为断言」双兜底。
④ **成员自述 ≠ 事实**：R66 线3 自述曾误执行补丁两次，主理人抽验才确认最终态正确。**收工前一律实测落盘状态**（mtime + 内容签名）。
⑤ **判断本批改了哪些文件用 mtime，不要用 git**（本仓库 HEAD 很旧，`git status` 会列出几十个更早批次文件）；但根 HTML 的 mtime 会被波末打戳统一覆盖，届时只能用内容 grep / MD5 对照。
⑥ **禁 git 写操作**（push 需动态代理）；重命名用 `os.rename`。
⑦ **禁原生 `alert/confirm/prompt`**，用 `uiConfirm`/`toast`（设置页为页内自建轻量实现，因未引 `app.js`）。
⑧ **生产操作先问用户**：删用户数据 / 改 nginx / 部署上线，三件必须用户点头。

---

## §7 需求台账指引

| 编号 | 主题 | 台账位置 |
|---|---|---|
| R63 | AI 模型接入 + 模型设置页（24 模型 6 平台、Gemini 方案 A） | `用户需求与决策总账.md` R63 章节 |
| R64 | 模型设置页 14 条升级 | 同上 R64 备忘（8 组 + 6 条架构决策） |
| R65 | 三模式 + 记忆 + MAX 升级 | 同上 R65 备忘（3 条 + 4 条决策） |
| R66 | AI 第三轮优化 9 组（密钥/下拉/用量/检测/表单） | 同上 R66 备忘（9 组 + 6 条决策） |
| R67 | 上下文格式 + 表单再优化 3 组 | 同上 R67 备忘（3 组 + 4 条决策） |

**需求原文位置**（用户桌面，接手时先读）：`模型升级.txt`(R64)、`AI继续升级.txt`(R65)、`AI再次优化.txt`(R66)、`持续升级优化.txt`(R67)。

---

## §8 文档编号关系（本目录）

| 文件 | 覆盖范围 |
|---|---|
| `交接文档-20260916d-第三批执行现状与对接.md` | 第三批（R49~R61 等） |
| `交接文档-20260916e-第三批续作与对接.md` | 第三批续作 + R62 |
| **`交接文档-20260916f-第四批执行现状与对接.md`** | **本文件：第四批 R64~R67** |
| `更新日志-20260916-第四批-R64至R67.md` | 本批更新日志（含契约表、踩坑、未完成清单） |

> ⚠️ 前三份文档部分结论已被后续批次推翻（例：e 文档「AI 三件套全站无戳」在 R63 后已不成立），**以本文件与总账为准**。

---

## §9 收尾声明（本轮主理人 · 2026-09-16 22:25）

**停止原因**：用户指示「剩下的任务你不用再做了，把日志和交接文档更新好，并告诉下一个 AI 让他快速上手」。

**已确认落盘并抽验通过（接手者可直接采信）**
- R64 + R65 全部改动 + **独立 QA 14/14 PASS** + **已打戳 `20260916Q`**（32 页 / 65 处）+ 全站 `escheck DONE 0`。
- R66 三线全部改动落盘（自测 70/34/37，**无独立验证**）；备份齐全；行尾零污染；R64 特性计数对照备份逐一相等；新文件平台密钥明文 0 命中。
- R67 线3 `AI_CONFIG.providerGroups` 落盘（自测 15/15 + 主理人抽验：单块、13 key 无重复、`node --check` 通过、备份真原样）。
- 两份文档（本文件 + 更新日志）+ `给接手AI的开场指令-20260916f.txt`。

**明确未做（接手者的工作，别当成已完成）**
1. R66 独立回归（一次都没跑完）。
2. R67 线1（设置页表单服务商分组与精简）、线2（上下文用量新格式）——**一行代码都没改**。
3. 打戳 `20260916R`、部署上线、R67 最终 QA。

**未动的东西（保持原样，勿误改）**
- `server/` 后端、`app.js`、`api.js`、`common.css`、其余 36 个根 HTML、他批 96 个 `.bak-*`。
- 旧会话团队目录与 transcript（可能已随会话清理，不作为依赖）。

**一句话交接（22:25 时点，已被 §0.5 取代）**：*代码都在，验证/打戳/部署没做完；先把 R66 验了，再放行 R67 线1线2，最后打 `20260916R`。*

**一句话交接（2026-09-17 最新）**：*代码、戳、部署、APK 全部就绪；先陪用户跑 APK 真机验收，再补 providerGroups 展示名对齐与 R68/R70 台账。*
