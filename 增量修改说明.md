# 学习工作台 · 5 项增量改造说明

> 本次改动均**直接落在了现有文件上**，你打开网页即可使用。下方按「修改哪个文件 + 粘贴到哪个位置」标注了每一段代码，方便你对照、微调或重新手动应用。
> 涉及文件：`assets/common.css`、`assets/app.js`、`设置.html`、新增 `登录.html`、`个人中心.html`，其余 16 个模块页由脚本 `tools/inject-v2.js` 批量注入（侧边栏菜单 + 顶栏搜索框）。

---

## 一、新增 / 修改的文件清单

| 文件 | 类型 | 说明 |
|---|---|---|
| `登录.html` | **新增** | 全站第一道入口，未登录强制跳到这里 |
| `个人中心.html` | **新增** | 从学习博客剥离出来的独立个人中心页 |
| `assets/common.css` | 修改（末尾追加） | 新增：全局搜索 / 个人中心 / 登录页 / AI配置 四组样式 |
| `assets/app.js` | 修改（多处） | 新增：登录门禁、个人中心渲染、全局搜索、AI服务商配置与上下文记忆 |
| `设置.html` | 修改（插入一张卡片） | 新增「AI 服务商配置」卡片 |
| 其余 16 个 `.html` | 修改（脚本注入） | 侧边栏新增「个人中心」菜单项 + 顶栏新增全局搜索框 |
| `tools/inject-v2.js` | **新增** | 幂等批量注入脚本（可重复跑，不破坏已有结构） |

---

## 二、登录门禁（需求 2）

**位置：`assets/app.js` 顶部，`// ========== 数据存储 ==========` 之前**

```js
// ========== 登录门禁（本地演示版） ==========
const AUTH_KEY = 'study_workbench_auth';        // 当前登录会话 {account, loginAt}
const USERS_KEY = 'study_workbench_users';     // 本地账号表 {账号: {pass:哈希, createdAt}}
function getAuth() { try { return JSON.parse(localStorage.getItem(AUTH_KEY)); } catch (e) { return null; } }
function doLogout() {
  if (!confirm('确定退出登录吗？')) return;
  localStorage.removeItem(AUTH_KEY);
  location.replace('登录.html');
}
if (!getAuth()) { location.replace('登录.html'); }   // 未登录 → 强制跳登录页
```

**位置：`assets/app.js` 顶部 `pageTitles` 与 `PAGE_FILES`**，各加一行：

```js
'profile': '个人中心'                                  // pageTitles 内
profile: '个人中心.html',                              // PAGE_FILES 内
```

**新增独立页 `登录.html`**：完整登录/注册界面（`登录/注册` 标签切换、账号/确认密码/密码、登录按钮、本地演示提示），自带一套极简 `script`（**不引用 app.js**，避免与门禁循环）。
- 存储：账号表 `study_workbench_users`，密码用 `simpleHash()`（非安全加密，防肉眼明文）；会话 `study_workbench_auth`。
- 首次登录无账号时自动注册并进入，降低上手门槛。

> ⚠️ **本地限制**：账号仅存本机浏览器 localStorage，刷新不丢、换设备/换浏览器不可见。多用户注册、找回密码、权限管理必须后端。

---

## 三、拆分个人中心页（需求 1 + 5）

**① 新增 `个人中心.html`（由脚本从 `学习博客.html` 克隆外壳生成）**，页面区为：

```html
<div class="page active" id="page-profile">
  <div class="module-hero">
    <div class="module-hero-left">
      <div class="module-hero-title">👤 个人中心</div>
      <div class="module-hero-desc">个人资料 · 笔记统计 · 数据导出 · 退出登录</div>
    </div>
  </div>
  <div id="profileBox"></div>   <!-- 由 app.js renderProfilePage() 渲染 -->
</div>
```

**② `assets/app.js`：新增 `renderProfilePage()`**，渲染：头像/昵称/签名/编辑资料按钮、账号、退出登录、本地单用户提示、笔记统计卡（已发布/草稿/归档/点赞/评论/阅读）、笔记分类分布、导出全部 Markdown。

**③ 侧边栏新增「个人中心」菜单项**——`tools/inject-v2.js` 已给全部 18 页在「中心」分组末尾、`<div class="nav-section">系统</div>` 之前注入：

```html
<div class="nav-item" data-page="profile">
  <span class="nav-icon">👤</span><span>个人中心</span>
</div>
```

**④ `openBlogProfile()` 改为跳独立页**（原先是跳到博客统计视图）：

```js
function openBlogProfile() {
  closeMorePanel();
  location.href = '个人中心.html';   // 已独立成页
}
```

**⑤ 博客统计 tab 不再内嵌个人中心**：`renderBlogStats()` 已去掉原来的「个人资料卡」，只保留**博客数据统计**（6 指标卡 + 分类分布 + 导出按钮 + 「前往个人中心」按钮）。博客页仍是 4 个 tab：📚 笔记广场 / 👤 我的文章 / ✍️ 写笔记 / 📊 统计。

**⑥ `editProfile()` 扩展**：新增修改签名，并同时刷新个人中心页与博客统计：

```js
function editProfile() {
  // ...读 name/avatar/motto → 保存 → renderProfilePage(); renderBlogStats();
}
```

---

## 四、全局搜索（需求 3）

**位置：`assets/app.js` 末尾（`updateProfileUI();` 之后）**，新增：

- `MODULE_INDEX`：18 个模块的标题/图标/描述/关键词索引（含首页、设置、个人中心）。
- `globalSearch(kw)`：检索**① 博客笔记**（标题/内容/标签/摘要）+ **② 各模块标题/关键词**；输出分组下拉，要点即跳。
- `closeGsDropdown()`：收起下拉；并监听 `document` 点击外部自动收起。

**顶栏注入搜索框**（`tools/inject-v2.js` 已给 18 页在 `<div class="topbar-title"…>` 之后注入）：

```html
<div class="gs-wrap" id="gsWrap">
  <input type="text" class="gs-input" id="gsInput" placeholder="🔍 搜索笔记 / 模块…"
         autocomplete="off" oninput="globalSearch(this.value)" onfocus="globalSearch(this.value)">
  <div class="gs-dropdown" id="gsDropdown"></div>
</div>
```

**点击结果行为**：
- 笔记 → 若已在博客页则 `openBlogDetail(id)`，否则 `location.href='学习博客.html#note=ID'`（博客页初始化已支持 `#note=` 直达详情视图）。
- 模块 → `navigateTo('page')`（多页面版会自动跨页跳转到对应 `.html`）。

---

## 五、AI 功能优化（需求 4）

**位置：`assets/app.js`，`const AI_CONFIG` 附近**——把原 AI 段扩展为**三模式**：服务商直连 → 后端中转 → 本地演示。

**① 服务商配置体系（新增）**：

```js
const AI_CFG_KEY = 'study_workbench_ai_config';
const AI_PROVIDERS = [ /* 预置 DeepSeek/通义/Kimi/智谱/OpenAI/自定义，均兼容 OpenAI 协议 */ ];
function getAiProviderConfig()  // 读 localStorage
function saveAiProviderConfig(cfg)
function clearAiProviderConfig()
function currentAiMode()         // 返回 {mode:'provider'|'backend'|'demo', cfg}
```

**② 上下文记忆**：`buildAiContextMessages()` 取最近 14 条历史组装 `messages`（角色 user/assistant）。

```js
function buildAiContextMessages() {
  return aiChatHistory.slice(-14).map(m => ({ role: m.role === 'user' ? 'user' : 'assistant', content: m.text }));
}
```

**③ `sendAiMsg()` 三分发** + **`fetchProviderReply()`（OpenAI 兼容 /chat/completions + SSE 流式解析 + 上下文）**——密钥从 localStorage 读取仅用于本次请求头，不写进页面/日志/代码。

**④ 流式输出保留**：依然是 SSE 逐段 `data:` 解析，增量写入气泡；后端 `fetchAiReply()` 也补了 `messages` 上下文字段。

**⑤ 模式徽标/副标题自适应**：`toggleAiPanel()` 按 `currentAiMode()` 显示「服务商名 · 流式 · 带上下文」/「已连接后端」/「本地演示」。

**⑥ `设置.html`「AI 服务商配置」卡片**（插入在「数据管理」卡片之后、`<!-- 【后续扩展点】高级设置模块` 之前），表单由 `renderAiProviderForm()` 渲染：

```html
<div class="card">
  <div class="card-header">
    <div class="card-title"><span class="title-icon">🤖</span>AI 服务商配置</div>
    <div class="card-action" id="aiProviderBadge">右下角 AI 助手</div>
  </div>
  <div id="aiProviderForm"></div>   <!-- 由 app.js 渲染 -->
</div>
```

表单字段：服务商下拉、模型名称、接口地址、API Key（password 输入）、保存/测试连接/清除配置、安全提示。配套函数：`renderAiProviderForm()` / `onAiProviderChange()` / `saveAiProviderForm()` / `testAiConnection()` / `clearAiProviderConfigUI()`。

---

## 六、CSS（全部追加到 `assets/common.css` 末尾）

| 选择器前缀 | 作用 |
|---|---|
| `.gs-*` | 顶栏全局搜索框 + 下拉结果样式 |
| `.profile-*` | 个人中心头像 / 统计网格 |
| `.login-*` | 登录页卡片、标签、提示 |
| `.ai-cfg-*` | 设置页 AI 配置表单（两栏网格、安全提示） |

---

## 七、本地版本限制（务必知晓）

1. **存储**：所有数据（学习记录、笔记、账号、AI配置、聊天历史）均存本机浏览器 `localStorage`。**刷新不丢失**，但换浏览器/换设备/清除浏览数据即丢。
2. **用户**：账号密码仅本地，**无法实现真正的多用户**（别人看不到你的笔记，也登录不了你的账号体系）。
3. **AI 直连**：密钥只存本机，仅适合个人本地体验；**一旦发布/共享，密钥会暴露，务必改用后端中转**。
4. **评论/点赞/收藏/关注**：当前仍是单设备内的本地模拟，跨用户联动需要后端。

---

## 八、如需真正后端，需要新增的接口

> 可在现有 `ai-server/`（Node + Express）基础上新增以下路由，前端 `assets/app.js` 改指向后端即可，UI/交互不用重写。

**认证与用户**
- `POST /api/auth/register` 注册（存 DB：账号+密码哈希）
- `POST /api/auth/login` 登录 → 返回 JWT/Session
- `POST /api/auth/logout` 登出
- `GET  /api/users/:id` 个人资料；`PUT /api/users/:id` 修改资料

**笔记（博客）**
- `GET  /api/notes` 列表（分页/分类/标签/关键词）
- `POST /api/notes` 新建；`GET /api/notes/:id` 详情；`PUT /api/notes/:id` 编辑；`DELETE /api/notes/:id`
- `POST /api/notes/:id/like` 点赞；`POST /api/notes/:id/favorite` 收藏
- `GET/POST/DELETE /api/notes/:id/comments` 评论

**社交**
- `POST /api/users/:id/follow` 关注；`GET /api/users/:id/followers` 粉丝

**管理 / 统计**
- `GET /api/stats/me` 个人统计（发布/收藏/点赞/评论）
- `GET /api/admin/users`、`PUT /api/admin/users/:id/role` 权限管理（管理员）

**AI**
- `POST /api/chat` 统一 AI 入口（密钥放后端 `.env`，前端只传消息与上下文，`stream:true` 走 SSE）——替换掉前端 `AI_PROVIDERS` 直连，前端零密钥。

---

## 九、验证结论（浏览器实测通过）

- ✅ 登录门禁：未登录打开主页 → 自动跳 `登录.html`；注册 → 进入主页；登出 → 回登录页；账号密码可再次登录
- ✅ 个人中心：侧边栏「个人中心」菜单 / 侧边栏头像点击 → 独立 `个人中心.html`，含头像/签名/编辑/统计/分类分布/导出/退出/本地提示
- ✅ 博客保留 4 tab，统计 tab 无个人资料卡
- ✅ 全局搜索：命中笔记（标题/内容/标签）与模块；点笔记直达详情（本页或跨页 `#note=`），点模块跨页跳转
- ✅ AI：设置页可配置服务商/密钥/模型/地址并保存到 localStorage；模式徽标随配置切换；流式输出与上下文记忆已接入；假密钥收到 401 且错误提示/锁释放正常
- ✅ 全部 19 个 `.html` div 平衡、`node --check` JS 语法 OK、控制台无报错
