# 交接文档 · 2026-09-18h —— 第六批（R87）执行现状与对接

> 上一份：`交接文档-20260917g-第五批执行现状与对接.md`
> 本份覆盖 **R86（当日早间·硅基流动重接）+ R87（本批·AI 模型设置模块 8 条需求）**，实测时点 **2026-09-18 16:50**。
> 配套：`更新日志-20260918-第六批-R87.md`（改动明细 / 数据契约 / 踩坑全记录）、`给接手AI的开场指令-20260918h.txt`。
> 事实来源：全部为**落盘实测**（size / mtime / 行尾字节 / 符号计数 / node 真值求值 / 真内核 ES2017 解析），不采信成员自述。

---

## §0 30 秒速览

- **代码层：全部落盘并自测通过。** 本批改动 = **13 个 AI 资产 + `ai-settings.html` + 37 个根 HTML 的 cap 引用注入 + 后端 1 个路由确认 + 3 个工具脚本**。
- **八条需求全部有代码落地**（逐条见 §2），其中需求 ⑧ 有**一个前置缺口**：APK 未就位 → 更新检查只能显示「安装包暂时不可用」。
- **三件"最后一公里"没做**：**① 版本戳仍是 dry-run 未写盘**（目标 `?v=20260918c`）、**② 版本号五处不一致（一致性检查 exit=1）**、**③ 未部署**到 `110.42.134.62`（部署脚本已写好并 dry-run 三闸全过）。
- **已通过的真实验证**：真内核 ES2017 解析 `DONE 0`；部署闸1 行尾 47 页违规 0；部署闸2 新增符号 9/9、闸3 已删符号 1/1；刷戳 dry-run 14 资产全 PASS 且 `.js.js?v=` 命中 0。
- **一个已澄清的误报**：结构闸曾报 `ai-settings.html` 行尾 `loneLF=808` 为"CRLF→LF 回归"——经**历史备份比对**（`ai-settings.html.bak-pre-r72-20260917` 同样是 LF）确认该文件**本来就是 LF**，**不是回归**，无需修。

---

## §1 项目铁律（改文件前必读，违反＝返工）

### 1.1 唯一可写代码树

- ✅ **唯一可写**：`D:\下载的文件\学习工作台`
- ⛔ **绝对不碰**：`C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-*`（停在 R70 的过期空壳）
  - 历史上发生过"跑错树导致整批结论作废"的事故，**每次派活必须在任务书第一行写死唯一可写树**。

### 1.2 行尾矩阵（本批 R87 逐字节实测，是权威版）

| 文件 | 行尾 | 实测 |
|---|---|---|
| `ai-settings.html`（根目录） | **LF** | CRLF=0 / lone=808 / 42908 B |
| `assets/ai-settings.js` | **LF** | lone=3238 |
| `assets/ai-page.js` | **LF** | lone=2418 |
| `assets/ai-service.js` | **LF** | lone=3252 |
| `assets/ai-cap-*.js`（8 个，含 `registry`） | **LF** | 全部 CRLF=0 |
| `assets/xt-aiusage.js` | **LF** | lone=1421 |
| `assets/xt-update.js` | **LF** | lone=1035 |
| `assets/ai-config.js` | **CRLF** | CRLF=1101 / lone=0 |
| 其余根 `.html` | **CRLF** | 例：`关于.html` CRLF=419 |
| 其余 `assets/*.js`、`*.css` | **CRLF** | — |
| `server/**/*.py` | **CRLF** | — |

> ⚠️ **旧文档（截至 20260917g）把"LF 白名单"写成只含 4 个文件，且暗示 `ai-cap-*.js` 是 CRLF——错。**
> 正确口径：**LF 白名单 = `ai-settings.html` + `ai-settings.js` / `ai-page.js` / `ai-service.js` + 8 个 `ai-cap-*.js` + `xt-aiusage.js` / `xt-update.js`**，`ai-config.js` 是 CRLF。
> **改文件一律二进制读写**：`open(p,'rb')` → 字节级替换 → `open(p,'wb')`。**禁止 text 模式**（会静默把 CRLF 转 LF）。

### 1.3 ES2017 语法上限（安卓老 WebView 硬约束）

禁用清单（`node --check` **抓不到**，必须 Grep 逐项自查 0 命中 + 真内核解析）：

```
?.    ??    对象展开 {...obj}   对象剩余解构 {...rest}
.replaceAll(    Object.fromEntries    .at(
正则后行断言 (?<=  (?<!)    指数 **    可选 catch 绑定  catch {
```

- ✅ **权威检具**：`tools/qa/escheck_es2017.js`（真内核解析器）→ 本批实测输出 **`DONE 0`**（0 处违规）。
  - 运行：`node tools/qa/escheck_es2017.js <文件...>`
  - **不要**用 `node --check` 代替（它不报 ES2017+ 语法）。
- CSS 另禁 `clamp()` / `min()` / `max()` → 用固定值 + `@media` 分级。

### 1.4 本机环境（无 coreutils）

- ⛔ `bash` 里**没有** `ls / grep / cat / head / tail / cp / env / dirname`（所有 shell 命令直接 `command not found`，实测 exit 127）。
- ✅ **一律用 Python 落盘脚本再执行**；含中文/反引号的代码**绝不内联给 bash**（会被吞）。
- ✅ 统计结果**写 UTF-8 文件再 Read**（PowerShell stdout 常被吞成空）。
- 工具链绝对路径：

| 用途 | 路径 |
|---|---|
| Python | `C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe` |
| Node | `C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe` |
| jsdom | `C:/Users/ATM/node_modules/jsdom`（jsdom 30，不导出 ResourceLoader → 页面真实源码需内联后执行；localStorage 需 http origin，起临时 127.0.0.1 静态服务；`location` 只读，抓跳转要作用域遮蔽） |

> 注：本机 bash 的 `cd` 也常报 `cd: null directory`，但 `cd <绝对路径> && python ...` 仍能生效（已实测）。

### 1.5 版本戳规范（本项目"AI 修好了但用户测还是老样子"的头号成因）

资产 `<script src>` 不带 `?v=` → 浏览器 / APK WebView 命中旧缓存。打戳必须同时满足：

1. **属性锚定**：只在 `(src|href)=["']assets/<name>["']`（可带 `?v=`）处替换 → 绝不误伤注释与错误文案里的裸路径（R63 事故）。
2. **无双后缀**：捕获组不得含 `.js`，替换串里 `assets/<name>.js` 只出现一次 → 杜绝 `xxx.js.js`（R64 事故）。
3. **二进制读写**：行尾逐字节不变（曾把 39 页 CRLF 静默改 LF 推生产）。
4. **最小变更**：只 bump **内容真改过**的资产。
5. **独立终态复验**：用与替换**不同的** pattern 重数戳分布；显式扫 `\.js\.js\?v=` 与"非属性命中"必须为 0。
6. **不复用旧戳**，也不顺手刷未改资源的戳（项目审计靠 mtime）。

### 1.6 其他约定

- 改文件先备份：`xxx.bak-pre-rNN-日期`。**不要清理任何 `.bak-pre-*`**（回滚要用）。
- 顶层**禁重复声明已有全局名**（本项目"AI 功能集体失效"的历史元凶）；新增全局写 `typeof` 守卫。
- 禁原生 `alert/confirm/prompt`（用 `uiConfirm`/`toast`）；不删现有功能、保留既有 DOM id 与全局函数名。
- `assets/app.js` 里有一条约 2.7 MB 的超长行 → 任何批量脚本都不要碰。
- **git 工作树有大量 M 漂移（含 `server/`）属常态**——判断"本批改了什么"用 **mtime + 内容对照备份**，**不要用 `git status`**。
- `git push` 需走代理（第一优先读环境变量 `HTTP_PROXY`/`HTTPS_PROXY`，备选 `127.0.0.1:7897`）。
- **生产操作（删用户数据 / 改 nginx / 部署上线）必须先问用户。**
  - ✅ 本批用户已明确授权：**部署上线由主理人执行并验证**；**APK 由用户本地打包后上传**。

---

## §2 八条需求 × 落盘现状对照

| # | 用户需求 | 状态 | 落点（证据） |
|---|---|---|---|
| ① | 功能分类页按**真实能力**重做 | ✅ 完成 | `ai-settings.js`：`BUILTIN_CATS` **10→12 类**、`FAMILY_OF_CAT` + `FAMILY_ORDER` **6 族**、`FAMILY_LABEL`；`RESERVED_KEYS=[]` |
| ② | 新增「按功能分类排序」 | ✅ 完成 | `ai-settings.js`：`setSortCat`(3) / `setSortCatInner`(2) / `lastSort`(9)；`ai-settings.html`：排序弹窗新增分类下拉 |
| ③ | 只把**健康检查可用**的模型映射到 AI 页 | ✅ 完成 | `ai-page.js`：`isHiddenByHealth`(2) / `hideUnavailableOn`(5)/`usableIds`/`visibleModels`；无记录时兜底放行 |
| ④ | 不可用**自动隐藏** + 恢复**自动回归** | ✅ 完成 | `recordHealth` 派发 `xt:health-changed`（1 处定义）；`ai-page.js` 监听同名事件（1 处）→ 双向同步 |
| ⑤ | 图像/视觉/语音在**浏览器与 APK 都真能跑** | ✅ 完成 | 7 个 `ai-cap-*.js` 各加 `probe(ctx)`；视觉 **64×64 PNG** + HTTP-400 文本回退；3D **512×512 PNG**；translate 读 `provider.responsesUrl`；视频/3D 标 `CAP.probeNoAuto=true` |
| ⑥ | 优化模型说明 | ✅ 完成 | `ai-config.js`：五段式描述 + `modelDetails` 47 键全量 `stars/speed`；退役模型 `tag:"即将下线"` |
| ⑦ | 用量统计挪到「关于」页**顶部** + 排序优化 + **前后端真连通** | ✅ 完成 | `ai-settings.html` `#setUsageRoot` 已移到 `#setPanelAbout` **首子元素**；`xt-aiusage.js` 真 `fetch GET /api/ai/usage`；后端 `server/routers/ai.py` 提供 `GET /usage`（mtime 09-18 15:03:50） |
| ⑧ | **检测更新**真能用 | ⚠️ **代码完成，前置缺口 1 个** | `xt-update.js` 三态文案 + `reasonCode`；`web/static/apk/` **缺失目录已补**（原缺失正是更新检查失败主因）。**但 APK 未放进目录** → `/api/app/version` 返回 `apkReady=false` / `apkUrl=""`，前端只能显示「安装包暂时不可用」 |

---

## §3 本批已完成内容明细（五条并行工作线 T01–T05）

### 3.1 T01 — 模型库与能力注册（`ai-config.js` + 8 个 `ai-cap-*.js`）

**`assets/ai-config.js`**（`46895 B`，`09-18 16:27:09`，CRLF，备份 `assets/ai-config.js.bak-pre-r87-20260918` 40696 B）

node 真值求值实测结果：

```
builtinModels 数量 = 47        （本批 39 → 47，新增 8）
modelDetails  键数 = 47
按平台: ark 23 / zhipu 3 / qianfan 2 / openrouter 3 / gemini 2 / arkimage 2 / siliconflow 12
按能力: general 25 / math 1 / reasoning 5 / creative 3 / longtext 2 / video 5 / 3d 3 /
        image 3 / imagegen 3 / translate 1 / audio 5 / embedding 3 / rerank 1
即将下线(tag): ark-seedance-1-5-pro, ark-seedance-1-0-lite-t2v, ark-seedance-1-0-lite-i2v
```

- `FUNC_TYPES` 8 → **13 个能力槽**（新增 `audio` / `embedding` / `rerank` / `video` / `three_d`）。
- 新增 8 个视频/3D 模型（`provider: "ark"`，复用已有 ark Key，**无需新平台、无需新 Key**）。
- 3 个即将退役的视频模型标 `tag: "即将下线"`（**不是 `rate`**，见 §6.1）。

**8 个 `assets/ai-cap-*.js`**（全部 LF；`registry` / `image` / `vision` / `audio` / `embed` / `translate` / `video` / `3d`）

- 每个能力模块新增 `probe(ctx)`——健康检查按能力分派，而不是一律打文本对话探针。
- `ai-cap-video.js`（14861 B）与 `ai-cap-3d.js`（15769 B）：`CAP.probeNoAuto = true`（各 2 处命中）→ **不参与批量自动检测**，只允许人工单点触发（成本保护）。
- `ai-cap-vision.js`（8510 B）：`PROBE_PNG` (2) + `textFallback` (2)。
- `ai-cap-translate.js`（11357 B）：读 `provider.responsesUrl`（7 处）判断端点。

### 3.2 T02 — 健康检查内核（`assets/ai-service.js`）

`146973 B`，`09-18 16:36:57`，**LF**，备份 `assets/ai-service.js.bak-pre-r87-20260918`（140942 B）。

- `aiHealthCheckImpl` 改为**按能力分派**（`cap.probe` 优先，无则回落 kind 默认探针）。
- 替换 3 个 `XT_KIND_*` 常量，新增 `XT_KIND_BY_TYPE`（3 处）+ `model3d`（10 处）。
- 新增 `aiHealthCheckBatch`（7 处）——批量入口；遇 `CAP.probeNoAuto` 的模型返回 `skipped: true`。
- 新增错误码 `unsupported_probe`（7 处）——该能力未实现自动探测时**如实区分**，不再误报 `http_400`。

> **HTTP 400 根因（本批已修）**：旧 `aiHealthCheckImpl()` 对**所有**非 imagegen 模型都发文本对话探针；`audio` / `embedding` / `rerank` 这些没有专用探针分支的模型被拿去打硅基流动的 `chat/completions` → 一律 400。

### 3.3 T03 — 设置页（`assets/ai-settings.js` + `ai-settings.html`）

**`assets/ai-settings.js`**：`131393 B`，`09-18 16:38:29`，**LF**，备份 `assets/ai-settings.js.bak-pre-r87-20260918`（112287 B）。

12 类能力分类（`BUILTIN_CATS`，与 6 个展示族一一落位）：

| 族 | 族标签 | 分类 key → 标签 |
|---|---|---|
| `text` | 文本 | `general` 文本对话 / `longtext` 长文本理解 / `content` 内容创作 / `reasoning` 推理与数学 / `translate` 翻译 |
| `vision` | 视觉 | `vision` 视觉识图 / `imagegen` 图像生成 |
| `audio` | 语音 | `audio` 语音识别 |
| `retrieval` | 检索 | `embedding` 向量嵌入 / `rerank` 结果重排 |
| `video` | 视频 | `video` 视频生成 |
| `three_d` | 3D | `three_d` 3D 生成 |

> ⚠️ `vision` 的 **key 不能改名**（`ai-service.js` 硬编码依赖），本批只改 `label` 为「视觉识图」。

其他落点：`CAT_OF_TYPE`（14 键，含 `'3d' → 'three_d'`）、`migrateCatSchema`（5 处，**幂等**，以 `catSchema=2` 为闸，把用户旧 `catSchema=1` 设置无损升级）、`hideUnavailable`(13) / `lastSort`(9) / `soleCategoryOfModel`(2) / `usableIds`(7) / `visibleModels`(5) / `即将下线`(6) / `probeNoAuto`(2) / `aiHealthCheckBatch`(2)。

**`ai-settings.html`**：`42908 B`，`09-18 16:39:21`，**LF**，备份 `ai-settings.html.bak-pre-r87-20260918`（40916 B，**在仓库根目录**，不是 `assets/`）。

- `#setUsageRoot` 从原位置移到 `#setPanelAbout` **首子元素**（`setUsageRoot` 命中 2 处：容器 + JS 引用）。
- 排序弹窗：新增「按功能分类」下拉 + 「隐藏不可用模型」开关。
- 新增 3 个能力脚本引用（`ai-cap-translate.js` / `ai-cap-video.js` / `ai-cap-3d.js`，各 1 次）。

### 3.4 T04 — AI 页与用量统计（`assets/ai-page.js` + `assets/xt-aiusage.js`）

**`assets/ai-page.js`**：`131014 B`，`09-18 16:33:55`，**LF**，备份 129669 B。
`isHiddenByHealth`(2) / `hideUnavailableOn`(5) / 监听 `xt:health-changed`(1) / 无记录兜底放行。

**`assets/xt-aiusage.js`**：`58485 B`，`09-18 16:39:30`，**LF**，备份 37337 B。
- 真 `fetch GET /api/ai/usage`（2 处）+ `AbortController`(3) 超时 + 本地 `serverSnapshot` 双口径对照。
- **四态降级**：401 未登录 / 404 后端未上线 / 网络异常 / 坏 JSON——各自独立文案，**不假装成功**。
- `sortRows`(5) 三层排序；`clampPct`(3) 百分比封顶 100；`displayNameOf`(3) 展示名解析。
- 展示名优先级：**用户覆盖 > 内置名 > 原始 id**（与 `ai-settings.js` 的 `displayName()` 口径一致）。

### 3.5 T05 — 版本/更新/部署链路

| 文件 | 大小 | mtime | 行尾 | 说明 |
|---|---|---|---|---|
| `assets/xt-update.js` | 35977 B | 09-18 16:30:25 | LF | 三态文案（有新版本 / 已是最新 / 最新版）+ `reasonCode`(2)；`CURRENT_VERSION` 19 处引用 |
| `tools/bump_versions_safe.py` | 13577 B | 09-18 16:37:33 | CRLF | **重写**：从"只同步 3 个资产"扩展为**四处版本同步**（version.json + AndroidManifest.xml + xt-update.js `CURRENT_VERSION` + APK 文件名） |
| `tools/qa/check_version_consistency.py` | 6771 B | 09-18 16:37:33 | CRLF | **新建**：五值一致性检查，不一致则 exit≠0 |
| `tools/r87_stamp_apply.py` | 12670 B | 09-18 16:45:14 | LF | **新建**：本批刷戳（默认 dry-run，`--apply` 才写盘） |
| `tools/deploy_update_20260918r87.py` | 12644 B | 09-18 16:46:17 | LF | **新建**：部署（含三道闸 + tar 打包 + pscp/plink 上传） |
| `web/static/apk/.gitkeep` | 0 B | 09-18 16:31:14 | CRLF | **新建**：目录占位 |
| `web/static/apk/README.md` | 1266 B | 09-18 16:31:14 | LF | **新建**：说明 APK 应放 `星途-<版本>.apk` |

- 原 `bump_versions_safe.py` 已于 2026-09-11 被标记**弃用**（只处理 `api.js`/`polish.css`/`app.js`）→ 本批**重写复用**，不影响任何既有流程。
- **根 HTML cap 引用注入**：向 **37 个根 HTML**（48 个根 HTML 去掉 `blog_wechat.html` 及不加载 AI 的页面口径）注入 3 行 `ai-cap-*` 引用；`ai-cap-registry/image/vision/audio/embed/translate/video/3d` **各被 37 个根 HTML 引用**，`ai-cap-translate/video/3d` 从 0 → 36/37 引用。

---

## §4 已验证证据（真实命令 + 真实输出）

| 项 | 命令 / 工装 | 结果 |
|---|---|---|
| ES2017 真内核解析 | `node tools/qa/escheck_es2017.js …` | **`DONE 0`**（`tools/_r87_escheck.txt`、`_r87_escheck2.txt` 均为 7 B，内容 `DONE 0`） |
| 部署闸1 行尾 | `tools/deploy_update_20260918r87.py`（dry-run） | **47 个 HTML，违规 0** |
| 部署闸2 新增符号 | 同上 | **9 项，缺失 0** |
| 部署闸3 已删符号 | 同上 | **1 项，残留 0** |
| 打包 | 同上 | **62 个成员 / 573018 B → `tools/frontend_r87_20260918.tar.gz`** |
| 刷戳 dry-run 终态 | `tools/r87_stamp_apply.py`（dry-run） | **14 个目标资产全部 PASS**；`.js.js?v=` 命中 0；非属性命中 0；**行尾 loneLF 违规 0** |
| 符号残留 | Grep 计数 | `probeCostly` 在 `ai-cap-video.js` / `ai-cap-3d.js` / `ai-service.js` **均为 0** ✅ |
| 符号在位 | Grep 计数 | `probeNoAuto`（video/3d 各 2）、`aiHealthCheckBatch`(7)、`unsupported_probe`(7)、`skipped`(6) ✅ |
| 后端路由 | 解析 `server/routers/ai.py` | `GET /models`、**`GET /usage`**、`POST /usage/consume`、`POST /usage/reset`、`GET /history`、`POST /chat` |
| HTML 结构闸 | `tools/_r87_gatecheck.py` + `tools/_r87_struct_diff.py` | 5 页报不平衡 → 复核 **4 页为历史既有**（只 +3 行成对 script 引用）；`ai-settings.html` 的 `loneLF` 经历史备份比对**确认为既有 LF，非回归** |
| 部署工具 | 存在性探测 | `tools/pscp.exe`(1035400 B) ✅ / `tools/plink.exe`(1036448 B) ✅ |

---

## §5 未完成清单（按优先级，含可直接粘贴的命令）

> 统一前缀：`PY="C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe"`
> 工作目录：`cd "D:\下载的文件\学习工作台"`

### P0 —— 阻断上线

**1. 🔴 刷版本戳 `--apply`（当前只跑过 dry-run，磁盘上仍是旧戳）**

实测戳现状：8 个 `ai-cap-*.js` + `xt-aiusage.js` + `xt-update.js` **全裸无 `?v=`**；`ai-config.js` / `ai-service.js` 是 `?v=20260918b`；`ai-settings.js` / `ai-page.js` 是 `?v=20260918b`。
全站戳值分布：`20260916O×455 / 20260917b×141 / 20260918b×76 / 20260917×3 / 20260918a×1`。

```bash
"$PY" tools/r87_stamp_apply.py            # 再确认一次 dry-run（预期：14 资产 PASS）
"$PY" tools/r87_stamp_apply.py --apply    # 真正写盘，目标戳 20260918c
```

> 前置条件：T01–T05 **五条线已全部冻结** ✅（本批已确认）。`xt-aiusage.js` 的引用行在 `ai-settings.html`（T03 独占文件），T03 冻结后才可写。

**2. 🔴 版本号五处不一致（一致性检查当前 exit=1）**

`python tools/qa/check_version_consistency.py` 实测输出：

```
server/routers/version.json    version      1.24      versionCode 25
android/AndroidManifest.xml    versionName  1.23      versionCode 24
assets/xt-update.js            CURRENT_VERSION 1.23
[APK] 约定产物: web/static/apk/星途-1.24.apk  (当前存在: 否)
      ⚠ APK 未就位 —— /api/app/version 会返回 apkReady=false、apkUrl=""，前端只能显示「安装包暂时不可用」

[✗] versionName 不一致: 1.24 != 1.23
[✗] versionCode 不一致: 25 != 24
[✗] CURRENT_VERSION 不一致: 1.24 != 1.23
QA_EXIT=1
```

**处理口径（需先向用户确认，因为涉及"要不要现在发版"）**：

- **方案 A（推荐·随 APK 一起发版）**：用户本地重新打包 APK 时，先 `python tools/bump_versions_safe.py --apply` 把四处统一到 `1.24`，再打包 → APK 落到 `web/static/apk/星途-1.24.apk`。
- **方案 B（保留"待发布"态）**：`version.json / server/routers/version.json` 保持 `1.24`（服务端宣告的最新版），`AndroidManifest.xml` + `xt-update.js` 保持 `1.23`（当前在野版本）→ 这样**更新检查会真实报出「发现新版本 1.24」**，适合演示需求 ⑧。此时一致性检查的 exit=1 属**预期**，但必须在文档里写明"这是有意为之"，否则下一个 AI 会当成 bug 反复修。
- 无论哪种方案，**APK 必须真的落到 `web/static/apk/`**，否则需求 ⑧ 永远只能显示「安装包暂时不可用」。

**3. 🔴 部署上线（⚠️ 用户已授权由执行方操作并验证）**

```bash
"$PY" tools/deploy_update_20260918r87.py            # dry-run（已跑过一次，三闸全过）
"$PY" tools/deploy_update_20260918r87.py --apply    # 实际上传（pscp/plink 已在 tools/ 就位）
```

- 目标：`root@110.42.134.62` → `/opt/study-workbench/`（凭据长度 9，脚本内**不打印**）。
- 部署内容：**47 个根 HTML（排除 `blog_wechat.html`）+ 13 个 assets + `version.json` + apk 目录**。
- 部署后必做：① 远端文件数与 MD5 manifest 对照；② 抽查 `curl -s https://<域名>/ai-settings.html | grep 20260918c` 确认戳已生效；③ 确认 `/api/ai/usage` 返回 200。

### P1 —— 验收欠账

**4. 🟡 QA 回归（独立验证，尚未开始）**

- ⚠️ **派 QA 必须用 `subagent_type: "general-purpose"`**：`software-qa-engineer` 类型**没有 Bash**，拉起约 6 秒即挂（报 `Bash not found in agent software-qa-engineer`）；`name` 仍可写 `software-qa-engineer`。
- 验收重点（不要只重跑实现方跑过的东西）：
  - 需求 ① 分类页 12 类 × 6 族渲染正确、空族不渲染、`catSchema=1 → 2` 迁移幂等（跑两次结果一致）；
  - 需求 ③④ 隐藏/恢复闭环：造一个 `recordHealth(id, 'http_400')` → 该模型在 AI 页消失；改成成功 → 自动回来；`probeNoAuto` 模型（视频/3D）**不被**批量隐藏；
  - 需求 ⑤ 各能力探针：`audio/embedding/rerank` 不再落到 `chat/completions`（不再 400）；视频/3D **不参与**批量自动检测（成本保护）；
  - 需求 ⑦ 用量页四态降级：模拟 401 / 404 / 网络中断 / 返回非 JSON，四者文案必须不同且不谎报成功；
  - 需求 ⑧ 更新检查三态 + `reasonCode`，APK 缺失时文案为「安装包暂时不可用」而非崩溃。

**5. 🟡 git 提交 + push**

- 本机 `git status` 有 **941 条**变更（大量历史 M 漂移 + 881 条未跟踪），**判断本批改动用 mtime/备份对照，不要按 git status 全收**。
- 两条历史红线（沿用）：① 先 `git add 我的动态.html`（未跟踪，漏加会远端死链）；② `git rm --cached 学习工作台-安卓App.apk`（96 MB 二进制已被跟踪，勿推远端）。
- push 走代理：`git -c http.proxy=<P> -c https.proxy=<P> -c http.version=HTTP/1.1 push origin main`，`P` 先探测（第一优先环境变量，备选 7897）。

### P2 —— 小尾巴

**6. ⚪ 历史遗留（本批未动，非本批引入）**

- `assets/api.js:354-355` 空壳 `if`（两分支内容相同）。
- `工具.html` 也有 `.morepage-list`，口径未裁定。
- `用户需求与决策总账.md`（CRLF，111231 B）需补记 R86 / R87。
- 用户侧：`ai.hub.camera` 端点已死需更换；`ADMIN_PASSWORD` 与各平台 AI Key 待轮换。

**7. ⚪ 备份位置提醒**

`assets/ai-settings.html.bak-pre-r87-20260918` **不存在于 `assets/`**——正确位置在**仓库根目录** `ai-settings.html.bak-pre-r87-20260918`（40916 B）。做存在性检查时别用 `assets/` 前缀。

---

## §6 本批关键决策与纠错（血泪清单，别重犯）

1. **`rate` 是「倍率」不是「标签」** —— 渲染出来是 `… 倍率`。退役标记必须写 `tag: "即将下线"`。本批曾错用 `rate:'退役中'`，会渲染成「退役中 倍率」。
2. **`ai-settings.js` 模型角标读的是 `m.custom` 而不是 `m.tag`** → `tag:"即将下线"` **不会**自动出现在模型简介浮层；退役语义改由设置页处理（角标 + 排序末尾 + 禁止设为默认）。
3. **命名冲突**：T01 自造 `probeCostly`，与架构设计的 `probeNoAuto` 撞概念 → 已统一为 `probeNoAuto`，Grep 残留 **0**。
4. **链式风险（差点废掉需求 ④）**：批量跳过时若写入 `unsupported_probe`，会被 `recordHealth` 当成"失败"落库 → 视频/3D 被**自动隐藏**，与需求 ④ 相反。修法：T02 返回 `skipped: true`，T03 的 `recordHealth` 遇 `skipped` / `unsupported_probe` **不落库**。
5. **批量入口错位**：`batchHealthCheck` 原本遍历**全部**模型而不是 `pendingHealthIds` → T03 在 `batchHealthCheck` **和** `pendingHealthIds` 两处都过滤 `isProbeNoAutoId`（双保险）。
6. **视觉探针尺寸**：1×1 PNG 会被服务端以尺寸不合规拒（伪 400）→ 改 **64×64 PNG** + HTTP-400 时走文本回退；3D 用 **512×512 PNG**。
7. **translate 探针端点**：读 `provider.responsesUrl`，缺失则回落 `chat`（硅基流动没有 `responsesUrl`）；保留 `/responses` 分支（ark 需要）。
8. **行尾矩阵纠错**：旧文档称 `ai-cap-*.js` 是 CRLF → **实为 LF**（8 个全部 CRLF=0）；`ai-config.js` 是 CRLF。已写入设计文档 §9.2。
9. **用量降级不撒谎**：401 / 404 / 网络 / 坏 JSON 四态各自文案；百分比 `clampPct` 封顶 100；后端口径与前端口径**双列对照**而不是覆盖。
10. **展示名优先级**：用户覆盖 > 内置名 > 原始 id。示例：真实内置名是 **`Seedream-4.0-Fast`**，不是 `Doubao-Seedream-4.0`（主理人曾给错示例，被 T04 纠正）。
11. **废弃脚本复用**：`tools/bump_versions_safe.py` 早已弃用（只处理 3 个资产）→ 本批**重写**为四处版本同步，不破坏既有流程。
12. **行尾回归误报已澄清**：结构闸报 `ai-settings.html loneLF=808` 看着像"CRLF→LF 回归"，但 `ai-settings.html.bak-pre-r72-20260917` 同样是 LF（`CRLF=0 lone=596`）→ 该文件**本来就是 LF**，**无需修**。判断行尾回归必须**与历史备份比对**，不能只看主观的"应该是什么"。

---

## §7 关键契约（跨文件约定，改动前必看）

```
# 能力注册
XT_AI_CAPS.register(cap)                     在 assets/ai-cap-*.js 内
  cap = { kind, defaultUrl, probe?(ctx), probeNoAuto?: true, ... }
ai-service.js 按 cap.probe 优先 / 否则按 kind 分派

# 能力 key（不可改名）
'image' | 'vision' | 'audio' | 'embed' | 'translate' | 'video' | '3d'
CAT_OF_TYPE 映射：'3d' -> 'three_d'（设置页分类 key）
注意：分类 key 'vision' 被 ai-service.js 硬编码依赖，只能改 label

# 健康检查
aiHealthCheckImpl(id)       单模型
aiHealthCheckBatch(ids)     批量；probeNoAuto 的 id 返回 { skipped: true, code: 'unsupported_probe' }
错误码：network | cors | timeout | empty | truncated | unsupported_probe
recordHealth 落库规则：skipped / unsupported_probe 一律不落库

# 事件总线
'ai-settings.js' 派发  xt:health-changed
'ai-page.js'     监听  xt:health-changed   → 重算 isHiddenByHealth 并重绘

# 用量
GET  /api/ai/usage                    服务端账本快照（server/routers/ai.py）
POST /api/ai/usage/consume            前端直连能力（生图/视频/3D）上报消费
前端失败降级：401 未登录 / 404 后端未上线 / 网络异常 / 坏 JSON（四态独立文案）

# 版本号四处（tools/bump_versions_safe.py 同步）
version.json (根) / server/routers/version.json / android/AndroidManifest.xml
/ assets/xt-update.js CURRENT_VERSION / APK 文件名 web/static/apk/星途-<ver>.apk
```

---

## §8 本批文件清单

### 8.1 前端资产（13 个，全部已落盘）

| 文件 | 大小 | mtime | 行尾 |
|---|---|---|---|
| `assets/ai-config.js` | 46895 | 16:27:09 | CRLF |
| `assets/ai-service.js` | 146973 | 16:36:57 | LF |
| `assets/ai-settings.js` | 131393 | 16:38:29 | LF |
| `assets/ai-page.js` | 131014 | 16:33:55 | LF |
| `assets/ai-cap-registry.js` | 4343 | 14:23:51 | LF |
| `assets/ai-cap-image.js` | 3822 | 16:27:09 | LF |
| `assets/ai-cap-vision.js` | 8510 | 16:30:40 | LF |
| `assets/ai-cap-audio.js` | 13050 | 16:27:09 | LF |
| `assets/ai-cap-embed.js` | 9513 | 16:27:09 | LF |
| `assets/ai-cap-translate.js` | 11357 | 16:27:09 | LF |
| `assets/ai-cap-video.js` | 14861 | 16:30:40 | LF |
| `assets/ai-cap-3d.js` | 15769 | 16:30:40 | LF |
| `assets/xt-aiusage.js` | 58485 | 16:39:30 | LF |
| `assets/xt-update.js` | 35977 | 16:30:25 | LF |

### 8.2 页面（1 个重点 + 37 个被注入引用）

- `ai-settings.html`（42908 B，16:39:21，LF）——用量区移动 + 排序弹窗 + 3 个 cap 引用。
- **37 个根 HTML** 已注入 8 个 `ai-cap-*.js` 引用：
  `AI.html`、`AI模拟面试.html`、`PPT案例拆解.html`、`PPT版式库.html`、`ai-settings.html`、`blog_wechat.html`、`万能金句库.html`、`个人中心.html`、`企业定向库.html`、`关于.html`、`动态空间.html`、`商务礼仪.html`、`四级词汇.html`、`场景话术库.html`、`学习工作台.html`、`学习概括.html`、`导入题库.html`、`工具.html`、`我的动态.html`、`我的文件.html`、`时政热点.html`、`更多.html`、`朋友圈发布.html`、`演示.html`、`申论刷题.html`、`社区.html`、`私聊.html`、`管理员.html`、`英语.html`、`行测.html`、`行测刷题.html`、`表达.html`、`设置.html`、`赞助.html`、`错题本.html`、`面测.html`、`面试题库.html`
- ⚠️ 部署时**排除** `blog_wechat.html`（部署脚本已内置排除）。

### 8.3 后端（本批确认，未新增大改动）

| 文件 | 大小 | mtime |
|---|---|---|
| `server/routers/ai.py` | 14348 | 09-18 15:03:50 |
| `server/schemas.py` | 13401 | 09-18 15:03:50 |
| `server/quota_ledger.py` | 14100 | 09-18 15:18:13 |
| `server/routers/version.json` | 4373 | 09-18 15:33:07 |
| `server/data/model_registry.json` | 4510 | 09-18 15:25:57 |
| `server/data/model_quota.json` | 3811 | 09-18 15:26:27 |
| `server/data/model_usage.json` | 56 | 09-18 15:29:14 |

### 8.4 工具（本批新增/重写）

`tools/r87_stamp_apply.py`(12670) / `tools/deploy_update_20260918r87.py`(12644) / `tools/bump_versions_safe.py`(13577, 重写) / `tools/qa/check_version_consistency.py`(6771, 新建) / `web/static/apk/.gitkeep` + `web/static/apk/README.md`

### 8.5 交付文档（本批产出）

- PRD：`deliverables/software-company/ai-models-incremental-PRD-20260918.md`
- 设计：`deliverables/software-company/ai-models-incremental-design-20260918.md`
- 本文件 + `更新日志-20260918-第六批-R87.md` + `给接手AI的开场指令-20260918h.txt`

### 8.6 备份（**禁止清理**）

| 备份 | 大小 | 位置 |
|---|---|---|
| `assets/ai-config.js.bak-pre-r87-20260918` | 40696 | assets/ |
| `assets/ai-service.js.bak-pre-r87-20260918` | 140942 | assets/ |
| `assets/ai-settings.js.bak-pre-r87-20260918` | 112287 | assets/ |
| `assets/ai-page.js.bak-pre-r87-20260918` | 129669 | assets/ |
| `assets/xt-aiusage.js.bak-pre-r87-20260918` | 37337 | assets/ |
| `assets/xt-update.js.bak-pre-r87-20260918` | 33539 | assets/ |
| `assets/ai-cap-3d.js.bak-pre-r87-20260918b` | 13719 | assets/ |
| `assets/ai-cap-video.js.bak-pre-r87-20260918b` | 14849 | assets/ |
| `assets/ai-cap-vision.js.bak-pre-r87-20260918b` | 7615 | assets/ |
| `ai-settings.html.bak-pre-r87-20260918` | 40916 | **仓库根目录** |
| `tools/bump_versions_safe.py.bak-pre-r87-20260918` | 4006 | tools/ |

---

## §9 禁止事项

- ⛔ 不要把「开发自测通过」写成「已验证通过」；**QA 独立回归尚未开始**（见 §5.4）。
- ⛔ 不要改 `C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-*`。
- ⛔ 不要顺手刷新**未改动**资源的版本戳（项目审计靠 mtime，无谓改动会被误判为本批内容）。
- ⛔ 不要清理任何 `.bak-pre-*`。
- ⛔ 不要按 `git status` 全量提交（941 条里有大量历史漂移 + 881 条未跟踪垃圾）。
- ⛔ 不要用 `node --check` 代替 ES2017 检查（它抓不到）。
- ⛔ 不要删 `blog_wechat.html`（部署排除 ≠ 删除）。
- ⛔ 不要把平台密钥说成"已保密"——各平台 Key 明文在 `assets/ai-config.js`，前端直连架构下无法真保密；已交付的只是「UI 不回填 + 不复制进 localStorage + 清洗存量副本」。
