# 星途（学习工作台）· 剩余需求与任务分配方案（第三批续作）

> 成文：2026-09-16 14:35 ｜ 主理人：齐活林（Qi）· 交付总监
> 唯一可写代码树：`D:\下载的文件\学习工作台` ⛔ 禁碰 `C:\Users\ATM\WorkBuddy\Worktrees\...`
> 本文为「开工前作战图」，**未改任何业务文件**，等用户点头即开波。

---

## 0. 接手核实结果（本次实测，非文档转述）

| 项 | 实测值 | 与文档对比 |
|---|---|---|
| 根 HTML 数 | **39** | 一致 |
| 顶层条目 | 407（含 68 个 `_` 临时文件） | — |
| `escheck_es2017.js` | **`DONE 0`** | ✅ 一致（无解析级破损） |
| `global_name_scan` | **FATAL 0 / RISK 4 / WARN 46**（bare 922 / guarded 452） | ✅ 与文档基线一致，**无新增** |
| `crossline_conflict` | **`CONFLICTS=0`** | ✅ 13 线文件零重叠 |
| `wave_check`（12 个半成品） | **110 项 / FAIL 4** — 全部为已知/已归类项 | 见 §1.1 |
| 全站版本戳 | **567 处，100% `20260916L`** | ✅ 一致 |
| `live_chat.html` | **不存在** | ⚠️ 文档说它「仍是 20260911c」→ 实测该文件**已不在树里**（应为早前已删/改名），bump 时**无需再管它** |

### 0.1 🔧 本次顺手修掉的一个工具 Bug（重要）
`tools/qa/wave_check_20260916m.py` **本身有 Bug**：`L325` 用 4 个参数调用 `snippet()`，而 `L226` 定义只收 3 个 → 一旦真命中禁用语法就 **TypeError 崩栈**（即「工具在最该报警的时候挂了」）。
**已修**：`snippet(text, idx, width=90, line_offset=0)` 补上第 4 参并正确叠加行号偏移。修复后工具跑通（110 项）。

---

## 1. 四类剩余工作（按「能不能立刻做」重排）

### 1.1 🔴 A 类 · 已落盘待收尾（**本批最值钱，文件互不重叠，可全并行**）

| 线 | 任务 | 独占文件 | 实测现状（本次核验） | 还差什么 |
|---|---|---|---|---|
| **A1** | 需求 D · AI 400 修复复核 | `assets/ai-service.js`（1080 行 LF） | **六项要求基本已做**：L118-182 已按**魔数**推导 MIME（`iVBO`/`/9j/`/`R0lGOD`/`UklGR`/`Qk0` + 字节级 `0xFFD8`/`0x4E47`/`RIFF..WEBP`/`GIF8`/`BM`）；`NO_FALLBACK_STATUS=[400,403,404,422]`；`TIMEOUT_*_TOKEN=15000`×2；`rate429` 会话内跳过；`L548`/`L694` 两处 `getReader` 均有 `L518` 守卫 | ⚠️ **`data:image/jpeg;base64,` 2 处**需逐处判定是「魔数兜底的默认值」还是「残留硬编码」（L182 `if (!real) real = ... : "image/jpeg"` 是**合法兜底**）。**需只读复核 + 出交付报告**，大概率零改动 |
| **A2** | 需求 C · AI 页输入框 + 模型按钮 | `AI.html`（CRLF）+ `assets/ai-page.js`（LF） | `AI.html` `closeSidebar`=**0**；`max-width:none`×1 仍在；`z-index:330` ✓。但 `ai-page.js` 里 `closeSidebar`=**3**、`toggleSidebar`=**3** | 补 `AI.html:491` 窄屏覆盖；确认「先关抽屉+关遮罩再开面板」链路已通；**375/1280 双端截图未出** |
| **A3** | N9-11 阅读理解布局 | `assets/cet-read.js` | `cr-grid`=7，语法干净 | 只差**三题型切换实测**（右栏无空卡片） |
| **A4** | N9-19 模拟面试重构 | `AI模拟面试.html` + `assets/iv-prep.js` | `stage`=13 / `STAGES` 类=13，+583/-101 行 | 补**六阶段状态机交互文档** + 六阶段走通实测 |
| **A5** | N9-14 PPT 训练（部分） | `assets/ppt-tips.js` | 60,259 B，有实质改动 | 确认 **P0 五件完成度**；`演示.html`/`data-ppt-tips.js` 未动（转 B5） |
| **A6** | C18 错题本 AI 分析 | `错题本.html` | `callAI`=**4**，结构全平衡 | 降级不白屏 + 空错题本不报错 实测 |
| **A7** | N9-4 个人中心作品集 + B1 | `个人中心.html` | +847/-31 行；结构配对全 PASS（`<head>` FAIL 是**历史遗留**，非破损） | 收藏持久化实测；确认未越界改 app.js/api.js |
| **A8** | N9-22A 学习概括页 + N9-3 notify.js | `学习概括.html`（LF）+ `assets/notify.js`（LF） | **`notify.js` 顶层声明 = 0 ✓**（`lookbehind` 2 处命中是**注释里的铁律说明文字**，**误报**）；`学习概括.html` 结构全平衡 | 换行符统一（新建页用了 LF，需与全站 CRLF 规矩对齐）＋共用 DOM 七件核验；**入口接线 + 30+ 页注入未做** |
| **A9** | N9-5 工具页 + P3 注释 | `工具.html` + `assets/common.css` | ✅ 文档标「已验收」，本次复查结构 PASS | 已可采信，无需再动 |

> **A 类结论**：8 项待收尾（A1~A8），文件两两零重叠 → **可一次性全并行**。

### 1.2 🟠 B 类 · 零产出（原封未动，从头做）

| 线 | 任务 | 目标文件（mtime 实测） | 特殊约束 |
|---|---|---|---|
| **B1** | N9-18 听力精听全面改版（含 N9-10） | `assets/voiceplayer.js`（51,030 B，09-15 22:13）、`英语.html`（09-16 12:18） | 🔴 **禁改 `vp-back` 语义**（W4 页头基准）、**禁改 `#vpMask` id**、**禁改 `SCENES` 结构签名**（SCENES=19 处引用） |
| **B2** | N9-12 PPT 版式库升级 | `PPT版式库.html`（09-16 12:18）、`assets/data-ppt-templates.js`（09-14）、`assets/tpl-preview.js`（09-16 12:18） | 🔴 **`assets/images/ppt-tpl` 目录不存在** → 占位图先上 |
| **B3** | C12 申论批改（接 AI 底座） | `申论刷题.html`（`callAI`=**0** ✓零产出） | 底座口径：`window.callAI(funcType, messages, opts)` **前端直连**（**不是** `/api/ai/chat`）；公共 Key 10 次/分 → **禁自动发问** |
| **B4** | N9-15 我的文章布局 | `blog_wechat.html`（09-16 12:18）、`assets/common.css` ★ | ⚠️ **开工第一步必解 F3**：`renderBlogStats`/`renderBlogMine`/`openBlogStats` 在 app.js 与 api.js **各有副本**（本次实测：前两个 app=1/api=1；`openBlogStats` app=1/api=0）→ 加载序 app.js→api.js，**api.js 覆盖** → 先判生效副本再动刀 |
| **B5** | N9-14 剩余部分 | `演示.html`（09-16 12:43）、`assets/data-ppt-tips.js`（09-15 22:17） | 🔴 `assets/images/ppt-tips` 目录不存在 → 占位图先上 |
| **B6** | C8 前置 · `fetchAiReply` 委托 `callAI` | `assets/app.js` ★ | 实测 `L3739 async function fetchAiReply` 仍 **`fetch(APP_AI_DEMO_CONFIG.apiUrl)`**（L3741、L7908 各一处直连）→ 必须改为内部委托 `window.callAI`（**保留函数签名**）。**这是 C 组 13 项的前置** |

### 1.3 🟡 C 类 · 批次九 Wave 3~7 未开工（10 项 + 收尾）

| 波 | 任务 | 独占文件 | 备注 |
|---|---|---|---|
| **W3** | N9-1+N9-2 题库筛选 | `app.js` ★ + `行测刷题.html` | 缺陷复现型：**先复现再定是改数据还是改逻辑** |
| | N9-8 筛选标签交互 | `行测刷题.html` | 与 N9-1/2 跨波串行 |
| **W4** | N9-24 金句库升级 | `app.js` ★ + `万能金句库.html` + `表达.html` | **XHR 单路径，禁 fetch** |
| | N9-21 写发帖 | `社区.html` + `blog_wechat.html` + `assets/api.js` ★ | 先解 F3 |
| **W5** | N9-13 PPT 案例拆解 | `app.js` ★ + 新建 `data-ppt-cases.js` | 🔴 截图未到位先占位 |
| **W6** | 🔴 **最高风险 · 必须一次原子批量** | 36 个根 HTML | 三变换合一：① 页头返回(33 页) ② 删「学习统计」(28 页) ③ 注入 `notify.js`(30+ 页)。豁免：`mock_exam_run.html`、`登录.html` |
| **W7** | N9-7 `app.js` 拆分 | `app.js` ★ | **最后做**；硬约束：全局符号名不变、`INLINE_*_BANK` 仍在 `examInit` 之前、33 页 script 标签同步 |

> **`app.js` 接力链（严格串行，关键路径）**：~~L1 ✅~~ → **B6(C8)** → N9-1+2 → N9-24 → N9-13 → N9-22B → C20 → N9-7
> **`api.js` 接力链**：~~B2 资料卡重构~~ → N9-21
> **`common.css` 接力链**：~~L4 ✅~~ → **B4(N9-15)**

### 1.4 🟢 D 类 · 更早遗留

| ID | 任务 | 文件 | 状态 |
|---|---|---|---|
| B1 | feed-btn 挪进资料卡操作区 | `个人中心.html` | 并入 A7 |
| B2 | 个人资料卡布局重构 | `assets/api.js` `_renderProfileOnline` | 与 N9-21 同线 |
| D-1 | 数量关系题库回滚重做 | `app.js` + 题库数据 | 🔴 **P0 遗留，范围待用户拍板** |
| D-2 | `common.css` L68 滚动锁 | — | ✅ **已决策冻结**（L68 实测仍在，按决策不动） |
| D-3 | 「工具页导入跑到博客页」悬案 | — | ⏸ **等用户补截图** |
| D-5 | 文件清理（68 个 `_*` + 2 目录 + PDF） | — | **全批次最后一步** |
| C 组 | 全站 AI 升级 C-P1~P3（13 项） | 多页 | **前置 = B6(C8)**；准入 = 决策 D8 三条件真机过一遍 |

---

## 2. 本次开工建议（Wave 0-Plus · 14 线并行）

### 2.1 波次划分

| 波 | 线 | 文件独占 | 说明 |
|---|---|---|---|
| **Wave 0** | **A1~A8**（8 线） | 8 组互斥文件 | 「收尾 + 出报告」，**零风险，先拿回可交付成果** |
| **Wave 1** | **B1~B5**（5 线） | 5 组互斥文件 | 零产出重做，与 Wave 0 完全并行（文件零重叠） |
| **Wave 2** | **B6(C8)** | `app.js` ★ 独占 | 必须在 app.js 接力链首位，**Wave 1 结束后立即开**（不与其它线并行写 app.js） |
| **Wave 3** | W4 原子批量 + 波末 bump + 部署 | 36 HTML | **需用户点头**（生产操作） |

### 2.2 能否一次性全并行？**能**
- **Wave 0 的 8 组 + Wave 1 的 5 组 = 13 组文件集合两两交集为空**（已用 `crossline_conflict_20260916m.py` 实测 `CONFLICTS=0` 佐证）。
- 共享文件处置：`app.js` 归 B6 独占；`api.js` 归 B4 独占（N9-15 与 N9-21 需串行，本波只放 N9-15）；`common.css` 归 B4 独占。
- **唯一冲突点**：`B4` 同时要 `blog_wechat.html`+`common.css`；`B-类` 无其他线碰这两个 → 安全。

### 2.3 spawn 纪律（血泪执行）
| 角色 | `name`（Agent ID） | `subagent_type` |
|---|---|---|
| 工程师 | `software-engineer-任务N` | `software-engineer`（**有 Bash** ✅） |
| QA | `software-qa-engineer` | **`general-purpose`**（`software-qa-engineer` **无 Bash，6 秒即挂** ❌） |

- **429 未完全解除**（配额 2026-09-17 13:32 重置）→ **全部 spawn 时指定 `model: "lite"`** 保底可跑。
- 每条派活指令必须带**固定前言**：唯一可写树 / ES2017 禁用语法清单 / 全局声明守卫写法 / 禁原生弹窗 / 波内不 bump / 换行符保持 / bash 无 coreutils 绕法 / python+node 绝对路径 / `QA_PORT` 坑 / **命令各跑一次即判定**。
- 子代理看不见本对话 → **每条指令自带完整上下文**，要求完成后 `SendMessage` 回报。

---

## 3. 需要用户拍板（4 件）

1. **开工范围**：是否按本方案 **Wave 0（8 线收尾）+ Wave 1（5 线零产出）共 13 线一次性全开**？
   （备选：先只开 Wave 0，确认报告质量后再上 Wave 1）
2. **D-1 数量关系题库回滚重做**：回滚到哪个版本？重做范围多大？（**不自行决定**）
3. **部署策略**：Wave 0+1 完成即 bump `20260916L`→`20260916M` 并部署，还是攒到 W4 原子批量后一起上？
4. **D-3 悬案**：能否补一张「工具页导入题库跑到博客页」的截图？（否则此项继续挂起）

---

## 4. 铁律速查（派活必带）

1. **唯一可写树** = `D:\下载的文件\学习工作台`；⛔ 禁碰 `C:\Users\ATM\WorkBuddy\Worktrees\...`
2. **ES2017 上限**（老 WebView Chrome 50~58），禁：`?.` `??` `.replaceAll(` `Object.fromEntries` `.at(` 顶层 `await` `(?<=` `(?<!` `{...obj}` `{a,...rest}` `**` `catch {}`。**`node --check` 抓不到，必须 Grep 确认 0 命中**。
   - 两个**已验证误报**别改：`**` 在引号内是 markdown 字符串；`...` 在 `[` 之后是合法数组展开。
3. **顶层声明冲突** = 整文件 SyntaxError 静默失效 → 新增全局一律 `if (typeof window.x !== 'function') window.x = function () {...};`
4. **禁原生 `alert/confirm/prompt`** → 用 `window.xtToast` 或页内区块。
5. **波内不 bump**；改 `assets/*.js` 或 CSS 后由**波末单点**统一 bump。
6. **共享文件同一时刻只允许一条线改**：`app.js`(33 页) / `api.js`(30 页) / `common.css`(全站)。
7. **换行符必须保持**：根 HTML/`app.js`/`api.js`/`common.css` = **CRLF**；`ai-service.js`/`ai-page.js`/`ppt-works.js`/`notify.js` = **LF**。
8. **判「哪些文件被动过」只能用 mtime**（本批开工时刻 **2026-09-16 13:30**），**不要用 git**（HEAD 很旧，极具误导）。
9. **改前先备份**（命名含 `bak`）；文件重命名用 `os.rename`，禁 `git mv`。
10. **不删现有功能、保留既有 DOM id 与全局函数名**。
11. **生产操作先问用户**：删数据 / 改 nginx / 部署上线。
12. **命令/脚本各跑一次即判定**，不反复回跑、不多轮回读自审。

---

*主理人齐活林 · 2026-09-16 14:35。本文数据全部来自本次对 `D:\下载的文件\学习工作台` 的实测（escheck / global_name_scan / crossline_conflict / wave_check / 签名正则探针 / mtime 采样），非文档转述。*
