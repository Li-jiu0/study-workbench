# 星途（学习工作台）· 交接文档（交接给下一个 AI）· 第三批续作版

> 生成时间：2026-09-16 17:50
> **本文取代 `交接文档-20260916d-第三批执行现状与对接.md`**（那份写于 14:25，当时线上是 `20260916k`、本批未部署；**现已全部过期**）
> **本文目标：让你在不读任何历史对话的前提下，安全接手并完成剩余工作，且不把站点改白屏**
> 配套阅读：`更新日志-20260916-第三批续作.md`（本批实际落盘 + 两次推送 + 我的纠错记录）

---

## 0. 一句话现状

**四项 UI 需求（企业定向库删标签、金句库/商务礼仪收放、听说训练两级收放）已全部实现并上线生产；线上版本戳 = `20260916O`，全站 569 处统一、无杂质戳；39 个 HTML 行尾已修复为干净 CRLF；全站 `escheck` = `DONE 0`。** 仍纯记录未执行的需求**只剩 1 条**（「导入题库」入口 —— 口径已于 2026-09-16 18:04 补全，登记为 **R62**，见 §4.1）。

🔴 **与旧文档最大的差异（务必先纠正认知）**：

| 项 | 旧文档（`d`，14:25 写） | **现在（实测）** |
|---|---|---|
| 线上版本戳 | `20260916k` | **`20260916O`** |
| 本批改动 | **未部署** | **已全部部署** |
| 行尾 | 未提及（当时正常） | 曾被污染为 LF，**已修复回 CRLF** |
| 部署脚本模板 | `deploy_update_20260916k.py` | **`deploy_update_20260916o.py`**（含 3 道安全闸，优先照抄它） |

---

## 1. 🔴 五条最关键的警告（先看这个）

### 1.1 唯一可写代码树是 `D:\下载的文件\学习工作台`，绝对不要碰 C 盘
`C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-*` 是**同一仓库的废弃副本**（无完整 `.git`、缺文件）。**改这里等于全部白干。** 所有读写一律用 `D:\` 绝对路径。
> 例外：主理人可把**一次性探针脚本**写在 worktree 里（不污染交付树），但**交付物永远在 `D:\`**。

### 1.2 判断「哪些文件被动过」只能用 mtime，不能用 git
本仓库 git HEAD 很旧（很多文件是 `?? 未跟踪`），`git status` 会列出几十个**更早批次**改的文件，极具误导性。
**本批（续作）施工时段 = 2026-09-16 14:24 ~ 17:46。** 判据见 `更新日志-20260916-第三批续作.md` §二 的 mtime 表。

### 1.3 🔴 行尾是红线：改文件必须二进制读写（本批出过生产事故）
**规矩**：
- **CRLF**：39 个根 HTML、`app.js`、`common.css`、`company-lib.js`、`voiceplayer.js`、`ai-config.js`
- **LF**：`ai-page.js`、`ai-service.js`

**事故**：部署脚本用 `io.open(p,'w',encoding='utf-8',newline='')` + 默认读模式 → 读时 `\r\n` 归一化为 `\n`、写时不补回 → **39 个 HTML 每行少 1 字节，全站 CRLF 变 LF，并推上了生产**。

**唯一正确写法**：
```python
with open(p, 'rb') as f:
    raw = f.read()
raw2, n = re.subn(rb'\?v=[0-9A-Za-z_\-\.]+', b'?v=' + STAMP.encode('ascii'), raw)
if n:
    with open(p, 'wb') as f:
        f.write(raw2)
```
**任何 bump 脚本都必须内置行尾自检**（`count(b'\n') - count(b'\r\n') == 0`，非 0 立即中止）。
**改完自测**：`CRLF=N / loneLF=0`。

### 1.4 部署脚本必须带 3 道安全闸（缺一道 = 出错会静默上生产）
照抄 `tools/deploy_update_20260916o.py`：

| 闸 | 检查 | 不过 |
|---|---|---|
| **行尾闸** | bump 后逐 HTML 验 `loneLF == 0` | `exit(3)` |
| **新增符号闸** | 从 tar 内**读文件内容**验新符号存在 | `exit(5)` |
| **已删符号闸** | 验应删符号已归零 | `exit(6)` |

### 1.5 版本戳**不要复用已服务过的值**
`M` → `N` → `O` 每次都是新字母。理由：浏览器按 `?v=` 缓存静态资源，**复用同值会让已缓存客户端不再拉新** → 新功能拿不到。
推送前先探生产当前戳：`plink ... "cd /opt/study-workbench/web && grep -oh '?v=[0-9A-Za-z_-]*' *.html | sort | uniq -c"`

---

## 2. 项目坐标与环境

| 项 | 值 |
|---|---|
| **唯一可写代码树** | `D:\下载的文件\学习工作台` |
| ⛔ **禁改** | `C:\Users\ATM\WorkBuddy\Worktrees\...`（废弃副本） |
| 线上静态站 | `http://110.42.134.62/`（nginx，远端根 `/opt/study-workbench/web`）——**当前 `20260916O`** |
| 线上后端 | `http://110.42.134.62:8000`（FastAPI / uvicorn） |
| **部署脚本模板（首选）** | **`tools/deploy_update_20260916o.py`**（含 3 道安全闸） |
| 部署工具 | `tools/plink.exe`、`tools/pscp.exe`（hostkey 已固定：`SHA256:i2DNTi3iEPauuTxvJEPlZ4jLf8K0LdDsaijQxArUU1M`） |
| 部署凭据 | 脚本自动从 `upload_v23.ps1` 正则解析 → **不要打印或外传** |
| node | `C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe` |
| python | `C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe` |
| jsdom | `C:/Users/ATM/node_modules/jsdom`（需 `NODE_PATH="C:/Users/ATM/node_modules"`） |
| 根 HTML 数 | **39 个**（本地）／**47 个**（生产，多 8 个历史遗留旧页） |

### 2.1 技术形态
- 前端：**多页静态 HTML + `assets/*.js` 原生 JS**。**无框架、无 CDN、无打包、无构建步骤**。改完直接传文件。
- 后端：FastAPI + SQLite（`server/`），**本地 `server/` 是旧副本，不要以它为准**。
- 运行环境：**安卓 APK 里的老 WebView，内核 ≈ Chrome 50~58（只支持到 ES2017）**，同时也要在桌面浏览器正常运行。

### 2.2 429 配额
- 配额打停时提示 **`2026-09-17 13:32:39 UTC+8` 重置**。
- **耗尽期间**：spawn 子代理时指定 **`model: "lite"` 可立刻继续**。
- ⚠️ **UI 右上角切模型对子代理无效** —— 子代理 model 只能在 `Agent` 调用时指定、中途不可切换。

### 2.3 ⚠️ 子代理类型与 Bash 可用性（2026-09-16 实测，别一刀切）
| `subagent_type` | Bash | 结论 |
|---|---|---|
| `software-engineer` | ✅ **有** | 工程师线用它（能跑 `node --check` / escheck / python） |
| `software-qa-engineer` | ❌ **没有** | 拉起后 **6 秒即挂**，报 `Bash not found in agent software-qa-engineer` |
| `general-purpose` | ✅ 有 | **QA 线必须用它**（`name` 仍可写 `software-qa-engineer`） |

### 2.4 环境坑（会让你白浪费大量时间）
1. **bash 缺 coreutils**：`ls` / `grep` / `head` / `tail` / `wc` / `dirname` / `env` / `rm` / `cat` **全部不存在** → 一律**用 Write 工具落盘 `.py`/`.js` 再执行**；**绝不对 bash 内联含中文/反引号/CSS 花括号的代码**（bash 会做命令替换与花括号解析）。查文件用 Read/Grep/Glob。
2. **绝不用** `node xxx | tail`（exit 127）。
3. **Windows 中文路径 + python 控制台**：`print` 中文会 GBK 崩溃 → 结果一律**写 UTF-8 文件**，再用 Read 工具读。
4. **plink 命令行会破坏中文文件名** → 远端校验一律用 `plink ... python3 - <<'PYEOF'` heredoc，**不要** `for f in 中文名.html; do grep ...`（会假报 `No such file or directory`）。
5. **换行符**：见 §1.3。
6. **常识性坑**：`len(str)` 是**字符数**不是字节数 —— 量字节一律用 `len(open(p,'rb').read())`。
7. **jsdom 无排版引擎**：能测计算 `display`，**不能**测像素几何 → 视觉类需求必须真机/浏览器截图。

---

## 3. 铁律（违反 = 整页白屏 / 白干）

### 3.1 老 WebView 语法禁令（内核约 Chrome 50~58）
```
?.        ??        .replaceAll(        Object.fromEntries
.at(      顶层 await      正则后行断言 (?<= (?<!
{...obj}  ← 对象展开（ES2018）★历史全站事故元凶
const {a, ...rest} = x     ← 对象剩余
**        ← 指数运算符
catch {}  ← 可选 catch 绑定
```
**允许**：`let/const`、箭头函数、模板字符串、`class`、`async/await`（非顶层）、数组展开、`Math.max(1, ...arr)`。
⚠️ **`node --check` 抓不到这些**，必须 Grep 逐项确认 0 命中。
⚠️ **已验证的误报（不要当 bug 改）**：
- `**` 出现在**引号内**是 Markdown 字符串（如 `'**一、核心考点**'`）
- **`/**` JSDoc 注释起始符**也是 `**` —— 本批 `app.js` 75 处、`company-lib.js` 37 处 **全部**属此类误报
- `...` 出现在 `[` 之后是**合法数组展开**；只有 `{` 之后才是禁用对象展开

### 3.2 顶层声明冲突禁令（"AI 功能集体失效"的真凶）
**禁止**在任何 js 里用 `var/const/let` 重复声明别的文件已存在的全局名。浏览器遇到重复声明会判该文件 **SyntaxError 并整文件拒绝执行**。
新增全局一律写成：
```js
if (typeof window.xxx !== 'function') window.xxx = function () { ... };
```
**现状基线（上批 QA 实测，未复测）**：FATAL **0** ／ RISK **4**（`AUTH_KEY`、`INTERVIEW_QUESTIONS`、`USERS_KEY`、`toastTimer`）／ WARN 46。**守卫覆盖率仅 32.9%**。波末门槛：**FATAL = 0 且 RISK = 0**。

### 3.3 版本戳（`?v=`）
- **改任何 `assets/*.js` 或 CSS 后，必须 bump 引用它的 `?v=`**，否则用户设备永远加载旧文件。
- **现状**：全站 **569 处，100% `20260916O`**，无杂质戳。
- ⚠️ **不要复用已服务过的戳**（见 §1.5）。
- ⚠️ **严禁跨行正则**（跨行会吞掉 `-->` 和标签 = 整页白屏，已踩两次）。
- ⚠️ 生产 HTML 47 个 > 本地 39 个 —— 多出的是**历史遗留旧页**（不上导航、不在 bump 清单），`?v=` 统计里看不到它们，**属正常不是缺陷**。

### 3.4 共享文件与并行铁律
- **`assets/app.js`（33 页）、`assets/api.js`（30 页）、`assets/common.css`（全站）是共享文件 —— 同一时刻只允许一条线改**，并行写必然后写覆盖先写。
- 派活时**必须显式列出独占文件清单**。

### 3.5 其它
- **禁原生 `alert/confirm/prompt`**（ADR-3），用 `window.xtToast` / `showToast` / `uiConfirm` / `uiPrompt` 或页面内区块。
- 改完必须做**配对校验**：`<!--`/`-->` 相等、剔除 script/style 后 `<div`/`</div>` 相等、`<script`/`</script>` 相等、无损坏特征 `.js\?v=xxx"[^>\s]`。
- 改文件前**先备份**（命名含 `bak`，部署脚本会自动跳过含 bak 的文件）。
- **不删现有功能、保留既有 DOM id 与全局函数名**（外部页面可能引用）。
- **`<head>` 计数别用字面量 `'<head>'`** —— 很多页是 `<head data-page-node-id="...">`；且 `'<head'` 会误命中 `<header class="topbar">`。**`个人中心.html` 与 `mock_exam*.html` 天然缺 `</head>`**，属历史遗留，不是破损。
- **文件清理（铁律：永远最后一步）** —— 根目录现有 **115 个 `_*` 临时文件**，别急着删。
- **生产操作先问**：删用户数据、改 nginx、部署上线，三件必须用户点头。
- **数据质量红线**：内容全部原创；宁少而准，严禁臆造；新增**数据文件**用 **ASCII 文件名**、UTF-8 无 BOM。

### 3.6 🔴 定位「用户截图里的元素」的正确姿势（本批血泪教训）
企业定向库页面有**两个来源**渲染外观几乎一样的分类标签：
- `CL_renderTabs()` → `#clTabs`（旧版）
- `CL_renderHero()` → `.cl-hero-tags`（**用户截图里真正的那排**）

我第一轮删了 `#clTabs`，用户回「**怎么还在**」。
**正确流程**：先看截图确认位置 → 用 Read 打开本地 HTML 找可见文字 → **grep 全 `assets/` 找渲染源** → 确认是哪个函数/类名 → 再动手。
**不要**假设「一排胶囊 = 某个已知容器」。

---

## 4. 未完成工作（按优先级）

### 4.1 🔴 唯一仍纯记录未执行的需求：「导入题库」入口（口径已于 2026-09-16 18:04 补全）
**用户原文（两条，同一件事）**
1. 2026-09-16（带截图）：「这个更多功能里的**导入题库**按钮跳转的页面不对是个bug，现在你记录一下不用执行」
2. **2026-09-16 18:04（补充口径）**：「导入题库应该跳转到一个页面，这个页面可以通过 word，excel 等等方式导入自定义题库」

**状态**：⏸ **仅记录，未动代码**（用户两次都只要求记录）。已登记为 **R62**，详见 `用户需求与决策总账.md` §九「R62 备忘」。

**需求口径（已明确）**：入口应**跳转到一个页面**，该页面支持 **Word / Excel 等方式导入自定义题库**。

**实测现状（2026-09-16 18:0x，逐处定位）**
- 入口**不是页面跳转，而是页内全屏浮层**：`更多.html:160` `onclick="openImporterView()"` → `#importerView`（`.importer-view{position:fixed;inset:0;z-index:900}`）。
- **格式能力其实已经够**：`assets/importer.js` 已支持 `.docx`(Word) / `.xlsx`(Excel) / `.pptx` / `.txt` / `.csv`；`.doc` / `.xls` 老格式明确不支持。
- **文案与实际不符**：`更多.html:163` 只写「Excel/CSV 一键导入」、向导标题只写「Word .docx / 文本 .txt / 表格 .csv」—— 两处均漏报 `.pptx`。属 R47/R48「名实不符」同一治理范畴。
- **`工具.html` 的导入题库入口卡已迁走**（`:124-126` 注释自述「三张入口卡已迁移至『更多』页」，本页仅保留 `#importerView` / `#impLibs`）→ 旧悬案 **D-3「工具页导入题库跑到博客页」可能已随迁移消失，本次亦未复现**。

**开工前必须向用户确认（不要自行决定）**
1. **「独立 .html 页」还是「页内全屏浮层」可接受？**（用户原话是「跳转到一个页面」，现状是浮层，视觉等同全屏页且带返回按钮。）
2. 若确实跳到过「不对的页面」，请补**截图或入口路径**（本次两个入口的兜底都指向 `设置.html`，未复现跳到博客页）。
3. 是否需要新增 `.doc` / `.xls` 老格式支持，还是**只修文案**维持现有能力。

**硬约束**：`更多.html` 与 `工具.html` **共用** `assets/importer.js`，改一处必须同步另一处；不得改 `importer.js` 的函数签名/行为；`#importerView` / `#impBody` / `#impLibs` / `openImporterView` / `closeImporterView` 全部保留；改 HTML 必须**二进制读写保 CRLF**（`更多.html` 带 UTF-8 BOM，勿重复加）。

### 4.2 🟠 P1：复核上批"落盘但未验证"的文件
见 `更新日志-20260916-第三批.md` §四。**这批文件本批完全没碰**，状态与旧文档一致：
| 文件 | 关注点 |
|---|---|
| `assets/ai-service.js`（42,170 B） | **`data:image/jpeg;base64,` 仍 2 处** —— 图片 MIME 硬编码（400 头号根因）**可能没修** |
| `AI.html` + `assets/ai-page.js` | `closeSidebar` = 0；「点侧栏模型先关抽屉」未做 |
| `assets/cet-read.js` / `AI模拟面试.html` / `assets/iv-prep.js` / `assets/ppt-tips.js` / `错题本.html` / `个人中心.html` / `学习概括.html` / `assets/notify.js` | 见旧文档 §4.3 |

### 4.3 🟠 P1：上批零产出的 5 项（**原封不动，从头做**）
| 线 | 任务 | 目标文件 |
|---|---|---|
| L6 | N9-18 听力精听全面改版 | `assets/voiceplayer.js`（**注意本批已改过它的收放，别冲突**）、`英语.html` |
| L9 | N9-12 PPT 版式库升级 | `PPT版式库.html`、`assets/data-ppt-templates.js`、`assets/tpl-preview.js` |
| L10 | C12 申论批改（接 AI 底座） | `申论刷题.html`（`callAI` 仍 **0**） |
| — | N9-15 我的文章布局 | `blog_wechat.html`、`assets/common.css` |
| L8 余 | N9-14 剩余部分 | `演示.html`、`assets/data-ppt-tips.js` |

**N9-18 硬约束（不可违反）**：**禁改 `vp-back` 语义**、**禁改 `#vpMask` id**、**禁改 `SCENES` 结构签名**。
**N9-15 开工第一步必须解 F3**：`renderBlogStats`/`renderBlogMine`/`openBlogStats` 在 `app.js` 与 `api.js` **各有一份** → **先判明生效副本再动刀**。
**C12/C18 的 AI 底座口径（已定案）**：全站 AI 底座是 `assets/ai-service.js` 的 **`window.callAI(funcType, messages, opts)`**，**前端直连**。公共 Key **10 次/分钟限频** → **不能自动发问**。

### 4.4 🟡 P2：批次九 Wave 3~7 未开工项
完整拆解见 `架构-批次九-全线改造-波次计划-20260915.md`。
- **W6（最高风险）**：N9-9 页头返回(33 页) + N9-22B 删「学习统计」(28 页) + N9-3 注入 `notify.js`(30+ 页) → **必须合并为一次原子批量**
- **W7**：N9-7 `app.js` 拆分（**最后做**）+ bump + 部署 + 清理
> **`app.js` 接力链**：L1 ✅ → D-1 → N9-1+2 → N9-24 → N9-13 → N9-22B → C8 → C20 → N9-7

### 4.5 🟡 P2：D-1 数量关系题库回滚重做
**范围待用户拍板**。**开工前需向用户确认，不要自行决定。**

### 4.6 🟢 P3：更早遗留
| ID | 任务 | 状态 |
|---|---|---|
| B1 | 「我的动态」feed-btn 挪进资料卡操作区 | 未做 |
| B2 | 个人资料卡布局重构 | 未做 |
| D-2 | `common.css` L68 全局滚动锁 | **已决策冻结**，别顺手改 |
| D-3 | 「工具页导入题库跑到博客页」 | ⏸ **已并入 R62**（见 §4.1）；实测 `工具.html` 的导入题库入口卡已迁走、本次未复现 → **可能已随迁移消失** |
| D-5 | 文件清理（115 个临时文件） | **全批次最后一步** |

---

## 5. 自检与验收工具

| 目的 | 脚本 | 用法 |
|---|---|---|
| **老内核解析级体检（最重要总闸）** | `tools/qa/escheck_es2017.js` | `node tools/qa/escheck_es2017.js` → 必须 **`DONE 0`** |
| 一波多线统一体检 | `tools/qa/wave_check_20260916m.py` | `"$PY" ".../wave_check_20260916m.py" "文件1,文件2,..."` |
| 跨文件全局名唯一性扫描 | `tools/qa/global_name_scan_20260916m.py` | 门槛 **FATAL=0 且 RISK=0** |
| 跨线文件冲突检测 | `tools/qa/crossline_conflict_20260916m.py` | 内置 13 线清单自检两两交集 |
| 单页运行时体检 | `tools/qa/page_check.js` | `QA_PORT=8911 node tools/qa/page_check.js "页面.html" "关键词1,关键词2"` |

**每批必跑的自查清单**：
- [ ] 改动过的 js 跑 `node --check`
- [ ] `escheck_es2017.js` = `DONE 0`
- [ ] `<!--`/`-->`、`<div`/`</div>`、`<script`/`</script>` 配对平衡
- [ ] 跨文件全局名 **FATAL = 0 且 RISK = 0**
- [ ] 无 §3.1 禁用语法（`node --check` 抓不到，必须 Grep；**注意 `/**` 与引号内 `**` 是误报**）
- [ ] 原生 `alert(`/`confirm(`/`prompt(` = 0
- [ ] 改动过的资源 `?v=` 已 bump
- [ ] **换行符未被改混（CRLF/LF 按 §1.3）** ← 本批新增强调

**⚠️ 工具坑**：`tools/qa/_qa_origin.js` 会先探测本地端口上有没有「自己人」服务。若复用了你手工起的 `python -m http.server`，jsdom 会把**所有** `assets/*.js?v=` 报成 `Could not load script` → 看着像全站 js 崩了，**其实是噪声**。
**正确用法**：显式指定**没被占用**的 `QA_PORT`（8911~8920），逼脚本自启内置 Node 服务；输出头必须显示「**本脚本自启的本地静态服务**」才可信。

**⚠️ 诊断经验**：
1. **只测服务层会假绿** —— 必须用 **jsdom 加载真实完整页面**。
2. **"某个模块集体失效"优先怀疑该 js 解析失败**（跑 `escheck_es2017.js` 最快）。
3. jsdom 报 `Not implemented: navigation to another Document` 是 **jsdom 自身限制，不是 bug**，忽略。
4. **视觉类需求必须真机/浏览器截图**（375px + 1280px）。
5. **判断"文件是否被改"用 mtime，不要用 git**。
6. 🔴 **探针报 FAIL 时先怀疑探针** —— 本批我的验证脚本错了 3 次（`var c` 起点找错、断言写成中文字面量），产生过**假 FAIL**。先自证探针正确，再下结论。

---

## 6. 部署流程

**照抄 `tools/deploy_update_20260916o.py` 改 3 处：**
1. `STAMP = '<新版本戳>'`（**必须是没用过的新字母**，见 §1.5）
2. `OUT = r'C:\Users\ATM\_dep_<x>_out.txt'`（避免覆盖）
3. 远端校验段的 grep 关键词 + 新增/已删符号断言列表

脚本会自动：bump 全部根 HTML 的 `?v=` → **行尾自检** → 打包 tar.gz → **新增/已删符号断言** → `pscp` 上传 → `plink` 远端解压 → `curl` 状态码 + `grep` 内容校验 + **远端行尾校验**。

**流程**：先 `SW_DRY_RUN=1` 预演 → 用户点头 → 真跑 → **另写只读脚本独立复核**。

### 6.1 部署脚本的坑（血泪清单）
1. **🔴 bump 必须二进制读写**（§1.3，本批出过事故）。
2. **凭据必须是 `root@ip`**：正则若只抓到裸 IP，plink/pscp 会用本地用户名认证 → `Access denied`，报错是极具迷惑性的「password not accepted」。模板已处理。
3. **远端备份命令必须容错**：`tar -czf 备份 <filelist>` 在文件不存在时会让 `&&` 链断掉 → **根本不解压** → 备份命令后加 `|| echo BACKUP_SKIP_NEW`。
4. **部署后一定实测**：`curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1/xxx.html` + 关键字符串 `grep`，不要只看上传 exit code。
5. **远端中文文件名校验用 python3 heredoc**（§2.4 第 4 条）。
6. 别用 `sw` 当变量名（与 shell 冲突）。
7. 用 `curl -s -D -` 看响应头，不要用 `-I`。
8. **推送后独立复核**（不要只信部署脚本自己的输出）——本批用的是「直连生产 plink 只读探针」，关键项：行尾、新增/已删符号计数、版本戳统一性、**带戳真实拉取**（`curl assets/xxx.js?v=<STAMP>`）。
9. **重命名后线上存量链接会 404**（书签/外链指向旧名）—— 属生产操作，需用户点头。

---

## 7. 回滚点与备份

### 7.1 上批新建的备份（部署脚本会自动跳过含 bak 的文件）
| 文件 | 字节 | 用途 |
|---|---|---|
| `更多.html.bak-pre-morefix-20260916` | 35,295 | ⚠️ 是**破损后**快照，不能当干净基准 |
| `AI模拟面试.html.bak-pre-l7-20260916` | 48,684 | L7 改动前 |
| `assets/iv-prep.js.bak-pre-l7-20260916` | 27,428 | L7 改动前 |

### 7.2 干净结构基准
**线上版**（现为 `20260916O`）。用 python `urllib` + `urllib.parse.quote` 抓下来做结构对照。

### 7.3 本批推送包（可作回滚源）
| 包 | 用途 |
|---|---|
| `tools/frontend_20260916M.tar.gz` | 旧（**含 LF 污染，不要用它回滚行尾**） |
| `tools/frontend_20260916N.tar.gz` | 四项 UI 需求（CRLF 已修复） |
| `tools/frontend_20260916O.tar.gz` | **当前线上版** |

---

## 8. 需求原文索引

| 文档 | 内容 |
|---|---|
| **`更新日志-20260916-第三批续作.md`** | **本批实际落盘 + 两次推送 + 纠错记录（最新）** |
| **`更新日志-20260916-第三批.md`** | 429 打停时的落盘现场（mtime 表 + 未验证清单） |
| `交接文档-20260916d-第三批执行现状与对接.md` | ⚠️ **已被本文取代**（版本戳/部署状态已过期） |
| `计划书-20260916-第三批执行（分工与波次）.md` | 13 线名册 + 接力链 + Wave 0~7 |
| `交接文档-20260916c-第二批执行现状与对接.md` | 更早总纲（含 20 项实测校正表） |
| `架构-批次九-全线改造-波次计划-20260915.md` | 批次九 23 项 7 波依赖分析 + 冲突矩阵 |
| `任务清单-待办总览-20260915.md` | A/B/C/D 四组待办 + 拍板记录 |
| `用户需求与决策总账.md` | 用户全部需求与拍板记录（**已拍板项照办，不要再问**） |

---

## 9. 工作方式偏好（用户多次强调，务必遵守）

1. **「直接给出结论，不用反复验证」** —— 命令/脚本**各跑一次即判定**；不做多轮回读、不做多轮自审。**这条也适用于调研/核实类工作**（外部文档、方案、代码行号只做**一次**抽验即下判断）。
2. **多线并行** —— 文件零重叠的前提下尽量并行。
3. **文件独占原则** —— `app.js` / `api.js` / `common.css` 同一时刻只允许一条线改。派活时必须显式列出独占清单。
4. **版本戳 bump 是全局互斥锁** —— 波内不 bump，波末单点统一。
5. **生产操作先问** —— 删用户数据、改 nginx、部署上线，三件必须用户点头。
6. **数据质量红线** —— 内容全部原创；宁少而准；新增数据文件用 ASCII 名 + UTF-8 无 BOM。
7. **不删现有功能、保留原有 DOM**。
8. **交付即交棒** —— 共享文件线做完立刻交棒。

---

## 10. 建议的接手顺序

1. **先读** `更新日志-20260916-第三批续作.md` **§二「落盘实况」表** —— 建立"哪些是本批动的、哪些是上批遗留"的地图。**这一步不读完不要动代码。**
2. **跑总闸确认基线**：`node tools/qa/escheck_es2017.js` → 期望 **`DONE 0`**。
3. **向用户确认「导入题库」跳转的正确目标页**（§4.1）—— 这是唯一还挂着的用户需求，成本低、价值直接。
4. **收尾 `assets/ai-service.js`**（§4.2）—— P0 且最有价值（AI 400 是用户直接痛点）；复核 `data:image/jpeg;base64,` 那 2 处是否真按魔数推导了 MIME。
5. **收尾 `AI.html` + `ai-page.js`**（§4.2）—— 补 `closeSidebar` + 窄屏覆盖 + 双端截图。与 §4.2 文件零重叠，可并行。
6. **并行启动上批零产出的 5 项**（§4.3）—— 文件互不重叠。
7. **批次九 Wave 3~5** → **Wave 6 全站原子批量**（最高风险）→ **Wave 7 收尾**。
8. **波末**：统一 bump（新字母）→ `SW_DRY_RUN=1` 预演 → 用户点头 → 部署 → **独立复核**。
9. 部署后提示用户**强制刷新**（清缓存/重开），否则设备上仍是旧文件。
10. **文件清理放在最后**。

---

## 11. 如果需要拉子代理

| 角色 | `name` | `subagent_type` |
|---|---|---|
| 工程师 | `software-engineer-任务N` | `software-engineer`（**有 Bash**） |
| QA | `software-qa-engineer` | **`general-purpose`**（`software-qa-engineer` **没有 Bash**，6 秒即挂） |

**派活指令必须自带的固定前言**（子代理看不到本对话）：
> 唯一可写树 `D:\下载的文件\学习工作台`（⛔ 禁碰 C 盘）／ ES2017 禁用语法清单（含 `/**` 是误报）／ 全局声明守卫写法 ／ 禁原生弹窗 ／ 波内不 bump ／ **改文件必须二进制读写、CRLF 红线** ／ bash 无 coreutils 的绕法（写 `.py` 落盘再跑，绝不内联中文）／ python+node 绝对路径 ／ `QA_PORT` 坑 ／ **命令各跑一次即判定，不做重复回读**。

要求子代理完成后 **`SendMessage` 回报**（它在对话里直接输出，主理人看不到）。

---

*交接人：主理人 齐活林 · 2026-09-16 17:50。所有基线数据均为本机实测（mtime/md5 采样 + 真实 jsdom 断言 + 生产 plink 直连复核），非文档转述。*
