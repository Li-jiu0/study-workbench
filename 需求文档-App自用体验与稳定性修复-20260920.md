# 需求文档：App 自用阶段体验 / 稳定性 / 显示缺陷修复清单

> 日期：2026-09-20
> 交付对象：workbuddy
> 使用范围：
>
> **自己和朋友小范围真机测试，不上架应用商店**
>
> 。因此本清单只收 "真机每天用得到、不依赖资质 / 域名 / 商店、改动可控" 的项；上架合规类（targetSdk、16KB、差分更新、账号注销、备案等）本期一律不做（见文末 "暂不做"）。
> 架构：安卓 WebView 套壳（自研 Python 打包，非 Gradle），入口 
>
> `file:///android_asset/学习工作台.html`
>
> ；包名 
>
> `com.study.workbench`
>
> 。
> 关联文档（本文不重复展开，按对应文档执行）：
> 《需求文档 - 位置共享 - 消息横幅 - 聊天界面修复 - 20260920.md》：实时位置双向、后台消息横幅、聊天底部 Tab 夹键盘 / 双层头、顶部状态栏黑边。
> 《需求文档 - 微信登录分享 - 设备统计 - 安装列表风控 - 20260920.md》：微信登录分享、设备统计后台、安装列表风控。



***

## 一、显示 / 第一眼观感（改动小、提升最直观）

### S1. 应用图标不是自适应图标，桌面会被裁出白边 / 方块（P0）



* 现状：`android/res/` 下只有一张 `drawable/ic_launcher.png`，没有 mipmap、没有 adaptive-icon。在圆形 / 水滴 / 方形图标和不同厂商桌面上会被强制裁切，出现白边或显示成方块。

* 改法：

1. 新增 `res/mipmap-anydpi-v26/ic_launcher.xml`（`<adaptive-icon>`，前景层 + 背景色 / 背景层），前景 logo 居中并预留约 66% 安全区；

2. 提供背景色 `res/values/colors.xml` 与前景 drawable；为 API 21-25 保留各密度 `mipmap-*/ic_launcher.png`；

3. `AndroidManifest.xml` 的 `android:icon`（现 `@drawable/ic_launcher`）改为 `@mipmap/ic_launcher`，roundIcon 同理（如提供）；

4. `build_apk.py` 用 aapt2 已会自动编 res，确认新资源进包并在几款桌面（圆形 / 水滴）验证。

* 验收：小米 / 华为 / OPPO 等不同桌面、不同主题下图标不变形、无白边；旧系统仍正常显示。

### S2. 没有启动闪屏，点开 App 先白 / 黑屏一下（P0）



* 现状：`res/` 无 layout/values/styles，主题用系统 `@android:style/Theme.Material.Light.NoActionBar`，WebView 初始化到首页渲染之间是白 / 黑屏。

* 改法（零 androidx，沿用项目零依赖风格）：

1. 新增 `res/values/styles.xml`，定义启动主题，`android:windowBackground` 用 layer-list（品牌底色 + 居中 logo）；

2. Manifest 的 application/activity 使用该主题；首页 `onPageFinished` 后恢复正常背景（或直接保留，进入即被页面覆盖）；

3. 保证冷启动 "点图标即见品牌底色 + Logo"，不出现纯白 / 纯黑空档。

* 验收：低端机冷启动也先显示品牌闪屏再进首页，无白 / 黑闪烁；横竖屏切换不花屏。

> 顶部状态栏黑边、聊天键盘遮挡、底部 Tab 夹键盘、双层头部：见关联文档一（问题三、问题四），本文不重复，但
>
> **必须与 S1/S2 同批在真机验证**
>
> （edge-to-edge 会影响键盘 inset）。



***

## 二、稳定性 / 可排查性（自用测试最该先补）

### B1. WebView 渲染进程被杀后整页白屏，没有自动恢复（P0）



* 现状：`MainActivity` 未重写 `onRenderProcessGone`。国产省电 / 内存紧张时系统会杀掉 WebView 渲染进程，表现为页面白屏或卡死，只能手动杀 App。

* 改法：重写 `WebViewClient.onRenderProcessGone`：从父容器移除崩溃的 WebView、调用 destroy，重新创建 WebView（复用现有初始化逻辑，建议把 WebView 初始化抽成可重入方法）并重新 `loadUrl` 首页；必要时提示 "页面已自动恢复"。

* 验收：模拟渲染进程崩溃（开发者选项或后台大量占用触发）后，App 自动重建页面回到可用状态，不需杀进程。

### B2. 没有加载失败兜底页和加载进度（P1）



* 现状：未实现 `onReceivedError/onReceivedHttpError`（错误页 + 重试），也未实现 `WebChromeClient.onProgressChanged`（顶部进度条）。内核异常、子资源 / 页面加载失败时用户只看到空白。

* 改法：

1. 主页面加载失败显示 "加载失败，点击重试" 占位（覆盖 "无网络 / DNS / 服务器不可达"），点击重新加载；

2. 顶部加一条细进度条（ProgressBar，水平、不挡操作），随进度走完隐藏；

3. 注意只对主框架（file:// 主页面）错误兜底，别把接口 XHR/fetch 的失败误当成整页错误。

* 验收：断网启动 / 杀服务器时出现可点击重试的错误页而非白屏；正常加载有顶部进度反馈。

### B3. 没有崩溃 / 错误日志，朋友闪退你拿不到原因（P0，自用强烈建议）



* 现状：原生未设置 `Thread.setDefaultUncaughtExceptionHandler`，前端 `window.onerror/unhandledrejection` 也未落盘；出问题只能靠截图猜。

* 改法：

1. 原生：注册全局未捕获异常处理器，把崩溃栈、机型（Build.BRAND/MODEL）、系统版本、App 版本、时间写入 `getExternalFilesDir(null)/logs/crash.log`（按天滚动、限大小）；

2. 前端：H5 全局捕获 `window.onerror`、`unhandledrejection`，通过 JS 桥追加写入同一日志目录（记录页面 URL、脚本、行列、最近操作）；

3. "设置 / 关于" 页加 "导出日志 / 分享日志"（用系统分享或保存到下载，复用现有 FileProvider 思路），方便朋友一键发你。

* 验收：手动制造一次原生崩溃和一次 JS 异常后，日志文件有完整记录，可从设置页导出 / 分享。

### B4. WebView 调试开关未显式管理（P2）



* 现状：未见 `setWebContentsDebuggingEnabled`。

* 改法：自用测试包显式 `WebView.setWebContentsDebuggingEnabled(true)`，便于 `chrome://inspect` 排查；预留正式分发时关闭的开关（如按 BuildConfig / 常量控制）。

* 验收：测试包可被 chrome://inspect 调试；关闭后不可调试。



***

## 三、网络健壮性（真实功能缺陷）

### N1. 前端请求没有超时，弱网 / 服务器卡死时无限等待（P0）



* 现状：统一请求封装 `assets/api.js:136-156` 的 `api()` 直接 `fetch`，**没有 AbortController / 超时**；401 静默续期逻辑有，但请求一旦发起到服务器不响应，Promise 一直 pending，按钮一直转圈、无提示无重试（AI 子模块自带 XHR 超时，但主业务接口没有）。

* 改法：

1. 给 `api()` 增加可配置超时（普通接口建议 15s），用 `AbortController` 中止，超时抛出明确错误 "网络超时，请检查网络后重试"；

2. 文件上传（`/api/uploads`、视频、语音）和 SSE / 流式接口单独放宽超时或不设总超时；

3. 超时 / 网络错误在交互上给 "重试"，不要让按钮永久 loading；提交类按钮加防重复提交（同一请求进行中禁用）。

* 验收：断网 / 服务器不响应时，15s 左右明确报错并可重试，不再无限转圈；重复快速点击提交不会产生重复数据；上传 / 流式不受超时误杀。

### N2. 缺少全局断网 / 切网提示（P1）



* 现状：仅朋友圈等个别页面有局部离线提示（如 `xt-moments.js:577`），IM、AI、社区等页面不统一；App 内没有统一的 "当前无网络 / 已恢复" 状态条。

* 改法：前端监听 `online/offline`（结合原生网络状态桥，必要时原生 ConnectivityManager 回调通知 H5），在顶部统一显示 "网络不可用 / 已恢复" 横条；离线时禁用 / 提示发送，恢复后自动刷新当前页与未读数。

* 验收：飞行模式 / 断网时全站出现一致的离线提示且发消息被明确拦截，恢复后提示消失并自动刷新。

### N3. 后端走 http 明文 + IP + 端口（P2，有条件再做）



* 现状：`assets/config.js:21` 写死 `http://110.42.134.62:8000`，Manifest `usesCleartextTraffic=true`。自用可跑，但明文可能被运营商劫持 / 插广告，部分网络对 http / 非标端口不友好。

* 改法（不强求，等有域名再做）：上备案域名 + HTTPS，`config.js` 切到 https 域名，收紧 cleartext；在那之前保持现状即可，但**不要**在传输里放敏感明文（登录密码目前走 http，有条件优先上 https）。

* 验收（实施后）：https 下登录 / 上传 / IM / 地图 / AI 全功能正常，无混合内容拦截。



***

## 四、交互顺手度（高频小毛病）

### U1. 外部链接 / 新窗口点了没反应或冲掉当前页（P0）



* 现状：`MainActivity` 没有 `shouldOverrideUrlLoading`，也没有 `setSupportMultipleWindows/onCreateWindow`。H5 中 `target="_blank"` 或外部 http (s) 链接在 WebView 内可能无反应，或在当前 WebView 打开把单页应用冲掉、回不来。

* 改法：

1. 重写 `shouldOverrideUrlLoading`：`file:///android_asset` 内部导航放行；外部 http/https 链接用系统浏览器打开（`Intent.ACTION_VIEW` + `FLAG_ACTIVITY_NEW_TASK`，项目已有该工具写法可复用，见 `MainActivity:1363`）；

2. `tel:`、`mailto:`、`geo:` 等 scheme 交给对应系统应用；无法处理的 scheme 静默忽略，勿崩溃；

3. 正确处理 `target="_blank"`（关闭多窗口并在 shouldOverride 中接管，或实现 onCreateWindow）。

* 验收：点文章 / 动态里的外部链接在系统浏览器打开且 App 内页面不丢失；电话 / 邮箱可拉起对应应用；target=\_blank 不再无响应。

### U2. 根页面按返回键直接退出，容易误触（P1）



* 现状：`MainActivity.onKeyDown`（约 2434-2446）里全屏视频先退全屏、`canGoBack()` 则网页后退，否则直接 `super` 退出 App，没有 "再按一次退出"。

* 改法：当不在网页历史、且当前在主入口页时，第一次返回 toast"再按一次退出星途"，2 秒内第二次才真正退出；聊天 / 子页面优先由网页历史返回。注意不要破坏现有全屏视频返回逻辑。

* 验收：首页误碰返回不退出，连按两次才退出；聊天 / 网页内返回仍是逐级回退。

### U3. 消息发送失败后没有 "重发" 入口（P1）



* 现状：图片 / 语音上传或发送失败仅弹 toast（`chat-local.js:2280、2300、2316、2821` 等），失败后要重新选图 / 重录；文本发送 `imSendText`(3204)/`imSendGroupText`(3833) 也需核对失败态。微信式 "红色感叹号点按重发" 缺失。

* 改法：

1. 文本 / 图片 / 语音 / 位置消息发送失败时，气泡置为失败态并显示红色重发按钮，保留原内容不丢；

2. 点重发按原消息内容重走发送（图片 / 语音复用已选文件 / 录音，不要让用户重来）；发送中置灰防重复；

3. 成功后清除失败态，失败给出原因（网络 / 超时 / 被踢群等）。

* 验收：断网发消息出现失败态与重发按钮，恢复网络后点重发能发出且不产生重复消息；语音 / 图片失败不需重录重选。

### U4. 主框架高度用 100vh，是键盘 / 地址栏遮挡的框架级根源（P0，与关联文档一联动）



* 现状：`assets/common.css:88` `.app{height:100vh}`，全站少量地方才用 100dvh（如 `.im-modal`）。移动端 100vh 不随软键盘 / 动态工具栏收缩，导致底部内容、输入框定位偏高 / 被盖。

* 改法：主容器与全屏页改用 `100dvh`（老内核用 `--xt-*` 变量或 visualViewport 兜底，项目已有 `--xt-satop` 兜底先例）；配合 visualViewport 处理键盘（详见关联文档一问题三）。

* 验收：弹 / 收键盘、呼出 / 隐藏地址栏时，输入框与底部导航始终贴边、不被盖、不留多余空白；老机型不崩。

### U5. 滚动穿透与默认回弹发光（P2）



* 现状：未设置 `overscroll-behavior`，聊天列表、弹窗（`.im-modal` 等）滚到边界会带动底层页面滚动，并出现 WebView 默认边缘发光效果。

* 改法：弹窗 / 聊天滚动容器加 `overscroll-behavior:contain`；弹窗打开时对底层做滚动锁定（项目已有 `body.modal-lock{touch-action:none}`，统一套用）；按需关闭不需要的边缘发光。

* 验收：弹窗内滚动到底不会带动背后页面；聊天滚动不穿透到会话列表 / 主页。

### U6. 核查 user-select:none 是否误伤 "需要复制 / 选择" 的正文（P1）



* 现状：`common.css` 多处 `user-select:none`（如 223、305、861、893、1517、1535 等，多在顶栏 / 按钮 / 导航 / 拖拽物）。需确认笔记正文、社区 / 博客文章、AI 回答、动态文字等 "用户想长按复制" 的区域没有被连带禁用。

* 改法：仅对按钮、导航栏、纯交互控件保留 `user-select:none`；正文 / 消息 / AI 回复 / 资料等可读文本放开选择（消息已有长按复制菜单可保留，长文本阅读区允许系统选择）。

* 验收：文章、笔记、AI 回答、个人简介等可长按选择复制；按钮 / 导航仍不会误选中。

### U7. 群消息通知点击不直达群聊（P2）



* 现状：原生深链对私聊已直达会话（`MainActivity` 约 2533-2569，`gotoChatWith`/`私聊.html?uid=`），但群 / 系统类通知 `pid=0` 只降级打开聊天列表（代码注释 2536 已说明）。

* 改法：补群聊直达契约（通知 extras 带 groupId，前端提供打开指定群的入口，原生深链调用），与私聊体验对齐。

* 验收：点群消息通知直接进入对应群聊并定位，私聊直达不回归。

### U8. 收紧 file:// 跨域开关（P1，安全 / 稳定性）



* 现状：`MainActivity:152-153` 开启了 `setAllowFileAccessFromFileURLs(true)` 与 `setAllowUniversalAccessFromFileURLs(true)`（注释称后者为 "预留局域网后端"）。在 `file://` 加载任意 H5 的场景下，universal access 会放大 XSS 读取本地文件 / 跨域的风险；而实际接口走 http 远程，并不依赖该开关。

* 改法：先确认无真实局域网跨域需求，优先把 `setAllowUniversalAccessFromFileURLs` 置为 false 并全量回归（页面相对资源、fetch http 接口、上传、地图、AI、登录）；`setAllowFileAccessFromFileURLs` 按是否需要页面内跳本地 html 决定能否关闭。若确有局域网需求，改为仅在调试构建开启。

* 验收：关闭后真机全功能回归通过；不能为了 "预留" 在分发版长期开着 universal access。



***

### U9. 长按消息可「引用 / 回复」（微信式引用，私聊 + 群聊都要）（P1）

* 现状：长按消息菜单（`assets/chat-local.js:608-665`，`imShowMenu`，菜单项在 663-665）目前只有 **撤回 / 复制 / 删除本端**，没有“引用”；后端 `messages` 表（`server/database.py:200-228`，私聊 `group_id=NULL`、群聊 `group_id` 非空，私聊群聊共用此表）也没有任何引用字段。
* 目标：长按 **自己或对方** 发的消息，菜单出现「引用」；选中后输入框上方挂引用条，发送的新消息带引用块，对方可见、可点引用块跳回原消息（对标微信）。

**前端（`assets/chat-local.js`、`私聊.html`、`common.css`）**

1. 菜单加「引用」项（本人、对方消息都显示；与撤回的“仅本人”判定互不影响）。引用能力尽量覆盖 text/image/voice/location/file；图片气泡当前不弹长按菜单（`chat-local.js:563` 附近），需让图片也能长按出菜单并可引用（至少文本、图片必做，语音 / 位置 / 文件显示占位摘要）。
2. 选中后在输入框正上方显示引用预览条：被引用者昵称 + 内容摘要（文本截断一到两行省略；图片显示「[图片]」、语音「[语音 n″]」、位置「[位置] 名称」、文件「[文件] 名」），右侧「×」取消；同时让输入框获焦。切换会话、发送完成、点 × 都要清空待发引用。
3. 发送链路携带引用：私聊 `imSendText`(3204)、群聊 `imSendGroupText`(3833)，以及图片 / 语音 / 位置发送体，统一带 `reply` 对象；发送后清引用条。乐观渲染的气泡也要带引用块。
4. 气泡渲染：消息气泡顶部渲染引用块（左侧竖线 + 昵称 + 摘要，灰色小字，点击 **平滑滚动定位并高亮** 原消息几秒）；原消息被“删除本端 / 撤回”或拉取不到时，显示“原消息已撤回 / 已删除”，不得报错或空白。
5. 会话列表最后一条预览、后台通知摘要只取本次正文，不带引用块；本地 / AI 好友（localStorage）会话不支持引用时按普通消息降级，不报错。

**后端（`server/database.py`、`server/routers/chat.py`、`server/routers/groups.py`，含迁移）**

1. `messages` 表新增可空列（用项目现有的“守卫式 ALTER / init_db 无损加列”方式，参考 `database.py:583-588` feedbacks 加列写法，存量库零破坏）：
   * `reply_to_id INTEGER NULL`（被引用消息 id，逻辑外键；删除 / 撤回时前端按快照兜底）；
   * 冗余 **最小快照**，避免被引用消息被“删除本端 / 撤回”后引用空白：`reply_sender_name`（昵称快照，限长）、`reply_kind`（text/image/voice/location/file）、`reply_text`（文本或占位摘要，≤100 字）；图片是否回传 URL 自行权衡，最简可只回传「[图片]」占位。
2. 私聊发送（`chat.py`）与群聊发送（`groups.py:send_group_message` 约 358、建 Message 约 70、序列化 `group_msg_dict` 约 31）接收 `reply`：校验被引用消息存在且 **属于同一会话**（同一对私聊或同一群），非法引用忽略而不是报错；落库并在历史消息 / 未读 / 实时下发的消息 dict 里返回 `reply` 字段（老消息无该字段时前端正常）。
3. 不改变已读、撤回、删除本端、未读数、位置卡片等既有逻辑。

* 验收：
  1. 私聊、群聊中长按自己 / 对方的文本、图片消息都能「引用」；选中后输入框上方出现引用条、可 × 取消；
  2. 发送后双方气泡都显示引用块（昵称 + 摘要），点击引用块滚动到原消息并高亮；
  3. 图片 / 语音 / 位置 / 文件引用显示对应占位，不串会话；引用别的会话的消息被服务端忽略；
  4. 原消息被撤回或“删除本端”后，引用块显示“原消息已撤回 / 已删除”，不崩、不留白；
  5. 切换会话、发完消息引用条清空；不引用时发消息与现状完全一致；老消息、未升级端无引用字段仍正常显示；
  6. 存量数据库执行迁移不丢消息、不报错。

***

## 五、本期明确不做（不上架、自用，避免过度工程）



* 应用商店合规类：targetSdk 升级、64 位 / 16KB 页对齐、商店资料、差分更新、静默安装、账号注销、备案、无障碍 / 适老化认证。

* 厂商系统推送（个推 / 极光 / 小米华为 OPPO vivo）：大工程，自用阶段先用 "前台服务 + 自启动 + 电池不优化 + 通知渠道横幅" 顶着（见关联文档一问题二 P2）。

* `file://` 改 WebViewAssetLoader 虚拟 https 域：改动大、回归面广，自用阶段 file:// 够用；除非将来要做 PWA 离线缓存或网页相机等 "必须安全上下文" 的能力再评估。

* 微信登录 / 分享、设备统计后台、安装列表风控：见关联文档二，按资质 / 合规进度单独推进。

* 生物识别登录、桌面小组件、画中画、域名版 App Links 深链等锦上添花项：暂缓。



***

## 六、建议实施顺序



1. **第一批（观感 + 保命，改动小）**：S1 自适应图标、S2 启动闪屏、B1 白屏自动恢复、B3 崩溃日志导出。

2. **第二批（真机高频）**：N1 请求超时与防重、U1 外链处理、U2 再按退出、U4 100dvh（与关联文档一键盘 / 黑边同批改）、B2 错误页 / 进度条。

3. **第三批（体验完善）**：N2 全局断网、U3 失败重发、U5 滚动穿透、U6 user-select 核查、U8 收紧 file 权限、B4 调试开关、U7 群通知直达、U9 长按引用。

4. N3（https 域名）等具备域名条件后再做。

> 每条改完按 "验收标准" 真机自测并逐条回报；B1/U4 涉及 WebView 重建与全局高度，改后必须全主要页面（首页、私聊、群聊、AI、社区、模拟面试、地图、设置）回归一遍；原生改动请实际出包验证，不要只在浏览器自测。