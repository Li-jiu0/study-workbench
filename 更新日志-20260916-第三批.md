# 星途 · 更新日志（第三批 · 2026-09-16）

> 记录本批**实际落盘**的全部变更。以工作区实测为准（mtime + md5 + 签名核验），非计划。
> 代码树：`D:\下载的文件\学习工作台` ｜ 线上仍为 `20260916k`（**本批全部改动未部署**）
> 本批状态：**13 条工程线 + 1 条 QA 线并行开工，于 14:06 被 429 配额打停**（配额 `2026-09-17 13:32:39 UTC+8` 重置）

---

## 一、总体结论（先看这三条）

| # | 事实 | 数据 |
|---|---|---|
| 1 | **全站没有解析级破损** | `escheck_es2017.js` = **`DONE 0`**（本批开工前也是 0） |
| 2 | **本批只有 14 个业务文件真正落盘** | mtime ≥ 13:30 的文件清单见 §二；其余目标文件**零产出** |
| 3 | **其中仅 2 项经过完整自检 + 主理人验收** | `assets/app.js`（L1）、`工具.html`+`assets/common.css`（L4）；其余 12 项落盘但**未经验证** |

> ⚠️ **最重要的判断**：12 个"落盘但未验证"的文件**不能当作已完成**。它们是 429 打停时留下的一手现场，语法安全（escheck 0）但没有交付报告、没有功能验证。接手者必须逐个 diff 后自行判定完成度。

---

## 二、本批落盘实况（mtime 实测，决定性证据）

| # | 文件 | mtime | 字节 | 线 | 交付状态 |
|---|---|---|---|---|---|
| — | `更多.html` | ~13:22 | 25,758 | 抢修 | ✅ **已验收**（P0 白屏破损修复，见 §六） |
| 1 | `assets/app.js` | 13:41:42 | 7,073,398 | L1 | ✅ **已交付 + 已验收**（见 §三） |
| 2 | `assets/ai-page.js` | 13:40:03 | 61,710 | L3 | ⚠️ 落盘未验证 |
| 3 | `AI.html` | 13:40:03 | 49,770 | L3 | ⚠️ 落盘未验证 |
| 4 | `assets/common.css` | 13:42:13 | 121,032 | L4 | ✅ **已交付 + 已验收**（见 §三） |
| 5 | `工具.html` | 13:45:42 | 28,247 | L4 | ✅ 同上 |
| 6 | `assets/iv-prep.js` | 13:47:34 | 31,944 | L7 | ⚠️ 落盘未验证 |
| 7 | `AI模拟面试.html` | 13:49:14 | 70,853 | L7 | ⚠️ 落盘未验证 |
| 8 | `assets/notify.js` | 13:48:46 | 21,408 | L13 | ⚠️ **新建**（正文已写，30+ 页注入未做） |
| 9 | `assets/cet-read.js` | 13:49:54 | 35,497 | L5 | ⚠️ 落盘未验证 |
| 10 | `assets/ai-service.js` | 13:49:35 | 42,170 | L2 | ⚠️ 落盘未验证（**601 行 → 1080 行**） |
| 11 | `错题本.html` | 13:49:35 | 36,868 | L11 | ⚠️ 落盘未验证（`callAI` 已接 4 处） |
| 12 | `assets/ppt-tips.js` | 13:50:50 | 60,259 | L8 | ⚠️ 落盘未验证 |
| 13 | `学习概括.html` | 13:52:19 | 44,186 | L13 | ⚠️ **新建**（正文已写，入口未接） |
| 14 | `个人中心.html` | 13:52:21 | 102,718 | L12 | ⚠️ 落盘未验证 |

**写入已停止**：二次采样（间隔 3 秒）零变化，**无文件处于半写状态**，可安全接手。

### 2.1 零产出（本批派了活、但目标文件 mtime 未变，内容未动）

| 线 | 任务 | 目标文件 | 结论 |
|---|---|---|---|
| L6 | N9-18 听力精听改版 | `assets/voiceplayer.js`、`英语.html` | ❌ **零产出**（文件未被触碰） |
| L8 | N9-14 PPT 训练（部分） | `演示.html`、`assets/data-ppt-tips.js` | ❌ 未动（只改了 `ppt-tips.js`） |
| L9 | N9-12 PPT 版式库 | `PPT版式库.html`、`assets/data-ppt-templates.js`、`assets/tpl-preview.js` | ❌ **零产出** |
| L10 | C12 申论批改 | `申论刷题.html` | ❌ **零产出**（`callAI` 仍 = 0） |
| N9-15 | 我的文章布局 | `blog_wechat.html`、`assets/api.js` | ❌ **零产出** |

> 注：`演示.html`、`PPT版式库.html`、`assets/tpl-preview.js`、`assets/api.js` 相对 git HEAD **有**差异，但那些差异来自**更早的批次**，不是本批。**判据只能是 mtime**（本批开工时刻 = 13:30）。

### 2.2 落盘文件的签名证据（供接手者快速判定完成度）

| 文件 | 实测签名 | 解读 |
|---|---|---|
| `assets/app.js` | `recentMap` 已为 `'plaza'`/`'interview'`；3 条死条目已删；9481 行全 CRLF；新增 44 个全局名**跨文件零冲突** | P2 已完成 |
| `assets/ai-service.js` | `'429'`×20、`'15000'`×2、`getReader`×7（原 2）、`data:image/jpeg;base64,` 仍 **2 处** | 降级/超时/流式守卫**大概率已做**；**图片 MIME 硬编码可能仍在** ← 重点复核 |
| `AI.html` | `max-width:760px` **已 0 处**（删除）、`768px`×6、`max-width:none`×1 仍在；`.ai-model-panel` `z-index:99 → 330`；`closeSidebar` = **0** | 外观统一 + z-index 已动；**"先关抽屉再开面板"未做** |
| `工具.html` | `mpc-title` = **11 张**（PPT版式/PPT素材/案例拆解/四级经验/四级词汇/面试题库/商务礼仪/行测刷题/申论刷题/时政热点/企业定向）；`.mpc-icon` 嵌套 11、裸写法 0 | 已完成 |
| `assets/common.css` | 「学习博客」= **0**、「社区」= 4；花括号 1007/1007；L68 **未动** | P3 已完成；D-2 按决策冻结 |
| `assets/cet-read.js` | 31,276 → 35,497 B | 有实质改动 |
| `assets/iv-prep.js` | 25,669 → 31,944 B | 有实质改动 |
| `AI模拟面试.html` | 48,684 → 70,853 B（git diff +583/-101） | 有实质改动 |
| `assets/ppt-tips.js` | 51,565 → 60,259 B | 有实质改动 |
| `错题本.html` | `callAI` = **4**（原 0） | AI 底座已接 |
| `assets/notify.js` | 新建 21,408 B | 正文存在 |
| `学习概括.html` | 新建 44,186 B | 正文存在 |
| `个人中心.html` | 102,718 B（git diff +847/-31） | 有实质改动 |

---

## 三、已交付并通过验收的 2 项（可直接采信）

### ✅ L1 · `assets/app.js` —— 首页小助手核验 + P2
**核验工单 7 项**：6 项通过，1 项发现真缺陷并修。

| 项 | 结论 |
|---|---|
| 卡片头部 | ✅ 通过（按钮文案「切换」→「切换角色」） |
| 对话区 | ⚠️→✅ **原缺陷**：现代内核从点「发送」到首个 chunk 之间**完全没有反馈**，网络慢时像「点了没反应」。**已修**：两种内核都先落「AI 正在思考…」气泡 |
| 输入框 +「在 AI 页打开」 | ✅ 通过 |
| 快捷功能一行横滑 + 走真实 AI | ✅ 通过（只填 prompt **不自动发送**，符合公共 Key 10 次/分钟限频） |
| 「换个伙伴」弹窗 | ✅ 通过（2 卡 / 图标统一走 `lucideIcon` / localStorage 记住 / 按伙伴独立历史不丢 / 遮罩+✕ 双通道退出） |
| 与 `AI.html` 模型历史共享 | ✅ 通过（`ai_selected_model`、`ai_chat_history` 键名一致，且两边都用**裸 key 不加前缀**；会话结构一致） |
| 「本地演示模式」7 处 | ✅ 已甄别：**卡片副标题已干净**；余 6 处为配置注释/兜底提示/设置页既有描述，按口径保留 |

**顺手加固**：`finishStreaming` / `sendAiMsg` 里 `aiSendBtn` 未判空（缺输入行的页面会抛错中断落库）已补 `if (sb)`。

**P2 修复**：`'社区.html':'blog' → 'plaza'`、`'面试题库.html':'iv' → 'interview'`；另**删除 3 条死条目**（`'私聊.html':'chat'`、`'设置.html'/'个人中心.html':'settings'` —— `HOME_DEF` 的 `k` 只有 `plaza/cet/exam/comm/interview/ppt`，无对应 k 即永不写入，属死代码），并加了判据注释。

**自检**：`node --check` rc=0 ｜ **escheck `DONE 0`** ｜ CRLF=9481 / 裸 LF=0 ｜ 禁用语法全 0 ｜ 原生弹窗 0 ｜ 新增 44 个全局名跨文件扫描**每个只出现 1 次** ｜ 30 个含 `#aiPanel` 的页面三要素 `BAD_COUNT=0` ｜ 运行时 `typeof callAI = function`。

### ✅ L4 · `工具.html` + `assets/common.css`
**N9-5 工具页填充**：2 张卡 → **11 张卡**。
- 目标页**逐个 `os.path.exists` 校验、图标名逐个在 `icon-map.js` 校验 —— 0 死链、0 unknown 图标**
- 原 2 张卡的 `.mpc-icon` 是「裸 div 挂 data-icon」的已知缺陷写法（图标不渲染）→ 全部改为 `<div class="mpc-icon"><span class="nav-icon" data-icon="xxx" data-icon-size="20"></span></div>`，裸写法归 0
- 卡片：PPT版式 / PPT素材 / 案例拆解 / 四级经验 / 四级词汇 / 面试题库 / 商务礼仪 / 行测刷题 / 申论刷题 / 时政热点 / 企业定向
- 侧边栏 / 底部导航 / 更多面板 / 工具面板**一字未动**；B4 已删的「万能金句/场景话术」未复活；导入向导全套依赖（`#importerView`/`#impLibs`/`importer.js`/4 个内联函数）原样保留

**P3 · `common.css` 注释旧名**：「学习博客」→「社区」，**三处**（旧文档只记了 2 处）
- **L1353** `/* ===== 学习博客（笔记系统）样式 ===== */`
- **L1355** `/* 博客顶部子导航 */` ← **旧文档漏记的第 3 处**（藏在 L1353 注释块正下方两行）
- **L2102** `（学习博客.html:236）` → `（社区.html:292）`（行号实测校正）
- 只改注释文字，**任何 CSS 属性值未动**；校验：全文件「博客」残留 = 0

**D-2（`common.css` L68 全局滚动锁）—— 按决策冻结，未改**（详见 §五）

**自检**：**escheck `DONE 0`** ｜ 工具.html CRLF=436 无裸 LF ｜ `<!--`17=`-->`17、`<div`178=`</div>`178、`<script`15=`</script>`15 ｜ common.css CRLF=2803 未变、花括号 1007/1007 ｜ 损坏戳 0 ｜ 禁用语法 0 ｜ 原生弹窗 0。

---

## 四、落盘但未验证的 12 项（**不可当作已完成**）

| 线 | 任务 | 文件 | 已知线索 | 接手者必须做的 |
|---|---|---|---|---|
| L2 | 需求 D：AI 400 修复 + 降级/超时/流式守卫 | `assets/ai-service.js` | 601→1080 行；`'429'`×20、`'15000'`×2、`getReader`×7；**`data:image/jpeg;base64,` 仍 2 处** | 复核图片 MIME 是否真按魔数推导；逐项对照 §7 六项要求 |
| L3 | 需求 C：输入框双槽位统一 + 模型按钮 | `AI.html`、`assets/ai-page.js` | `760px` 已删、`z-index` 已提到 330；**`closeSidebar` = 0**、`max-width:none` 仍在 | 补「先关抽屉+关遮罩再开面板」；**375/1280 双端截图未出** |
| L5 | N9-11 阅读理解布局 | `assets/cet-read.js` | 31,276→35,497 B | 三种题型切换实测无空卡片 |
| L7 | N9-19 模拟面试重构 | `AI模拟面试.html`、`assets/iv-prep.js` | +583/-101 行；**六阶段状态机交互文档未产出** | 补交互文档 + 六阶段走通实测 |
| L8 | N9-14 PPT 训练（部分） | `assets/ppt-tips.js` | 51,565→60,259 B | 确认 P0 五件完成度；`演示.html`/`data-ppt-tips.js` 仍未动 |
| L11 | C18 错题本 AI 分析 | `错题本.html` | `callAI` 0→4 | 降级不白屏实测；空错题本不报错 |
| L12 | N9-4 个人中心作品集 + B1 | `个人中心.html` | +847/-31 行 | 收藏持久化实测；确认未越界改 app.js/api.js |
| L13 | N9-22A 学习概括页 + N9-3 通知脚本 | `学习概括.html`、`assets/notify.js` | 两个新文件均在（44,186 / 21,408 B） | 结构配对 + 共用 DOM 7 件核验；`notify.js` 顶层声明是否 0 |

### 4.1 L7 留下的备份与中间产物（可作回滚/对照）
- `AI模拟面试.html.bak-pre-l7-20260916`（48,684 B）
- `assets/iv-prep.js.bak-pre-l7-20260916`（27,428 B）
- `_tmp_l3_qa/`（33 项，含 `_tmp_l7_*` 诊断脚本与截图报告 `_report.json`）

---

## 五、两条"故意没做"的决策（不是遗漏，别当 bug 修）

### 5.1 D-2 · `common.css` L68 全局滚动锁 —— **冻结**
L68 实测为 `html, body { height: 100%; overflow: hidden; }`。**它是全站「固定外壳 + 内部滚动」模型的地基**，不是疏漏：
- 布局链：`html,body{height:100%;overflow:hidden}` → `.app{height:100vh;display:flex}` → `.main{flex:1;overflow:hidden}` → `.content{flex:1;overflow-y:auto}`
- **实测影响面**：38 个根 HTML 中 **12 个没有 `.app` 外壳**（AI模拟面试 / PPT素材库 / 个人中心 / 四级经验分享 / 好友申请 / 学习工作台 / 学途 / 登录 / 设置 / mock_exam×3），**25 个没有 `.content`**；这 12 页各自用内联样式补偿（如 登录.html 自写 `html,body{overflow-y:auto;overflow-x:hidden}`）
- 一旦解锁 → 这 12 页出现**双滚动条 / 整页抖动 / 位置错乱**，而本波做不了 37 页视觉回归
- CSS 无法按「是否存在 `.app` 后代」限定作用域（`:has()` 老 WebView 不支持）

**建议的三步走（留 W4，别混在本波做）**：
1. 给 12 个无 `.app` 的页面补 `<div class="app">` 外壳 + `.content` 滚动容器，统一布局模型
2. 再把 L68 从 `overflow:hidden` 放宽为 `overflow-x:hidden`
3. 全站视觉回归 + APK 真机滚动验证

### 5.2 版本戳本批**未 bump**（按项目约定）
**波内任务一律不 bump，波末单点统一执行**（bump 会重写全部根 HTML，是全局互斥锁）。
现状：**`?v=20260916L` 共 567 处，全站统一，无杂质戳**。目标新戳 `20260916M`。

---

## 六、开工前的 P0 抢修：`更多.html` 白屏级破损（已完成并验收）

本批开工前，`更多.html` 是**白屏级破损**（`escheck` 报 `DONE 3`）。按交接文档 §4.1 方案做**定向字节区间删除**：
- 保留 L198 `<script>` + L199 `(function(){`，**删除 L200–L352**（误搬的整份工具页骨架），保留 L353 起的 6 个函数体 + `})();</script>`
- **未整文件重写**，CRLF 全程保持
- 额外修掉**修复区外**的既有缺陷：L141 是**孤儿 `-->` + 裸露文本节点**（会在卡片之间渲染出一行乱字），已补 `<!--` 前缀成完整注释

| 指标 | 修复前 | 修复后 |
|---|---|---|
| `escheck_es2017.js` | **DONE 3** | **DONE 0** |
| 字节 / 行数 | 35,295 / 572 | 25,758 / 420 |
| `<script` vs `</script>` | 16 / 15 | 15 / 15 |
| `</head>` / `<body` | 2 / 2 | 1 / 1 |
| `#importerView`/`#impBody`/`#impLibs`/`.morepage-list` | 各 2 | 各 1 |
| `.mpc-icon` 裸 `data-icon` | 5 | 0（span 嵌套 8） |
| 卡片标题 | 13 条 | 8 条，序列与验收表**严格一致** |
| `<!--` vs `-->` | 30 / 31 | 20 / 20 |
| div 平衡（剔 script/style） | 162/162 | 162/162 |

运行时体检：`typeof callAI = function`、body 文本 7105 字符、关键内容静态+运行时双命中、**零 SyntaxError**。
备份：`更多.html.bak-pre-morefix-20260916`（35,295 B，破损前快照）。

---

## 七、QA 线新增的验收工具链（本批**净收益**，可直接复用）

| 脚本 | 用途 |
|---|---|
| `tools/qa/wave_check_20260916m.py` | **一波多线统一体检**：escheck 总闸 + 结构配对 + 禁用语法 + 原生弹窗 + 版本戳 + 换行符，逐文件 PASS/FAIL 表 |
| `tools/qa/global_name_scan_20260916m.py` | **跨文件全局名唯一性扫描**（acorn AST + 作用域栈，能抓隐式全局） |
| `tools/qa/crossline_conflict_20260916m.py` | 跨线文件冲突检测（算两两交集） |
| `tools/qa/_wave_acorn.js`、`tools/qa/_globals_scan.js` | 上面两个脚本的内部辅助 |

调用示例：
```
PY="C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe"
"$PY" "D:/下载的文件/学习工作台/tools/qa/wave_check_20260916m.py" "文件1,文件2,..."
"$PY" "D:/下载的文件/学习工作台/tools/qa/global_name_scan_20260916m.py"
```

**已做反向验证**：故意塞入 `?.` `??` `.replaceAll(` `Object.fromEntries` `.at(` `**` `catch{}` `{...obj}` 顶层 await `alert/confirm/prompt` 与不配对 `<div>` —— 全部被逐条拦截。**工具会咬人，可信**。

### 7.1 QA 测出的全局名冲突基线（**本批重要发现**）
| 档位 | 数量 | 含义 |
|---|---|---|
| **FATAL**（必整文件白屏） | **0** | ✅ |
| **RISK**（看加载顺序可能崩） | **4** | `AUTH_KEY`（app.js const × 登录.html var）、`INTERVIEW_QUESTIONS`（AI模拟面试.html var × app.js const）、`USERS_KEY`（app.js const × 登录.html var）、`toastTimer`（AI模拟面试.html var × app.js let） |
| **WARN**（后定义覆盖，不白屏） | **46** | 主要是 `api.js` 与 `app.js` 之间 39 处 `function` 重名（`sendAiMsg`/`saveBlogNote`/`renderBlogList`…） |

**守卫写法覆盖率 = 32.9%**（裸全局定义 922 / 带守卫 452）。覆盖率越低，并行改动越容易撞出「重复声明白屏」。
> ⚠️ 该基线是**半成品快照**（13 线施工中采样），波末必须重跑。RISK 项很可能是某线新加 `let X` 而旧文件还留着 `var X`。

### 7.2 已知工具坑（务必记住）
`_qa_origin.js` 会先探测端口上有没有「自己人」。若复用了手工起的 `python -m http.server`，jsdom 会把所有 `assets/*.js?v=` 报成 `Could not load script` → `typeof callAI = undefined`，**看着像全站 js 崩了，其实是噪声**。
**正确用法**：显式指定一个没被占用的 `QA_PORT`（8911~8920），逼脚本自启内置 Node 服务；输出头必须显示「**本脚本自启的本地静态服务**」才可信。

---
 
## 八、版本戳与遗留物

### 8.1 版本戳
| 项 | 值 |
|---|---|
| 全站 `?v=` 总数 | **567 处** |
| 分布 | **100% `20260916L`**（无杂质戳） |
| 目标新戳 | `20260916M`（波末单点统一 bump） |
| 🔴 历史遗漏 | **`live_chat.html` 仍是 `?v=20260911c`**，落后 5 天，bump 时务必拉齐 |

### 8.2 根目录遗留临时文件（**用户铁律：文件清理永远最后一步，现在不要删**）
- 本批各线遗留 **40 个** `_*.py` / `_*.js` / `_*.txt` 临时文件（`_t5_*.js`、`_sl_patch.py`、`_w2t1_*.py`、`_wire_*.js`、`_nav_scan*.py` 等）
- 两个目录：`_tmp_l3_qa/`（33 项）、`_w2t1_img/`（29 项）
- `_w2t1_design.pdf`（617,387 B，L8/L9 系列的设计稿提取产物）

### 8.3 备份（部署脚本会自动跳过含 `bak` 的文件）
`更多.html.bak-pre-morefix-20260916`（35,295）｜ `AI模拟面试.html.bak-pre-l7-20260916`（48,684）｜ `assets/iv-prep.js.bak-pre-l7-20260916`（27,428）

---

## 九、本批未部署

**线上仍是完好的 `20260916k`**，本批全部改动只在本地。部署前必须先完成 §四 的 12 项验证。

---

*生成时间：2026-09-16 14:20 · 依据：对 `D:\下载的文件\学习工作台` 的 mtime 采样 + md5 对照 + git numstat + escheck 实测。本批中止原因：429 频率限制（配额 2026-09-17 13:32:39 UTC+8 重置）。*
