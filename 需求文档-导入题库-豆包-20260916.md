# 需求文档：导入题库功能设计（豆包建议稿）

> **来源**：**豆包**（AI 生成的需求建议稿）—— 用户 2026-09-16 18:08 从桌面转交，指示「你学习记录一下」。**非用户本人亲笔、非已拍板项。**
> **原始文件**：`C:\Users\ATM\Desktop\导入题库需求.txt`（已归档入本项目；本文件＝归档副本 + 主理人复核批注）
> **登记编号**：**R62**（见 `用户需求与决策总账.md` §九）—— ⚠️ **本稿是本条需求的详细设计稿，不另开新编号**
> **状态**：⏸ **仅登记，未开工**
> **配套阅读**：`用户需求与决策总账.md` §九「R62 备忘」（用户原话 + 现状实测）｜`交接文档-20260916e-第三批续作与对接.md` §4.1｜`assets/importer.js`（现状向导）

---

## ⚠️ 接手者必读：与现状的差距、冲突与风险（主理人 2026-09-16 实测复核）

### A. 与现状的差距对照（现状＝`assets/importer.js` + `更多.html` / `工具.html` 的 `#importerView`）

| 能力 | 现状（实测） | 本稿要求 | 判定 |
|---|---|---|---|
| **输入格式** | `.docx`(Word) / `.xlsx`(Excel) / `.pptx` / `.txt` / `.csv`；`.doc` 明确不支持（提示另存为 `.docx`） | `.xlsx` / `.csv` / **`.doc`** / **`.pdf`** | 🔴 **`.doc` 与 `.pdf` 现状不支持**，属**新增能力**；⚠️ 本稿**未提 `.pptx`**（现状已有，**别丢**） |
| **页面形态** | **页内全屏浮层** `#importerView`（`.importer-view{position:fixed;inset:0;z-index:900}`），非独立页 | 独立「导入页面」（含「← 返回」头 / 拖拽区 / 最近导入） | 🟠 与 **R62 用户诉求**（「应该跳转到一个页面」）**指向同一改造**，两稿合并执行 |
| **存储结构** | `localStorage['study_workbench_imports']`，按**文件名**自动生成题库「自定义·XXX」 | `localStorage['question_bank_{分类名}']` = 题目数组 | 🔴 **会形成两套结构** → 必须先决策「复用现有 / 迁移 / 新增」，**切忌并行两套** |
| **题目字段** | `{ q, options, word, meaning, ... }`（见 `impItemText`） | `{ id, type, question, options, answer, analysis, category }` | 🟠 字段名不同 → 需要**映射层**，否则做题页读不到 |
| **去重** | 待核（本轮未验） | 按**题目内容哈希**去重 | 🟡 新增能力 |
| **大文件分片** | 待核 | **>500 题分批导入**，避免卡死 | 🟡 新增能力 |
| **导入记录** | 有「我的导入题库」列表（`#impLibs`，可展开、可继续追加） | 「保留最近 **10 次**导入，可重复导入或删除」 | 🟡 增强 |
| **模板下载** | 无 | 提供 **Excel 模板**给用户照着填 | 🟢 易做（纯静态文件） |
| **解析预览校对** | 待核（现状向导为多步 `shell()`，是否已有预览步骤需现场确认） | **关键要求**：导入后**不直接存**，先出预览校对页，异常题标 ⚠️ 可手动补选项 | 🟠 若现状无预览则需新增；有则做增强 |
| **解析能力** | Word/txt 走通用文本提取（含 GBK/Big5 回退） | 明确要识别**题号**（`1.` / `一、`）、**选项**（`A.` `B.`）、**答案标记**（`答案：A` / `【答案】A`） | 🟠 需新增**结构化解析**（现状偏「提取文字 + 简单切分」） |
| **拖拽上传** | 无 | 「拖拽文件到此处，或点击选择」 | 🟠 老内核支持差 → **必须以「点击选择」为主路径**，拖拽作增强 |

### B. 老 WebView / 项目铁律风险

| # | 风险 | 说明 | 建议 |
|---|---|---|---|
| 1 | **`.pdf` 支持成本高** | 浏览器解析 PDF 需 pdf.js；项目**禁 CDN**（无外链库）→ 必须本地内联，**数百 KB～MB 级**，显著拖慢老 WebView 首屏 | **建议降级**：提示用户把 PDF 转成 Word/Excel；或列为 P2 再评估 |
| 2 | **`.doc`（老 Word）解析无轻量方案** | OLE2 复合二进制格式，浏览器端无成熟轻量解析器 | **建议不做**，维持现状「提示另存为 `.docx`」 |
| 3 | **ES2017 上限** | 豆包原稿未提兼容约束 | 新增代码**禁** `?.` `??` `{...obj}` `.replaceAll(` 等；`node --check` 抓不到，须 Grep 复核 |
| 4 | **拖拽** | 「拖拽文件到此处」 | 老内核 `dragenter/drop` 兼容性差，必须以「点击选择文件」为**唯一保证可用路径** |
| 5 | **改文件行尾** | — | `assets/importer.js` = **CRLF**；`更多.html` = **CRLF 且带 UTF-8 BOM**（写入勿重复加）。一律**二进制读写** |
| 6 | **两页共用同一向导** | `更多.html` 与 `工具.html` 共用 `assets/importer.js`，两页注释均写明「不改动 importer.js 任何函数」 | 改一处**必须同步**另一处；`#importerView`/`#impBody`/`#impLibs`/`openImporterView`/`closeImporterView` **一律保留** |

### C. 建议的范围切分（供用户拍板）

| 批次 | 内容 | 风险 |
|---|---|---|
| **P0** | 入口改为独立页/全屏页 + **Excel/CSV 表头映射**（题目/选项A-D/答案/解析）+ **解析预览校对页** + 内容哈希去重 + 导入记录（最近 10 次） | 低，收益最直接 |
| **P1** | 结构化解析增强（题号 / 选项字母 / 答案标记识别）+ 分片导入（>500 题）+ **Excel 模板下载** | 中 |
| **P2** | `.pdf` 支持 | 高（禁 CDN 下体积大） |
| **不做** | `.doc`（老 Word）解析 | — 维持「另存为 .docx」提示 |

---

## 原始需求全文（豆包原稿，逐字保留）

导入题库功能应该这样设计，实用为主：

## 核心流程

**选择文件 → 自动解析 → 预览校对 → 确认导入**

## 支持的格式

1. **Excel/CSV**（最常用）
   - 第一行表头：题目、选项A、选项B、选项C、选项D、答案、解析
   - 支持单选、多选、判断
2. **Word/PDF**（兼容）
   - 自动识别题目编号（1. 2. 3. 或 一、二、三）
   - 识别选项（A. B. C. D.）
   - 识别答案（答案：A 或 【答案】A）
3. **纯文本**（兜底）
   - 按空行分割题目
   - 手动标注题型

## 导入页面设计

```
┌─────────────────────────────────────┐
│  ← 返回    导入题库                  │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐    │
│  │                             │    │
│  │   📄 拖拽文件到此处          │    │
│  │   或点击选择文件             │    │
│  │                             │    │
│  │   支持 .xlsx .csv .doc .pdf │    │
│  └─────────────────────────────┘    │
│                                     │
│  最近导入：                          │
│  ┌─────────────────────────────┐    │
│  │ 行测5000题-言语理解.xlsx  ✓  │    │
│  │ 申论真题集.pdf            ✓  │    │
│  └─────────────────────────────┘    │
│                                     │
└─────────────────────────────────────┘
```

## 解析预览页（关键）

导入后**不要直接存**，先展示预览让用户校对：

```
┌─────────────────────────────────────┐
│  解析完成，共识别 128 道题            │
│  单选 96 | 多选 20 | 判断 12         │
│  有 3 道题识别异常，请检查           │
├─────────────────────────────────────┤
│  ✅ 1. 中国的首都是？               │
│     A.上海 B.北京 C.广州 D.深圳     │
│     答案：B  解析：...              │
│                                     │
│  ✅ 2. ...                          │
│                                     │
│  ⚠️ 5. 选项缺失，手动补充  [编辑]   │
│     [___] [___] [___] [___]        │
│     答案：[__]                      │
│                                     │
│  ...                                │
├─────────────────────────────────────┤
│  选择分类：[行测-言语理解 ▾]         │
│  [取消]  [确认导入 128 题]           │
└─────────────────────────────────────┘
```

## 数据存储

- 存在 localStorage（和项目现有数据一致）
- 结构：`question_bank_{分类名}` = 题目数组
- 每道题：`{id, type, question, options, answer, analysis, category}`
- 导入时去重（按题目内容哈希）

## 几个实用细节

1. **大文件分片导入**：超过500题分批次，避免卡死
2. **导入记录**：保留最近10次导入，可重复导入或删除
3. **模板下载**：提供Excel模板，用户按格式填好直接导
4. **错题自动标记**：导入后可直接在做题页使用
