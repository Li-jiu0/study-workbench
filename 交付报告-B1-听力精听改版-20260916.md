# 交付报告 · B1 · 听力精听全面改版（N9-18，含 N9-10）

> 任务编号：B1（N9-18，含 N9-10）
> 执行人：software-engineer-任务九
> 执行日期：2026-09-16
> 代码树：`D:\下载的文件\学习工作台`（唯一可写树）
> 独占文件：`assets/voiceplayer.js`、`英语.html`

---

## 一、需求原文来源与范围界定

### 1.1 需求原文（已找到，非推断）
- **文件**：`D:\下载的文件\学习工作台\需求文档-听力精听改进-综合-20260915.md`（6,169 B）
- **来源标注**：用户综合 GPT 产品思路 + 可落地改进（2026-09-15 提供）
- **原文明确点名的改造对象**：「改造对象指向 `听说训练` 相关页 + `assets/voiceplayer.js`（N9-10 已锁定场景标签条改分类列表，本综合版吸收并大幅扩展）」

→ 因此本任务 = **N9-10（场景两级分类）已在基线中实现** + **N9-18（该综合版全量改进）**。

### 1.2 范围界定（三级优先级）
| 级别 | 事项 | 基线状态 | 本次处置 |
|---|---|---|---|
| P0-1 | 场景两级分类，不要横向滚动 | **已实现**（`SCENE_GROUPS`/`renderSceneBar`） | 保留，回归验证 |
| P0-2 | 主卡片放大（60–70%）+ 改掉模糊隐藏 | **已实现**（`.vp-card width:70%`、`.vp-hint`） | 保留，回归验证 |
| P0-3 | 按钮一行排开 + 原文/中文开关 | **已实现**（`.vp-ctl` + `.vp-sw`） | 保留，微调（新增模式条） |
| P0-4 | 进度条 + 查看全文 | **已实现**（`.vp-bar` / `.vp-full`） | 保留，回归验证 |
| P0-5 | AI 助教去重 + 挂钩内容 | **已实现**（单 `.vp-aifab` + 4 tab） | 保留，挑战模式内隐藏 |
| **P1-6** | **三阶段训练流程（听→理解→说）** | **缺失** | **本次实现** |
| **P1-7** | **训练状态反馈（本组点阵 + 今日统计）** | **缺失** | **本次实现** |
| **P1-8** | **精听 / 挑战 双模式** | **缺失** | **本次实现** |
| P2-9 | 扩展训练类型（听写/辨义/综合） | 未做 | 长期方向，本次不做（需求原文：「P2 是长期方向，不用急」） |

**结论**：本次改版 = 补齐需求原文 **第二阶段（P1）全部三项**，并保持 P0 五项不回归。

### 1.3 数据质量红线遵守
- 未新增任何场景 / 句子内容（**零内容扩充** → 不存在抄袭真题风险）。
- 新增的只可能是**交互文案**（如「你听懂了吗？」「现在请跟读」），全部为原创通用提示语，不含任何题目原文。

---

## 二、基线行为（改动前实测）

在读改前对 `assets/voiceplayer.js`（51,030 B，mtime 09-15 22:13）与基线逻辑做静态+动态梳理：

| 链路环节 | 基线行为 | 评价 |
|---|---|---|
| 入口 | `英语.html` 「听力训练」卡 → `openVoiceTrain('listen')` | ✅ 能用 |
| 场景选择 | 两级分类（大类 chips + 场景 chips），无横向滚动 | ✅ P0-1 已达标 |
| 播放 | `play()` → App 内 `speakUtterance`；浏览器内 `vpSafeSpeak`（安全播放层，含 voiceschanged 兜底） | ✅ 能用 |
| 慢速 | `__slow()` 走 0.7 倍速 | ✅ 能用 |
| 原文/中文 | `.vp-sw` 开关；隐藏态为整块 `.vp-hint` 「点此显示原文」 | ✅ P0-2 已达标 |
| 精听步骤 | **无**：只有「听 → 上一句/下一句」，就是纯播放器 | ❌ **不符合需求 6** |
| 训练反馈 | **无**：练完即走，无任何状态/统计 | ❌ **不符合需求 7** |
| 模式区分 | **无**：只有精听一种模式 | ❌ **不符合需求 8** |
| 完成 | `__next()` 到末句弹 `toast('本情景完成！换个情景再练吧')` | ✅ 能用 |

**基线一句话结论**：P0 布局/交互已达标，**但整体仍是「播放英语」而非「训练听力」**——这正是需求原文的一句话定位，也是本次改版要解决的核心。

---

## 三、改版内容清单（含行号）

> 行号以 **改后** `assets/voiceplayer.js`（64,965 B）为准；新增 249 行。改动 100% 集中在 `assets/voiceplayer.js`；`英语.html` **零改动**（字节级相同）。

| # | 位置（改后行号） | 内容 | 对应需求 |
|---|---|---|---|
| 1 | CSS 段（`css()` 内，约 305–340 行） | 新增样式：`.vp-gmode`（模式条）、`.vp-steps`（阶段指示）、`.vp-guide`（阶段引导）、`.vp-yn`（听懂/没听懂按钮）、`.vp-stat`（训练状态面板 + 点阵 dot/ok/cur） | 6 / 7 / 8 |
| 2 | 约 168–256 行（`sceneKeysOf` 之后） | 新增训练状态模块：`TRAIN_KEY`、`trainToday()`、`trainLoad()`、`trainSave()`、`trainKey()`、`trainGet()`、`trainMark()`、`trainStats()`、`sceneDoneFlags()`、`sceneAcc()`；存储于 `localStorage['vt_train_v1']`，跨天自动清零，全 try/catch 降级 | 7 / 8 |
| 3 | 约 520–580 行（主渲染之前） | 新增阶段机：`STAGE_DEF`（listen/check/speak）、`stageIdx()`、`stageBarHtml()`、`isChallenge()`、`challengeStatHtml()`、`statPanelHtml()` | 6 / 7 / 8 |
| 4 | `render()`（约 584–670 行） | ① 挑战模式强制 `showEn=false/showZh=false`；② 控制区条件渲染（挑战模式隐藏 慢速/原文/中文 三键）；③ 新增阶段指示 + 阶段引导 + 阶段专属操作区（听→「听完了，去判断」；理解→「😕没听懂/😊听懂了」；说→「开始跟读/下一句」）；④ 挑战模式隐藏 AI 助教与「查看全文」，显示正确率面板；⑤ 精听模式显示训练状态点阵 + 今日统计 | 6 / 7 / 8 |
| 5 | `openVoice()`（约 676–712 行） | ① 新增 `trainLoad()`；② `S` 新增 `stage:'listen'`、`genMode:'intensive'`；③ 面板 DOM 新增 `<div class="vp-gmode" id="vpGMode">`；④ 初始化调用 `renderGModeBar()` | 6 / 8 |
| 6 | `renderSceneBar()` 之后（约 355–375 行） | 新增 `renderGModeBar()`：精听/挑战双模式切换条；口语跟读 tab 自动隐藏该条 | 8 |
| 7 | `__mode`（约 760 行） | 切 tab 时 `S.stage='listen'` 复位，并调用 `renderGModeBar()` | 6 |
| 8 | `__genMode`（新增，约 775–786 行） | **新方法**：精听/挑战模式切换，挑战模式立即隐藏原文/中文，复位阶段，toast 提示 | 8 |
| 9 | `__group` / `__scene` / `__prev` / `__next` / `__jump` | 全部追加 `S.stage='listen'` 复位（切句/切场景后回到阶段①） | 6 |
| 10 | `__toCheck`（新增） | **新方法**：阶段① → 阶段② | 6 |
| 11 | `__mark`（新增） | **新方法**：阶段② 记录「听懂/没听懂」→ `trainMark()`；没听懂时精听模式自动揭示原文+中文；随后进入阶段③ | 6 / 7 |
| 12 | `__en` / `__zh`（约 800–801 行） | 挑战模式下拦截并 toast 提示（不改变隐藏态） | 8 |
| 13 | `__slow`（约 855 行） | 挑战模式下拦截并 toast 提示 | 8 |
| 14 | `__rec`（约 803–820 行） | 跟读评分结果并入今日统计（`trainMark(..., score)`），随后 `render()` 刷新点阵与统计 | 7 |

**新增全局契约（均为 `window.openVoiceTrain.__*` 命名空间内，零新增裸全局）**：
`__genMode`、`__toCheck`、`__mark`。

---

## 四、三条硬约束的遵守证据

### 约束 1：禁改 `vp-back` 语义（W4「N9-9 全站页头返回补齐」基准样式，影响 33 页）
**证据（前后逐行对比，`EQUAL = True`）**：

```
OLD vp-back lines:
  '.vp-back{display:inline-flex;align-items:center;gap:4px;background:var(--primary-light);border:none;color:var(--primary-dark);padding:7px 14px;border-radius:8px;font-size:13px;font-weight:600;cursor:pointer}' +
  '<button class="vp-back" onclick="openVoiceTrain.__close()">← 返回</button>' +
NEW vp-back lines:
  （与 OLD 完全一致）
count: OLD = 2, NEW = 2，逐行字符串比较 EQUAL = True
```
→ `vp-back` 的出现次数（2）与两行定义/行为**逐字节一致**，未做任何修改。本次新增的 DOM（`vp-gmode`/`vp-steps` 等）均在 `vp-top` 之外，未触碰返回栏。

### 约束 2：禁改 `#vpMask` 的 id
**证据**：
```
OLD: m = document.createElement('div'); m.id = 'vpMask'; m.className = 'vp-mask open';
NEW: m = document.createElement('div'); m.id = 'vpMask'; m.className = 'vp-mask open';
逐行比较 EQUAL = True；全文件 'vpMask' 出现 9 次（与基线一致）
运行时（jsdom）：#vpMask 存在 = true；__close 后 #vpMask 已移除 = true
```

### 约束 3：禁改 `SCENES` 结构签名
**证据**：抽取 `var SCENES = {` … `};` 整块逐行比较：
```
SCENES block: OLD 120 行, NEW 120 行, 逐行比较 EQUAL = True
```
→ **零字段删改**。全文 `SCENES` 出现次数由 19 → 26，增量全部为**新增的只读引用**（`sceneDoneFlags`/`sceneAcc`/`statPanelHtml` 等新函数中读取 `SCENES[scene].lines`），未新增/删除/改名任何字段与语义。`loadListeningExt()` 的 ext JSON 合并逻辑亦未改动（仍为「已存在 key 不覆盖」）。

---

## 五、实测结果

### 5.1 工具与命令
```
node --check                              → rc = 0
env QA_PORT=8911 node tools/qa/page_check.js "英语.html" "听力训练,听力精听,口语跟读,词汇打卡"
node tools/qa/escheck_es2017.js           → DONE 0
```
> ⚠️ `page_check.js` 的输出落在共享文件 `C:\Users\ATM\_page_check_out.txt`，实测**被并发 teammate 覆盖**（读回的是 `演示.html` 的内容，属竞态噪声）。故另写独占探针 `.qa/_b1_probe.js`（输出 `.qa/_b1_probe_out.txt`）以拿到可信结果。

### 5.2 探针输出头（证明走的是本脚本自启的本地静态服务）
```
# 数据源 ORIGIN = http://127.0.0.1:8912
# 来源         = 本脚本自启的本地静态服务（端口 8912）
# 是否本地     = 是（验的是本地工作区代码）
URL = http://127.0.0.1:8912/%E8%8B%B1%E8%AF%AD.html
html bytes = 40165
```

### 5.3 断言结果（全部 PASS）
| 断言 | 结果 |
|---|---|
| 页面正常加载（html bytes = 40165，body text 非空） | ✅ PASS |
| `typeof window.openVoiceTrain = function` | ✅ PASS |
| `openVoiceTrain.__*` 21 个方法齐全，缺失 = 无 | ✅ PASS |
| `openVoiceTrain('listen')` 无异常，#vpMask 存在 | ✅ PASS |
| 听力模块初始化：#vpCats = 5 个大类、#vpSubs = 13 个场景、#vpGMode = 2 个模式、.vp-steps = 3 个阶段、.vp-stat = 1 个状态面板 | ✅ PASS |
| 精听链路 **阶段①**：含「请认真听这一句」+「听完了，去判断 →」 | ✅ PASS |
| 精听链路 **阶段②**：`__toCheck()` 无异常 → 含「你听懂了吗？」+「😕没听懂 / 😊听懂了」 | ✅ PASS |
| 精听链路 **阶段③**：`__mark(1)` 无异常 → 含「现在请跟读」+「开始跟读」 | ✅ PASS |
| 训练状态写入：`localStorage['vt_train_v1'] = {"d":"2026-09-16","done":{"coffee#0":{"u":1,"s":0,"n":0,"ok":1}}}` | ✅ PASS |
| 今日统计渲染：含「已完成」 | ✅ PASS |
| **挑战模式**：`__genMode('challenge')` 无异常；含「挑战模式」「正确率」；**不含**「慢速」按钮；显示「先听音频，点此显示原文」；**不含** AI 助教 | ✅ PASS |
| 挑战模式拦截 `__en/__zh/__slow`（不改变隐藏态，无异常） | ✅ PASS |
| 切回精听：`__genMode('intensive')` 无异常，恢复「慢速」+ AI 助教 | ✅ PASS |
| 两级分类：`__group('travel')` 无异常 → 旅行出行下 2 个场景；`__scene('hotel')` 无异常且阶段复位 | ✅ PASS |
| `__next` / `__prev` / `__jump(2)` 无异常，阶段均复位到① | ✅ PASS |
| 切 tab：`__mode('speak')` 无异常 → 标题「口语跟读」，挑战模式条隐藏 | ✅ PASS |
| `__close()` 无异常，#vpMask 已移除 | ✅ PASS |
| **未捕获异常**：仅 1 条 `JSDOM_ERR: Not implemented: navigation to another Document`（jsdom 对 `location.href` 跳转的固有限制，与本次改动无关；基线同样存在） | ✅ PASS |

### 5.4 静态检查清单
| 项目 | 命令/方法 | 结果 |
|---|---|---|
| 语法 | `node --check assets/voiceplayer.js` | **rc = 0** |
| ES2017 上限 | `node tools/qa/escheck_es2017.js` | **DONE 0** |
| 可选链 `?.` | Grep | **0** |
| 空值合并 `??` | Grep | **0** |
| `.replaceAll(` | Grep | **0** |
| `Object.fromEntries` | Grep | **0** |
| `.at(` | Grep | **0** |
| 正则后行断言 `(?<=` / `(?<!` | Grep | **0** |
| 指数 `**` | Grep | **0** |
| 原生 `alert(` / `confirm(` / `prompt(`（JS） | Grep | **0 / 0 / 0** |
| 原生 `alert(` / `confirm(` / `prompt(`（HTML） | Grep | **0 / 0 / 0** |
| `英语.html` `<!--` / `-->` 配对 | Python 计数 | **38 / 38 EQUAL** |
| `英语.html` `<div` / `</div>` 配对 | Python 计数 | **189 / 189 EQUAL** |
| `英语.html` `<script` / `</script>` 配对 | Python 计数 | **23 / 23 EQUAL** |
| 换行符（voiceplayer.js） | 二进制计数 | **CRLF 973 / LF 973 → bare_lf = 0（纯 CRLF，未被改混）** |
| 换行符（英语.html） | 二进制计数 | **CRLF 722 / LF 722 → bare_lf = 0（未动）** |
| 版本戳 bump | Grep `<script src="assets/voiceplayer.js?v=` | **仍为 `?v=20260916L`，未 bump（遵守波内不 bump 铁律）** |
| 裸全局新增 | 函数名去重 | **42 个 function 名，重复 = []；新增全局仅 `window.openVoiceTrain.__*` 3 个** |
| 越界文件 | 改动范围核查 | **仅 `assets/voiceplayer.js` 变化；`英语.html` 与备份 sha 完全一致** |

### 5.5 备份
| 备份文件 | 大小 |
|---|---|
| `assets/voiceplayer.js.bak-pre-b1-20260916` | 51,030 B |
| `英语.html.bak-pre-b1-20260916` | 44,937 B |

---

## 六、未验证项与人工核验步骤

### 6.1 明确未做验证（jsdom 能力边界）
1. **视觉呈现未验证**：卡片宽度（70%）、字号（英文 22px / 中文 16px）、按钮一行的实际换行表现、模式条/阶段指示/点阵的配色与间距——jsdom 不计算布局与 CSS 层叠。
2. **音频播放未验证**：`speechSynthesis` 在 jsdom 中不存在，`play()` / `__slow()` 的**实际发声**（走 `vpSafeSpeak` 或 App 内 `speakUtterance`）未被验证；`vpEnsureVoices` 的 `voiceschanged` 兜底同样未验证。
3. **语音识别与评分未验证**：`startEnglishRecognition` / `evaluateSpeaking` 依赖 App 桥或真实麦克风，未验证「跟读分并入今日统计」的真实链路（仅验证了 `trainMark(score)` 的调用路径存在且无异常）。
4. **老内核真实兼容性未验证**：本次断言在 Node(jsdom) 中完成，未在真实 Chrome 50~58 WebView 中运行。**已用静态手段规避**：全部禁用语法 Grep = 0、`node --check` rc=0、`escheck_es2017` = DONE 0。

### 6.2 人工核验步骤（建议在真机 APK / 桌面 Chrome 上执行）
1. 打开 `英语.html` → 点「听力训练」卡，确认面板全屏打开、顶部返回栏样式与其它页一致（`vp-back` 观感不变）。
2. **阶段流程**：确认卡片下方出现「① 听 · ② 理解 · ③ 说」三枚指示，且①为高亮态；点「听完了，去判断 →」→ 指示切到②并出现「😕 没听懂 / 😊 听懂了」；点「😊 听懂了」→ 指示切到③并出现「开始跟读 / 下一句 →」。
3. **点「😕 没听懂」**（精听模式）：确认自动揭示原文与中文；若切到挑战模式再点，原文/中文应保持隐藏。
4. **训练状态**：完成若干句后，确认「本组进度」点阵中已听懂句显示 `✓`、当前句显示 `●`、未完成句显示序号；「今日统计」的「已完成 / 平均听懂 / 跟读平均分」随操作增长。**刷新页面后统计应保留（当日）**，次日应自动清零。
5. **挑战模式**：点「🎯 挑战模式」→ 确认「慢速 / 原文 / 中文」三个控件消失、AI 助教隐藏、底部出现「本情景正确率」；尝试点击（若仍残留）应被 toast 拦截。切回「📖 精听模式」确认全部恢复。
6. **场景两级分类**：点「旅行出行」→ 确认只显示该大类下场景；点具体场景确认句子与序号刷新且阶段回到①。
7. **音频**：桌面 Chrome 点「播放」应能听到英文朗读；App 内应走原生 TTS。若首次无声，再点一次（自动播放策略）。
8. **口语跟读 tab**：切到「口语跟读」→ 确认挑战模式条消失、跟读流程正常、评分后今日统计的「跟读平均分」变化。
9. **返回/关闭**：点「← 返回」与右上角 ✕ 均应关闭面板并解锁页面滚动；ESC 亦应关闭（app.js 按 `#vpMask` 联动）。

---

## 七、遗留问题 / 需要主理人裁决的点

1. **P2（需求 9）未做**：听写训练 / 听音辨义 / 综合训练三类扩展训练类型未实现。需求原文明确「P2 是长期方向，不用急」，且会引入新数据结构与内容生产（含真题风险），建议**另开独立任务**并单独评审数据红线。
2. **`page_check.js` 共享输出文件竞态**：`C:\Users\ATM\_page_check_out.txt` 被多个并发 teammate 覆盖，导致单次跑的结果不可信。**建议主理人裁决**：让 `page_check.js` 支持 `QA_OUT` 环境变量指定输出路径（我本次用独立探针规避，未越界改工具）。
3. **训练统计的跨设备同步**：当前仅 `localStorage` 本地存储，换设备/清缓存即丢失。需求原文未要求云同步，故按最小方案实现；若后续需同步，应走既有用户数据接口（不在本任务范围）。
4. **「今日统计」口径**：我按「当日已标记过的句子数」计「已完成」，按 `trainMark` 累积的听懂次数取平均。「平均听懂次数」含首次即听懂的句（计 1 次）。若产品期望口径不同（如只统计「重复听」的增量），需主理人确认后微调 `trainMark`/`trainStats`。

---

## 八、改动文件清单（路径 + 前后字节数 + mtime）

| 文件 | 前（字节 / mtime） | 后（字节 / mtime） | 说明 |
|---|---|---|---|
| `D:\下载的文件\学习工作台\assets\voiceplayer.js` | 51,030 B / 2026-09-15 22:13 | **64,965 B / 2026-09-16 14:33** | 本次唯一改动文件，+249 行 |
| `D:\下载的文件\学习工作台\英语.html` | 44,937 B / 2026-09-16 12:18 | 44,937 B / 2026-09-16 12:18 | **零改动**（与备份 sha 相同），入口 `openVoiceTrain('listen')` 已满足，无需修改 |
| `…\assets\voiceplayer.js.bak-pre-b1-20260916` | — | 51,030 B | 改动前备份 |
| `…\英语.html.bak-pre-b1-20260916` | — | 44,937 B | 改动前备份 |
| `…\.qa\_b1_probe.js` / `_b1_probe_out.txt` / `_b1_es.txt` | 新增 | — | 本次自检脚本与输出（非交付代码） |

---

## 九、结论

- **三阶段精听闭环（需求 6）已落地**：听 → 理解 → 说，每步有明确引导、状态指示与专属操作。
- **训练状态反馈（需求 7）已落地**：本组完成点阵 + 今日统计（已完成句数 / 平均听懂次数 / 跟读平均分），localStorage 持久化、跨天清零。
- **精听/挑战双模式（需求 8）已落地**：挑战模式强制隐藏原文/中文/慢速与 AI 助教，并给出本情景正确率。
- **P0 五项全部保留未回归**（两级分类 / 卡片放大 / 一行控件 / 进度条+全文 / AI 助教去重）。
- **三条硬约束零违反**（`vp-back`、`#vpMask`、`SCENES` 逐行比较均 EQUAL）。
- **全部禁用语法 = 0，`node --check` rc=0，`escheck_es2017` = DONE 0，换行符未被改混，版本戳未 bump。**
- 视觉与音频/语音识别部分**未验证**，已给出 9 步人工核验清单。
