### 可疑 ?.
  615 | * ES2017 上限：仅用 var + function，不用 ?. / ?? / 对象展开 / 反引号模板 等。

### 可疑 ??
  615 | * ES2017 上限：仅用 var + function，不用 ?. / ?? / 对象展开 / 反引号模板 等。

### 可疑 ...
   18 | * 结构：[ { n: 省名, c: [ { n: 市名, d: [区/县...] }, ... ] }, ... ]

### 可疑 **
  239 | /** 拼接展示文本：直辖市与省同名时去重，形如「广东省 广州市 天河区」。 */
  252 | /** 关键词搜索：省 / 市 / 区 任意一级命中即返回，最多 limit 条。 */
  278 | /** 把「广东省 广州市 天河区」这类已存文本解析回 {province, city, district}（尽力而为）。 */
  334 | /**
  356 | /**
  416 | /**
  422 | /**
  448 | /** 单次逆地理请求（按 provider 组 URL + JSONP）。 */
  469 | /**
  493 | /** 浏览器定位（失败不抛异常，统一走 err 回调）。 */
  543 | /**
  574 | /** 折叠逆向结果为单一文本（text + recommend + street 去重合并，≤64 字符）。 */
  623 | /** 安全取 localStorage 引用（隐私模式 / file:// 可能抛异常）。 */
  631 | /** 截断文本到 TEXT_MAX 字符（超长加省略号）。 */
  638 | /** 写最近一次位置缓存（失败静默，不抛异常）。 */
  645 | /** 统一失败提示（文案一字不差，禁原生弹窗）。 */
  662 | /**
  695 | /**
  755 | /**
  772 | /**
  786 | /** 读常用城市列表（失败返回 []）。 */
  804 | /** 把一个地址文本推入常用城市（去重 + LRU 置顶 + ≤8，失败静默）。 */

### place/v1 全部出现点
  546 | * place/v1/search?boundary=nearby 兜底补一次；失败一律给 []，绝不抛异常。
  559 | var url = 'https://apis.map.qq.com/ws/place/v1/search?key=' +

### get_poi 全部出现点
  459 | '&get_poi=1&poi_options=page_size=20&output=jsonp&callback=' + cbName;
  471 | * 腾讯优先（含 get_poi=1 一次取回地址与周边 POI）；腾讯失败降级 amap →
  545 | * 主经 reverseGeocode（腾讯 get_poi=1 已带回 POI）；结果为空时用

### status 判定行

### 121 配额
