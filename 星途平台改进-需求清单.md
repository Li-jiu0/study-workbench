# 星途平台改进 · 需求清单（What）

> 配套文档：《星途平台改进-技术实施方案.md》（= How）
> 生成时间：2026-09-12（最后更新 14:07，按用户决定恢复直接删除 + 真题网上搜集）
> 说明：本清单整合两轮来源——01–14 为用户原始需求，15–29 为第三方锐评不足提炼，30 为评审新增。按 12 大类组织，P0 批次附验收标准。
> 严重度：🔴 P0 立即修复 ｜ 🟠 P1 短期优化 ｜ 🟡 P2 中期处理 ｜ 🔵 战略性建议

---

## 分类索引

| 类别 | 涉及需求编号 |
| --- | --- |
| A. 账号与权限 | 01 |
| B. 跨平台兼容 | 13 |
| C. 功能删减与模块合并（按用户决定直接删除） | 03、04、05、07、12 |
| D. 页面布局与交互重构 | 08、09、10、11 |
| E. 图标 / 视觉统一升级 | 02、03、04、05、06、07、09、10、11、12 |
| F. 聊天富媒体能力 | 14 |
| G. 数据隔离与安全（P0 主） | 15、16、17、22、23 |
| H. 数据一致性与计数器 | 18 |
| I. 功能库质量治理与去同质化 | 19、20、21、26、28 |
| J. 内容诚实性与入口修正 | 24、25 |
| K. AI 与内容深度 | 27 |
| L. 产品战略建议 | 29 |
| M. 首页数据真实性（评审新增） | 30 |

> 图标升级（E 类）涉及多条需求，建议作为一批统一处理，全站图标风格一次性对齐。

---

## 一、账号与权限

### 需求 01｜管理员超级账号（单向可见）

- **现状**：需要一个具备全局好友权限的特殊管理账号。
- **需求**：创建管理员账号（账号 `13523175196`，**密码不写入任何入库文档，部署时单独配置于服务器环境变量**），该账号具备以下特性：
  1. **单向可见**：管理员可查看所有用户的在线状态与个人资料；但用户端**不显示管理员为好友**（用户无感），用户仅能在需要时主动与管理员发起私信；
  2. 新用户注册后，管理员侧自动将该用户纳入可观察范围（无需用户同意，也不在用户好友列表出现管理员）；
  3. 管理员不参与普通好友可见性广播（其他用户看不到管理员的在线 / 资料 / 状态）。
- **本质**：单向可见的「隐身」管理员账号。
- **安全约束**：密码、JWT 密钥等凭据严禁写入入库文件；本清单仅记录账号 ID，密码走部署时环境变量注入（遵循项目「绝不把密码写进入库文件」红线）。

---

## 二、跨平台兼容

### 需求 13｜APP 端好友功能跨平台兼容

- **现状**：APK 安装到手机后好友功能无法使用，网页端正常。
- **需求**：排查并修复 APP 端好友功能失效根因，使 APP 与网页端互动 / 数据共享一致。
- **具体检查项**（补充排查方案）：
  1. `AndroidManifest.xml` 的 `networkSecurityConfig` 是否允许明文 HTTP（Android 9+ 默认禁明文）——若禁，则与需求 17（HTTPS）强关联，**两者合并处理**；
  2. WebView 是否 `setDomStorageEnabled(true)`（localStorage 在 APP 内依赖此开关，否则好友/消息本地态失效）；
  3. WebView cookie / token 跨域是否被拦（`CookieManager` 是否 `setAcceptThirdPartyCookies`）；
  4. WebSocket 在 APP 内是否可达（好友实时状态依赖 WS）；
  5. api.js 基址在 APP 打包后是否指向可达后端。
- **关联**：与需求 17（HTTPS）合并为「跨端可达性」专项。

---

## 三、功能删减与模块合并（按用户决定直接删除）

> 用户决定：下列删减照原计划直接执行，不再做"先查使用数据"前置门槛。被删模块如后续需恢复，按需求 20/28 改造补内容。

### 需求 03｜四级备考页面调整

- **删除**「写作提升」功能模块；
- 将该页面内所有功能模块图标统一升级。
- **关联**：豆包确认阅读理解 / 翻译专项亦为同质化选择题，其差异化治理见需求 20。

### 需求 04｜央国企笔试页面调整

- **删除**「数量关系」「判断推理」「资料分析」三个模块；
- 删除「题型正确率」统计项（该项本身有计数 bug，见需求 18）；
- 保留「学习状况」模块；
- 将该页面所有图标统一升级。
- **关联**：央国企「真题模考」治理见需求 19。

### 需求 05｜高情商表达页面调整

- **删除**「情景选择实战卡片」；
- 将该页面所有图标统一升级。
- **注意**：豆包评价高情商页「万能金句库 / 场景话术库」为扎实功能（位于高情商页，非工具页），与本条及需求 12 不冲突，**勿误删**。

### 需求 07｜PPT 训练页面重构

- 新建「综合功能模块」入口；
- 保留「版式库」「优秀案例拆解」为独立模块（豆包确认版式库为真 SVG，质量扎实）；
- 其余模块合并归入「综合功能模块」；
- 该页面所有图标统一升级。

### 需求 12｜工具页面调整

- 删除工具页内「万能金句」「场景话术」重复入口（高情商页同名功能保留）；
- 该页面所有图标统一升级。

---

## 四、页面布局与交互重构

### 需求 08｜好友页面菜单优化与重排

- 优化「会话、好友、+好友、群聊、申请」各按钮显示样式；
- 重新排序为：**会话 → 好友 → 群聊 → 申请 → 加好友**（「+好友」改「加好友」并置末位）。

### 🔴 需求 09｜个人中心页面 Bug 修复与重构（P0，评审提级）

- **现状（多个 Bug，用户每天都会点的页面）**：
  1. 点「个人资料」进入后，页面内混杂「发帖统计」「今日学习时长」「学习数据」等不属于个人资料的模块；
  2. 点「发帖统计」却跳转进「个人资料」页（路由错乱）；
  3. 点「学习偏好」「本机数据」进入后内容为空；
  4. 整体显示与信息组织不合理。
- **需求**：
  1. 修复路由跳转错乱，各入口指向正确页面；
  2. 补全「学习偏好」「本机数据」页面内容；
  3. 重新组织个人中心信息层级与模块划分；
  4. 该页面所有图标统一升级。
- **验收标准**：个人中心每个入口点击后均进入语义匹配的页面（个人资料只含资料、发帖统计只含统计），无空白页，无错误跳转。

### 需求 10｜广场页面重构

- 删除顶端说明性卡片（含「广场·写发贴·分类标签·草稿归档·数据本地保存」字样）；
- 「广场、我的文章、写帖子、统计」提升为顶端主导航；
- 该页面所有按钮与图标统一升级美化。
- **关联**：豆包指出"数据本地保存"与实际（服务器共享存储）不符，删卡片已部分覆盖。

### 需求 11｜设置页面层级重构

- **现状（层级混乱）**：
  1. 点「数据与关于」进入后还含「帮助与反馈」（应平级）；
  2. 点「外观」进入后还含「阅读与界面」「语音与朗读」（应平级）。
- **需求**：
  1. 「帮助与反馈」提升为设置页面平级一级菜单；
  2. 「阅读与界面」「语音与朗读」提升为平级一级菜单；
  3. 设置页所有图标与按钮统一升级。
- **关联**：设置页"数据存于服务器可多端同步"描述与实际数据隔离失效矛盾，见需求 15。

---

## 五、图标 / 视觉统一升级

### 需求 02｜图标统一升级（遗漏范围补全）

- **现状**：仅完成侧边栏标签图标，底部导航栏及其他位置未同步，视觉风格不统一。
- **需求**：底部导航栏菜单图标及全站所有遗漏位置图标一并升级，确保全局统一。

### 需求 06｜商务礼仪及面试页面图标升级

- 将商务礼仪及面试页面内所有图标统一升级美化。

---

## 六、聊天富媒体能力

### 需求 14｜私聊与群聊富媒体消息能力

- **现状**：聊天可能仅支持文本，缺语音 / 图片 / 视频。
- **需求**：私聊与群聊均支持：
  1. **语音消息**：录制并发送，接收方可播放；
  2. **图片消息**：发送图片，可点击预览 / 查看大图；
  3. **视频消息**：发送视频，可在线播放。
- **跨端一致**：网页端与 APP 端均支持，共享同一套消息数据。
- **关联**：与需求 13（APP 跨平台）、需求 25（群聊未实现）合并为「聊天能力跨端统一」专项。

---

## 七、数据隔离与安全 🔴

### 🔴 需求 15｜数据按账号隔离（P0）

- **现状**：退出甲号登录乙号后，乙号首页显示甲号倒计时与做题记录。localStorage 业务键（`study_workbench_data` / `study_workbench_tool_<工具>` / `study_workbench_recent` 等）均无账号前缀，仅 token 带账号概念。设置页却宣传"数据存于服务器，可多端同步"。
- **危害**：A 账号可看 B 账号学习记录，属隐私泄露。
- **需求**（止血 + 根治，退出策略已统一）：
  1. **止血**：所有 localStorage 业务键加账号前缀（封装 `lsKey(name)` 工具函数）；
  2. **退出登录不清除数据**，靠账号前缀隔离（避免换设备 / 重登丢本地进度，且后端同步未完成前清除即丢数据）；
  3. **登录后强制从后端拉取该账号最新数据覆盖本地快照**，防本地脏数据；
  4. **根治（中期）**：做题记录 / 连续打卡 / 学习统计迁移到后端 study router 按 `user_id` 存储，前端只缓存只读快照；
  5. 修正设置页"数据存于服务器可多端同步"的失实描述（与实际本地存储矛盾）。
- **验收标准**：用账号 A 学习产生数据 → 退出 → 登录账号 B → B 首页 / 各模块均不显示 A 的倒计时与做题记录；再登录 A，A 数据仍在。

### 🔴 需求 16｜修复倒计时日期解析 Bug（P0）

- **现状**：输入 `2026-12-21` 保存后显示「73262308 天」，日期显示 `202612-02-01`，解析错乱。
- **需求**：用 `new Date('YYYY-MM-DD')` 标准解析，禁手动字符串拼接；剩余天数 `Math.floor((target-now)/86400000)`；显示"还剩 XX 天" + 日期 `2026-12-21`。
- **验收标准**：输入今天 / 明天 / 明年 / 闰年 2 月 29 日 等边界日期，天数与显示均正确。

### 🟠 需求 17｜部署 HTTPS 与域名（P0 安全 / P1 排期）

- **现状**：IP 直连（110.42.134.62），无域名无 HTTPS，账号密码与私聊明文传输。
- **需求**：域名 + Let's Encrypt + Nginx 443 反代 + 80→443 跳转；前端 / APP 基址切 https。
- **关联**：与需求 13（APP 跨平台）合并为「跨端可达性」专项——APP 端明文 HTTP 被 Android 9+ WebView 拦截可能正是好友失效根因之一。

### 🟠 需求 22｜注册密码强度校验（P1）

- **现状**：弱密码（如 `test123456`）可直接注册，无强度要求。
- **需求**：注册 / 改密前后端双校验——≥8 位 + 必含字母与数字，弱密码拒绝并提示。
- **验收标准**：`123456`、`test`、`test123456` 等均被拒；`Abc12345` 通过。

### 🟠 需求 23｜修复注册昵称不生效（P1）

- **现状**：注册填昵称「测试号乙」，登录后显示「同学」，昵称未持久化。
- **需求**：注册 nickname 入后端 users 表（非 localStorage，保障跨端）；登录返回 nickname；个人中心可 PATCH 改昵称。
- **验收标准**：注册填昵称 → 登录后个人中心 / 评论 / 私聊均显示该昵称；APP 端登录同样显示。

---

## 八、数据一致性与计数器

### 🔴 需求 18｜全局修复计数器同步 Bug（P0/P1）

- **现状**：① 阅读理解答对后「已对 0」不更新；② 央国企做对 1 题正确率仍 0%；③ 各模块页「连续打卡 1」与首页「连续 0 天」矛盾。
- **需求**：
  1. 答题后即时 `setItem` + 即时刷新 DOM；
  2. 正确率 `= 做对题数 / 已做题数 * 100`，做对 1 题显示 100%；
  3. **连续打卡用单一全局键** `study_workbench_streak`（带账号前缀），当天任一模块学习即 +1，跨天未学清零；首页与所有模块读同一全局数据；
  4. 全局排查所有「已对 / 正确率 / 连续打卡 / 已做题数」更新链路。
- **验收标准**：各模块答对题后计数器即时 +1；首页与模块页连续打卡数一致；正确率随答题实时变化。

---

## 九、功能库质量治理与去同质化

### 🟠 需求 19｜真题模考：网上搜集真题 + 真模考（P1）

- **现状**：四级与央国企各有一个「真题模考」，实际都只是考试攻略折叠面板，无真题无计时无判分，双重虚假宣传。
- **需求**（按用户决定，搜集网上真题做真模考）：
  1. 真正实现模考：套卷 + 倒计时 + 自动判分 + 成绩归档；
  2. **真题从网上搜集添加**（用户决定）：搜集历年公开真题，整理为套卷；
  3. 原来的考试攻略折叠面板移至「考试指南」子页，不再占「真题模考」之名；
  4. 页面文案可保留「真题模考」（因内容确为真题）。
- **⚠️ 版权提示**（非阻断，提示用户知悉）：完整复制整套真题存在版权风险，建议优先采用官方公开样题、或对搜集来的真题做改编与来源标注；若涉及商业运营，建议取得授权或评估风险后再上线。
- **质量保障**：搜集的真题须人工校对题目 / 选项 / 答案完整性与正确性；标注年份与来源；先小批量（每模块 1-2 套）验证再扩充。

### 🟠 需求 20｜同质化选择题库差异化或合并（P1）

- **现状**：至少 10 个功能库（阅读 / 写作 / 翻译 / 数量 / 判断 / 资料 / 综合知识 / 案例拆解等）是同一弹窗选择题模板换题，描述特色（线段图 / 推理标注 / 原题标注 / 高频考点卡片 / 逐句拆解）几乎全未实现。
- **需求**（对保留项二选一）：
  1. 差异化：数量加线段图、资料加原题标注、判断加推理路径、案例拆解改真逐句拆解；或
  2. 合并精简：无特色的换皮选择题统一并入「综合选择题库」。
- **关联**：需求 03/04/05/12 已直接删减部分同质化模块，本条针对保留模块做差异化或二次合并。
- **质量保障**：同需求 19——人工审核、标注难度来源、小批量验证再扩充。

### 🟠 需求 21｜行测图形推理重写题干（P1）

- **现状**：题干直接描述图形规律（"小黑点依次在左上、右上、右下、左下"），等于泄题。
- **需求**：题干只留图形描述，规律入解析；题库改 JSON 加载便于更新。

### 🟡 需求 26｜商务礼仪「SVG 插画」实为 emoji 修正（P2）

- **现状**：描述称有「SVG 插画」，实际只用 emoji。
- **需求**：补真正 SVG 场景插画（8 张统一扁平风，400×300），或修正描述去掉"SVG 插画"。

### 🟡 需求 28｜简略内容项补全（P2）

- **现状**：i 人沟通专区（5 条简略建议，无"提前准备 / 文字辅助"工具）、企业定向库（5 家各 1-2 句，无"历年笔试形式"详情）、i 人伙伴团（回复偏问候）。
- **需求**：按各功能库原始描述补全真实内容与工具，兑现承诺。

---

## 十、内容诚实性与入口修正

### 🟡 需求 24｜私聊去掉「演示模式」误导标签（P2）

- **现状**：私聊消息实际跨账号同步，顶部却标"演示模式"。
- **需求**：移除"演示模式"标签，或改"在线"状态显示。

### 🟡 需求 25｜群聊功能实现或移除入口（P2）

- **现状**：好友页「我的群聊」始终"加载中..."。
- **需求**（二选一）：
  1. 实现群聊（建群 / 群消息 / 成员列表），与需求 14 群聊富媒体一并落地；或
  2. 暂移除「群聊」入口或标"敬请期待"，避免长期"加载中"。
- **关联**：与需求 08（好友菜单保留群聊位）、需求 14（群聊富媒体）联动。

---

## 十一、AI 与内容深度

### 🟡 需求 27｜i 人伙伴团 AI 回复深度优化（P2）

- **现状**：5 个性格 AI 伙伴可对话，但回复偏问候式，未给具体话术。
- **需求**：优化 AI prompt 与上下文，回复针对用户输入给可操作沟通建议与话术示例。

---

## 十二、产品战略建议 🔵

### 🔵 需求 29｜做减法 + 建立学习闭环（战略）

- **现状**：40+ 功能库，约 40% 同质化、20% 名不副实、真有深度 <30%；大部分"看完即止"，仅词汇打卡有艾宾浩斯闭环。
- **建议**：
  1. **做减法**：保留约 15 个扎实功能库（词汇打卡 / 听力 / 时政热点 / 话题表达 / 万能金句库 / 场景话术库 / 角色扮演 / i 人伙伴团 / 模拟面试 / 高频问题库 / 版式库等），集中做深做透；
  2. **建学习闭环**：把词汇打卡的艾宾浩斯间隔重复 / 进度追踪 / 效果评估推广到所有学习模块（学-练-复-评）。
- **说明**：方向性建议，需结合需求 19/20/28 质量治理落地。

---

## 十三、首页数据真实性（评审新增）

### 🟡 需求 30｜清理首页硬编码假数据（P2，评审新增）

- **现状**：旧账号首页存在硬编码假数据（各模块进度 45%/30%、薄弱点提醒、最近学习等），新账号则没有。首页展示与真实学习记录脱节。
- **需求**：清理首页所有硬编码假数据，所有进度 / 薄弱点 / 最近学习均从真实学习记录（localStorage 账号前缀数据 + 后端 study router）动态计算；无数据时显示空状态引导而非假数据。
- **关联**：依赖需求 15（数据按账号隔离）与需求 18（计数器同步）先落地，首页才能读到真实且按账号隔离的数据。
- **验收标准**：新注册账号首页进度为 0 / 空状态引导；学习产生数据后首页进度随真实记录变化；不同账号首页数据各自独立。

---

## 附：执行建议（按优先级，P0 附验收标准）

### 🔴 批次 P0-A：数据安全与一致性（本周，立即）

| 需求 | 验收标准（摘要） |
| --- | --- |
| 15 数据隔离 | 账号 A 学习→退出→登录 B→B 看不到 A 数据；再登录 A 数据仍在 |
| 16 倒计时解析 | 今天 / 明天 / 明年 / 闰年 2-29 等边界日期天数与显示均正确 |
| 18 计数器同步 | 答对即时 +1；首页与模块连续打卡一致；正确率随答题变化 |
| 23 昵称 | 注册昵称→登录后各处显示；APP 端同样 |
| 09 个人中心路由 | 每个入口进语义匹配页，无空白页，无错误跳转 |

### 🟠 批次 P0-B：诚实化与基础安全（2-4 周）

- 需求 19 真题模考→搜集网上真题做真模考（版权提示 + 人工校对 + 小批量）
- 需求 22 密码强度（≥8 位 + 字母数字，前后端双校验）
- 需求 17 HTTPS（与需求 13 APP 跨端合并专项）
- 需求 21 图形推理题干重写

### 🟡 批次 P1：内容填充与去同质化（逐模块，AI 生成 + 人工审核 + 小批量验证）

- 需求 20/28：四级阅读 / 翻译 / 口语、央国企综合知识 / 企业定向、高情商案例拆解 / i 人专区、商务礼仪 SVG
- ⚠️ 已删模块（03 写作提升 / 04 数量判断资料 / 05 情景卡片 / 12 工具页金句话术）的"填充"指令不再执行

### 🟡 批次 P2：聊天能力跨端统一专项（1-3 月）

- 需求 13 APP 好友 + 需求 14 聊天富媒体 + 需求 25 群聊

### 🟡 批次 P2：标签与入口修正

- 需求 24 私聊演示标签 / 需求 27 i 人伙伴 AI 深度 / 需求 30 首页假数据（依赖 15/18）

### 🔵 战略：需求 29 做减法 + 学习闭环

### 横向批次

- **图标升级**（02/03/04/05/06/07/09/10/11/12）：先确定全站统一图标风格再落地。
- **功能删减**（03/04/05/07/12）：按用户决定直接删除，改图标前先完成删减。
- **数据安全与一致性**（15/16/17/18/22/23）：合并专项，P0 优先，需求 30 依赖本批先落地。
