# 更新日志 · 2026-09-17 第五批（R73 需求 11 / 20-A / 20-B + 当日夜间增补）

> 本文档由本批主理人在 **2026-09-17 20:41** 依据磁盘实测（mtime + 内容签名 + 计数）落盘。
> 所有"已完成"结论都附实测证据；凡主理人未亲自抽验的，一律标注 **【未抽验】**，请接手者自行复核。
> 上一份日志：`更新日志-20260916-第四批-R64至R67.md`（其 §八 收尾实录覆盖 R68/R70）。

---

## 一、批次总览

| 项 | 值 |
|---|---|
| 批次 | 第五批（R73） |
| 主战场 | 21 项需求清单（需求 1~21）+ 用户当日陆续新报的 12 项增补 |
| 代码树 | `D:\下载的文件\学习工作台`（git `main`） |
| 当前版本戳 | **混合态**：`20260916O`(504) / `20260916R`(39) / `20260916S`(122) / `20260917`(6) / `20260917a`(6) |
| **下一版戳** | **`20260917b`**（不得复用旧戳） |
| 代码层 | 已全部落盘 |
| 戳 / 部署 / 提交 | **均未做**（见 §九） |
| APK | 已重打为 **versionCode 20 / versionName 1.19**，96,290,713 B（91.8 MB），2026-09-17 17:35:29；**真机验收未做** |

---

## 二、R73 需求 20-B（移动端适配补齐）

**落盘证据**

| 文件 | 大小 | mtime | 关键签名实测 |
|---|---|---|---|
| `assets/common.css` | 125165 B | 09-17 18:19:11 | `max-width:768px` ×8、`max-width:480px` ×4、注释含 `R73` ×1 |
| `ai-settings.html`（**根目录, LF**） | 34735 B | 09-17 19:03:00 | 全启 ×1、全停 ×1、`xt-set-used` ×2、`width=device-width` ×1、`max-width:400px` ×1、`max-width:360px` ×1 |
| `学途.html` | 42906 B | 09-17 18:23:45 | 错题/待复习文案修正（活2） |
| 其余 26 页 | 见 §A 表 | 09-17 17:48 前后 | 窄屏断点补齐 |

**已做内容（任务八线）**

1. **common.css 6 处窄屏根治**：11px 字号下限 + `flex-wrap` + `word-break`，全部写在 `@media (max-width:768px)` / `(max-width:480px)` 内，桌面端（>768px）渲染不动。
2. **26 页快扫重跑**：320/360/390/412 四档 164 断言全绿（**注意**：首轮曾因跑错工作树导致结论作废，已重跑）。
3. **ai-settings.html（根目录）三处移动端修复**：
   - `viewport` 改为 `width=device-width, initial-scale=1.0`（**有意去掉 `maximum-scale`/`user-scalable`**，这是对全站口径的一次有意豁免，对抗老 WebView 缩放 bug）；
   - 开关/检测行同行贴右：`.xt-set-row-right{flex-shrink:0;flex-basis:auto;justify-content:flex-end}` + `.xt-set-row-main{flex:1 1 0}`（@320 折行 11/24 → 24/24）；
   - **「当前使用」仅窄屏隐藏**：`.xt-set-row .xt-set-used{display:none}` 写在 `@media(max-width:640px)` 内，**桌面端保留该字样**。
4. **文案改短**：`全部启用→全启`、`全部停用→全停`（仅 `ai-settings.html` 两处；`assets/ai-settings.js` 的 `setEnableAll`/`setDisableAll` 按 id 绑定，不受文案影响）。
5. **新增两档断点**：`@media(max-width:400px)`（name 13.5px / ico-btn 34×34 / switch 40×22）与 `@media(max-width:360px)`（13px / 32×32 / 38×20）；`.xt-set-switch::after` 补 32px 透明触控热区。
6. **倍率胶囊响应式**：`.xt-set-chip` 在 400/360 档分别降到 10.5px / 10px（下限 10px），与模型名称行垂直居中偏差 ≤2px。

**约束遵守**：CSS **禁用 `clamp()/min()/max()`**（老 WebView 不支持），一律固定值 + `@media` 分级。

**【未抽验】**：需求 20-B 的 A1–A13 逐条验收结论由任务八自述，本任主理人只抽验了 `ai-settings.html` 与 `common.css` 的落盘签名。

---

## 三、R73 需求 20-A（全项目代码审查 + 致命缺陷修复）

**已落盘**

| 文件 | 大小 | mtime | 说明 |
|---|---|---|---|
| `server/rate_limit.py` | 4502 B | 09-17 17:13:54 | 限流 XFF 绕过修复（`X-Forwarded-For` 出现 5 次） |
| `server/routers/admin.py` | 14587 B | 09-17 17:14:15 | 管理员硬编码口令修复 |
| `server/routers/moments.py` | 22946 B | 09-17 17:21:07 | `_can_view` 重复定义致命 bug 修复（`_can_view` 出现 4 次） |

**两个致命/严重缺陷（已修）**

1. **限流可被一个请求头完全绕过**：`rate_limit.py:29-33` 的 `_client_ip()` 无条件信任 `X-Forwarded-For` 第一个值 → 伪造不同 XFF 即可获得无限配额，绕过登录/注册/改密等所有限流。
2. **`moments.py` 里 `_can_view` 被定义两次**：第一处（原 :63-84）逻辑完整；第二处（原 :87-99）**覆盖**了它，而其函数体末行调用的 `_visibility_allows(...)` **在 `server/` 全目录下无任何定义** → 动态可见性判定必然 `NameError`。

**【未抽验】**：前后端审查报告（任务六 / 任务七）的完整缺陷清单未在本任主理人处逐条过账，接手者需从各自产物文件补读。

---

## 四、R73 需求 11（动态空间）—— 本批用户最关注的一条

### 4.1 定案架构（⚠️ 曾有一版被作废的改名计划，别再执行）

| 角色 | 文件 | 说明 |
|---|---|---|
| **动态空间落地主页** | `动态.html`（35326 B，09-17 19:41:23） | `<title>动态空间 · 星途</title>`；`:91-103` 是「需求11 增量：动态空间入口」卡片区，卡 1「朋友圈信息流」(发现→朋友圈) → `朋友圈.html`，卡 2「我的动态」(我→头像→动态) → `我的动态.html` |
| 朋友圈信息流页 | `朋友圈.html`（10731 B，17:35:08） | `<title>朋友圈 · 星途</title>`，但页内 topbar 标题是「动态空间」，`:34` 有 `.page.active#page-moments` |
| 我的动态页 | `我的动态.html`（11531 B，19:24:51） | **由 `我的朋友圈.html` 改名而来** |
| 发布编辑页 | `朋友圈发布.html`（11553 B，17:35:08） | 未改名 |
| 共享样式/逻辑 | `assets/xt-moments.css`（19275 B）、`assets/xt-moments.js`（45613 B） | 均 17:35:08 |
| 后端 | `server/routers/moments.py`（22946 B） | 含富发布（可见范围/位置/视频/链接/提醒）接口 |

> ⛔ **已作废的计划**：曾有一版方案是「朋友圈.html→动态空间.html、我的朋友圈.html→我的动态.html、**动态.html 删除**、api.js:1457 改指动态空间.html」。
> 该方案**后半段已失效**：`动态.html` 被证明就是动态空间的落地主页（它的注释明写"需求11 增量：动态空间入口"），删它等于拆掉动态空间本身。
> **最终口径：`动态.html` 保留且是 TA 视角的目标页；`朋友圈.html` 文件名不动。** 只有 `我的朋友圈.html → 我的动态.html` 这一项改名被完整执行。

### 4.2 本批真正修掉的根因（用户报「动态空间怎么没上线」）

追查结论：**不是"忘了改名"，是两个硬故障**。

**故障 A：入口是死的。** 那 4 个动态页侧栏的「动态空间」写的是 `data-page="moments"`，但 `assets/app.js` 的 `PAGE_FILES` / `pageTitles` **完全没有 `moments` 键**（全 7MB app.js 里 `moments` 出现 **0 次**）→ 点击只走 `console.warn('未知页面：moments')` 然后原地不动。

**故障 B：入口看不见。** 全站 36 个带侧栏的页面里，只有那 4 个动态页**自己**有这一项，其余 **32 页**看不到。

**已修（任务十，2026-09-17 20:32 落盘，主理人已抽验）**

- `assets/app.js` 7082822 → **7082874 B**（+52B），mtime 20:32:06：
  - `pageTitles` 尾部追加 `, moments: '动态空间'`
  - `PAGE_FILES` 在 `profile: '个人中心.html',` 之后插入 `  moments: '动态.html',`
  - CRLF=9642 / bareLF=0，`node --check` RC=0；第 665 行那条 ~2.7MB 超长行未被触碰
- **32 页侧栏 32/32 全中**（`data-page="moments"` 每页恰好 1 处）
- **31 页移动端「更多」面板 31/31 全中**（AI.html 确认无 `morePanel`）
- `更多.html` 额外补 1 张 `.morepage-card.morepage-list-item` 卡片
- 4 个动态页 mtime **未被改动**（无误伤）

**插入规范（接手者后续如需批量插入同类入口，照抄）**

```html
<div class="nav-item" data-page="moments"><span class="nav-icon" data-icon="rss"></span><span>动态空间</span></div>
```

- 位置：紧跟「社区」项（`data-page="blog"`）的闭合 `</div>` 之后、下一个 `<div class="nav-section">` 或 `<div class="nav-item">` 之前。
- **不要带 `data-page-node-id`**（那 4 个动态页的现有项也没有）。
- **不要带渐变内联样式**——那 4 个动态页给「动态空间」挂的 `style="background:linear-gradient(90deg,var(--primary),var(--accent));color:#fff;border-radius:10px"` 是「当前模块高亮」，往别的页面挂会变成**假高亮**。

**3 页结构特殊，必须定向处理**（批量脚本会漏）：

| 页面 | 特殊之处 |
|---|---|
| `个人中心.html`、`私聊.html` | 侧栏是**单行 inline** 风格（`… </div> <div class="nav-item"…`），要按 inline 风格插 |
| `blog_wechat.html` | 「社区」是侧栏**最后一项**（后接 `.sidebar-footer`），按单行风格插 |

**更多面板的特殊之处**：`学习工作台.html` / `设置.html` 的 `.bottom-more-grid` 带 `data-page-node-id` 属性，**按纯类名扫描会漏判**，要按属性版定位；子节点 class 是 `.bm-icon` + **`.bm-label`**（不是 `.bm-text`）。

### 4.3 附带修掉的死链（任务二十四）

- `assets/xt-profile.js:448` 指向 `我的动态.html`，**而该文件当时不存在**（改名只改了引用、没改文件名）→ 真死链。
- 已补完改名：`我的朋友圈.html` → **`我的动态.html`**（11531 B，19:24:51），旧名文件已消失（`Test-Path` = False），活文件里 0 处残留旧名。
- 同步改 `动态.html` 4 处：`:91` 注释、`:98` href、`:100` 卡片标题「我的动态」、`:101` 副标题「我 → 头像 → 动态」；`:93-97` 的「朋友圈信息流」卡未动。
- `我的动态.html` 页内文案同步：`:7 <title>我的动态 · 星途</title>`、`:40` nav-title、`:52` header-sub。

> ⚠️ **git 陷阱**：`我的朋友圈.html` **原本就没被 git 跟踪**（所以 `git mv` 直接 `exit 128 not under version control`，最后用「复制+删除」回落方案）。
> 因此新建的 **`我的动态.html` 目前是未跟踪文件（`??`）**。提交前**必须显式 `git add 我的动态.html`**，否则远端会出现"旧文件没了、新文件没上传"→ 死链复发。

---

## 五、R73 需求 6（邮箱绑定端到端）

| 文件 | 大小 | mtime | 说明 |
|---|---|---|---|
| `server/routers/auth.py` | 14380 B | 09-17 17:14:46 | 邮箱相关 57 处命中 |
| `server/mailer.py` | 3665 B | 09-17 17:12:17 | **新建**，SMTP 封装（`smtplib` ×4） |
| `server/config.py` | 5016 B | 09-17 17:12:17 | 新增 SMTP 配置项 |
| `server/database.py` | 28851 B | 09-17 17:12:17 | 邮箱字段/验证相关 |
| `server/schemas.py` | 13290 B | 09-17 17:12:17 | 请求/响应模型 |
| `登录.html` | 37600 B | 09-17 17:12:17 | 「邮箱」×17、「验证码」×12 |

覆盖：发送验证码 / 校验验证码 / 绑定成功 / 通过已验证邮箱找回账号 / 邮箱登录。

> 🔴 **上线前必须先填 SMTP 凭证**（服务端 `.env`），否则整套功能在真机上只会回报发送失败。

---

## 六、当日夜间增补（用户当日陆续新报，不在 21 项清单内）

### 6.1 个人资料页（任务二十 / 二十二 / 二十四）

| 文件 | 大小 | mtime | 说明 |
|---|---|---|---|
| `assets/xt-profile.js` | **81382 B** | 09-17 19:34:51 | 1649 行，**纯 CRLF（CR=1648 / LF=1648）** |
| `assets/xt-profile.css` | **26371 B** | 09-17 19:10:15 | 862 行，纯 CRLF |
| `个人资料.html` | 2353 B | 09-17 19:10:49 | 47 行，**内联 JS 已清零**（`onclick=` 0 处，改走 `data-act` 委托：`back` / `theme`） |

**已交付 8 项**

1. **修复不能滑动**：根因是 `assets/common.css:68` 的 `html,body{height:100%;overflow:hidden}` 全站锁死，本页没有豁免覆盖 → `xt-profile.css` 顶部加 `html,body{height:auto!important;min-height:100%;overflow:visible!important}`。
2. **删除成就徽章**：`badgeDefs` / `badgeText` / `badgesBody` / `xtpShowBadge` 全清（已核实无外部引用）。
3. **改名**：`我的朋友圈`→`我的动态`（href 指向 `我的动态.html`）、`备考目标`→`目标`。
4. **删除入口**：错题本、导入题库（连带清死代码 `wrongBadge` / `dueReviewCount` / `todayStr`）。
5. **头像裁剪优化**：舞台可用区 260→**340**、裁剪环 0.73→**0.86**、缩放滑杆 max 300→**500**、新增双指捏合 + 滚轮 + 双击缩放、输出 `clamp(Math.round(ring*dpr*1.2),320,1024)` + `toDataURL('image/jpeg',0.92)`。
6. **视觉升级**：品牌渐变头部、磨砂 topbar、悬浮名片（14px 圆角 + 柔和阴影）、品牌色图标磁贴 `rgba(91,141,239,0.08)`、12px 组标题、52px 行高、分割线左缩进 56px；新增 **560 / 400 / 340 三档断点**；深色 token（`--xtp-bg:#16181d` / `--xtp-card:#1f2228` / `--xtp-line:#2c3038` / `--xtp-text:#e8eaed` / 品牌色提亮 `#6f9cf2`）。**零 CSS 新增规则以外的新文件**。
7. **修「整页不可点」**：根因是裁剪导出抛错时 `closeModal()` 在 `try` 内被跳过 → 全屏遮罩残留（放大输出触发 localStorage `QuotaExceededError` 的概率升高）。修法：`closeModal()` 移入 `finally` + `#xtpViews:empty,#xtpModalHost:empty{display:none}` 防拦截 + 移除临时 file input。
8. **修残留遮罩**：同上；并在四个上下文（预览原 URL / 沙箱模拟 / 桌面 / `file://`）做真实点击回归 96/96 全过。

**【已验证】**：jsdom 冒烟 `IS_PASS=YES`；CDP 四视口（320/360/390/412）断言全过（无横向溢出、mask 数=0、`elementFromPoint` 命中链、真实点击开/关子视图）；视觉 token 实测值与规格逐项一致；深色 token 实测一致。

### 6.2 查看他人资料（TA 视角）—— 用户报「点击查看其他用户的个人资料页面你好像没做」

**先纠正一个事实**：TA 视角**早就做了**，但做在**旧的** `个人中心.html?user=<uid>` 上（`assets/api.js:1327 renderProfilePage()` → `api.js:261-333 renderUserHome()`），而后建的 `个人资料.html` 里一行他人视角代码都没有 → 用户点进去看到的全是自己那一套「学习记录 / 我的笔记 / 退出登录」，自然判定"没做"。

**定案**：TA 视角宿主页**统一为 `个人资料.html?user=<服务端数字 uid>`**；旧的 `个人中心.html?user=` **降级为兜底保留**（`renderUserHome` 未删，一行可回退）。

**改动清单**

| 文件 | 大小 | mtime | 改动 |
|---|---|---|---|
| `assets/xt-profile.js` | 81382 B | 19:34:51 | **新增 16 个函数**：`viewUid 424` / `isOtherMode 434` / `fmtDate 437` / `relTime 443` / `presenceText 459` / `otherHeroHtml 468` / `otherActionsHtml 498` / `otherGroupDefs 514` / `paintOther 547` / `bindOtherActions 559` / `otherAddFriend 577` / `loadOtherMomentCount 587` / `renderOtherLogin 599` / `renderOtherPage 615` / `renderOtherEntry 641` / `xtpBack 647` / `otherReferrer 659`；`boot() 1634-1635` 分叉 `var otherUid = viewUid(); if (otherUid) { renderOtherEntry(otherUid); } else { renderPage(); }` |
| `assets/api.js` | 112319 B | 19:40:52 | `openUserHome` 两分支改指 `个人资料.html?user=`；`renderProfilePage` 缺口1 拆三段判定（:1337-1347）；「← 回我的主页」保持指向不带参数的 `个人中心.html` |
| `assets/chat-local.js` | 227473 B | 19:41:32 | 新增 `imOpenPeerHome`（:1245-1250 三段式）；`imShowUserProfile` 加 403/401 自动回退；更正 :3178 过时注释 |
| `assets/subpage-router.js` | 9743 B | 19:33:43 | 面包屑在存在合法 `user=` 时拼回，其他页面零变化 |
| `assets/xt-moments.js` | 45613 B | 19:41:05 | `:901 XTM.openUser` 改指新页 |
| `动态.html` | 35326 B | 19:41:23 | `:536 window.moOpenUser` 改指新页 |
| `好友申请.html` | 13009 B | 19:41:38 | 新增页内 shim `openUserHome`（:162-165）+ 头像/昵称绑 onclick |

**修掉的 4 个真实缺口**

| # | 严重度 | 现象 | 根因 | 修法 |
|---|---|---|---|---|
| 1 | **高** | 未登录/离线时点任何人头像 → 看到**自己的**资料 | `api.js:1263` `if (!apiGetToken() && !apiRefreshToken()) return;` 让 `apiBoot` 提前返回 → 生效版 `renderProfilePage` 不执行 → 退化为 `_renderProfileLocal` | `api.js:1337-1347` 拆三段：① 合法 `user` 参数但未登录 → 渲染「需登录后查看 TA 的主页」+ 去登录（**不回落到本机资料**）；② 已登录且非本人 → `renderUserHome`；③ 其余 → 本人视角，行为一字不改 |
| 2 | **高** | `好友申请.html` 点头像**完全无反应** | 头像/昵称**根本没绑 onclick**，且该页只加载 `config.js`（连 `openUserHome` 都是 undefined） | **不引入 api.js**，写页内最小 shim（见下方"关键判断"） |
| 3 | 中 | 私聊会话列表点头像只弹 toast「查看资料请到好友列表」 | `chat-local.js:1088` 绑的是 `imShowPeerHint` | 新增 `imOpenPeerHome`：有 `openUserHome` 就委托 → 直接拼 URL 兜底 → `serverId` 非法（0/空）才回退 toast。保留 `imShowPeerHint` 定义（0 死代码） |
| 4 | 中 | 管理员「全部用户」tab 里普通用户看资料必失败 | `chat-local.js:3193` 直打 `/api/admin/users/{uid}`，普通用户 403 | **不依赖前端角色判断**：先打 admin 接口，`403/401` 自动回退公开接口 `/api/users/{uid}`，两者都失败才提示 |

**🔑 关键判断（值得写入团队经验）：不要为了"加一个跳转函数"给页面挂 `assets/api.js`。**
实测：往 `好友申请.html` 注入真实 `api.js` 后，`apiBoot` 会在有 token 时**额外发起 `/api/auth/me`、`/api/notifications`、`/api/chat/unread` 三个无关请求并启动 30s/60s 两个轮询**。正确做法是页内 3 行 shim。

**安全红线（已用对抗测试证明）**：TA 页面**绝不渲染** `phone` / `email` / `emailVerified` / `gender` / `birthday` / `privacy` / `username` / `token_version`。
后端**双重脱敏**已确认：`server/routers/users.py:174-194` 的 `public_profile` 用**显式白名单**构造返回，只有 `id/nickname/avatarUrl/motto/bio/city/goal/tags/createdAt/isMe/isFriend/presence/stats/notes`；`user_brief` 只有 3 个字段。**即使前端写错，后端也不下发。**

### 6.3 AI 接口非 JSON 响应容错（任务二十一）

用户报 AI 对话抛 `Unexpected token '<', "<!DOCTYPE "... is not valid JSON`。根因：用户自定义端点 `https://ai.hub.camera/api/chat`（**该域名已死**：本机 DNS 解析失败，返回 HTML 错误页）。

| 文件 | 大小 | mtime | 改动 |
|---|---|---|---|
| `assets/ai-service.js`（**LF**） | 80609 B | 19:06:23 | 新增 `safeRespJson`(×3) / `looksLikeHtml` / `nonJsonMessage` / `respStatus` / `respCtype`；聊天主链 `relayProviders`(~:771) / `relayChat`(~:1050) / `requestModel`(~:1294) 走安全解析；`readSSEStream` / `readRelayBody` 加 HTML 前缀窗口拦截 |
| `assets/ai-page.js`（**LF**） | 90923 B | 19:09:46 | 新增 `parseJsonOrFriendly`(×2)，`testCustom` 的 2xx 分支由裸 `r.json()` 改为先 `text()` 再解析 |

失败时抛人类可读中文：`接口返回了非 JSON 内容（HTTP <status> <ct>），API 地址可能填错或服务不可用 · 响应开头：<前80字符>`。

> ⚠️ **本项发生过"假交付"**：首次报告称已写入并附 mtime 证据，全盘 hunt 显示任何副本 `safeRespJson` 计数为 **0**（它把改动前文件的原有属性当成了写入证据）。作废重做后才通过。

### 6.4 model 列表页两处响应式（需求 16/17）

- 模型名称字号、ico-btn 尺寸、switch 尺寸在 400/360 档按比例缩放（见 §二 第 5 条）。
- 倍率胶囊 `.xt-set-chip` 同步缩放（见 §二 第 6 条）。
- `当前使用` 的隐藏口径**经过一次纠正**：最初被误下为"全局删除"，用户澄清是"**移动端随页面缩小才隐藏**" → 最终形态为仅 `@media(max-width:640px)` 内 `display:none`，桌面端保留。

---

## 七、本批新增/变更的数据契约（接手必读）

1. **路由契约**：`assets/app.js` 的 `PAGE_FILES` / `pageTitles` 现含 `moments` 键 → `PAGE_FILES.moments = '动态.html'`、`pageTitles.moments = '动态空间'`。**凡新增可导航模块，必须同时登记这两个表**，否则 `navigateTo()` 只打 `console.warn('未知页面：xxx')` 原地不动（这正是动态空间"上线不了"的元凶）。
2. **TA 视角 URL 契约**：`个人资料.html?user=<服务端数字 uid>`（**必须 `/^\d+$/`**，非法值静默回落本人视角，不抛错）。判"是不是我自己"用 `GET /api/users/{uid}` 返回的 `isMe` 字段（与 `api.js:266` 同口径），`isMe:true` 时 `location.replace('个人资料.html')` 回落。
3. **「看他人资料」入口统一契约**：5 个入口全部指向 `个人资料.html?user=` —— `api.js openUserHome` / `chat-local.js imOpenPeerHome` / `xt-moments.js:901 XTM.openUser` / `动态.html:536 moOpenUser` / `好友申请.html` 页内 shim。**旧 `个人中心.html?user=`（→ `renderUserHome`）保留为兜底，不得删除。**
4. **`个人资料.html` 的动作契约**：内联 JS 已清零，全部走 `data-act` 事件委托（绑在 `.xtp-wrap`，用 `data-bound` 幂等）。现有取值：`back` / `theme` / `edit` / `about` / `logout`；另有 `data-view∈{notes,fav,data,chat}`、`data-href∈{学习概括.html,我的文件.html,我的动态.html,学习工作台.html}`、`data-frow∈{region,phone,email,birthday,goal}`。
5. **localStorage 键（绝不可改）**：前缀 `study_workbench_*`。本页只认 **`study_workbench_token`** 作登录判断；另用 `xt_profile_uid_v2` / `xt_profile_fields_v1` / `study_workbench_auth` / `study_workbench_last_account`。
   ⚠️ `xt-profile.js:396-413` 里展示的「用户ID」是**本地哈希派生的 6 位假 ID**，**不是服务端 uid**，不能当 TA 视角的参数用。
6. **后端公开资料接口**：`GET /api/users/{user_id}`（`server/routers/users.py:156-194`，需登录 Bearer）→ 已脱敏白名单（见 §6.2）。另有 `GET /api/users/presence?ids=`、`GET /api/moments/user/{uid}`（`moments.py:303-325`，非好友 403）。
7. **版本戳**：下一版用 **`20260917b`**。打戳必须 ① 锚定 `src=`/`href=` 属性 ② 只 bump 内容真改过的资产 ③ 捕获组不得带后缀（曾产出 `xxx.js.js`）④ 二进制读写保行尾 ⑤ 用与替换不同的独立 pattern 做终态复验 ⑥ 显式扫 `\.js\.js\?v=` 必须 0。

---

## 八、踩坑记录（本批新增，逐条都踩过）

1. **`renderProfilePage` 同名重复声明三份**：`api.js:220`（**无效**）、`api.js:1327`（**生效**）、`app.js:8102`（**无效**）。
   依据 `个人中心.html:487(app.js defer) → :490(api.js defer)` 的文档序，api.js 后执行覆盖 app.js；api.js 内部后声明覆盖前声明。
   **→ 改错一份会静默无效。这是本项目头号"改了不生效"陷阱。** 动手前先确认哪一份生效。
2. **同一条消息里对同一文件发多个 Edit 会 last-write-wins**：只有最后一处生效。曾有成员首轮并发改 4 处、实际只落了 1 处。**必须逐条串行改。**
3. **往只做好友申请这类小页面挂 `api.js` 会带来无关请求与轮询**（见 §6.2 关键判断）。
4. **`git mv` 对未跟踪文件直接 `exit 128 not under version control`** → 改名前先确认跟踪状态，回落方案是「复制内容 + 删旧文件」，并在提交时显式 `git add` 新文件。
5. **PowerShell 工具 stdout 会被吞成空** → 一切命令结果先 `Out-File` 落盘，再用 Read 读。
6. **Node 脚本里写中文字符串字面量会因编码不一致而损坏**：曾产出乱码路径 `D:\涓嬭浇鐨勬枃浠禱...` 导致 `ENOENT`。**含中文路径的批量脚本优先用 python + 显式 `encoding='utf-8'`**，或把中文路径拆成字符数组拼接规避。
7. **`jsdom 30` 不导出 `ResourceLoader`**：正确姿势是把页面真实源码（含 `<script src>` 引用的 assets 内容）**内联后执行**；`localStorage` 需要 **http origin**，即起一个临时 `http://127.0.0.1:<port>` 静态服务。本机 jsdom 在 `C:\Users\ATM\node_modules\jsdom`。
8. **`jsdom` 的 `location` 属性只读、无法拦截导航**：抓跳转目标要用「作用域遮蔽 `location`」或 `Object.defineProperty`。副作用：`location.replace()` 不真跳，`isMe` 回落会让容器暂留「加载中…」——**这是测试环境产物，不是缺陷**。
9. **批量脚本按纯类名扫描会漏判**：`学习工作台.html` / `设置.html` 的 `.bottom-more-grid` 带 `data-page-node-id`，得按属性版定位。
10. **同一条线跑错工作树**：曾发生某成员在 `C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-*`（**停在 R70 的过期空壳**）上做活并提交"已完成"报告。**唯一可写代码树是 `D:\下载的文件\学习工作台`。**
11. **CSS 禁 `clamp()/min()/max()`**（老 Android WebView 不支持），一律固定值 + `@media` 分级。

---

## 九、未完成事项（权威清单 · 截至 2026-09-17 20:41）

### 🔴 阻断上线（必须做）

1. **版本戳未打**。当前是混合态（`20260916O` ×504 / `20260916R` ×39 / `20260916S` ×122 / `20260917` ×6 / `20260917a` ×6），本批改过的资产**一个都没 bump** → 浏览器与 APK 的 WebView 都会命中旧缓存。下一版用 **`20260917b`**。
2. **未部署**到生产服务器 `110.42.134.62`（`/opt/study-workbench/`）。
3. **未 git 提交**。工作树有大量 `M` 漂移（含 `server/` 多个文件）。**提交前两件事**：
   - ⚠️ **`git add 我的动态.html`**（未跟踪，漏加会导致远端死链）；
   - ⚠️ **`git rm --cached 学习工作台-安卓App.apk`**（96 MB 二进制已被跟踪，勿推远端）。
   - `git push` 需走动态代理（第一优先读环境变量 `HTTP_PROXY`/`HTTPS_PROXY`，备选 `127.0.0.1:7897`）。
4. **SMTP 凭证未填**（服务端 `.env`）→ 需求 6 邮箱功能在真机上只会回报发送失败。

### 🟡 质量与验收欠账

5. **APK 真机验收未做**。当前包：`学习工作台-安卓App.apk`，**96,290,713 B（91.8 MB）**，mtime **2026-09-17 17:35:29**，manifest **`versionCode="20"` / `versionName="1.19"`**，已开 `INTERNET` / `RECORD_AUDIO` / `POST_NOTIFICATIONS` / `FOREGROUND_SERVICE` / `FOREGROUND_SERVICE_DATA_SYNC` / `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`、`usesCleartextTraffic="true"`、`foregroundServiceType="dataSync"`（配套 `android/java/com/study/workbench/MsgPollService.java` 12239 B）。
   **待真机验证**：连接服务器、好友私信、留言板、社区互动、语音、题库加载、**后台通知**。
6. **任务二十三 的正式断言报告未产出**。其工装 `tools\_r73_qa23_verify.js` **自身编码损坏**（中文字符串字面量被按 GBK 写出 → Node 读到乱码路径 `…鎴戠殑鏈嬪弸鍦?html` → `readBuf` 处 `ENOENT`）。产出物只有：
   - ✅ `tools\_r73_qa23_baseline.json`（5849 B，**可用**：`PAGE_FILES` 全键值 + `pageTitles` 全键 + BOM 分布 + CRLF 计数，是**改动前的比对基准**）
   - ❌ `tools\_r73_qa23_baseline.txt` / `_r73_qa23_result.txt` **均缺失**
   - 建议：改用 `tools\_r73_qa23_verify.py`（15440 B，看起来是好的版本）+ 显式 `encoding='utf-8'`，重跑并落 `_r73_qa23_result.txt`。
7. **R67 遗留小项**：`AI_CONFIG.providerGroups` 第二层厂商模型展示名与设置页下拉文案对齐。

### ⚪ 小尾巴（低风险，按需处理）

8. `assets/api.js:354-355` 现为 `if (document.getElementById('page-profile')) { A } else A;`——两个分支内容相同，`if` 已成**空壳**（无害，待清）。
9. `工具.html` 也有 `.morepage-list`（内含 `gotoBlogMine` 卡），本批**只给 `更多.html` 补了动态空间卡片**，`工具.html` 未补 —— **未裁定**，需要产品口径。
10. 台账补记：`用户需求与决策总账.md`（CRLF，98 KB）需补 **R68 / R70 / 本批 R73** 备忘。
11. **用户侧待办**：`ai.hub.camera` 端点已死需恢复或改新地址；`ADMIN_PASSWORD`、6 个平台 AI Key 轮换（历史遗留）。

---

## 十、验证结论存档

| 产物 | 结论 |
|---|---|
| `tools\_r73_qa27_report.txt`（5180 B，09-17 20:11:51） | 「查看他人资料」链路 **S1–S16 全部 PASS**。含对抗测试：假 payload 注入 `phone/email/gender/birthday/username/privacy/token_version/password_hash` → **0 泄露**；7 种非法 `?user=`（`abc` / `../../etc/passwd` / 空值 / `0` / `-1` / `1e9` / `%20`）全部安全降级；`/api/users/{id}` 返回 500 / 403 / 非 JSON / 断网四种失败均无脏文本与未捕获异常；`isMe:true` 不把访问者渲染成陌生人；page error = 0。它**换了一种方式**（非实现者的"作用域遮蔽 location"）抓取跳转目标，避免同源同错。 |
| `tools\_r73_qa23_baseline.json` | 改动前基线快照（见 §九 第 6 条） |
| 任务八 26 页快扫 | 320/360/390/412 四档 164 断言全绿（**重跑版**，首轮因跑错树作废） |
| 任务二十 CDP | 四视口断言全过 + 裁剪回归 `IS_PASS=YES` + jsdom 冒烟 `IS_PASS=YES` |
| 本任主理人抽验 | `app.js` moments=2 / 32 页侧栏 32/32 / 31 页更多面板 31/31 / `xt-profile.js` CR=1648·LF=1648 / `api.js` 需登录后查看=1 / `api.js` 个人资料.html?user==3 / 活文件里 `个人中心.html?user=` 仅剩 3 处**注释** |

**证据文件位置**：`D:\下载的文件\学习工作台\tools\` 下 `_r73_qa27_*` / `_r73_qa23_*`；本批主理人核查脚本产物在 `C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-2d763cd8\_r73_*.txt`（**该目录是过期副本，只读参考，不要在里面改代码**）。
