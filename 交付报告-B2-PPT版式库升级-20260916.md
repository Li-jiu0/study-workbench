# 交付报告 · B2 / N9-12 「PPT版式库升级」

- **任务编号**：B2（对应批次登记号 **N9-12**）
- **状态**：**DONE**
- **执行日期**：2026-09-16
- **版本戳**：**未 bump**（遵照波内铁律 #4；页面仍为 `?v=20260916L`）
- **代码树**：`D:\下载的文件\学习工作台`（唯一可写树，未触碰 C:\Users\ATM\WorkBuddy\Worktrees\...）

---

## 一、需求原文来源与范围界定

**找到需求原文**：`D:\下载的文件\学习工作台\需求文档-PPT版式库升级-豆包-20260915.md`
（来源：豆包，2026-09-15 18:04，登记编号 N9-12；另见 `交接文档-批次九-20260915.md` 第 10.6 节）

**范围界定**：**完全照需求原文执行**。该文档给出了 9 个章节的详细规格（页面布局 / 卡片 4 层结构 / 3 个按钮交互 / 分类筛选 / 搜索框 / 学习进度 / 数据结构 / 文件目录 / 12 条验收标准），本任务逐条落地。

**原文与现状的差异（已按原文为准）**：

| 项 | 需求原文口径 | 页面既有旧实现（app.js `PPT_LAYOUTS`） | 本任务处置 |
| --- | --- | --- | --- |
| 分类 | 封面页 / 目录页 / 过渡页 / 内容页 | 结构页 / 内容页 | 新增独立数据集按原文 4 分类 |
| 预览图 | `<img>` + `previewImage` 字段 | 内联 SVG 字符串 `svg` 字段 | 新增数据结构，图片缺失走占位降级 |
| 数据文件 | `data-ppt-templates.js` | 数据硬编码在 `app.js` | 版式库数据外置到 `data-ppt-templates.js` |
| 卡片交互 | 收藏 / 预览 / 下载 三按钮 | 无（仅「标记已看」/「移除」） | 按原文实现三按钮 |
| 搜索框 | 页面右上角，实时过滤 | 无（仅全局搜索 gs-input） | 新增版式库内搜索框 |

> **越界说明**：旧实现全部在 `assets/app.js` 里，不在本任务独占文件清单内，**未改动**。因此新版式库以「新增区块」形式叠加在页面顶部，旧「15 种常用版式速览」原样保留在下方（DOM id `#layoutFilter` / `#layoutList` 与全局函数 `renderLayouts/renderLayoutFilter` 一律未动）。

---

## 二、改动文件清单（路径 + 前后字节数 + mtime）

| 文件 | 前字节 | 后字节 | 前 mtime | 后 mtime | 备份 |
| --- | --- | --- | --- | --- | --- |
| `D:\下载的文件\学习工作台\PPT版式库.html` | 12,924 | **15,681** | 09-16 12:18 | 2026-09-16 06:34 UTC | `…bak-pre-b2-20260916`（12,924 B） |
| `D:\下载的文件\学习工作台\assets\data-ppt-templates.js` | 25,641 | **39,734** | 09-14 16:09 | 2026-09-16 06:32 UTC | `…bak-pre-b2-20260916`（25,641 B） |
| `D:\下载的文件\学习工作台\assets\tpl-preview.js` | 32,863 | **70,754** | 09-16 12:18 | 2026-09-16 06:33 UTC | `…bak-pre-b2-20260916`（32,863 B） |

**新增（非独占，QA 工具）**：
- `D:\下载的文件\学习工作台\tools\qa\b2_pptlib_probe.js`（B2 专项体检脚本，本次新建）

**换行符**：三文件改后 `lone_lf = 0`，CRLF 与原文件一致（`PPT版式库.html` crlf=284 / `data-ppt-templates.js` crlf=888 / `tpl-preview.js` crlf=1525），**未被改混**。

**未越界确认**：`assets/ppt-tips.js`（A5 线）、`演示.html`、`assets/data-ppt-tips.js`（B5 线）**本任务未执行任何写入**（其 mtime 变化来自同批次并行队友，非本任务所为）。本任务全部写入仅限上表 3 个独占文件 + 3 个备份 + 1 个 QA 脚本 + 若干 `_b2_*` 临时产物。

---

## 三、升级内容清单（含行号）

### 3.1 `assets/data-ppt-templates.js`

- **保留区 L1–L565**：A7「实战模板库」原 `window.MINI_BANK['ppt-templates']`（v2，5 套模板），**一字节未删未改**（仅头部注释由 v2 升为 v3 加了一段 B2 说明）。
- **新增区 L566 起（文件尾部）**：新增数据集 `window.MINI_BANK['ppt-layout-lib']`（v3），字段严格对齐需求原文第七节：
  - 每条含 `id / name / category / previewImage / pptFile / useCase / designPoints[]`，另加 `palette`（占位骨架着色）与 `ph`（占位骨架布局键）。
  - **`previewImage` 一律 `''`**（空串）——**绝不写 `assets/images/ppt-templates/xxx.png` 这类会 404 的路径**。
  - **`pptFile` 一律 `null`** —— 本波无真实 .pptx，下载按钮按原文显示「制作中」置灰。
  - **共 16 条**：封面页 4（`cover-01`~`04`）/ 目录页 3（`toc-01`~`03`）/ 过渡页 3（`section-01`~`03`）/ 内容页 6（`content-01`~`06`）。
  - 顶层含 `cats`（4 分类顺序）、`counts`（分类计数）、`imageDir`/`fileDir`（后续补真图锚点说明）、`credit`、`meta.total=16`。
- **数据质量**：全部内容原创，为经验性排版建议，**无任何臆造权威结论 / 数据断言**。

### 3.2 `assets/tpl-preview.js`

- **保留区 L1–L711**：A7 渲染器（`window.PPTV2['ppt-templates']` + `window.TplPreview`）**完全未动**。
- **新增区 L712 起（文件尾部）**：独立 IIFE，零污染命名（全部 `Xt_` 前缀），对外暴露：
  - `window.XtPptLib.mount(rootEl)` —— 版式库完整 UI 挂载
  - `window.XtPptLib.openPreview(id)` / `placeholderSvg(ph,palette)` / `filtered()`
  - `window.PPTV2['ppt-layout-lib']` —— 与 A7 同构的注册方式
- 实现要点（逐条对应需求原文）：
  1. **卡片 4 层**：预览图 → 名称（加粗）→ 适用场景（灰字、`-webkit-line-clamp:2` 两行截断）→ 三按钮（收藏 / 预览 / 下载）。
  2. **点预览图 = 点预览按钮**（需求 二.第1层）：卡片预览图点击事件委托 → 打开遮罩层。
  3. **三按钮交互**：
     - 收藏：空心↔实心（`.xtpl-btn-fav.on` 黄色）、`hover` 放大 1.1 倍；写 `localStorage`；在「我的收藏」页签取消收藏 → 卡片立即消失。
     - 预览：打开全屏遮罩层（遮罩 `rgba(0,0,0,.7)`，白色面板 `width:80%` / `max-width:1000px`），左侧大图 + 右侧「适用场景 + 设计要点」，底部「下载模板 / 收藏 / ← 返回」。
     - 下载：`pptFile` 为 null → 按钮置灰显示「制作中」`disabled`，**不弹 alert**；有文件 → `<a download>` 标签实现。
  4. **遮罩三种关闭**：✕ 按钮 / 点击遮罩空白（`data-act="close"` 的 `.xtpl-modal-mask`）/ ESC 键；**点白色内容面板不关闭**（反向已实测）。
  5. **分类筛选页签**：`全部 · 封面页 · 目录页 · 过渡页 · 内容页 · ⭐ 我的收藏`；选中高亮；筛选后卡片实时刷新；收藏状态保留。
  6. **空状态**：收藏为空 → 「还没有收藏的版式，去逛逛吧」+「查看全部版式」按钮；搜索无果 → 「未找到匹配的版式，换个关键词试试」+「清空搜索」按钮。
  7. **搜索框**：`placeholder="搜索版式名称/关键词"`、`width:220px` 圆角；`oninput` 实时过滤；匹配 `名称 + 适用场景 + 设计要点`；不区分大小写；与分类筛选**同时生效**。
  8. **禁止事项遵守**：全程 **无 `alert` / `confirm` / `prompt`**；必显示大图（占位骨架）；仅点空白才关遮罩。

### 3.3 `PPT版式库.html`

- **L97 起 `#page-ppt-layouts`**：
  - **新增卡片 A**：`card-title` 标题 + 副标签「封面 / 目录 / 过渡 / 内容」，主体为挂载点 `<div id="pptLibMount"></div>`（新组件渲染入口）。
  - **保留卡片 B**：旧「经典版式速览」卡片（`#layoutFilter` / `#layoutList`）原样保留，仅标题由 `PPT版式库` 改为 `经典版式速览`，标签文字未动。
- **L~245（`</body>` 前）**：
  - 新增 `<script src="assets/data-ppt-templates.js?v=20260916L" defer>`
  - 新增 `<script src="assets/tpl-preview.js?v=20260916L" defer>`
  - 新增内联引导脚本：多路兜底（`DOMContentLoaded` / `load` / 立即）挂载 `window.XtPptLib.mount(#pptLibMount)`，`mounted` 标志去重；渲染异常时写可见提示，**绝不 alert**。
  - **加载顺序**：数据 → 渲染器 → 引导，且三者均晚于 `assets/app.js`（渲染器复用 `window.lsKey` / `xtToast`）。

> 脚本引用均带 `?v=20260916L`（与页面既有版本戳一致，**未 bump**）。

---

## 四、占位图策略与替换锚点

### 策略
**所有版式缩略图/大图位置一律用「内联 SVG 版式示意骨架」绘制**，绝不引用不存在的文件：
- 由 `Xt_placeholderSvg(ph, palette)` 按 `item.ph` 渲染 16 种版式示意（`viewBox 0 0 320 180`，16:9），用不同灰度的矩形/圆/线示意「标题位 / 正文位 / 图位 / KPI 位 / 节点」。
- 着色取该条 `palette`（`main`/`accent`/`bg`/`text`），深浅用 `rgba` 推导。
- 真图存在时（`previewImage` 非空）自动改用 `<img>`；`<img>` 加载失败时 `error` 捕获（capture 阶段）**回退为内联 SVG 占位**，杜绝裂图。
- 未知 `ph` 值有 `default` 兜底，保证**永不空白**。

### 后续补真图时的替换锚点
| 位置 | 锚点 | 替换动作 |
| --- | --- | --- |
| 版式缩略图 | `data-ppt-templates.js` 每条 item 的 `previewImage` 字段（当前均为 `''`） | 填入真实路径 `assets/images/ppt-templates/<id>.png`（先建目录） |
| 卡片预览图容器 | `tpl-preview.js` → `Xt_previewBox()` 输出的 `<div class="xtpl-pv" data-ph=… data-id=…>` | 无需改；`previewImage` 非空即自动渲染 `<img>` |
| 预览遮罩大图 | 同上函数 `mode='large'` 分支 `.xtpl-pv-lg` | 无需改，共用同一降级逻辑 |
| 下载文件 | 每条 item 的 `pptFile` 字段（当前均为 `null`） | 填入 `assets/ppt-files/<id>.pptx`；按钮自动由「制作中」变「下载」 |
| 占位骨架键 | 每条 item 的 `ph` 字段 | 换真图后 `ph` 可保留作降级兜底，不必删 |
| 目录约定 | 数据顶层 `imageDir` / `fileDir` | 与需求原文第八节目录结构一致：`assets/images/ppt-templates/`、`assets/ppt-files/` |

---

## 五、实测结果

### 5.1 页面体检（标准工具）
命令：
```
QA_PORT=8913 node "D:/下载的文件/学习工作台/tools/qa/page_check.js" "PPT版式库.html" "封面页,目录页,过渡页,内容页,我的收藏,设计要点"
```
输出头显示 **`本脚本自启的本地静态服务（端口 8913）`** —— 可信（非复用旧服务）。
结论：页面正常加载；`封面页/目录页/过渡页/内容页/我的收藏/设计要点` 六关键词**运行时 DOM 全部 = true**；`.card 数量 = 2`（新库卡 + 旧速览卡）；`[class*=card] = 37`（含 16 张版式卡）；`button 数量 = 75`。唯一运行时告警为 jsdom 自身 `Not implemented: navigation to another Document`（来自页面既有导航链接，非本任务代码）。

### 5.2 B2 专项体检（本任务脚本）
命令：`QA_PORT=8916 node tools/qa/b2_pptlib_probe.js`，输出头为自启服务（可信）。逐项结果：

| 检查项 | 结果 |
| --- | --- |
| `#pptLibMount` 存在 / `.xtpl-wrap` | ✅ true / 1 |
| 卡片渲染 | ✅ `.xtpl-card = 16` |
| 占位骨架 | ✅ `.xtpl-ph-svg = 16`；真图 `.xtpl-pv-img = 0`（应然） |
| 空卡片 | ✅ 无名称 0 / 无占位图 0 / 完全空 0 |
| 分类页签 | ✅ `全部 \| 封面页 \| 目录页 \| 过渡页 \| 内容页 \| ⭐ 我的收藏` |
| 分类过滤计数 | ✅ 全部 16 / 封面页 4 / 目录页 3 / 过渡页 3 / 内容页 6 |
| 我的收藏空状态 | ✅ 文案「还没有收藏的版式，去逛逛吧」+「查看全部版式」→ 点击回全部(16) |
| 搜索 | ✅ 商务→1、时间轴→1、乱码→0(空状态「未找到匹配的版式，换个关键词试试」)、清空→16 |
| 搜索×分类同时生效 | ✅ 封面页 + 搜「极简」→ 1（封面页·极简白） |
| 预览遮罩 | ✅ 标题正确 / 大图占位 true / 适用场景 true / 设计要点 4 条 |
| 关闭方式①✕ | ✅ 收起 |
| 关闭方式②ESC | ✅ 收起 |
| 关闭方式③点遮罩空白 | ✅ 收起 |
| 反向：点白色面板 | ✅ **仍展开**（不误关） |
| 收藏 | ✅ `on` 类切换生效；「我的收藏」内出现 1 张；取消收藏后→0 |
| 下载按钮 | ✅ 16/16 显示「制作中」且 `disabled=true` |
| **404 资源请求** | ✅ **0**（fetch 404 计数 = 0） |
| 未捕获异常 | ✅ 无（仅 jsdom 导航告警，非本任务代码） |

### 5.3 自检清单
| 检查 | 结果 |
| --- | --- |
| `node --check` 两个改动 js | ✅ rc=0 |
| `node tools/qa/escheck_es2017.js` | ✅ **`DONE 0`** |
| HTML `<!--`/`-->` | ✅ 12 / 12 |
| HTML `<div`/`</div>` | ✅ 117 / 117 |
| HTML `<script`/`</script>` | ✅ 15 / 15 |
| 禁用语法 Grep（`?. ?? .replaceAll( Object.fromEntries .at( 后行断言 ** …`） | ✅ 0 命中（`**` 仅出现在 JS 注释/markdown 串内，误报不改） |
| 原生 `alert(`/`confirm(`/`prompt(` | ✅ 0 命中（3 文件全查） |
| 换行符 | ✅ 未改混（三文件 lone_lf=0） |
| 无对不存在 `assets/images/ppt-tpl*` 的引用 | ✅ 0 命中（仅规划 .md 文档提及） |
| 未越界改 `ppt-tips.js` / `演示.html` / `data-ppt-tips.js` | ✅ 本任务对其零写入 |

---

## 六、遗留问题 / 需主理人裁决

1. **图片资源目录仍不存在**（`assets/images/ppt-templates/`、`assets/ppt-files/` 均无）。本任务已用内联 SVG 占位**完全规避 404 与白屏**；补真图属 **B5/资源线** 范畴，需主理人统一供给 16 张 PNG + 16 个 PPTX 后，按第四节锚点填入即可，**无需再改渲染代码**。

2. **收藏键名物理前缀（需知悉，非缺陷）**：需求原文要求 `localStorage key = ppt_favorites`。本项目既有 `window.lsKey()` 会给业务键统一加 **`@<当前账号>`** 后缀（多账号隔离约定）。因此实测物理键为 **`ppt_favorites@shared`**，逻辑名仍为 `ppt_favorites`。此为**遵从项目全局约定**的合理偏差，其它模块（如 `study_workbench_data@shared`）同此规则。**如需严格裸键 `ppt_favorites`，请裁决**——但会破坏多账号隔离一致性，不建议。

3. **新旧两套版式视图并存**：页面顶部为 B2 新版式库（4 分类 + 搜索 + 三按钮），下方保留旧「经典版式速览」（15 种，`app.js` 渲染）。两者数据来源不同（`ppt-layout-lib` vs `PPT_LAYOUTS`）。因 `app.js` 不在本任务独占清单内，**未做合并/下线**。**是否要下线旧速览区**（需改 `app.js`，属越界）请主理人裁决。

4. **未做「学习进度」右上角迁移**：需求原文第六节要求把「已学 3/15 ▾」从顶部整行移到分类标签行右上角，逻辑保留。该进度组件由 `app.js` 提供（不在独占清单），本任务在版式库内以 `.xtpl-count`（当前显示 X/16 种 · 已收藏 Y 种）提供**轻量进度展示**，未改动 app.js 原进度条。**如需严格迁移原进度组件，需授权改 `app.js`**。

5. **`.xtpl-pv-svg` 与 `.xtpl-ph-svg` 类名**：占位 SVG 类名为 `xtpl-ph-svg`（placeholder），预览容器为 `xtpl-pv`。已同步 QA 脚本，无外部依赖。

6. **`data-ppt-templates.js` 顶部 v2→v3 注释标注**：仅注释描述升级为 v3，**数据结构 `ppt-templates` key 的 `v` 值仍为 2**（保持 A7 渲染器 `d.v !== 2` 判定不变，互不影响）。

---

*报告生成：2026-09-16 · 执行人：software-engineer-任务十（B2）*
