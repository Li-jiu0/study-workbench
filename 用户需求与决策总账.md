# 用户需求与决策总账（交接版 · 2026-09-13）

> **目的**：让接手的 AI **不需要重新向用户询问任何需求**。本文件汇总用户在批次三～20260913h 期间表达过的全部需求、偏好与拍板记录。
> 配套阅读：`交接文档-批次三.md` **§十二（最新：20260913h 批现状 + 接手三步）** → §十/§十一（历史批次）→ `星途平台改进-需求清单-状态盘点-2026-09-13.md`（30 条需求真相）→ `deliverables/software-company/batch3-incremental-PRD.md` §9（需求逐条进度）。
> **规则：凡列入 §三「已拍板」的事项照办即可，不要再问用户；凡列入 §四「待拍板」的事项只提醒、不自作主张。**

---

## 一、产品定位（一句话）

「星途学习工作台」：面向大学生备考群体的多模块学习 Web 应用 —— 四级词汇/备考、行测刷题（央国企笔试）、听力训练、高情商表达/场景话术、AI 模拟面试、PPT 素材库、学习博客/动态/好友社交、个人中心。**32 个纯 HTML 页面 + 静态 assets（app.js 约 6600 行）+ FastAPI 后端 + 安卓 APK**。
**底线：不删任何现有功能、保留原有 DOM**（铁律，违约即打回）。

---

## 二、功能需求全景（速查）

- **PRD 原始需求池 R1～R7** 的逐条状态 → 见 `deliverables/software-company/batch3-incremental-PRD.md` **§9**（R1 6/6、R3 7.5/8、R4 7/7、R5 用户搁置、R6 7/7、R7 3.5/4）。
- **PRD 之外用户新增的需求**（批次四/五提出，同样有验收标准）：

| 需求 | 状态 |
|---|---|
| 词库冲 2500+（批次四） | ✅ 实际 2702，example/example2 双 100% |
| example2 第二例句全回填（批次四） | ✅ 2236/2236，58 条同义/重复例句已返工 |
| 听力三类题型各 3–5 篇（批次四） | ✅ 各 4 篇 |
| 题库双源整合（批次四） | ✅ 统一入口 `loadExamBankExt()`（覆盖层+增量） |
| 废弃存档字段清理（批次四） | ✅ 4 字段移除 + 老档静默兼容 + 不回写 |
| bump 脚本自动发现（批次四） | ✅ 0913c（自动发现+exit 3 门禁）→ 0913d（三类注入点） |
| 词库分片（批次五，阈值 >2500 词或 >600KB） | ✅ 8 分片+索引，等价 0 差异 |
| 缓存版本体系（批次五，4 步法） | ✅ ①②③④ 完成，差全局 bump `--write 20260913d` |
| root/collocation 字段补产（批次五） | 🔴 41.6%（930/2236，a–i），剩 j–z 1306 条 |
| 服务端 gzip | ✅ 3.76×（564KB→150KB） |
| 0913d/0913e 部署（批次五） | ✅ 已上线（20260913e；用户数回 6；gzip + C 方案生效） |
| 词库 root/collocation 补产（批次五尾） | ✅ 2236/2236 全覆盖，已合并进 8 分片 |
| **20260913h 批**：两副本合并 + 数据隔离修复 + B/C 删减重构 + E1 图标 149 处 | ✅ 已上线（线上版本 `20260913h`，commit `c1bb3f7`；详见 `交接文档-批次三.md` **§十二**） |

---

## 三、用户已拍板（照办，不要再问）

| 时间 | 决策 | 执行状态 |
|---|---|---|
| 批次四 | **APK 不重新打包**（跳过） | ✅；安卓离线端暂回退内置 60 题/466 词/6 场景 |
| 批次三起 | **首屏压缩维持现状**（PRD R5 搁置） | ✅ |
| 批次三起 | **HTTPS + 域名不做**（运维决策，非代码范围） | ✅ |
| 批次四 | 听力惰性条目（内置 SCENES 同名 key 不覆盖）**保持现状不删** | ✅（不删原功能铁律优先） |
| 批次四 | 词库 +663 超出约定区间**保留**（宁多不少）；len≤3 基础词（bee/toy/tax 等 19 个）**保留** | ✅ |
| 长期 | **词库/题库内容全部自撰**（中文释义、英文例句自写），不照抄词典原文（版权红线） | ✅ 立为入库红线 |
| 长期 | **泄题扫描是每批入库关卡**（`tools/qa/scan_question_leak.py` 非零退出即阻断） | ✅ 每批 0 命中 |
| 批次四上线 | **开启 gzip**（nginx 最小追加，已完成） | ✅ |
| 批次五 | 缓存版本体系走 **4 步法**（正则列候选→定保留名单→批量替换→jsdom 回归），方案 **A+A'**（JS 常量硬编码 + 索引 JSON `file` 字段脚本化带版本），**B（全局常量）放弃** | ✅ 执行中 |
| 批次五 | 词库分片**先拆 8 片、字段补产走独立补丁文件再合并**（避免并行冲突） | ✅ 模式验证有效 |
| 20260913h 批 | **功能删减 B 批次照办**：四级删「写作提升」、央国企删「数量关系/判断推理/资料分析」+「题型正确率」、高情商删「情景选择实战」、工具删「万能金句/场景话术」重复入口 | ✅ 已删并上线 |
| 20260913h 批 | **C 批次页面重构照办**：好友页菜单重排（会话→好友→群聊→申请→加好友）、广场页删说明卡+主导航提级、设置页三菜单提级 | ✅ 已上线 |
| 20260913h 批 | **「按你的意见排序执行，中间不需要再问我」**（主理人自主推进） | ✅ 本批全程照此执行 |
| 20260913h 批 | **要求多成员并行加速**（"多个成员同时执行会不会更快一点"） | ✅ 全程三线并行 |
| 20260913h 批 | 修服务器「真题模考返回按钮乒乓」bug（本地已修未部署的 P0-B 成果） | ✅ 已部署上线 |
| 长期 | **绝不 push GitHub**；本地 commit；中文路径禁止命令行 add（静默 no-op）→ 用 `git add -A`（先清临时文件）或 `git add -u`，message 走 `-F <UTF8文件>` | 铁律（20260913h 批修正） |
| 20260913h 批后 | **APK 不打包（永久）**：安卓端不追新前端，旧内置资源即可；需求13真机验证随之无限期搁置 | ✅ 用户 2026-09-13 拍板 |
| 2026-09-14 | **页面名实问题一律走「做真」，不走改名** —— 不改页面名去迁就现有内容，而是直接补真内容（真文章 / 真翻译 / 真案例 / 真模板）。**接手者不要再问"是否改名"**。见 §九 R48 | ⏳ 待开工（R48 三批） |
| 2026-09-16 | **R63（AI模型接入与模型设置页）4 处冲突逐条拍板**：① 模型清单 **16 个全上**（千帆4 + 方舟6 + OpenRouter4 + Gemini2 → **6 平台 27 模型**）；② 侧栏**保留**「当前模型」显示行，但**移除**「+ 添加模型」弹窗按钮（改为「⚙ 模型设置」跳独立页）；③ 功能分类 **7 个 funcType 为预设 + 允许用户自定义新增**；④ 新增模型配置**写进 `ai-config.js`** 并加**启动结构自检** | ⏳ 待开工（① 前置：4 个新 Key 需先实测；② 需改 `ai-service.js` 适配 Gemini） |

---

## 四、待用户拍板（接手者只提醒、不自作主张）

1. ~~APK 是否重新打包~~ → **已于 20260913h 批后用户拍板：不打包**（重申并永久生效，安卓端继续用旧内置资源；除非用户日后主动提出，接手者不得再询问此事）。
2. ~~删生产用户 id=8 / id=9~~ → **已于批次五尾执行**（连同探针号 id=10，合计删 36 行；用户数 8 → **6**，回 SOP 基线）。
3. ~~C 方案 nginx `/assets/data/` no-cache~~ → **已于批次五尾执行**（实测生效，`Cache-Control: no-cache` + gzip）。
4. 需求07 PPT 训练页重构、E1 剩余 23 处图标（需先给 icon-map.js 注册新图标）——等用户排期，不算拍板项但需确认是否开工。

---

## 五、用户工作方式偏好（多次强调，照做）

1. **「直接给出结论，不用反复验证」** —— 命令/脚本各跑一次即判定；不做多轮回读大文件、不开第二轮自审。
2. **超多线程并行** —— 文件零重叠的前提下尽量多线并行，不要串行单人作业。
3. **模型路由**（派子代理时必须按任务性质指定）：数据生产/机械执行 → `lite`；改核心代码（app.js 等）→ `default`；疑难卡住 → `reasoning`；**纯数据批关掉 Max 模式**（收益低、消耗翻倍）。
4. **app.js 单写者原则** —— 大文件同一时刻只允许一条线改；并行需求用「补丁文件/分工」化解（批次五已验证有效）。
5. **生产操作先问** —— 删用户数据、改 nginx、部署上线，三件必须用户点头。
6. **成本敏感** —— 纯扫描/清点类任务用脚本做（"无 token 消耗"），不烧模型。
7. **「中间不需要再问我，按你的意见来」**（20260913h 批明确）—— 主理人应按既定排序自主推进、不逐项确认；仅「生产操作（删数据/改 nginx/部署）」与「标为待拍板项」才停下问用户。
8. **多成员并行是显式诉求** —— 用户主动问"多个成员同时执行会不会更快一点"；派活时按「文件零重叠」拆多线并行。

---

## 六、数据质量红线（比"量"更重要）

1. **宁少而准**：搭配/词根不确定就少写（2 个也行），**严禁臆造**。
2. 每词/每题入库前过 `scan_question_leak.py`。
3. **答案分布必须均衡**（历史上出过 100% 集中在 A 的事故：`renderExamQuestion()` 渲染不乱序，`a=0` 等于永远选 A）。
4. 选项文本本身是字母 A/B/C/D 的题**跳过重排**；`tip` 字段不参与字母置换。
5. 新增数据文件 **ASCII 文件名、UTF-8 无 BOM**；中文内容可以，中文文件名不行（pscp GBK 坑）。

---

## 七、原始需求文档索引（要追溯更早的需求时读）

| 文档 | 内容 |
|---|---|
| `优化方案1.docx` / `优化方案2.docx`（项目根） | 最早两轮优化的用户需求 |
| `整改处理方案-批次三.md`（项目根） | 外部全功能体验报告 4.7 分的整改清单（批次三需求来源） |
| `batch3-incremental-PRD.md` §1–§8 | 需求池/用户故事/验收标准/约束风险/待确认问题 Q1-Q8（均已决议） |
| `batch3-incremental-PRD.md` §9 | 需求逐条进度 + 难易评估 + 剩余工作量 |
| `交接文档-批次三.md` §十 | 工程状态快照 + 8 步接手任务书 + 环境坑 |
| `手机真机打包运行方案.md` / `运行说明.md` / `增量修改说明.md`（项目根） | APK 与运行说明 |

---

## 八、本轮成果快照（2026-09-14 批，多线并行）

> 本节为 2026-09-14 批（批次四后）成果回写，对应《星途平台改进-需求清单-状态盘点-2026-09-13.md》同批状态更新。§四「待用户拍板」保持不动。

### 需求状态变化（❌ → ✅ 共 5 条）

| 需求 | 内容 | 成果要点 |
|---|---|---|
| 需求 01 | 管理员超级账号（单向可见） | 账号 `username=管理员`/`nickname=管理员`（非手机号）；users 加 `is_admin`；新增 `/api/admin/*`（overview/users/online/users{id}/feedback/reply）非管理员 403；全接口对普通用户隐形（搜索/好友/在线/公开主页均不可见）；私信放行（`can_message`=好友 OR 任一方管理员）；用户反馈管理员可见可回复；前端 `管理员.html` 观察台 + 私聊「联系管理员」入口 + 设置页「我的反馈」；后端端到端自测 12/12 通过 |
| 需求 06 | 商务礼仪及面试页图标升级 | emoji → `data-icon`（icon-map 补齐所需图标） |
| 需求 07 | PPT 训练页重构 | 新建「综合功能模块」入口收拢其余模块；「版式库」「优秀案例拆解」保留独立 |
| 需求 26 | 商务礼仪「SVG插画」失实描述修正 | 按用户拍板走「改描述」路线：`商务礼仪面试.html:131`「SVG插画」→「场景图标」 |
| 需求 28 | 简略内容项补全 | 企业定向库补「历年笔试形式/题型结构/时间分配」3 条；i人伙伴团回复深度（10 场景话术库 + LLM prompt/上下文组装）；i人沟通专区补「提前准备」「文字辅助」并镜像进 `高情商表达.html` 使其用户可见 |

**剩余未做：需求 20（同质化选择题库差异化或合并）** —— 30 条需求中唯一 ❌。

### 其它本轮成果

- **R35 遗留已修**：题库加载链接入 `fetchJSONAnywhere` + 一次性失败提示。
- **icon-map.js 补 20 图标**；**需求 02 全站 emoji 收尾 166 处**。
- **需求 27 勘误（台账与盘上不一致，2026-09-14 实测）**：`i-partner.js` 盘上实为本地模板回复，并未接真 LLM。本轮已补齐 prompt/上下文/话术库（`buildSystemPrompt` / `buildMessages`），并留兼容签名 **`window.IPartnerLLM({partner, systemPrompt, messages}, cb)`** —— 将来任何线接真 LLM，按此签名对接即自动吃到位（人设/replyStyle/coachFocus/10 条场景话术/近 10 条上下文 + 禁臆造硬约束），未接时行为与原先完全一致。

### 本轮提交（本地 commit，未 push）

| commit | 内容 |
|---|---|
| `c239d61` | 需求28：mini-exam.js / i-partner.js / mini-comm.js 内容补全 + `_se5_smoke.js`（32 项断言全 PASS） |
| `f803f4f` | 需求01 超级管理员账号（前后端）+ 用户反馈管理 + 需求28 i人沟通镜像（多线合并提交） |
| `ff1c184` | 需求28 收尾：`_se5_gq_check.js` jsdom 校验（24 项断言全 PASS） |

### 本轮新立备注（供后续批次遵守）

- **数据红线在需求 28 落地实例**：企业定向库新增条目全部为通用规律表述，明确写"以当年招聘公告和笔试通知为准"，未出现具体企业名/年份/真题题型断言；话术均为可直接照说的口语模板，无空话套话。
- **备份规约**：改动前 `.bak-0913o` 后缀备份，`.gitignore` 已覆盖，不进 git。

---

## 九、用户口述需求登记（未开工 · 2026-09-14）

> 用户在对话中直接提出、**尚未排期开工**的需求统一登记在此，避免跨会话丢失。
> 编号延续批次五的 R 序列（**R44 已占用** = 2026-09-14c 私聊/管理员角色时序修复）。

| 编号 | 提出时间 | 用户原话 | 现状 | 目标 | 涉及文件 | 状态 |
|---|---|---|---|---|---|---|
| **R45** | 2026-09-14 14:21 | 「红框框住的『联系管理员』能不能设置得和好友的对话列表显示的一样」 | 私聊页「联系管理员」是 `.ac-entry`：**独立条**，位于 `#imList` 会话列表**之外**、列表上方；样式为浅绿底 + 盾牌图标 + 主色加粗文字（`私聊.html:97-102` DOM / `:326-329` CSS） | 改为**与好友会话行 `.im-sess` 同款**：圆形头像（`.im-av`）+ 昵称（`.im-n`）+ 最近一条消息或时间（`.im-sub`），融入 `#imList` 列表；保留未读角标与点击进会话行为 | `私聊.html`（DOM + `.ac-` 样式）、`assets/admin-contact.js`（`xtOpenAdminChat` / `refreshEntryBadge` / `renderEntryBadge`）、`assets/chat-local.js`（`renderChats` 行模板，作样式参照） | ⏳ 已登记，待排期 |
| **R46** | 2026-09-14 14:28 | 「（私聊页）每隔几秒闪一下」（附一份通用前端排查清单，要求核实并记录） | **已实地定位根因**：`assets/chat-local.js:2495` 的 5s 未读轮询，回调末尾 **无条件** 执行 `renderList()`（`:2566`） → `renderChats()` 每次 `box.innerHTML = …`（`:355`）**全量重建整个会话列表 DOM** → 头像重绘 + `.im-sess` transition 重放 = 周期性闪烁 | 列表**只在数据真变化时**才重渲染（快照比对后早退）；页面不可见时暂停 5s 轮询；避免整表 `innerHTML` 重建 | `assets/chat-local.js`（`renderChats:324-361`、`loadChats:233-294`、5s 轮询 `2495-2579`） | ⏳ 已记录，待排期 |
| **R47** | 2026-09-14 14:32 | 「看一下《星途平台-页面名实问题与重构优化方案-2026-09-14》，把这些需求记录下来」 | 全站 **11 个页面名实不符**：A1–A7「名字承诺技能训练，实际只是四选一选择题 / 弹窗文字」（7 个）、B1–B4「形态对但缺学练闭环」（4 个）。根因是**页面名大于实际内容 + 内容资产用弹窗承载** | 按方案 4 批次推进：**P0** 企业定向库改独立页（含时政/导引同批页面化）→ **P1** 5 处快捷改名 → **P2** PPT 四件套学练闭环 → **P3** 真题内容生产 | `assets/mini-exam.js`、`assets/mini-ppt.js`、新建 `企业定向库.html`、`央国企笔试.html`、`PPT训练.html`、`四级备考.html`、`高情商表达.html`、`商务礼仪面试.html` | ⏳ 已记录，待排期 |
| **R48** | 2026-09-14 14:41 | 「（配套）看一下《星途平台-页面做真内容生产方案-2026-09-14》，了解并记录」 | 承接 **R47** 的执行方案，且**记录了一项已拍板决策：不走「短期改名」路线，全部走「中期做真」**（真文章 / 真翻译 / 真案例 / 真模板）。覆盖 **9 个页面**，逐页给出内容量、内容来源、工作量与验收标准 | 按 **3 批**推进：第 1 批 A6 企业定向库独立页 + A7 模板骨架 + B2 设计课堂 → 第 2 批 A1 阅读 4 篇 30 题 + A2 翻译输入评分 → 第 3 批 A3 案例 + A4/A5 面试 + B1 技巧练习 | `assets/mini-exam.js`、`assets/mini-ppt.js`、新建 `企业定向库.html`、`四级备考.html`、`PPT训练.html`、`高情商表达.html`、`商务礼仪面试.html` | ⏳ 已记录，待排期 |
| **R62** | 2026-09-16 18:04 | 「导入题库应该跳转到一个页面，这个页面可以通过 word，excel 等等方式导入自定义题库」（承接同日「更多功能里的『导入题库』按钮跳转的页面不对是个 bug」，当时用户要求仅记录不执行） | **实测：入口是「页内全屏浮层」而非页面跳转** —— `更多.html:160` 卡片 `onclick="openImporterView()"` 打开 `#importerView`（`.importer-view{position:fixed;inset:0;z-index:900}`）；`window.openImporter` 缺失时兜底 `location.href='设置.html'`。**格式能力实际已够**：`assets/importer.js` 已支持 `.docx`(Word)/`.xlsx`(Excel)/`.pptx`/`.txt`/`.csv`（`.doc`/`.xls` 老格式不支持）；但 `更多.html:163` 文案只写「Excel/CSV 一键导入」、向导标题只写「Word .docx / 文本 .txt / 表格 .csv」→ **两处文案均漏报 .pptx，与真实能力不符**。`工具.html` 的入口卡**已迁走**（仅保留 `#importerView`/`#impLibs`），旧悬案 D-3 未复现 | ① 确认「独立 .html 页」还是「页内全屏浮层」可接受（用户原话是「跳转到一个页面」）；② 该页面支持 Word/Excel 等方式导入**自定义**题库（能力已有，重点是入口形态与文案对齐）；③ 入口文案与真实支持格式（docx/xlsx/pptx/txt/csv）校准 | `更多.html`（卡片 `:160-164`、`#importerView` `:187-195`、`openImporterView` `:202-225`）、`工具.html`（同构浮层，已无入口卡）、`assets/importer.js`（`accept` `:320`、格式分发 `:416-426`）；若做独立页则新增页面 + 两处入口改 `location.href` | ⏸ 已登记，待用户确认入口形态后开工 |
| **R63** | 2026-09-16 18:08 | 豆包生成的需求建议稿《星途学习工作台 - AI模型接入与模型设置页面需求文档》（用户从桌面转交并指示「你学习记录一下」，**非用户本人拍板**） | 要求在 `assets/ai-config.js` 新增 4 个平台（百度千帆 / 火山方舟 / OpenRouter / Google Gemini）共 **16 个模型**（现状 2 平台 11 模型 → 6 平台 **27 模型**）；并**新建独立「模型设置」页**（4 个 Tab：模型管理拖拽排序 / 功能分类 / 模型说明搜索 / 关于）+ 自定义模型表单（支持 OpenAI 兼容 / Gemini / 自定义格式、额外请求头、top_p）+ 侧栏「+ 添加模型」改为「⚙ 模型设置」跳转该页。原稿已归档为 `需求文档-AI模型接入与模型设置页-豆包-20260916.md`（含主理人复核批注） | **4 处与既有约束正面冲突，必须先拍板**：① 推翻交接文档 §4.1 需求D 硬约束「不改 `ai-config.js` 模型列表/参数」+ 既有工单「只用 11 个免费模型」；② 要求**删除**本轮刚完成的「侧栏模型文案跟随」（撞「不删现有功能」铁律）；③ 新的 5 类功能分类与既有 **7 个 funcType**（general/reasoning/vision/translate/longtext/interview/creative）**体系打架**（缺 longtext/interview/creative、多出「代码」）；④ 原稿自相矛盾（§4 要改 ai-config.js，§6.9 又写「不修改 ai-config.js」）。另：豆包原稿**完全未提** ES2017 老内核限制、拖拽兼容、Key 公开暴露风险；3 个新 Key 形态需实测确认 | `assets/ai-config.js`（**CRLF**）、`assets/ai-service.js`（**LF**，需加 Gemini 非 OpenAI 格式适配）、`AI.html`（CRLF）、`assets/ai-page.js`（LF）、**新建** `ai-settings.html` | ⏸ 已登记，待用户拍板 4 处冲突后开工 |

### R45 备忘（接手者注意）

1. **行为不能砍**：`admin-contact.js` 的 `.ac-badge` 未读角标（退避轮询 + 打开面板即清零）是 R43/R44 的成果，改样式时必须原样保留。
2. **顺手清遗留**：`.ac-entry` 图标 span 内残留 emoji `🛡️`（见 `交接文档-批次五-20260914b.md` §七-2）。改成头像式会话行时建议一并换掉。
3. **头像待定**：管理员对普通用户保持隐形、无公开头像字段，需先拍板头像方案 —— **固定盾牌图标头像** 还是走 `renderAvatar()` 兜底。
4. **排序待定**：会话列表目前按「群聊置顶 → 会话」排（`renderChats`），「联系管理员」融进列表后放在**固定首行**还是**按时间参与排序**，需用户确认。

### R46 备忘（已完成代码侧核实 · 2026-09-14）

> 用户转来一份**通用前端排查清单**（面向 React/Vue）。**结论：方向对，但清单里的 `setState` / 列表 `key` / `useEffect` 依赖数组在本项目全部不适用** —— 本项目是原生 JS、无框架、无虚拟 DOM。不要照着那份清单去搜 `setState`。真实根因已定位到具体行。

**实测根因链（已逐行核对）**

| 环节 | 位置 | 说明 |
|---|---|---|
| ① 定时器 | `assets/chat-local.js:2495` | `setInterval(fn, 5000)` —— 未读消息轮询 |
| ② 无条件重渲染 | `chat-local.js:2566` | 轮询回调末尾 **无条件** 调用 `renderList()`（注释自述「重新渲染会话列表」） |
| ③ 整表重建 | `chat-local.js:355` | `renderChats()` 结尾 `box.innerHTML = groupHtml + chatHtml` —— **整个列表 DOM 销毁重建** |
| ④ 视觉表现 | — | 头像重新生成 + `.im-sess` 上的 CSS transition 重放 → 每 5 秒「闪一下」 |

**旁证**：`renderChats` 自身注释（`:356-359`）已写明「每 5s 未读轮询都会重渲染」——说明团队知道会重渲染，只是**没做变化检测**。此外该函数刻意清空 `S.swipeOpen`，正是为应对频繁重建的副作用。

**顺路可修点**：`chat-local.js` 三个轮询中，2.5s 会话轮询（`:2408`）与 30s 群/在线轮询（`:2414`）**都带** `document.visibilityState !== 'visible'` 守卫，**唯独 5s 未读轮询（`:2495`）没有** —— 页面切到后台仍在每 5 秒发请求 + 重建列表。

**用户清单 → 本项目落地对照**

| 通用清单条目 | 是否适用 | 本项目落地做法 |
|---|---|---|
| 取消高频轮询，改 WebSocket/SSE 推送 | ⚠️ 部分适用 | 项目**已有** `/api/ws`（`server/wsmanager.py`），但架构定位是「仅加速、不做主干」（降级路径要求无网可用）。**不建议**把列表刷新改成纯推送 |
| 数据更新做 diff，不要直接替换 | ✅ **首选** | 轮询回调内先比对快照（未读数 / `last` / `lastId` / 置顶 / 免打扰），**无变化直接 return**，不写 `innerHTML` |
| 列表项加稳定 key | ❌ 不适用 | 无虚拟 DOM 与 key 概念；等价命题就是「整表 `innerHTML` 重建」，修法同上 |
| 请求加锁 / 页面不可见时暂停 | ✅ 适用 | 给 5s 轮询补 `visibilityState` 守卫（与另两个轮询对齐）；`/api/friends/requests` 不应每 5s 拉，可降到 30s 或与群列表轮询合并 |

**建议修法（按性价比排序，待指令后再动）**

1. **变化检测 + 早退** —— 最小改动，直接消除闪烁（推荐先做这一条）
2. 给 5s 轮询补 `visibilityState` 守卫（与 `:2408` / `:2414` 对齐）
3. `/api/friends/requests` 从 5s 降频（减少后台请求）
4. 若仍有残余闪烁，再考虑**行级 diff**（只更新变化行的 badge 文本/摘要，不动节点）

### R47 备忘（页面名实问题与重构优化方案 · 2026-09-14）

- **方案文档（用户提供）**：`星途平台-页面名实问题与重构优化方案-2026-09-14.md`（项目根目录）
- **一句话结论**：全站练习型页面几乎都是「四选一选择题」或「弹窗文字卡片」，但页面名大量使用**阅读/翻译/拆解/训练/专项/提升/实战/库**等承诺性大词 —— **名字大于内容、内容载体缺失、内容资产用弹窗承载**。

**问题页面清单（11 个）**

| # | 页面 | 名字承诺 | 实测内容 | 严重度 |
|---|---|---|---|---|
| A1 | 四级备考·阅读理解 | 篇章阅读 + 理解题 | 10 道**策略选择题，无文章** | 高 |
| A2 | 四级备考·翻译专项 | 段落汉译英产出 | 10 道四选一**表达辨析**，无翻译输入 | 高 |
| A3 | 高情商·案例拆解库 | 真实案例 + 拆解 | 8 道**情景选择题**，无案例 | 高 |
| A4 | 商务礼仪面试·面试准备 | 「面试准备」系统训练 | 8 道四选一（子标题「自测题库」相符） | 中 |
| A5 | 商务礼仪面试·面试后 | 含义模糊 | 6 道复盘选择题 | 中 |
| A6 | 央国企·企业定向库 | 「库」= 可浏览/检索的知识资产 | **弹窗**承载文字内容，无导航/进度/检索 | **最高** |
| A7 | PPT训练·实战模板库 | 真实可套用模板 | **文字清单**，无模板本体/骨架 | 中 |
| B1 | PPT·技巧提升 | 技巧提升 | 5 张知识卡，只有「看」没有「练/用」 | 中 |
| B2 | PPT·设计基础 | 知识体系 | **无教学直接考试**（8 题） | 中 |
| B3 | 央国企·时政热点 | — | 弹窗承载文字（同 A6 类） | 中 |
| B4 | 央国企·真题模考导引 | — | 弹窗承载文字（同 A6 类） | 低 |

**方案规定的三条全站框架（可直接作为验收标准）**

1. **内容形态分层规则**：「名字承诺什么，就必须提供什么」—— 库/手册/清单/指南 → 独立页可浏览可检索；训练/提升/基础/专项 → 学→练→测闭环；模板库 → 模板本体；题库/自测 → 选择题（轻量合理）；拆解/案例 → 案例正文+要点+验证题；阅读/翻译/写作/口语 → 真实材料 + 产出型任务。
2. **弹窗使用红线**：弹窗只留三类 —— ①快捷查看 ②确认操作 ③轻量小测。**内容资产型功能一律独立页面化，禁止用弹窗承载。**
3. **学练测三段式**：`[学]内容载体 → [练]交互载体 → [测]验证载体`；内容型页面至少实现「学+练」两段，测段尽量联动现有错题本。

**实施批次（方案原定，未开工）**

| 批次 | 内容 | 工作量 |
|---|---|---|
| **P0** | A6 企业定向库改独立页（+ 时政/导引同批页面化），含数据扩写 | 大 |
| **P1** | 5 处快捷改名：阅读理解→阅读策略、翻译专项→高频表达辨析、案例拆解库→情景应对、面试题库·自测、面试复盘自测 | 小 |
| **P2** | PPT 四件套：B1 技巧练习、B2 设计课堂、A7 模板骨架预览 + 套用框架 | 中 |
| **P3** | 真题内容生产：阅读三类真题题、翻译段落题、企业考点题、案例库内容 | 大（内容为主） |

#### ⚠️ 复核结论（执行前必读 —— 方案依据的副本与现状有偏差）

方案第 5 行自述「依据：线上全站体验（20260913c）+ 代码核实（**worktree main-8d0a1649**）」。而 **worktree 已于 2026-09-13 两副本合并后停用**（主项目为唯一副本），因此**方案中的行号不能直接照用**。本会话已实地复核：

| 方案说法 | 主项目实测 | 判定 |
|---|---|---|
| `mini-exam.js` `exam-company` L69 | **L69**，`mode:'info'` ✓ | ✅ 准确 |
| `mini-exam.js` `exam-politics` L80 / `exam-guide` L93 | 实测 **L83 / L96** | ⚠️ 轻微偏移 |
| A6「弹窗内 **5 条**文字，每类仅 1 小段」 | 实测已是 **8 条**（L72–79）：5 条企业 + 3 条长文（历年笔试形式 / 常见题型结构 / 时间分配与做题顺序） | ⚠️ **描述过时**（那 3 条是需求 28 补的）。问题性质不变（仍是弹窗 `info`），但「内容量」结论需更新 |
| `mini-ppt.js` `ppt-design` L5 `quiz` | **L5** ✓ | ✅ 准确 |
| `mini-ppt.js` `ppt-templates` L45 `info` | 实测 **L22**，且数据标题实为「PPT · 模板库」 | ⚠️ **行号过期**（20260913j 批次删掉 ppt-layout / ppt-chart / weekly 造成位移） |
| `mini-ppt.js` `ppt-tips` L56 `info` | 实测 **L33** | ⚠️ **行号过期** |

**结论**：方案的**诊断结论与框架依然成立**（价值高，可作改造依据），但**所有文件行号、以及部分"内容量"描述，执行前必须按主项目现状重新核实一遍**。

#### 与既有需求的交叉（避免重复施工）

- **需求 28（简略内容项补全）** 已完成过部分同类工作（企业定向库 +3 条长文、i 人专区等）→ A6/B3/B4 的"内容量"部分已被动过，改造时应在此基础上做**结构升级**而非重写。
- **需求 20（同质化选择题库差异化或合并）** 是 30 条需求里**唯一 ❌ 未做**项，与本方案 A1/A2/A3/A4/A5（名字承诺训练、实际是选择题）**主题高度重叠**，建议**合并处理**，不要拆成两批。
- 方案 §5.2「命名规范落地为硬性检查项」与项目铁律「不删现有功能、保留原有 DOM」不冲突，可纳入发布前自检。

#### 岔路口状态（2026-09-14 14:41 更新 —— 见 R48）

1. ~~A1 / A2 / A3 走哪条路~~ → ✅ **已拍板：走「中期做真」，不做改名**（用户拍板，记录于 R48 方案首部）。
2. ~~P0（企业定向库独立页）是否本轮开工~~ → ✅ **已拍板优先**，写入 R48 第 1 批。
3. 是否抽 `content-page` 通用组件（hero / 进度条 / 已学标记 / 收藏 / 页签导航）—— **尚未拍板**；R48 另提出把「翻译输入评分 / 面试开口自查 / 复盘生成」沉淀为共用 JS。

> ⚠️ **R47 与 R48 是同一件事的两段**：R47 = 诊断 + 三条框架（继续有效）；R48 = 执行方案（**取代 R47 的「短期改名」路线**）。**开工以 R48 为准。**

### R48 备忘（页面「做真」内容生产方案 · 2026-09-14）

- **方案文档**：`星途平台-页面做真内容生产方案-2026-09-14.md`（项目根）
- **🔴 已拍板决策（本份最重要的一条）**：**不走改名路线，直接把内容做实** —— 真文章、真翻译、真案例、真模板。此决策**取代 R47 的「短期改名」路线**；R47 的诊断结论与三条框架继续有效。
- **行号可信度**：本份自述基线为「合并后的主项目」（已声明 worktree 停用、以主项目代码为准）。本会话抽验**一致**：`mini-exam.js` L69 / L83 / L96 ✓、`mini-ppt.js` ppt-design L5-6 / ppt-templates L22-23 / ppt-tips L33-34 ✓。

**9 个页面的做真规格**

| 页面 | 做真成什么样 | 内容量 | 工作量 |
|---|---|---|---|
| A1 四级·阅读理解 | 真实四级阅读：选词填空 1 篇 + 长篇匹配 1 篇 + 仔细阅读 2 篇 | **4 篇 + 30 题（含解析）** | 大 |
| A2 四级·翻译专项 | 中文句 → 用户输入英文 → 参考译文 + 关键词评分 | 6 单句 + 2 段落 | 大 |
| A3 高情商·案例拆解库 | 案例：背景 → 冲突 → 处理 → 拆解要点（可复用公式） | 8 个案例（各含验证题） | 中 |
| A4 面试·面试准备 | 开口训练：高频题 → STAR 框架 + 示范回答 → 自答对比 | 8 题完整答案体系 | 中 |
| A5 面试·面试后 | 复盘工具：输入复盘记录 → 结构化改进清单 | 1 套复盘模板 | 小 |
| A6 央国企·企业定向库 | 独立页：每企业 名片 / 考情 / 自测 3–5 题 | 8 类企业 × 3–5 题 | 大（P0） |
| A7 PPT·实战模板库 | HTML/CSS 骨架预览 5 帧 + 「套用框架」 | 5 套模板骨架 | 中 |
| B1 PPT·技巧提升 | 学练闭环：每技巧 2–3 题 + 实战任务 | 5 技巧 × 2–3 题 | 小 |
| B2 PPT·设计基础 | 前置课堂 3 节（四原则 / 配色 / 字体）+ 正反例 | 3 节课，现有 8 题降为课后小测 | 中 |

**三条做真铁律（验收硬约束）**

1. **全部原创，不抄真题原文** —— 四级阅读/翻译按真题难度仿写，标注「原创改编」（版权红线）。
2. **每题必带解析** —— 正确项怎么来的、干扰项为什么错，格式与现有题库统一。
3. **保留旧内容不删** —— 策略题→「技巧速览」、表达辨析→「表达积累」；旧数据只增不减，**不调 `saveData()` 覆盖用户数据**。

**开工顺序（3 批）**

| 批次 | 内容 |
|---|---|
| 第 1 批 | A6 企业定向库独立页（已拍板优先）+ A7 模板骨架 + B2 设计课堂 |
| 第 2 批 | A1 阅读内容生产（最大块）+ A2 翻译输入交互 |
| 第 3 批 | A3 案例 + A4/A5 面试做真 + B1 技巧练习 |

**验收标准（7 条 checklist，可直接当测试用例）**

- [ ] 阅读理解能读到**真实文章**并作答（不再是纯策略选择题）
- [ ] 翻译专项能**输入译文**并获参考对照与评分
- [ ] 案例拆解库每案例有**背景 → 冲突 → 处理 → 拆解**完整四段
- [ ] 面试准备提供**框架 + 示范 + 自答对比**；面试后能生成**改进清单**
- [ ] 企业定向库为**独立页面**（非弹窗），含进度 / 已学 / 收藏 / 自测题
- [ ] 实战模板库每套有**可见骨架预览**与套用入口
- [ ] 全站无「名字承诺训练、实际只有选择题 / 弹窗文字」的页面

**数据与代码改造点（方案 §4）**

- `assets/mini-exam.js`：exam-company（L69，现 8 条）→ 富结构 `{name, profile, examInfo, quiz:[3-5题], tips}`；exam-politics（L83）/ exam-guide（L96）同批独立页面化
- `assets/mini-ppt.js`：ppt-templates / ppt-tips → info 富结构（加 preview 骨架 / practice 题）；ppt-design → 前置课堂数据
- 新增数据块：四级阅读 4 篇 30 题 / 翻译 6+2 题 + 参考译文 → `四级备考.html` 渲染升级（文章区 / 输入框）
- 新建页面：`企业定向库.html`
- 交互组件（可沉淀共用 JS）：翻译输入评分、面试开口自查、复盘生成

#### 第 1 批完工记录（2026-09-14）

- **已交付**：T00 契约基建（`assets/xt-content.js` + `PPT训练.html` 挂载点 / 注册表 / 代理分发）、T10 A6 企业定向库独立页（8 类 × 四段式 + 40 题自测，**题题带解析**）、T11 A7 模板骨架（5 套 × 5 帧 = 25 帧纯 CSS + 一键套用）、T12 B2 设计课堂（3 节课约 4000 字 + 10 组正反例 + 9 题随堂测，原 8 题降为课后小测）。
- **架构落地**：ADR-1 数据外置 + 后置覆盖（`mini-exam.js` / `mini-ppt.js` / `mini-cet.js` **全程零改动**，见下方铁律核验）、ADR-2 注册表 `PPTV2`、ADR-3（A6 独立页 / A7·B2 页内全屏视图）、ADR-4 版本戳 **`20260914e`**。
- **入口文案已同步改**（否则内容做真了、入口描述还是旧的，名实仍不符）：`CATS['ppt-design']` → 「3 节课 · 10 组正反例 · 课后小测」；`CATS['ppt-templates']` → 「5 套模板 · 25 帧骨架预览 · 可一键套用」；`PPT训练.html:172` 实战模板库 `feature-desc` 同步改为 5 套口径（原为旧 6 场景）。
- **本批实测纠正（供后续参考）**：`openPptPanel` **唯一定义在 `PPT训练.html:404`**（页尾 IIFE 顶层，非 DOMContentLoaded 内）；`assets/mini-ppt.js` 全项目 grep `openPptPanel` **零命中**。架构文档 §2 ADR-2 里的 `mini-ppt.js` 行号是**示例片段**，不是源码位置，勿再引用。

#### ⚠️ 第 3 批必办遗留（B1，勿忘）

`PPT训练.html` 的 `CATS['ppt-tips']`（PPT·技巧提升）**入口文案仍是旧的**：tag「知识卡片」、desc「六个专题技巧，点击卡片标题展开或收起阅读」。
实际数据在 `assets/mini-ppt.js` 的 `PPT['ppt-tips']`（L33-34，`mode:'info'`），**尚未做真**。
→ **第 3 批 B1 落地时必须同步改这两处文案**，否则又留一个「名实不符」。
挂载点 `PPTV2['ppt-tips']` **已由 T00 预埋**，届时只注册渲染函数即可，**不必再动 HTML**。

### R62 备忘（「导入题库」入口与格式支持 · 2026-09-16 实测）

**用户原话（两条，同一件事）**

1. 2026-09-16（带截图）：「这个更多功能里的**导入题库**按钮跳转的页面不对是个bug，现在你记录一下不用执行」
2. 2026-09-16 18:04：「导入题库应该跳转到一个页面，这个页面可以通过 word，excel 等等方式导入自定义题库」

> ⚠️ **与既有悬案的关系**：`交接文档-20260916d/e` 的 **D-3「工具页导入题库跑到博客页」**极可能是同一条，合并排查，不要当两条需求做。

**实测现状（2026-09-16，逐处定位）**

| 项 | 实测 |
|---|---|
| 更多页入口 | `更多.html:160` `<div class="morepage-card morepage-list-item" onclick="openImporterView()">`，标题「导入题库」，描述文案 **「Excel/CSV 一键导入」** |
| 工具页入口 | **已无入口卡**（`工具.html:124-126` 注释自述：导入题库 / AI面试 / 穿越英语三张卡**已迁移至「更多」页**，本页降为「无入口保留页」，仅原样保留 `<style>` / `#importerView` / `#impLibs` / `importer.js` / 内联脚本） |
| 打开方式 | **页内全屏浮层，不是页面跳转**：`.importer-view{position:fixed;inset:0;z-index:900;background:var(--bg);overflow-y:auto;display:none}` / `.importer-view.open{display:block}` |
| 兜底跳转 | `if (!window.openImporter) { location.href = '设置.html'; return; }` —— 仅向导脚本未加载时触发，**不是**「跑到博客页」的原因 |
| 向导依赖 | `assets/importer.js`：由它自建弹层外壳，再把 `#impBody` 搬进本页浮层（两页注释均写明「不改动 importer.js 任何函数」） |
| 数据落地 | `localStorage['study_workbench_imports']`，按文件名自动生成「自定义·XXX」题库，列表渲染在 `#impLibs` |
| **格式支持** | ✅ `.docx`（Word，走原生 `DecompressionStream` 解压 `word/document.xml`）、`.xlsx`（Excel，**不支持 `.xls` 老格式**）、`.pptx`（逐页提取文字，每页加【第N页】）、`.txt`、`.csv`（UTF-8 严格解码失败/出乱码则回退 GBK/GB18030/Big5）；❌ `.doc` 明确不支持（提示另存为 `.docx`） |
| **文案不符** | 入口写「Excel/CSV 一键导入」，向导第 1 步标题写「Word .docx / 文本 .txt / 表格 .csv」—— 两处均**漏报 `.pptx`**，入口还漏报 `.docx`。属 R47/R48「名实不符」同一治理范畴 |

**开工前必须向用户确认（不要自行决定）**

1. **「独立 .html 页」还是「页内全屏浮层」可接受？** 用户原话是「跳转到一个页面」，但现状是页内浮层（视觉上等同全屏页、且带返回按钮 `closeImporterView()`）。
2. **「跳到不对的页面」的复现路径**：实测两个入口的兜底都指向 `设置.html`，**本次未复现**跳到博客页；需用户补截图或说明具体入口。
3. **是否扩老格式**：现无 `.xls`（老 Excel）与 `.doc` 支持 —— 是否新增（老内核 ES2017 限制下需额外解析库、成本高），还是**只修文案**维持现有能力。

**硬约束（开工时不得违反）**

- `更多.html` 与 `工具.html` **共用同一个 `assets/importer.js`**，改一处必须同步另一处；不得改 `importer.js` 的函数签名与行为。
- `#importerView` / `#impBody` / `#impLibs` / `openImporterView` / `closeImporterView` **一律保留**（既有 DOM 契约，外部可能引用）。
- 改这两个 HTML 必须**二进制读写保 CRLF**；⚠️ `更多.html` 带 **UTF-8 BOM**，写入时不要重复加。
- 若新增独立页，页面须自包含、兼容老内核（ES2017 上限），且不得删现有页内浮层实现（不删现有功能铁律）。

### R63 备忘（AI 模型接入与模型设置页 · 豆包建议稿 · 2026-09-16）

- **原稿归档**：`需求文档-AI模型接入与模型设置页-豆包-20260916.md`（含逐处冲突分析，**开工前先读它**）
- **来源性质**：**豆包生成的建议稿**，用户转交并指示「学习记录」→ **不是用户拍板项**，4 处冲突不得自行决断。

**4 处必须用户拍板的冲突（摘要）**

1. **模型清单**：既有两条约束说「不新增模型 / 只用 11 个免费模型」，本稿要加到 **27 个（6 平台）** → 正面推翻。
2. **删功能**：本稿要删侧栏「模型 · GLM-4.6V-Flash」行，而该「文案跟随选中模型」是**本轮刚验收完成**的成果，且撞「不删现有功能」铁律。
3. **分类体系打架**：既有 **7 个 funcType** vs 本稿 **5 个分类** → 申论批改（`longtext`）、模拟面试（`interview`）、金句生成（`creative`）会**选不到模型**。
4. **稿内自相矛盾**：§4 要改 `ai-config.js`，§6.9 又说「不修改 `ai-config.js`」。

**可直接采纳的好设计**：§五 模型平铺不分组；§6.9 本地存储 + 内置只读 + 自定义覆盖 + 一键恢复默认（与项目兼容策略同向）；§七 国内优先 / 429 降级、**400 不降级** / 15 秒超时（**与交接文档 §4.1 需求 D 口径逐条一致**）；§九「不做的事」范围收敛清晰。

**已具备、不要重做**：模型列表面板（已由弹窗改为输入框上方、`z-index` 已修）、`ai-service.js` 的降级链 / 15 秒超时 / 429 会话内跳过 / 图片 MIME 按魔数推导。

**未提及但必须补的兼容约束**：ES2017 上限（Chrome 50~58）、拖拽需降级、`ai-settings.html` 需进 bump 清单、文件行尾（`ai-config.js` CRLF / `ai-service.js` LF / `AI.html` CRLF）。

**凭据风险**：`assets/ai-config.js` 是**前端公开资源**且已明文存 Key；再加 4 平台 Key 将**全网可见**，存在额度盗刷风险（现有「10 次/分钟限频」是前端限频、可绕过）。若要收口需走后端代理，会推翻「前端直连」架构决策。

#### 2026-09-16 18:08 补充：豆包详细设计稿已归档

用户转交豆包生成的《导入题库》详细设计稿 → 已归档为 **`需求文档-导入题库-豆包-20260916.md`**（含主理人复核批注）。要点：

- **与 R62 是同一需求**（本稿即详细方案），**不另开编号**。
- **差距最大的 3 处**：① 本稿要支持 `.doc` / `.pdf`，而现状 `importer.js` **只支持 `.docx`/`.xlsx`/`.pptx`/`.txt`/`.csv`**（`.doc` 明确不支持）；⚠️ 本稿**未提 `.pptx`**（现状已有，别丢）。② **存储结构会变成两套**（现状 `study_workbench_imports` 按文件名建库 vs 本稿 `question_bank_{分类名}`）→ 须先决策，切忌并行两套。③ 题目字段名不同（现状 `{q, options, ...}` vs 本稿 `{id, type, question, options, answer, analysis, category}`）→ 需映射层。
- **成本最高的 2 项**：`.pdf` 需 pdf.js 而项目**禁 CDN**（本地内联数百 KB～MB，拖慢老 WebView）；`.doc` 是 OLE2 老二进制、浏览器端无轻量解析方案 → **建议 `.doc` 不做、`.pdf` 列 P2**。
- **建议 P0 先做**：入口页面化 + Excel/CSV 表头映射 + **解析预览校对页** + 内容哈希去重 + 导入记录。
- **可直接采纳**：解析预览校对（本稿 §「解析预览页」）、去重、>500 题分片、Excel 模板下载、最近 10 次导入记录。

### R63 拍板记录（2026-09-16 · 用户逐条选择，**照办，不要再问**）

| # | 冲突 | 用户拍板 | 落地口径 / 主理人解读 |
|---|---|---|---|
| **①** | 模型清单 | **16 个全上（6 平台 27 模型）** | 照豆包稿全接：百度千帆 4 + 火山方舟 6 + OpenRouter 4 + Google Gemini 2。**前置硬条件：4 个新 Key 必须先实测**（实测不过则该项需回退）。**必然要改 `assets/ai-service.js`** 适配 Gemini 非 OpenAI 格式（URL `?key=`、`contents/parts`、`candidates[0].content.parts[0].text`）—— 该文件是 **LF 核心文件**、装着降级链/15s 超时/魔数 MIME，**高风险，须单线独占** |
| **②** | 侧栏模型行 | 用户原话：「**我说的就点击出现弹窗的模型删除按钮，然后显示当前模型的这个功能保留在侧边栏**」 | 解读＝**否决豆包稿 §6.1-1**（不删侧栏「模型 · 当前模型」显示行），**采纳 §6.1-2/3/4**（「+ 添加模型」按钮 → 改为「⚙ 模型设置」跳独立页；模型说明整合进设置页；自定义模型移到设置页右上角）。⚠️ 落地时用**替换/隐藏**而非物理删除，保留 `#aiModelBtn` / `updateModelLabel` / `getSelectedModelId` / `#cmModelPanel` 等 **DOM 与全局函数名**（铁律）。⚠️ 若解读有误请立即纠正 |
| **③** | 功能分类 | **7 类为预设 + 允许用户自定义新增** | 预设＝现有 7 个 funcType（`general` / `reasoning` / `vision` / `translate` / `longtext` / `interview` / `creative`），只做**名称产品化**（通用问答 / 深度推理 / 视觉理解 / 翻译 / 长文本处理 / 面试 / 创意生成）；**砍掉豆包稿的「代码」**（项目无代码类 AI 功能）。Tab2 的「+ 添加分类」保留给用户自建。🔴 **关键约束**：用户自建分类**必须能被 `callAI` 识别**，否则静默失效 → 须在 `ai-service.js` 加「**未注册 funcType → 回退 `general` 并留痕**」的兜底，不得静默走空 |
| **④** | 配置落点 | **写进 `ai-config.js` + 启动自检** | ① 代码层面新增 providers / builtinModels → **写进 `ai-config.js`**（⚠️ **CRLF 文件**）；② 设置页的用户操作（启用 / 排序 / 自定义模型 / 分类）→ **只写 `localStorage['ai_model_settings']`，不回写 `ai-config.js`**；③ **必须补启动结构自检**：`providers` / `builtinModels` 结构不完整就回退内置默认、**不白屏**（防「改坏配置 → 全站 AI 集体失效」的历史事故） |

**待补确认项（用户可随时纠正）**

- ② 的解读（保留显示行 + 移除弹窗按钮）为**主理人依用户原话推断**，用户未逐字确认。
- ① 的 4 个新 Key 需实测通过；实测未通过时该项需回退到「只加可用平台」。

**本次拍板解锁的后续事实**

- 需要改的共享/高风险文件：`assets/ai-config.js`（**CRLF**）、`assets/ai-service.js`（**LF**）、`AI.html`（**CRLF**）、`assets/ai-page.js`（**LF**）、**新建** `ai-settings.html`。
- 需新增 `localStorage` key：`ai_model_settings`。
- 6 个公开 Key 的安全面从 2 个扩大到 6 个 → 建议把「连续 401/429 熔断」提升到 **Key 维度**（现仅「某模型连续 2 次 429 跳过」）。
### R63 ① 实测结果（2026-09-16 · **真实联网验证**，非文档转述）

> ⚠️ **本表已被下方「R63 ① 复测与修订」取代**（初测对千帆 / Gemini 的判定有误，勿引用本表数字）。保留于此仅为留痕。

**方法**：用脚本从归档稿解析 4 个新平台的 `apiUrl`/`apiKey`（未手抄），各发**最小 chat 请求**（`max_tokens` 8~40），直连优先、失败才走环境代理；**Key 未写入任何输出文件**。

| 平台 | 稿称模型数 | **实测可用** | 结论 |
|---|---|---|---|
| **火山方舟** | 6 | **6** | ✅ **全部 200**，6 个模型 ID **逐个验证有效** |
| **OpenRouter** | 4 | **3** | ⚠️ `google/gemma-4-31b-it:free` → **400「User location is not supported for the API use」**（地区限制，中国 IP 被拒）；其余 3 个 200 |
| **百度千帆** | 4 | **0** | ❌ **403 `account_overdue`「Access denied due to overdue account」→ 账号欠费**，4 个全不可用 |
| **Google Gemini** | 2 | **0** | ❌ 直连失败；走环境代理亦 **超时**（`URLError: timed out`）→ 当前网络**不可达** |
| **合计** | **16** | **9** | ⇒ 现有 11 + 9 = **20 个模型 / 4 个平台**（≠ 稿称的 27 / 6） |

**✅ 方舟 6 个模型 ID 逐验通过**：`deepseek-v4-flash-ga-260731` / `doubao-seed-2-0-mini-260428` / `deepseek-v4-1-flash-260910` / `deepseek-v4-pro-ga-260813` / `doubao-seed-2-1-pro-260915` / `glm-5-3-flash-260828`

**✅ 与稿子说法相反的一处**：OpenRouter **直连即可达**（未走任何代理即 200）→ **并不需要梯子**；真正不可达的只有 Gemini。

**🔴 新发现的产品缺陷风险（必须在 `ai-service.js` 兜底）**

`deepseek-v4-1-flash-260910` 出现 `status=200` 但 **`choices[0].message.content === null`**（输出额度被 `reasoning_content` 吃光）。首轮测 `openrouter/free` 时同现象（`max_tokens=8` 下 content=null，放宽到 40 即正常）。
→ **前端不得假设 `content` 非空**，否则会**渲染空白、看着像「AI 没反应」**（本项目历史上最难查的正是这类静默失效）。须：`content` 为空时回退 `reasoning_content`，或给出明确错误提示。

**① 的执行口径 —— 要兑现稿子的「16 个全上」，需用户先处理 3 件事**

1. **百度千帆**：账号欠费 → **需充值或换 Key**，否则该平台 4 个模型**先不接**（`fallback` 链里也不要挂它，否则每次都要白等一轮超时）。
2. **Google Gemini**：需**可用网络**（直连 + 代理均超时）→ 否则该平台先不接；同时 **`ai-service.js` 的 Gemini 适配可一并暂缓**（省掉全批风险最高的一处改动）。
3. **OpenRouter**：`google/gemma-4-31b-it:free` 受地区限制 → **换其他免费模型或删该条**（其余 3 个已验证可用）。

**可立即执行的方案（不阻塞开工）**：先接**火山方舟 6 个**（100% 验证通过），OpenRouter 接**已验证的 3 个**（共 9 个）；千帆与 Gemini 待用户解决后再补。
### R63 ① 复测与修订（2026-09-16 18:3x · **本表取代上表，以本表为准**）

**触发**：用户反馈三点 —— ①「千帆我测了 `ernie-4.5-turbo-128k` 能够使用，你再测一下」；②「Google Gemini 需要有梯子才行，可以让他走梯子代理访问外网」；③「gemma 那条删除」。

| 平台 | 稿称 | **复测可用** | 明细 |
|---|---|---|---|
| **火山方舟** | 6 | **6** | ✅ 全部 200（上一轮已逐个验证模型 ID） |
| **OpenRouter** | 4 | **3** | ⚠️ `google/gemma-4-31b-it:free` **按用户指示删除**（400 地区限制）；其余 3 个 200 |
| **百度千帆** | 4 | **2** | ✅ `ernie-4.5-turbo-32k` 200、✅ `ernie-4.5-turbo-128k` 200（**用户反馈正确**）；❌ `deepseek-v4-flash-0731` 403、❌ `glm-5.3` 403（均 `account_overdue`） |
| **Google Gemini** | 2 | **2** | ✅ 走 **Clash `127.0.0.1:7897`** 实测：`gemini-3.5-flash-lite` / `gemini-3.5-flash` 均 **200 + 正文正常** |
| **合计** | **16** | **13** | ⇒ 现有 11 + 13 = **24 个模型 / 6 个平台** |

**✅ 关键修正一：千帆不是「整个账号欠费」**
同一个 Key 下 **ERNIE 系 2 个可用、DeepSeek / GLM 系 2 个报 `account_overdue`** → 千帆是**按模型开通/计费**，不是账号级欠费。
→ 千帆的 `fallback` 链须重排为：`ernie-4.5-turbo-32k` → `ernie-4.5-turbo-128k` → 直接降级到智谱 `glm-4-flash`。**不要再挂 `deepseek-v4-flash-0731` / `glm-5.3`**，否则每次白等一轮 403。

**✅ 关键修正二：「content=null」定性由「模型缺陷」改为「边界情况」**
把 `max_tokens` / `maxOutputTokens` 放大到 256 后，方舟 `deepseek-v4-1-flash-260910` 与 Gemini 两个模型**均返回正常正文**（`finish=stop`）。
→ 之前的空正文是**推理型模型在额度过小时把预算全给了思维链**，**模型本身没坏**。
→ 但**前端兜底仍必须做**：若 `ai-config.js` 的 `maxTokens` 被设小（尤其用户自定义模型），reasoning 类模型会返回空正文 → 必须 `content` 为空时回退 `reasoning_content` 或给明确错误，**不得渲染空白**（否则表现为"AI 没反应"，本项目最难查的静默失效类）。

### 🔴 Gemini「走梯子代理」的架构真相（**重要，需用户知悉后再定**）

用户要求「让 Gemini 走梯子代理访问外网」——**Key 测通了，但代码层面做不到**：

1. **实测可用**：经本机 Clash `127.0.0.1:7897` 访问 Gemini，两个模型都 200 → **Key 与模型本身完全有效**（此前判「网络不可达」是**代理选错**，非 Key 问题）。
2. **前端 `fetch` 无法指定代理**。Gemini 能不能通，**只取决于访问者自己设备的网络**：
   - 桌面浏览器：需用户本机开了系统代理 / VPN；
   - **安卓 APK 老 WebView：不会继承电脑的代理**，手机必须自己装梯子（VPN 模式）才可能通。
3. 附带发现（代理端口真相）：**WorkBuddy 自带代理**（env `HTTPS_PROXY=127.0.0.1:62036`）访问 Google 返回 **502 Bad Gateway**，**不通**；本机唯一能通 Google 的是 **Clash `127.0.0.1:7897`**（与早前「7897 已 down」的记录相反，现已恢复；7890 / 10809 / 1080 / 10808 / 8889 均关）。

**两条出路（需用户选一个）**

| 方案 | 做法 | 代价 |
|---|---|---|
| **A（推荐）** | Gemini 作为「**需自备网络**」的可选模型：网络通的用户能用，不通的自动降级到国内模型 | 零架构变更；但相当比例用户（尤其安卓端）实际用不到 |
| **B** | **走后端代理**：FastAPI 加转发接口，由服务器出海请求 Gemini | 🔴 **推翻「前端直连」既有架构决策**；且**服务器自身必须有出海网络**；属重大变更，需单独拍板 |

**→ Gemini 的 2 个模型不作为「必接项」**：按 A 方案先接，真机（尤其安卓端）验证不通则从默认列表下架。

**→ 最终可执行模型清单（13 个新增 / 共 24 个）**

- **火山方舟 6**：`deepseek-v4-flash-ga-260731`、`doubao-seed-2-0-mini-260428`、`deepseek-v4-1-flash-260910`、`deepseek-v4-pro-ga-260813`、`doubao-seed-2-1-pro-260915`、`glm-5-3-flash-260828`
- **OpenRouter 3**：`openrouter/free`、`nvidia/nemotron-3-super-120b-a12b:free`、`nvidia/nemotron-3-ultra-550b-a55b:free`（~~`google/gemma-4-31b-it:free` 已删~~）
- **百度千帆 2**：`ernie-4.5-turbo-32k`、`ernie-4.5-turbo-128k`（~~`deepseek-v4-flash-0731`、`glm-5.3` 403 欠费，不接~~）
- **Google Gemini 2**：`gemini-3.5-flash-lite`、`gemini-3.5-flash`（标注「需自备网络」）

### R63 最终拍板（2026-09-16 18:42 · 用户逐条答复，**照办，不要再问**）

用户答复三问：①「Gemini 选 A」②「保留侧栏『模型 · 当前模型』显示行，把点击弹窗里的『+ 添加模型』按钮改成『⚙ 模型设置』，**把模型说明功能加入到模型设置里面**懂吧」③「**不能用的模型就不要加入了**」

| # | 事项 | 用户拍板 | 落地口径 |
|---|---|---|---|
| **⑤** | Gemini 走 A 还是 B | **方案 A** | Gemini 2 个模型**接入**，标「**需自备网络**」；不通时**自动降级国内模型**；**不做后端代理**（不推翻前端直连架构） |
| **⑥** | ② 解读确认 + 模型说明归属 | **确认**，并**追加**：模型说明并入模型设置页 | ① `#aiModelInfoBtn` **保留**（「模型 · XXX」，点击看介绍）；② `#aiSettingsBtn` 文案「添加模型」→「**模型设置**」、图标 plus→gear，点击跳 `ai-settings.html`；③ 模型面板内 `#aiCustomEntry`「＋ 配置自定义模型」同步改为「**⚙ 模型设置**」；④ **豆包稿 §6.5「模型说明」Tab 采纳**，但**不再单独开弹窗入口** |
| **⑦** | 模型清单终稿 | **不能用的不加入** | 最终 **13 个新增 / 共 24 个模型 / 6 平台**：方舟 6 + OpenRouter 3 + 千帆 2 + Gemini 2。**排除**：千帆 `deepseek-v4-flash-0731` / `glm-5.3`（403 欠费）、OR `google/gemma-4-31b-it:free`（400 地区限制） |

**⑦ 的连带约束（写进代码，防止白等）**

- `fallback` 链**不得包含任何未实测通过的模型**（否则每轮白等一次 403 / 超时）。
- 千帆链：`ernie-4.5-turbo-32k` → `ernie-4.5-turbo-128k` → 智谱 `glm-4-flash`。
- Gemini 属「需自备网络」档，**不放进默认自动降级链的首选位**，避免拖慢国内用户。

**⑥ 的落地边界（防止误删 + 防全局名冲突）**

- 侧栏三个按钮**顺序与 id 不变**：`#aiModelInfoBtn`（模型 · XXX）/ `#aiSettingsBtn`（模型设置）/ `#aiClearBtn`（清除所有对话）。
- `openModelIntroFromSidebar` / `openModelIntro` / `renderModelIntro` / `closeModelIntro` 及 `#modelIntroPopup` / `#modelIntroOverlay` / `#modelIntroList` **一律保留不删**（铁律：不删现有功能、保留既有 DOM id 与全局函数名），改由设置页复用。
- 🔴 **新页 `ai-settings.html` 的全局名冲突是本项最大技术风险**：`assets/ai-page.js` 顶层有大量 `var`（`MODEL_DETAILS` / `RATE_FALLBACK` / `CM_PRESETS` 等），若新页直接 `<script src="assets/ai-page.js">` 会**重复声明全局名 → 整文件 SyntaxError → 静默失效**（本项目历史事故元凶）。→ 倾向**新建 `assets/ai-settings.js`**，只复用 `ai-config.js` 的数据与 `window.callAI`；**禁止**在新 js 里用 `var/const/let` 重复声明已有全局名。

**⑤ 的架构说明（已在文档留痕，不再重复论证）**

前端 `fetch` 无法 per-request 指定代理；安卓 APK 老 WebView 不继承电脑代理 → Gemini 能否连通**只取决于访问者本机网络**。方案 A 即接受此限制，用「标注 + 自动降级」兜住体验。

### R64 备忘（模型设置页升级 · 用户桌面文档《模型升级.txt》· 2026-09-16 19:52）

**来源**：`C:\Users\ATM\Desktop\模型升级.txt`，用户指示「你看一下可以直接开工了」。14 条，实质是豆包稿 §6 的完整版 + 新增编辑/删除/拖拽/健康检查/关于页。

**需求清单（14 条归并为 8 组）**

| 组 | 条目 | 内容 |
|---|---|---|
| A | 1/2/11 | 设置页可选当前模型并同步对话页下拉；列表行选中高亮（橙色）+「当前使用」标记；拖拽排序；恢复默认；自定义模型可删（内置只能禁用） |
| B | 3/5 | 每个模型 1-5 星级，模型列表与模型说明都展示 |
| C | 4/12 | 功能分类**只按模型能力**（文本/视觉识图/推理/翻译），删除一切场景分类字样；分类可增删排序；每类多选模型按优先级；展示类下模型标签 |
| D | 6/14 | 三个 Tab 无法滚动要修；保存有 toast；开关 0.2s 过渡；移动端 Tab 横滑 |
| E | 7/9 | 「添加自定义模型」提为**第 4 个顶部 Tab**（独立添加页），表单全字段：名称/API格式(OpenAI兼容/Gemini/自定义)/端点/Key/模型ID/额外请求头/能力标签/星级/温度/最大token/速率/降级模型/说明 |
| F | 8 | 模型行加 ✏️ 编辑：内置模型可覆盖配置，自定义模型全字段可改 |
| G | 10/13 | 模型说明标响应速度（快/中/慢）；页面加载后台自动 ping 每个模型、不可用标灰；模型可用性状态显示；「关于」页（总数/已启用/自定义数/版本号） |
| H | 11 补充 | 高亮当前使用（橙框/背景） |

**主理人定稿的 6 条架构决策（开工口径，照办）**

1. **数据落点**：选中仍写既有键 `ai_selected_model`（AI.html 下拉自动同步，**这是 C 组「同步」的实现方式**）；自定义模型仍写 `ai_custom_models`（既有 schema，新增 apiFormat/extraHeaders/stars/rate/temperature/maxTokens/fallback/desc/speed 可选字段，向后兼容）；其余（disabled/order/overrides/providerOverrides/catModels/categories/health/stars）全部进 `ai_model_settings` 一个键。**不回写任何 .js 文件**。
2. **自定义模型必须真正可调**：现状发现 **latent bug** —— `ai-service.js` 的 `findModel` 只查 `builtinModels`，手选自定义模型会被**静默忽略**落到默认链。本批修：`findModel` 增查 `ai_custom_models`；`requestModel` 支持模型自带 `apiUrl/apiKey/apiFormat/extraHeaders`（无 provider 也能发）。
3. **能力分类定稿**：内置 4 类 = **文本 / 推理 / 视觉·识图 / 翻译**（分别映射路由槽 general/reasoning/vision/translate）；`model.types` 映射 general|longtext|creative|interview→文本、math|reasoning→推理、image→视觉、translate→翻译；**「代码」不内置**（维持 R63 ③「项目无代码类 AI 功能」），用户可自建。`FUNC_TYPES` 的 longtext/interview/creative 槽保留配置、不在 Tab2 单列。每类「多模型优先级」写 `catModels[funcType]`，`ai-service.js` 的 `buildChain` 运行时优先读它 —— **顺带把 R63 遗留的「自定义分类不生效」做通**（未注册 funcType 先查 catModels，有才回退 general）。
4. **星级是「参考默认值 + 可编辑」**：写进 `AI_CONFIG.modelDetails[id].stars`（旗舰 5 / pro·推理 4 / 通用 3 / mini·lite 2-3），UI 明确标注「综合参考评分，可自行调整」—— 不伪称基准实测，守住数据质量红线。**响应速度** 13 个新模型用 2026-09-16 联网实测毫秒值（<1.5s 快 / 1.5-4s 中 / >4s 慢），**11 个旧模型不臆造、显示「待检测」**，由健康检查实测后回填。
5. **健康检查不烧限频**：新增 `window.aiHealthCheck(modelId)`（复用 requestModel，maxTokens=1，12s 超时，**不 recordCall、不计入 10 次/分钟限频**）；设置页后台队列每 ~6.5s 一个、结果缓存 30 分钟。⚠️ **CORS 风险如实呈现**：ark/千帆/OR/Gemini 的浏览器跨域未实测过，失败时区分「网络/跨域」与「HTTP 错误」展示，不误导。
6. **场景字样删除范围**：仅 `ai-config.js`（FUNC_TYPES 7 个 desc + L508 自检默认），改为能力化短语（文本对话/深度推理/视觉识图/机器翻译/长文本理解/文本生成/文本创作）。全库只有这一处真实消费方（需求文档里的历史表述不动）。

**改动文件与分工（4 线，文件零重叠）**

- 线1（设置页重构）：`ai-settings.html` + `assets/ai-settings.js` —— 5 Tab / 选中同步 / 编辑删除排序 / 添加模型全表单 / 分类管理 / 星级速度 / 关于 / 健康队列 / 滚动修复 / 交互动效
- 线2（底座）：`assets/ai-service.js` —— findModel/cloneModel/requestModel 支持自定义模型与 overrides、buildChain 读 catModels+跳 disabled、自定义分类路由、window.aiHealthCheck
- 线3（AI 页+配置）：`assets/ai-page.js`（disabled 过滤/order 排序/override 名称/modelDetails 优先）+ `assets/ai-config.js`（modelDetails 补 stars/speed、FUNC_TYPES desc 能力化、自检默认同步）
- QA：独立回归（沿用 `_r63_qa_*` 夹具 + 新功能行为验证）

### R65 备忘（AI对话页三模式+记忆+MAX升级 · 用户桌面《AI继续升级.txt》· 2026-09-16 20:19）

**来源**：`C:\Users\ATM\Desktop\AI继续升级.txt`，用户指示「把这个需求也读取一下然后继续执行」。

**现状盘点（开工前已核实，避免重复建设）**

- 上下文：**已有**。`askAI` 带最近 N 轮（`getCtxTurns`，设置弹窗有上下文长度 chips），需求②的「上下文」部分无需重做。
- MAX 模式：**开关已有**（`ai_max_mode` + aiMaxSwitch + 设置弹窗 setMaxSwitch），现状仅放大 maxTokens（cap 4000，ai-service.js L311/L1022），无深度思考联动、无提示词注入。
- 深度思考：**已有**（`ai_deep_think`），效果 = funcType 强制 `reasoning`（ai-page.js L369）；与模型选择互斥（L776）。
- 长期记忆：**无**。
- 三模式（快速/均衡/极致）：**无**。

**需求清单（3 条）**

| # | 内容 |
|---|---|
| 1 | 对话页底部模型下拉顶部加三模式：⚡快速（自动挑响应最快）/⚖均衡（速度效果兼顾）/🏆极致（能力最强），分隔线隔开手动列表；选中后输入框旁显示模式名（如「⚡快速模式」）、不显示具体模型；模式内模型全挂自动降下一模式；手动选具体模型则取消模式 |
| 2 | 增加上下文（已有，不重做）和记忆（新做长期记忆） |
| 3 | MAX 模式升级：开启深度思考、先推理再答、数学/逻辑/申论批改类一步步推导给完整过程、讲透（知识点/解题思路/易错点/举一反三）、输出长度放宽不截断 |

**主理人定稿的 4 条架构决策（开工口径，照办）**

1. **三模式**：新键 `ai_model_mode`（`''`/`'fast'`/`'balanced'`/`'ultimate'`）；模式链定义写 `ai-config.js` 的 `AI_CONFIG.modelModes`（`{fast:{label,chain:[ids]},balanced:{...},ultimate:{...}}`，线3 按 stars/speed 从 24 内置挑名单定稿）；`buildChain` 在 mode 非空时用模式链（过滤 disabled），链内用尽**自动降下一模式**（fast→balanced→ultimate→general 默认链）；手动选模型清 mode；选模式清深度思考（对齐既有互斥）；深度思考开启时模式不生效（reasoning 优先）；`ai_selected_model` 保留不动、mode 非空时 mode 优先。输入框旁模式 chip 显示模式名；气泡上的实际使用模型标注保留（透明性）。
2. **记忆**：新键 `ai_memory`（字符串数组，单条 ≤200 字、上限 50 条 FIFO）；注入点在 `ai-page.js` 的 `askAI` 构建 apiMessages 时头部插 system 消息（仅对话页，不污染申论/面试等其它模块）；管理 = 消息操作加「存入记忆」+ 设置弹窗加记忆管理（列表/逐条删/清空）；**不做自动提炼**（守公共 Key 10 次/分钟限频铁律：不能自动多发请求）。
3. **MAX 升级**：开 MAX → ① 自动开深度思考（`ai_deep_think=1`，UI 同步）② `ai-service.js` 两处 cap 4000→8000 ③ `callAI` 内 `opts.max` 为真时 prepend MAX system 提示词（逐步推导、讲透、知识点/易错点/举一反三、允许长回答不截断；messages 首条已是 system 则置其前）；关 MAX 不动深度思考状态。
4. **文件归属**：全部落在线2（`ai-service.js`：模式链+模式降级+MAX cap+MAX system 注入）与线3（`ai-page.js`+`ai-config.js`：modelModes 定义+下拉三模式 UI+模式 chip+记忆+MAX 联动深度思考）的既有独占文件内，**不新开线**；`AI.html` 不改（chip 由 ai-page.js 动态创建，参照既有 aiDeepThinkChip 样式）。

### R66 备忘（AI 对话页与模型设置第三轮优化 · 用户桌面《AI再次优化.txt》· 2026-09-16 21:23）

**来源**：`C:\Users\ATM\Desktop\AI再次优化.txt`，用户指令「一起做，开工」。共 9 组需求（密钥隐藏 + 8 组 UI/功能优化），**含存量数据清洗**（用户已确认一起做）。

**需求清单（9 组）**

| 组 | 内容 |
|---|---|
| A 密钥隐藏（用户列为第 1 条，安全项） | 编辑（内置）模型时不得显示平台内置密钥 |
| B 模式项 | 三模式行只保留标题，删除「响应最快/速度效果兼顾」类副标题，压缩高度 |
| C 菜单改名 | 入口文案「模型设置」→「添加模型」 |
| D 记忆管理挪位 | 下拉列表里移除「记忆管理」入口；记忆管理功能放进模型设置页内 |
| E 下拉溢出 | 限最大高度+内部滚动、防文字撑破容器、对齐触发按钮且不弹出可视区 |
| F 对话框 | 模型选择下拉从输入框左下角移到发送按钮左边；右侧加上下文用量「已用 1.2k / 8k」实时更新；>80% 橙、>95% 红；上限随当前模型动态；灰色小字 |
| G 连通性检测 | 「待检测」静态文字 → 可点【检测】按钮（⏳/✅/❌ 三态 + hover 提示）；列表行编辑旁 + 表单保存旁【测试连接】；弹窗极简反馈（成功/失败含简短原因）；可选【批量检测】+失败置灰 |
| H 添加模型表单 | 名称改「预设下拉（可手输）+【自定义】」；选预设自动回填端点+模型 ID；端点可手改；Key 加眼睛显示/隐藏；模型 ID 联动名称；表单也要【测试连接】 |
| I 关于页 | 删重复的「设置保存在本机」；新增【重新检测全部模型】按钮 |

**问题定位（主理人开工前已核实，行号为现状）**

- `assets/ai-settings.js:1350` —— 内置模型编辑时 Key 回落 `providers()[model.provider].apiKey`（平台密钥）；`:1365` 灌入表单；`ai-settings.html:410` 该 input 是 `type="text"` 明文可见。
- `assets/ai-settings.js:1416` —— 保存时把该 Key 写入 `overrides[id].apiKey` → **平台密钥被复制进 localStorage**；日后轮换 Key 会因残留副本静默失败。
- 架构事实（须如实告知，不推翻）：`assets/ai-config.js` L15/20/26/31/36/45 平台 Key 本身明文硬编码在静态 JS 中，前端直连架构下**无法真正保密**；真保密只能走后端转发（R63 方案 B 已否）。本批交付「UI 不回填 + 不落本地存储 + 存量清洗」，不动架构。

**主理人定稿的 6 条架构决策（开工口径，照办）**

1. **密钥口径（A）**：编辑时 Key 字段**一律不回填明文**（内置与自定义皆然）；改为状态提示行 —— 内置「🔒 已使用平台内置密钥」/ 已有覆盖「已自定义密钥（留空保持不变）」/ 自定义「已保存密钥（留空保持不变）」。**保存语义改为「空 = 保持原值」**（内置模型须先读旧 overrides 合并，不得整对象替换导致丢 key）；另提供「清除密钥覆盖」显式动作（仅内置且确有覆盖时显示）。Key 输入框改 `type="password"` + 眼睛按钮。
2. **存量清洗（A 附带，用户已确认）**：在 `ai-service.js` 的 `aiGetModelSettings` 首次调用时做一次幂等清洗 —— `overrides[id].apiKey` 与 `AI_CONFIG.providers[model.provider].apiKey` **完全相等**时删除该字段并写回；用户自填 Key 一律不动。
3. **连通性契约扩展（G）**：`window.aiHealthCheck(modelId)` 增加**第二参数临时配置** `{apiUrl, apiKey, apiFormat, extraHeaders, modelId}`（表单「测试连接」用未保存值测试，不改动已存配置）；无第二参数时行为不变。`err` 值域不变（http_XXX/cors/network/timeout/empty），**语义映射在 UI 层**（401/403→密钥无效；404→地址错误；cors→跨域受限；network→网络不可达；timeout→超时无响应）。批量/重新检测沿用 ~6.5s 间隔队列（守 10 次/分钟限频铁律）。
4. **记忆管理落点（D）**：键 `ai_memory`（字符串数组、单条 ≤200 字、上限 50 条 FIFO）**不变**；设置页在 **Tab5 关于页内**新增「🧠 记忆管理」区块（列表/逐条删/清空），**不新增第 6 个 Tab**；`ai-page.js` 移除 `#aiCustomEntry` 下方的记忆管理注入行，但 `openSettings` 及相关 DOM 一律保留不删。
5. **上下文用量口径（F）**：用量 = 当前会话 `state.messages` 内容 + 输入框当前草稿的估算值（中文 1 字 ≈ 1 token、英文数字 4 字符 ≈ 1 token，向上取整）；上限 = 当前生效模型 `maxTokens`（模式生效时取模式链首个模型；MAX 开启时 8000；无配置按 service 默认 1000）；阈值 >80% 橙、>95% 红；样式灰色小字。前缀「已用 」+ `k` 缩写（≥1000 用 1.2k）。
6. **文件归属（3 线零重叠）**：
   - 线1 = `ai-settings.html` + `assets/ai-settings.js`（A/B? 否——B 在下拉，属线2；线1 = A、C?否、D 记忆区块、G 检测按钮/弹窗/批量、H 表单预设下拉与眼睛、I 关于页）
   - 线2 = `assets/ai-page.js` + `AI.html`（B 模式副标题删除、C 入口改名、D 入口移除、E 下拉溢出、F 对话框布局与用量）
   - 线3 = `assets/ai-service.js`（A 存量清洗、G 的 aiHealthCheck 第二参数）
   - QA 独立回归（含密钥泄漏专项：断言 localStorage 内无平台密钥副本）

### R67 备忘（上下文格式 + 添加模型表单再优化 · 用户桌面《持续升级优化.txt》· 2026-09-16 21:58）

**来源**：`C:\Users\ATM\Desktop\持续升级优化.txt`（首读为空文件，用户补写后重读有效）。用户三问拍板：① 服务商按「主流公开的 API 服务商」列 ② API 格式折叠方案接受 ③ 直接开工。

**需求清单（3 组）**

| 组 | 内容 |
|---|---|
| A 上下文用量 | 格式改 `0.0% · 0 / 1000.0K 上下文已使用`（百分比 1 位小数 + 已用 + 上限 K 单位 1 位小数）；hover 悬浮说明含义；接近上限变色预警（沿用 R66 >80% 橙 / >95% 红）；排版紧凑不挤输入框 |
| B 删重复入口 | 删除设置页模型列表「＋ 添加自定义模型」按钮（`ai-settings.html:423` `#setAddCustom`；空态文案 `ai-settings.js:1065` 同步改，不得出现指向已删按钮的提示） |
| C 添加模型表单 | ① 模型名称改**服务商分组下拉**：选服务商自动回填 API 端点；模型 ID 改**下拉**（展示该服务商下全部模型），仅自定义模式开输入框；下拉限高内部滚动、选中高亮；样式与项目既有服务商下拉对齐 ② API 格式固定 OpenAI 兼容、**默认隐藏**（保留「高级设置 ▾」可展开——保住 Gemini/自定义协议入口） ③ 联动：切服务商→回填端点；切模型 ID→自动预填能力标签；表单底部「检测连接」按钮（复用 R66 的 aiHealthCheck 第二参数契约） |

**主理人定稿的 4 条架构决策（开工口径，照办）**

1. **服务商数据 = 两层，新增 `AI_CONFIG.providerGroups`**（线3 写，ai-config.js）：
   - 第一层 `builtin:true` = 已接入且实测的 6 家（智谱AI / 硅基流动 / 火山方舟(豆包) / 百度千帆(文心一言) / OpenRouter / Google Gemini），模型候选取自现有 `builtinModels`，**可直接用内置 Key**。
   - 第二层 `builtin:false` = 主流公开厂商模板（DeepSeek 官方 / 通义千问(DashScope) / Kimi(Moonshot) / OpenAI / Claude(Anthropic) / 讯飞星火）：只预填**公开的 OpenAI 兼容端点**与模型 ID 候选，`needKey:true` 且界面标注「需自备 Key，端点未经本项目实测」。⚠️ Claude 官方协议**非 OpenAI 兼容**，标注「协议不兼容，需经自定义格式/网关」，不预填端点冒充兼容。**严禁把未实测厂商伪装成已接入**。
   - 结构契约：`{key,label,builtin,apiUrl,apiFormat,needKey,note,models:[{id,name,types}]}`。
2. **API 格式**：默认隐藏 + 「高级设置 ▾」折叠展开（用户已确认接受）；隐藏状态下提交一律按 `openai`。
3. **模型 ID**：已知服务商 → `<select>` 下拉（来自 providerGroups.models）；「自定义/兼容接口」→ 切换为文本输入框开放手填；切模型 ID 时自动预填能力标签（models[].types → formTypes）。
4. **开工顺序（保归因）**：R66 QA 补验与 R67 线3（ai-config.js，不在 R66 验证范围）**并行**；R67 线1/线2（ai-settings、ai-page、AI.html——正是 R66 QA 的被测文件）**等 R66 QA 报告后放行**；R66 QA 若发现 bug，由对应 R67 线顺手修（同一文件）。最终 R67 QA 全覆盖后统一打 `?v=` 戳。

**文件归属（3 线零重叠）**：线1 = `ai-settings.html`+`assets/ai-settings.js`（B+C 组）；线2 = `assets/ai-page.js`+`AI.html`（A 组）；线3 = `assets/ai-config.js`（providerGroups 数据）；QA 独立回归（R66 补验 + R67 验证）。

---

## R68 备忘（补记 · 2026-09-17）

**移动端 AI 对话框布局**：输入区单行化、控件缩小、隐藏圆环，4 连修 commits `64c7945`/`f748547`/`d7d8f76`/`98cdac0`。已随戳 `20260916R` 上线。

## R70 备忘（补记 · 2026-09-17）

1. **设置页连通性检测修复**：`ai-settings.html` 补载 `assets/ai-service.js`（根因=该页从未加载底座，`window.aiHealthCheck` 不存在 → 误报「当前环境不支持」）；新坑=script 清单插标签漏 `</script>` 会静默吞掉后续脚本，改完必须核对 count(<script)==count(</script>)（现 9/9）。commit `f34939d`。
2. **APK 静态壳修复**：`android/build_apk.py` 曾用正则剔除各页 api.js 导致 673KB 无联网空壳 → 改二进制整页复制 + 资源白名单补全（api.js + AI 五件等）+ 打包期硬断言（入口页必须含 api.js+config.js 否则中止）；重打 versionCode 18 / versionName 1.17 / 87.3MB 完整包，apksigner 验证通过。**真机功能验收未做（唯一硬性遗留）**。

## R71 备忘（2026-09-17 12:40 用户报，截图 2 张）

**移动端「模型设置 → 模型列表」Tab 内容挤压，要求重设计为同页「模型说明」Tab 同款布局**

- 现象（截图 1）：窄屏下模型列表卡片内容竖向堆叠挤压——模型名逐字折行（如 GLM-4.5-Flash 折成 3 行）、倍率徽章/状态徽章/星级/开关/操作按钮（编辑/检测/↑↓）全部纵向堆叠、「待检测·实测」等文字逐字折行，卡片超高、一屏只装得下 1~2 张卡。
- 参照（截图 2）：同页「模型说明」Tab 布局正常——①标题行：模型名 + 倍率徽章 + 状态徽章同行；②标签行：平台 + 能力徽章；③明细行：星级/速度/说明 按「标签列 + 值列」对齐；④底部：模型 ID 灰字。信息密度高且不折行。
- **用户要求**：把「模型列表」Tab 移动端卡片布局设计成与「模型说明」Tab 一致（信息同行排布、不再竖向挤压）；必须保留模型列表自身全部功能（点行选中 / 启用开关 / 当前使用高亮 / 编辑 / 检测 / 拖拽+↑↓排序 / 星级）。
- 涉及文件（预估）：`assets/ai-settings.js`（LF，模型列表卡片渲染）、`ai-settings.html`（LF，卡片 CSS）；行尾铁律照旧，改前备份 `.bak-pre-r71-日期`。
- 状态：**已记录，未开工**（等用户指令放行）。

---

## R72 备忘（2026-09-17 13:28 用户报，共 18 项 · 第五批）—— **已记录，未开工**

> 用户原话口径：「聊天应用」= 星途站内社区/私信体系。需求分 5 组：Bug 修复（1-5）/ 页面与功能调整（6-11）/ 模型设置优化（12-16）/ 登录页调整（17-18）。
> 归并说明：第 13 项与 R71 为同一需求（模型列表移动端布局参照模型说明页），实施时合并做，不再单独立项。

### A · Bug 修复
1. 修复超级管理员回复完用户消息后，与该用户的对话从会话列表中消失的问题；并将用户们的群聊同步显示到管理员的群聊列表中。
2. 修复私信互动中好友之间无法互相备注昵称的问题。
3. 修复给 AI 发消息时偶尔误触发内置回复的问题。
4. 修复互动私聊中的 AI 好友无法正常使用的问题。
5. 修复打包成手机软件后，收到消息不在屏幕顶部弹出通知横幅的问题（APK 内通知/横幅，涉及 android 壳层与 WebView 桥接）。

### B · 页面与功能调整
6. 新建【我的文件】页面，替换主页面中的【演示】入口，并删除【演示】页面里的全部功能；同时在个人中心增加【我的文件】功能按钮，点击跳转该页。
7. 删除个人中心中的【我的作品集】功能，并入【我的文件】页面。
8. 更多功能页的【导入题库】按钮改为点击后进入独立页面，在该页面内完成导入操作。
9. 用户导入题库或文件时，自动同步添加到【我的文件】页面，并支持下拉列表按模块分类。
10. 删除更多功能页中的【穿越英语】功能。
11. 优化更多功能页中【关于】页面的内容。

### C · 模型设置优化
12. 模型列表【批量测试】改为多模型并发测试（替代现行串行 6.5s/个），提升速度；注意勿撞平台限频（现行健康检查守 10 次/分钟铁律，并发需与用户确认口径）。
13. 模型列表页面布局参照模型说明页布局样式，移动端不挤压。**＝ R71，合并实施**。
14. 【恢复默认排序/设置】按钮改为点击弹窗，弹窗内支持「按可用模型排序」与「按速率排序」，并增加一个按钮：可将排序后的可用模型连同名称映射到 AI 页对话框的模型下拉列表。
15. 重新优化并布局模型设置页【功能分类】，仅保留 文本 / 识图 / 推理 三个分类（现有第 4 类「翻译」删除，涉 FUNC_TYPES 与 catModels 迁移清洗）。
16. 升级优化打包成软件后的应用图标（APK icon）。

### D · 登录页调整
17. 登录按钮只显示「登录」两个字。
18. 将「多人在线版 · 注册登录后开启学习」改为「祝君一切安好 · 诸事顺宜」；删除「💡 多人在线版：账号密码保存在服务器数据库（密码加盐哈希），登录使用 JWT 令牌，支持静态续期。」整段文字。

### 实施备注（主理人预判，开工前与用户核对）
- 涉及面广：社区/私信（app.js + 动态/好友申请/留言板等根 HTML + **可能含 server/ 后端**——管理员会话列表、备注昵称大概率要动服务端）、更多/演示/个人中心页改造、ai-settings 系（12-15）、android 壳（5/16）、登录.html（17/18）。
- 第 5/16 项涉及 APK，改完需重打包并真机验收；第 6/7 项注意保留既有 DOM id 与全局函数名（铁律）；第 15 项需做存量 `catModelss/categories` 数据兼容清洗。
- 建议批次：多线并行 + 独占文件清单；server/ 文件若需改动单独一条线。全站 escheck → 打新戳（下一版 `20260916S`）→ 用户点头部署。
- 状态：**已记录，未开工**（等用户指令放行）。
