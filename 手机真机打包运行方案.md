# 学习工作台 · 手机真机打包运行完整方案

> 适用对象:想把「学习工作台」装到自己安卓手机(如红米 K80 / HyperOS / Android 15)上长期使用。
> 结论速览:本项目**已有**可直接出包的安卓工程(`android/` + `学习工作台-安卓App.apk`,v1.1,今天 15:17 已按最新站点重打)。
> 你要做的只有三步:① 补齐工具链中的 adb(可选,强烈建议) → ② `bash android/build-apk.sh` 出包 → ③ 传到手机安装。
> 文末有「真机验收检查清单」,逐条打勾通过即可确认「已在手机上正常安装运行」。

---

## 一、项目移动端现状分析

### 1.1 形态
`学习工作台-安卓App.apk` = **WebView 壳 + assets 内嵌整套静态站**,单机离线、数据存应用私有目录的 localStorage,无需服务器。

| 项目 | 现状 |
|---|---|
| 包名 / 版本 | `com.study.workbench` / versionCode 3 / versionName 1.2（导出补桥已实施） |
| 系统要求 | Android 5.0(minSdk 21)~ 任意新版本(targetSdk 33) |
| 签名 | v1+v2+v3 三套齐,自签名密钥 `android/workbench.keystore` |
| 内容核对 | 20 个 html + 34 个 assets(含 polish.css/chat.js/qbank.js/importer.js/voiceplayer.js/mini*.js)已确认全部在包内 |
| 关键修复 | `resources.arsc` 已强制 STORED+4 字节对齐(否则小米/Android 11+ 报 -124),已 `zipalign -c 4` 通过 |

### 1.2 手机端能力边界(务必先知道)
| 功能 | 手机端状态 |
|---|---|
| 五大学习模块 / 词汇 / 行测 / 错题本 / 博客 / 设置 / 主题 / 个人资料头像 | ✅ 离线可用 |
| AI 助手 | ⚠️ 仅「本地演示规则」模式(无密钥无网络) |
| 多人在线博客 / 好友私信 | ❌ 不在 APK 内(需连电脑端 FastAPI 后端,见第八节) |
| 笔记插图 / 头像选图(`<input type=file>`) | ✅ 已支持(onShowFileChooser) |
| **导出 JSON / 导出 Markdown** | ⚠️ 页面用 `a[download]`+blob 触发下载,**WebView 默认不接管这类下载,真机大概率点了没反应** → 需补桥(见 3.3/第八节) |
| 四级听说·口语跟读(语音识别) | ⚠️ 依赖 `webkitSpeechRecognition`,**Android WebView 多数未实现**,代码有 feature-detect 会安全降级;听力精听走系统 TTS,可用 |
| 本地朗读(en-US 单词发音) | ✅ 走系统 TTS |

---

## 二、开发环境与工具配置(一次性)

### 2.1 本机(这台 Windows)已就绪的部分
| 组件 | 路径 | 用途 |
|---|---|---|
| JDK 17 | `C:\Users\ATM\android-build\jdk\jdk-17.0.20.1+1` | javac / keytool / apksigner |
| Android Build Tools 34.0.0 | `C:\Users\ATM\android-build\sdk\build-tools\34.0.0` | aapt2 / d8 / zipalign / apksigner |
| Android Platform 35 | `C:\Users\ATM\android-build\sdk\platforms\android-35\android.jar` | 编译用 android.jar |
| 签名密钥 | `android\workbench.keystore`(密码 `workbench123`) | 见 2.3,务必备份 |
| Python 3.13 | `C:\Users\ATM\.workbuddy\binaries\python\versions\3.13.12\python.exe` | merge_apk.py |

> 换一台电脑重新搭:到 `C:\Users\ATM\android-build` 同级目录放好 jdk、build-tools、platforms 三样,再改 `android/build-apk.sh` 顶部的 `BUILD/JDK/SDK` 三行路径即可。

### 2.2 唯一缺的:platform-tools(adb)——真机安装/调试推荐补上
当前机器**没有 adb**(真机 USB 安装、看日志都要它)。二选一安装:

```bash
# 方式 A:直接下载官方 platform-tools(约 10MB),解压到 android-build/sdk/ 下
curl -L -o /c/Users/ATM/android-build/platform-tools.zip \
     https://dl.google.com/android/repository/platform-tools-latest-windows.zip
cd /c/Users/ATM/android-build && unzip -o platform-tools.zip -d sdk/

# 方式 B:用已有 cmdline-tools 的 sdkmanager 安装
/c/Users/ATM/android-build/sdk/cmdline-tools/bin/sdkmanager.bat "platform-tools"
```

验证:
```bash
/c/Users/ATM/android-build/sdk/platform-tools/adb.exe version   # 出现 Android Debug Bridge 版本号即 OK
```

### 2.3 签名密钥(最重要的资产)
- 位置:`android\workbench.keystore`,别名 `workbench`,密码 `workbench123`。
- **请把该文件备份到网盘/另一台机器**。同一个 APK 的后续升级(覆盖安装)必须用**同一把密钥**签名;密钥丢了,老用户只能卸载重装(数据清空)。
- 查看证书:apksigner 或 keytool:
```bash
BT=/c/Users/ATM/android-build/sdk/build-tools/34.0.0
"$BT/apksigner.bat" verify --print-certs "D:/网页/学习工作台/学习工作台-安卓App.apk"
```

### 2.4 一键出包
```bash
cd "D:\网页\学习工作台" && bash android/build-apk.sh
# 产出:学习工作台-安卓App.apk(脚本自动把 19 个 html 里的 api.js 引用剔除、
#       拷最新 assets、ASCII 临时目录编译、合并、zipalign、签名)
```

---

## 三、最低适配与权限配置

### 3.1 版本适配(minSdk/targetSdk 为什么这样)
- `minSdkVersion=21`(Android 5.0):满足绝绝大多数存量手机;本项目纯 WebView 静态页,21 以上能力足够。
- `targetSdkVersion=33`:对应 Android 13 的隐私/权限模型。**不要盲目升到 34+**,会引入「前台服务类型」「预测性返回」「部分照片访问」等新限制,对自用离线 App 无收益。
- Android 11+(targetSdk≥30)**强制要求 `resources.arsc` 未压缩且 4 字节对齐**,这是当初红米 K80 报「-124 安装包与系统不兼容」的根因,`android/merge_apk.py` 已修复并内建到出包流程,后续重打包无需再管。

### 3.2 当前 Manifest 权限清单(逐条说明)
`android/AndroidManifest.xml` 现状:

| 声明 | 是否必需 | 说明 |
|---|---|---|
| `INTERNET` | 单机版可不留 | 保留它是为了将来连电脑局域网后端;离线页面不会发起请求 |
| `usesCleartextTraffic="true"` | 将来连后端时需要 | 允许 http 明文访问局域网 `http://电脑IP:8000` |
| 存储权限 | 无 | 现阶段不写公共存储,所以无需申请 |
| 麦克风 / 相机 / 通知 | 无 | 页面未使用系统级权限能力(见下) |

应用内已开启的 WebView 能力:`setJavaScriptEnabled`、`setDomStorageEnabled`(localStorage 持久化必需)、`setAllowFileAccess(FromFileURLs/Universal)`(asset 内页面跳转、预留跨域)、`onShowFileChooser`(选图)、自定义 `shouldInterceptRequest`(直接喂 css/js/html 并补 MIME,保证样式脚本必加载)。

### 3.3 需要补的适配(按需,推荐做 ①,语音如在意再做 ②)
**① 文件导出补桥(推荐)**:页面「导出 JSON / 导出 Markdown」在 WebView 里点下载无反应。
做法:MainActivity 注册 `addJavascriptInterface`,把文件内容交给原生写入「下载」目录;前端 `downloadBlob()` 优先调用该桥。具体代码见第八节。做完后:设置→数据管理→导出 JSON、博客→导出 Markdown 即可在手机生成可读文件。

**② 语音评测(可选)**:若想四级听说的「口语跟读」可用,需
- Manifest 加 `<uses-permission android:name="android.permission.RECORD_AUDIO"/>`;
- WebChromeClient 覆写 `onPermissionRequest` 放行(WebView 的录音/摄像头请求不走系统弹窗,默认全拒);
- 但**Android WebView 对 `webkitSpeechRecognition` 的支持本身不保证**,真机实测不可用的概率高;代码已有 feature-detect 降级提示,不补也不影响其他功能。

**③ 其它**:页面无 Web Notification 使用,无需通知权限;头像/插图选图已支持,无需额外处理。

---

## 四、完整打包流程(改完站点 → 出新包)

1. 改站点文件(html / assets/*.js / *.css)→ 建议同步把各页面里的版本号 `?v=` 顺手 +1(仅本地开发调试用,APK 内不强制)。
2. 重打包:
```bash
cd "D:\网页\学习工作台" && bash android/build-apk.sh
```
3. 产物三重校验(必做,输出见下):
```bash
BT=/c/Users/ATM/android-build/sdk/build-tools/34.0.0
APK="D:/网页/学习工作台/学习工作台-安卓App.apk"
"$BT/apksigner.bat" verify --print-certs "$APK"        # 1) 签名 OK + 证书 CN=Study Workbench
"$BT/zipalign.exe" -c 4 "$APK" && echo ALIGN-OK        # 2) 4 字节对齐 PASS(权威判定,勿用脚本算偏移)
"$BT/aapt.exe" dump badging "$APK" | head -1           # 3) package: name='com.study.workbench' versionName='1.1'
md5sum "$APK"                                          # 记住 MD5,传手机后对比防损坏
```
4. 若改过 `MainActivity.java`/`AndroidManifest.xml`,需要同步更新 `android/java`、`android/AndroidManifest.xml` 再打包(build-apk.sh 会自动拷贝)。

---

## 五、真机安装的两种方式

### 方式 A:USB + adb(推荐,可看日志可覆盖安装)
1. 手机开「开发者选项」:设置 → 我的设备 → 全部参数与信息 → 连点「OS 版本」7 次(不同品牌入口略有差异)。
2. 进「开发者选项」打开:**USB 调试**(如需无线:无线调试 → 配对)。
3. 数据线连电脑,手机弹窗选「允许 USB 调试」(勾选始终允许)。
4. 验证与安装:
```bash
ADB=/c/Users/ATM/android-build/sdk/platform-tools/adb.exe
"$ADB" devices            # 出现 device 而非 unauthorized/offline 即连接成功
"$ADB" install -r "D:/网页/学习工作台/学习工作台-安卓App.apk"   # 输出 Success 即装好
"$ADB" shell am start -n com.study.workbench/.MainActivity      # 直接拉起 App
```

### 方式 B:文件传输 + 系统安装器(不用电脑工具)
1. USB/微信/网盘把 APK 拷到手机「下载」。
2. 点开 APK → 系统提示「禁止安装未知来源」→ **HyperOS/小米**:弹窗选「设置」→ 允许「通过此来源安装应用」;华为/Oppo/Vivo 类似(安装未知应用 → 允许来源)。
3. 点「安装」。若是覆盖升级且提示「与已安装应用签名不一致」→ 见第六节排查。

---

## 六、常见失败与排查修复

### 6.1 打包期失败
| 现象 | 根因 | 修复 |
|---|---|---|
| aapt2 报路径/文件找不到 | 原生工具不认中文路径 | build-apk.sh 已自动复制到 ASCII 临时目录;若手工跑命令务必走 `cygpath -w` |
| javac 中文乱码 | 编码 | 脚本已带 `-encoding UTF-8`,勿删 |
| `resources.arsc` 被压缩 → 装不上 -124 | targetSdk≥30 要求 STORED+4 对齐 | merge_apk.py 已强制;验证只看 `zipalign -c 4` |
| apksigner 报 keystore 错 | 密钥被移动/密码错 | 确认 `android/workbench.keystore` 存在;密码 workbench123 |
| d8 内存不足 | 默认堆小 | `d8.bat --release ...` 前设 `JAVA_OPTS=-Xmx2g`,或关掉占用内存的大程序 |

### 6.2 安装期失败(重点)
| 现象 | 根因 | 修复 |
|---|---|---|
| 「解析包错误 / 应用未安装」 | APK 传输损坏 | 手机端对比 `md5sum` 与电脑端;改用 USB 直拷 |
| 「安装失败(-124) 与系统不兼容」 | arsc 未对齐(旧包)/ 罕见机型 | 用最新 build-apk.sh 重打;仍失败则把 targetSdk 临时降到 28 试验以定位 |
| 「应用未安装」且是**覆盖升级** | 新旧包签名不一致 | 旧包非本 keystore 签名 → 只能卸载旧版重装(数据清);同密钥则用 `adb install -r` |
| 一直「禁止安装未知来源」 | 品牌安全拦截 | 按第五节方式 B 逐来源放行;HyperOS 还可在「应用设置→纯净模式/安装外部来源应用」放行 |
| 微信传完打不开 | 文件名被加后缀/重命名 | 传输后确认扩展名仍是 `.apk` |
| 提示「有风险/病毒」(部分厂商) | 自签名 App 误报 | 属正常误报,选「仍然安装」;个人自用代码可放心 |

### 6.3 运行期问题
| 现象 | 根因与修复 |
|---|---|
| 打开白屏 | asset 中文文件名解码异常 → MainActivity 已做 URLDecoder 兜底;仍白屏则 `adb logcat | grep -i chromium` 看加载了哪个 URL |
| 样式/脚本没加载 | MIME 不对被拒 → 已用 shouldInterceptRequest 显式喂 MIME;检查 guessMime 覆盖 |
| 一直停在登录页 | 登录页探测后端超时 → 单机无后端会立即降级本地;若卡住,关掉重开;也可把登录页探测超时调短 |
| 点击「导出」没反应 | WebView 不接管 blob/a[download] → 实施第八节文件导出补桥 |
| 数据莫名丢失 | localStorage 在应用私有目录,卸载即清;重装前先在设置里「导出 JSON」备份 |
| 页面按钮无响应 | 多为 JS 报错:USB 连电脑 `adb logcat -s chromium:V` 或开 `chrome://inspect` 远程调试看 console |

---

## 七、真机验收检查清单(逐条打勾 = 「已在手机上正常安装运行」)

> 建议用电脑 USB 连手机,边装边验,全程可留 `adb logcat`。

- [ ] **APK 静态校验**:apksigner verify 通过;`zipalign -c 4` PASS;versionName 与预期一致
- [ ] **安装成功**:`adb install -r` 输出 `Success`(或手机端安装器走到「完成」)
- [ ] **桌面入口**:桌面出现「学习工作台」图标,点击能进入应用
- [ ] **离线判定**:能正常出现**登录/注册页**(说明未误连后端),注册本地账号后进入首页
- [ ] **模块可用**:依次打开 四级备考 / 央国企笔试 / 高情商表达 / 商务礼仪面试 / PPT训练 任一,页面渲染正常、无白屏
- [ ] **核心功能**:四级词汇能翻卡/答题;行测能出题并判分
- [ ] **数据持久(关键)**:答题几条 + 学习博客写一篇笔记 → **强杀应用(上滑关闭)后重开** → 数据仍在
- [ ] **主题持久**:设置里切深色/浅色 → 重开仍保持
- [ ] **导出可用**(补桥后):设置→数据管理→导出 JSON,能在系统「下载」目录看到文件且内容可读
- [ ] **返回键**:逐级后退正常,首页再按返回退出应用;旋转屏幕不白屏
- [ ] **升级链路**:改一行页面文案 → `bash android/build-apk.sh` → `adb install -r` 覆盖成功,数据保留
- [ ] **离线兜底**:开飞行模式再打开,除 AI(本地演示仍可用)外功能不受影响

全部勾选 = 验收通过,可以放心在自己手机上日常使用。

---

## 八、可选增强补丁(推荐做「文件导出」)

### 8.1 前端 `assets/app.js` —— `downloadBlob()` 优先走原生桥
把 `function downloadBlob(name, text, type)` 开头改为:
```js
function downloadBlob(name, text, type) {
  if (window.AndroidBridge && window.AndroidBridge.saveFile) {   // APK 内:交原生落盘
    window.AndroidBridge.saveFile(name, text, type || 'text/plain');
    return;
  }
  const blob = new Blob([text], { type });
  const a = document.createElement('a');
  a.href = URL.createObjectURL(blob); a.download = name; a.click();
  URL.revokeObjectURL(a.href);
}
```

### 8.2 原生 `MainActivity.java` —— 注入桥 + MediaStore 写公共下载目录
```java
// onCreate 中,loadUrl 之前:
web.addJavascriptInterface(new Object() {
    @android.webkit.JavascriptInterface
    public void saveFile(final String name, final String text, final String mime) {
        final String safe = name.replaceAll("[\\\\/:*?\"<>|]", "_");
        java.io.File f = new java.io.File(getExternalFilesDir(
            android.os.Environment.DIRECTORY_DOWNLOADS), safe);
        try {
            java.io.FileOutputStream os = new java.io.FileOutputStream(f);
            os.write(text.getBytes("UTF-8")); os.close();
            runOnUiThread(() -> android.widget.Toast.makeText(MainActivity.this,
                "已保存:" + f.getAbsolutePath(), android.widget.Toast.LENGTH_LONG).show());
        } catch (Exception e) { e.printStackTrace(); }
    }
}, "AndroidBridge");
```
> 说明:写入 `getExternalFilesDir(DIRECTORY_DOWNLOADS)` **无需任何存储权限**,文件位于
> `内部存储/Android/data/com.study.workbench/files/Download`。若想要文件出现在公共「下载」且让文件管理器直接可见,
> 可改为 MediaStore(API 29+)写入 Downloads 集合,代码稍长——需要我做完整版可直接说。
> ⚠️ 注意:这是 `android/java/com/study/workbench/MainActivity.java`(安卓工程源码),不是网页用的 js;改完重跑 build-apk.sh 即可。
> 同时建议在 App 内「导出成功」提示上附上该路径,方便在手机上找到文件。

### 8.3 若要在手机上用「多人在线 / AI 后端」
App 打包的是静态站,手机与电脑同一 Wi-Fi 时,可让 App 连电脑上的 FastAPI:
- 电脑启动后端:`cd server && python -m uvicorn main:app --host 0.0.0.0 --port 8000`(关防火墙或放行 8000);
- 需要给 App 增加「后端地址设置」并把页面里 `API_BASE` 的默认 `http://localhost:8000` 改成可配置的 `http://电脑局域网IP:8000`(INTERNET + usesCleartextTraffic 均已放行)。
- 这属于下一阶段功能,当前单机自用不需要。

---

## 九、术语与命令速查
| 名词 | 含义 |
|---|---|
| adb | Android 调试桥:USB 安装/看日志/拉数据 |
| keystore / 别名 / 密码 | 签名密钥三要素;换一台电脑打包也必须用同一个 keystore |
| -124 | Android 11+ 对 arsc 对齐的安装拒绝码(已修复) |
| `adb logcat` | 实时日志;`-s chromium:V` 只看 WebView,`-s AndroidRuntime:E` 只看崩溃 |
| `chrome://inspect` | 电脑 Chrome 远程调试手机 WebView(需 USB + 调试开启),可看 DOM/console |
| zipalign / apksigner | 对齐 / 签名与校验,出包后两道必检 |

*文档版本:2026-09-09 · 配套 APK v1.1(com.study.workbench)*
