# 交付报告 · A5 —— N9-14 PPT 训练课程改进（ppt-tips.js 完成度复核收尾）

- 任务编号：**A5**（含追加 **A5b**：`PT_setHeader` 池长口径修正，见文末「八」）
- 独占文件：`D:\下载的文件\学习工作台\assets\ppt-tips.js`
- 报告日期：2026-09-16
- 复核基线：51,565 B（上批落盘 60,259 B / 未验证）→ 本批收尾 **66,940 B** → A5b 后 **67,624 B**（纯 LF，CRLF=0）
- 版本戳：**未 bump**（波内铁律）
- 备份：`assets\ppt-tips.js.bak-a5-20260916`（60,259 B，A5 改动前）、`assets\ppt-tips.js.bak-a5b-20260916`（67,624 B，A5b 改动前）

---

## 一、P0 五件清单（①–⑤，2026-09-15 P0 改造）

来源：`assets/ppt-tips.js` 文件头注释 **L18–L31**（`2026-09-15 · P0 五项改造（需求 N9-14 / W2-T4，只增不改）`）逐条写明，直接采信。

| 编号 | P0 事项 | 需求出处 | 关键落点 |
|---|---|---|---|
| ① | **正反例对比图**：数据侧 `items[i].cases[k,t,bad,good,note]`，渲染器 `PT_caseSvg()` 内联 SVG 自绘（k = key/align/image/anim/flow），不外链图片 | L19–L21 | `PT_caseSvg` |
| ② | **标记已学**：每节底部「我已学完本节」按钮，存 `xtc:lib:pt:<id>:learned`；顶部细进度条随 `#pptPanelBody` 滚动更新 | L22–L23 | `PT_learned` / `PT_updateProgress` |
| ③ | **小测 / 正反例入口**：死标签改页内可点击入口；小测优先 `XTC.renderQuiz`、缺失走本地兜底 `PT_localQuiz`（交卷给分 + 逐题解析）；正反例页内翻页对比；**一律不做全屏 modal**（ADR-3） | L24–L26 | `PT_panelHtml` / `PT_localQuiz` / `PT_caseHtml` |
| ④ | **正文卡片化**：`detail.groups` 拆成可折叠要点小卡（默认只开第一张），关键句加粗高亮 | L27–L28 | `PT_groupHtml` / `PT_hl` |
| ⑤ | **顶部导航栏去橙**：在宿主页 `演示.html` 的 `<style>` 内覆盖 `.ppt-view-top` | L29–L30 | 宿主页负责，本文件不碰 |

---

## 二、逐件完成度判定表（含行号证据 · 行号为收尾后最新值）

| 编号 | 事项 | 判定 | 行号证据 |
|---|---|---|---|
| ① | 正反例对比图（`PT_caseSvg` 内联 SVG） | **已实现** | `PT_caseSvg` L527–534；模板 `PT_svgKey` L341、`PT_svgAlign` L377、`PT_svgImage` L415、`PT_svgAnim` L449、`PT_svgFlow` L487 |
| ② | 标记已学 + 滚动进度条 | **已实现** | `PT_learned` L168；按钮渲染 L695–700；点击处理 L1177–1185；进度条 `PT_updateProgress` L1106–1128、`PT_bindProgress` L1130–1142；`ptProg` 容器 L782–786 |
| ③ | 小测 / 正反例页内入口（无 modal） | **已实现** | 入口按钮 L766–772；`PT_panelHtml` L725–740；`PT_localQuiz` L1023–1044 + `PT_bindLocalQuiz` L1046–1108；`PT_caseHtml`（页内翻页）L549–572；`pt-box2` 区块样式 L513–516 |
| ④ | 正文卡片化 + 关键句高亮 | **已实现** | `PT_groupHtml` L706–720；`PT_grpOpen` L173–180；`PT_hl` L216–225 / `PT_hlSeg` L199–214 |
| ⑤ | 顶部导航去橙 | **已实现（宿主页侧）** | 本文件 L29–L30 注释声明由 `演示.html` 覆盖 `.ppt-view-top`；本文件不含 `.ppt-view-top`（Grep 0 命中），符合职责边界 |

**①–⑤ 五件全部已实现，功能侧无需为改而改。**

---

## 三、本次改动及原因（唯一改动文件：`assets/ppt-tips.js`）

### 复核发现的关键缺陷（上批 429 中断的残留）

上批文件头 **L33–L43** 声明了 L8 收口「三件事 ⑥⑦⑧」，但**代码只落地了 ⑥ 的一半**，留下三处「声明与实现不符」的死代码：

| 声明 | 复核实际状态（收尾前） |
|---|---|
| ⑥ `PT_art()` 把 `casesArt[]` 画成 SVG | 函数存在（L373/L404），但**从未被任何渲染路径调用** → `casesArt[]`（11 组，数据侧 42,681 B 文件里实打实的 11 组）仍是**死数据** |
| ⑦ `PT_CASE_IMG` + `PT_figure()` 回填 + `PT_bindFigures()` 兜底 | **仅注释**：`PT_CASE_IMG`（L84）定义后无人读；`PT_figure()` / `PT_bindFigures()` 全文件 **0 个定义** |
| ⑧ `PT_casePool()` 合并画廊 + dup 过滤 | **仅注释**：`PT_casePool()` **0 个定义**；`PT_HIDE_DUP`（L93）定义后无人读；`setHideDup` / `imgInfo` / `setImgMap` 未导出 |

**后果**：`casesArt[]` 完全不显示；`PT_caseSvg` 只吃 `cases[]` 单一来源；README 级注释描述的能力不存在。

### 改动内容（最小变更，只增不改）

| # | 改动 | 位置 | 原因 |
|---|---|---|---|
| 1 | 新增 `PT_casePool(it, hideDup)` | L411–455 | 把 `casesArt[]`(art) + `cases[]`(tpl) 合成统一画廊池；art 在前、tpl 在后；`dup` 项由 hideDup 过滤 |
| 2 | 新增 `PT_imgSrc(id,kind,seq,mode)` | L461–466 | 按 `<id>\|<kind>\|<seq>\|<bad\|good>` 规则查 `PT_CASE_IMG` |
| 3 | 新增 `PT_figure(it,id,e,mode)` | L470–492 | 有截图出 `<img>`（下层垫手绘 SVG 兜底）；**表为空时连 `<img>` 都不生成** → 零图片请求 |
| 4 | 新增 `PT_bindFigures(root)` | L495–508 | `<img>` 加载失败 / `naturalWidth===0` 时摘掉，露出下层 SVG 兜底，不留白屏 |
| 5 | 新增 `PT_imgInfo()` / `PT_setImgMap()` | L511–524 / L1373–1383 | QA 与资源方对表；临时塞图试看 |
| 6 | 改写 `PT_side(it,e,mode)` | L536–546 | 由「只吃 `c.k` 单侧」改为吃画廊条目 `e`，接 `PT_figure` |
| 7 | 改写 `PT_caseHtml(it)` | L549–572 | 数据源由 `it.cases` 换成 `PT_casePool(it, PT_HIDE_DUP)` |
| 8 | 正反例计数改用池 | L764（`PT_scanHtml`） | 「正反例 M 组」口径与画廊一致 |
| 9 | 翻页处理改用池 | L1196（`case-prev/next`） | 翻页范围与画廊一致，标签与内容不再错位 |
| 10 | 渲染后调用 `PT_bindFigures(pane)` | L993（`PT_renderPane` 末尾） | 图片兜底挂载点 |
| 11 | 新增 `.pt-fig*` CSS | L539–543 | 图片层 `z-index:1`，手绘 SVG 垫底层 `z-index:0` |
| 12 | 导出 `casePool/art/imgInfo/setImgMap/setHideDup` | L1387–1396 | 兑现 L80–81、L43 的对外接口承诺 |
| 13 | 修正文件头 ⑥⑦⑧ 注释措辞 | L33–L43 | 让注释与实现一致（原注释描述的是未实现的能力） |

### 一处由自检**发现并修复**的真实缺陷

`setHideDup` / `setImgMap` 初版直接 `PT_renderPane(PT_cur)`，而 `PT_cur` 在首次 `render()` 前为 `null` → `PT_scanHtml(null)` 抛 `TypeError: Cannot read properties of null (reading 'items')`。已加 `if (PT_cur)` 守卫（L1388、L1393）。该缺陷由运行态桩测试（`_a5runtime.js`）捕获，非人工臆测。

---

## 四、图片资源缺失的影响面与降级策略

### 4.1 现状

- `assets/images/ppt-tips/` 目录**不存在**（实测 `os.path.isdir === False`）。
- `assets/images/` 存在，但内容全是「2027判断推理」题库 PNG，与 PPT 对比图无关。
- `PT_CASE_IMG`（L86–91）当前为**空表**；仅有的 2 条示例 key 被包在 `/* ... */` 注释内（L87–L90），**不参与运行**。

### 4.2 依赖不存在的图片资源的位置

自检 `_a5check.txt` 命中 5 行含 `assets/images/ppt-tips`，**逐行判定全部为「注释态」，无一行是可执行死链**：

| 行号 | 内容性质 | 是否运行期引用 |
|---|---|---|
| L67 | 文件头 L63–L85 注释块内「现状说明」 | 否（注释） |
| L72 | 注释块内「回填命名建议」 | 否（注释） |
| L75 | 注释块内「示例路径」 | 否（注释） |
| L88 | `PT_CASE_IMG` 表内示例 key，被 L87–L90 的 `/* */` 包住 | 否（注释） |
| L89 | 同上 | 否（注释） |

**⇒ 运行期对 `assets/images/ppt-tips/` 的真实引用数 = 0。**

### 4.3 降级策略（三层，逐层兜底）

1. **零请求层（当前生效）**：`PT_CASE_IMG` 为空 ⇒ `PT_imgSrc()` 恒返回 `''` ⇒ `PT_figure()` **不生成 `<img>` 标签**，页面**一个图片请求都不发**，从源头杜绝 404 刷屏与白屏。全部对比图由 `PT_art()`（`casesArt`）与 `PT_caseSvg()`（`cases`）现场绘制内联 SVG。
2. **单图缺失层**：一旦资源方回填了部分 key，对应位出 `<img>`，但**同一位置恒垫一份手绘 SVG**（`.pt-fig-fb`，`z-index:0`）；`PT_bindFigures()` 在 `onerror` / `complete && naturalWidth===0` 时摘掉 `<img>`，下层 SVG 自然露出。
3. **兼容层**：SVG 仅用 `<rect>/<circle>/<line>/<text>` 等老 WebView（Chrome 50~58）原生支持的元素，不用 `transform-box` 等新技术，`file://` 亦可渲染。

### 4.4 运行态实证（`_a5runtime.js`，最小 window/document 桩 + 真实两文件）

```
PptTips.version = 2.0.0
imgRequests(Image构造) = 0
data v = 2 items = 5
  tips-key   pool=5 (art=2 tpl=3)
  tips-align pool=7 (art=3 tpl=4)
  tips-image pool=5 (art=2 tpl=3)
  tips-anim  pool=5 (art=2 tpl=3)
  tips-flow  pool=5 (art=2 tpl=3)
TOTAL pool=27 art=11 tpl=16
PT_art output has <svg = true / <rect = true / viewBox 200x130 = true
imgInfo key count = 54 (期望 = pool*2 = 54)
== 断言 ==
NO_IMG_REQUEST = true     ← 零图片请求成立
POOL_MERGED_OK = true     ← 27 = 11 art + 16 tpl，与数据 meta 完全吻合
ART_WIRED = true          ← casesArt 11 组已真正接入画廊（不再是死数据）
```

> `27 组` 与文件头 L42「合成同一组正反例画廊」、L94「27 组全陈列」的表述**完全一致**，也与数据侧 `meta: { cases: 16, casesArt: 11 }`（`data-ppt-tips.js` L777）合计 27 吻合。

---

## 五、完工自检（逐项结果）

| 自检项 | 命令 / 方法 | 结果 |
|---|---|---|
| `node --check` rc=0 | `node --check "D:/下载的文件/学习工作台/assets/ppt-tips.js"` | **PASS**（rc=0） |
| escheck ES2017 = `DONE 0` | `node tools/qa/escheck_es2017.js` | **PASS**（`DONE 0`） |
| 禁用语法 Grep = 0 | 12 类正则逐条统计（`_a5check.txt`） | **PASS** — 仅 `{...}` 1 处，位于 L83 **文件头注释块内的文档示例**（`PptTips.setImgMap({...})`），非代码，属既定误报，未改 |
| 原生 `alert(/confirm(/prompt(` = 0 | 同上 | **PASS**（0/0/0） |
| 换行符纯 LF | `CRLF=0`，`bare_LF=1396` | **PASS** |
| 无死链引用 | 5 处引用全为注释态，运行期真实引用 = 0 | **PASS**（且已全走占位降级） |
| 未越界改动 | mtime 自查：`演示.html` = 09-16 12:43（未动）、`data-ppt-tips.js` = 09-15 22:17（未动） | **PASS** |

---

## 六、遗留问题 / 需主理人裁决

1. **`PT_HIDE_DUP` 过滤当前是 no-op**：数据侧 `data-ppt-tips.js` **全文无 `dup` 字段**（Grep 命中 0），故 `dup` 过滤目前不产生任何效果——27 组全陈列（与注释宣称一致）。机制已就绪，待 B5 线在数据侧补 `dup:1` 后自动生效。**若 B5 决定不打 dup 标记，本文件无需再改**（`setHideDup(false)` 亦等价）。
2. **`PT_cases()` 成为未引用函数**（L188–191）：原为 `PT_caseHtml`/翻页的数据源，现改走 `PT_casePool` 后无人调用。按「不删现有功能」铁律**保留未删**，如需清理请主理人明示。
3. **`PT_caseSvg` 的 `casesArt` 图元组未做「模板图」语义**：art 组走 `PT_art()`，tpl 组走 `PT_caseSvg(k)`，两者视觉风格不同（art 画布 200×130、tpl 画布 240×140），同池混排时**宽高比不一致**（都在 `.pt-cmp-side` 内自适应，不会溢出，但并排观感略有差异）。如需统一，属 P1 视觉打磨项，本批未动。
4. **图片资源仍缺**：`assets/images/ppt-tips/` 目录未建、实拍图未交付。当前零请求降级已保障不掉链不白屏；**拿到图后只需改 `PT_CASE_IMG` 一张表**（回填三步见 L69–L83），无需动逻辑。
5. **`演示.html` 的 ⑤（去橙）由 B5 线验证**，本报告不覆盖；本文件侧仅确认未越界。

---

## 七、产出文件清单

| 文件 | 说明 |
|---|---|
| `assets/ppt-tips.js` | **唯一改动**，60,259 B → 66,940 B(A5) → 67,624 B(A5b)，纯 LF |
| `assets/ppt-tips.js.bak-a5-20260916` | A5 改动前备份（60,259 B） |
| `assets/ppt-tips.js.bak-a5b-20260916` | A5b 改动前备份（67,624 B） |
| `交付报告-A5-ppt-tips完成度复核-20260916.md` | 本报告 |
| `_a5probe.py` 等 8 个 `_a5*` / `_a5b*` 脚本与结果 | 临时自检件，收尾后已清理 |

> 结论：**P0 五件（①–⑤）全部已实现，功能侧无需为改而改**；本批收尾的价值在于补齐上批 429 中断遗留的 **L8 ⑥⑦⑧ 死代码**（`casesArt[]` 从死数据 → 27 组画廊实装），并固化**图片资源零请求降级**，让文件头注释与实现彻底一致。

---

## 八、A5b 追加修正：`PT_setHeader` 正反例组数口径动态化

### 8.1 问题

`PT_setHeader(data)`（原 **L844–L861**，硬编码在 **L852**）报数用的是数据侧 `meta.cases`：

```js
tag.textContent = '5 专题 · ' + (m.quiz||0) + ' 道练习 · ' + (m.cases||0) + ' 组正反例 · ' + (m.tasks||0) + ' 项实战任务';
//                                                                     ^^^^^^^^^^ = 16（只含文字组 cases[]）
```

而 A5 补齐 ⑧ 后，**实际画廊池 = `casesArt`(11) + `cases`(16) = 27 组**（`data-ppt-tips.js` 无 `dup:1`，故未过滤）。标签报 16、画廊实有 27，**渲染器内部自相矛盾**（同页「正反例 M 组」入口按钮已走池，报的是 27）。

### 8.2 修正（最小变更）

1. 新增 **`PT_poolCount(data)`**（L844–L861 上方）：**优先实时池长**——遍历 `data.items` 累加 `PT_casePool(it, PT_HIDE_DUP).length`，与翻页计数、入口按钮**同一函数同一口径**；
2. 数据未就绪 / 无 `items` 时**回退** `meta.cases + meta.casesArt`（即 16+11=27），保证极端时序下仍有合理值；
3. `PT_setHeader` 改用 `var poolN = PT_poolCount(data);` 拼文案，**删除硬编码 `m.cases`**。

**选择「优先实时池长」的原因**：池函数是画廊的**唯一真相源**——入口按钮、翻页范围、画廊渲染三处都已走它。让 header 也走同一函数，才能保证「改数据不改编码」时四处口径自动一致；meta 只作 A 兜底，避免因 `render` 时 `data` 缺 `items` 而报 0。

### 8.3 验证（运行态桩，真实两文件）

```
数据 meta.cases = 16 , meta.casesArt = 11
实时池长 = 27 (art=11 + tpl=16)

== PT_setHeader 实测 ==
pptPanelTag.textContent = "5 专题 · 13 道练习 · 27 组正反例 · 6 项实战任务"

== 断言 ==
HEADER_REPORTED       = 27
LIVE_POOL             = 27
HEADER_MATCHES_POOL   = true     ← header 组数 == PT_casePool 实际长度
OLD_WAS_16            = no(已动态化)
NO_IMG_REQUEST        = true     ← 零图片请求仍成立
```

### 8.4 A5b 自检

| 自检项 | 结果 |
|---|---|
| `node --check assets/ppt-tips.js` | **PASS**（rc=0） |
| `node tools/qa/escheck_es2017.js` | **PASS**（`DONE 0`） |
| 禁用语法 Grep = 0 | **PASS**（12 类全 0；唯一 `{...}` 仍在 L83 注释块内，既定误报未改） |
| 原生 `alert(/confirm(/prompt(` = 0 | **PASS**（0/0/0） |
| 换行符纯 LF | **PASS**（CRLF=0，bare_LF=1414） |
| 运行时 header 组数 == 池长 | **PASS**（27 == 27） |
| 未越界 | **PASS**（`演示.html` = B5 线于 14:34 改，非本任务；`data-ppt-tips.js` = 09-15 22:17 未动） |
| `PT_HIDE_DUP` no-op 保持不改 | **PASS**（按主理人裁决，机制保留，数据无 `dup:1` 故目前 27 组全陈列） |

### 8.5 ⚠️ 需主理人协调的跨线口径（本任务**无权**处理）

- 本渲染器现报 **27 组**（实时池长，正确）。
- 但宿主页 `演示.html` **L609** 的静态 tag 仍写 **`16 组正反例`**（B5 线按要求先对齐为 16，mtime 14:34）。
- **⇒ 两处现不一致（16 vs 27）**。按分工铁律，`演示.html` 归 B5 线，**我未改动**。请主理人裁决并转 B5 把 L609 的 `16` 对齐为 **27**（或由 B5 决定其静态 tag 改用「27 组」口径）。
- 另注：`演示.html` L285 描述文案未含组数、L601「设计基础」为其他专题，与本口径无关。
