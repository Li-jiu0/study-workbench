# 写发帖页面改进需求（综合版 · N9-21，2026-09-15）

> 登记来源：用户 2026-09-15 综合 GPT 评价，提供「聚焦做减法」的改进方案，明确要求「记录一下」。
> 本文件为需求原文登记，**未开工**——用户铁律「不说开工不要自己执行」。
> 改造落点（已只读核实）：`学习博客.html`（广场/写发帖编辑页，含 快速模板 L204–212 / 基本信息 6 字段 L214–248 / 富文本工具栏 10 按钮 L255–272 / 左写右预览 L278 / AI 辅助写发贴 L290）+ `blog_wechat.html`（互动广场写发帖视图，同含 封面(emoji 或图片URL) L137 / 截图插图(会以 base64 存入) L148 字段，需同步清理技术术语）。

---

## 0. 一句话定位

这是三个页面（AI模拟面试 / 听说训练 / 写发帖）里**完成度最高**的，但问题是**「功能堆得太多」**——用户只是想发个帖子，被一堆表单字段和工具栏包围。
前两个页面是「功能不够、需要加」，这个页面反过来：**需要砍、需要藏、需要简化**。

---

## 1. 第一阶段：做减法，降低发帖门槛（P0，马上能改）

### 1.1 渐进式填写，不要一上来把所有字段甩给用户
- **现在**：基本信息区有 标题、分类、可见范围、封面、标签、图片 6 个字段同时摆着（`学习博客.html` L214–248）。
- **改法**：第一屏只留 3 个核心字段——**标题、正文、分类**；其余（标签 / 封面 / 图片 / 可见范围）收进底部「＋ 添加」按钮，点了才展开。
- **原则**：让用户 30 秒内能发出去，高级功能需要时再找。

### 1.2 删掉技术细节，不要给用户看实现逻辑
- **现在**：封面字段写「封面（emoji 或图片URL）」（`学习博客.html` L234 / `blog_wechat.html` L137）；图片字段写「截图/插图（本地文件，会以 base64 存入）」（L244 / L148）。
- **改法**：
  - 封面 → 「添加封面」，两个按钮 `😀 选Emoji` `🖼 上传图片`，URL 作为高级选项藏起来。
  - 图片 → 「添加图片」，下面小字「支持 PNG/JPG，最多 5 张」，**删掉「会以 base64 存入」**。
- 技术实现留后台，用户只需知道「能上传图片」。

### 1.3 顶部导航简化，当前页面不要重复高亮
- **现在**：顶部有 4 个主导航 `广场 / 我的文章 / 写发贴 / 统计`（`学习博客.html` L166–169 `sb-tab`），当前在「写发贴」还高亮；底部又有 5 个导航，两层职责重叠。
- **改法**：写发帖视图下，顶部改成 `← 返回广场` + `发布帖子` 按钮；底部导航在任务页可隐藏，减少干扰。

### 1.4 编辑器简化，不要像程序员工具
- **现在**：工具栏 10 个按钮 B / I / 行内代码 / H₂ / 列表 / 待办 / 引用 / 代码块 / 公式 / 图片 / 分割线（`学习博客.html` L255–272）。
- **改法**：只保留 5–6 个 `加粗 / 斜体 / 列表 / 引用 / 图片`；代码、公式、分割线收进「更多（⋯）」；「支持 Markdown」从常驻改成问号图标 `?`，悬停显示说明。

### 1.5 写作区域放大，减少周围干扰
- **现在**：工具栏 + 语法说明占空间，写作区不够突出。
- **改法**：正文编辑器高度加大（≥屏幕 40%）；工具栏简化后整体降高；Markdown 说明收进问号图标。

---

## 2. 第二阶段：提升体验（P1）

### 2.1 快速模板默认收起（L204–212 `ed-tpl-row`）
- **现在**：5 个模板按钮（复习提纲/错题总结/课程读书笔记/知识点梳理/今日计划）直接铺首屏。
- **改法**：改成入口「✨ 不知道写什么？试试模板」，点击才展开；模板点击自动填充标题+正文框架。

### 2.2 AI 辅助写作，让 AI 真正有用（L290 `editorAiAssist`）
- **现在**：`AI 辅助写发贴` 按钮，疑似通用聊天摆设。
- **改法**：展开场景绑定选项——`帮我起标题` / `整理成笔记` / `检查错别字` / `续写`；不做通用聊天。

### 2.3 发布前预览
- 点「发布」后给简单预览（标题/正文/分类/标签）+ `确认发布` / `返回修改`，防误发。

---

## 3. 第三阶段：长期优化（P2）

### 3.1 视觉层级梳理
- 建立三级：一级（发布按钮、正文编辑区）/ 二级（区块标题）/ 三级（字段标签、说明、辅助按钮）；当前三级差距不明显。

### 3.2 草稿自动保存
- 每 30 秒存 localStorage，下次打开提示「恢复上次未完成的草稿？」
- **注：本页已有 `ed-draft-bar`（L197–202）/ `edRestoreDraft` / `edDiscardDraft` / `edDraftState`（L271）部分实现**——开工复用，不要重做。

---

## 4. 砍掉的 / 保留的

| 砍掉 / 隐藏 | 保留 |
|---|---|
| 首屏展示所有 6 个表单字段 | ✅ 快速模板（默认收起） |
| 「封面（emoji 或图片URL）」文案 | ✅ 所有表单字段（只是藏起来） |
| 「会以 base64 存入」 | ✅ Markdown 编辑器（简化工具栏） |
| 工具栏代码/公式/分割线按钮 | ✅ 整体视觉风格（白底+灰边+橙+圆角） |
| 常驻「支持 Markdown」 | |
| 顶部 4 个 Tab（任务页改返回+发布） | |
| 首屏展开快速模板 | |

---

## 5. 约束（沿用全站铁律）

- 老 WebView 语法禁令：禁 `?.`/`??`/`replaceAll`/`Object.fromEntries`/`Array.at`/顶层 await。
- ADR-3 合规：预览/更多菜单走页面内（非全屏 modal）；保留原 DOM id 与函数名（`showBlogView` / `beTitle` / `beCover` / `blogEditorInput` / `editorTool` / `editorAiAssist` / `edInsertTemplate` 等）。
- 编辑器逻辑主要在内联脚本（`学习博客.html` L430+）与 `api.js` 的 `editorTool`/`saveBlogNote`/`editorAiAssist`/`renderBlogStats`；**改 `api.js` 后必 bump 全站 `app.js` 版本戳**（6.4）；只改 `学习博客.html` 内联部分则按部署 SOP 打包。
- `blog_wechat.html` 的 封面/图片 字段文案同步清理（1.2 的技术术语），保持两处一致。

---

*文档结束。改造落点已只读核实，待用户「开工」后按 P0→P1→P2 推进。*
