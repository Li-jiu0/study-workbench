# 各平台AI模型状态更新与配置调整需求

> 日期：2026-09-17
> 基于全量模型测试结果，更新各平台可用模型和API Key配置

---

## 一、各平台模型可用状态汇总

| 平台 | 可用模型数 | 状态 | 操作 |
|------|-----------|------|------|
| 火山方舟 | 18文本 + 3图片 | ✅ 全部正常 | 保持不变，新增模型见单独文档 |
| 智谱 | 2个稳定 + 1个限流 | ⚠️ 大部分需充值 | 保留glm-4.7和glm-4v-flash，其他删除 |
| 百度千帆 | 2个 | ⚠️ 大部分额度用完 | 只保留2个ernie，其他删除 |
| 硅基 | 0个 | ❌ 全部402欠费 | 所有模型删除，provider保留待换key |
| OpenRouter | 3个 | ✅ 新key可用（需梯子） | 更换key，保留3个可用模型 |
| Gemini | 2个 | ✅ 新key可用（需梯子） | 更换key，保留2个可用模型 |

---

## 二、OpenRouter 配置更新

### 2.1 更换 API Key

旧key已过期（401），更换为新key：

```
****REDACTED-KEY(full-key-kept-local-only)****
```

在 `ai-config.js` 的 providers 中更新 openrouter 的 apiKey 字段。

### 2.2 可用模型（3个）

| 配置ID | 显示名称 | 模型ID | 响应时间 | 星级 | 能力 |
|--------|---------|--------|---------|------|------|
| or-auto | OR-Auto | openrouter/free | 1.8s | 4 | text |
| or-nemotron-super | Nemotron-Super | nvidia/nemotron-3-super-120b-a12b:free | 1.2s | 4 | text |
| or-nemotron-ultra | Nemotron-Ultra | nvidia/nemotron-3-ultra-550b-a55b:free | 3.3s | 5 | text, reasoning |

### 2.3 已删除模型

以下模型已下线（404）或限流（429），**直接从 ai-config.js 中删除**，不保留：
- google/gemma-4-31b-it:free（429限流）
- meta-llama/llama-3.1-8b-instruct:free（404已下线）
- meta-llama/llama-3.2-3b-instruct:free（404已下线）
- huggingfaceh4/zephyr-7b-beta:free（404已下线）
- mistralai/mistral-7b-instruct:free（超时）
- gryphe/mythomist-7b:free（404已下线）
- undi95/toppy-m-7b:free（404已下线）

### 2.4 网络要求

⚠️ **OpenRouter 需要梯子才能访问**（国内普通网络无法直连 openrouter.ai）。用户无梯子时，调用失败自动降级到国内模型。

---

## 三、智谱配置调整

### 3.1 更换 API Key

旧key限流严重，更换为新key：

```
339ab396568541d0b7c0be4a577e5e53.VM5HxadQcgew1JdB
```

### 3.2 可用模型（2个稳定 + 1个限流观察）

| 配置ID | 显示名称 | 模型ID | 响应时间 | 星级 | 能力 | 状态 |
|--------|---------|--------|---------|------|------|------|
| glm-4.7 | GLM-4.7 | glm-4.7 | 5.5s | 5 | text | ✅ 正常 |
| glm-4v-flash | GLM-4V-Flash | glm-4v-flash | 1.0s | 4 | text, vision | ✅ 正常 |
| glm-4.6v-flash | GLM-4.6V-Flash | glm-4.6v-flash | - | 4 | text, vision | ⚠️ 限流中 |

### 3.3 当前状态说明

- `glm-4.7`：免费可用，文本能力强，响应约5.5秒
- `glm-4v-flash`：免费可用，视觉模型（支持图片识别），响应仅1秒，适合拍题功能
- `glm-4.6v-flash`：免费但当前访问量过大被限流，保留在配置中，自动降级到 glm-4v-flash
- 其他模型（glm-4.5、4.5-air、4.6、5、5.1、5.2、5.3、5.3-flash、5-turbo）全部需要充值（余额不足），暂时禁用

### 3.4 操作

- 保留 `glm-4.7`（文本）和 `glm-4v-flash`（视觉）
- `glm-4.6v-flash` 保留在配置中，标记为"限流中"，调用失败时自动降级到 glm-4v-flash
- **其他需充值的模型（glm-4.5、4.5-air、4.6、5、5.1、5.2、5.3、5.3-flash、5-turbo）直接从 ai-config.js 中删除**，不保留
- 模型设置页面不显示已删除的模型

---

## 四、百度千帆配置调整

### 4.1 当前状态

16个模型中只有2个可用：
- `ernie-4.5-turbo-32k`（2.0s）
- `ernie-4.5-turbo-128k`（1.8s）

其他模型403（额度用完）或401（未开通）。

### 4.2 操作

- 保留上述2个模型
- **其他模型（deepseek系列、glm-5.3、qwen、kimi、旧版ernie等）直接从 ai-config.js 中删除**，不保留
- 模型设置页面不显示已删除的模型

---

## 五、硅基配置调整

### 5.1 当前状态

全部52个文本模型402（Payment Required，欠费/免费额度用完）。

### 5.2 操作

- **硅基所有模型直接从 ai-config.js 中删除**，不保留
- 硅基 provider 配置（apiUrl、apiKey）可保留，待后续充值或换新key后再添加模型
- 模型下拉列表和设置页面不显示硅基模型

---

## 六、Gemini 配置更新

### 6.1 更换 API Key

旧key已过期（401），更换为新key：

```
****REDACTED-KEY(full-key-kept-local-only)****
```

在 `ai-config.js` 的 providers 中更新 gemini 的 apiKey 字段。

### 6.2 可用模型（2个）

| 配置ID | 显示名称 | 模型ID | 响应时间 | 星级 | 能力 |
|--------|---------|--------|---------|------|------|
| gemini-flash | Gemini-3.5-Flash | gemini-3.5-flash | 2.9s | 4 | text, vision |
| gemini-flash-lite | Gemini-3.5-Lite | gemini-3.5-flash-lite | 1.0s | 3 | text |

### 6.3 已删除模型

以下旧版本已下线（404），**直接从 ai-config.js 中删除**：
- gemini-2.5-flash、gemini-2.0-flash、gemini-1.5-flash
- gemini-2.5-pro、gemini-2.0-pro

### 6.4 网络要求

⚠️ **Gemini 需要梯子才能访问**（国内普通网络无法直连 googleapis.com）。用户无梯子时，调用失败自动降级到国内模型。

### 6.4 API 格式说明

Gemini 非 OpenAI 兼容格式，需单独适配：
- 端点：`https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent?key={apiKey}`
- Key 通过 URL 参数传递，不是 Authorization header
- 请求体：`{"contents": [{"parts": [{"text": "用户问题"}]}]}`
- 响应解析：`candidates[0].content.parts[0].text`

---

## 七、模型下拉列表最终显示顺序

调整后，用户在AI对话页能看到的模型按以下顺序排列：

1. **快速/均衡/极致模式**（三个快捷选项）
2. **火山方舟**（18个文本模型，按推荐度排序）
3. **智谱**（2个：glm-4.7、glm-4v-flash视觉）
4. **百度千帆**（2个：ernie-4.5-turbo-32k、128k）
5. **OpenRouter**（3个：OR-Auto、Nemotron-Super、Nemotron-Ultra）
6. **Gemini**（2个：Gemini-3.5-Flash、Gemini-3.5-Lite）
7. **分隔线**
8. **图片生成模型**（火山方舟3个：seedream系列）
9. **分隔线**
10. **模型设置**入口

---

## 八、降级链更新

由于硅基、Gemini不可用，降级链调整为：

- **快速模式**：火山方舟flash模型 → 火山方舟mini → 智谱glm-4.7 → 百度千帆ernie → Gemini-lite → OpenRouter auto
- **均衡模式**：火山方舟pro → 火山方舟turbo → 智谱glm-4.7 → 百度千帆ernie → Gemini-flash → OpenRouter super
- **极致模式**：火山方舟最强pro → OpenRouter ultra → 火山方舟v4-pro → Gemini-flash → 百度千帆ernie-128k

国内模型优先，海外模型（OpenRouter、Gemini）作为最后降级。

---

## 九、其他要求

1. **不可用的模型直接从 ai-config.js 中删除**，不保留 disabled 状态，保持配置文件干净
2. 硅基 provider 配置保留（apiUrl、apiKey），方便后续换新key后快速恢复
3. OpenRouter 和 Gemini 需梯子访问，调用失败时自动降级到国内模型（火山方舟→智谱→百度千帆）
4. 模型设置页面只显示当前可用的模型，不显示已删除的
5. 启动时自动检测各平台key有效性，失效的平台在模型设置页标红提示
6. 后续换新key后，只需修改 ai-config.js 中的 apiKey，模型自动恢复可用，不需要改其他代码
