# 需求文档：微信登录 / 分享 + 手机型号统计后台 + 已安装应用列表（风控）

> 日期：2026-09-20
> 交付对象：workbuddy
> 端：安卓壳（
>
> `android/`
>
> ，自研 Python 打包，
>
> **非 Gradle**
>
> ）、H5（
>
> `登录.html`
>
> 、设置页等）、后端（
>
> `server/routers/`
>
> 、
>
> `server/database.py`
>
> 、
>
> `server/.env`
>
> ）
> 包名：
>
> `com.study.workbench`
>
> ；versionName 1.33 /versionCode 34；minSdk 21 /targetSdk 33；签名 
>
> `android/workbench.keystore`
>
> （storepass/keypass 均为 
>
> `workbench123`
>
> ，alias 
>
> `workbench`
>
> ）。
> 说明：本文只提需求与接入点，不代表已改。三项相互独立，可按文末顺序实施。
>
> **涉及隐私采集与微信开放平台资质的前置条件必须先满足，见文末 "前置与合规"。**



***

## 一、手机型号 / 设备信息统计（采集已具备，本次只补 "后台查看"）

### 现状（已实现，勿重复造）



* 原生 `MainActivity` 的 JS 桥已采集设备信息（`MainActivity.java:437-468`）：`brand`(Build.BRAND)、`model`(Build.MODEL)、`osVersion`、`appVersion`、`androidId`(Settings.Secure.ANDROID\_ID)、屏幕宽高、语言。

* `登录.html:696` 登录成功后调用 `POST /api/user/device` 上报（每次登录一次，防重复）。

* 后端 `server/routers/device.py` 已实现 upsert，落库 `user_devices` 表（`database.py:413` 起，每用户一行，字段为白名单 JSON，含 `last_active`）。

* **缺口：数据收上来了，但管理后台没有任何查看 / 统计入口（**`admin.py`**&#x20;未引用 UserDevice）。**

### 本次要做



1. 后端新增管理员接口（鉴权同现有 admin）：

* `GET /api/admin/devices?page=&size=&brand=&model=&appVersion=`：分页设备明细，返回用户名 / 昵称、brand、model、osVersion、appVersion、屏幕、语言、last\_active；`androidId` 脱敏（只留前 4 后 4）。

* `GET /api/admin/devices/stats`：聚合 —— 设备总数、活跃用户数、**品牌分布、型号 Top N、App 版本分布、Android 版本分布、近 7/30 天活跃**。

* `GET /api/admin/devices/export`：导出 CSV（同样仅管理员、androidId 脱敏）。

1. 管理后台前端新增 "设备统计" 页：上述聚合用图表 / 表格展示（品牌、型号、App 版本、系统版本、最近活跃），明细可搜索可翻页可导出。

2. （可选补字段，保持白名单机制，**严禁加 IMEI/MAC/ 通讯录 / 短信**）：如需更细，可在 `device.py` 的 `DeviceIn`、原生桥、`登录.html` 上报里补 `manufacturer`(Build.MANUFACTURER)、`sdkInt`(Build.VERSION.SDK\_INT)。不补也能满足 "统计手机型号"。

### 验收



* 任意用户用 App 登录后，管理员在 "设备统计" 能看到其品牌 / 型号 / 系统 / App 版本 / 最近活跃；

* 分布统计数字与 `user_devices` 实际记录一致（用 SQL 抽查）；

* 普通用户无法访问该接口（403/401），androidId 不明文展示；

* 重复登录不产生多行（仍为每用户一行，刷新 last\_active）。



***

## 二、微信登录 + 微信分享（全新功能，依赖微信开放平台 AppID）

> ⚠️ 强前置：微信登录 / 分享必须先在 
>
> **微信开放平台（**
>
> [open.weixin.qq.com](https://open.weixin.qq.com)
>
> **）注册 "移动应用" 并通过审核拿到 AppID / AppSecret**
>
> ，否则真机调不起来。资质、签名、域名等见文末 "前置与合规"，需主理人办理；AppID 未到位前可先写代码框架但无法真机验收。

### 2.1 安卓侧：集成微信 OpenSDK（自研打包脚本必须改造）

当前 `android/build_apk.py` 是 javac + d8 手搓打包，classpath 只含 `android.jar`、d8 只吃自己编译的 `classes.jar`（`build_apk.py:233-241`），**不会自动编译第三方 SDK**。需：



1. 下载官方 SDK：微信开放平台资源中心的 `wechat-sdk-android-without-mta-*.aar`（用**不带 mta** 的版本），解压取出其中 `classes.jar`，放到 `android/libs/wechat-sdk.jar`（微信 SDK 基本无自带资源，无需合并 res；若该版本含 res，需在 aapt2 compile 阶段一并编入）。

2. 改 `android/build_apk.py`：

* javac（233-234 行）classpath 增加 `android/libs/wechat-sdk.jar`（与 `-bootclasspath android.jar` 并列，用 `-classpath`）；

* d8（240-241 行）输入在 `classes.jar` 后追加 `wechat-sdk.jar`，把 SDK 类一起打进 dex；

* 顺带核对现存 `android/libs/webkit-classes.jar` / `webkit-1.10.0.aar` 当前是否真的参与编译（脚本里未见引用），未使用就保持现状并注释说明。

1. **微信强制要求回调类放在固定包名路径**：新增 `android/java/com/study/workbench/wxapi/WXEntryActivity.java`（包名 `com.study.workbench.wxapi`，类名必须是 `WXEntryActivity`，否则收不到微信回调）。该 Activity 实现 `IWXAPIEventHandler`，同时处理：

* 登录授权回调 `SendAuth.Resp`（取 `code`）；

* 分享回调 `SendMessageToWX.Resp`（成功 / 取消 / 失败 toast）。

1. `AndroidManifest.xml` 注册回调 Activity（在 `<application>` 内）：



```
\<activity android:name=".wxapi.WXEntryActivity"

&#x20;   android:exported="true"

&#x20;   android:launchMode="singleTop"

&#x20;   android:taskAffinity="com.study.workbench"

&#x20;   android:theme="@android:style/Theme.Translucent.NoTitleBar" />
```

并在现有 `<queries>`（39-43 行，目前只有 TTS）内增加微信包名可见性（Android 11+ 检测是否安装微信必需）：



```
\<package android:name="com.tencent.mm" />
```



1. 新增 JS 桥方法（沿用 MainActivity 现有 `addJavascriptInterface` + `evaluateJavascript` 回调风格）：

* `wxIsInstalled()` → 返回是否安装微信（`IWXAPI.isWXAppInstalled()`）；

* `wxLogin()` → 注册 AppID 后发送 `SendAuth.Req`（scope=`snsapi_userinfo`，state 用随机串），回调把 `code` 回传前端；

* `wxShare({title, desc, link, thumbDataUrl, scene})` → 用 `WXWebpageMessage`+`WXMediaMessage` 组装网页分享，scene 支持 `session`（好友）/`timeline`（朋友圈）。

* AppID 放原生常量或前端配置均可；**AppSecret 绝不允许进 APK / 前端，只放服务器&#x20;**`.env`**。**

### 2.2 后端侧：code 换 openid 并签发本站令牌

新增 `server/routers/auth_wechat.py`（或在 `auth.py` 内扩展），并在 `main.py` 注册：



1. `POST /api/auth/wechat`，入参 `{code}`（+ 可选 `bind` 场景）。服务端用 `.env` 的 `WECHAT_APP_ID/WECHAT_APP_SECRET` 调微信：

* `GET https://api.weixin.qq.com/sns/oauth2/access_token?appid=&secret=&code=&grant_type=authorization_code` → `access_token/openid/unionid/refresh_token`；

* 需要昵称头像再调 `GET https://api.weixin.qq.com/sns/userinfo`（移动应用授权）。

1. 账号匹配 / 创建：

* **优先按&#x20;**`unionid`（跨端唯一），其次 `openid` 查用户；

* 数据库 `users` 表新增可空列 `wechat_unionid`、`wechat_openid`（写迁移，参考 `server/routers/migrate.py` 现有方式），加唯一索引；

* 首次微信登录且无账号：自动创建账号（用户名生成如 `wx_` + 短随机，昵称 / 头像取微信资料，允许后续在 "账号与安全" 补用户名 / 邮箱 / 密码）；

* 命中则直接登录。无论新建 / 命中，**返回结构必须与现有登录完全一致**（复用 `auth.py:_auth_payload`，即 token/refreshToken/isAdmin/user），前端无需分叉。

1. 已登录用户 "绑定微信"：`POST /api/user/bind-wechat {code}`，把 unionid/openid 绑到当前账号（已被别人绑定则报错）；并提供解绑。

2. 加与登录一致的限流（`rate_limit("auth")`）、错误文案与日志；微信侧错误（invalid code / 过期）转成 400 友好提示。

### 2.3 前端侧



1. `登录.html` 增加 "微信一键登录" 按钮：先调 `wxIsInstalled()`，未安装则置灰提示；已安装调 `wxLogin()` 拿 code → `POST /api/auth/wechat` → 用返回的 token 走现有 `saveSession` 流程（成功后同样触发一次 `/api/user/device` 上报）。

2. 设置 → 账号与安全：增加 "绑定 / 解绑微信"。

3. 分享入口：笔记 / 博客 / 成绩报告等详情页加 "分享到微信 / 朋友圈"，调 `wxShare`，传标题、描述、**https 网页链接**、缩略图。

4. 纯浏览器（非 App / 未装微信）环境隐藏微信登录与分享按钮，保留账号密码登录。

### 验收



* 装了微信的真机：微信登录能拉起授权 → 回调 → 本站登录成功、拿到 token、进入首页；首次登录自动建号，再次登录识别为同一账号；

* 已登录用户绑定微信后，下次可用微信登录进同一账号；解绑后不能再用其登录；

* 分享到好友 / 朋友圈卡片标题、描述、缩略图、链接正确，点击可打开落地页；

* 未装微信时按钮置灰并有提示，不崩溃；浏览器版不显示微信入口；

* AppSecret 不出现在 APK / 前端（解包与前端源码核查）；

* 打包脚本改造后能正常出包、微信 SDK 类已进 dex（用 dexdump / 解包核对），登录与分享回调能进 `WXEntryActivity`。



***

## 三、读取 "已安装应用列表" 用于设备风控（敏感权限，合规先行）

> 用途定位：
>
> **账号与设备安全风控**
>
> （识别多开 / 模拟器 / 异常设备、风控指纹）。这是敏感个人信息，必须按第四节做告知 - 同意 - 最小化；不要为了 "弹通知" 加它（与通知无关）。

### 3.1 原生



1. `AndroidManifest.xml` 增加（与现有 uses-permission 并列）：



```
\<uses-permission android:name="android.permission.QUERY\_ALL\_PACKAGES" />
```

（targetSdk 33，声明后 `getInstalledApplications` 才能见全部包；国产 ROM 可能另有 "读取应用列表" 开关，见合规。）



1. 新增 JS 桥 `getInstalledAppsForRisk()`：

* `getPackageManager().getInstalledApplications(0)` 取全部，**排除系统应用**（`ApplicationInfo.flags & FLAG_SYSTEM`），只保留用户手动安装的第三方应用，降低噪音与敏感度；

* 默认只取**包名**，不取应用名 / 图标 / 版本；如风控确需，可附 `firstInstallTime/lastUpdateTime`（`getPackageInfo`）；

* **默认对每个包名做 SHA-256 哈希（加盐，盐放服务端或固定常量）后再上报**，服务端存哈希做指纹比对；除非主理人明确要明文，否则不存可读包名。

1. 采集时机：**必须在用户同意隐私政策之后**、且在设置中 "设备安全 / 风控" 开关开启时才采集（默认随隐私同意开启、可关闭）；未同意 / 关闭时绝不调用。

### 3.2 后端



1. 新增表 `user_app_sigs`（`database.py` + 迁移）：`user_id` 主键、`sigs`(JSON 哈希列表)、`app_count`、`updated_at`；与 `user_devices` 分开存。

2. 新增 `POST /api/user/installed-apps`（鉴权，upsert，数量上限，如最多 500 条、单条长度 ≤128，超长拒绝 422）。

3. 仅用于风控：可在登录 / 异常行为时与历史指纹比对（同账号设备突变、模拟器特征等），**不做用户画像 / 广告，不对外公开**。

4. **不要**把这些字段塞进现有 `POST /api/user/device` 白名单（`device.py` 注释已划定隐私红线），单独成表、单独同意。

### 3.3 前端 / 设置



* 设置 → 隐私与安全里列出 "读取已安装应用列表（用于账号与设备安全）" 开关与说明，可随时关闭；隐私政策弹窗 / 文本中同步说明。

### 验收



* 同意隐私政策后才采集；拒绝 / 关闭时不产生该请求（抓包验证）；

* 上报内容为哈希列表（默认看不到明文包名）、仅第三方应用、数量受限；

* 重装 / 换设备指纹变化可被服务端识别为不同设备；同设备重复上报为 upsert 不膨胀；

* 普通接口、前端页面无法获取该数据，仅风控 / 管理员受控使用。



***

## 四、前置条件与合规（主理人办理，代码替代不了）

### 微信开放平台（登录 / 分享的硬前置）



1. 注册开放平台账号并完成开发者资质认证（个人或企业，**通常需认证费 300 元 / 年**，以微信官方为准）。

2. 创建 "移动应用"，填写：应用名称、图标、包名 `com.study.workbench`、**应用签名 MD5**、官网 / 下载页，提交审核。

* 签名 MD5 取法（用发布证书，不是 debug 证书）：

  `keytool -list -v -keystore android\workbench.keystore -storepass workbench123`，复制证书指纹的 **MD5**，去掉冒号、转小写，填到微信后台。

* 注意：微信后台登记的签名必须和最终发包签名一致，否则真机返回未注册 / 授权失败。以后若换签名证书，需同步改微信后台。

1. 审核通过后把 **AppID** 给客户端配置、**AppSecret 仅填到服务器&#x20;**`server/.env`（新增 `WECHAT_APP_ID`、`WECHAT_APP_SECRET`）。

2. 分享落地页：微信内打开 IP + http 链接体验差且可能被拦截，建议准备**已备案域名 + HTTPS**（线上现为 `http://110.42.134.62`）。在域名 / HTTPS 就绪前，分享卡片链接可能不达标。

3. 范围说明：本需求是 **App 内 OpenSDK 拉起微信**（手机装了微信才可用）。微信**之外的手机浏览器**不能 "微信一键登录"；微信内 H5 网页授权是 "公众号服务号" 另一套资质，本期不做。

### 隐私合规（安装列表 + 微信 SDK 都涉及）



1. **隐私政策更新并在首启弹窗取得同意**，明确披露：

* 接入 "微信 OpenSDK"（提供微信登录 / 分享）及其收集的信息（设备标识、应用信息等，按微信官方 SDK 隐私清单写明）；

* 收集 "已安装应用列表"，目的为账号与设备安全风控、可关闭、留存期限。

1. 最小必要：安装列表默认哈希、排除系统应用、可关闭、不做画像广告；未同意不采集。

2. 渠道：本项目为自有服务器分发 APK（`星途-安卓App.apk`）。须知 `QUERY_ALL_PACKAGES` 在 **Google Play 基本不允许**普通应用使用；国内安卓商店上架时需在权限申报里说明用途，否则可能被要求整改。

3. 微信 SDK、安装列表相关权限与采集，均不得与 "通知 / 保活" 混为一谈；用户拒绝这些**不影响**核心学习功能与消息通知。



***

## 五、建议实施顺序



1. **第一项（设备统计后台）**：纯增量、无资质依赖，先做并自测。

2. **第三项（安装列表风控）**：原生桥 + 独立表 + 隐私开关 / 政策，先于或与微信并行；上架前完成隐私申报文案。

3. **第二项（微信登录 / 分享）**：

* 先做不依赖审核的部分：打包脚本支持第三方 jar、`WXEntryActivity` 骨架、JS 桥、后端接口与 users 迁移、前端按钮（AppID 用配置占位）；

* 主理人同步办理开放平台认证 / 应用审核；AppID/AppSecret、HTTPS 域名到位后填配置并真机验收。

> 改完按每条 "验收标准" 真机 / 后端自测并逐条回报；涉及 
>
> `users`
>
> 、新表的改动务必附迁移脚本并在服务器演练；打包脚本改动后请实际出一个包验证第三方 SDK 已正确打入 dex。