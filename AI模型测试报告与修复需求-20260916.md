# AI 模型可用性测试报告

> 测试时间：2026-09-16
> 测试方式：使用项目内置 Key 直接调用各平台 API，发送 "1+1=?"
> 测试范围：ai-config.js 中全部 11 个内置免费模型

---

## 一、测试结果总览

| # | 模型名称 | 平台 | 测试结果 | HTTP | 说明 |
|---|---------|------|---------|------|------|
| 1 | GLM-4.7-Flash | 智谱 | ⚠️ 限流 | 429 | 高峰期访问量过大，非配置错误，降级链应接住 |
| 2 | GLM-4.5-Flash | 智谱 | ✅ 可用 | 200 | 正常回复 |
| 3 | GLM-4-Flash | 智谱 | ✅ 可用 | 200 | 正常回复 |
| 4 | GLM-4.6V-Flash | 智谱 | ⚠️ 限流 | 429 | 视觉模型，高峰期限流 |
| 5 | GLM-4V-Flash | 智谱 | ✅ 可用 | 200 | 视觉模型，正常回复 |
| 6 | Qwen2.5-7B | 硅基 | ✅ 可用 | 200 | 正常回复 |
| 7 | Qwen3-8B | 硅基 | ✅ 可用 | 200 | 响应较慢，需 60s 超时 |
| 8 | Qwen3.5-4B | 硅基 | ✅ 可用 | 200 | 响应较慢，需 60s 超时 |
| 9 | DeepSeek-R1-8B | 硅基 | ✅ 可用 | 200 | 推理模型，正常回复 |
| 10 | GLM-4-9B | 硅基 | ✅ 可用 | 200 | 正常回复 |
| 11 | Hunyuan-MT-7B | 硅基 | ✅ 可用 | 200 | 翻译模型，正常回复 |

**结论：11 个模型全部可用，Key 有效，模型名正确。**

---

## 二、关键发现

### 1. 前端 400 报错不是模型的问题
直接用同样的 Key、同样的模型名调用 API，全部正常（限流除外）。
前端控制台连续 `400 Bad Request`（ai-service.js:411），说明是 **前端构造请求体时参数有问题**，与模型/Key 无关。

**最可能的原因：**
- 用户发了图片，前端构造的图片消息格式不符合智谱视觉模型要求（image_url 结构、base64 前缀等）；
- 或前端传了模型不支持的参数（如 reasoning 字段、stream 格式错误）。

### 2. 智谱免费模型高峰期会 429 限流
GLM-4.7-Flash（自动模式首选通用模型）和 GLM-4.6V-Flash（自动模式首选视觉模型）在高峰期返回 429。
这是正常现象，但要求**降级链必须可靠**：429 时自动切到 fallback 模型，不能卡住。

### 3. 硅基部分模型响应慢
Qwen3-8B、Qwen3.5-4B 响应较慢，30 秒内可能回不完。
**解决方向不是拉长超时干等**，而是：开启流式输出（边生成边显示）+ 自动模式优先用快模型 + 15 秒未出首字立即切换。详见下方修复需求第 3、6 条。

### 4. 降级逻辑误判
页面提示"当前模型繁忙，已自动切换到 GLM-4V-Flash"，但实际错误是 400（参数错误），不是繁忙。
**400 不应该触发降级切换**，否则会掩盖真正的参数 bug，切完还是 400。

---

## 三、给 workbuddy 的修复需求

### 需求名称：AI 接口 400 修复 + 模型降级与超时优化

**修复内容：**

1. **排查并修复 400 报错**（最高优先级）
   - 在 ai-service.js 中打印完整请求体（model、messages、图片字段、所有参数），定位 400 根因；
   - 重点检查图片消息格式：按智谱官方文档构造 image_url（base64 需带 `data:image/jpeg;base64,` 前缀，或先上传拿 URL）；
   - 确保前端显示名（如 "GLM-4V-Flash"）与实际请求 model 字段（`glm-4v-flash`）分开，不要把显示名当 model 发。

2. **修正降级触发条件**
   - 只有 **429（限流）、500/502/503/504（服务端错误）、网络超时** 才触发自动降级切换；
   - **400（参数错误）不触发降级**，直接展示具体错误原因，避免误切换掩盖 bug；
   - 降级时按 ai-config.js 中每个模型的 fallback 链依次切换，切换后提示"已切换到 XX 模型"。

3. **请求超时策略（15秒未出首字即切换）**
   - 所有 AI 请求超时设为 **15 秒**，不是 30/60 秒；
   - 15 秒内未收到第一个字（流式首 token），立即按 fallback 链切换到下一个快模型，并提示"响应较慢，已切换模型"；
   - 慢模型（Qwen3-8B、Qwen3.5-4B）仅在用户手动选择时使用，自动模式不优先选。

4. **优化错误提示**
   - 接口报错时，在对话区显示可读的错误信息（如"图片格式不支持"、"当前模型繁忙，已切换"），不要只在控制台报红、用户侧无反馈；
   - 400 错误应把后端返回的 error.message 展示给用户。

5. **限流模型的备选优化**
   - GLM-4.7-Flash 作为自动模式首选，高峰期经常 429，建议在自动路由中增加判断：若该模型连续 2 次 429，本次会话内直接跳过它，用 GLM-4.5-Flash 或 Qwen2.5-7B 作为首选。

6. **AI 响应体验优化（流式输出，核心）**
   - 所有 AI 对话开启**流式输出（stream=true）**，模型边生成前端边逐字显示，发完请求 2 秒内出第一个字，不再等全部生成完才一次性展示；
   - 自动模式路由优先选响应快的模型（GLM-4-Flash、Qwen2.5-7B，2~5 秒回），慢模型（Qwen3-8B 等）仅用户手动选或推理场景调用；
   - 等待时显示"AI 正在思考…"加载态，避免用户以为卡死；
   - 流式中断（网络抖动）时，已显示的内容保留，提示"连接中断，点击重新生成"。

**验收标准：**
- [ ] 发文字消息，11 个模型逐一手动选择测试，全部能正常回复，无 400；
- [ ] 发图片消息，视觉模型（GLM-4.6V-Flash / GLM-4V-Flash）能正常识别并回复，无 400；
- [ ] 人为构造 400 时，不触发"繁忙自动切换"，页面显示明确错误信息；
- [ ] 模拟 429 时，自动按 fallback 链切换模型并提示；
- [ ] **流式输出：发消息后 2 秒内可见文字开始逐字输出，不是等全部生成完才显示；**
- [ ] **15 秒未出首字时自动切换快模型并提示，不傻等 30/60 秒；**
- [ ] **自动模式默认用快模型（GLM-4-Flash / Qwen2.5-7B），不优先调用慢模型；**
- [ ] 等待时显示"AI 正在思考…"加载态；
- [ ] 控制台无连续 400 报错。

---

## 四、不在本次范围

- 不更换模型、不新增模型、不修改 Key；
- 不修改 ai-config.js 中的模型列表和参数；
- 不涉及后端 /api/ai/chat 中转逻辑（400 是前端直连智谱的问题）。
