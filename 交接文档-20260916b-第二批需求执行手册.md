# 交接文档：星途（原「学习工作台」）第二批需求执行手册

> 生成时间：2026-09-16 10:50
> 交接对象：接手执行的 AI / 工程师
> 本文目标：**让你在不读历史对话的前提下，独立完成 4 组需求，并且不把站点改白屏**

---

## 0. 一句话现状

站点主体功能正常，线上版本 `20260916k`。但**上一批（`20260916L`）的改动已落盘、尚未部署，且 `更多.html` 处于半成品破损状态**（详见 §3）。你要做的是：**先修掉 §3 的在途破损 → 再执行 §4~§7 的 4 组新需求**。

---

## 1. 项目坐标与环境（必读）

| 项 | 值 |
|---|---|
| **唯一可写代码树** | `D:\下载的文件\学习工作台` |
| ⛔ **绝对不要碰** | `C:\Users\ATM\WorkBuddy\Worktrees\...`（同一 git 仓库的废弃 worktree，改这里等于全部白干） |
| 线上静态站 | `http://110.42.134.62/`（nginx，远端站点根 `/opt/study-workbench/web`） |
| 线上后端 | `http://110.42.134.62:8000`（FastAPI / uvicorn，远端根 `/opt/study-workbench`） |
| 部署脚本模板 | `tools/deploy_update_20260916k.py`（**照抄它改 3 处即可**） |
| 部署凭据 | 脚本自动从 `D:\下载的文件\学习工作台\upload_v23.ps1` 正则解析 → **不要打印或外传** |
| 部署工具 | `tools/plink.exe`、`tools/pscp.exe`（已就位，hostkey 已固定） |
| node | `C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe` |
| python | `C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe` |
| jsdom + acorn | `tools/verifier/node_modules/`（已装好，路径硬编码在 QA 脚本里） |

### 1.1 技术形态
- 前端：**多页静态 HTML + `assets/*.js` 原生 JS**。**无框架、无 CDN、无打包、无构建步骤**。改完直接传文件。
- 后端：FastAPI + SQLite（`server/`），本地 `server/` 是**旧副本**，不要以它为准。
- 线上运行环境：**安卓 APK 里的老 WebView，内核 ≈ Chrome 50~58（只支持到 ES2017）**，同时也要在桌面浏览器正常运行。

### 1.2 环境坑（会让你白浪费大量时间）
1. **bash 缺 coreutils**：`ls` / `grep` / `head` / `tail` / `wc` / `dirname` **全部不存在**。→ 一律写 python 脚本，或用 Read/Grep/Glob 工具；命令一律写绝对路径。
2. **Windows 中文路径 + python 控制台**：`print` 中文会 GBK 崩溃。→ 结果一律**写 UTF-8 文件**，再用 Read 工具读。
3. 用 python 写 `-c` 单行命令时，含 `["']` 的正则极易被 shell 吃掉引号导致 `SyntaxError`。→ **正则类脚本写成 `.py` 文件再执行**。
4. `curl -w '%{size_download}'` 在本机**恒返回 0**，要量体积请用 python `urllib`。
5. **禁 git 操作**（本机 push 需动态代理，不值得折腾）。

---

## 2. 铁律（违反 = 整页白屏 / 白干。本项目已因此出过两次全站事故）

### 2.1 老 WebView 语法禁令
站点跑在安卓 APK 的老 WebView 里（内核约 **Chrome 50~58**）。以下语法**一律禁止**：

```
?.        ??        .replaceAll(        Object.fromEntries
.at(      顶层 await      正则后行断言 (?<= (?<!
{...obj}  ← 对象展开（ES2018）★历史全站事故元凶
const {a, ...rest} = x     ← 对象剩余
**        ← 指数运算符
catch {}  ← 可选 catch 绑定
```

**允许**：`let/const`、箭头函数、模板字符串、`class`、`async/await`（非顶层）、数组展开 `[...arr]`、`Math.max(1, ...arr)`。内核 ≥ ES2017。

### 2.2 顶层声明冲突禁令（"AI 失效"的真凶）
**禁止**在任何 js 里用 `var/const/let` 重复声明别的文件已存在的全局名。浏览器遇到重复声明会判该文件 **SyntaxError 并整文件拒绝执行**（该文件的所有功能静默失效）。

新增全局一律写成：
```js
if (typeof window.xxx !== 'function') window.xxx = function () { ... };
```

### 2.3 版本戳（`?v=`）—— 头号"修好了但用户看到老样子"的原因
- **改任何 `assets/*.js` 或 CSS 后，必须 bump 引用它的 `?v=` 版本戳**，否则用户设备永远加载旧文件（本轮已踩：`ai-service.js` 没带版本戳，用户测的一直是旧版）。
- 现状：全站 37 个 HTML 的 `?v=` 已统一为 `20260916L`（共 535 处），**本批改动要升到新的戳（建议 `20260916M`）**。
- ⚠️ 历史 bump 脚本的正则 `\?v=[0-9A-Za-z_\-\.]+` **只能替换已存在的 `?v=`，无法给裸路径补戳**。补戳请用：`(src|href)="(assets/[^"?]+\.(?:js|css))"` → `\1="\2?v=版本戳"`，且**必须单行内匹配，禁止跨行**（跨行正则吞掉 `-->` 和标签 = 整页白屏，已踩两次）。

### 2.4 其它
- **禁原生 `alert/confirm/prompt`**，用页面内 toast（`window.xtToast`）或页面内区块。
- 改完必须做**配对校验**：`<!--` 与 `-->` 数量相等、`<div` 与 `</div>` 数量相等、无损坏特征 `.js\?v=xxx"[^>\s]`。
- 改文件前**先备份**（命名 `xxx.html.bak-<用途>-20260916`，含 `bak` 的文件会被部署脚本自动跳过）。
- **工作方式**：命令/脚本**各跑一次即判定**，不要反复回跑、不要多轮回读自审。发现问题直接给结论。

---

## 3. ⚠️ 当前在途状态（必须先处理，否则你会在破损文件上继续施工）

### 3.1 已落盘、已完成、但尚未部署（不要重做，也不要破坏）
| 项 | 状态 |
|---|---|
| 全站版本戳统一为 `20260916L` | ✅ 已完成（535 处 / 37 个文件，`?v=20260916k` 已归零） |
| 底部导航顺序统一为 `首页→互动→我的→AI→更多` | ✅ 已完成（28 个页面全部正确，仅 `AI.html` 原本就正确） |
| `AI.html` 补上缺失的 `assets/ai-presets.js` | ✅ 已完成（**带 `defer`**，位置在 `ai-config.js` 之后、`ai-service.js` 之前） |
| `工具.html` 删除「导入题库/AI面试/穿越英语」3 张卡片 | ✅ 已完成（现仅剩「PPT版式、四级经验」） |

### 3.2 ❌ 破损待修：`更多.html`（**高优先级，可能引发白屏**）
上一轮执行「把工具页的 3 张卡片迁到更多页」时，**误把 `工具.html` 的整段页面内容追加进了 `更多.html`**，而非只搬「导入题库」这一个功能所需的依赖。实测症状：

- 卡片标题出现**两组**：
  `互动广场 / 错题本 / 设置 / 管理后台 / 关于 / 导入题库 / AI面试 / 穿越英语` **+** `导入题库 / PPT版式 / 四级经验 / AI面试 / 穿越英语`
  （后一组是工具页的，属误搬）
- `class="morepage-list"`、`id="importerView"`、`id="impBody"`、`id="impLibs"` **各出现 2 次** → **同页重复 id**，`getElementById` 只取第一个，导入向导会渲染进错误节点（表现为"点了没反应"）
- `.mpc-icon` 混进 **5 个裸 `data-icon` 写法**（`<div class="mpc-icon" data-icon="xxx" data-icon-size="20">`），而更多页自己的写法是 `<div class="mpc-icon"><span class="nav-icon" data-icon="xxx" data-icon-size="20"></span></div>` → **裸写法在更多页会导致图标空白**
- 残留一个**孤立的 `-->`**（约 L141，原「学习动态」改名注释的尾巴）
- 好消息：剔除 script/style 后真实 DOM 的 `<div>` 是 **162/162 平衡**，暂无白屏级损坏

**修复动作（只删不加）：**
1. 保留**第一次**出现、删除**第二次**出现：第二个 `<div class="morepage-list">…</div>` 整块（其上方注释以 `<!-- 【A8】工具卡片纵向列表` 开头）、第二个 `#impLibs`、第二个 `#importerView` 视图块、第二段定义 `openImporterView/closeImporterView/renderImportLibs/removeImportLibs/escImp/impItemText` 的内联 `<script>`；`<head>` 里重复的 `.importer-view` 相关 `<style>` 只留一份。
2. 删掉那个孤立 `-->`；并删除标注「**工具页**」归属的残留注释。
3. 备份已存在：`更多.html.bak-pre-cfix-20260916`、`工具.html.bak-pre-cfix-20260916`（可直接回滚比对）。

**修复后必须断言全绿：**
| 断言 | 期望值 |
|---|---|
| `更多.html` 的 `id="importerView"` / `id="impBody"` / `id="impLibs"` / `class="morepage-list"` | 各 **1** |
| `更多.html` 卡片标题序列 | 严格等于 `互动广场 / 错题本 / 设置 / 管理后台 / 关于 / 导入题库 / AI面试 / 穿越英语` |
| `更多.html` 的 `.mpc-icon` | span 嵌套 = **8**，裸 `data-icon` = **0** |
| `更多.html` 配对 | `<!--` 数 = `-->` 数；剔除 script/style 后 `<div` = `</div>` |
| 共用 DOM 仍在（两页） | `#adminEntryCard`、`#countdownModal`、`#morePanel`、`#toolsPanel`、`#aiFab`、`#aiPanel`、`#toast` |
| 全站「不带 `?v=` 的 assets 引用」 | **0** |

> 说明：`工具.html` 删除「工具箱」卡片后，该页**已无任何入口**（全站唯一入口就是更多页那张卡片）。这是**用户已确认接受**的"孤岛"状态，不要自行加回入口。但工具页的导入相关 DOM/脚本/样式**必须全部保留**（页面要能正常加载不报错）。

### 3.3 部署
`20260916L` 批次的部署脚本**尚未生成**（`tools/deploy_stamp_20260916L.py` 不存在），也未上传。修完 §3.2 后按 §9 部署。

---

## 4. 需求 A：PPT训练页「添加作品」改为弹窗录入

### 4.1 ⚠️ 先读这条：本需求与项目既有决策 ADR-3 **正好相反**
`PPT训练.html:80-81` 明确记录：
> **ADR-3（20260915adr3）：作品集「添加作品 / 作品详情」由遮罩弹窗改为页面内布局** —— 去掉 `.app-modal-mask` 遮罩外壳（无遮罩、不锁滚动），改为作品集卡片内的页面内区块。

`PPT训练.html:371-372` 亦注明：
> 「添加作品弹窗」与「作品大图预览弹窗」已按 ADR-3 上移为 `#page-ppt` 内页面内区块，**id 与控件全部保留**。

**也就是说：当前实现是"页面内区块"，新需求要求"带遮罩的弹窗"。这是把 ADR-3 改回去。**
👉 **动手前先向用户确认这一点**（很可能用户只是觉得当前形态不像"弹窗"，或想恢复遮罩）。若确认要改，按 §4.3 执行。

### 4.2 现有实现位置（已实测）
| 内容 | 位置 |
|---|---|
| 「+ 添加作品」按钮 | `PPT训练.html:130` → `onclick="window.pptAddWork()"` |
| 「我的作品集」卡片 / 列表视图 | `PPT训练.html:127-137` |
| 添加作品页面内区块（含 `#pptWorkModal`） | `PPT训练.html:138-145` 起 |
| 作品集主逻辑（含添加、缩略图、大图预览） | `assets/ppt-works.js`（约 500 行），`ppt-works.js:33` 有 `var pending = []`（待保存图片 base64），`ppt-works.js:237` 空态文案，`ppt-works.js:461` 动态生成「+ 添加作品」按钮 |
| 作品集样式 | `PPT训练.html:43 起`、`PPT训练.html:433 起` |

### 4.3 改造要求
1. 点【+ 添加作品】**唤起弹窗，不新开页面**。
2. 弹窗内录入项：**作品标题**（输入框）、**作品简介**（多行文本域）、**上传作品图片/文件**、**作品链接**（可选填）。
3. 弹窗底部按钮：【取消】（关闭不保存任何内容）、【保存】（**校验标题非空** → 保存作品集数据 → 关闭 → 刷新「我的作品集」区域，新增作品可见）。
4. **弹窗样式与当前页面风格统一**（复用现有 `.modal-overlay` / `.modal` / `.form-group` / `.form-label` / `.form-input` / `.modal-actions` / `.btn` 体系 —— 站内已有这些类，见 `更多.html` 的 `#countdownModal` 可作模板）。
5. 点**遮罩区域可关闭**弹窗。
6. 数据持久化沿用现有方案（勿新增存储层）：`ppt-works.js` 现有的 localStorage 键与数据结构不变。

### 4.4 验收
- [ ] 点【添加作品】弹窗出现，**不发生页面跳转**
- [ ] 表单可填写、可选文件
- [ ] 保存成功后作品集列表实时刷新、新增作品可见
- [ ] 取消直接关闭、不产生脏数据
- [ ] 点遮罩可关闭
- [ ] 手机端 + 电脑端样式正常、与其他弹窗风格一致

---

## 5. 需求 B：首页功能中心模块更名 + 对应 HTML 文件重命名

### 5.1 目标映射
| 位置 | 旧 | 新 |
|---|---|---|
| 卡片文案 / 页面标题 / 侧栏 / 面包屑 | 广场 | **社区** |
| | 四级备考 | **英语** |
| | 央国企笔试 | **行测** |
| | 高情商表达 | **表达** |
| | 商务礼仪面试 | **面测** |
| | PPT训练 | **演示** |
| HTML 文件 | `学习博客.html` | `社区.html` |
| | `四级备考.html` | `英语.html` |
| | `央国企笔试.html` | `行测.html` |
| | `高情商表达.html` | `表达.html` |
| | `商务礼仪面试.html` | `面测.html` |
| | `PPT训练.html` | `演示.html` |

卡片简介文字**保留不变**，卡片图标**保持原样**。

### 5.2 数据源：改这一处即可决定卡片文案与跳转
`assets/app.js:6719-6726`：
```js
var HOME_DEF = [
  { k: 'plaza',     ic: '🌍', dc: 'globe',          t: '广场',       d: '看大家的',     url: '学习博客.html' },
  { k: 'cet',       ic: '📖', dc: 'book-open',      t: '四级备考',   d: '词汇听力阅读', url: '四级备考.html' },
  { k: 'exam',      ic: '📝', dc: 'pencil',         t: '央国企笔试', d: '行测刷题',     url: '央国企笔试.html' },
  { k: 'comm',      ic: '💬', dc: 'message-square', t: '高情商表达', d: '场景话术',     url: '高情商表达.html' },
  { k: 'interview', ic: '🤝', dc: 'handshake',      t: '商务礼仪面试', d: '面试题库',   url: '商务礼仪面试.html' },
  { k: 'ppt',       ic: '🎨', dc: 'palette',        t: 'PPT训练',    d: '版式案例',     url: 'PPT训练.html' }
];
```
- **只改 `t`（文案）与 `url`（文件名）**。
- ⚠️ **`k` 键（`plaza/cet/exam/comm/interview/ppt`）必须保持不变** —— 用户的显示偏好存在 `localStorage['study_workbench_home_show']` 里按 `k` 索引；改 `k` 会让老用户的显示/隐藏设置全部失效。
- `renderHomeQuick()` 在 `app.js:6739-6762`，它还会渲染「🕘 最近打开」的 chip；`recentMap` 在 `app.js:6776-6779` **按文件名映射模块**，重命名后**必须同步更新**，否则"最近打开"记录会丢。

### 5.3 ⚠️ 影响面：已实测，别低估
**旧名称作为「文案」的出现次数（根目录非备份 HTML + `assets/*.js`，共 92 个文件）：**

| 旧名 | 总次数 | 涉及文件数 | 主要集中处 |
|---|---|---|---|
| 广场 | **106** | 32 | `app.js` 21、`学习博客.html` 10、`blog_wechat.html` 9、`设置.html` 7、`更多.html` 4、`私聊.html` 3、`api.js` 3、`icon-map.js` 3 |
| 四级备考 | **70** | 38 | `app.js` 17、`mock_exam.html` 4、`设置.html` 4、`学途.html` 3、`chat-local.js` 3、`ai-presets.js` 2 |
| 央国企笔试 | **61** | 37 | `app.js` 12、`mock_exam.html` 3、`设置.html` 3、`企业定向库.html` 2、`学途.html` 2、`api.js` 2、`chat-local.js` 2、`topic-express.js` 2 |
| 高情商表达 | **56** | 32 | `app.js` 12、`设置.html` 4、`高情商表达.html` 4、`roleplay.js` 4、`更多.html` 3、`工具.html` 2 |
| 商务礼仪面试 | **48** | 32 | `app.js` 10、`设置.html` 4、`mock_exam.html` 3、`商务礼仪.html` 1 |
| PPT训练 | **51** | 34 | `app.js` 7、`PPT训练.html` 4、`设置.html` 4、`ppt-tips.js` 2、`ppt-works.js` 2 |

**旧文件名被引用的次数（共 70 处）：**

| 旧文件名 | 引用次数 | 涉及文件 |
|---|---|---|
| `学习博客.html` | 23 | `app.js` 9、`api.js` 8、`学途.html` 5、`私聊.html` 1 |
| `四级备考.html` | 15 | `app.js` 6、`mock_exam.html` 4、`学途.html` 3、`cet-read.js` 1、`cet-translate.js` 1 |
| `央国企笔试.html` | 12 | `app.js` 4、`mock_exam.html` 3、`api.js` 2、`企业定向库.html` 1、`学途.html` 1、`qbank.js` 1 |
| `商务礼仪面试.html` | 7 | `mock_exam.html` 3、`app.js` 3、`group-discussion.js` 1 |
| `PPT训练.html` | 9 | `app.js` 2、`ppt-tips.js` 2、`ppt-works.js` 2、`四级备考.html` 1、`design-class.js` 1、`tpl-preview.js` 1 |
| `高情商表达.html` | 4 | `app.js` 3、`comm-cases.js` 1 |

### 5.4 🚨 五个高风险点（不做处理必然出 bug）
1. **「广场」会误伤「互动广场」。** `互动广场` 是**另一个功能**（我的发贴/草稿/收藏，入口在更多页、个人中心、blog_wechat 等），出现在 `blog_wechat.html`、`更多.html`、`个人中心.html`、`动态.html` 等多处。
   → **不要把全站的「广场」无脑替换成「社区」**。需求只要求改「首页功能中心的卡片文案 + 模块相关的导航/标题」。
   **必须先列出所有含「广场」的位置让用户确认哪些要改**，或采用**白名单替换**（只改 `HOME_DEF.t`、`<title>`、`.topbar-title`、侧栏/底部导航的模块项）。
2. **替换顺序必须「长优先」**。`商务礼仪面试` 与 `商务礼仪`（**独立页面 `商务礼仪.html`**）是不同模块；若先替换 `商务礼仪` 就会破坏 `商务礼仪面试`。`高情商表达` 同理含 `表达`。
   → 按 `商务礼仪面试 → 商务礼仪`、`高情商表达 → 表达` 的顺序，**先长后短**。
3. **`行测.html` 与既有 `行测刷题.html` 并存**。`央国企笔试.html → 行测.html` 后，站内会有 `行测.html`（新）和 `行测刷题.html`（旧，且被 `央国企笔试.html` 引用）。
   → 先确认两者定位不冲突，避免用户混淆；`行测刷题.html` 的去留需用户拍板。
4. **`mock_exam.html` 里的兜底跳转**含 `四级备考.html` ×4、`央国企笔试.html` ×3、`商务礼仪面试.html` ×3（形如 `'...' ? '央国企笔试.html'` 与注释「参照商务礼仪面试.html 同步」）。**漏改 = 404 或跳错页**。
5. **`assets/*.js` 里也有文件名硬编码**：`app.js`、`api.js`、`qbank.js`、`cet-read.js`、`cet-translate.js`、`comm-cases.js`、`group-discussion.js`、`ppt-tips.js`、`ppt-works.js`、`design-class.js`、`tpl-preview.js`、`chat-local.js`、`topic-express.js`、`roleplay.js`、`ai-presets.js`、`icon-map.js`。
   → 改这些 js **必须 bump 它们的 `?v=` 版本戳**（见 §2.3），否则用户拿到旧文件、旧文件名继续被引用 → 404。
   → 注意 `ai-presets.js` 含「四级备考」2 处、`roleplay.js` 含「高情商表达」4 处，属于**关键词/角色提示词语料**，改文案可能影响 AI 回答质量，**建议只改展示文案、不动语料**（请甄别后再改）。

**新文件名冲突检查（已实测）**：`社区.html`、`英语.html`、`行测.html`、`表达.html`、`面测.html`、`演示.html` **当前都不存在**，不会撞名。

### 5.5 建议执行顺序
1. 先用脚本**输出一份完整的「旧名 → 命中位置」清单**（文件 + 行号 + 上下文），交给用户确认白名单/黑名单；
2. 用 `git mv` 之外的纯文件系统重命名（本项目禁 git）重命名 6 个 HTML；
3. 用**长优先**的顺序批量替换 `assets/*.js` 与根 HTML 里的**文件名引用**；
4. 再按白名单替换**展示文案**（`HOME_DEF.t`、`<title>`、`.topbar-title`、`morepage-title`、侧栏 `.nav-item`、底部导航 `.bn-label`、`pageTitles`）；
5. 同步 `app.js:6776-6779` 的 `recentMap`；
6. 全站扫描确认**无旧文件名残留**、**无死链**；
7. bump 全部 `?v=` → 部署 → 抽 6 个新页面各跑一次 HTTP 200 + 页面标题核对。

### 5.6 验收
- [ ] 首页功能中心 6 张卡片显示新名称，图标与简介不变
- [ ] 点卡片正常跳转、无 404
- [ ] 进入各模块页，页面标题/侧栏/底部导航/面包屑均为新名称，无旧名残留
- [ ] 「互动广场」等**非首页功能中心的同名文本未被误改**
- [ ] `行测刷题.html` 等既有页面仍可正常访问

---

## 6. 需求 C：AI 对话页输入框布局统一（"输入框变脸" bug）

### 6.1 机制（已实测，这是根因）
**全页只有一份输入框 DOM** —— `AI.html:638-677` 的 `<div class="ai-input-box" id="aiInputBox">`，内部包含：
`#aiImgPreview`、`textarea#aiInput`、`.ai-input-foot`（左侧 `.ai-foot-left` = 深度思考 `#aiDeepThinkChip` + 模型按钮 `#aiModelBtn`/`#aiModelLabel`；右侧 `.ai-foot-right` = 附件 `#aiAttachBtn` + 发送 `#aiSendBtn`）、`#aiFileInput`、`#aiModelPanel`。

它被 **JS 在两个槽位之间搬来搬去**：
- `AI.html:637` 空态槽 `<div class="ai-input-wrap" id="aiWelcomeInputSlot">`（在 `.ai-welcome#aiWelcome` 内，L635）
- `AI.html:685` 对话态槽 `<div id="aiDockInputSlot">`（在 `.ai-input-dock` 内，L683-687，原来是空的）

搬动逻辑在 **`assets/ai-page.js:249-254`**：
```js
if (toDock) { if (aiDockInputSlot) aiDockInputSlot.appendChild(aiInputBox); }
else { if (aiWelcomeInputSlot) aiWelcomeInputSlot.appendChild(aiInputBox); }
function enterConversation() { if (aiChat) aiChat.classList.add('in-conversation'); mountInput(true); updateSendEnabled(); }
function enterWelcome() { if (aiChat) aiChat.classList.remove('in-conversation'); mountInput(false); updateSendEnabled(); }
```
状态由 `#aiChat` 上的 `.in-conversation` 类控制（`AI.html:167-168`）：
```css
.ai-chat.in-conversation .ai-welcome{display:none;}
.ai-chat.in-conversation .ai-messages{display:flex;}
```

### 6.2 DOM 本身其实已经"一体化"了 —— 差异全在 CSS
`.ai-input-box`（`AI.html:235-242`）**本来就是一体化大圆角容器**，且深度思考/模型在内部左侧、附件/发送在内部右侧 —— 与需求描述的"目标形态"一致。视觉跳变来自**两个槽位的外层规则不一致**：

| 位置 | 规则 | 差异 |
|---|---|---|
| `AI.html:163` | `.ai-welcome .ai-input-wrap{width:100%;max-width:760px;margin-top:8px;}` | 空态：**760px** + `margin-top:8px` |
| `AI.html:232` | `.ai-input-dock .ai-input-wrap{max-width:768px;margin:0 auto;}` | 对话态：**768px** + `margin:0 auto` |
| `AI.html:491`（媒体查询内） | `.ai-welcome .ai-input-wrap{max-width:none;}` | **窄屏下空态变全宽**，对话态仍 768px → 跳变更明显 |
| `AI.html:500`（媒体查询内） | `.ai-input-box{border-radius:18px;}` | 窄屏圆角 |
| `AI.html:158-161` | `.ai-welcome{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:24px 16px;}` | 空态下输入框**垂直居中**于整块区域 |
| `AI.html:231` | `.ai-input-dock{flex-shrink:0;padding:10px 16px 14px;background:var(--ai-bg);}` | 对话态下输入框**贴在底部** |
| `AI.html:235-242` | `.ai-input-box{background:var(--ai-bg);border:1px solid transparent;...}` / `.ai-input-box.focus{background:var(--ai-card);border-color:rgba(0,0,0,.06);box-shadow:...}` | **未聚焦时容器背景与页面底色相同、边框透明 → 容器"隐形"，里面的控件看起来是"散开的"**；聚焦/对话后变白卡片 → 看起来是"一体化容器" |
| `AI.html:312` | `.ai-input-box{position:relative;}` | — |

> ⚠️ **最后一条（`.focus`）很可能是用户看到的"变脸"主因**：空态未聚焦 → 容器透明、控件看起来分散；发消息后输入框获得/保持 `.focus` → 容器显现为白卡片。
> **动手前请先复现确认**到底是"槽位宽度差异"还是"`.focus` 透明态"导致（可参考 §10 用 jsdom 或真机/浏览器复现），不要凭猜测改。

### 6.3 改造要求
1. **不管空白新会话还是已有聊天记录，输入区域 UI 完全一致**：
   - 整体大圆角容器包裹输入框；
   - 「深度思考」按钮、模型下拉选择框**在容器内部左侧**；
   - 附件图标、发送箭头**在容器内部右侧**。
2. **只统一外观布局，不改现有交互逻辑**（`enterConversation/enterWelcome/mountInput` 的行为、发送、上传、模型面板、深度思考互斥逻辑都不动）。
3. 页面其它元素（欢迎文字、消息气泡、复制/重新生成按钮）保持原样。
4. **不建议为了统一样式而删掉双槽位机制** —— 最稳的做法是让两个槽位的外层规则完全一致（或统一由 `.ai-input-box` 自身决定外观，不依赖父级），把 `AI.html:491` 的窄屏覆盖改掉。
5. 改完必须**同时验证窄屏与宽屏**，且**电脑端与手机端一致**。

### 6.4 验收
- [ ] 打开 AI 新会话空白页，输入框就是一体化圆角容器样式，与聊天后完全一致
- [ ] 发送消息产生对话后，输入区域样式**不发生任何变化**
- [ ] 深度思考、模型切换、附件上传、发送按钮功能全部正常
- [ ] 窄屏 / 宽屏、手机端 / 电脑端表现一致

---

## 7. 需求 D：AI 接口 400 修复 + 模型降级与超时优化

> 依据：`D:\下载的文件\学习工作台\AI模型测试报告与修复需求-20260916.md`
> 报告结论：**11 个内置免费模型 Key 与模型名全部有效**（限流的 GLM-4.7-Flash / GLM-4.6V-Flash 属正常高峰限流）。**前端 400 不是模型问题，是前端构造请求体的问题。**

### 7.1 关键位置（已实测）
| 内容 | 位置 |
|---|---|
| 核心请求函数 | `assets/ai-service.js:384` `async function requestModel(modelConfig, messages, onChunk, signal)` |
| **报 400 的 fetch 调用** | `assets/ai-service.js:411` `var resp = await fetch(provider.apiUrl, fetchOpts);` |
| 请求体构造 | `ai-service.js:393-399`：`{ model, messages, temperature, max_tokens, stream: true }` |
| 错误归一化 | `ai-service.js:412-418`（`resp.ok` 为假 → 抛 `makeError("API错误:" + resp.status + " " + emsg, resp.status, ecode)`，**错误对象里带 `status`**） |
| 流式解析 | `ai-service.js:420+`（`resp.body.getReader()` + `TextDecoder`，按 `data:` 行解析 SSE） |
| 流式内的错误 | `ai-service.js:433-437`（`obj.error` → 抛错；`code===1305` 被特殊处理为 200） |
| provider 降级链 / 重试循环 | `ai-service.js:350`（另一处 `resp.body.getReader()`）与 `ai-service.js:375-381`（`lastErr`、`服务端中转全部失败`） |
| 模型/provider 配置 | `assets/ai-config.js`（**本需求范围明确：不修改它的模型列表和参数**） |

### 7.2 🚨 400 的头号可疑根因（我已定位，请优先验证）
`assets/ai-service.js:77` 与 `:91` 把图片消息**硬编码为 JPEG**：
```js
{ type: "image_url", image_url: { url: "data:image/jpeg;base64," + image } },
```
**如果用户上传的是 PNG / WebP / HEIC**，data URI 声明 `image/jpeg` 而字节是别的格式 → 视觉模型（GLM-4.6V-Flash / GLM-4V-Flash）会因 MIME 与内容不符拒绝 → **400**。这完全符合报告里"发图片最容易 400"的现象。

**要求：**
- 打印/记录**完整请求体**（`model`、`messages`、图片字段与实际 MIME、全部参数），定位真实 400 根因；
- 按智谱官方文档构造 `image_url`：**base64 必须带正确的 MIME 前缀**（`data:image/png;base64,` 等，从文件真实类型推导，而非写死 jpeg），或先上传拿 URL；
- 确认**前端显示名**（如 `GLM-4V-Flash`）与**实际请求的 `model` 字段**（如 `glm-4v-flash`）是分开的，**不要把显示名当 model 发**。

### 7.3 降级触发条件（必须精确）
- **只有** `429`（限流）、`5xx`（500/502/503/504）、**网络超时** 才触发自动降级切换；
- **`400` 不触发降级** —— 直接展示后端返回的 `error.message`。否则会掩盖真正的参数 bug（现状就出现了"提示已切换到 GLM-4V-Flash，但切完还是 400"的误判）；
- 降级按 `ai-config.js` 里每个模型的 `fallback` 链**依次**切换，切换后提示「已切换到 XX 模型」；
- 实现要点：`ai-service.js:417` 抛出的错误对象**已带 `status`**，可直接用它判定。

### 7.4 超时策略（15 秒未出首字即切换）
- 所有 AI 请求超时设为 **15 秒**（不是 30/60 秒）；
- 15 秒内未收到**流式首 token** → 立即按 fallback 链切下一个快模型，并提示「响应较慢，已切换模型」；
- 慢模型（Qwen3-8B、Qwen3.5-4B）**仅用户手动选择时使用**，自动模式不优先选。

### 7.5 错误提示（用户侧必须有反馈）
- 接口报错时在**对话区**显示可读错误（如「图片格式不支持」「当前模型繁忙，已切换」），**不要只在控制台报红**；
- 400 必须把后端返回的 `error.message` 展示出来。

### 7.6 限流模型的备选优化
GLM-4.7-Flash 作为自动模式首选，高峰期经常 429 → 在自动路由中增加：**若某模型连续 2 次 429，本次会话内直接跳过它**，改用 GLM-4.5-Flash 或 Qwen2.5-7B 作为首选。

### 7.7 流式输出（核心）—— ⚠️ 有一条硬约束必须先知道
要求：所有对话开启 `stream=true`，**2 秒内出第一个字**、逐字显示；等待时显示「AI 正在思考…」加载态；流式中断时保留已显示内容并提示「连接中断，点击重新生成」。

**现状**：`ai-service.js:398` **已经**是 `stream: true`，且 L420+ 已实现 SSE 逐字解析。

**⚠️ 硬约束（老内核）**：项目有 `assets/net-compat.js`，其注释（`net-compat.js:9,16,17,296,440-444`）明确写着：
> Chromium 42：有 fetch，但**无流式 `body.getReader()`** 的早期实现 → 为「有 fetch 但没有 `body.getReader()`」的中间版本补一个**一次性流的读器**，让 `ai-service.js` 的 `resp.body.getReader()` 流式循环能跑通（**退化为整段返回**）。

**即：在安卓 APK 的老 WebView 里，流式会自动降级为"一次性返回"，做不到"2 秒内逐字出字"。**
👉 因此「流式 2 秒出字」的验收标准**只能在支持 `ReadableStream` 的现代浏览器上达成**；老内核路径需要**另设降级验收口径**（例如：至少保证「AI 正在思考…」加载态 + 15 秒未出首字切换）。**动手前请与用户对齐这个口径**，否则验收必然不通过。
- 相关代码：`ai-service.js:350`、`:420` 的 `resp.body.getReader()`；`api.js:919`、`:967` 也有；`app.js:3043-3044`、`:3360-3361`、`:7505-7506` 则用了 `if (res.body && res.body.getReader)` **带守卫**的写法（**更安全，建议对齐这种写法**）。

### 7.8 验收
- [ ] 11 个模型逐一手动选择发文字，全部正常回复、无 400
- [ ] 发图片，视觉模型（GLM-4.6V-Flash / GLM-4V-Flash）能正常识别回复、无 400
- [ ] 人为构造 400 时**不触发**「繁忙自动切换」，页面显示明确错误
- [ ] 模拟 429 时按 fallback 链切换并提示
- [ ] 流式：现代浏览器下 2 秒内可见文字开始输出（**老 WebView 按 §7.7 的降级口径**）
- [ ] 15 秒未出首字自动切换快模型并提示，不傻等
- [ ] 自动模式默认用快模型（GLM-4-Flash / Qwen2.5-7B），不优先慢模型
- [ ] 等待时有「AI 正在思考…」加载态
- [ ] 控制台无连续 400

### 7.9 明确不在范围内
不更换/不新增模型、不修改 Key、**不修改 `ai-config.js` 的模型列表和参数**、不涉及后端 `/api/chat` 中转逻辑（400 是前端直连智谱的问题）。

---

## 8. 改动后的自检（每批必跑）

`tools/qa/` 下已固化以下脚本（依赖 `tools/verifier/node_modules` 的 jsdom/acorn，路径硬编码，保持目录不动）：

| 目的 | 脚本 | 用法 |
|---|---|---|
| **老内核解析级体检（最重要）** | `tools/qa/escheck_es2017.js` | `node tools/qa/escheck_es2017.js` → 必须输出 **`(全部脚本 ES2017 解析通过)`** 或 `DONE 0`；任何命中都是**老内核必崩**的致命点 |
| 单页运行时体检 | `tools/qa/page_check.js` | `node page_check.js "页面名.html" "关键词1,关键词2"` |
| 多页导航体检 | `tools/qa/multi_check.js` | 无参，跑 8 个页面 |
| AI 端到端（真页面） | `tools/qa/ai_jsdom_check.js` | jsdom 加载 AI.html → 输入 → 点发送 → 断言气泡**不含**「本地参考」 |
| 机器人球点击/拖动 | `tools/qa/ball_verify.js` | 断言球存在 + 触摸拖动后位置变化 |
| 拉线上文件对比本地 | `tools/qa/fetch_srv_files.py` | 下载线上文件与本地做 md5 对比，快速发现"线上还是旧的" |

**注意**：jsdom 会报 `Not implemented: navigation to another Document` —— 这是 **jsdom 自身限制，不是 bug**，看错误列表时请忽略。

此外每批必须自查：
- [ ] 改动的 js 跑 `node --check`
- [ ] `<!--`/`-->`、`<div`/`</div>` 配对平衡
- [ ] 无新增顶层重复全局声明
- [ ] 无 §2.1 禁用语法
- [ ] 改动过的资源 `?v=` 已 bump
- [ ] 全站「不带 `?v=` 的 assets 引用」= 0

---

## 9. 部署流程

**照抄 `tools/deploy_update_20260916k.py` 改 3 处：**
1. `STAMP = '20260916X'`（新版本戳）
2. `files = [...]`（**要上传的 `assets/` 文件列表**；纯 HTML 改动则留空 `[]`）
3. 远端校验段的 grep 关键词

脚本会自动：bump 全部根 HTML 的 `?v=` → 打包 `tar.gz` → `pscp` 上传 → `plink` 远端解压 → `curl` 状态码 + `grep` 内容校验。

**先跑 `SW_DRY_RUN=1` 预演确认打包正常，再真跑。**

### 9.1 部署脚本的坑（血泪清单）
1. **凭据必须是 `root@ip`**：正则若只抓到裸 IP，plink/pscp 会用本地用户名认证 → `Access denied`，报错信息是极具迷惑性的「password not accepted」。模板脚本已处理。
2. **远端备份命令必须容错**：`tar -czf 备份 <filelist>` 在文件不存在时会让 `&&` 链断掉 → **根本不解压**（现象：上传成功但线上 grep 报 No such file）→ 备份命令后加 `|| echo BACKUP_SKIP_NEW`。
3. **部署后一定实测**：`curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1/xxx.html` + 对关键字符串 `grep`，不要只看上传 exit code。
4. **版本戳**：新上的 js 必须带 `?v=` 且 bump，否则用户拿到旧文件。
5. 别用 `sw` 当变量名（与 shell 冲突）。
6. **用 `curl -s -D -` 看响应头，不要用 `-I`**；期望 `Cache-Control: no-cache`。

---

## 10. 复现与验证要求（不要靠"看起来对"）

- **前端行为验证必须用 jsdom 加载真实完整页面**，不要只测服务层。
  > 历史教训：曾用 vm 只加载 `ai-config.js`/`ai-service.js` 得到"真模型回答"，但没加载 `app.js`，掩盖了全局声明冲突。**只测服务层会假绿。**
- **"某个模块集体失效"优先怀疑该 js 解析失败**（跑 §8 的 `escheck_es2017.js` 最快），而不是逐个按钮排查。
- 视觉类需求（§4 弹窗、§6 输入框）**必须真机/浏览器截图对比**，窄屏（375px）与宽屏（1280px）都要出图。

---

## 11. 交付要求

完成后向用户汇报（不要只写文件）：
1. **TL;DR**：一句话说明交付了什么
2. **逐需求结果**：每组需求的改动文件清单 + 自检真实输出（不要写"已通过"，贴实际数字）
3. **部署结果**：dry-run 结论、上传 exit code、远端各页 http_code、关键 grep 计数
4. **未决/风险**：做了什么假设、哪些点需要用户确认（尤其 §4.1 的 ADR-3 冲突、§5.4 的「广场」范围、§7.7 的老内核流式口径）
5. **下一步建议**

---

## 12. 附录：其它在途待办（本批之外，但别忘）

以下事项来自更早的需求文档，**尚未完成**，请勿与本批混淆：

| 事项 | 状态 |
|---|---|
| **AI 页侧边栏「模型」按钮点击无反应** | 根因未定位（诊断任务被中断），需重新排查：确认侧边栏「模型」/「设置」入口的 `onclick` 指向的函数是否真实存在、与输入区「自动 ▾」（`#aiModelBtn` + `#aiModelPanel`）是否为同一面板 |
| **用户头像加载不出来** | **根因已定位**：字段与判据错配 —— `assets/ai-page.js:265` 用 `apiFileUrl(CURRENT_USER.avatarUrl)`（正确），但 `assets/app.js:7910`、`assets/api.js:1560`、`assets/api.js:1278`、`assets/api.js:1468` 读的是 `p.avatarImg` 且**强制 `/^data:image\//` 开头**；而 `assets/api.js:196` 明确写「多人在线版：头像为文件上传（**不再 base64 入库**）」。→ 在线模式下 `avatarImg` 是 URL 或空，判据必然失败 → 回退首字头像。**修法**：统一字段（`avatarUrl`）+ 统一走 `apiFileUrl`（定义在 `assets/api.js:20`：`function apiFileUrl(u){ return (!u || /^(https?:|data:)/.test(u)) ? u : (window.API_BASE + u); }`），并保留 `<img onerror>` 兜底 |
| **首页「重要倒计时」卡片缩小一档** | 需先定位 CSS（重点 `assets/common.css` / `polish.css` / `head.css` 与 `学习工作台.html` 内联 `<style>`）。**风格参考**：同项目此前做过轮播卡缩小（`.home-carousel-card` padding 20px→14px、窄屏 10px、字号与高度同步下调，并加 `@media (max-width:480px)` 断点），新改动与它保持同量级 |
| **更多页「关于」改为独立页面 `关于.html`** | `showAbout()` 当前是弹窗实现（在 `assets/app.js` 内），需改为页面跳转；新建 `关于.html` 时要复用现有页面骨架（顶部栏 `.topbar` + 返回 `.morepage-head`/`.morepage-back` + `.content`/`.page` + 底部导航顺序遵循本文 §3.1 的 `首页→互动→我的→AI→更多`），并**必须保留**共用 DOM：`#countdownModal`、`#toast`、`#aiFab`、`#aiPanel`、`#morePanel`、`#toolsPanel`、`.more-overlay`。全站 `showAbout` 调用点需一并替换 |

---

## 13. 有用的历史文档（都在 `D:\下载的文件\学习工作台\`）

- `交接文档-20260916-全站修复收口与APK.md`（上一轮全站修复，含 APK 打包 6 项必查）
- `交接文档-批次九-20260915.md`、`任务执行文档-20260915-全线自主执行.md`
- `排查报告-20260915-HTML.md` / `-JS.md` / `-UI.md`
- `星途平台-线上全站体验报告-2026-09-13.md`（含功能中心卡片图标升级等 P0/P1/P2 清单）
- `安卓App安装说明.md`、`手机真机打包运行方案.md`
- `AI模型测试报告与修复需求-20260916.md`（本文 §7 的依据）
