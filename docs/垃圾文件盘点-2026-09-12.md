# 星途项目 · 垃圾文件盘点报告（2026-09-12）

> 方法：只读扫描（Glob / PowerShell 实测文件大小与数量 / git ls-files 核对入库状态），未删除、未移动任何文件。
> 工作区总占用 ≈ 71MB（不含 .git），其中**约 60% 是垃圾/备份/临时文件**。
> 好消息：**git 仓库本身很干净**（入库 291 个文件，垃圾极少）——绝大多数垃圾是未跟踪的工作区文件，清理不影响 git 历史。

---

## 一、总体数字

| 区域 | 大小 | 文件数 | 性质 |
|---|---|---|---|
| 备份/ | 35.7MB | 278 | 历史备份 + 2 份 git 快照 + 1 份敏感数据 |
| tools/ | 28.4MB | 2079 | 其中 node_modules 21.5MB（**活的**），其余多为一次性脚本 |
| server/ | 4.3MB | 213 | 其中 .venv 3.7MB（残缺、实际不用） |
| 根目录 | ~15MB | 235 | 活页面 32 个 + **垃圾重灾区**（脚本/截图/空文件） |
| .qa/ + .tmp_eng/ | 1.2MB | 27 | QA 复现截图 + 临时打包目录 |
| 项目本体（assets+data+docs+android+活页面） | ~10MB | — | **必须保留** |

---

## 二、A 类：纯垃圾，可直接清理（未入库，删除无 git 影响）≈ 17MB

| # | 内容 | 数量 | 体积 | 依据 |
|---|---|---|---|---|
| A1 | 根目录一次性 Python 脚本：`do_rename_*`×39、`fix_*`×26、`rename_*`×16、`unify_*`×7、`restore_*`×4、`modify_*`×4、`add_*`×8、`enhance/merge/rewrite/rollback/...` 若干 | **~103 个** | ~1MB | 全部未入库（git ls-files 为 0）；均为历史改版的「一次性手术脚本」 |
| A2 | 根目录调试截图 PNG：`bug1~11.png`、`sidebar_broken1~3.png`、`old_settings_1~5.png`、`screenshot_*`×7、`two_learn.png`、`profile_*`×3、`home_broken/page_broken/error_screenshot`、`wx1~3.png`、`more_menu_5`、`settings_*`×5、`ai_settings_full` | **~38 张** | ~12MB | 排错过程截图，问题均已修复 |
| A3 | 空文件/杂项：`ai1.json`、`bad.json`、`dup.json`、`exam-自定义.json`、`参考文件`(0B)、`deadpages.txt`(0B)、`rootcount.txt` | 7 个 | ~0 | 空壳文件；deadpages/rootcount 是本会话遗留 |
| A4 | `tools/qa/_*.txt`（QA 一次性输出） | **71 个** | ~1MB | gitignore 已排除（`tools/qa/_*.txt`），物理残留 |
| A5 | `tools/*.tar.gz`（历史部署包） | **9 个** | ~3MB | gitignore 已排除，服务器上已有同版本 |
| A6 | `tools/` 一次性脚本：`fix_*`×9、`probe_*`×11、`patch_*`×14、`deploy_update_2026091*`×8、旧 `verify_*`×9 等（**保留**：`bump_versions_0912a/d.py`、`deploy_update_20260912a.py`、`cleanup_deadcode_0912d.py`、`pscp/plink.exe`、`verifier/`、qa 活脚本） | ~60 个 | ~1MB | 均为已完成批次的一次性产物 |
| A7 | `.qa/` 整目录（QA 复现截图+快照） | 10 个 | 0.8MB | `docs/缺陷清单-2026-09-11.md:174` 自述「报告引用完可整目录删除」 |
| A8 | `.tmp_eng/` 临时打包工作目录（含 `学习工作台-安卓App.apk.bak-20260909`） | 17 个 | 0.4MB | APK 打包临时区，产物已拷出 |

---

## 三、B 类：敏感文件，建议移出项目目录（而非删除）

| # | 文件 | 问题 |
|---|---|---|
| B1 | `upload_v23.ps1` | **含 root 服务器明文密码**（部署脚本会从这里正则提取）。虽已 gitignore，但明文躺在工作区任何本地扫描都能读到 |
| B2 | `备份/purged-secrets-20260911/data.db.bak` | **数据库备份，含真实用户数据**（密码哈希、手机号、生日）。09-11 泄漏事件处置产物 |
| B3 | `备份/git-history-backup-20260911`（10.4MB/43 文件）<br>`备份/git-before-purge-swap-20260911`（20.9MB/132 文件） | 两份完整 git 历史快照（防丢保险）。现在远端 GitHub 已恢复同步（HEAD=a53d218 已核实入库），保险价值下降，建议移到项目外另存或择期删 |

---

## 四、C 类：历史备份，可压缩归档（有回滚价值）

- `备份/` 根下 ~180 个 `*.backup-20260911*.html/js/css`（每次改版的手工备份）+ `学习工作台-单文件版-20260908.html` + `备份/cleanup-20260912/`（今天的 19 项死代码）
- 现在全部改动都在 git 里了（六 commit 已入库），手工备份的意义只剩「git 之外再一道保险」。建议：**保留最近 2 天的（20260912 系列 + cleanup-20260912），09-11 及更早的可打包成单个 zip 归档**。

## 五、D 类：看似垃圾但必须保留

| 内容 | 原因 |
|---|---|
| `tools/verifier/node_modules`（21.5MB） | jsdom 校验器，**项目最可靠的前端验证手段**（未入库，删了要重装） |
| 9 张入库 PNG（参考图×3、search_bug、security_group、server_config×2、tools/截图×2） | 已入 git，删除会改仓库；体积小，留着无害 |
| `学习工作台-安卓App.apk`（657KB） | 活产物 |
| `优化方案1/2.docx`、`参考文件` 相关 | 用户自己的参考资料 |
| `server/.venv`（3.7MB） | 残缺虚拟环境，**本机跑后端实际用 `envs\xingtu-backend`，不依赖它**——严格说可删，但按记忆「残缺勿修」原则保守起见列此待定，删前先验证 |
| `.codebuddy/`、`.workbuddy/` | 配置与项目记忆 |

---

## 六、🚨 顺带发现的两个新问题（盘点意外收获）

1. **用户隐私数据已入 git**：`server/uploads/avatars/u1_*.png`、`server/uploads/images/u1_*.png`×2、`u2_*.png` 共 4 张用户上传图片（头像/聊天图）在入库清单里。`server/uploads/` 是运行时上传目录，**不该进版本库**（gitignore 漏了它，只挡了 `server/.env`/`data.db*`）。建议：gitignore 补 `server/uploads/` + `git rm --cached` 移出跟踪。
2. **垃圾文件混进了 git**：`ai1.json`、`bad.json`、`dup.json` 三个空/杂文件已入库（与 A3 同类）。建议随下次提交 `git rm` 掉。
3. **注意**：`ai-server/README.md` 等仍在 git index 里（今天已把 `ai-server/` 移入 `备份/cleanup-20260912/`），下次提交时会自动体现为删除，属预期。

---

## 七、清理后的预期效果

| 方案 | 动作 | 工作区从 71MB 降到 |
|---|---|---|
| 最小清理 | 只做 A 类（移入 `备份/cleanup-20260912/`，延续「移动非删除」惯例） | ~54MB |
| 标准清理 | A 类 + C 类归档 zip + B3 git 快照移出项目 | ~33MB |
| 深度清理 | 再加 server/.venv（验证后） | ~29MB |

**git 仓库体积不受任何影响**（A 类全部未入库）。

## 八、执行安全规则（若用户批准清理）

1. 全部走「移动到 `备份/cleanup-20260912/`」而非真删，可一键回滚（延续上轮 T06 惯例）。
2. 分小批执行（每批 ≤ 10 项类别），每批后 Glob 验证。
3. `upload_v23.ps1` 不删——部署脚本还依赖它的正则提链；**移走前必须先把部署脚本改为纯环境变量取密**（SW_HOST/SW_PASS），否则下次部署直接失败。
4. `server/uploads` 入库问题单独处理：gitignore + `git rm --cached`（需要用户确认后执行）。
