# 星途平台 · 文档/日志 vs 代码实测 校准报告

- 校准日期：2026-09-12
- 校准人：架构师 高见远（software-architect）
- 方法：只读通读。对根目录 9 份权威文档、`docs/` 关键文档、`.workbuddy/memory/` 四份日志、以及仓库 Git 历史逐条比对代码实测。
- 边界声明：本轮不修改任何业务代码，不修改已有文档；本报告为新建。

---

## 一、文档清单与用途表

| 文档 | 位置 | 用途 | 最后更新/版本线索 | 是否有效 |
|---|---|---|---|---|
| 运行说明.md | 根 | 多人博客版部署与运行（目录结构+服务器+Nginx） | 含 `upload_v23.ps1` 时代，无 `data/`、无 `工具.html`/`更多.html`/`subpage-router.js` | ❌ 已滞后 |
| 增量修改说明.md | 根 | 5 项增量改造说明 | 仍称 `ai-server/` 为后端扩展基础（该目录已死） | ❌ 已滞后 |
| 安卓App安装说明.md | 根 | APK 安装/单机离线说明 | v1.8，仍用旧品牌「学习工作台」 | ⚠️ 部分滞后（版本对、品牌错） |
| 手机真机打包运行方案.md | 根 | APK 打包流程 | v1.1 | ❌ 严重滞后（现 v1.8） |
| 整改处理方案-批次三.md | 根 | 豆包遗留项 1/3/4/5/6 整改 | 明确排除 HTTPS | ✅ 有效 |
| 星途平台-合并实施路线图-2026-09-12.md | 根 | 全量需求归位 + 10 条执行线 + 待拍板 | 现状快照日期 09-12 | ⚠️ 关键章节已滞后（见第二节） |
| 星途平台改进-需求清单.md | 根 | 30 条需求 What 清单 | 09-12 | ✅ 有效（需求源） |
| 星途平台改进-技术实施方案.md | 根 | 豆包手册采纳整合 How | 09-12 | ✅ 有效 |
| QA分层执行改造方案-2026-09-12.md | 根 | L1/L2/L3 QA 模型 | 09-12 | ✅ 有效 |
| docs/项目全景说明书-2026-09-12.md | docs | **上一轮代码实测产物**（7 条结论+依据索引） | 09-12 | ✅ 有效（本报告校准对象） |
| docs/缺陷清单-2026-09-11.md | docs | QA 复核 6 类 Bug + 21 项需求盘点 | 09-11 | ⚠️ 部分条目被代码架空 |
| docs/p0b-rebuild-prd.md / -arch.md | docs | P0-B 重建 PRD/架构 | 09-12 | ✅ 有效（已落地） |
| docs/增量PRD-图标与交互-2026-09-12.md | docs | 批次五图标与交互 | 09-12 | ✅ 有效（已上线） |
| docs/WITHDRAWN.md | docs | 撤回登记 | 仅 HTTPS 1 条 | ✅ 有效 |
| .workbuddy/memory/MEMORY.md + 09-08/09/11/12.md | memory | 长期约定 + 日进度日志 | 至 09-12 | ✅ 有效（一手进度源） |

> 结论：**根目录 4 份「运行/打包」类文档已整体滞后于代码**；`docs/` 与 `memory/` 是当前较可信的一手来源。

---

## 二、对上轮 7 条结论的校准

### 结论 1 — 项目形态与体量（静态站 / 32 活页 / assets 27 js 16407 行 / 后端 84 接口 23 表 / WebView 壳）
**✅ 确认。** 文档交叉印证：`运行说明.md` 目录结构与 `docs/项目全景说明书` 一致；`安卓App安装说明.md`（v1.8）与 `手机真机打包运行方案.md` 佐证 WebView 壳路线。无任何文档推翻该形态判断。

### 结论 2 — 「代码已跑到文档前面」
**✅ 确认，且有文档原话反证自身滞后。** 路线图 `现状快照` 表（第 18-19 行）明确写：
> 「❌ 已核验未开工：需求 **03/04/05/12 的删减**…P0-A 全批」

而第 140-155 行需求归位表逐条标注 `15/16/18/23/30（P0-A）= 未开工`、`B2 未开工`。但代码实测（`assets/app.js` 的 `lsKey()`/`migrateLegacyKeys()`/`STREAK_KEY = lsKey('study_workbench_streak')`）证明 T15/T18 已落地；`mock_exam.html`/`_run`/`_result` 三页 + `data/mock-papers.js` 证明 B2 已落地；令牌已由 `20260912a` 推进至 `20260912c`（15+ HTML 页面 grep 命中）。**路线图未随代码更新，是最主要的滞后文档。**

### 结论 3 — 高危：`data/` 未进任何交付管线
**✅ 确认（双向证据）。** 交付侧：`tools/deploy_update_20260912a.py` 对 `data/` **零引用**（Grep 无匹配），`:61-86` 仅打包 root html + `assets/*` + `assets/emoji/*`；三份 APK 脚本同构。消费侧：`mock_exam*.html` 均以 `data/mock-papers.js` 为数据源。→ 模考三页部署/打包后**必 404**。文档侧无人提及此风险（`运行说明.md` 目录结构里根本没有 `data/`），属文档盲区。

### 结论 4 — 死代码（`assets/chat.js`、`ai-server/`、`settings_*.html`×12、`profile_*.html`×3、`设置_旧版.html`）
**✅ 确认。** 反证：`增量修改说明.md` 仍把 `ai-server/` 当作「后端扩展基础」介绍——即文档在推荐一份已死目录，进一步证明其滞后。

### 结论 5 — 需求 03/04/05/12 的删减均未执行
**✅ 确认。** 路线图第 19 行原文：「需求 03/04/05/12 的删减（`写作提升`/`数量关系`/`题型正确率`/`情景选择实战`/工具页金句入口 **仍在页面中**）」——文档与代码实测**结论一致**，这一条上轮判断与文档完全对齐。

### 结论 6 — 手册 10 模块中 8 个未开工
**➕ 补充。** 文档侧未见统一「手册模块完成度」表；证据来自路线图 B3–B6 执行线（第 81-118 行）仍为规划态，以及 `星途平台改进-技术实施方案.md` 把阅读/翻译/口语等列为后续阶段。结论方向一致，但**文档没有形成可核对的模块清单**，建议补一张完成度矩阵。

### 结论 7 — 部署脚本从 `upload_v23.ps1` 正则提取 root 明文密码
**✅ 确认。** `tools/deploy_update_20260912a.py:25-32` 存在该正则提取逻辑；6 个 deploy/precheck 脚本均引用 `upload_v23.ps1`。日志 `2026-09-11.md` 记录过公开仓库泄漏 root 密码 + `server/data.db`、filter-repo 重写历史但仍可读旧 SHA——**代码与日志互证**，风险仍然成立。

> 汇总：7 条中 **7 条全部确认成立，0 条被推翻**；结论 6 需补充「文档缺完成度矩阵」的说明。

---

## 三、09-08 → 09-12 进度脉络（日志还原）

| 日期 | 批次/主线 | 关键提交/事件 | 踩坑 | 节点状态 |
|---|---|---|---|---|
| 09-08 | 多页拆分 | 单文件→多页、登录门禁、个人中心、全局搜索、AI 服务商配置、APK 打包 | — | 多页骨架成型 |
| 09-09 | APK 加固 | v1.4→v1.8 TTS 加固 | APK -124（`resources.arsc` 需 STORED+对齐）；WebView `file://` 稳定组合 | 打包链路打通 |
| 09-11 | 安全+批次一/二/四 | 公开仓泄漏 root 密码 `Li050800!` + `server/data.db`，filter-repo 重写历史；A1-A9；批次二 12 条；隐私与群设置 | 中文文件名 pscp 大坑；**23:09 .git 对象库被沙箱删除**（pack 从回收站找回） | 事故恢复 |
| 09-12 | 批次四收口 / 批次五 / P0-B | 批次五图标交互上线（`20260912a`）；**P0-B 重建六 commit，HEAD=`a53d218`**；.git 销毁事故二次恢复 | BUG-1 私密动态泄漏 feed、BUG-2 `everyone` 残留 | 当日收口 |

**P0-B 六 commit 与事故恢复的仓库核实（只读 `git log`）：** 真实存在且顺序正确——
`a53d218`(T05 HTTPS/gitignore) → `3195b97`(T04 24 页接入) → `fe02cce`(T03 模考引擎) → `35a1c68`(T04-00 公共基础，新增 `states.css`/`page-head.css`/`error-boundary.js`/`xt-toast.js` 等 7 文件 1037 行) → `2930d3d`(T02 图形推理题干) → `4a16760`(T01 密码强度，`server/schemas.py` +52)。其下为 `0247420`(批次五+APK) → `d402184`。HEAD 提交时间 `2026-09-12 20:56:21 +0800`。**→ 日志所述「六 commit 收口 + .git 恢复」与仓库实测完全吻合，可信。**

> 另注：`git status --short` 显示多份 `docs/` 与工具文件**未入库**，两份根目录需求文档也未入库——文档资产存在入库缺口。

---

## 四、基于文档/日志的最高优先级行动（3-5 条）

| # | 行动 | 文档依据 | 与上轮代码建议对照 |
|---|---|---|---|
| 1 | **把 `data/` 纳入部署与 APK 打包管线**，并在 `运行说明.md` 补目录 | 上轮 C2；`运行说明.md` 目录无 `data/` | ✅ 一致（代码建议同条） |
| 2 | **清除 root 明文密码依赖**：将凭据移出 `upload_v23.ps1` 正则链，改用环境变量/密钥文件 | `2026-09-11.md` 泄漏事故；deploy 脚本 `:25-32` | ✅ 一致 |
| 3 | **重做「现状文档」**：更新路线图 `现状快照` 为 `20260912c` 实况，消解「未开工」误标 | 路线图 18-19/134-155 行自相矛盾 | ✅ 一致（上轮第 3 条） |
| 4 | **补「手册模块完成度矩阵」与「需求 21/22/B2 已上线」登记** | 路线图 B0 把 P0-B 记为「待收口」，实际已 commit | ➕ 文档驱动的新增项（代码建议未覆盖） |
| 5 | **对齐根目录品牌与版本**：4 份运行/打包文档统一到 v1.8 + 「星途」品牌 | `安卓App安装说明.md`/`手机真机打包方案.md` | ➕ 文档驱动的收尾项 |

> 一致性判断：**上轮基于代码的 3 条建议（补 `data/` 管线、清 root 密码、重做现状文档）与文档/日志证据完全一致，无冲突**；文档视角额外补出 2 条「文档治理」类行动（完成度矩阵、品牌/版本对齐）。

---

## 五、「如果只信文档会犯什么错」警示清单

1. **会以为 P0-A / B2 / 需求 03/04/05/12 都「未开工」**——路线图明写「未开工」，实际 T15/T16/T18/B2 已 commit 上线，照文档派工会**重复劳动**。
2. **会以为部署脚本覆盖全量资源**——`运行说明.md` 目录结构压根没有 `data/`，照它部署会**漏掉 `data/mock-papers.js`，模考三页 404**。
3. **会照着 `增量修改说明.md` 去用 `ai-server/`**——该目录已死，文档仍在推荐，会**在废弃代码上二次投入**。
4. **会按 v1.1 打包方案操作**——`手机真机打包运行方案.md` 落后现网 v1.8 七个小版本，**打包必失败**（缺 STORED+对齐等关键修正）。
5. **会以为 P0-B「待收口」**——实际六 commit 已入库（HEAD=`a53d218`），照文档会**重复收口**。
6. **会误判安全水位**——文档未标注 `upload_v23.ps1` 明文密码链，照文档运维会**延续 09-11 泄漏风险**。

> 一句话忠告：**先读 `docs/项目全景说明书-2026-09-12.md` + `memory/2026-09-12.md` + `git log`，再读根目录「运行/打包」类文档；后者只可用于环境背景，不可作为现状事实来源。**
