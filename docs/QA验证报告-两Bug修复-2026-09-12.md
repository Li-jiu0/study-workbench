# QA 验证报告 · 两 Bug 修复（T06 / 2026-09-12）

- 验证人：QA 工程师 严过关（Edward）
- 验证对象：Bug1（个人中心 prefs/local-data 空白）、Bug2（模考页后退死循环）、任务3（死代码清理）
- 验证方式：**独立视角**（自建 jsdom 沙箱，不复用工程师脚本逻辑）
- 验证脚本（新增，非业务代码）：
  - `tools/qa/qa_bug1_profiles.js` —— Bug1 页面真加载
  - `tools/qa/qa_bug2_goback.js` —— Bug2 goBack 真执行 + 反向自证
  - `tools/qa/qa_global_pageload.js` —— 全局 4 页真加载、零未捕获异常
  - `tools/qa/qa_appjs_impact.js` —— app.js 抛错影响面评估
  - `tools/qa/inspect_data.js` —— mock-papers 真实分类核对
- 环境：node 22.22.2-3 + jsdom 30.0.1（复用 `tools/verifier/node_modules`）

## 结论速览

| 项 | 结果 | 判定 |
|---|---|---|
| Bug1 prefs/local-data 空白 | 4/4 用例 PASS（含有数据、空数据边界） | ✅ 修复生效 |
| Bug2 goBack 三页映射 | 15/15 用例 PASS + 反向自证有效 | ✅ 修复生效 |
| Bug2 分类映射是否与真实数据一致 | `cet-*`→cet4、`exam-*`→exam，与 mock-papers.js 完全一致（非猜测） | ✅ 属实 |
| Bug2 回归（beforeunload/saveDraft） | 三页无 `history.back()` 活调用；引擎 beforeunload 挂载点与 saveDraft 未动 | ✅ 未破坏 |
| 任务3 死代码清理 | 19 项确认在 `备份/cleanup-20260912/`，未删除；活代码引用 0 | ✅ 属实 |
| 全局：4 改动页零未捕获异常 | 个人中心 PASS；3 模考页触发 **app.js 既有** 抛错（非本次改动） | ⚠️ 见"已知问题" |
| 版本号 20260912d | 4 改动页 token 统一为 20260912d，无 20260912c 残留 | ✅ 属实 |

**智能路由判定：全通过 → 报 NoOne**（Bug1/Bug2 修复均真实生效；另附一条 pre-existing、非本次改动引入的既有隐患，供团队知晓，不阻塞本次交付）。

---

## A. Bug1 独立验证（个人中心 #prefs / #local-data）

### A1. 真读代码
- `个人中心.html` L95-103 `section[data-subpage="prefs"]` 内 `.card` **已无** `style="display:none"`；`#prefBody` 存在。
- L108-115 `section[data-subpage="local-data"]` 内 `.card` **已无** `style="display:none"`；`#localStats` 存在。
- section 结构、class、ID 完整；`subpage-router.js:167-181` 仅显形 section（与描述一致）。
- 全页 `.module-hero` 仍保留 `display:none`（v1.14 有意隐藏旧横幅），与本次修复无关。

### A2. 真跑页面（jsdom 真加载 + SubpageRouter.navigate）
| 用例 | section 可见 | 卡片可见 | 内容非占位 | 结果 |
|---|---|---|---|---|
| prefs（有数据） | ""(可见) | ""(可见) | ✅「📖 每日新增学习内容：50 条…」 | PASS |
| local-data（有数据） | ""(可见) | ""(可见) | ✅「⏱12 总学习…🎯80% 正确率…🔥7 连续打卡」 | PASS |
| prefs（**空数据边界**） | ""(可见) | ""(可见) | ✅ 回退 DEFAULT_SETTINGS（每日新增 50） | PASS |
| local-data（**空数据边界**） | ""(可见) | ""(可见) | ✅ 显示 0/0/0%/0，非空白 | PASS |

### A3. 反例（全新用户 / 空 localStorage）
**未出现空白。** `loadAllSettings()` 在无 `study_workbench_settings` 时回退 `DEFAULT_SETTINGS`；`appData.stats` 为合法默认对象（各字段 0）。因此两个子页在空数据下仍渲染真实内容，**不存在缺空状态处理的问题**。
（附注：`renderProfileOverview` 的 `catch` 分支会写"读取失败/偏好设置读取失败"，属兜底文案，非本页主路径。）

**Bug1 判定：修复真实生效。**

---

## B. Bug2 独立验证（模考页后退死循环）

### B1. 真读代码
- 三页返回按钮均为 `onclick="goBack();return false"`，**已无 `history.back()` 活调用**。
- 三页出现的 `history.back()` 字样**全部位于注释内**（描述旧问题），非执行代码。
- `findPaperCategory()`（mock-engine.js:73-87）按 `data.cet4.papers` / `data.exam.papers` 的 `id` 匹配返回 `'cet4'`/`'exam'`。

### B2. 分类映射独立核对（"工程师说没猜"→ 我核了）
从 `data/mock-papers.js` 在沙箱执行取真实数据：
- `cet4` 分类卷：`cet-demo-1`、`cet-real-a/b/c`
- `exam` 分类卷：`exam-comp-a/b`、`exam-real-1`
- `window.MOCK_CAT_ALIAS = { "cet-mock": "cet4", "exam-mock": "exam" }`
- goBack 内映射：`'exam' → exam-mock`、`'cet4' → cet-mock`。

→ **与真实数据字段完全一致，非猜测。** 且 `cet-demo-1`（前缀 `cet-` 非 `cet4-`）也被正确归为 `cet4`，说明依赖引擎函数而非字符串前缀猜测，可靠。

### B3. 真跑逻辑（jsdom 沙箱真执行 goBack，捕获目标 URL）
| 输入 | 期望 | 实际 | 结果 |
|---|---|---|---|
| `run?paper=cet-real-a` | `mock_exam.html?cat=cet-mock` | 同 | PASS |
| `run?paper=cet-demo-1` | `mock_exam.html?cat=cet-mock` | 同 | PASS |
| `run?paper=exam-comp-a` | `mock_exam.html?cat=exam-mock` | 同 | PASS |
| `run?paper=exam-real-1` | `mock_exam.html?cat=exam-mock` | 同 | PASS |
| `run`（无 paper） | 兜底 `cat=cet-mock` | 同 | PASS |
| `run?paper=不存在` | 兜底 `cat=cet-mock` | 同 | PASS |
| `exam?cat=cet-mock` | `四级备考.html` | 同 | PASS |
| `exam?cat=exam-mock` | `央国企笔试.html` | 同 | PASS |
| `exam`（无 cat） | 兜底 `四级备考.html` | 同 | PASS |
| `exam?cat=垃圾值` | 兜底 `四级备考.html` | 同 | PASS |
| `exam?cat=exam-mock&extra=1` | `央国企笔试.html` | 同 | PASS |
| `result?paper=cet-real-a&run=xyz` | `mock_exam.html?cat=cet-mock` | 同 | PASS |
| `result?paper=exam-comp-b&run=abc` | `mock_exam.html?cat=exam-mock` | 同 | PASS |
| `result`（无 paper） | 兜底 `cat=cet-mock` | 同 | PASS |
| `result?paper=垃圾` | 兜底 `cat=cet-mock` | 同 | PASS |

**15/15 PASS。**

### B4. 目标文件存在性（逐个确认，跳 404 排除）
`四级备考.html`、`央国企笔试.html`、`mock_exam.html` 均在磁盘存在（已 fs.existsSync 确认）。

### B5. 反向自证（证明测试非空转）
临时把 `exam` 页映射 `exam-mock → '不存在的页面.html'`，重跑：测试**成功捕获**到错误目标 `不存在的页面.html`。证明用例确实在检验映射，而非恒真。（改动仅改内存字符串，未落盘，已还原。）

### B6. 回归（beforeunload / 草稿持久化）
- `installBeforeUnload()` 定义（mock-engine.js:470）与调用点（:241，位于 `start()` 内）**未动**。
- `saveDraft` 定义与 `mock_exam_run.html` L643 的 `onDraft` 调用**未动**。
- 三页真实导航仅剩显式 `window.location.href`（run 页交卷跳成绩页 + 三页 goBack），无 `history.back()`。

### 与工程师描述的细微出入（如实报告）
- 描述称"三页都保留 `xt-page-back` class"，实际 **`mock_exam_run.html` 用的是 `xt-btn xt-btn-text`（顶部固定栏按钮样式），并无 `xt-page-back`**。这是合理的样式选择（该页无 `xt-page-header` 结构），**非功能缺陷**，但描述与事实不符，予以更正说明。
- 三页均保留 `chevron-left` 图标，`goBack()` 调用均在。

**Bug2 判定：修复真实生效。**

---

## C. 任务3 死代码清理验证

### C1. 备份可回滚（未真删）
`备份/cleanup-20260912/` 顶层 **19 项**，与声称一致：
- 16 个草稿：`profile.html`、`profile_v2.html`、`profile_v3.html`、`settings.html`、`settings_enriched.html`、`settings_final.html`、`settings_final_v2.html`、`settings_fixed.html`、`settings_full.html`、`settings_hier.html`、`settings_merged.html`、`settings_restore.html`、`settings_simple.html`、`settings_v2.html`、`settings_wechat.html`、`设置_旧版.html`
- `chat.js`、`mock-exam.js.removed`（均位于备份**根目录**，非 `assets/` 子目录）
- `ai-server/`（含 `.env.example`、`README.md`、`package.json`、`server.js`）

原位置（`assets/chat.js`、`assets/mock-exam.js.removed`、根 `ai-server/`、各 settings*/profile* HTML）**均已不存在**。**属移动，非删除，可回滚。**

### C2. 独立 grep 全仓库引用
- **活代码（live HTML/JS/CSS）中对被移动文件的真实引用 = 0**：
  - 无任何 `<script src=...chat.js>` / `<link>` 指向被移动文件。
  - 23 个活页面出现 `ai-server` 字样，**逐一核查均为注释/说明文案**（如"启动 ai-server 后端后…"），**非 fetch、非 script src**，无 404 风险。
- **"全仓库引用为 0"字面不成立**（如实指出）：另有非活代码引用分布在：
  - `.tmp_eng/`（工程师临时工作产物，不发布）
  - `.workbuddy/memory/`（Agent 记忆）
  - `docs/*.md`（含本次修复说明、PRD、全景说明书 —— 属"描述性引用"）
  - `tools/*.py` 与根目录 `rename_*.py`、`do_rename_settings.py`（历史一次性改名脚本）
- **实质结论**：无任何**可发布页面/运行时代码**引用被移动文件，清理**未引入 404**。

### C3. 反例检查（活页面是否引用已移动文件）
- `设置.html`、`个人中心.html` 两个活页面**均存在**（未误移）。
- 无活页面 `fetch('ai-server...')` 或 `src="assets/chat.js"`。
- 指向 `settings*.html` / `profile*.html` 的链接：活页面中**无**（草稿页未被任何活页链接）。

**任务3 判定：属实，可接受。**

---

## D. 全局完整性

### D1. 标签配平（粗检）
| 文件 | `<style>/</style>` | `<script>/</script>` | `<div>/</div>` |
|---|---|---|---|
| 个人中心.html | 0/0 | 11/11 | 237/237 |
| mock_exam.html | 1/1 | 8/8 | 23/23 |
| mock_exam_run.html | 1/1 | 8/8 | 22/22 |
| mock_exam_result.html | 1/1 | 9/9 | 20/20 |
| 设置.html（对照） | 1/1 | 15/15 | 461/461 |

无 `</style>` 吞掉类问题。

### D2. 页面真加载 / 零未捕获异常
- `个人中心.html`、`设置.html`：**零未捕获异常、零 error-boundary 触发**，PASS。
- `mock_exam.html` / `mock_exam_run.html` / `mock_exam_result.html`：**触发一条 app.js 既有抛错**（详见"已知问题"），但**关键全局（lsKey / countdownDays / MockEngine / MOCK_PAPERS_DATA / goBack）全部可用，关键 DOM 全部渲染**，功能未受影响，**非白屏**。

### D3. 版本号
- 4 改动页 `?v=` token **唯一值均为 `20260912d`**（个人中心 11 个、mock_exam 9、run 9、result 10）。
- **无 `20260912c` 残留于这 4 页**。
- 其余 23 个未改动页面仍为 `20260912c`（未被误改，符合"其他页面版本号不应被误改"要求）。

---

## 已知问题（pre-existing，非本次改动引入）

### K1. `assets/app.js:1392` 在无 `#countdownModal` 的页面抛未捕获异常
```js
// assets/app.js L1392（顶层，未做存在性判断）
document.getElementById('countdownModal').addEventListener('click', (e) => { ... });
```
- **影响**：`mock_exam.html` / `mock_exam_run.html` / `mock_exam_result.html` 三个页面**不含** `#countdownModal`，加载 app.js 时该行抛 `Cannot read properties of null (reading 'addEventListener')`，导致 app.js 中**该行之后的顶层语句不再执行**。
- **影响面评估（实测）**：由于 `lsKey`(L15)、`countdownDays`(L1020) 均定义在 L1392 **之前**，且 `mock-papers.js`/`mock-engine.js` 为独立 `<script>` 标签（不受影响），模考页核心功能（找卷、计时、答题卡、goBack 返回）**全部正常**，未白屏。
- **归因**：`assets/app.js` mtime = 11:42，**早于** 4 个修复页面（13:42-13:43），**本次修复未触碰 app.js**，属**历史遗留**的健壮性缺陷（同类 `#blogEditorInput` L5095 亦然）。
- **建议**：后续独立修复（加存在性守卫或按页条件绑定），**不阻塞本次两 Bug 交付**。

### K2. 描述性偏差（非缺陷，仅更正）
- 工程师称 Bug2"三页都保留 `xt-page-back`"，实际 `mock_exam_run.html` 用 `xt-btn xt-btn-text`（功能等价，无 `xt-page-back`）。
- 工程师称清理后"全仓库引用为 0"，字面不准确（docs/scratch/memory/历史脚本仍有描述性引用）；**实质（活代码引用=0、无 404）成立**。

---

## 复现命令
```bash
# 均在项目根目录执行，NODE_PATH 指向 tools/verifier/node_modules
node tools/qa/qa_bug1_profiles.js       # Bug1：期望 PASS=4 FAIL=0
node tools/qa/qa_bug2_goback.js         # Bug2：期望 PASS=15 FAIL=0 + 反向自证有效
node tools/qa/inspect_data.js           # 分类映射核对
node tools/qa/qa_global_pageload.js     # 全局：个人中心/设置 PASS；3 模考页见 K1
```

## 最终路由
**NoOne —— 两处 Bug 修复经独立验证确认真实生效，可交付。** 附 K1（pre-existing）与 K2（描述更正）供团队知悉，均不影响本次交付。
