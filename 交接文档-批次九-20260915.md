# 交接文档 · 批次九（2026-09-15）

> 接手必读。本文件写于 2026-09-15 17:20，撰写人 = 批次八主理人。
> **先读第 2 节（环境硬坑）和第 6 节（编码硬约束），再动手。** 这两节里每一条都是踩过的坑。

---

## 1. 一句话现状

**线上和 GitHub 都是好的（基线 `789ec80` / 版本 `20260915e`），批次八全部需求已交付上线。**
**只剩 3 件事没做**：① 数量关系题库回滚重做（唯一有技术难度的活）② `common.css` 全局滚动锁 ③ 文件清理（按用户铁律，排最后一步）。

---

## 2. 环境硬坑（每一条都踩过，务必照做）

### 2.1 路径与代码树（最容易搞错）

| 用途 | 绝对路径 |
|---|---|
| **唯一活的代码树 + 部署源** | `D:\下载的文件\学习工作台` |
| 本会话的工作目录（只是会话目录） | `C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-f0596da4` |
| 旧 worktree（**内容滞后，绝对不要改**） | `C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-6599a366` |
| 行测 OCR 工作根 | `D:\下载的文件\行测5000题【26年3月版】（最新27版）\_ocr_work\` |
| 线上站点 | `http://110.42.134.62/`（国内 IP，**复检要直连，不要走代理**） |
| 服务器 | `root@110.42.134.62`，web 根 = `/opt/study-workbench/web` |
| GitHub | `github.com/Li-jiu0/study-workbench`（分支 main） |

> ⚠️ `main-6599a366` 和 `main-f0596da4` 与部署源**共享同一个 .git**（`commondir` 指向 `D:\下载的文件\学习工作台\.git`）。所以在这三个目录里跑 git 看到的是同一个仓库，但**文件内容不同**——部署源才是线上真正跑的代码。**改代码只改部署源。**

### 2.2 bash 缺 coreutils
本机 git-bash 的 `ls / grep / head / tail / dirname` **全部不可用**（会报 `command not found` 或误报文件不存在）。
- 查文件存在性：用 Read / Grep / Glob 工具，**不要**用 `ls`/`test -f`
- 要跑脚本并把输出留痕：`<命令> > C:\Users\ATM\_xxx.txt 2>&1`，再用 Read 读那个文件

### 2.3 PowerShell 三个坑
1. **捕获 git 输出会变乱码**（`AI模拟面试.html` → `AI妯℃嫙闈㈣瘯.html`）→ 一律用 **Python `subprocess.run(..., capture_output=True)`** 执行 git，把结果写文件再 Read
2. **重定向写出来是 UTF-16**，Read 读不了 → 让 Python 脚本自己 `open(..., 'w', encoding='utf-8')` 写日志
3. **命令里含 `%VAR%` 会被拦**（被当成 cmd 语法）→ 不要用 `%` 包裹的变量

### 2.4 Python 控制台编码
Python 直接 `print` 中文到 PowerShell 控制台会抛 **GBK UnicodeEncodeError 导致进程崩溃**（但崩溃前已完成的操作是生效的）。
→ 不要在控制台 print 中文；一律写文件。

### 2.5 Glob 在中文路径下会误报「文件不存在」
本机 Glob 在中文目录**已误报过至少 4 次**，曾两次差点据此误判文件丢失。
→ **确认文件在不在，直接 Read 它的绝对路径。**

### 2.6 git 中文路径默认转八进制
→ git 命令一律加 `-c core.quotepath=false`。

### 2.7 代理端口每次都在漂移（推送必看）
2026-09-15 当天实测漂移序列：`58239 → 52588 → 56747 → 56269 → 64723`，`7897`（Clash）一度 down 后又恢复。
→ **不要写死端口**。推送前先探测：读环境变量 `HTTP_PROXY`，再依次试 `7897 / 7890 / 10809 / 1080`，取第一个 `socket.connect` 通的；再用 `ls-remote` 验一次（rc=0 才 push）。
→ 标准写法：`git -c http.proxy=P -c https.proxy=P -c http.version=HTTP/1.1 push origin main`
→ 远端核实用 `ls-remote`，**别信本地 tracking ref**（本机 `origin/main` 有缺失怪癖，会显示 `[gone]`，是假的）。

### 2.8 子代理（Agent 工具）
- 派子代理写码用 `subagent_type: "general-purpose"`，**不要用 `software-engineer`**（该角色未声明 tools，无 Bash，一跑就死）
- 子代理 **Bash 可用性不稳定**：有时有、有时报 `Bash not found` → 让子代理把命令输出重定向到文件再 Read；**数据生产/脚本类任务建议主会话自己做最稳**
- 429 是**按模型配额**计的：默认模型耗尽时，spawn 时指定 `model: "lite"` 立刻可用（UI 切模型救不了子代理）
- 子代理改数据管线前，**必须先让它脚本自留 .bak**——本次就因只有一个旧 bak 而丢了中间的好版本（见第 5.1 节）

---

## 3. 当前基线

| 项 | 值 |
|---|---|
| 本地 HEAD | `789ec803744d5584de34180af36ca54c3cb74c9b` |
| GitHub `refs/heads/main` | `789ec80`（与本地一致） |
| 线上版本戳 | `20260915e` |
| 部署批次 | 20260915e（32 文件）/ 20260915d（25 HTML） |
| 工作区状态 | 干净，仅 48 项未跟踪临时文件（第 5.3 节清理清单） |
| `assets/app.js` | 8761 行 / 6.8 MB，内联题库 5 类 |
| 根目录 HTML | 36 个，其中 31 个引用 `app.js?v=20260915e` |

最近 5 次提交：
```
789ec80 fix: 全项目 bug 排查修复（20260915e）
05ef596 feat: 申论入口迁移 + 行测题型筛选下拉化（20260915d）
f0db2a1 chore: 补入行测解析回填工具链与批次八任务执行文档
000bbba chore: 补充批次八使用的部署/同步/推送/清理工具脚本
d629946 批次八收尾：12 项需求交付 + 全项目 bug 排查修复
```

---

## 4. 已完成（**不要重做**）

### 4.1 批次八主需求（12 项，全部上线）
时政热点弹窗→独立页 / 图形推理出图 / 真题模拟可滚动 / 个人资料动态按钮 / 消息撤回 / 导入题库修复+自动建自定义题库 / TTS 朗读修复 / 阅读理解+翻译布局升级 / i人团伙优化+角色扮演并入 / 高情商表达删角色扮演菜单 / 内置 AI 扩知识 / 穿越英语充实

### 4.2 批次八收尾
- **行测解析回填**：五本解析册 → 4488 题中 3765 条有解析（覆盖率 0.4% → 83.9%）
- **申论入口迁移**（20260915d）：24 页侧边栏移除「申论刷题」，入口并入 `央国企笔试.html` feature-grid
- **行测题型筛选下拉化**（20260915d）：两行标签 → 单个 `<select id="ecTypeSelect">`；`filterExamType` 未改，页内包装同步（免 bump）

### 4.3 全项目 bug 排查修复（20260915e，已上线）
- `app.js` **14 处可选链 `?.` 清除**（老 Android WebView 解析期 SyntaxError → APK 全站白屏）
- `app.js` **5 处未判空 DOM 取值修复**（`topbarTitle` / `morePanel` / `greetingText` / `examMode*` / `blogEditor*`）
- **8 处 ADR-3 违规弹窗 → 页面内布局**：`学习工作台.html`（倒计时、任务）、`个人中心.html`（资料编辑）、`PPT训练.html`（作品增删+详情）、`设置.html`（改密码）、`私聊.html`（群设置、联系管理员）
- 三份排查报告落盘：`排查报告-20260915-JS.md` / `-HTML.md` / `-UI.md`（已随包提交）

### 4.4 排查已通过项（不用再查）
48/48 js 语法零报错 · 正则 lookbehind 0 · 原生 `alert/confirm/prompt` 0 · 7 页滚动锁覆盖仍在 · 591 个 `[IMG:]` 标记 0 真实断链 · 窄屏无硬溢出 · 20 个跳转目标全存在 · 20 个 onclick 全有定义 · 27 页侧边栏结构全配平 · 申论入口 0 残留

---

## 5. 未完成（按优先级）

### 5.1 🔴 P0 · 数量关系题库回滚重做（唯一有难度的活）

**现状**：`D:\下载的文件\行测5000题【26年3月版】（最新27版）\_ocr_work\数量关系_题本\2027数量关系（题本）.questions.json`（810 KB，1051 条）**不可用**。

**实测症状**（我 17:10 亲测）：
- 元数据全空：`chapter` / `point` / `difficulty` 均为空字符串，`section` 全是「未分类」，首条 `qid=None`
- 14 条 `answer` 为 `null`
- 与解析册顺序对齐一致率仅 **29.7%**（不可信，但明显不对）
- 脏字母（H/G/E）已清干净 —— 这部分白名单是生效的

**原因**：`quant-fix3` 子代理改了 `_ocr_work\build_book_questions.py`（08:33，27 KB）后重跑覆盖了产物，**破坏了 section/chapter/point 识别**；它随后失联（08:42 后无产出、没交报告）。
**损失**：此前那份 **819 题 / 一致率 99.5% 的好版本（02:54，677 KB）已被覆盖**。现存备份只有一个更早的：`2027数量关系（题本）.questions.json.bak`（01:17，624 KB）。

**两条恢复路线，建议先试 A**：
- **A（推荐，快）**：以 `questions.json.bak` 为起点，重新做「双栏答案速览修复」。要点：书后答案速览是**双栏排版**，原脚本用 `sorted(pg['lines'], key=lambda l: l['y'])` 按 y 全局排序会导致左右栏串行错位 → 正确做法是先按 x 分栏（`ak_reading_order()`），栏内再按 y，最后 `bucket.setdefault(a+i, ch)`。修好后与解析册答案交叉校验，目标一致率 ≥99.5%。
- **B（慢，彻底）**：修 `build_book_questions.py`，让 `section / chapter / point / difficulty` 重新正确填充 + 恢复 A–D 白名单 + 空答案列待核对清单，重跑。目标：有效题量 ~1004（解析册题量），一致率 ≥95%，不一致就停下来报告。

**对齐利器**：解析册 `_ocr_work\2027数量关系（解析）\2027数量关系（解析）_解析.json` 共 **1004 条**，字段里有 `match_key`，格式为
`章_节_考点_难度#section内序号#qid`，例如 `第一章数学运算_第一节核心方法_考点1代入排除法_夯实基础#1#1`。
题本侧可用 `chapter + section + point + difficulty + 该考点内序号` 拼出同构键去匹配，比之前失败的四元组裸匹配可靠得多。

**灌库方式**（写前端时照做）：
- 题库以**内联 IIFE** 形式写在 `assets/app.js`：`var INLINE_QUANT_BANK = [{id,type,sub,diff,q,o[4],a,x,tip}, ...]`，紧跟 `mergeExamBankQuestions(INLINE_QUANT_BANK, false)`，现有 5 个这类块（数量关系/判断推理/资料分析/言语理解/常识判断）
- **必须插在 `examInit` 之前**（末尾注入会导致首屏只出 60 题的老 bug）；插在既有 `INLINE_*` 块之后即可
- id 区间分配：数量关系 187–904（**不要越界**，`mergeExamBankQuestions` 同 id 会静默跳过）
- `a` 是 **0–3 的选项下标**，不是字母
- **`file://` 打开时必须内联**，不能只改分片 json
- 改完 `app.js` **必须 bump 全站版本戳**（见 6.4）

**验收**：数量关系 2000+ 题在线上可刷、有图（切图走 `[IMG:文件名]` 标记）、答案与解析册一致。

### 5.2 ⚠️ P1 · `common.css` L68 全局滚动锁
`assets/common.css` **L68** 原文是 `html, body { height: 100%; overflow: hidden; }`（注意有空格，别按无空格搜），会锁死全站滚动。
- 已给 7 个页面加**页面级覆盖**（页内 `<style>` 里写 `overflow-y: auto`）：`mock_exam.html` / `mock_exam_run.html` / `mock_exam_result.html` / `AI模拟面试.html` / `学途.html` / `好友申请.html` / `登录.html`
- 其余页面靠 `.content` / `.main > .page`（`common.css` L435 / L445）承载滚动，暂时没问题
- **改 L68 会影响 36 个页面，必须浏览器实测后再动**，所以一直没碰
- 顺带：`设置.html` 与 `个人中心.html` 里还有休眠的 `#countdownModal`（`countdownModal` 在 26 个页面存在，但只有 `学习工作台.html` 有 `#countdownRow` 用户入口，其余是 `app.js` 顶层监听的兼容保留，无入口、低风险）→ 归入 5.3 清理

### 5.3 🧹 P2 · 文件清理（用户铁律：**所有任务全部完成后才做，排最后一步，绝不提前动**）

| 类别 | 明细 |
|---|---|
| 休眠 DOM | 26 个页面残留 `#countdownModal`（无用户入口）：PPT案例拆解 / PPT版式库 / PPT训练 / blog_wechat / 万能金句库 / 个人中心 / 企业定向库 / 动态 / 商务礼仪 / 商务礼仪面试 / 四级备考 / 四级词汇 / 场景话术库 / 央国企笔试 / 学习博客 / 工具 / 时政热点 / 更多 / 申论刷题 / 私聊 / 管理员 / 行测刷题 / 设置 / 错题本 / 面试题库 / 高情商表达 |
| 旧备份 | `assets/` 下 10 个：`app.js.backup_20260914`(500KB) / `app.js.bak-0913n` / `app.js.bak-0913o` / `chat-local.js.bak-0914a` / `chat-local.js.bak-0914b` / `admin-contact.js.bak-0914b` / `i-partner.js.bak-0913o` / `icon-map.js.bak-0913o` / `mini-comm.js.bak-0913o` / `mini-exam.js.bak-0913o`（其中 3 个 app.js 备份含正则 lookbehind，**不在打包白名单、无页面引用**，但建议移出 `assets/` 以免被误打进包） |
| 临时脚本 | `tools/` 下约 50 个 `_` 前缀文件 + `tools/jx_patch/_s*.py` 27 个探针 + `tools/frontend_20260915d.tar.gz`、`tools/frontend_20260915e.tar.gz`（各 ~2.3 MB） |
| 已有清理脚本 | `tools/cleanup_temp_20260915c.py`（只删 git-UNTRACKED 的 `_` 前缀文件与 tar.gz，保留已跟踪文件和真实工具，可直接复用/改） |

> **注意**：`tools/` 下大量文件是**已纳入仓库的真实工具**（`bump_versions_*.py`、`deploy_update_*.py`、`plink.exe`、`pscp.exe`、`verifier/` 等），**不要一起删**。清理前先 `git status --short` 区分「已跟踪」和「未跟踪」。

---

## 6. 编码硬约束（写 `assets/*.js` 必须遵守）

### 6.1 禁用现代语法（**老 Android WebView 会炸**）
这个项目打包进 Android WebView（内核老），以下语法会造成**解析期 SyntaxError → 整个文件失效 → APK 全站白屏**：
- 可选链 `?.`、空值合并 `??`
- `String.prototype.replaceAll`、`Object.fromEntries`、`Array.prototype.at`、顶层 `await`

⚠️ **`node --check` 查不出来**（Node 22 支持这些语法）→ 必须 **Grep 人工确认**。本轮已清掉 `app.js` 14 处 `?.`，请保持 0。

### 6.2 禁止正则 lookbehind
`(?<=` / `(?<!` —— 同上，老 WebView 直接 SyntaxError。当前为 0，勿回潮。

### 6.3 `app.js` 是全站共用的
任何 `document.getElementById(...).xxx` **必须先判空**。只有部分页面存在的节点（`topbarTitle` / `morePanel` / `greetingText` / `examMode*` / `blogEditor*` 等）在缺节点的页面会抛错，并**打断后续所有脚本**。本轮已修 5 处，同类问题可能还有（报告里提到 `app.js` 共 139 处链式调用，只修了最典型的 5 处）。

### 6.4 改 `app.js` 后必须 bump 版本戳
把全站 HTML 里的 `assets/app.js?v=YYYYMMDDx` **一起升位**，否则浏览器吃旧缓存。
现成脚本：`tools/bump_versions_safe.py`（历史版本还有 `bump_versions_0913o.py` 等可参考）。

### 6.5 ADR-3 铁律
弹窗只承载三类轻交互：**①加入积累本勾选 ②删除/交卷/重新开始二次确认 ③轻量选择**。
**表单、详情、列表、聊天一律走页面内布局**（页面内区块展开/收起，或页面内视图切换）。
改造时**必须保留原 DOM id 与显隐函数名**，只改函数体 —— 否则 `app.js` 和其它页面的调用会死链。

---

## 7. 部署与推送 SOP

### 7.1 部署（前端）
**模板**：直接照抄 `tools/deploy_update_20260915e.py`（最新、最完整），复制一份改 `STAMP` 和 `FILES` 列表即可。

要点：
- 打包成 `web/<文件名>` 结构的 tar.gz（含 `assets/` 子路径）
- 凭据：脚本自动从 `upload_v23.ps1` 里正则解析出 `-pw <密码>` 和 `root@<IP>`；也支持环境变量 `SW_HOST` / `SW_PASS`
- 用 `tools/pscp.exe` 上传、`tools/plink.exe` 执行远端命令，`HOSTKEY = SHA256:i2DNTi3iEPauuTxvJEPlZ4jLf8K0LdDsaijQxArUU1M`
- 远端步骤：`tar -czf web_bak_<stamp>.tar.gz <本次改的文件>` 先备份 → `tar -xzf` 解压 → `echo BACKUP_OK / EXTRACT_OK` → grep 校验关键内容
- **纯前端改动不用重启 FastAPI**；只有改 `server/` 才需要
- 设 `SW_DRY_RUN=1` 可只打包不联网

### 7.2 线上复检
`http://110.42.134.62/` 是**国内 IP，直连**（Python 里用 `urllib.request.build_opener(urllib.request.ProxyHandler({}))` 显式禁代理）。文件名是中文，URL 要 `urllib.parse.quote()`；否则 urllib 抛 `UnicodeEncodeError: 'ascii' codec can't encode`。
另外：查 CSS 属性时注意源码可能是 `overflow-y:auto` 也可能是 `overflow-y: auto`（**中间有空格**），别因空格漏判（曾经因此误判线上有问题）。

### 7.3 推送 GitHub
见第 2.7 节的代理探测。提交时**白名单 add，绝不 `git add -A`**（会被临时文件污染）。
先例脚本：`tools/push_github_20260915c.py`。

### 7.4 用户协作共识（重要）
- **已获授权自主执行**：改代码 → 自检 → QA → bump → 部署 → 推送，**部署后知会即可**
- **遇到需要决策的事项，按最优判断直接执行，不再找用户确认**
- 但有两条例外：**push GitHub 需用户点头**（本批已授权，后续新任务仍需确认）；**清理类操作必须等所有任务完成后才做**
- 用户强调「**直接给出结论，不用反复验证**」：命令/脚本各跑一次即判定，不重复回跑、不逐条核对
- 用户偏好**多线并行**（文件零重叠前提下同时开多条线）
- **每完成一项任务/每次决策/每批部署，立即回写执行文档的进度日志区**（时间戳 + 做了什么 + 结果），不允许攒批补记
- 执行文档：`D:\下载的文件\学习工作台\任务执行文档-20260915-全线自主执行.md`（append-only，历史进度全在里面）
- 详细交接传统：`交接文档-批次三/四/五/六/八*.md` 都在部署源根目录，可翻历史决策

---

## 8. 建议的接手第一步

```
1. 通读本文件第 2、6 节（环境坑 + 编码约束）
2. 跑一次 git status --short 和 git log --oneline -5，确认基线还是 789ec80
3. 直连复检线上三个关键页，确认 20260915e 生效：
   - 行测刷题.html   → 含 ecTypeSelect
   - 央国企笔试.html → 含「申论刷题」入口卡
   - 更多.html       → 不含 location.href='申论刷题.html'
4. 然后攻 5.1 数量关系回滚（唯一有难度的活），建议走 A 路线（从 .bak 重做双栏答案修复）
5. 数量关系收尾后 → 5.2 common.css（可选）→ 5.3 清理
```

---

## 9. 关键文件索引

| 文件 | 说明 |
|---|---|
| `assets/app.js` | 全站主逻辑（8761 行）。含 5 个 `INLINE_*_BANK` 内联题库、`renderExamQuestion`、`filterExamType`、`localAiReply`、`uiConfirm/uiAlert/uiPrompt` |
| `assets/common.css` | L68 全局滚动锁（见 5.2）；L435 `.content` / L445 `.main > .page` 承载滚动 |
| `assets/qbank.js` | 题库注册/导入导出 |
| `assets/importer.js` | 文件导入题库（批次八修复） |
| `assets/chat-local.js` | 本地 IM：消息收发、撤回、顶部通知 |
| `assets/roleplay.js` | 角色扮演（从高情商表达抽离，`window.mountRolePlay`） |
| `assets/i-partner.js` | i人团伙（已并入角色扮演入口） |
| `assets/quest.js` | 穿越英语 |
| `assets/hotnews.js` | 时政热点 |
| `assets/voiceplayer.js` | TTS 朗读 |
| `tools/deploy_update_20260915e.py` | **部署模板，直接抄** |
| `tools/push_github_20260915c.py` | 推送模板 |
| `tools/cleanup_temp_20260915c.py` | 清理模板 |
| `tools/bump_versions_safe.py` | 版本戳批量升级 |
| `tools/plink.exe` / `pscp.exe` | 服务器通道 |
| `upload_v23.ps1` | 服务器凭据来源（脚本自动解析） |
| `任务执行文档-20260915-全线自主执行.md` | 历史进度日志（append-only） |
| `排查报告-20260915-JS.md` / `-HTML.md` / `-UI.md` | 本轮排查三份详细报告 |
| `_ocr_work/2027数量关系（解析）/..._解析.json` | 解析册 1004 条，带 `match_key`，用于答案对齐 |
| `_ocr_work/数量关系_题本/...questions.json.bak` | **数量关系唯一的可用备份（01:17，624 KB）** |

---

---

## 10. 批次九新增需求待办（2026-09-15 17:36 用户下达 · **未开工，等指令**）

> 登记人：批次九接手会话。以下 24 项**仅登记、未动一行代码** —— 用户明确要求「不说开工就不要自己执行」。
> ⚠️ 2026-09-15 18:57 更新：**N9-20（本机数据→学习报告）已被 N9-22 取代**（个人中心「本机数据」整块删除、并入新「学习概括」页，不再原地改名），实际待办 **23 项**（N9-20 仅存档）。
> ⚠️ 2026-09-15 19:05 新增 **N9-23（每日一题卡改造·豆包方案A）**：改造对象 `商务礼仪面试.html` `#ivQuizCard`（题卡改「今日一题」+ 答题框 + 进度 + 删开始模拟面试按钮），**未开工**。
> ⚠️ 2026-09-15 19:09 新增 **N9-24（万能金句库升级·豆包 A+B+C）**：`万能金句库.html`+共享 `assets/app.js`（加自定义金句+扩到80+句+网络金句API），**未开工**；落点核实发现 7 处差异/风险（共享文件高风险/字段/分类名/老WebView无fetch 等）。
> 用户原话分组：`修常识判断筛选 + 判断推理 type 匹配 / 全局消息通知（轮询+toast+跳转）/ 个人中心加作品集展示 / 工具页填充或隐藏 / 更多页内容填充 / app.js 拆分优化 / 筛选标签交互优化`

| ID | 需求 | 落点（已只读核实） | 风险 | 待确认 |
|---|---|---|---|---|
| N9-1 | 修常识判断筛选 | `assets/app.js` `filterExamType`（L5776）；`INLINE_CHANGSHI_BANK`（L494–497）；`行测刷题.html` 页内包装 | 低 | 具体现象需复现（筛选项缺失？点选不生效？） |
| N9-2 | 判断推理 type 匹配 | `INLINE_JUDGE_BANK`（L446–449）的 `type`/`sub` 与筛选值不一致 | 低 | 需先复现，确认是数据字段还是匹配逻辑 |
| N9-3 | 全局消息通知（轮询 + toast + 跳转） | **已有现成资产可复用**：`assets/xt-toast.js`（toast）、`chat-local.js` / `mini-comm.js`（本地 IM）、`server/` FastAPI | 中 | **轮询数据源是本地 storage 还是后端接口？** 覆盖全部 36 页还是仅常驻页？ |
| N9-4 | 个人中心加作品集展示 | `个人中心.html`（57 KB / 257 div） | 中 | **「作品集」数据来源**：PPT 训练作品 / 学习博客 / 错题本？需先定字段 |
| N9-5 | 工具页填充或隐藏 | `工具.html`（21 KB / 162 div，**实测无「敬请期待」类占位词**） | 低 | **二选一决策**：填充真实内容，还是隐藏整页入口（侧边栏菜单存在于 30+ 页，需联动） |
| N9-6 | 更多页内容填充 | `更多.html`（14 KB / 150 div） | 低 | 要填充哪些模块，需给清单 |
| N9-7 | app.js 拆分优化 | `assets/app.js` 8761 行，**被 31 个页面共用** | **高** | 拆分后必须保持：全局符号名不变、`INLINE_*_BANK` 仍在 `examInit` 之前注入、零现代语法、APK WebView 可解析 |
| N9-8 | 筛选标签交互优化 | `行测刷题.html` 的 `ecTypeSelect`（该 id 在 app.js 中 **0 命中**，属页内实现）+ 其他带标签筛选页 | 中 | 是否统一规范、全站换成下拉？ |
| N9-9 | 全站页头返回按钮补齐（详见 10.2；**用户已定基准样式**：听说训练面板的「← 返回」顶部栏） | 36 页中**仅 3 页**有页头返回；其余 33 页无。基准：`assets/voiceplayer.js` L234 `vp-back`（见 10.4 说明） | 中 | 各页返回目标映射表；考试进行页 / 登录页是否豁免 |
| N9-10 | 听力精听「场景标签条」改为分类列表（2026-09-15 17:56 用户提出，详见 10.4） | `assets/voiceplayer.js`：`.vp-scene` 横向滚动 chips（L172–174 样式 / L231 生成 / L241 注入），数据源 `SCENES`（L33 起，8 情景 + 新闻/长对话/短文题型） | 中 | 分类维度怎么分（情景类 vs 题型类分组？）；改 JS 需 bump voiceplayer.js 版本戳 |
| N9-11 | 四级 · 阅读理解页布局 bug（2026-09-15 18:00 用户截图提出，详见 10.5） | `assets/cet-read.js` L135–147：宽屏双栏 `1fr/1.05fr` + `.cr-col-art` sticky 内滚；右栏 `.cr-col-quiz` 作答前只有空的 `#crRes` → 大面积空白卡片 | 中 | 修复方向三选一（右栏放答题卡 / 调断点 / 单栏化）；三种题型（L258/L330/L402）需一起改；改 JS 需 bump cet-read.js 戳 |
| N9-12 | PPT版式库升级（2026-09-15 18:04 用户提供豆包需求文档，详见 10.6） | 需求文档全文：`需求文档-PPT版式库升级-豆包-20260915.md`（部署源根目录）；改造对象：`PPT版式库.html` + `assets/data-ppt-templates.js` + `assets/tpl-preview.js` | **高（有资源前置依赖）** | **预览图 ×15 与 .pptx 文件目前都不存在，需先生产**；emoji 图标需映射为全站 `data-icon` 体系 |
| N9-13 | PPT案例拆解改进（2026-09-15 18:08 豆包锐评，详见 10.7；与 N9-12 同病） | 需求文档：`需求文档-PPT案例拆解改进-豆包-20260915.md`；**改造直接落在 `assets/app.js`**（L4217 `const PPT_CASES` + L5323 渲染） | **高（动共享 app.js）** | 真实截图生产前置依赖（目录 `assets/images/ppt-cases/`）；收藏 localStorage；遮罩复用 openAppModal；**不做下载/应用到作品** |
| N9-14 | PPT训练课程改进（综合版，2026-09-15 18:15 豆包+GPT，详见 10.8） | 需求文档：`需求文档-PPT训练改进-综合-20260915.md`；改造对象：`PPT训练.html` + `assets/data-ppt-tips.js`（四原则课程数据）+ `assets/ppt-tips.js`（渲染器） | **高（分阶段）** | P0=体验打磨/对比图；P1=找茬实操+评分（需交互）；P2=能力雷达图闭环；导航栏去橙色撞色；emoji→`data-icon` |
| N9-15 | 广场·我的文章页布局 bug（2026-09-15 用户截图提出，详见 10.9） | `blog_wechat.html` `#blogViewMine` → `assets/api.js` `renderBlogMine()`（L450）；卡片网格复用 `.blog-grid`（`assets/common.css` L1383 `repeat(auto-fill, minmax(300px,1fr))`），`.note-card`（L1384）**无 max-width** | 中 | 修复方向：给 `.note-card` 加 `max-width` 或我的文章单独用更窄网格模板 + `justify-content:start`；与 N9-11 同属「卡片网格在少内容时拉伸」类问题 |
| N9-16 | 「联系管理员」入口只在「好友」tab 显示（2026-09-15 用户截图提出，详见 10.10） | `私聊.html`：`#acEntry`（L123，静态节点，位于 `.im-head` 与面板宿主之间）在**全部 5 个 tab**（会话/好友/群聊/申请/加好友）下都可见；页内脚本 `patchSwitchTab()`（L532–548）/ `showPanel()`（L502–519）/ `hidePanels()`（L521–529）只切 `#imList` 与 `#imPanelGroups`/`#imPanelAddFriend`，**未管 acEntry** | 低 | 「好友页面」= 好友 tab（`data-tab="friends"`）还是会话 tab？开工前确认；**隐藏整行会连带隐藏 R43 未读角标**（admin-contact.js L211–224 把角标挂在 acEntry 上），需定角标兜底策略 |
| N9-17 | 消息撤回改为微信式「长按弹菜单」触发（2026-09-15 用户提出，详见 10.11） | 现状：`私聊.html` 内联样式 `.im-recall-btn`（L44–54，桌面 hover 撤回按钮）+ `assets/chat-local.js` L448–594：移动端 `ontouchstart` 500ms 长按直接调 `imAskRecall` 确认气泡（L500–507），**无操作菜单** | 中 | 菜单项范围（撤回/复制/转发/收藏/删除？）；桌面端触发（右键 or 长按）；老 WebView `document.execCommand('copy')` 复制兜底；改 `chat-local.js` 需 bump `私聊.html` L341 戳 `20260915c` |
| N9-18 | 听说训练·听力精听全面改版（综合版，2026-09-15 用户综合 GPT 思路提供，详见 10.12；**吸收 N9-10 场景分类需求**） | 需求文档全文：`需求文档-听力精听改进-综合-20260915.md`（部署源根目录）；改造对象：`听说训练相关页` + `assets/voiceplayer.js`（`.vp-scene` 横向滚动 chips L172–174/231/241、数据源 `SCENES` L33起；`vp-back` 返回按钮 L234 已成全站基准） | **高（P0×5 + P1×3 分阶段，P2 长期）** | P0：场景两级分类/卡片放大去模糊/按钮一行+原文中文开关/进度条+查看全文/AI助教去重挂钩；P1：三阶段训练闭环(听→理解→说)/训练状态反馈/精听挑战双模式；P2：听写/辨义/综合训练类型 |
| N9-19 | 模拟面试页面重构（设计稿驱动，2026-09-15 用户提供画布设计稿+PDF，详见 10.13） | 设计稿：`C:\Users\ATM\Documents\模拟面试页面重构.pdf` + 用户 Ardot 画布两屏（回答中 / 已点评）；改造对象 `AI模拟面试.html`（含内联 JS，仅引 config.js+icon-map.js；关联 `assets/iv-prep.js` 待开工确认） | **高（重做信息架构 + 状态机）** | 落地 6 件事（单主按钮/语音文字Tab/猫头鹰降级/删AI美女/删演示球/提交反馈）+ 用户两点补充（五段状态条 / 查看示范回答降级次级按钮）；状态机 6 阶段（读题→准备→回答→提交→点评→?）开工细化 |
| N9-20 ⚠️已取代·仅存档 | （原）学习数据页面调整：「本机学习数据」→「学习报告」—— **已被 N9-22 合并方案取代，本行仅存档、不执行** | 原落点：`个人中心.html` L269–276 `本机学习数据` section + `assets/api.js` L1297/1377「学习数据」卡 | 仅存档（不执行） | 原 P0=改名「学习报告」+空状态+移除3按钮；现整块并入 N9-22 综合「学习概括」页（个人中心「本机数据」菜单与页面将删除，不再原地改名） |
| N9-21 | 写发帖页面改进（综合版·做减法，2026-09-15 用户综合 GPT 提供，详见 10.15） | 需求文档：`需求文档-写发帖页面改进-综合-20260915.md`；落点：`学习博客.html`（快速模板 L204–212 / 基本信息 6 字段 L214–248 / 富文本工具栏 10 按钮 L255–272 / 左写右预览 L278 / AI辅助写发贴 L290 + 草稿恢复条 L197–202 部分已实现 / 顶部 4 tab L166–169）+ `blog_wechat.html`（封面 emoji或图片URL L137 / 截图插图 base64 L148） | **中**（纯前端；改 `学习博客.html` 内联 + 可能 `api.js` 的 editorTool/saveBlogNote/editorAiAssist） | P0=渐进式填写(首屏只留标题/正文/分类)+删技术细节(封面改按钮选择/删base64文案)+顶部导航简化(返回广场+发布)+编辑器简化(工具栏10→5-6/代码公式收更多/Markdown改问号)+写作区放大；P1=快速模板默认收起+AI辅助写作场景绑定(起标题/整理/纠错/续写)+发布前预览；P2=视觉层级+草稿自动保存(复用 ed-draft-bar) |
| N9-22 | 学习概括页面整合（**合并终版**·2026-09-15 18:57 用户澄清修正，详见 10.16，取代 N9-20） | 需求文档：`需求文档-学习概括页面整合-20260915.md`；**删**：个人中心 `本机数据` 菜单卡(L106–108)+子页(L269–276)+引用(L485/L823/SubpageRouter)、更多 `学习统计` 卡(L102–106)+旧弹层(L164–166)、30+页底部导航 `学习统计`(`openBlogStats()`)；**改**：更多 L102–106 → `学习概括` 入口卡指向新综合页；**复用**：`openBlogStats()`/`renderBlogStats()`(api.js L761) | **中**（删散落+新建/增强综合页+入口上更多，纯前端无新增后端） | ONE 综合「学习概括」页吸收「本机数据」+「学习统计」(核心指标/各模块/近7天柱状图/最近记录/环形图，数据=activityLog+EXAM_BANK+appData.stats)；入口只在「更多」页；N9-20 已并入本项 |
| N9-23 | 每日一题卡改造（豆包方案A·实用版·2026-09-15 用户转发，详见 10.17） | 需求文档：`需求文档-每日一题卡片改造-豆包-20260915.md`；落点：`商务礼仪面试.html` `#ivQuizCard`(L101–125) + 题库 `IV_BANK`(L642–655,12题 `{cat,q,tip}`) + `IV_CATS`(L656) + 函数 `ivQuizNext/ivQuizShowTip/ivQuizCat/pickQuestion`(L666–687) + AI面板 `#ivAiPanel`(L113–124) | **中**（纯前端改本页内联；新增 `ivQuizSubmit()`+localStorage `interview_daily_answers`） | 题卡改「今日一题·{分类}」+右上进度「已答 X/Y」+题下 textarea 答题框+三按钮(换一题/提交回答/查看要点)；删「开始模拟面试」按钮；提交存记录+进度+1+置灰；⚠️ 冲突①删按钮后 `#ivAiPanel` 成孤儿需隐藏；冲突②实际要点是单段 `tip` 非 `keyPoints` 数组 |
| N9-24 | 万能金句库升级（豆包 A+B+C·2026-09-15 用户转发，详见 10.18） | 需求文档：`需求文档-万能金句库升级-豆包-20260915.md`；落点：页面 `万能金句库.html` L95–96(标题+`18句直接套用`静态tag) + 共享 `assets/app.js` `COMM_QUOTES`(L4359–4378,18条) / `renderQuotes`(L5493) / `renderQuoteCard`(L5532) / `filterQuotes`(L5485) / 合并点(L5497) | **高**（全落在共享 app.js，31页共用；改完必 bump 全站戳+WebView语法合规） | A=用户加金句(遮罩表单+`custom_quotes`+可删可改)；B=扩到80+句(68句新增，含分类/字段映射)；C=🌐网络金句(`v1.hitokoto.cn` fetch+收藏)；⚠️ 7处差异：共享文件高风险/自定义缺`icon`/`id`/分类名(不同意见→表达不同意见,被甩锅+万能开场未覆盖)/来自网络新分类/老WebView无fetch/静态tag |

### 10.1 执行前置：三处实测校正（与第 5.1 节描述冲突，**以本节为准**）
1. **P0 病灶描述不准**：题本 json 实测 chapter 仅 3 条空（数学运算 853 / 数字推理 195）、difficulty 仅 136 条空、section 有 87 个不同取值 —— 并非「全空 / 全是未分类」。真正坏的是 **`point` 字段被 OCR 括号噪声污染**（`考点3「知识梳理`、`考点1广知识梳理」`，538/1051 为空）。
2. **文档漏报了更致命的缺陷（这才是「不可用」主因）**：选项残缺 —— 仅 865/1051 有完整 4 个选项，**153 条（14.6%）含空选项文本、80 条 0 选项**；「有答案 + 4 个非空选项」的**完全可用题仅 842 条**。回滚重做时必须一并修选项，否则仍旧刷不了题。
3. **新增一处畸形版本戳**：`blog_wechat.html:325` = `app.js?v=20260915eaaa`（其余 30 页皆是 `20260915e`）→ bump 脚本若用词边界匹配会**永久漏掉该页**，造成该页长期吃旧缓存。

### 10.2 N9-9 全站页头返回按钮盘点（2026-09-15 17:50 实测，用户提出「很多页面没有回退方式」）

**现状**：全站 36 个 HTML，只有 **3 个**有统一页头返回（`xt-page-head` + `xt-page-back`）：

| 页面 | 返回实现 |
|---|---|
| `企业定向库.html` | 固定跳转 `location.href='央国企笔试.html'`（标准范式，**禁用 history.back()**） |
| `mock_exam.html` / `mock_exam_result.html` | `goBack()` 函数（JS 回退，非标准范式） |

**其余 33 页均无页头返回**，分两类：

- **A 类 · 有侧边栏的站内子页（26 个）**——桌面端可靠侧边栏导航，但页头无返回，窄屏（侧边栏收起时）无回退路径：
  `行测刷题` / `时政热点` / `申论刷题` / `商务礼仪` / `商务礼仪面试` / `场景话术库` / `高情商表达` / `四级备考` / `四级词汇` / `万能金句库` / `面试题库` / `错题本` / `PPT训练` / `PPT案例拆解` / `PPT版式库` / `学习博客` / `blog_wechat` / `动态` / `私聊` / `管理员` / `央国企笔试` / `更多` / `工具` / `个人中心` / `设置` / `学习工作台`
  （注：后 6 个属顶级入口页，可讨论是否豁免；用户截图点名的 7 个页面全在 A 类）
- **B 类 · 无侧边栏的独立页（7 个）**——`AI模拟面试` / `mock_exam_run`（考试进行中，建议豁免或仅禁用不显示）/ `PPT素材库` / `四级经验分享` / `学途` / `好友申请` / `登录`（建议豁免）

**已有部分页面用「页内散装返回按钮」补偿**：如 `四级词汇.html` 的「返回四级」、`行测刷题.html` 底部操作条的「返回题库」——不统一、位置不固定。

**实现要点（开工时照做）**：
- **基准样式（用户 17:56 指定）**：以「听说训练」面板左上角的 **`← 返回` 顶部栏**（`assets/voiceplayer.js` L234，`class="vp-back"`）为准——顶部窄条 + 左上角返回按钮，全站页面照此形态补齐
- ⚠️ 注意语义差异：`vp-back` 在面板里是**关闭面板**（`openVoiceTrain.__close()`，移除 `#vpMask` 回到下层页面）；普通 HTML 页面没有「面板」可关，等价实现 = **固定 `location.href='上级页.html'; return false`**（沿用全站**禁用 `history.back()`** 约定，WebView/file:// 下不可靠）。视觉/位置照搬 vp-back，行为按页面类型二选一
- 需先产出「页面 → 上级页」映射表（按侧边栏归属定），再批量注入
- `xt-page-head` 的 CSS 已在 `common.css`（企业定向库在用），可在此基础上对齐 vp-back 的视觉；**动 HTML 不动 app.js 则不需要 bump app.js 版本戳**，但仍需按部署 SOP 打包上传
- `mock_exam` 两页的 `goBack()` 是否顺势改成标准范式，一并决策

### 10.4 N9-10 听力精听场景标签条 → 分类列表（2026-09-15 17:56 用户提出）

**现状**：听说训练面板（`#vpMask`，`assets/voiceplayer.js` 动态注入，从 `四级备考.html` 进入）的顶部场景切换是一根**横向滚动 chip 条**（`.vp-scene`），18+ 个标签横向拖动，红框即用户圈选处：

- 样式：`voiceplayer.js` L172–174（`.vp-scene` `overflow-x:auto` + `.s` chip / `.s.on` 选中态）
- 生成：L231 `sceneHtml = Object.keys(SCENES).map(...)`，注入点 L241
- 数据源：`SCENES` 对象（L33 起）——8 个情景对话（咖啡/机场/餐厅/酒店/购物/问路 等）+ 新闻/长对话/短文三类题型，标签文本如「机场值机 / 餐厅点餐 / 酒店入住 / 购物退换 / 问路 / 新闻听力 / 长对话 / 短文精听 / 短篇新闻·科技前沿 / 招聘·春季校园招聘会 / 长对话·选课咨询…」

**目标**：改成**分类列表**——按「情景对话 / 新闻类 / 长对话 / 短文」等维度分组，平铺展示（可换行、带分组小标题），不再横向滚动找标签。

**开工要点**：
- 只动 `voiceplayer.js`（面板是 JS 注入的，改 HTML 无用）；`.vp-scene` 样式块与 `sceneHtml` 生成逻辑一起改
- **改完必须 bump `voiceplayer.js` 的版本戳**（引用它的页面，至少 `四级备考.html`，可能还有其他页——开工时先 grep `voiceplayer.js?v=` 确认引用面）
- 保持 `SCENES` 数据结构与 `__scene(k)` 切换回调签名不变，只改呈现层
- 面板是 `openAppModal` 体系的一部分（`#vpMask` id 不可改，app.js L1843/1850 有联动）

### 10.5 N9-11 四级 · 阅读理解页布局 bug（2026-09-15 18:00 用户截图提出）

**页面**：「四级 · 阅读理解」面板（`assets/cet-read.js` 渲染、`assets/data-cet-read.js` 出数据，从 `四级备考.html` 进入），选词填空 tab。

**截图症状**：
1. 正文被压成**窄条**（约占一半宽），行宽极短、观感差；
2. 右侧是一块**几乎全空的大卡片**——那是 `.cr-col-quiz` 里的 `.cr-quiz-card`，作答前内部 `#crRes` 无任何内容，交卷后才出结果 → 看起来像布局坏了；
3. 正文底部被截断（最后一行贴边），要靠**栏内滚动**才能继续读。

**根因定位（只读核实，`cet-read.js` L135–147）**：
- 默认 `.cr-grid` 单栏 `minmax(0,1fr)`；
- 宽屏 media query 切成双栏 `grid-template-columns:minmax(0,1fr) minmax(0,1.05fr)`，同时 `.cr-col-art` 变 `position:sticky` + `max-height:calc(100vh - …)` + `overflow-y:auto`（栏内滚动）；
- 右栏 `.cr-col-quiz` 固定占 1.05fr，但作答前**没有内容** → 大空白。

**修复方向（开工时三选一或组合）**：
- A. 作答前右栏填充有用内容（答题卡 / 已选概览 / 生词表），交卷后换成结果；
- B. 调断点与栏宽比例（当前 ~1080px 窗口即触发双栏，正文太挤），或干脆单栏化、结果区改为交卷后插入文章下方；
- C. 去掉 sticky 内滚，让正文随页面自然滚动（截断感来源）。

**注意**：三种题型共用同一套 `.cr-grid` 结构——选词填空 L258 / 长篇匹配 L330 / 策略浏览 L402，**必须一起改**，否则修一个坏两个。改 JS 后需 bump `cet-read.js` 版本戳。

### 10.6 N9-12 PPT版式库升级（2026-09-15 18:04 用户提供豆包需求文档）

**需求文档全文**：`需求文档-PPT版式库升级-豆包-20260915.md`（部署源根目录，已原样落盘，此处只记差异与执行注意）。

**需求概要**：PPT版式库改为「卡片网格 + 真实预览图 + 收藏/预览/下载三按钮 + 分类标签（全部/封面页/目录页/过渡页/内容页/我的收藏）+ 实时搜索 + 空状态」，收藏存 localStorage `ppt_favorites`，预览用全屏遮罩（✕ / 点空白 / ESC 三种关闭），下载用 `<a download>`，无文件显示「制作中」，学习进度移到右上角小字。验收 12 条见原文档第九节。

**现状与差距（已只读核实）**：
- 现渲染器 `assets/tpl-preview.js`（`window.PPTV2['ppt-templates']`），数据 `assets/data-ppt-templates.js`（ADR-1 后置覆盖 `MINI_BANK.PPT['ppt-templates']`，mode='gallery'）
- **当前卡片是 CSS 画的 HTML 骨架**（`SK_*` 幻灯片字符串），没有 `<img>` 预览图 → 与需求第 1 条直接冲突，是本次最大的改造点
- **没有任何 `previewImage` / `pptFile` 字段，15 张预览图与 .pptx 文件均不存在** → 资源生产是前置依赖（预览图可用 AI 生图；.pptx 若无法生产则全部按钮显示「制作中」，功能上仍成立）
- 现无收藏功能、无搜索框、无分类标签

**执行注意（与豆包文档的适配）**：
1. **图标**：文档里的 ⭐👁⬇📭✕ 全站不用 emoji，映射为 `data-icon`（icon-map.js）：star / eye / download / inbox / close
2. **弹窗**：禁 alert/prompt 与全站 ADR-3 约定一致；预览遮罩可直接复用 `openAppModal` 基建（ESC、点空白关闭、锁滚动都是现成的）
3. **数据结构**：`data-ppt-templates.js` 的 items 增补 `id / category / previewImage / pptFile / useCase / designPoints` 字段；ADR-1 的「后置覆盖、旧数据零删除」约定要保持；进度键前缀 `xtc:ppt:tpl:<id>:*` 机制沿用
4. **老 WebView 约束**：渲染器 JS 禁 `?.`/`??`/`replaceAll` 等现代语法；改完 `tpl-preview.js` / `data-ppt-templates.js` 需 bump 对应版本戳
5. **下载 404 探测**：文档要求「文件 404 自动切制作中」——file:// 协议下探测不可靠，开工时定策略（Web 下 HEAD 探测 / file:// 下仅靠 pptFile 字段判空）
6. **版式数量**：文档按 15 个版式写，现有条数需开工时清点对齐

### 10.7 N9-13 PPT案例拆解改进（2026-09-15 18:08 豆包锐评）

**需求文档全文**：`需求文档-PPT案例拆解改进-豆包-20260915.md`（部署源根目录，已落盘）。核心：① 把 CSS 画的假案例图换成真实 PPT 截图（`<img>`，无图则占位「案例图待补充」，目录 `assets/images/ppt-cases/`）；② 加「查看大图」全屏遮罩（✕/点空白/ESC 关闭，旁边保留【设计亮点】【可借鉴】）；③ 加「收藏案例」⭐（localStorage，顶部「⭐ 我的收藏」筛选）。不做下载、不做「应用到我的作品」。文字拆解内容保留。

**现状与差距（已只读核实）**：
- 与 N9-12 **同一个毛病**：配图是 CSS 线框图（封面=深蓝矩形+白线、执行摘要=灰列表+橙柱、数据页=蓝柱+灰列表），非真实案例
- **关键区别（风险更高）**：本页渲染逻辑**直接写在共享的 `assets/app.js` 里**——数据 `const PPT_CASES`（L4217），渲染函数（L5323）都在这 8761 行的全站主文件内，不像版式库有独立 `tpl-preview.js` 隔离
  → 改动 **blast radius 更大**：必须保证零现代语法、所有 DOM 取值判空（6.1/6.3 铁律），改完 **bump 全站 app.js 版本戳**
- 现无「查看大图」、无收藏、无收藏筛选
- 右侧【设计亮点】【可借鉴】文字拆解内容有价值，保留不动

**执行注意**：
1. 真实截图是前置依赖（麦肯锡/苹果等真实报告页截图，或暂用占位 `<img>`），目录 `assets/images/ppt-cases/`
2. ⭐ 图标 / ✕ 关闭 → 全站 `data-icon` 体系（star / close），不用 emoji
3. 大图遮罩复用 `openAppModal`（ESC/点空白/锁滚动现成）
4. 收藏 localStorage key 建议 `ppt_case_favorites`（与版式库 `ppt_favorites` 区分）
5. 与 N9-12 可共享同一套「遮罩查看大图 + 收藏」实现，开工时一并抽公共逻辑，避免两处各写一套

### 10.8 N9-14 PPT训练课程改进（2026-09-15 18:15 豆包+GPT 综合）

**需求文档全文**：`需求文档-PPT训练改进-综合-20260915.md`（部署源根目录，已落盘）。一句话定位：现版本 80% 是"看课"，要往"训练"升级。

**现状（已只读核实）**：课程渲染器 `assets/ppt-tips.js`（`window.PPTV2['ppt-tips']`，PPT训练.html 预埋宿主），课程数据 `assets/data-ppt-tips.js`（设计四原则：对齐/对比/重复/亲密性，每节含 `body` 长文 + 选择题 `q/o/x`）。与用户描述吻合（已学 0/3 节、课后小测/正反例卡片）。

**分三期，先 P0 后 P1/P2**：
- **P0（5 件，马上落地）**：① 每原则配「正反例对比图」（❌vs✅，最紧急，缺图=白讲）② 「标记已学」按钮 + 顶部阅读进度条（随滚动）③ 「课后小测/正反例」从死标签改成可点击入口（小测→答题页给分解析；正反例→翻页对比图）④ 正文卡片化（要点小卡+加粗高亮+默认折叠）⑤ 顶部导航栏去橙色（与深蓝课程卡片撞色，改浅灰/白）
- **P1（2 件）**：⑥ 每节加「找茬/修改」实操题（先做模式A找茬点击，模式B可编辑后续）⑦ 训练评分+解析（给分+改进方向，不只对错）
- **P2（1 件，长期）**：⑧ 「学→练→评→改」闭环 + 课程结束「PPT设计能力雷达图」

**执行注意**：
1. **对比图/正反例图是前置资源**：同 N9-12/13，没有就先用占位 `<img>`（建议目录 `assets/images/ppt-tips/`）
2. emoji（❌✅⭐）→ 全站 `data-icon` 体系（x-circle / check-circle / star）
3. 收藏/已学态走 localStorage（课程已学键沿用现有 `xtc:ppt:tips:*` 之类前缀，开工时确认）；进度条随滚动更新
4. P1 找茬题需「可点击热点」交互，P2 雷达图需 canvas/SVG——**均受老 WebView 语法禁令约束**（禁 `?.`/`??`/`replaceAll`/`Object.fromEntries`/`Array.at`/顶层 await），不引第三方库
5. 改 `ppt-tips.js` / `data-ppt-tips.js` 需 bump 对应版本戳；若逻辑回流到 `app.js` 则 bump app.js
6. 课程文字内容本身扎实——**保留**，只增不改




### 10.9 N9-15 广场·我的文章页布局 bug（2026-09-15 用户截图提出）

**现象（用户原话）**：「这个页面的显示布局是不是有bug，记录一下」——指「互动广场 → 我的文章」(`blog_wechat.html` 的 `blogViewMine` 视图) 截图。

**根因（已只读核实）**：`我的文章` 的卡片容器 `#blogMineGrid` 直接套用 `.blog-grid` 类（`assets/common.css` L1383）：
```css
.blog-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 16px; }
```
而 `.note-card`（L1384）**没有 `max-width` 限制**。`auto-fill + minmax(300px,1fr)` 在容器内只有 1 张卡片时会把它放到占满整行的 `1fr` 轨道 → 单卡被拉伸到容器满宽（桌面主区 ~700px、手机满宽）。`广场` 列表因帖子多、自然排成多列不显眼；但 `我的文章` 是**个人页、通常只有 1–3 篇**，于是出现「一张巨大的横条卡片、封面 110px + 左对齐小字」的失衡观感——这就是截图里看起来像布局 bug 的地方。

**佐证**：
- `blog_wechat.html` L109–114：`#blogViewMine` 内 `#blogMineTabs`(筛选 chips) + `#blogMineGrid`(`.blog-grid`)。
- `assets/api.js` L450–484 `renderBlogMine()`：用 `noteCardHtml(n,{mine:true})` 渲染，`blogMineType` 切 全部/已发布/草稿/归档/收藏/回收站，每类都常常是 0–数条 → 必然触发单卡满宽。
- `assets/common.css` L1384 `.note-card { … }` 全段无 `max-width`。

**修复方向（三选一，开工时定）**：
1. 给 `.note-card` 全局加 `max-width: 360px`（最简单，但会影响广场列表最大卡宽，需确认观感）；
2. 我的文章单独用更窄模板，如 `.blog-mine-grid { grid-template-columns: repeat(auto-fill, minmax(280px,320px)); justify-content: start }`，不动广场；
3. 用 `grid-auto-columns` + 固定列数（如固定 2 列）。

**连带注意**：
- 6 个筛选 chip（含图标 + 文字 + 计数）在 `blog-filters` flex-wrap 下，窄屏会折成 2–3 行——属正常换行，非 bug，但若想更紧凑可把计数移到角标。
- 改 `common.css` 需 bump 其版本戳（`blog_wechat.html` L8 `common.css?v=20260915caaa`）；注意同 HTML 的 `app.js?v=20260915eaaa`（L325）戳位已被 10.1 标记异常（`eaaa` 非标准日期戳），开工时一并核对。

### 10.10 N9-16 「联系管理员」入口只在「好友」tab 显示（2026-09-15 用户截图提出）

**现象（用户原话）**：「这个联系管理员只需要在好友页面显示即可」——截图为 `私聊.html`（页头标题「好友」），可见左上「管理员 / 管理员在线时会回复你」这一行出现在**非好友 tab**（截图里是加好友 tab：下方紧跟「输入昵称或 @账号」搜索框）之上，位置突兀。

**根因（已只读核实）**：`#acEntry` 是 `私聊.html` 里的**静态节点**（L123–127），刻意放在 `#imList` **之外**（L118–122 注释说明：为避免随 5s 未读轮询重建而闪烁），因此它**不属于任何 tab 的列表内容，在任何 tab 下都常驻显示**：
- tab 切换 `imSwitchTab(tab)`（`assets/chat-local.js` L2007）只负责给 `.im-tab` 切 `.active` + 调 `renderList()`（L649，渲染进 `#imList`）；
- 页内包装 `patchSwitchTab()`（`私聊.html` L532–548）：只有 `addfriend` 走面板，其余走 `hidePanels()`；
- `showPanel()`（L502–519）/ `hidePanels()`（L521–529）：只切 `#imList` 与 `#imPanelGroups` / `#imPanelAddFriend` 的显隐，**没有任何地方控制 `#acEntry`** → 它永远可见。

**用户诉求**：把 `#acEntry` 收窄到**只在「好友」tab**显示。

**修复方案（开工时定，改动很小）**：
- **首选**：在 `私聊.html` 页内脚本里给 acEntry 加一处显隐判断——最省事的位置是 `patchSwitchTab()` 包装器（L535–546）中，`real(tab)` 之后加：
  `var ae=$id('acEntry'); if(ae) ae.style.display = (tab==='friends') ? '' : 'none';`
  并在 `init()`（L564–568）里补一次「默认 tab（会话）→ 隐藏」的初始调用。
- **备选**：把该判断写进 `chat-local.js` 的 `imSwitchTab`（更靠近 tab 状态，但要 bump `chat-local.js` 版本戳；页内方案只需动 `私聊.html`，无需 bump 任何 assets 戳）。
- 若"好友页面"实指**会话 tab**（`data-tab="chats"`）而非好友 tab，则把上面判断的 `'friends'` 换成 `'chats'` 即可——**这是开工前必须确认的一点**。

**风险与连带（务必注意）**：
1. **未读角标会被一起藏掉**：R43（`assets/admin-contact.js` L211–224）把「管理员来信」未读角标 `#acEntryBadge` 挂在 `.ac-entry` 上。若整行在会话/其它 tab 隐藏，普通用户在主 tab 就看不到管理员回信提醒 → 需定兜底策略（如：仅当有未读时强制显示该行；或把角标迁到 tab 上）。
2. 该行是普通用户**联系管理员的唯一入口**（管理员对普通用户隐形，见 admin-contact.js 文件头注释），隐藏要保证「好友 tab 一定进得去、点得开 `xtOpenAdminChat()`」。
3. 无后端改动；`xtOpenAdminChat` / `acClose` / `#acPanel` 内部逻辑与 id 全不动。

### 10.11 N9-17 消息撤回改为微信式「长按弹菜单」触发（2026-09-15 用户提出）

**用户原话**：「信息撤回功能变成微信那样长按撤回」—— 即长按一条消息弹出**操作菜单**（微信形态：复制 / 转发 / 收藏 / 删除 / 撤回），而非当前的「直接弹撤回确认气泡」。

**现状（已只读核实）**：撤回功能已存在（批次八 2026-09-15），但形态不是微信式：
- 桌面：气泡 hover 出现 `.im-recall-btn`「撤回」按钮（`私聊.html` L44–54 样式 + `chat-local.js` `imRecallEntryHtml` L492–497）。
- 移动：自己发的消息 `ontouchstart/end/move` 挂 500ms 长按（`imMsgAttrs` L484–490 → `imRecallPressStart/End` L499–510），到点直接 `imAskRecall(id)`（L517–555）弹「确定撤回这条消息？」轻量气泡（符合 ADR-3，非全屏 modal），再 `imDoRecall`（L557–594）落库隐藏。
- **缺的就是「菜单」这一层**：微信是长按 → 菜单 → 点撤回；现在长按是 → 直接确认撤回，没有菜单、没有复制/转发等其它项。

**目标形态（微信式）**：长按消息气泡 → 弹出**非模态操作菜单**（锚定气泡旁，点外部 / Esc 关闭，ADR-3 合规）→ 菜单里含「撤回」（仅本人 2 分钟内私聊消息可见，复用 `imDoRecall`）+ 其它项。

**改造思路（开工时定细节）**：
1. 在 `chat-local.js` 新增 `imShowMsgMenu(m, anchorEl)`：组装 `.im-msg-menu` 浮层（新增 CSS 放 `私聊.html`），按 `m.senderId===S.myId && (Date.now()-m.time)<=RECALL_MS && !S.group` 决定「撤回」项是否出现；其余项（复制 / 转发 / 删除本端 / 收藏）按范围决定。
2. **触发改造**：移动端 `imRecallPressStart`（L500–507）把动作从 `imAskRecall(id)` 改为 `imShowMsgMenu(...)`；桌面补 `oncontextmenu`（右键）触发同菜单（微信桌面端也是右键/长按）。
3. **撤回流程复用**：菜单里「撤回」→ 仍走现有 `imAskRecall` 确认气泡 → `imDoRecall`（逻辑零改动，只换入口）。
4. **其它项落地**：
   - 复制：优先 `navigator.clipboard.writeText`，老 WebView / `file://` 不可用则 `document.execCommand('copy')` 兜底（选区 trick）。
   - 删除本端：本地隐藏（可复用撤回那套 `data.recalled` 占位机制，或新增 `data.deleted` 集合，二者不要混）。
   - 转发 / 收藏：若本次不做，菜单先不显露（避免空入口），做 P1。

**约束与风险**：
- 老 WebView 语法禁令：禁 `?.` / `??` / `replaceAll` / `Object.fromEntries` / `Array.at` / 顶层 await；菜单与手势一律 ES5 友好写法。
- 长按 vs 滚动冲突：`ontouchmove` 已能取消（L490 / L509），保留；菜单出现后吞掉后续 touch，避免误触。
- 现有 hover `.im-recall-btn`（桌面）与微信「长按/右键菜单」理念不一致 —— 建议**移除 hover 按钮、统一走菜单**（也消除桌面/移动两套入口），但需确认用户体验，开工前与用户核对。
- 改 `chat-local.js` 后 **必须 bump 版本戳**（`私聊.html` L341 `assets/chat-local.js?v=20260915c` → 升一位，如 `20260915d`），否则 APK/浏览器吃到旧缓存。
- `imDoRecall` / `imAskRecall` 内部逻辑与 id 全不动；`#imRecallConfirm` 气泡保留作撤回的二次确认。

### 10.12 N9-18 听说训练·听力精听全面改版（综合版，2026-09-15 用户综合 GPT 思路提供）

**需求文档全文**：`需求文档-听力精听改进-综合-20260915.md`（部署源根目录，已落盘）。一句话定位：现在页面在"播放英语"不是"训练听力"，要把功能组织成训练流程。

**核心判断（用户原话）**：先做 P0 把"播放英语"体验做好，再往"训练听力"升级。

**分三期**（P0×5 先落地，再 P1×3 训练闭环，P2 长期）：
- **P0（5 件，马上落地）**：
  1. 场景标签**两级分类**：第一级大类（日常生活/旅行出行/校园学习/职场沟通/新闻），点大类才出具体场景；**砍掉横向滚动条**（吸收原 N9-10"场景标签条→分类列表"需求，原 N9-10「分类维度怎么分」疑问已由 5 大类定档；新闻大类含原 新闻/长对话/短文 题型）。
  2. 主卡片放大（宽 60–70%、英文 ≥18px / 中文 16px），**砍掉"模糊隐藏"**（隐藏=完全不显英文，只留"🔊 先听音频，点此显示原文"；显示=清晰），卡片加场景名/句序号/播放进度条。
  3. 按钮**一行排开** `[←上一句][▶播放][慢速][原文开关][中文开关][下一句→]`，原文/中文做**切换开关**（非普通按钮），播放按钮视觉中心；砍掉第二行孤零零的"隐藏中文"。
  4. 卡片顶部加"第 X 句 / 共 N 句" + 可视化进度条；加"📄 查看全文"展开完整对话（可点句跳转、可收起）。
  5. AI 助教**去重 + 挂钩内容**：只留一个头像（提示"💡 这句没听懂？点我"），点击展开 ① 生词 ② 语法 ③ 慢速 ④ 举例；砍掉与内容无关的通用聊天机器人。
- **P1（3 件）**：⑥ 单句改成三阶段（听→理解[听懂/没听懂]→说[跟读录音 AI 评分]）；⑦ 训练状态反馈（本组进度 ✓●○ + 今日统计：完成句数/平均听懂次数/跟读分）；⑧ 精听/挑战双模式切换（挑战模式全隐藏、最后给正确率）。
- **P2（长期）**：⑨ 扩展训练类型（听写 / 听音辨义 / 综合），顶部一级 Tab 放训练类型、二级放场景。

**砍掉**：模糊隐藏英文 / 第二行"隐藏中文"按钮 / 重复 AI 头像 / 裸露横向滚动条。
**保留**：听力精听/口语跟读 Tab / 播放·慢速·上一句·下一句 / 原文·中文功能（改开关）/ 多场景内容。

**改造落点（已只读核实）**：
- 场景标签条与返回按钮在 `assets/voiceplayer.js`：`.vp-scene` 横向滚动 chips（样式 L172–174 / 生成 L231 / 注入 L241），数据源 `SCENES`（L33 起，8 情景 + 新闻/长对话/短文题型）；`vp-back` 返回按钮 L234（**已是 N9-9 全站返回按钮基准，勿改其语义**）。
- 模糊隐藏英文、按钮两行、AI 双头像等应在 `听说训练` 相关页 + `voiceplayer.js` 渲染逻辑里；开工时先 `Grep` `vp-` / `模糊` / `blur` 定位确切行号（本会话尚未逐行摸底语音播放器内部，开工前再补精读）。
- `.vp-back` 返回基准、SCENES 结构、`#vpMask` 宿主 id（app.js L1843/1850 联动）均**不可改**；改 `voiceplayer.js` 需 bump 其版本戳。

**约束（沿用全站铁律）**：
- 老 WebView 语法禁令：禁 `?.` / `??` / `replaceAll` / `Object.fromEntries` / `Array.at` / 顶层 await；录音用 `MediaRecorder`（兼容性需验证，旧内核可能不支持 → 回落提示）。
- ADR-3 合规：全文弹层/AI 面板用页内视图或轻量 popover，不堆全屏 modal；AI 讲解弹层复用 `openAppModal` 体系（N9-12/13/14 可共享）。
- 改 JS 后 bump 对应版本戳，避免 APK 旧缓存。
- 训练状态/统计：本端 localStorage 起步（无后端训练接口），与 P0 纯前端改造解耦。

### 10.13 N9-19 模拟面试页面重构（设计稿驱动，2026-09-15 用户提供画布设计稿 + PDF）

**设计稿交付物（用户已画好）**：
- PDF：`C:\Users\ATM\Documents\模拟面试页面重构.pdf`（用户在画布工具里导出/附上的重构说明与设计稿）。
- 画布设计文件（Ardot 工具内，用户称"可直接编辑"）：含两个屏幕 —— **Screen 1「回答中」**（对应"流程感"）、**Screen 2「已点评」**（对应"提交后反馈"）。设计基线：用户先在画布"加载了 Web 应用设计规范"，风格=浅紫灰画布 + 白卡片 + Google 蓝主色（克制、找回面试工具的"严肃感"）。

**用户的判断（已采纳豆包+GPT 诊断，更直白）**：页面不是"丑"，是**信息架构失败**——① 三个主操作平级竞争 ② 两个 AI 人格互相打架 ③ 提交后石沉大海；三者任一都足以流失用户，猫头鹰只是表层。**他们给的 6 条改动清单基本照单全收**，仅补充两点。

**设计稿里落地的 6 件事**：
| 需求 | 落地方式 |
|---|---|
| ① 每阶段只有一个主按钮 | Screen1 当前阶段只有蓝色「结束回答」；Screen2 只有「下一题」 |
| ② 语音/文字二选一 | Tab 切换，默认「文字回答」高亮，语音 Tab 灰显 |
| ③ 猫头鹰降级 | 缩成 40px 圆形小头像 + 标「AI 面试官」，题目移入文字气泡 |
| ④ 删 AI 美女 | 已删，AI 辅助合并为「答题提示」按钮 |
| ⑤ 演示悬浮球 | 删掉悬浮球形态，改为题目下方「查看示范回答」 |
| ⑥ 提交后必须有反馈 | Screen2 完整展示：三维评分条（表达逻辑 8 / 内容深度 7 / 岗位匹配度 8）+ 优点 + 不足 + 改进建议 + 8 分参考回答 |

**用户的两点补充（相对豆包/GPT）**：
1. **"完成 0/5"进度不该只在顶栏** → 升级为**页面内五段状态条**（读题→准备→回答→提交→点评），比"当前步骤高亮"更强的流程锚点。
2. **"查看示范回答"不做悬浮球** → 采纳方案 B，做成题目卡片里的**次级按钮**，与"答题提示"并列（功能保留、视觉降级、不抢戏）。

**砍掉的**：居中猫头鹰、右下角助手、演示球、音频播放器样式的上一题/耳机/进度条 —— 新稿均无；顶部保留进度点 +「第 1/5 题」。

**状态机（开工写交互文档的依据）**：六阶段（读题→准备→回答→提交→点评→下一题？），每阶段按钮与显示内容由两屏定义；用户可再补一屏「准备中（30 秒倒计时）」状态稿。

**改造落点（已只读核实）**：
- `AI模拟面试.html`：仅引 `assets/config.js?v=20260915c` + `assets/icon-map.js?v=20260915c`（L609–610），面试运行时逻辑为**页内内联**（或经 config.js 注入）；视觉元素（面试官区域 L579–580 猫头鹰/面试官、评定报告 L592、重新面试 L606）均在页内。
- 关联 `assets/iv-prep.js`（面试准备/备考 JS，待开工确认是否本页共用）→ 若改动需 bump 其戳。
- 设计基线"Web 应用设计规范"是画布工具内规范，非代码文件；实现时 UI 对齐该规范的浅紫灰/白卡/Google 蓝令牌（common.css 已有设计令牌，开工时确认新增/复用）。

**执行注意（沿用全站铁律）**：
- 老 WebView 语法禁令：禁 `?.`/`??`/`replaceAll`/`Object.fromEntries`/`Array.at`/顶层 await；状态机用 ES5 友好写法。
- ADR-3 合规：评分/示范回答/答题提示弹层用页内视图或轻量 popover，不堆全屏 modal；可复用 `openAppModal`。
- 改 `AI模拟面试.html` 内联 JS / `iv-prep.js` 后 bump 对应版本戳（config.js/icon-map.js 版本戳在 L609–610，若动本页逻辑需升戳防旧缓存）。
- 建议开工顺序：先按设计稿把"两屏状态机 + 五段状态条"骨架搭出来（P0 视觉/架构），再补"准备中倒计时"第三屏与评分算法对接。

### 10.14 N9-20 学习数据页面调整：「本机学习数据」→「学习报告」（2026-09-15 用户提供实用方案 · ⚠️已被 N9-22 合并方案取代，仅存档）

> **状态：仅存档，不执行。** 用户 2026-09-15 18:57 澄清：个人中心「本机数据」整块删除、并入综合「学习概括」页（N9-22 终版），不再原地改名。本节的全部 P0/P1/P2 改动已并入 N9-22。

**原需求文档全文**：`需求文档-学习数据页面调整-20260915.md`（部署源根目录，已落盘）。原一句话定位：把冷冰冰、全是 0、还混着跳走按钮的「本机学习数据」，改成真正有用的「学习报告」。

**落点（已只读核实）**：
- `个人中心.html` L266–274：`<section data-subpage="local-data">` 内 `本机学习数据` 标题（L272）+ 空 `#localStats`（L274），内容由本文件底部 `renderProfileOverview()` 填充。
- `assets/api.js` `renderProfileOverview()`（本地档案）L1297–1304 的 `学习数据` 卡 + L1315–1321 `学习模块` 快捷入口（**错题本 / 四级词汇 / 行测刷题** 三个 `onclick` 跳走按钮）。
- `assets/api.js` `_renderProfileOnline()`（在线档案）L1377–1384 的 `学习数据` 卡（同名卡片，在线态同样需同步改名）。

**核心改动（分两档）**：
- **P0（3 件，纯文案/删节点，风险最低）**：① 标题 `本机学习数据` → `学习报告`（HTML L272 + api.js L1298/L1378）；② 空状态——任何指标/列表/图表无数据时**不显示 0**，给「还没有学习记录…」+ 浅色 `data-icon` 占位；③ 移除 `学习模块` 入口里的 **错题本 / 四级词汇 / 偏好设置** 三个按钮（行测刷题是否保留开工确认，建议保留或同样移除让用户走侧边栏）。
- **P1（3 件）**：④ 核心指标卡接空状态（连续打卡/总时长/总题数/正确率，沿用 `profile-stat`）；⑤ 最近学习记录流水（`activityLog`）；⑥ 近 7 天学习时长柱状图（纯 CSS 柱 / 内联 SVG，不引图表库）+ ⑦ 行测各模块正确率/题量表格（`EXAM_BANK` / `appData.examTypeProgress`）。
- **P2（1 件）**：⑧ 各模块学习时长环形图（`activityLog` 按模块聚合）。

**数据来源（已确认存在、可复用，无需新增后端）**：
- `activityLog`：已存在的本机学习行为日志，承载「最近学习记录 / 近 7 天时长 / 各模块时长」。
- `EXAM_BANK` + `appData.examTypeProgress`：行测各模块做题累计（批次八已修复新题型漏统，常识判断等能正确进统计）。
- `appData.stats`：连打卡/总时长/总题数/正确率（现 `api.js` L1300–1303 渲染）。

**明确不做（防范围蔓延）**：AI 分析师 / 目标完成度 / 能力雷达图 / 导出 PDF。

**约束（沿用全站铁律）**：老 WebView 语法禁令（禁 `?.`/`??`/`replaceAll`/`Object.fromEntries`/`Array.at`/顶层 await）；图表纯 CSS/SVG 不引第三方库；ADR-3 全页内（无弹窗）；保留 `#localStats` / `renderProfileOverview` 等 id 与函数名；**改 `api.js` 后必 bump 全站 `app.js` 版本戳**（6.4）。

### 10.15 N9-21 写发帖页面改进（综合版·做减法，2026-09-15 用户综合 GPT 提供）

**需求文档全文**：`需求文档-写发帖页面改进-综合-20260915.md`（部署源根目录，已落盘）。一句话定位：三个页面里完成度最高，但「功能堆太多」——反向做减法。

**落点（已只读核实）**：
- `学习博客.html`（广场/写发帖编辑页）：`ed-tpl-row` 快速模板 L204–212；基本信息 6 字段（标题/分类/可见范围/封面/标签/图片）L214–248；富文本工具栏 10 按钮 L255–272；左写右预览 `blogEditorInput` L278；`AI 辅助写发贴` L290（`editorAiAssist`）；草稿恢复条 `ed-draft-bar` L197–202（**部分已实现**，勿重做）；顶部主导航 4 tab `广场/我的文章/写发贴/统计` L166–169（`sb-tab`）。
- `blog_wechat.html`：互动广场写发帖视图，同含 封面（emoji 或图片URL）L137 / 截图插图（会以 base64 存入）L148 字段，需同步清理技术术语。

**分阶段**：
- **P0（5 件，做减法）**：① 渐进式填写——首屏只留 标题/正文/分类，其余收进「＋添加」；② 删技术细节——封面改按钮选择（选Emoji/上传图片，URL藏高级）、删「会以 base64 存入」；③ 顶部导航简化——写发帖视图改 `←返回广场`+`发布帖子`；④ 编辑器简化——工具栏 10→5-6（加粗/斜体/列表/引用/图片），代码/公式/分割线收进「更多」，「支持 Markdown」→问号图标悬停；⑤ 写作区放大（≥屏幕 40%）。
- **P1（3 件）**：⑥ 快速模板默认收起（入口「✨ 不知道写什么？试试模板」，点击展开、自动填充）；⑦ AI 辅助写作场景绑定（起标题/整理成笔记/检查错别字/续写，非通用聊天）；⑧ 发布前预览（标题/正文/分类/标签 + 确认发布/返回修改）。
- **P2（2 件）**：⑨ 视觉三级层级；⑩ 草稿自动保存（每 30s 存 localStorage，复用本页 `ed-draft-bar`/`edRestoreDraft`/`edDiscardDraft`/`edDraftState`）。

**约束**：老 WebView 语法禁令；ADR-3（预览/更多页内，非全屏 modal）；保留 `showBlogView`/`beTitle`/`beCover`/`blogEditorInput`/`editorTool`/`editorAiAssist`/`edInsertTemplate` 等 id 与函数名；改 `api.js` 的 editorTool/saveBlogNote/editorAiAssist 需 bump 全站 `app.js` 戳（6.4），只改 `学习博客.html` 内联则按部署 SOP 打包。

### 10.16 N9-22 学习概括页面整合（**合并终版**·2026-09-15 18:57 用户澄清修正 · 取代 N9-20）

**需求文档全文**：`需求文档-学习概括页面整合-20260915.md`（部署源根目录，已落盘，已重写为合并终版）。一句话定位：**做 ONE 综合「学习概括」实时多维分析页，吸收「本机数据」+「学习统计」两块能力；删除个人中心「本机数据」菜单与页面、删除全站「学习统计」菜单；新综合页唯一入口挂在「更多」页。**

**用户终版口径（截图澄清）**：把「学习概括」综合页 + 个人中心「本机数据」合并成 ONE 页；删「本机数据」菜单和页面；新建页入口加到「更多」页；把「学习统计」功能并入该页；删「学习统计」菜单。

**要删除 / 合并的资产（已 Grep 核实行号）**：
- 个人中心「本机数据」菜单卡：`个人中心.html` L106–108（`<div class="subpage-group-card" onclick="SubpageRouter.navigate('local-data')">` + `sgc-title">本机数据`）→ 删除整卡。
- 个人中心「本机数据」子页：`个人中心.html` L269–276（`<section data-subpage="local-data">` + `<div id="localStats">`）→ 删除整 section。
- 个人中心引用清理：`个人中心.html` L485 `getElementById('localStats')`、L823 `else if (key === 'prefs' || key === 'local-data')`、`SubpageRouter.init({...})`（L782–793）内 `local-data` 路由项 → 一并清理，避免空引用。
- 更多页「学习统计」功能卡：`更多.html` L102–106（`onclick="openBlogStats()"` + `mpc-title">学习统计`）→ **改为「学习概括」入口卡**（改名 + 改 onclick 指向新综合页）。
- 更多页旧底部弹层：`更多.html` L164–166（`.bottom-more-item` + `bm-label">学习统计`）→ 删除（或随 `#morePanel` 整体下线，legacy 兜底层）。
- 30+ 页底部导航「学习统计」按钮：各页 `.bm-label">学习统计` + `onclick="openBlogStats()"`（如 `行测刷题.html` L171 / `错题本.html` L150 / `四级词汇.html` L179 / `申论刷题.html` L238 / `商务礼仪.html` L137 …）→ 删菜单项（见待确认②）。
- 现有聚合入口：`openBlogStats()`（api.js）→ `学习博客.html#stats`、`renderBlogStats()`（api.js L761）、`学习博客.html` `blogViewStats`（L306）→ 综合页吸收其统计渲染逻辑。

**新建综合「学习概括」页（吸收两块）**：
- 核心指标卡（P0）：连续打卡/总时长/做题总数/正确率 —— `appData.stats`。
- 各模块进度表（P0/P1）：行测各模块正确率+题量 —— `EXAM_BANK`/`appData.examTypeProgress`。
- 近 7 天学习时长柱状图（P1）：`activityLog`（纯 CSS/SVG）。
- 最近学习记录（P1）：`activityLog`。
- 各模块时长占比环形图（P2）：`activityLog`/`appData`（纯 SVG）。
- 「更多」页入口（P0）：`更多.html` L102–106 改「学习统计」→「学习概括」，指向新综合页。

**与 N9-20 关系**：N9-22 终版 **取代** N9-20 —— 个人中心「本机数据」整块删除并入综合页，N9-20 不再单独执行（仅存档，见 10.14）。

**待确认（开工前）**：
1. 综合页承载位置：独立新 HTML（A，倾向）还是增强 `学习博客.html#stats`（B）。
2. **✅ 已定（2026-09-15 19:27 主理人拍板）：30+ 页底部导航「学习统计」= 删除（方案①）**，但 `openBlogStats()` **函数保留并重定向**到新综合页作兼容别名（防止漏网引用触发 ReferenceError 白屏——本项最大风险点）；更多.html L102–106 改「学习概括」入口卡、L164–166 删；`renderBlogStats()` 不动。理由：贴合「删学习统计菜单」原话，留着叫「学习统计」的按钮等于没删干净；入口收敛到「更多」页。代价：各页底部导航少一格 + 失去任意页一键触达（用户已认可）。
3. `#morePanel` 旧底部弹层（L161–179）整层下线 还是 仅删「学习统计」项（需结合部署 SOP 确认底部弹层是否仍启用）。
4. `renderProfileOverview()`（填 `#localStats`）在「本机数据」删除后是否弃用。

**约束**：老 WebView 语法禁令；图表纯 CSS/SVG 不引库；ADR-3 页内；改 `api.js` 的 `openBlogStats`/`renderBlogStats`/`renderProfileOverview` 需 bump 全站 `app.js` 戳（6.4）；删 `个人中心.html` 内 `local-data` 子页与路由时勿残留 `'local-data'` 分支。

### 10.17 N9-23 每日一题卡改造（豆包方案A·实用版·2026-09-15 用户转发）

**需求文档全文**：`需求文档-每日一题卡片改造-豆包-20260915.md`（部署源根目录，已落盘）。一句话定位：把「面试题库·四分类」题卡改成「今日一题」——打开就能答、答完有反馈、进度可累计、不跳页；去掉与下方「模拟面试」卡重复的「开始模拟面试」入口。

**落点（已只读核实）**：
- 题卡 `#ivQuizCard`：`商务礼仪面试.html` L101–125（标题 `.sprint-head` L102 / 分类 tab `#ivQuizTabs` L103 / 题目 `#ivQuizQ` L105 / 三按钮 `.qtab-btns` L106–110 / 要点区 `#ivQuizTip` L111 / AI 面板 `#ivAiPanel` L113–124）。
- 题库 `IV_BANK`：L642–655（**共 12 题**，结构 `{cat, q, tip}`，要点是单段字符串 `tip` 而非 `keyPoints` 数组）。
- 分类 `IV_CATS`：L656（`自我介绍/简历深挖/压力面/礼仪细节`）；状态 `IV={cat:'全部',idx:0,order:[]}`：L657。
- 函数（均本页内联 IIFE）：`renderTabs`(L659) / `pickQuestion`(L666) / `ivQuizCat`(L679) / `ivQuizNext`(L680) / `ivQuizShowTip`(L681) / `ivStartInterview`(L692，触发 `#ivAiPanel`) / `boot`(L774)。**未分离到独立 assets 文件** → 改动直接改本页内联。

**豆包方案核心（分 6 步，可独立验证）**：
1. 标题→`今日一题 · {分类}` + 右上「已答 X / Y 题」+ 题下 textarea + 三按钮改为「换一题/提交回答/查看答题要点」+ **删「开始模拟面试」按钮**。
2. 换一题：同分类随机不重复 + 清空输入 + 收起要点（复用 `ivQuizNext`，补清空逻辑）。
3. 查看要点：展开/收起 toggle（复用 `ivQuizShowTip`，补 toggle）。
4. 提交回答（**新增 `ivQuizSubmit()`**）：空输入 `showToast` 提示；展开要点区；进度 +1；按钮置灰「已提交」；存 `localStorage: interview_daily_answers`（questionId+answer+timestamp）。
5. 分类切换：复用 `ivQuizCat`，进度随分类更新；「全部」随机。
6. 刷新恢复：显示上次题/分类，已答题按钮置灰。

**不做的（豆包砍掉）**：AI 评分 / 答题历史列表页 / 题目收藏 / 分享 / 改下方 6 个功能卡（只改红框题卡）。

**❗ 开工前待确认（主理人核实发现 2 处冲突）**：
- **冲突①·AI 面板孤儿**：删「开始模拟面试」按钮后，`#ivAiPanel`（L113–124 模拟面试官多轮对话）失去唯一入口 → 删按钮时**一并 `display:none` 或移除面板**（模拟面试入口由下方功能卡 L149 提供），倾向隐藏面板。
- **冲突②·要点字段**：豆包假设 `keyPoints` 数组，实际只有单段 `tip` 字符串 → 直接用 `tip` 渲染要点区即可（强拆编号列表属可选润色）。
- 附加：改本页内联 JS 后按部署 SOP 打包/补版本戳破缓存。

**约束**：老 WebView 语法禁令（禁 `?.`/`??`/`replaceAll`/`Object.fromEntries`/`Array.at`/顶层 await）；ADR-3 页内；保留 `ivQuizNext`/`ivQuizShowTip`/`ivQuizCat`/`pickQuestion` 等 id 与函数名；改 `商务礼仪面试.html` 内联需 bump 版本戳（6.4）。

### 10.18 N9-24 万能金句库升级（豆包 A+B+C·2026-09-15 用户转发）

**需求文档全文**：`需求文档-万能金句库升级-豆包-20260915.md`（部署源根目录，已落盘）。一句话定位：① 用户自己加金句（遮罩表单 + localStorage + 可删可改）；② 内置金句 18→80+ 句；③ 加「🌐 网络金句」按钮调一言 API 一键收藏。

**落点（已只读核实）**：
- 页面 `万能金句库.html` L95–96：标题「高情商万能金句库」+ 静态 tag `18句直接套用`（**硬编码，须改**）。
- 共享数据 `COMM_QUOTES`：`assets/app.js` L4359–4378（**18 条，10 分类**：拒绝/赞美/道歉/表达不同意见/催进度/安慰人/向上沟通/被甩锅/i人专属/万能开场；结构 `{id:number, category, icon:emoji, quote, scene, example, why}`）。
- 渲染 `renderQuotes()`(L5493) / `renderQuoteCard(q,viewed,isToday)`(L5532，用 `commIconHtml(q.icon)` L5542、`q.quote/scene/why/example`)；过滤 `filterQuotes`(L5485)；合并点 `catFiltered = COMM_QUOTES.filter(...)`(L5497)；队列 `getDailyQueue('commQuotes', catFiltered)`(L5498)；已有「移除」按钮 `removeBtn`(L5537，仅当日)。

**豆包三方案核心**：
- A 加功能：标题右「+ 添加金句」橙色按钮 → 遮罩表单（金句内容*/分类*/使用场景/为什么有效/示例对话）→ 存 `localStorage:custom_quotes`（`{id:'custom_1',category,quote,scene,why,example,isCustom:true}`）→ 合并显示；用户句有「删除」「编辑」按钮（内置仅「移除」当日）。
- B 扩内容：68 句按分类加入 `COMM_QUOTES`（拒绝+10/赞美+10/道歉+10/不同意见+10/催进度+8/安慰人+8/向上沟通+8/i人专属+6 = 86）；每句补 `icon`+`id`+正确 `category`；更新 L96 静态 tag。
- C 网络金句：标题「🌐 网络金句」按钮 → `fetch('https://v1.hitokoto.cn/?c=d')` → 弹窗（句子+出处+换一句/收藏/关闭）→ 收藏存 `custom_quotes`（`category:'来自网络'`，`isNetwork:true`）；防抖 1s；离线 catch→toast。

**❗ 开工前须对齐的 7 处差异/风险（主理人核实）**：
1. **共享文件高风险**：数据+渲染+表单+API **全在 `assets/app.js`（8761 行 / 31 页共用）** → 改完必 bump 全站 `app.js` 戳（6.4）+ WebView 语法合规 + `node --check`。
2. **字段不符**：内置每条有 `icon`(emoji)+数字`id`；豆包 custom 结构缺 `icon` → `commIconHtml(q.icon)` 对自定义句会空/报错，须补默认 icon 或函数容错。
3. **✅ 已定（19:27）：沿用实际 10 分类**——一个分类不删；豆包 68 句里「不同意见」全部重映射为「**表达不同意见**」；`被甩锅`(1)/`万能开场`(1) 各补 +7 句 → 均达 8 句；**总量 18 → 100 句**（现有18 + 豆包68 + 新增14），10 分类全 ≥8。理由：删分类=删已上线内容且破坏用户已学进度数据（appData 存内置 id）；补 14 句成本远低于功能回退。
4. **「来自网络」新分类**：功能C 收藏用该分类，须纳入 filter tab 类别集合（custom 合并时动态补）。
5. **✅ 已定（19:27）：`XMLHttpRequest` 单路径**——不依赖 `fetch`、不使用 `async/await`（老 WebView(API<21) 无 fetch 且 CORS 不稳）；配 `try/catch`+`onerror`+`ontimeout`(8s)→toast 不崩页；「换一句」1s 防抖；不走后端代理。遗留 L3 真机验证：APK 若 `file://` 加载需确认 CORS 放行，失败则降级为 APK 内隐藏该按钮。
6. **CORS/离线**：hitokoto 支持 CORS 但老 WebView fetch CORS 不稳，离线必须 catch→toast 不崩页。
7. **静态 tag**：`万能金句库.html` L96「18句直接套用」→ 改动态或「86句直接套用」；`高情商表达.html` L143「18句万能金句」描述可一并更新。

**约束**：老 WebView 语法禁令（禁 `?.`/`??`/`replaceAll`/`Object.fromEntries`/`Array.at`/顶层 await）；ADR-3（表单/弹窗全页内非 prompt）；保留 `renderQuotes`/`renderQuoteCard`/`filterQuotes`/`COMM_QUOTES` 等符号名；改 `assets/app.js` 必 bump 全站 `app.js` 版本戳（6.4）。

### 10.3 环境补充（本次实测新增）
- `C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\` 下 **12 个 `main-*` 目录全部**是指向 `D:\下载的文件\学习工作台\.git\worktrees\` 的 linked worktree（含当次会议目录 `main-88ed6fb3`）→ 第 2.1 节「不要改 worktree」的结论对**所有** worktree 成立。
- 两棵树 `app.js` 体量差 8761 字节 = 纯 CRLF/LF 差异（worktree `core.autocrlf=true`），内容与 HEAD 一致，**不是丢代码**。
- `任务执行文档-20260915-全线自主执行.md` mtime = 09:12:54、最后条目也是 09:12 → **09:12 之后的决策（含 quant-fix3 事故、17:10 复测）均未回写**，交接传统存在 8 小时断档。
- bash 缺 coreutils 再次复现（`dirname` / `cat` not found）→ 稳定做法：用 Write 写 `.py`，再用托管 Python 执行，结果 `open(..., 'w', encoding='utf-8')` 落文件后 Read。

---

*文档结束。有疑问先翻 `任务执行文档-20260915-全线自主执行.md` 的历史日志，那里有每一次决策的上下文。*

---

## 10.19 N9-25 · 个人中心卡片精简 + 博客统计入口修复 + 模拟面试按钮宽度

> 登记时间：2026-09-15 17:35 ｜ 来源：用户 4 张截图逐条反馈 ｜ 状态：**开工中（Wave 1 收尾后并行执行）**

### 用户原话要点
1. 个人中心页 —— 删除「本机数据」卡片
2. 个人中心页 —— 删除「发贴统计」卡片（**后经用户 17:38 确认为误判，改为不处理、保留**）
3. 广场（学习博客）「我的文章」页 —— 「统计」按钮点开是**一片橙色空白**，失效
4. 模拟面试页 —— 「提交回答」按钮**太长**（通栏），改短

> 附：用户另发 1 张「好友」页截图，17:38 确认为**误发，忽略**，不需任何处理。

### 定位（已逐条亲自验证）

| # | 目标文件 | 精确位置 | 判定 |
|---|---|---|---|
| ① | `个人中心.html` | L106-110 `subpage-group-card` → `SubpageRouter.navigate('local-data')`，标题「本机数据」 | 入口卡片需删；但 `section[data-subpage="local-data"]`(L269-276) 与 `#localStats` **必须保留**（`renderProfileOverview()` 依赖） |
| ② | `个人中心.html` | L91-95 `subpage-group-card` → `SubpageRouter.navigate('posts')`，标题「**发贴统计**」 | **✅ 用户 17:38 确认：不用处理，保留不动**。该卡是 `navigate('posts')` 的正常入口（用户初判"失效"经核对为该卡可正常跳转），`posts` 子页全套逻辑（L796/L809 `#profileBoxPostsHost`、`ppStatsCard`、`.posts-mode`）**原样保留** |
| ③ | `学习博客.html` | L169 `onclick="showBlogView('stats')"` | **真 bug**，见下方根因 |
| ④ | `商务礼仪面试.html` | `#ivQuizSubmitBtn`（提交回答）宽度 | 内联/CSS 通栏 → 改内容宽 |

### ③ 根因链（本批次最有价值的发现）

```
showBlogView('stats')                    // assets/app.js:6631
  → renderBlogStats()                    // assets/app.js:6638
      × 两份实现：app.js:7231  与  api.js:761
      × 学习博客.html 先载 app.js 再载 api.js
        → app.js L300 / api.js L303，api.js 后加载 ⇒ **api.js 版覆盖生效**
      × app.js:7248 找的容器 id = #blogStatsBox
      × 学习博客.html:306 外层容器 id = #blogViewStats（仅被 showBlogView 切 display:flex）
      → 内外层 id 不匹配 ⇒ 渲染落空 ⇒ 用户看到的「橙色空白壳」
```

**结论**：这是 F3（`renderBlogStats` / `renderBlogMine` / `openBlogStats` 在 app.js 与 api.js 中重复定义）的**首次实际爆雷**。
修复原则：**只改生效的那一份（api.js）所对应的 HTML 容器 id**，最小侵入。

### ④ 附带发现：`#toolsPanel` 并非死代码（纠正 W1-T4 判断）
- `assets/app.js:1352` `toggleToolsPanel()` 要求 `toolsPanel` **且** `toolsOverlay` 同时存在（`if (panel && overlay)`）。
- `id="toolsPanel"` 在 `工具.html` / `更多.html` / `个人中心.html` / `管理员.html` / `blog_wechat.html` 五页均存在，且每页 `toggleToolsPanel` 被引用 5-6 次。
- ⇒ **它是活的共享底栏面板，禁止删除**。（真正零调用的是 `closeToolsPanel`，属 app.js 层面，与各页无关。）

### 验收标准
- [x] ② **不处理**（用户确认保留）
- [ ] ① 「本机数据」入口卡片消失（5→4 张卡）；`posts` / `local-data` 底层 DOM 与路由 key 完整，页面 jsdom 0 报错
- [ ] ③ 点「统计」显示真实数据（容器可见 **且内容非空**）；`学习博客.html#stats` 跨页 hash 直达同样有效；25 个页面底部「学习统计」按钮不受影响
- [ ] ④ 提交按钮改为内容宽，`onclick="ivQuizSubmit()"` 保留，窄屏不溢出
- [ ] 全部改动文件：禁用语法 Grep 0 命中；配对平衡；`node --check` rc=0
