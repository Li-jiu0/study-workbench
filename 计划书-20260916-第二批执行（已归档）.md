# 星途 · 第二批 + 新增工单 执行计划书（开工前定稿）

> 定稿时间：2026-09-16 11:05
> 代码树：`D:\下载的文件\学习工作台` ｜ 线上：`20260916k`
> 状态：**准备完毕，等待开工指令**
> 本文件为执行依据；三处需用户拍板的决策已按最优意见定案（见 §1），执行中不再回头确认。

---

## 一、三件待拍板事项 · 最终定案（附证据）

| # | 事项 | 定案 | 依据 |
|---|---|---|---|
| 1 | 「互动广场」是否随「广场→社区」一起改 | **不改**（保留「互动广场」） | 实测 `更多.html:130-133`：卡片 `onclick="gotoBlogMine()"`、desc「我的发贴 · 草稿 · 收藏」；`blog_wechat.html:50-51` 侧栏 `title="互动广场 · 我的发贴"`。→ 它是**「我的发贴」功能**，与首页功能中心的「广场」模块（浏览大家的帖子 → `学习博客.html`）是两回事。若改成「互动社区」，会与首页「社区」模块撞名。 |
| 2 | 老内核流式验收口径 | **双口径**：现代浏览器按「2 秒内首字逐字输出」为硬指标；老 WebView 降级口径为「显示『AI 正在思考…』加载态 + 15 秒未出首字自动切换 + 整段返回后正确渲染」，**不要求逐字** | 实测 `ai-service.js:350/420` 两处均为**裸** `resp.body.getReader()`（全文 `res.body` 守卫 = 0，`stream:true` 硬编码在 `:398`）；老内核无 `ReadableStream` → 会直接抛错。且 `net-compat.js`（664 行、26KB、**顶层声明 0 个**、专为此场景写）**全站零引用**。 |
| 3 | 首页小助手接 AI 的落点 | **走 `callAI`（`ai-service.js`，前端直连），不走后端 `/api/ai/chat`** | 全站 AI 底座即 `ai-service.js`；`app.js` 内**已有 3 处 `callAI`**；手册 §7.9 明确「不涉及后端 `/api/ai/chat` 中转逻辑（400 是前端直连智谱的问题）」。走后者等于推翻整个底座并废掉需求 D。 |

---

## 二、⚠️ 开工前发现的两个「文档指引有坑」（必须先知道）

### 2.1 破损比文档记录严重得多：是**白屏级**的 script 边界破损

文档 §3.2 说「剔除 script/style 后真实 DOM 的 `<div>` 是 162/162 平衡，**暂无白屏级损坏**」。**这个判断是错的。**

`tools/qa/escheck_es2017.js` 实测输出 **`DONE 3`**（原文：`C:\Users\ATM\_escheck_out.txt`）：

```
*** 更多.html [内联#2] :: ES2017 解析失败（老内核会整文件报废）
     需要 ES>2020  |  Unexpected token (3:0)
     L3: </head>
```

**根因链（逐行实测）**：

| 位置 | 事实 |
|---|---|
| `更多.html` L198 / L199 | `<script>` + `(function(){` —— 更多页自己的内联脚本开头 |
| `更多.html` L200 | **多出来的第二个 `</head>`**（L3 已有 `<head>`、L34 已有 `</head>`） |
| L201 / L203 / L204 | `<body class="theme-home">` / `<div class="app">` / `<nav class="sidebar">` —— **整份工具页骨架被吞进来** |
| L352 | 误搬内容里夹带的 `<script>` |
| L437 | 左近唯一的 `</script>` |
| 全文统计 | `<script>` **16** vs `</script>` **15** → **缺 1 个闭合标签** |

→ 浏览器（及 escheck）看到 L198 的 `<script>` 后，会把 **L200 的 `</head>` 一直到 L437 之间 240 行、272 个 HTML 标签**全部当成脚本内容 → 老内核上解析失败（`Unexpected token`）→ **更多页内联脚本整段报废 + DOM 结构错乱**。

> 这正是 §3.2 断言表之外的第 6 项破损，**优先级最高**，必须最先修。

### 2.2 文档说的「可直接回滚比对」的备份**不可用**

文档 §3.2 称备份已存在、可直接回滚。实测：

| 版本 | 行数 | script 配对 | importerView | div 平衡 | 结论 |
|---|---|---|---|---|---|
| `更多.html`（当前） | 573 | 16 / **15** | 2 | 243 / 242 | 破损 |
| `更多.html.bak-pre-batch2-20260916`（本次新建） | 573 | 16 / 15 | 2 | 243 / 242 | 同破损 |
| `更多.html.bak-pre-cfix-20260916` | 573 | 16 / 15 | 2 | 243 / 242 | **同破损** |
| **线上 `20260916k`** | **304** | **14 / 14** | **0** | **155 / 155** | ✅ **唯一干净基准** |

与 `bak-pre-cfix-20260916` 做 difflib：**差异块数 = 0** —— 即那个备份是**破损之后**做的快照，**不能用于回滚**。

**→ 修正后的修复策略**：以**线上 `20260916k` 版**为唯一可信结构基准做对齐比对，在本地文件上做**精确切除**（只删不加），而不是依赖那个失效的备份。

---

## 三、文件占用矩阵与波次编排（核心）

### 3.1 争抢关系（决定串并行）

| 共享文件 | 争抢方 | 约束 |
|---|---|---|
| `assets/app.js`（**31 页引用**，9071 行，7.05 MB） | 需求 B（`HOME_DEF` L6719 / `recentMap` L6776）、首页小助手（`AI_PARTNERS` L3088 / 卡片 L3238-3342）、关于独立页（`showAbout` L8604 + 入口 L7737） | **三者严格串行**（后写覆盖先写） |
| `AI.html` | 需求 C（输入框双槽位）、模型按钮修复（`#aiModelInfoBtn`） | **串行**（同一输入区） |
| `assets/ai-service.js` | 需求 D（主写者）、首页小助手（消费方 `callAI`） | 需求 D 主写，小助手只读调用 |
| `assets/common.css`（全站） | 倒计时卡缩小 | 共享 CSS → 全站 bump |
| `assets/api.js`（**30 页引用**） | 头像修复 | 共享 js → 30 页 bump |
| 6 个待重命名 HTML | 需求 B | 影响 70 处文件名引用 |

### 3.2 波次编排（含独占声明）

| 波 | 内容 | 独占文件 | 与前后波关系 |
|---|---|---|---|
| **W0** | **修 `更多.html` 破损**（只删不加）：切除误搬区块 + 补齐 `</script>` 配对 → 达成 §3.2 全部 6 组断言 + escheck 归 0 | `更多.html` | 前置，不做完不开工后续 |
| **W1** | 需求 C（`AI.html` 输入框统一）+ 模型按钮修复（`#aiModelBtn` → z-index 提到 ≥300、点侧栏先关抽屉/遮罩、侧栏按钮文案跟随选中模型） | `AI.html` + `ai-page.js`（读） | AI.html 独占；一次改完一次 bump |
| **W2** | 需求 D（400 修复 / 降级条件 / 15s 超时 / 流式守卫）+ 首页卡片接 `callAI` | `ai-service.js` | ai-service.js 独占 |
| **W3** | 需求 B（首页 6 卡更名 + 6 个 HTML 重命名 + 70 处引用 + 文案替换 + `recentMap`） | **`app.js` 独占窗口开启** | app.js 串行起点 |
| **W4** | 首页小助手重构（`AI_PARTNERS` 改名/接真 AI/弹窗）+ 关于独立页（`关于.html` + `showAbout` 改跳转） | `app.js`（延续）+ `学习工作台.html` + 新建 `关于.html` | app.js 独占窗口延续 |
| **W5** | 倒计时卡缩小（`common.css`）+ 头像修复（`api.js`） | `common.css` / `api.js` | 与 W4 可并行（无重叠） |
| **W6** | 自检 → 统一 bump（→ `20260916M`）→ dry-run → 部署 → 线上复检 | 全站 `?v=` | 收尾 |

**并行度**：W0/W1 可并行（`更多.html` × `AI.html` 无重叠）；W2 与 W3 可并行（`ai-service.js` × `app.js` 无重叠）；W5 与 W4 可并行。

### 3.3 每波硬闸门（全部必须过）

1. `node tools/qa/escheck_es2017.js` → 输出必须为 `DONE 0`（当前 `DONE 3`）
2. `<!--` 数 = `-->` 数；剔除 script/style 后 `<div` = `</div>`；`<script>` = `</script>`
3. 老内核禁用语法 Grep 全 0：`?.` `??` `replaceAll(` `Object.fromEntries` `.at(` 顶层 await `(?<=` `(?<!`
4. 原生 `alert(` / `confirm(` / `prompt(` = 0（ADR-3）
5. 无新增顶层重复全局声明（新增全局一律 `if (typeof window.x !== 'function') window.x = ...`）
6. 改过的 `assets/*.js` 与 HTML **全部 bump `?v=`**；全站「不带戳的 assets 引用」= 0
7. 视觉类（弹窗、输入框、倒计时卡）375px + 1280px 双端截图对比

---

## 四、回滚点与基线

### 4.1 已建立的回滚点（`*.bak-pre-batch2-20260916`，部署脚本自动跳过含 bak 的文件）

`更多.html`、`工具.html`、`PPT训练.html`、`AI.html`、`学习工作台.html`、`学习博客.html`、`四级备考.html`、`央国企笔试.html`、`高情商表达.html`、`商务礼仪面试.html`、`设置.html`、`个人中心.html`、`blog_wechat.html`、`动态.html`、`行测刷题.html`、`商务礼仪.html`、`app.js`、`api.js`、`ai-service.js`、`ai-page.js`、`common.css`、`icon-map.js`、`ai-config.js`、`xt-content.js`（共 24 个）

### 4.2 md5 基线（用于事后校验）

| 文件 | 字节 | md5 |
|---|---|---|
| `更多.html` | 35382 | `8fb1a09995ac51aa59b2d2417a670844` |
| `工具.html` | 24696 | `a0bd6fe938999203187fe17c9b731ccb` |
| `PPT训练.html` | 48307 | `25184a2ece9271f8491198f23c9877cd` |
| `AI.html` | 48737 | `58567c6592fad11eb8b3ad248fcb505b` |
| `学习工作台.html` | 51962 | `5dc5b53940e1fb5b458ed47b90deb87c` |
| `assets/app.js` | 7052711 | `385ccf7b1e653facc482ec199da03436` |
| `assets/api.js` | 108440 | `5d2e616f550ffa9196955b82ca3e5072` |
| `assets/ai-service.js` | 21518 | `7bbc836adc043d752a181dca5b863525` |
| `assets/ai-page.js` | 60089 | `c7427d932143234253df9fe4ab054052` |
| `assets/common.css` | 120789 | `d7cbc48f10d33796002f3b7348029462` |

### 4.3 换行符（**改文件必须保留，否则全文件 diff**）

- **CRLF**：全部根 HTML、`app.js`、`api.js`、`common.css`、`icon-map.js`、`ai-config.js`、`xt-content.js`
- **LF**：`ai-service.js`、`ai-page.js` ← 仅此两个是 LF，勿混

---

## 五、需求 B 替换口径（已定案，负向断言）

实测运行时 98 个文件中：

| 旧名 | 总命中 | 文件数 | 主要集中 |
|---|---|---|---|
| 广场 | 106 | 32 | app.js 21、学习博客.html 10、blog_wechat.html 9、设置.html 7 |
| 四级备考 | 71 | 39 | app.js 17、mock_exam.html 4、设置.html 4 |
| 央国企笔试 | 62 | 38 | app.js 12、mock_exam.html 3、设置.html 3 |
| 高情商表达 | 57 | 33 | app.js 12、设置.html 4、高情商表达.html 4 |
| 商务礼仪面试 | 49 | 33 | app.js 10、设置.html 4、mock_exam.html 3 |
| PPT训练 | 52 | 35 | app.js 7、PPT训练.html 4、设置.html 4 |

**「广场」替换口径（负向断言）**：
- 替换为「社区」：**70 处**
- 保留（`广场` 前紧邻「互动」）：**36 处**
- 剔除的 3 类特殊项（需人工判断，已列入白名单审计文件）：
  - `学途.html:547`「笔记广场」→ 属该页自有命名，**不在需求范围，保留**
  - `设置.html:211/212/250/857/860`「广场编辑/阅读区字号」「广场编辑器自动套用」→ 是**设置项文案**，跟着模块改名**要改**
  - `学习博客.html:662` `'星途广场发贴'`（分享标题）→ **要改**为「星途社区发贴」

**替换顺序（长优先，防误伤）**：`商务礼仪面试 → 商务礼仪`、`高情商表达 → 表达`、`四级备考 → 英语`、`央国企笔试 → 行测`、`PPT训练 → 演示`、`广场 → 社区`（最后，且带负向断言）。

**注意**：`k` 键（`plaza/cet/exam/comm/interview/ppt`）**保持不变**（localStorage 显示偏好按 k 索引）；`app.js:6776 recentMap` 需同步文件名映射。

---

## 六、工程补充项（文档未提，但建议一并做）

1. **重命名后线上存量链接会 404**。可在 nginx 加 301 旧→新跳转兜住 —— **属生产操作，需用户点头**，默认不做。
2. **`ai-service.js` 流式加固方案（双层）**：
   - 第一层（本波必做）：加能力守卫 + 非流式兜底 —— `if (resp.body && resp.body.getReader)`，不可用时改发 `stream:false`（或 `resp.text()` 整段解析），保证老内核不抛错、仍能出答案。
   - 第二层（留到 Wave 6 全站批量波）：注入 `net-compat.js`（它专为此写、顶层声明 0 个、风险低），让老内核也能流式。**现在不动 37 页加载序。**
3. **`tools/sync_audit_20260916.txt` 实测不存在**（09-16 交接文档 §7.3 引用它），做数据同步前需重新生成。
4. `更多.html` 破损失败意味着**该页在本地已不可用**，若误部署会带来全站第 3 次事故 —— 部署前必须 `DONE 0`。

---

## 七、开工顺序（一句话）

**W0 修破损 → W1（AI.html 需求C+模型按钮）∥ W2（ai-service 需求D）→ W3 需求B 改名 → W4（app.js 小助手+关于页）∥ W5（css 倒计时+api 头像）→ W6 自检/bump/部署/复检**

---

*准备人：主理人 齐活林 · 2026-09-16 11:05。所有基线数据均为本机实测，非文档转述。*
