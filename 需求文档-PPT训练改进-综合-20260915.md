# PPT训练页面改进需求（综合版）

> 来源：用户综合豆包 + GPT 观点（2026-09-15 18:15 提供）。**仅登记，未开工。**
> 登记编号：N9-14（见 `交接文档-批次九-20260915.md` 第 10.8 节）。
> 改造对象：`PPT训练.html` + `assets/data-ppt-tips.js`（设计四原则课程数据）+ `assets/ppt-tips.js`（渲染器 `window.PPTV2['ppt-tips']`）。

## 一句话定位
**现在它是"教你怎么做PPT"，还不是"训练你把PPT做好"。**
内容本身是扎实的（设计四原则、配色、字体讲得都到位），配得上叫"PPT基础课程"。但叫"PPT训练"还差一截——现在 80% 是"看课"，只有 20% 是"训练"。真正的训练得让用户动手改，而不只是读文字。

## 第一阶段：把现有课程体验做好（P0，马上能落地）

### 1. 每个原则配"正反例对比图"（最紧急）
- **问题**：课程介绍写着"每节都有正反例对比"，但正文全是文字，讲"对齐"没有对齐前vs对齐后的图，讲"对比"没有对比示例
- **改法**：每个原则至少配一组对比图——左边 ❌ 错误示例，右边 ✅ 正确示例，中间箭头
- PPT设计是视觉的东西，**没有图等于白讲**，这个必须加

### 2. 加"标记已学"按钮 + 阅读进度
- **问题**："已学 0/3 节"摆在那，但没有按钮让用户标记已学，读完了也不知道怎么记录
- **改法**：
- 每节末尾加"✅ 我已学完本节"按钮
- 点击后统计更新，按钮变灰显示"已学完"
- 已学的章节标签上加 ✅ 标记
- 顶部加细进度条，显示当前节读了多少（随滚动更新）

### 3. "课后小测"和"正反例"做成可点击入口
- **问题**：课程卡片上"课后小测 8 题"、"正反例 10 组"看起来像纯展示，点了没反应
- **改法**：
- 点"课后小测 8 题"→ 进入测试页，8道选择题，答完给分和解析
- 点"正反例 10 组"→ 进入对比图浏览页，10组错误vs正确的翻页
- 不要做成死标签

### 4. 正文内容卡片化，不要大段文字
- **问题**：全是大段文字，密度高，没有重点，读着累
- **改法**：
- 每个要点（"把每页当成一张表格"、"检查顺序建议"等）做成小卡片
- 关键词加粗，重点句子高亮
- 长段落拆短，增加行间距
- 小节默认折叠（除了当前看的），减少页面长度

### 5. 顶部导航栏颜色改一下
- **问题**：橙色导航栏 + 深蓝色课程卡片，两个高饱和颜色撞在一起，看着不舒服
- **改法**：导航栏改成和整体统一的浅灰或白色，不要橙色

## 第二阶段：增加"动手训练"环节（P1，让"训练"二字名副其实）

### 6. 每节学完加"找茬/修改"实操题
- **思路**：学完"对齐"原则后，不要只做选择题，直接给一张**错误的PPT截图**，让用户动手：
- **模式A（简单）**：找茬题——"这张PPT有哪些对齐问题？点出来"，点对了给分
- **模式B（进阶）**：修改题——给一张可编辑的简化PPT，让用户拖动元素对齐，提交后系统给"对齐得分"
- 先做模式A（找茬），技术简单，效果直接；模式B后续再做

### 7. 训练结果给评分和解析
- 用户做完实操题后，给一个简单评分："对齐：82分，对比：91分"
- 然后告诉用户哪里没找出来、为什么错、正确改法是什么
- 不要只给"对/错"，要给**改进方向**

## 第三阶段：完整训练闭环（P2，长期方向）

### 8. 做成"学→练→评→改"的能力提升闭环
- 每一节的完整流程：学知识 → 看正反例 → 动手找茬/修改 → AI评分 → 看解析 → 再练一次
- 整个课程结束后，给用户一个"PPT设计能力雷达图"（对齐/对比/重复/亲密性/配色/字体各多少分）
- 让用户看到自己的能力曲线，而不只是"已学3/3节"

## 砍掉的
| 砍掉 | 原因 |
|---|---|
| 顶部橙色导航栏 | 和深蓝课程卡片撞色 |
| 大段纯文字 | 拆成卡片+要点+对比图 |
| "课后小测""正反例"死标签 | 做成可点击入口 |

## 保留的
- ✅ 课程内容本身（讲得扎实，保留）
- ✅ 章节标签切换
- ✅ "已学X/3节"统计（但要加标记已学按钮）
- ✅ 右下角AI助手（但要和课程内容挂钩，加"这节没看懂？问AI"）

## 优先级总结
| 优先级 | 事项 | 效果 |
|---|---|---|
| P0 | 加正反例对比图 | 解决"名不副实"，内容立刻变直观 |
| P0 | 标记已学+阅读进度 | 学习有记录，有成就感 |
| P0 | 小测/正反例做成入口 | 统计数字不再是摆设 |
| P0 | 内容卡片化 | 阅读体验提升 |
| P1 | 加找茬/修改实操题 | 从"看课"变成"训练" |
| P1 | 训练评分+解析 | 知道自己哪里弱 |
| P2 | 能力雷达图+学习闭环 | 完整的训练系统 |

**先做 P0 的 5 件事**，把现有课程体验打磨好；然后做 P1 的实操题，让"训练"二字真正名副其实。P2 是长期方向，不用急。
最核心的一句话：**先把"教你怎么做"做扎实，再往"训练你做好"升级**。
