# 星途学习工作台 · 更新日志（R129 → R130）

- 更新日期：2026-09-19
- 当前版本：**1.30（versionCode 31）** ／ 上一版：1.29（versionCode 30）
- 覆盖范围：Web 端全量 + Android App（星途-1.30.apk）
- 线上地址：http://110.42.134.62

---

## 一、用户可见更新（对应 App 更新提示 7 条）

| # | 更新内容 | 类型 |
|---|---|---|
| 1 | AI「深度思考」支持自动开启，思维链过程随回答实时呈现 | 新增 |
| 2 | 语音输入升级 —— 麦克风图标状态更清晰，识别结果可一键直接发送 | 新增/优化 |
| 3 | 头像编辑支持换边裁剪，操作更顺手 | 优化 |
| 4 | AI 生成的 3D 模型支持会话内预览，生成结果即点即看 | 新增 |
| 5 | 位置选择升级为全新地图选择器，选点更精准 | 新增 |
| 6 | 动态页在全面屏 / 刘海屏设备上的安全区显示修复 | 修复 |
| 7 | 向量与重排请求改为服务端分流处理，模型响应更稳定 | 优化 |

---

## 二、逐项明细（现象 → 根因 → 改动 → 验证）

### 1. AI 思维链看不到（深度思考）
- **现象**：勾选/未勾选深度思考都看不到 AI 的推理过程。
- **根因**：四层——① 服务端中转 `ai.py` 的 `gen()` 只 yield `content`，`reasoning_content` 在服务端即被剥离；② `ai-service.js` 的 `readSSEStream` 对 reasoning 增量只 `markActivity()` 不 emit；③ 页面无思维链容器；④ **深度思考开关默认关闭（`getDeepThink()` 默认 '0'），且手动选推理模型也不会自动开启**。
- **改动**：
  - `server/routers/ai.py`：`reasoning=true` 时 SSE 分帧 `{"t":"r"/"c"/"u"}`，按 base_url 白名单给上游加思考开关；
  - `assets/ai-service.js`：`onReasoning(delta, full)` 回调 + 双格式兼容（d/v）；
  - `assets/ai-page.js`：折叠思考面板 `createReasoningPanel`；请求 opts 传 `reasoning = 深度思考开关开 || isReasoningModelSelected()`（模型 `types` 含 `reasoning` 优先，id/name `/r1|think|reason/i` 兜底，`auto`/未知模型恒 false，非推理模型请求体字节级不变）。
- **验证**：A 线 26/26+4/4、B 线 40/40、独立复核 jsdom 全链路面板断言通过（分帧→onReasoning→面板 DOM 内容非空）。

### 2. 语音识别「点了没发出去」
- **现象**：语音识别完成后文字只被填进输入框，需再点一次发送。
- **根因**：AI 页 `sendAudioForTranscribe`（R86）识别成功后仅回填 `#aiInput` 等二次确认。
- **改动**：识别成功 → 合并文本后**立即 `sendMessage()`** 走既有发送链路（用户气泡+AI 回复）；识别中 `setSendBusy(true)` + 「正在识别语音…完成后将直接发送」提示；失败/空文本兜底为「保留音频可重试 + toast 说明」，不静默丢。
- **验证**：jsdom 19/19（成功路径气泡即发、失败不改动、空文本兜底、R130b/R107 回归区间逐字节一致）。

### 3. 语音条图标（▶ 字符 → SVG）
- **现象**：语音条播放图标为字符 `▶`，不同机型/老 WebView 大小与对齐不一致。
- **改动**：`icon-map.js` 新增 `play-solid` / `pause-solid` 实心圆角 SVG（三角 `stroke-width:2` + `stroke-linejoin:round`；老 outline 键保留避免跨页回归）；`chat-local.js` 语音条改走图标；收发镜像、播放态 4 根错拍声波、暂停态、未读红点、`prefers-reduced-motion` 降级。
- **验证**：jsdom 29/29（含镜像、声波、红点出现/消失、时长宽度公式、批5 位置逻辑回归）。

### 4. 私聊语音识别（IM 侧）与 ASR 模型自动选型
- **改动**：点发送 = 语音消息**立即发出**，转写并行进行、结果挂气泡下方小字，**不回填输入框**；ASR 模型四级选型：`localStorage im_asr_model_id` → `window.__imVoiceAsrModel` → `ai-config` 能力标记（首个 audio 模型）→ 白名单正则兜底 → 全无则静默跳过（绝不阻塞发送）。
- **实测选型结果**：`sf-sensevoice`。
- **验证**：jsdom 11/11 + 真实 ai-config 集成（44 个内置模型扫描）。

### 5. 3D 模型只能下 zip
- **现象**：AI 生成 3D 后会话内只有 zip 下载链接。
- **改动**：
  - 后端新增 `POST /api/ai/model3d/preview`：SSRF 防护（https + volces.com 白名单 + DNS 非内网）→ 流式下载（≤300MB）→ `zipfile` 解包（防 Zip Slip、单文件 ≤200MB）→ 预览图/视频/glb 落盘 `/uploads/model3d/<sha1前16位>/`（`meta.json` 缓存 + 3 天自动清理）→ 返回相对 URL；
  - 前端：上游快照 `<img>` 秒出 → 换服务端持久预览 + `<video>` 内联 + 「🧊 3D 查看（实验）」按钮（glb 懒加载 model-viewer，15s 超时降级），**zip 下载降为次按钮**。
- **验证**：后端自检 15/15、jsdom 17/17。

### 6. 位置选择器（真地图 + 微信化打磨）
- **改动**：Leaflet 1.9.4 本地化（`assets/leaflet/`，腾讯瓦片）；`xt-region.js` 六项打磨——底部按钮区删除、列表首行「📍 发送我的位置」（原生桥优先→一次性定位退化，rich 契约零改动、**绝不 IP 兜底**）、进页自动预选、历史归一键去重 + 两行省略号、省份折叠、黑条半透明胶囊；地图高度 `168px` 写死 → `height:32vh; max-height:420px; min-height:168px`（矮屏 110px），列表区 flex 滚动防挤压。
- **验证**：jsdom 36/36 + Chromium CDP 真链路 15/15（真瓦片 + 线上真实逆地理 + 4 张截图肉眼复核）。

### 7. 动态页底部被切（安全区）
- **根因**：底栏实际高度 = `64px + env(safe-area-inset-bottom)`，但 `.xtm-wrap` 底部留白写死 84/88px。
- **改动**：三处 `.xtm-wrap` padding-bottom → `calc(var(--bottom-nav-height, 64px) + env(safe-area-inset-bottom, 0px) + 24px)`；`common.css` 的 `.main > .page` 与 `.content` 同类问题一并补 env。
- **验证**：jsdom 32/32（`.xtm-wrap`）、10/10（common.css）。

### 8. 用户头像跑到气泡左侧
- **根因**：`addUserBubble` DOM 顺序为「气泡→头像」，配合 `.ai-msg-user{flex-direction:row-reverse}` 渲染成 `[头像][气泡]`。
- **改动**：插入顺序对调（头像先、气泡后），CSS 不动 → 渲染 `[气泡][头像]` 整组靠右。
- **验证**：jsdom 13/13（含 ASR 直发链路回归）。

### 9. 向量 / 重排模型 400（`code 20012 Model does not exist`）
- **根因**：前端中转捷径只排除 imagegen，向量/重排模型被送进硅基流动 **chat/completions** 端点；registry 4 条 sf 条目缺 `type` 字段，服务端无从识别。
- **改动**：`model_registry.json` 补 `type=embedding/rerank`；`ai.py` 按 type 分流到 `/v1/embeddings`、`/v1/rerank`（端点由 provider base_url 派生），响应原样透传 + `X-Ai-Model-Used` + 按上游 usage 记账；普通 chat 与旧请求体逐字节兼容。
- **验证**：真实请求 200（dim=1024 / results 正常）+ 反证 chat 端点 400 + TestClient E2E 全过。

### 10. 缓存与部署侧
- `AI.html` 加 nginx `location = /AI.html { add_header Cache-Control "no-cache"; }`（conf 备份 `/root/nginx.conf.bak-r130`、`-r130b`），根治「改了 HTML 但用户命中旧缓存」。
- 全站资产戳统一前进（只前进不回落）：`ai-page.js`→20260919q、`icon-map.js`→l、`common.css`→m、`xt-moments.css`→n、`xt-region.js`→j、`chat-local.js`→m。

---

## 三、上线记录

| 批次 | 内容 | 结果 | 备份 |
|---|---|---|---|
| R129 | 56 文件 + APK 1.29 | 全 PASS | `/opt/study-workbench/backups/r129-20260919-183500` |
| R130 | 60 文件（46 html + 5 assets + leaflet 7 + 后端 2） | 60/60 md5 归零 | `backups/r130-20260919-215211` |
| R130 收尾 | APK 1.30 发布（含删 1.29、version.json、xt-update.js） | 全 PASS，`/api/app/version`=1.30/31/apkReady=true | `backups/r130-final-20260919-221821` |
| R130b | 增量 48 文件（46 html + common.css + chat-local.js） | 48/48 md5 归零 | `backups/r130b-20260919-222725` |

- APK：`星途-1.30.apk`，87,083,351 B，md5 `cfe3addba01892b50b60730f79decf25`，签名通过，versionCode 31。
- 本地提交（**未 push**）：`2891e91`（R130 收尾入库）、`nd185665`（R130b 增量，仓库==线上）。

---

## 四、已知遗留

1. **`assets/ai-config.js` 内含明文 API key**，且该文件是公开静态资源（任何人可 curl 取到）→ 需尽快改走服务端中转并轮换 key（见交接文档「安全待办」）。
2. `common.css` 中 `.bottom-nav` 的 `var()`/`env()` 未加兜底、两处历史遗留 `min()`——行为可接受，暂不改。
3. `server/.venv` 的 uvicorn 安装损坏（缺 `__init__.py`），与功能无关，建议 force-reinstall。
4. 本地 `server/data/model_usage.json` 残留 3 条 E2E 测试用量（生产账本独立，不影响线上）。
