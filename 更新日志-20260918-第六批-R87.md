# 更新日志 · 2026-09-18 —— 第六批（R86 + R87）AI 模型设置模块

> 配套：`交接文档-20260918h-第六批执行现状与对接.md`（现状与待办）、`给接手AI的开场指令-20260918h.txt`（上手指令）
> 覆盖范围：**R86（当日早间·硅基流动重接 12 模型）** + **R87（本批·AI 模型设置模块 8 条需求）**
> 记录口径：全部为**落盘实测**（size / mtime / 行尾字节 / 符号计数 / node 真值求值 / 真内核解析），不采信成员自述。

---

## §一 需求来源（用户原话拆解）

用户在 R86 之后提出 8 条需求，并要求"不再出现修好了但用户测还是老样子"：

| # | 需求（用户口径） | 验收标准 |
|---|---|---|
| ① | 功能分类页**按真实能力**重做 | 分类不是"场景名"，而是模型真实具备的能力 |
| ② | 新增「**按功能分类排序**」 | 排序弹窗里能按分类排序 |
| ③ | **只把健康检查可用**的模型映射到 AI 页 | AI 页出现的模型必须是检测通过的 |
| ④ | 不可用**自动隐藏** + 恢复**自动回归** | 无人工干预的闭环 |
| ⑤ | 图像/视觉/语音在**浏览器与 APK 都真能跑** | 真发请求、真出结果，不是"有入口没实现" |
| ⑥ | 优化模型说明 | 说明按能力描述，不写场景 |
| ⑦ | 用量统计挪到「关于」页**顶部** + 排序优化 + **前后端真连通** | 数据来自后端真账本，不是前端自算 |
| ⑧ | **检测更新**真能用 | 三态正确 + 能真拿到 APK |

---

## §二 时间线（本批实测 mtime）

| 时刻 | 事件 |
|---|---|
| 14:00:41 | `server/main.py` 最后一次改动 |
| 14:23:51 | `assets/ai-cap-registry.js` 落地（能力注册表） |
| 15:03:50 | `server/routers/ai.py` + `server/schemas.py` 落地（`GET /usage` 就位） |
| 15:18:13 | `server/quota_ledger.py` |
| 15:25–15:29 | `server/data/model_registry.json` / `model_quota.json` / `model_usage.json` |
| 15:33:07 | `server/routers/version.json` |
| 16:27:09 | T01 第一批：`ai-config.js`（47 模型）+ 6 个 cap 文件 |
| 16:30:25 | `assets/xt-update.js`（三态 + reasonCode） |
| 16:30:40 | T01 第二批：`ai-cap-vision.js` / `ai-cap-video.js` / `ai-cap-3d.js` |
| 16:31:14 | `web/static/apk/` 新建（`.gitkeep` + `README.md`） |
| 16:33:55 | T04：`assets/ai-page.js` |
| 16:36:57 | T02：`assets/ai-service.js`（能力分派 + 批量 + skipped） |
| 16:37:33 | T05：`tools/bump_versions_safe.py` 重写 + `tools/qa/check_version_consistency.py` 新建 |
| 16:38:29 | T03：`assets/ai-settings.js`（12 类 / 6 族 / 迁移） |
| 16:39:21 | T03：`ai-settings.html`（用量区移位 + 排序弹窗 + cap 引用） |
| 16:39:30 | T04：`assets/xt-aiusage.js`（真 fetch + 四态降级） |
| 16:45:14 | T05：`tools/r87_stamp_apply.py`（刷戳工装，dry-run 已验证） |
| 16:46:17 | T05：`tools/deploy_update_20260918r87.py`（部署工装，dry-run 三闸全过） |
| 16:46:25 | T05：本地包 `tools/frontend_r87_20260918.tar.gz`（573018 B / 62 成员） |
| 16:50 前后 | 主理人独立复验（ES2017 真内核 `DONE 0`、结构闸、符号计数） |

---

## §三 改动明细

### 3.1 `assets/ai-config.js`（46895 B，16:27:09，CRLF）

**改了什么**
- `builtinModels` **39 → 47**；`modelDetails` **47 键**（与 builtinModels 一一对应）。
- 新增 8 个模型集中在**视频生成 5 个 + 3D 生成 3 个**（`provider: "ark"`，复用已有 ark Key）。
- 3 个即将退役的视频模型打 `tag: "即将下线"`：`ark-seedance-1-5-pro`、`ark-seedance-1-0-lite-t2v`、`ark-seedance-1-0-lite-i2v`。
- `FUNC_TYPES` **8 → 13 个能力槽**：新增 `audio` / `embedding` / `rerank` / `video` / `three_d`。
- `modelDetails` 全量补 `stars`（1–5）/ `speed`；描述改为**按能力**写，去场景字样。

**实测分布（node 求值真值，非正则猜测）**
```
按平台: ark 23 / siliconflow 12 / zhipu 3 / openrouter 3 / gemini 2 / qianfan 2 / arkimage 2
按能力: general 25 / reasoning 5 / video 5 / audio 5 / 3d 3 / image 3 / imagegen 3 /
        embedding 3 / creative 3 / longtext 2 / math 1 / translate 1 / rerank 1
```

### 3.2 `assets/ai-cap-*.js`（8 个，全部 LF）

**改了什么**：每个能力模块新增 `probe(ctx)`，让健康检查**按能力**发对应探针，而不是一律打文本对话。

| 文件 | 大小 | 关键落点 |
|---|---|---|
| `ai-cap-registry.js` | 4343 | 注册表本体（14:23:51 最先落地） |
| `ai-cap-image.js` | 3822 | 生图，无 probe（不做自动检测） |
| `ai-cap-vision.js` | 8510 | `PROBE_PNG`×2 + `textFallback`×2（HTTP-400 时回退文本探针） |
| `ai-cap-audio.js` | 13050 | 语音探针 |
| `ai-cap-embed.js` | 9513 | 嵌入探针 |
| `ai-cap-translate.js` | 11357 | 读 `provider.responsesUrl`（7 处）决定端点 |
| `ai-cap-video.js` | 14861 | `CAP.probeNoAuto = true`（×2）→ **不参与批量自动检测** |
| `ai-cap-3d.js` | 15769 | `CAP.probeNoAuto = true`（×2），探针用 512×512 PNG |

### 3.3 `assets/ai-service.js`（146973 B，16:36:57，LF）

- `aiHealthCheckImpl` 改为**按能力分派**：`cap.probe` 优先，缺失则回落 kind 默认探针。
- 3 个 `XT_KIND_*` 常量被替换；新增 `XT_KIND_BY_TYPE`（3 处）、`model3d`（10 处）。
- 新增 `aiHealthCheckBatch`（7 处）：批量入口；命中 `probeNoAuto` 的模型返回 `{ skipped: true }`。
- 新增错误码 `unsupported_probe`（7 处）：把"该能力没有自动探针"与"真的 400 失败"**分开**。
- `skipped` 标记（6 处）。

**根因记录（这就是本批最核心的一修）**
> 旧 `aiHealthCheckImpl()` 对**所有**非 imagegen 模型都发 `chat/completions` 文本探针。音频 / 嵌入 / 重排这些**没有专用探针分支**的模型，被拿去打硅基流动的 `chat/completions` → **一律 HTTP 400**，于是这些模型在设置页永远显示"不可用"，AI 页也永远看不到它们。
> 修法 = **能力分派 + 能力级探针 + 未实现探针如实报 `unsupported_probe`（不伪造成 400）**。

### 3.4 `assets/ai-settings.js`（131393 B，16:38:29，LF）

**需求 ① —— 分类体系重做：10 类 → 12 类 / 6 族**

| 族（`FAMILY_ORDER`） | 族标签（`FAMILY_LABEL`） | 分类 key → 标签 |
|---|---|---|
| `text` | 文本 | `general` 文本对话 · `longtext` 长文本理解 · `content` 内容创作 · `reasoning` 推理与数学 · `translate` 翻译 |
| `vision` | 视觉 | `vision` 视觉识图 · `imagegen` 图像生成 |
| `audio` | 语音 | `audio` 语音识别 |
| `retrieval` | 检索 | `embedding` 向量嵌入 · `rerank` 结果重排 |
| `video` | 视频 | `video` 视频生成 |
| `three_d` | 3D | `three_d` 3D 生成 |

- `CAT_OF_TYPE` 14 键，含 `'3d' → 'three_d'` 归一。
- `migrateCatSchema`（5 处）**幂等**迁移：以 `catSchema=2` 为闸，把用户 `catSchema=1` 的旧设置无损升到新分类；**重复执行结果一致**。
- `RESERVED_KEYS = []`（本期无预留类，常量名保留以便将来加）。
- ⚠️ 分类 key `vision` **不可改名**（`ai-service.js` 硬编码依赖），本批只把 `label` 改成「视觉识图」。

**需求 ② —— 按功能分类排序**：`setSortCat`(3) / `setSortCatInner`(2) / `lastSort`(9)，排序状态持久化。

**需求 ④ —— 隐藏/恢复闭环**：`recordHealth` 在写库后派发 `xt:health-changed`；遇 `skipped` / `unsupported_probe` **不落库**（关键，见 §四.2 踩坑）。

**需求 ⑥ —— 退役模型语义**：`即将下线`(6) —— 角标 + 排序置末 + 禁止设为默认。

**需求 ④ 补充**：`batchHealthCheck` / `pumpHealth` 都过滤 `isProbeNoAutoId`（双保险）；`usableIds`(7) / `visibleModels`(5)。

### 3.5 `ai-settings.html`（42908 B，16:39:21，LF）

- `#setUsageRoot` 从原位置**移到 `#setPanelAbout` 首子元素**（需求 ⑦ 的"挪到顶部"）。
- 排序弹窗新增「按功能分类」下拉 + 「隐藏不可用模型」开关。
- 新增 `ai-cap-translate.js` / `ai-cap-video.js` / `ai-cap-3d.js` 引用各 1 次。

### 3.6 `assets/ai-page.js`（131014 B，16:33:55，LF）

**需求 ③ ④**：`isHiddenByHealth`(2) / `hideUnavailableOn`(5) / `usableIds` / `visibleModels`；监听 `xt:health-changed`(1) 事件 → 实时重绘。**无健康记录时不隐藏**（兜底放行，避免"新用户什么都看不到"）。

### 3.7 `assets/xt-aiusage.js`（58485 B，16:39:30，LF）

**需求 ⑦**（这是本批从"零 fetch 的空壳"变成真功能的一处）：

- 真 `fetch GET /api/ai/usage`（2 处）+ `AbortController`(3) 超时；本地快照 `serverSnapshot` 做**双口径对照**（不用前端自算覆盖后端真值）。
- **四态降级**：401 未登录 / 404 后端未上线 / 网络异常 / 返回非 JSON —— 四者文案**各自独立**，**不假装成功**。
- `sortRows`(5) 三层排序；`clampPct`(3) 百分比**封顶 100**；`displayNameOf`(3) 展示名解析。
- 展示名优先级：**用户覆盖 > 内置名 > 原始 id**（与 `ai-settings.js:797 displayName()` 对齐）。

### 3.8 `assets/xt-update.js`（35977 B，16:30:25，LF）

**需求 ⑧**：三态文案（发现新版本 / 已是最新 / 当前为最新版）+ 失败态**带原因**（`reasonCode`×2，不吞异常不撒谎）。

### 3.9 根 HTML（37 个被注入 cap 引用）

向 37 个根 HTML 各注入 8 个 `ai-cap-*.js` 引用（部分页面 3 条新引用）→ `ai-cap-registry/image/vision/audio/embed/translate/video/3d` **各被 37 页引用**。
其中 `ai-cap-translate.js` / `ai-cap-video.js` / `ai-cap-3d.js` 是**从 0 → 36/37 页**（新增能力的引用此前完全不存在）。

> 逐页 `+3 行`、成对平衡，未破坏结构；每个文件均先备份 `xxx.html.bak-pre-r87-20260918`。
> ⚠️ 部署时排除 `blog_wechat.html`。

### 3.10 后端（本批确认在位）

`server/routers/ai.py`（14348 B，15:03:50）提供 6 个路由：
```
GET  /models
GET  /usage              ← 需求 ⑦ 的数据源
POST /usage/consume      ← 前端直连能力（生图/视频/3D）上报
POST /usage/reset
GET  /history
POST /chat
```
配套：`server/schemas.py`、`server/quota_ledger.py`（额度账本）、`server/data/model_registry.json` / `model_quota.json` / `model_usage.json`。

### 3.11 工具链（T05）

| 文件 | 说明 |
|---|---|
| `tools/r87_stamp_apply.py` | 本批刷戳（14 资产 → `?v=20260918c`）。**默认 dry-run**，`--apply` 才写盘。六项安全约束（属性锚定 / 无双后缀 / 二进制读写 / 最小变更 / 独立终态复验 / 扫 `\.js\.js\?v=`） |
| `tools/deploy_update_20260918r87.py` | 部署（行尾闸 + 新增符号闸 + 已删符号闸 + tar 打包 + pscp/plink 上传 + MD5 manifest）。**默认 dry-run** |
| `tools/bump_versions_safe.py` | **重写**：四处版本同步（`version.json` / `AndroidManifest.xml` / `xt-update.js CURRENT_VERSION` / APK 文件名）。原版 2026-09-11 已弃用（只处理 3 个资产） |
| `tools/qa/check_version_consistency.py` | **新建**：五值一致性检查，不一致 exit≠0 |
| `web/static/apk/.gitkeep` + `README.md` | **新建**：该目录此前**不存在**，正是"检测更新失败"的直接原因之一 |

---

## §四 踩坑记录（下次派活必须写进任务书）

### 4.1 `rate` 是倍率，`tag` 才是标签

`ai-page.js` 把 `entry.rate` 渲染成 `… 倍率`。本批曾把退役标记写成 `rate: '退役中'` → 界面会显示「退役中 倍率」这种荒谬文案。
**正确**：`tag: '即将下线'`。
**追加坑**：`ai-settings.js` 的模型角标读的是 `m.custom`，**不是** `m.tag` → 光加 `tag` 不会自动出现在模型简介浮层，退役语义必须在设置页单独处理。

### 4.2 批量跳过的"隐性链式故障"（差点废掉需求 ④）

`aiHealthCheckBatch` 跳过 `probeNoAuto` 模型时，如果只是简单返回错误/空结果，会被 `recordHealth` 当成**失败**写进健康库 → 视频/3D 模型**被自动隐藏** → 与需求 ④（可用就显示）**完全相反**。
**修法（两层）**：① `ai-service.js` 返回 `{ skipped: true }`（**语义独立于失败**）；② `ai-settings.js` 的 `recordHealth` 遇 `skipped` / `unsupported_probe` **一律不落库**。

### 4.3 批量入口错位

`batchHealthCheck` 原实现遍历**全部模型**而不是 `pendingHealthIds`。修法：在 `batchHealthCheck` **和** `pendingHealthIds` **两处都**过滤 `isProbeNoAutoId`（双保险，任一路径漏了都还有兜底）。

### 4.4 探针的尺寸/端点陷阱

| 能力 | 坑 | 修法 |
|---|---|---|
| vision | 1×1 PNG 被服务端以"尺寸不合规"拒 → 伪装成 http_400 | 改 **64×64 PNG**；同时 HTTP-400 时走**文本探针回退** |
| 3D | 同上 | 改 **512×512 PNG** |
| translate | 硅基流动**没有** `responsesUrl` | 读 `provider.responsesUrl`，缺失则回落 `chat`；保留 `/responses` 分支（ark 需要） |

### 4.5 命名冲突：`probeCostly` vs `probeNoAuto`

T01 自行发明了 `probeCostly`，与架构设计的 `probeNoAuto` 是同一概念的两个名字 → 若两线各用各的，T03/T05 会全部读不到。
**处理**：统一为 `probeNoAuto`，Grep 复查 `probeCostly` 在 `ai-cap-video.js` / `ai-cap-3d.js` / `ai-service.js` **残留 0**。
**教训**：跨线共用的字段名**必须由设计文档先定死**，实现方不得自创。

### 4.6 行尾矩阵的文档性错误（最容易连环返工的一类）

旧文档（截至 20260917g）声称 LF 白名单只有 4 个文件、`ai-cap-*.js` 是 CRLF —— **全错**。
本批逐字节实测结论：**LF** = `ai-settings.html` + `ai-settings.js` / `ai-page.js` / `ai-service.js` + **8 个 `ai-cap-*.js`** + `xt-aiusage.js` / `xt-update.js`；`ai-config.js` 与其余绝大多数是 **CRLF**。
**已写进设计文档 §9.2 与交接文档 §1.2。**

### 4.7 "行尾回归"误报（判据必须锚定历史备份）

结构闸曾报 `ai-settings.html` 的 `loneLF=808`，看着像"CRLF→LF 回归"。
拿历史备份 `ai-settings.html.bak-pre-r72-20260917` 一比：**它也是 LF**（`CRLF=0 lone=596`）→ 该文件**本来就是 LF**，**不是回归，无需修**。
**教训**：判断"行尾是否被改坏"**必须与备份逐字节比对**，不能凭主观"应该是什么"下结论。同理，部署闸的行尾判定用的是**豁免清单**口径（本批 47 页违规 0），比"一刀切全 CRLF"可靠。

### 4.8 展示名举例给错（被实现方纠正）

主理人在任务书里举例模型名写成 `Doubao-Seedream-4.0`，**真实内置名是 `Seedream-4.0-Fast`**。实现方 T04 对照 `ai-config.js` 真值纠正。
**教训**：任务书里的"举例"必须从配置文件**真值拷贝**，不要凭记忆写。

### 4.9 已废弃脚本的复用

`tools/bump_versions_safe.py` 早在 2026-09-11 被标记弃用（只处理 `api.js` / `polish.css` / `app.js`）。本批**直接重写**为四处版本同步，属**升级而非重复造轮子**（原脚本的功能已被其他链路替代，不存在双写冲突）。

---

## §五 数据契约（跨文件，改动前必读）

```
# ── 能力注册 ─────────────────────────────────────────────
XT_AI_CAPS.register(cap)            定义在 assets/ai-cap-*.js
cap = { kind, defaultUrl, probe?(ctx), probeNoAuto?: true, ... }
ai-service.js 分派顺序：cap.probe 优先 → 否则按 kind 默认探针

# ── 能力 key（★ 不可改名）────────────────────────────────
'image' | 'vision' | 'audio' | 'embed' | 'translate' | 'video' | '3d'
设置页分类 key 归一：CAT_OF_TYPE['3d'] === 'three_d'
★ 'vision' 被 ai-service.js 硬编码依赖 → 只能改 label，不能改 key

# ── 健康检查 ─────────────────────────────────────────────
aiHealthCheckImpl(id)                单模型
aiHealthCheckBatch(ids)              批量；probeNoAuto 的 id → { skipped: true, code: 'unsupported_probe' }
错误码集合：network | cors | timeout | empty | truncated | unsupported_probe
recordHealth 落库规则：skipped / unsupported_probe 一律不落库（否则会把"不检测"误判成"不可用"）

# ── 事件总线 ─────────────────────────────────────────────
ai-settings.js  dispatch  'xt:health-changed'
ai-page.js      listen    'xt:health-changed'  → 重算 isHiddenByHealth → 重绘

# ── 用量 ────────────────────────────────────────────────
GET  /api/ai/usage              服务端账本快照（唯一权威口径）
POST /api/ai/usage/consume      前端直连能力（生图 / 视频 / 3D）上报消费
前端四态降级文案：401 未登录 / 404 后端未上线 / 网络异常 / 坏 JSON
展示名优先级：用户覆盖 > 内置名 > 原始 id
百分比一律 clampPct 封顶 100

# ── 版本号（tools/bump_versions_safe.py 同步四处 + APK 名）─────
version.json（根）
server/routers/version.json
android/AndroidManifest.xml
assets/xt-update.js  CURRENT_VERSION
web/static/apk/星途-<version>.apk
一致性检查：python tools/qa/check_version_consistency.py（不一致 exit≠0）
```

---

## §六 验收证据（真实命令 / 真实输出）

| 检具 | 命令 | 结果 |
|---|---|---|
| ES2017 真内核 | `node tools/qa/escheck_es2017.js …` | **`DONE 0`**（`tools/_r87_escheck.txt`、`_r87_escheck2.txt`，各 7 B） |
| 部署三闸 | `python tools/deploy_update_20260918r87.py`（dry-run） | 闸1 行尾 47 页**违规 0**；闸2 新增符号 9 项**缺失 0**；闸3 已删符号 1 项**残留 0** |
| 打包 | 同上 | **62 成员 / 573018 B** → `tools/frontend_r87_20260918.tar.gz` |
| 刷戳 dry-run | `python tools/r87_stamp_apply.py` | **14 资产全 PASS**；`.js.js?v=` **0**；非属性命中 **0**；行尾 loneLF 违规 **0** |
| 符号残留 | Grep | `probeCostly` **0**（video / 3d / service 三个文件均 0） |
| 符号在位 | Grep | `probeNoAuto`(video 2 / 3d 2) · `aiHealthCheckBatch`(7) · `unsupported_probe`(7) · `skipped`(6) · `xt:health-changed`(各 1) |
| 后端路由 | 解析 `server/routers/ai.py` | 6 个路由含 **`GET /usage`** ✅ |
| 模型真值 | node 求值 `AI_CONFIG` | `builtinModels = 47`、`modelDetails = 47 键` |
| 页面覆盖 | 逐页 Grep | 8 个 `ai-cap-*.js` **各被 37 个根 HTML 引用** |

---

## §七 未完成（权威清单，与交接文档 §5 一致）

| 优先级 | 事项 | 精确命令 / 说明 |
|---|---|---|
| 🔴 P0 | **刷戳 `--apply`**（当前仅 dry-run，磁盘仍旧戳） | `python tools/r87_stamp_apply.py --apply` → 目标 `?v=20260918c` |
| 🔴 P0 | **版本号一致性**（当前 `QA_EXIT=1`） | 先决策：随 APK 发版走 `python tools/bump_versions_safe.py --apply`（统一到 1.24）；或有意保留"服务端 1.24 / 在野 1.23"以演示更新提示 → **必须在文档里写明是有意为之** |
| 🔴 P0 | **APK 就位** | 用户本地打包 → 放 `web/static/apk/星途-1.24.apk`；否则 `/api/app/version` 永远 `apkReady=false` |
| 🔴 P0 | **部署上线**（✅ 用户已授权执行方操作） | `python tools/deploy_update_20260918r87.py --apply` → `root@110.42.134.62:/opt/study-workbench/` |
| 🟡 P1 | **QA 独立回归**（尚未开始） | ⚠️ 必须用 `subagent_type: "general-purpose"`（`software-qa-engineer` 无 Bash，拉起即挂） |
| 🟡 P1 | **git 提交 + push** | 两条红线：先 `git add 我的动态.html`；`git rm --cached 学习工作台-安卓App.apk`；push 走代理 |
| ⚪ P2 | 历史遗留 | `assets/api.js:354-355` 空壳 if；`工具.html` 的 `.morepage-list` 口径；`用户需求与决策总账.md` 补记 R86/R87；用户侧 Key 轮换 |

---

## §八 本批产出文档

| 文档 | 路径 |
|---|---|
| PRD | `deliverables/software-company/ai-models-incremental-PRD-20260918.md` |
| 系统设计（改 5 版） | `deliverables/software-company/ai-models-incremental-design-20260918.md` |
| 交接文档 | `交接文档-20260918h-第六批执行现状与对接.md` |
| 本更新日志 | `更新日志-20260918-第六批-R87.md` |
| 上手指令 | `给接手AI的开场指令-20260918h.txt` |
