# 需求工单：全站 AI 升级（AI 能力 + 页面内容 + 交互功能 一起做实）

> 日期：2026-09-15　｜　执行方：workbuddy
> 配套技术文档：《问 AI 页面开发文档.md》——11 个免费模型、API Key、调用代码、自动降级链都在它的 10.2 节，直接取用，本工单不再重复。
> **这次不是 "接个接口" 就完事，而是三件事一起做：**
> **AI 能力接通**
>
> ：把现在的假回复 / 模板，全部换成真实免费模型输出；
> **页面内容做实**
>
> ：凡是现在 "看着有、其实是写死几句话 / 占位" 的 AI 内容，都要变成模型根据用户真实情况实时生成；
> **页面功能补全**
>
> ：该有的按钮、结果区、操作流程补齐，结果就地展示，不许用一个弹窗敷衍。



***

## 一、先看现状：为什么必须升级（已逐页排查）

现在项目里的 AI 是 "看起来有、实际没接上"：



* `app.js` 里已有服务商框架（`AI_PROVIDERS / fetchAiReply`），但**用户不自己填 Key 就走&#x20;**`demoStyle`—— 那是写死的几句套话，不是真 AI。

* **AI 模拟面试的点评是本地&#x20;**`generateFeedback()`**&#x20;加&#x20;**`setTimeout`**&#x20;假装思考**，换谁回答都是同一套模板。

* 各页面右下角悬浮 "AI 学习助手" 只会通用闲聊，**不知道用户当前在哪个模块、看哪道题**。

* 四级口语 `evaluateSpeaking()` 是本地规则打分；高情商表达的 "情景题 AI 点评" 已被下线；不少 AI 入口点了只回固定话术。

* 没有内置免费 Key，普通用户打开根本用不到真 AI。

**本次目标：内置免费模型开箱即用，让上面每一处都变成真实、有用、结合当前场景的 AI，同时把对应页面的内容和交互做完整。**



***

## 二、必须遵守的开发原则



1. **只建一套 AI 底座，全站复用**：在现有 app.js 的 AI 框架上扩展出统一调用函数（建议 `callAI(funcType, messages, opts)`）。**严禁每个页面各写一套 fetch、各写一份 Key。**

2. **一切可配、不写死**：模型清单、Key、每个功能用哪个模型、系统提示词、temperature、maxTokens 全部集中在一个配置对象里（照《问 AI 页面开发文档.md》的 `ai-config.js`）。以后调模型、改话术只动配置。

3. **开箱即用 + 可覆盖**：内置两个平台的公共免费 Key，用户什么都不填也能用；用户在设置里填了自己的 Key，则优先用用户的。

4. **三层兜底，永不白屏**：首选模型 → 按降级链自动换备用模型 → 全部失败才回退到现有本地意图 / 演示回复（`AI_LOCAL_INTENTS`、`demoStyle` 保留，作为最后一层，不许删）。

5. **只用 11 个永久免费模型**，不接任何收费模型。

6. **结果就地渲染，禁止 alert / 单弹窗敷衍**：讲题就在题目下方出解析，批改就在答题区出批改结果，点评就在对话流里出点评。

7. **统一交互**：调用中显示 "AI 思考中" 并防重复点击；长回答流式逐字输出；失败自动降级并 toast 提示；每条结果带「复制 / 重新生成」。

8. 现有本地能力（如输入 "刷数量关系" 直接跳转模块）保留，能本地处理的不调 AI，省额度、响应快。



***

## 三、功能类型 → 模型分工（写进配置，全站按这张表走）



| funcType  | 覆盖功能             | 首选模型           | 自动降级               | temperature     | maxTokens       |
| --------- | ---------------- | -------------- | ------------------ | --------------- | --------------- |
| general   | 通用问答、学习方法        | GLM-4.7-Flash  | GLM-4.5→Qwen2.5-7B | 0.7             | 1000            |
| reasoning | 数量 / 资料 / 判断推理讲题 | DeepSeek-R1-8B | GLM-4.7-Flash      | 0.1             | 2000            |
| vision    | 拍题、截图识别          | GLM-4.6V-Flash | GLM-4V-Flash       | 0.3             | 1500            |
| translate | 英语翻译、四级作文        | Hunyuan-MT-7B  | GLM-4.7-Flash      | 0.3             | 1200            |
| longtext  | 申论批改、面试报告        | GLM-4.7-Flash  | Qwen3-8B           | 0.4             | 2500            |
| interview | 面试出题 / 点评        | GLM-4.7-Flash  | Qwen3-8B           | 出题 0.8 / 点评 0.5 | 出题 300 / 点评 800 |
| creative  | 金句、话术、发帖文案       | GLM-4.7-Flash  | Qwen2.5-7B         | 0.9             | 800             |

模型 id、接口地址、Key、SSE 流式解析、降级写法全部照《问 AI 页面开发文档.md》10.2，不重复造轮子。



***

## 四、新建独立页面 `AI.html`



* 页面布局、欢迎页 / 对话页、侧边栏（历史对话 / 模型说明 / 设置）、11 模型选择器、模型说明、响应式，**完全按《问 AI 页面开发文档.md》实现**。

* 命名硬性要求：文件名 `AI.html`；页面内只出现「AI」两个字；**不要**"问 AI / 星途 AI / AI 助手" 字样，**不要**机器人头像和产品 Logo。

* 能力：任意学习问答、上传题目截图（vision）、切换 11 个模型、历史对话存 localStorage、自动模式按问题类型选模型。



***

## 五、入口改造（3 处）

### 5.1 左侧主导航新增「AI」

放在「首页」正下方，一级入口，不进任何分组；名称 "AI"，线性 SVG 星光图标（sparkles）；点击站内进入 AI.html，停留时橙色高亮；AI 页保留左侧导航，能随时切回其他模块。

### 5.2 全站底部导航「工具」→「AI」

第 4 个 Tab：`首页 | 互动 | 我的 | 工具 | 更多` 改为 `首页 | 互动 | 我的 | AI | 更多`，只改第 4 个。

标准替换：



```
\<!-- 旧 -->

\<div class="bottom-nav-item" onclick="location.href='工具.html'">\<div class="bn-icon">\<span class="nav-icon" data-icon="wrench" data-icon-size="20">\</span>\</div>\<div class="bn-label">工具\</div>\</div>

\<!-- 新 -->

\<div class="bottom-nav-item" onclick="location.href='AI.html'">\<div class="bn-icon">\<span class="nav-icon" data-icon="sparkles" data-icon-size="20">\</span>\</div>\<div class="bn-label">AI\</div>\</div>
```

特殊页面：



* **学习工作台.html**：bn-icon 直接带 data-icon（无内层 span），同样把 wrench 改 sparkles。

* **设置.html**：该项带 `data-page-node-id`，改跳转 / 图标 / 文字时这些属性原样保留。

* **工具.html**：原项带 `active`，改成 AI 后去掉 active（工具页里 AI 不是当前页，不能高亮）。

需同步修改的 26 个文件：学习工作台、更多、工具、设置、个人中心、私聊、学习博客、blog\_wechat、动态、错题本、行测刷题、申论刷题、央国企笔试、四级备考、四级词汇、高情商表达、场景话术库、商务礼仪、商务礼仪面试、面试题库、PPT 训练、PPT 版式库、PPT 案例拆解、万能金句库、企业定向库、时政热点、管理员（均 .html）。`tools/qa/` 下下划线开头的临时文件不动。

### 5.3 更多页：删「内容直达」分组，保留「工具箱」



* 整块删除 `#mpHeadContent` 标题和 `#mpGroupContent` 容器（时政热点、企业定向库、学习动态、PPT 素材库、PPT 案例拆解 5 张卡），并删掉 `MP_GROUPS` 数组里的 content 行，避免引用空节点报错，删后只剩 common、system。

* **保留**「常用」组里的「工具箱」卡片（`.morepage-list-item` 指向 工具.html）—— 底部工具入口移走后，用户从「更多→常用→工具箱」进工具，批量替换底部导航时别误改它。

* 只删更多页入口，时政热点.html 等 5 个页面文件不删。



***

## 六、各模块 AI 升级清单（内容 + 功能，逐个落地）

> 统一动作：把 "当前题目 / 材料 / 用户输入 / 页面上下文" 一起传给 
>
> `callAI`
>
> ；结果用固定小标题结构就地渲染；带加载、流式、复制、重新生成；失败自动降级。

### 6.1 AI 模拟面试（AI 模拟面试.html、商务礼仪面试.html、面试题库.html）

**现在的问题**：点评是 `generateFeedback` 模板，假延迟，回答好坏一个样。

**内容升级**：



* 出题：interview 类型按岗位 / 题型**真实出题，多套不重样**，不再从固定题池循环。

* 点评：针对用户**这一次的真实回答**，从 "内容切题度、逻辑结构、表达、量化成果、改进建议" 逐项点评并打分，彻底替换模板。

* 追问：AI 能就上一句回答继续追问，形成真实面试感。

* 结束报告：longtext 类型综合全部轮次，输出总评、分项分数、改进清单。

  **页面功能升级**：保留现有 "听题→作答→点评→下一题→报告" 流程；点评卡片改成真实流式输出；每题点评可展开看细节；报告页支持复制和保存到本地。

### 6.2 行测 / 央国企讲题（行测刷题.html、央国企笔试.html）

**内容升级**：



* 每题都有「AI 讲解」，数量关系 / 资料分析 / 判断推理走 reasoning（R1-8B 低温、分步推理），言语 / 常识走 general。

* 解析固定五段结构：**考点 → 分步思路 → 为什么选它 → 其他选项错在哪 → 同类题技巧**，杜绝一句话糊弄。

* 必须把题干、选项、正确答案、用户的选择一起传给模型，做到针对错题讲。

  **页面功能升级**：题目下方设可展开 "AI 讲解" 区；支持「上传题目截图」走 vision 识别后讲解；讲解区带 "换个讲法 / 出同类题" 按钮。

### 6.3 申论批改（申论刷题.html）

**内容升级**：



* 单一题 / 综合题：对照采分点逐条标 "答到 / 漏掉 / 表述不准"，给出改进答案。

* 公文题：加评格式、称谓、行文语气。

* 大作文：从立意、结构、论点、论证、语言 5 个维度评分，逐段提建议，给参考范文片段。

* **材料必须随题传给 AI**（申论答案离不开材料）。

  **页面功能升级**：答题区旁固定 "AI 批改" 按钮；批改结果分 "采分点对照 / 问题 / 参考" 三栏或三段就地展示；支持按修改后答案重新批改。

### 6.4 四级备考 / 词汇（四级备考.html、四级词汇.html）

**内容升级**：



* 听力精听：对当前句子 AI 讲生词、语法、连读、整句翻译。

* 口语跟读：本地发音对比分保留，**新增 AI 基于跟读文本评 "内容、流利度、语法、更地道表达"**（纯文本模型不能听音频，真实发音分仍走本地，别让 AI 假装听声音）。

* 作文 / 翻译：translate 类型批改，给修改版 + 讲解。

* 单词：AI 生成例句、助记、近义词、考点搭配。

  **页面功能升级**：听力页加 "讲解本句"，单词卡加 "AI 例句 / 助记"，作文区加 "AI 批改"，结果就地展示。

### 6.5 PPT 系列（PPT 训练、PPT 版式库、PPT 案例拆解、PPT 素材库）

**内容升级**：



* 用户贴一段自己的文字，AI 转成 PPT 大纲、分页标题与正文、给排版建议。

* 版式库 / 案例库加 "AI 仿写"：按选中版式生成配套文案。

  **页面功能升级**：补 "AI 帮我应用 / AI 仿写" 入口；输出按 "第 1 页 / 第 2 页…" 分页、可一键复制；解决现在 "只有原则文字、不能动手练" 的问题。

### 6.6 发帖 / 广场（学习博客.html、blog\_wechat.html）

**内容升级**：润色、扩写、压缩、拟 3 个标题、自动生成标签、把零散笔记整理成结构化帖子；配合现有快速模板，选模板后 AI 直接起稿。

**页面功能升级**：编辑器旁加一排 AI 操作按钮，结果可直接回填编辑器；删除 "以 base64 存储" 这类给用户看的技术细节文案。

### 6.7 万能金句库（万能金句库.html）

**内容升级**：按主题（奋斗 / 坚持 / 面试自我介绍 / 申论 / 职场）**实时生成新金句**，弥补本地固定句子太少、不能自己加的问题；支持输入关键词定制。

**页面功能升级**：加 "换一批 / AI 生成" 按钮和主题选择；生成结果可 "收藏" 进本地库，可复制。

### 6.8 高情商表达 / 场景话术库 / 商务礼仪

**内容升级**：输入 "场景 + 对方原话"，AI 一次给多版回复（稳妥 / 幽默 / 正式）；把已下线的 "情景题 AI 点评" 用真 AI 重做，点评表达是否得体并给优化说法。

**页面功能升级**：补场景输入框和 "生成回复"；多版本结果并列展示可复制。

### 6.9 错题本（错题本.html）

**内容升级**：选中错题 "AI 分析"—— 讲错因、归纳考点、给避坑要点，并**生成 1\~2 道同类变式题**；支持对整模块错题批量生成 "最该补的 3 个考点" 总结（数学 reasoning，其余 general）。

**页面功能升级**：错题列表加 "AI 分析 / 变式题"，顶部加 "AI 薄弱点总结"。

### 6.10 学习数据 AI 诊断（首页、个人中心、学习统计）

**内容升级**：读取 localStorage 真实的学习时长、做题数、正确率、错题分布，AI 生成个性化诊断（当前水平、薄弱项、下周具体计划），替代现在四个冷数字；全局 AI 问 "我学了多久 / 我的薄弱项" 时，也基于真实数据回答，不再背固定话术。

**页面功能升级**：数据卡上加 "AI 学习诊断" 按钮，报告就地展示并可保存。

### 6.11 全局悬浮 AI 助手（各页 `.ai-fab`）

**内容升级**：默认从本地演示升级为真实模型；**带页面上下文**—— 在申论页就知道在学申论，在某道行测题旁打开就自动带上这道题，不用用户重复描述。

**页面功能升级**：保留意图跳转（输入 "去背单词" 跳四级等）；打开时自动带一句 "当前在：XX 模块 / 这道题"，让用户知道它懂上下文。

### 6.12 拍题 / 图片理解（贯穿所有讲题场景）

所有讲题、批改入口支持上传图片，统一走 vision（GLM-4.6V-Flash），图片转 base64，按《问 AI 页面开发文档.md》多模态格式调用。



***

## 七、统一交互规范（每个 AI 功能都要满足）



1. 调用中按钮显示 "AI 思考中…" 并禁用，结果区出三点 / 骨架加载，防止重复提交。

2. 长文本流式逐字输出，自动滚动到底。

3. 首选失败静默切备用模型并 toast"当前模型繁忙，已自动切换"；全失败回退本地回复并说明 "网络不佳，以下为本地参考"。

4. 结果统一带「复制」「重新生成」；讲题 / 批改类用固定小标题，不写一大坨。

5. 频率超限友好提示；本地意图与本地兜底不受限。

6. 所有结果在当前页内联展开，不允许只弹一个 alert/modal 交差。



***

## 八、分批实施（每批做完真机点一遍，确认是真 AI 再进下一批）



* **P0 底座与入口**：统一 callAI + 内置免费 Key + 降级；新建 AI.html；左侧加 AI、底部工具改 AI、更多页删内容直达。

* **P1 核心学习**：行测讲题、申论批改、AI 模拟面试真点评。

* **P2 内容生产**：金句、话术、PPT 辅助、发帖辅助、四级。

* **P3 分析增强**：错题分析、学习数据诊断、悬浮助手上下文、拍题多模态。



***

## 九、验收清单

**底座与入口**



* [ ] 不填任何 Key，AI.html 和各功能返回的是真实模型内容（不是 demoStyle 那几句固定话）

* [ ] 断网 / 拥堵自动降级或回退本地，无报错白屏；填了自己的 Key 能优先使用

* [ ] 左侧 "首页" 下有 "AI"；26 个页面底部第 4 个 Tab 都是 "AI"+ 星光图标 + 跳 AI.html

* [ ] 工具.html 底部 AI 不高亮；设置.html 的 data-page-node-id 未丢失

* [ ] 更多页只剩常用 / 系统两组，内容直达消失，工具箱仍可进 工具.html，被移出的 5 个页面文件都还在

**内容与功能（逐个确认真实生成、结构完整、就地展示）**



* [ ] 面试：出题不重样、点评针对本次回答、结束有综合报告，不再是 generateFeedback 模板

* [ ] 行测：数学分步推理、五段结构解析、能拍题讲题

* [ ] 申论：按采分点批改、大作文 5 维评分、能带材料

* [ ] 四级：句子讲解、作文翻译批改、口语文本点评、单词讲解

* [ ] PPT：贴文字能出分页大纲与文案

* [ ] 发帖：润色 / 扩写 / 标题 / 标签可用并能回填

* [ ] 金句：按主题实时生成、可收藏

* [ ] 话术：按场景出多版本回复

* [ ] 错题：错因 + 考点 + 变式题

* [ ] 学习数据：基于真实本地数据出诊断

* [ ] 悬浮助手：能感知当前页面 / 题目，意图跳转仍有效

* [ ] 全部结果有加载态、流式、复制、重新生成，无 alert 敷衍



***

## 十、明确不做



* 不做语音识别 / 语音对话（口语沿用现有录音 + 文本评分路径）

* 不删任何现有功能、题库、本地意图和页面文件

* 不内置收费模型，只用 11 个永久免费模型

* 不把 Key 散写到各业务页面，统一放配置

* 不做云端账号同步，对话与配置仍存 localStorage