# 更新日志 · 2026-09-19 · R91 至 R102

> 覆盖第七~十二次迭代（R91-R102），承接 `更新日志-20260918-第六批-R87.md`。
> 工作树：`D:\下载的文件\学习工作台`（唯一可写）。生产：110.42.134.62（Nginx + FastAPI systemd `study-workbench`）。

---

## R91-R93 · 2026-09-18 晚（团队批次）

- **R91-A**：个人中心服务端 AI 记录区块移除（xt-profile.js 143050→141684B），`chatBadge` 改本机口径；`server/routers/ai.py` 新增 `DELETE /api/ai/history`（根治角标 144 清不掉）。
- **R91-B**：私聊弹窗 z-index 遮挡修复（遮罩210/菜单211/loc-sheet215）。
- **R91-C**：个人中心卡死根因=内联 MutationObserver 无条件写 textContent 死循环 → 幂等守卫（值未变不写）。
- **R91-D→E**：模型排序 chips 改 `#UsageQuotaSortSel` select；朋友圈发布页删 `#xtmLocBtn` 死引用。
- **R91-F**：更新页动态注入图标不渲染 → `paintIcons()` 补调。
- **R92**：视频路由接通（`routeCapabilityModel` 前置钩子）；退役 3 个 seedance 模型。
- **R93**：梯子分组（isNeedVPN 推导）；视频内嵌播放器（mp4/webm/mov 识别）；按模型 16px 有声开关（仅 seedance pro 家族）。

## R94 · AI 模型库 + APK#1

- 火山方舟模型库按实测收录、五类分组；服务端统一用量检测（多用户共享额度）；接入硅基流动 12 免费模型；视频/3D 生成。
- 首个正式 APK：`星途-安卓App.apk`（86.8MB，versionCode 24 / versionName 1.23），打包脚本 `android/build_apk.py`（aapt2+javac+d8+merge_apk+zipalign+apksigner，keystore `android/workbench.keystore`）。
- 修复：模型选择不生效、token 统计为 0、生图尺寸/翻译端点。

## R95 · 线上部署遗漏系统排查

- 全量 md5 实测：web 层 45 diff + 10 缺失、server 层 8 diff。**P0**：`ai-cap-*.js`×8 线上不存在 → 5 页面 404；`xt-region.js`/`地区选择.html` 缺失；`version.json`/`update.py` 缺失 → `/api/app/version` 404。
- 五根因：硬编码清单/无差异预检/依赖未闭包/失败不拦门/跨批欠账。
- 产出工装：`_r95_prod_diff.py` / `_r95_gap.py` / `_r95_prod_ref.py`。skill `study-workbench-deploy` 已补 §5.1。

## R96 · 安卓定位修复 + 深度思考

- **定位三根因**：AndroidManifest 缺定位权限（补 COARSE/FINE，刻意不声明 gps required=true）；MainActivity 缺 `setGeolocationEnabled(true)` + `onGeolocationPermissionsShowPrompt` 重写 + 运行时权限申请（REQ_LOCATION_PERM=2004）；xt-region.js 腾讯 Key 空（填 Key、provider 链 tencent→nominatim、`_parseTencentGeo`、nearby()、失败重试）。
- **腾讯配额红线**：`geocoder/v1` 6000/日、`place/v1/*` 仅 200/日 → **周边 POI 一律只用 geocoder 的 result.pois，禁止调 place/v1**。
- 深度思考：开启后强制推理模型链首（`forceReasoning`）；删灯泡图标。
- 全站版本戳统一 20260919a。

## R97-R99 · 头像编辑三阶段（对标微信）

- R97：双指捏合缩放+拖动、3×3 九宫格、顶栏「取消/完成」、底部弹窗（拍照/相册/取消）。
- R98：圆形裁剪→正方形；全站头像容器 50%→12px 圆角。
- R99：`ZMIN=0.4`+`baseScale()`（cover 语义）、区间退化防御、裁剪取样夹紧（治白边白角）。
- jsdom QA 28/31/36 全 PASS；部署 128/128 MD5 归零。

## R100 · 定位后端代理（含生产事故复盘）

- 新建 `server/routers/geo.py`（295 行）：`/api/geo/ip`（IP 定位降级）、`/api/geo/reverse`（街道级+POI≤10，缓存 3 位小数网格 TTL10min 容量500）、`/api/geo/children`（街道列表，永久缓存）。统一 HTTP 200 + `{ok:bool}`；全挂 `rate_limit("geo")`；Key 从 `.env` 的 `TENCENT_MAP_KEY` 读。
- 前端 `xt-region.js` 双路化：优先后端代理，失败降级 JSONP；denied+非安全上下文→自动 IP 降级（不再误报权限被拒）。`地区选择.html` 升 4 级选择器（省/市/区/街道，children 空→手输+逃生行）+「周边位置」≤10 条。
- 双 Key 体系：新 Key `F34BZ-...`（后端专用，授权 IP 锁 110.42.134.62，配额：IP 5000/逆地址 4000/行政区划 5000）；旧 Key `RRTBZ-...`（前端 JSONP 降级，域名白名单）。**腾讯 2024 改版：账户配额需手动按 Key 分配，0 配额 Key 报 121**。
- **⚠️ 生产事故（502 约 4 分钟）**：只推 5 文件 → 远端 routers/ 停在 R73c 缺 update.py → ImportError 崩溃循环。三教训进铁律：①后端部署=server/*.py 全量对齐+import 闭包自检；②探活 sleep≥20+重试循环（uvicorn 启动 ~6.3s）；③备份必须先于 pscp 覆盖。
- 修复提交 `0aec342`。

## R101 · APK 1.25 发版 + 检测更新修复

- version.json 预置 1.24/25（R94 遗留），顶层 notes 补 R96-R100（v2.6，8 条）。
- **关键拦截**：APK#1 内嵌 xt-update.js `CURRENT_VERSION='1.24'`（预写高了）→ 若发 1.24 检测不到更新。**解法：全链路升 1.25/versionCode 26**（三处同步：Manifest + xt-update.js + version.json）。
- **pscp/plink 中文远端路径 GBK 坑**：远端目标路径按 Windows ANSI(GBK) 编码发到 Linux → UTF-8 文件系统存成乱码字节名（pscp 仍报成功）。**解法**：①pscp 传 ASCII 中转名 ②远端 bash ANSI-C quoting mv：`mv xingtu-125-uploading.apk $'\xe6\x98\x9f\xe9\x80\x94-1.25.apk'`。诊断：远端 `ls | cat -A`。
- 提交 `3cd3060`。

## R101b · App 内下载修复 + 后台下载（P0×2）

- **用户真机暴露**：①下载完成手机找不到安装包 ②退出页面下载中断。
- **根因**：xt-update.js XHR blob 下载到 WebView 内存（进度条即此）→ `a.download` 保存触发 DownloadListener → **MainActivity 从未注册 setDownloadListener** → 静默丢弃、从未落盘。
- **修复（提交 `ac54aab`，7 文件 306 行）**：
  - MainActivity.java（56486→65761B 纯CRLF）：`setDownloadListener`（URLUtil.guessFileName 解码中文名）+ `@JavascriptInterface downloadApk(url,fileName)` 挂 AndroidBridge（两条入口共用 `enqueueApkDownload`）+ `ACTION_DOWNLOAD_COMPLETE` 广播（onCreate 注册/onDestroy 注销，Context 级不依赖页面）→ installApk 自动弹安装（N+ 走 content://）。
  - ApkFileProvider.java（新增，**零依赖**极简 ContentProvider，authority=`com.study.workbench.apkfile`）：工程无 androidx/support，系统 API 无 FileProvider 类——自写 openFile/query/getType + 三重路径校验。
  - xt-update.js（+16 行）：startDownload 开头检测 `window.AndroidBridge.downloadApk` → 直转系统下载器（退页/切后台/锁屏不中断，通知栏进度），失败降级原 XHR；escheck DONE 0；戳 20260919a×3 页。
  - AndroidManifest：+provider 声明；版本号 26/1.25 未动。
- 服务器覆盖上线同名 星途-1.25.apk（md5 b45ceb4e...）；解包实证（⚠️ 二进制 XML 字符串池 UTF-16LE，ASCII 匹配会误报缺失）。

## R102 · APK 1.26 发版（versionCode 27）

- 用户手动装 1.25 后，为验证新下载链路升 1.26（1.25=1.25 检测不到更新）。三处同步 + 戳 20260919b×3 页。
- 打包 59s（86,951,674B 同签名）→ 上传 → 1.25 旧包清理 → `/api/app/version` 返回 1.26/27/apkReady=true → 3 HTML + xt-update.js 线上 md5 对齐。

---

## 版本演进

| 版本 | versionCode | 内容 | 状态 |
|---|---|---|---|
| 1.23 | 24 | R86-F 检测更新（APK#1） | 已被 1.25 覆盖 |
| 1.25 | 26 | R96-R100 整合 + 下载修复/后台下载 | 用户已手动安装 |
| **1.26** | **27** | 同 1.25（版本号提升供链路测试） | **当前在野，服务器就位** |

git 提交链（本地 main，push 被拒见交接文档遗留）：01b2548（R97-99）→ 0aec342（R100）→ 3cd3060（R101）→ ac54aab（R101b）→ R102 提交。
