# 每日一题答题卡片改造需求（批次九 · N9-23，2026-09-15 · 豆包方案A·实用版）

> 登记来源：用户转发**豆包**整改建议原文（方案A·实用版），要求「记录一下」。
> 本文 = 豆包方案原文登记 + 主理人只读落点核实/差异注记。**未开工**——用户铁律「不说开工不要自己执行」。
> 改造对象：`商务礼仪面试.html` 的「面试题库·四分类」题卡 `#ivQuizCard`（L101–125）。

---

## 0. 一句话定位

把「面试题库·四分类」题卡改成「**今日一题**」——打开就能答、答完有反馈、进度可累计、不跳页；去掉与下方「模拟面试」功能卡重复的「开始模拟面试」入口。

---

## 1. 改造前 vs 改造后（豆包原文）

- **改造前**：标题「面试题库·四分类」；一行题目 + 三按钮（换一题 / 查看答题要点 / 开始模拟面试）；不能答题、按钮逻辑乱、和下面模拟面试卡重复。
- **改造后**：标题「今日一题 · {分类名}」+ 右上角「已答 X / Y 题」；题目 + 答题输入框 + 三按钮（换一题 / 提交回答 / 查看答题要点）；打开就能答、答完有反馈、不跳页。

---

## 2. 卡片布局（豆包像素级设计，已据实际 DOM 对齐）

实际 DOM 锚点（`商务礼仪面试.html`）：
- 标题行：`#ivQuizCard` 内 `.sprint-head`（L102）— 现文字「面试题库 · 四分类」+ `#ivQuizTabs`（L103，分类 tab 容器）。
- 题目：`#ivQuizQ`（L105）。
- 按钮区：`.qtab-btns`（L106–110）— 现 `换一题`(L107) / `查看答题要点`(L108) / `开始模拟面试`(L109)。
- 要点区：`#ivQuizTip`（L111，现用于 查看答题要点 展开）。
- AI 面板：`#ivAiPanel`（L113–124）— 由「开始模拟面试」按钮 `ivStartInterview()` 触发，**删按钮后此面板成孤儿，需一并隐藏/移除（见第 6 节冲突①）**。

豆包设计（落地时映射到上述锚点）：

| 元素 | 位置 | 样式 | 说明 |
|---|---|---|---|
| 标题 | 卡片左上 | 16px 加粗 + 📝 | `今日一题 · {分类名}`，分类随题变 |
| 进度 | 卡片右上 | 14px 灰 | `已答 X / Y 题`，Y=当前分类总题数 |
| 题目 | 标题下 | 16px 常规，行高1.6 | 完整题面，最多3行超出省略 |
| 答题框 | 题目下 | textarea 高100px 圆角8px 灰边 | placeholder「在这里输入你的回答…」，可拉伸 |
| 换一题 | 框下左 | 白底灰边次按钮 | 同分类随机换题 |
| 提交回答 | 换一题右 | 橙底白字主按钮 | 提交→显示要点+进度+1 |
| 查看要点 | 最右 | 白底灰边次按钮 | 展开/收起参考答案 |

---

## 3. 按钮交互（豆包原文）

### 3.1 🔄 换一题 → `ivQuizNext()`（已有，需补：清空输入框 + 收起要点）
- 同分类抽另一道随机题（不重复当前）；清空输入框；若要点展开则收起；分类名不变；纯前端切换不刷新。

### 3.2 ✅ 提交回答 → **新增 `ivQuizSubmit()`**
1. 空输入 → `showToast('请先输入你的回答')`。
2. 非空 → 输入框下方展开「答题要点」区（带标题「✅ 答题要点」+ 要点列表 + 「📊 你的回答已记录，继续加油！」）。
3. 右上进度 X +1。
4. 按钮文字「提交回答」→「已提交」并置灰不可再点。
5. 答题内容存 `localStorage`：`interview_daily_answers`（题目ID + 用户回答 + 时间戳）。
6. **不做 AI 评分**——仅记录 + 显示要点。

### 3.3 💡 查看答题要点 → `ivQuizShowTip()`（已有，需补展开/收起 toggle）
- 收起→展开显示要点；展开→收起。没答题也能看。展开后按钮变「收起要点」。

---

## 4. 分类切换（顶部 5 标签，现状已具备 `#ivQuizTabs` + `ivQuizCat()`）

- 标签：`全部 / 自我介绍 / 简历深挖 / 压力面 / 礼仪细节`（现状 `['全部'].concat(IV_CATS)`，L660）。
- 当前选中橙色高亮（`.qtab-tab.on`，L662）。
- 切换：换该分类题（或随机）、清空输入框、收起要点、进度更新为该分类「已答 X / Y」。
- 「全部」= 所有分类随机抽（现状 `IV.cat==='全部'` 走全量池，L668）。

---

## 5. 数据结构（豆包原文 + 主理人差异注记❗）

### 5.1 题目（现状 `IV_BANK`，L642–655，共 12 题）
```javascript
// 现状实际结构（非豆包假设的 keyPoints 数组！）
{ cat: '礼仪细节', q: '面试时接递名片/资料的正确做法是什么？',
  tip: '双手递接、字面朝向对方；接过认真看几秒再收好…' }   // 单段字符串
```
- ❗**差异①**：豆包假设 `keyPoints: [..]` 数组；**实际只有单段 `tip` 字符串**。落地方案：直接用 `tip` 渲染要点区即可（无需补 keyPoints）；若强要编号列表，可用正则按 `；`/`。` 拆句（但属可选润色）。
- ❗**差异②**：题库仅 12 题（各分类不均：自我介绍3/简历深挖3/压力面3/礼仪细节3），「已答 X / Y 题」的 Y 取 `IV_BANK.filter(cat).length` 即可。

### 5.2 答题记录（`localStorage`，新增）
```javascript
// key: interview_daily_answers  →  值：数组
[ { questionId: <idx>, category: '礼仪细节', answer: '应该双手递接…', timestamp: 1757932800000 } ]
```
- 进度 X = 该数组内「当前分类」去重题目数（按 questionId 去重，同题答多次算1）。

---

## 6. 实现步骤 + 验收（豆包原文，顺序可独立验证）

1. 改标题/布局：标题→`今日一题 · {分类}`；右上加进度；题下加 textarea；三按钮改「换一题/提交回答/查看答题要点」；**删「开始模拟面试」按钮**。✅验证布局。
2. 换一题：同分类随机不重复 + 清空输入 + 收起要点。✅连点5次不重复。
3. 查看要点：展开/收起 toggle；无要点显示「暂无要点」。
4. 提交回答：空提示 + 展开要点 + 进度+1 + 按钮置灰 + 存 localStorage。✅答后 12/48→13/48，刷新不变。
5. 分类切换：切题+切进度；「全部」随机。
6. 刷新恢复：显示上次题/分类；已答题按钮「已提交」置灰。

**验收 10 条**（略，见豆包原文）：标题正确 / 进度递增 / 可输入 / 换题清空 / 提交显要点+进度+置灰 / 要点展开收起 / 分类切换 / 刷新不丢 / 无开始模拟面试按钮 / 空提交有提示。

---

## 7. 不做的（豆包砍掉项）

- ❌ 不做 AI 评分；❌ 不做答题历史列表页；❌ 不做题目收藏；❌ 不做分享；❌ **不改下方 6 个功能卡**（商务礼仪/面试准备/高频问题库/AI无领导小组讨论/模拟面试/面试后），只改红框题卡。

---

## 8. ❗ 落点核实 + 开工前待确认（主理人只读核实）

**已核实落点（Grep + Read）**：
- 卡片 `#ivQuizCard`：`商务礼仪面试.html` L101–125。
- 题库 `IV_BANK`：L642–655（12 题，结构 `{cat,q,tip}`）；分类 `IV_CATS`：L656；状态 `IV={cat:'全部',idx:0,order:[]}`：L657。
- 函数：`renderTabs`(L659) / `pickQuestion`(L666) / `ivQuizCat`(L679) / `ivQuizNext`(L680) / `ivQuizShowTip`(L681) / `ivStartInterview`(L692) / `ivAiSend`(L704) / `ivEndInterview`(L718) / `boot`(L774 调 renderTabs+pickQuestion)。
- 全部逻辑在 `商务礼仪面试.html` 内联 `<script>`（IIFE，L772 起 `boot()`），**未分离到独立 assets 文件** → 改动直接改本页内联。

**待确认（开工前）**：
1. **冲突①·AI 面板孤儿**：删「开始模拟面试」按钮后，`#ivAiPanel`（L113–124，模拟面试官多轮对话）失去入口。→ 方案：删按钮时**一并 `display:none` 或移除 `#ivAiPanel`**（模拟面试入口由下方功能卡 L149 提供）；或保留按钮另放位置。倾向隐藏面板。
2. **冲突②·标题语义**：豆包要把卡标题从「面试题库 · 四分类」改「今日一题 · {分类}」——此卡本质仍是面试题库，改名后是否影响「面试题库」菜单语义？按豆包方案执行即可，但需确认用户接受。
3. **要点渲染**：直接用现有 `tip` 单段字符串（推荐），还是拆句成编号列表（润色，非必需）。
4. **版本戳**：改 `商务礼仪面试.html` 内联 JS 后，按部署 SOP 打包/补版本戳（本页若有 `app.js?v=` 引用需 bump；内联改动也建议 bump 以破缓存）。

---

*文档结束。豆包方案A 原文已登记，落点经只读核实，两处冲突（AI面板孤儿 / 标题语义）已标注待确认。待用户「开工」后按第 8 节确认项定稿推进。*
