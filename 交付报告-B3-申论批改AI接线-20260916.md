# 交付报告 · B3 · C12 申论批改（接 AI 底座）

- 任务编号：**B3 / C12 申论批改（接 AI 底座）**
- 状态：**DONE**
- 交付人：software-engineer-任务十一
- 日期：2026-09-16
- 独占文件：`D:\下载的文件\学习工作台\申论刷题.html`

---

## 一、现状梳理（改前）

改前对 `申论刷题.html`（82,698 B，mtime 09-16 12:18，CRLF 1,642 行）通读结论：

1. **题目渲染结构**
   - 单题作答页由 `slRenderAnswer()` 渲染（原 L730 起），写入容器 `#slViewAnswer`。
   - 题干/材料容器：`#slMatBody`（`data-slmaterial="1"`），材料标题 `#slMatCol`。
   - 作答要求：`.sl-box`（`作答要求` + `q.requirement`）。
   - 作答输入区：**`<textarea id="slAnswerBox" data-act="answer-input">`**（原 L763）。
   - 字数显示：`#slCountWrap` / `#slCount` / `#slSavedTip`。
   - 交卷按钮：`data-act="a-submit"`（顶部一个 + 作答区下方一个，原 L766「交卷查看答案」）。
   - 复盘页 `slRenderReview()` 写 `#slViewReview`，含 `#slRefBox`（参考答案）、`#slNoteBox`（复盘笔记）。

2. **本地评分/参考要点数据（已复用，未重复造）**
   - 题目对象字段：`q.material / q.materialTitle / q.requirement / q.score / q.wordLimit / q.type / q.subType / q.referenceAnswer / q.keyExpressions / q.solution / q.source / q.questionNo`。
   - 已有 `slKeHtml()` 用 `q.keyExpressions` 高亮参考答案规范表达（金色），`slAddExpr()` 可加入积累本。
   - **本次直接复用 `q.referenceAnswer / q.keyExpressions` 语义，但 AI 批改走独立链路，未改动任何既有评分/高亮逻辑。**

3. **assets 引用清单（改前已有）**
   - `assets/xt-polyfill.js`、`assets/icon-map.js`、`assets/xt-toast.js`、`assets/error-boundary.js`、`assets/app.js`、`assets/data/shenlun-inline.js`、`assets/config.js`、`assets/api.js`、`assets/qbank.js`。
   - **`assets/ai-config.js` / `assets/ai-presets.js` / `assets/ai-service.js` 已在文件末尾（原 L1638~L1640）引用** → 无需补引用，**也未新增任何 `?v=` 版本戳**。

4. **AI 接线现状**
   - 改前 `callAI` 在该页出现 **0 次** → 完全没接。

---

## 二、实现内容（含行号，均以改后文件为准）

> 新增代码块共 4 处，**不删除、不改名任何既有 DOM id / 全局函数**；新增 id 一律 `slAi*` 前缀。

### 1) 新增 CSS（插在 `.sl-saved` 之后，L53~L73 区段内）
```css
/* ===== C12 申论 AI 批改（本页新增；复用 common.css 变量，不引入新库） ===== */
.sl-ai-bar / .sl-ai-hint / .sl-aibox(.on) / .sl-ai-head / .sl-ai-body
/ .sl-ai-err / .sl-ai-thinking / .sl-ai-dot(@keyframes sl-ai-blink) / .sl-ai-meta
```
- 复用 `common.css` 变量（`--card/--border/--primary/--danger/--text-muted`），未引入新色板、未引入新库。

### 2) 作答区新增「AI 批改」按钮 + 结果容器（`slRenderAnswer()` 内，L763~L776 附近）
```html
<div class="sl-ai-bar">
  <button ... data-act="a-submit">交卷查看答案</button>
  <button class="btn btn-secondary btn-sm" type="button" data-act="a-ai" id="slAiBtn">
    <span class="nav-icon" data-icon="sparkles" ...></span> AI 批改</button>
  <span class="sl-ai-hint">写完点「AI 批改」，AI 会按采分点给分并指出漏点（每次点击调用一次）</span>
</div>
<div class="sl-aibox" id="slAiBox"></div>
```
- 渲染末尾追加 `slResetAiBox();`，保证切题/重渲染时清空 AI 结果与按钮态。
- 既有 `#slAnswerBox` / `#slCountWrap` / `data-act="a-submit"` **原样保留**。

### 3) AI 批改逻辑块（插在 `/* ---------------- 交互入口 ---------------- */` 之前，L1400~L1590 区段）
新增全局：`SL_AI`（状态）、`slAiBox`、`slAiBtn`、`slResetAiBox`、`slAiSetRunning`、`slAiPrompt`、`slAiMd`、`slAiRenderHtml`、`slAiFail`、`slAiGo`。
- `slAiPrompt(q, answer)`：组 prompt，含 题型/分值/字数/材料节选(≤1200 字)/作答要求/考生作答 + 固定输出结构（总分 → 采分点对照 → 主要问题 → 提分建议，≤500 字）。
- `slAiMd()/slAiRenderHtml()`：极简 markdown→HTML（老内核安全，不用后行断言）。
- `slAiGo()`：**唯一**触发点，仅由点击进入。校验 → 无 `window.callAI` 走降级 → 置加载态 → `window.callAI('longtext', [{role:'user', content: prompt}], opts)` → `Promise.resolve().then/.catch` 渲染。
  - 流式：仅在 `typeof ReadableStream!=='undefined' && typeof TextDecoder!=='undefined'` 时挂 `opts.onChunk` 逐字渲染，老内核整段返回。
  - 软看门狗 25s 提示"响应较慢"，不取消请求。
  - 降级：`callAI` 不存在 / 抛错 / reject / 返回空 → `slAiFail()` 就地显示「AI 暂时不可用」+ 红框 + 重试按钮 + 本地自查建议，**不白屏**。
  - 限频保护：`SL_AI.running` 期间重复点击直接 return（不发第二次）。

### 4) 事件委托新增 case（`document.addEventListener('click', ...)` switch 内）
```js
case 'a-ai':
  slAiGo(); break;
```

---

## 三、底座口径遵守证据

| 口径要求 | 落实 | 证据 |
|---|---|---|
| 用 `window.callAI(funcType, messages, opts)` 前端直连 | ✅ | `slAiGo()` 内 `window.callAI('longtext', [{role:'user',content:...}], opts)`；**未**调用任何 `/api/ai/chat` |
| `funcType` 与项目一致 | ✅ | 选 `longtext` —— `assets/ai-config.js` L218-224 `longtext.desc === "申论批改、面试报告"`，全站唯一对应"申论批改"档；实测调用日志 `funcType=longtext` |
| **不自动发问**（公共 Key 10 次/分钟） | ✅ | `slAiGo()` 只由 `a-ai` 点击 case 触发；无 `load`/`DOMContentLoaded`/`setTimeout` 自动调用。实测：进入作答页后 `stubCalls=0`；空作答点击 `stubCalls` 仍 `0`；仅在有作答内容且点击时 `stubCalls=1` |
| 不新增 `?v=` 戳 | ✅ | 文件内所有 `<script src=...?v=20260916L>` 保持原样，未新增/改动任何版本戳 |
| 不越界改其它文件 | ✅ | 本批仅改 `申论刷题.html`（mtime 14:33:21）；同窗口其它 mtime 文件（演示.html/PPT版式库.html/个人中心.html…）为**其它 teammate** 产出 |
| 不改 `错题本.html` / 各 assets | ✅ | 仅**只读参考** `错题本.html`（runAI/failOut 写法）与 `ai-config.js` / `ai-service.js`，均未写入 |

---

## 四、实测结果（自建静态服务，独立输出文件）

**验证脚本**：`D:\下载的文件\学习工作台\_b3_qa.js`（基于 `tools/qa/page_check.js` + `_qa_origin.js`，自建静态服务、独立输出 `_b3_out_qa.txt`，避免共享输出文件被其它 teammate 覆盖）
**运行**：`QA_PORT=8913 node _b3_qa.js`

```
############################################################
# 数据源 ORIGIN = http://127.0.0.1:8913
# 来源         = 本脚本自启的本地静态服务（端口 8913）      ← 断言可信
# 是否本地     = 是（验的是本地工作区代码）
############################################################
URL = http://127.0.0.1:8913/申论刷题.html
html bytes = 87427
typeof window.callAI = function                              ← ai-service.js 已生效
静态 HTML 含 “AI 批改” = true
列表 start 按钮数 = 100
slAiBtn 存在 = true | 文本 = "AI 批改"
slAiBox 存在 = true
slAnswerBox 存在 = true
slAiBtn.disabled(初始) = false
空作答点击后 stubCalls = 0（应为 0）                          ← 限频保护：空内容不调用
[加载态] 按钮文本 = "AI 正在批改…" | disabled=true            ← 加载态达标
[加载态] slAiBox 含“AI 正在批改” = true
[加载态] 重复点击是否再次触发 = false（应 false）             ← 防重复调用
  [stub1] funcType=longtext msgs=1 contentLen=1654            ← 底座口径：funcType=longtext
stubCalls 累计 = 2（慢桩1次+即时桩1次=2）
slAiBox 含批改正文 = true
slAiBox 含采分点 = true
slAiBox class = sl-aibox on
点击后按钮文本(完成后) = "AI 批改"
slAiBox 文本摘录 = "AI 批改结果重新批改批改结果：1. 总分：15/20 …… 2. 采分点对照：· 对策具体：写到· 机制建设：漏：3. 主要问题：要点缺失、表述不规范。4. 提分建议：补“长效机制”，分条作答。由 stub-model 生成 · 仅供参考，以官方评分为准"
抛错后 slAiBox 有内容(不白屏) = true
抛错后含“AI 暂时不可用” = true
抛错后 class 含 sl-ai-err = true
抛错后按钮恢复可点 = true
抛错后页面其余功能仍在（slViewAnswer 仍在 DOM）= true        ← 降级不影响其余功能

== 运行时错误（已过滤站内跳转噪声）==
error count = 0                                             ← 无未捕获异常
```

> 说明：`Not implemented: navigation to another Document` 为 jsdom 对站内 `location.href` 跳转的固有噪声（所有页面通用），已在脚本中过滤，非本页缺陷。

**页面体检**：`QA_PORT=8911 node tools/qa/page_check.js "申论刷题.html" "AI 批改,作答要求,AI 正在批改"`
→ `typeof callAI = function`；`AI 批改 / 作答要求 / AI 正在批改` 三项 `运行时DOM=true`。
（注：`page_check.js` 输出固定写 `C:\Users\ATM\_page_check_out.txt`，被并行 teammate 覆盖，故以自建脚本 `_b3_qa.js` 结果为准。）

---

## 五、完工自检逐项结果

| 检查项 | 结果 | 证据 |
|---|---|---|
| `escheck_es2017.js` = `DONE 0` | **PASS** | 输出 `DONE 0` |
| `<!--`/`-->` 配对 | **PASS** | 13 / 13 |
| `<div`/`</div>` 配对 | **PASS** | 276 / 276 |
| `<script`/`</script>` 配对 | **PASS** | 14 / 14 |
| 禁用语法 Grep = 0 | **PASS** | `?.`=0、`??`=0、`.replaceAll(`=0、`Object.fromEntries`=0、`.at(`=0、`(?<=`=0、`(?<!`=0、`**`=0 |
| 原生 `alert(`/`confirm(`/`prompt(` = 0 | **PASS** | 各 0 命中；提示统一走 `slToast()`→`window.xtToast` |
| 换行符未改混 | **PASS** | 改前 CRLF 1642 / loneLF 0；改后 CRLF 1828 / **loneLF 0**（全 CRLF） |
| 未越界改动非独占文件 | **PASS** | 本批仅 `申论刷题.html` mtime=14:33:21；`错题本.html`/assets 均未写 |
| 新增顶层全局不与他文件冲突 | **PASS** | 新增 `SL_AI + 9 个 slAi*`；全站扫描同名声明**碰撞=无**，本文件各名声明次数=1 |
| 保留既有 DOM id / 全局函数名 | **PASS** | `#slAnswerBox/#slCountWrap/#slViewAnswer/#slMatBody/data-act="a-submit"` 等全部保留，只新增 `slAi*` |

---

## 六、未验证项

1. **真实 AI 返回未验**：限频 10 次/分钟，本批**只注入桩函数**验证链路（符合"不自动发问"红线）；未对公共 Key 发真实请求，故真实模型返回的排版/评分质量未验。
2. **APK 老 WebView（Chrome 50~58）实机未验**：仅静态 ES2017 语法校验 + jsdom 运行；真机内核兼容（如 `fetch` 降级链）未实机跑。
3. **流式 `onChunk` 分支未覆盖**：jsdom 环境下 `stubCalls` 未触发 `onChunk`（桩未回调），仅在代码路径上做了保护，逐字渲染未实测。
4. **`prompt` 截断策略**：材料 >1200 字截断，超长材料题（材料很长时）批改信息是否足够，未实测。

---

## 七、遗留问题 / 需主理人裁决

1. **`slAiPrompt` 输出结构为原创设计**（总分/采分点对照/主要问题/提分建议 4 段），与 PRD 若有既定"批改输出格式"要求，需对齐。
2. **结果不持久化**：AI 批改结果目前仅内存渲染（切题即清空）。`错题本.html` 已有 `xt_wrongbook_ai` 本地缓存模式，若需"离线可回看批改"，可加 `shenlun_ai` 缓存键（本批未做，避免越界扩需求）。
3. **`funcType` 选择**：全站 `ai-config.js` 无"申论批改"专属键，选了 `longtext`（其 desc 明确写"申论批改、面试报告"）。若后续新增专属 `funcType`，此处 `'longtext'` 一处即可切换。
4. **未加 `?v=` 版本戳**：新代码为内联，不涉 assets 缓存，符合"波内不 bump"与"不新增戳"要求；若上线需刷新缓存策略，由部署环节统一决定。

---

## 八、改动文件清单

| 文件 | 改前 | 改后 | mtime | 说明 |
|---|---|---|---|---|
| `D:\下载的文件\学习工作台\申论刷题.html` | 82,698 B | **93,355 B** | 2026-09-16 14:33:21 | 新增 AI 批改（4 处） |
| `D:\下载的文件\学习工作台\申论刷题.html.bak-pre-b3-20260916` | — | 82,698 B | — | 改前备份 |

（`_b3_*.py` / `_b3_*.js` / `_b3_out_*.txt` 为本批验证过程文件，非交付物。）

---

## 九、独立复核（software-engineer-任务十一B · 2026-09-16）

> 复核人未参与首次实现，独立重跑全部断言与自检，结论：**全部通过**。

### 9.1 差异确认（用 difflib 逐行比对备份 vs 当前）

| 变更 | 备份行 | 当前行 | 内容 |
|---|---|---|---|
| insert | 53:53 | 53:72 | 新增 C12 AI 批改 CSS（19 行） |
| replace | 765:766 | 784:791 | `'<div><button … a-submit>交卷查看答案</button></div>'` → `.sl-ai-bar`（两按钮 + 提示） |
| insert | 770:770 | 795:796 | `slRenderAnswer()` 末尾新增 `slResetAiBox();` |
| insert | 1373:1373 | 1399:1557 | AI 批改逻辑块（`SL_AI` + `slAi*`，159 行） |
| insert | 1458:1458 | 1642:1644 | 事件委托新增 `case 'a-ai': slAiGo(); break;` |

共 **5 处**（报告正文写 4 处，实际含 `slResetAiBox()` 调用为 5 处插入/替换点），全部为**增量**，无删除既有 DOM id / 全局函数。

### 9.2 独立断言（`tools/qa/_b3_ai_assert.js`，QA_PORT=8911）

- 输出头：`# 来源 = 本脚本自启的本地静态服务（端口 8911）` → 断言可信。
- 结果：**32/32 PASS**。关键摘录：

```
typeof window.callAI = function
PASS  A1 页面已渲染（body 文本 > 3000 字符）   [76247]
PASS  F1 静置 1.5s 内 callAI 自动调用次数 = 0   [calls=0]
PASS  F2 打开题目后 callAI 仍未自动调用   [calls=0]
PASS  E1 空白作答点击后 callAI 未被调用   [calls=0]
PASS  C1 点击后 callAI 被调用 1 次   [calls=1]
PASS  C2 funcType 与项目口径一致(longtext)   [funcType=longtext]
PASS  C3 messages 为 [role,content] 结构   [len=1667]
PASS  C5 加载期按钮禁用(disabled=true)   [disabled=true]
PASS  C6 加载期按钮文案含「AI 正在批改」   [AI 正在批改…]
PASS  C9 结果渲染进 #slAiBox   [AI 批改结果重新批改1. 总分：14/20…]
PASS  C11 结算后按钮恢复可用+文案复原   [disabled=false txt=AI 批改]
PASS  D1 同步抛错 → #slAiBox 有错误区块   [AI 暂时不可用…SYNTHETIC_FAIL_500…]
PASS  D'1 异步 reject → 错误区块出现   [AI 暂时不可用…ASYNC_FAIL_TIMEOUT…]
PASS  D''1 限频提示显示「10 次/分钟」
PASS  X1 无未捕获 JS 异常（排除 jsdom 导航噪声）   []
========== 汇总 32/32 PASS ==========
```

- **复核脚本比首轮更强**：额外覆盖「异步 reject 分支」「`err.rateLimited` 限频提示分支」「静置 1.5s 自动调用计数=0」三项。

### 9.3 自检复核（`_b3_selfcheck.txt`）

| 检查项 | 结果 |
|---|---|
| `escheck_es2017.js` | **DONE 0** |
| 禁用语法（`?.` `??` `.replaceAll(` `Object.fromEntries` `.at(` 顶层 await `(?<=` `(?<!` `**` `catch{`） | 全部 **0** |
| 原生 `alert(`/`confirm(`/`prompt(` | 全部 **0** |
| `<!--`/`-->`、`<div`/`</div>`、`<script`/`</script>`、`<style`/`</style>` | 13/13、276/276、14/14、1/1 全 OK |
| 换行符 | 当前 CRLF=1828 / lone_LF=**0**；BOM 保留 → **未改混** |
| `?v=` 戳 | 16 处全部 `20260916L`，**未新增/未 bump** |
| 新增全局名与其它文件冲突 | `SL_AI + slAiBox/slAiBtn/slAiFail/slAiGo/slAiMd/slAiPrompt/slAiRenderHtml/slAiSetRunning`（9 个），全站扫描**无同名声明冲突** |
| 页面体检 `page_check.js` | `typeof callAI = function`；`AI 批改` 运行时 DOM=true；无 JS 异常 |

### 9.4 复核补充说明

1. `page_check.js` 的 `typeof callAI = function` 已由复核独立复现，确认 `assets/ai-service.js` 在本页生效。
2. `slAiFail()` 内的 `重试` / 结果区的 `重新批改` 均为 `data-act="a-ai"`，走同一事件委托 → 复核确认这两个 span 也能触发 `slAiGo()`（`slResolve` 向上找 `data-act`）。
3. 首轮报告「4 处改动」实为 5 处（漏计 `slResetAiBox()` 调用），已在 9.1 更正；**代码本身无问题**。

### 9.5 复核结论

**IS_PASS: YES** —— 代码与报告一致，全部断言与自检通过，可交 QA/收口。未验证项与首轮六、七节一致（真实 AI 返回、APK 老内核实机、流式 `onChunk` 分支、超长材料截断）。
