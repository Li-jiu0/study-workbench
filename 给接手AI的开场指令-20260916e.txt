# 给接手 AI 的开场指令（2026-09-16 · 第三批续作交接）

> 用法：把下面【版本 A】整段复制，粘给接手的 AI，作为**第一条消息**发出去。
> 前提：那个 AI 能读写本机文件（能打开 `D:\下载的文件\学习工作台`）。
> **本版取代 `给接手AI的开场指令-20260916d.txt`**（那份写的线上是 `20260916k`、且说本批未部署，已过期）。

---

## 【版本 A】完整版（推荐 · 直接整段复制）

```
接手「星途」（原「学习工作台」）项目，继续做第三批的收尾工作。

【第一步 · 必做，不读完不要动代码】
先完整读这份「更新日志」，它记录本批实际落盘 + 两次生产推送 + 踩坑纠错：
D:\下载的文件\学习工作台\更新日志-20260916-第三批续作.md

再完整读这份「总纲」（现状、铁律、未完成项任务书、部署流程、回滚点）：
D:\下载的文件\学习工作台\交接文档-20260916e-第三批续作与对接.md

⚠️ 注意：交接文档-20260916d-*.md 已被上面这份取代，别读旧的（里面说线上是 20260916k、本批未部署，都不对了）。
需要看上批 429 打停时的落盘现场时读：更新日志-20260916-第三批.md

【当前线上状态 · 2026-09-16 17:50 实测】
线上版本戳 = 20260916O（全站 569 处统一、无杂质戳）；39 个 HTML 行尾已修复为干净 CRLF；
四项 UI 需求（企业定向库删标签、金句库/商务礼仪收放、听说训练两级收放）已全部上线；
全站 escheck = DONE 0。

【铁律 · 违反会整页白屏 / 白干，本项目已因此出过三次全站事故】
1. 唯一可写代码树 = D:\下载的文件\学习工作台。
   绝对不要碰 C:\Users\ATM\WorkBuddy\Worktrees\... —— 那是废弃副本，改了等于全部白干。
2. 页面要跑在安卓 APK 的老 WebView 里，内核约 Chrome 50~58，上限 ES2017。以下语法一律禁止：
   ?.   ??   .replaceAll(   Object.fromEntries   .at(   顶层 await
   正则后行断言 (?<= (?<!    对象展开 {...obj}    对象剩余解构    指数 **    可选 catch 绑定 catch {}
   注意：node --check 抓不到这些，必须用 Grep 逐项确认 0 命中。
   已验证的误报，别当 bug 改：** 在引号内是 markdown 字符串；/** 是 JSDoc 注释起始符（最常见）；
   ... 在 [ 之后是合法数组展开。
3. 禁止用 var/const/let 重复声明别的文件已存在的全局名 —— 浏览器会判该文件 SyntaxError 并整文件拒绝执行
   （这是历史上「AI 功能集体失效」的真凶）。新增全局一律写成：
   if (typeof window.xxx !== 'function') window.xxx = function () { ... };
4. 禁原生 alert/confirm/prompt，用页面内 toast（window.xtToast）或页面内区块。
5. 🔴 换行符是红线：根 HTML / app.js / common.css / company-lib.js / voiceplayer.js 是 CRLF；
   ai-page.js、ai-service.js 是 LF。
   改任何文件必须【二进制读写】：
     with open(p,'rb') as f: raw = f.read()
     ...替换...
     with open(p,'wb') as f: f.write(raw2)
   绝不要用 io.open(p,'w',encoding='utf-8',newline='') —— 它会把 \r\n 静默变成 \n（每行少 1 字节），
   本项目已因此把 39 个 HTML 行尾污染并推上生产（已修复，别再犯）。
   改完自测：CRLF=N / loneLF=0。
6. 改任何 assets/*.js 或 CSS 后需要 bump 引用它的 ?v= 版本戳。项目约定「波内不 bump、波末单点统一」；
   且【不要复用已服务过的戳】（浏览器会按 ?v= 缓存，复用会让已缓存客户端不再拉新）。
   补裸路径戳要用单行内正则，严禁跨行正则（跨行会吞掉 --> 和标签 = 整页白屏）。
7. app.js（被 33 个页面引用）、api.js（30 个）、common.css（全站）是共享文件 ——
   同一时刻只允许一条线改，并行写必然后写覆盖先写。
8. 本项目禁 git 写操作（push 需动态代理）。文件重命名用 os.rename，不要用 git mv。
   ⚠️ 判断「哪些文件被动过」只能用 mtime，不要用 git —— 本仓库 HEAD 很旧，
   git status 会列出几十个更早批次改的文件，极具误导性。
9. 🔴 定位「用户截图里的元素」不要猜：先看截图确认位置 → Read 本地 HTML 找可见文字 →
   grep 全 assets/ 找渲染源 → 确认是哪个函数/类名，再动手。
   本项目已踩过：企业定向库有两个来源渲染外观一样的分类标签（CL_renderTabs→#clTabs 与
   CL_renderHero→.cl-hero-tags），我删错了那个，用户回「怎么还在」。

【本机环境坑 · 会让你白浪费大量时间】
- bash 缺 coreutils：ls / grep / head / tail / wc / dirname / env / rm / cat 全部不存在。
  → 一律用 Write 工具落盘 .py / .js 再执行；绝不对 bash 内联含中文/反引号/CSS 花括号的代码
    （bash 会做命令替换与花括号解析）。查文件用 Read/Grep/Glob；命令一律写绝对路径。
- 绝不用 node xxx | tail（exit 127）。
- Windows 中文路径 + python 控制台：print 中文会 GBK 崩溃 → 结果一律写 UTF-8 文件，再用 Read 读。
- plink 命令行会破坏中文文件名 → 远端校验一律用 plink ... python3 - <<'PYEOF' heredoc，
  不要 for f in 中文名.html; do grep ...（会假报 No such file or directory）。
- len(str) 是字符数不是字节数 —— 量字节用 len(open(p,'rb').read())。
- python = C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe
- node   = C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe
- jsdom  = C:/Users/ATM/node_modules/jsdom（需 NODE_PATH="C:/Users/ATM/node_modules"）
  ⚠️ jsdom 无排版引擎：能测计算 display，不能测像素几何 → 视觉类需求必须真机/浏览器截图。
- 跑 QA 页面体检时必须显式指定没被占用的 QA_PORT（8911~8920），逼脚本自启内置 Node 服务；
  否则它会复用你手工起的 python -m http.server，把 assets/*.js?v= 全报成加载失败，
  看着像全站 js 崩了，其实是噪声。输出头必须显示「本脚本自启的本地静态服务」才可信。
- 🔴 探针报 FAIL 时先怀疑探针：本项目我的验证脚本错过 3 次（变量起点行找错、断言写成中文字面量），
  产生过假 FAIL。先自证探针正确，再下结论。

【429 配额 · 影响你马上能不能开工】
本项目曾被 429 打停（配额 2026-09-17 13:32:39 UTC+8 重置）。
- 配额耗尽期间：派子代理时在 spawn 时指定 model: "lite" 可立刻继续
- UI 右上角切模型对子代理无效 —— 子代理的 model 只能在拉起时指定，中途不可切换
- 子代理类型：工程师线用 software-engineer（有 Bash）；QA 线必须用 general-purpose
  （software-qa-engineer 没有 Bash，拉起后 6 秒即挂）

【部署流程 · 照抄 tools/deploy_update_20260916o.py】
该脚本已内置 3 道安全闸（缺一道 = 出错会静默上生产），改 3 处即可复用：
  1) STAMP = '<没用过的新字母>'   2) OUT = 换新文件名   3) 远端校验的 grep 关键词/符号断言
顺序：改脚本 → SW_DRY_RUN=1 预演 → 【用户点头】→ 真跑 → 另写只读脚本独立复核生产。
🔴 生产操作（删用户数据 / 改 nginx / 部署上线）三件必须用户点头。

【先跑总闸确认基线，再决定做什么】
node tools/qa/escheck_es2017.js   → 期望 DONE 0
（其余体检工具见交接文档 §5）

【当前唯一还挂着的用户需求】
「更多功能里的『导入题库』按钮跳转的页面不对是个 bug」—— 用户当时只让记录、不让执行。
⚠️ 旧文档 §4.8 的 D-3「工具页导入题库跑到博客页」极可能是同一问题，别当两条。
开工前先向用户确认正确的目标页是哪个（截图只能看出"不对"，看不出"应该去哪"）。

【建议的接手顺序】
① 读更新日志 §二落盘实况表 → ② 跑总闸 escheck → ③ 向用户确认「导入题库」目标页（成本低价值直接）
→ ④ 收尾 assets/ai-service.js（AI 400 修复，最值钱）→ ⑤ 收尾 AI.html + ai-page.js
→ ⑥ 并行启动上批零产出的 5 项 → ⑦ 复核上批 12 个"落盘但未验证"的文件
→ ⑧ 再进批次九 Wave 3~7

【工作方式 · 用户明确要求，最高优先级】
命令/脚本各跑一次即判定，不要反复回跑、不要多轮回读自审。发现问题直接给结论 + 建议，
不要为了"再确认一下"多跑几轮。这条也适用于调研/核实类工作（外部文档、方案、代码行号只做一次抽验即下判断）。

【最后】
读完文档后，先复述你理解的三条最关键的铁律 + 你打算按什么顺序做，
并指出文档里任何你认为矛盾、含糊或信息不足的地方。先不要写代码。
```

---

## 【版本 B】精简版（只想让它先看懂项目）

```
读 D:\下载的文件\学习工作台\更新日志-20260916-第三批续作.md，
再读 D:\下载的文件\学习工作台\交接文档-20260916e-第三批续作与对接.md，
严格按里面的「铁律」和「未完成工作」章节执行。
（⚠️ 不要读交接文档-20260916d-*.md，它已被取代。）

重点五条：
① 唯一可写代码树是 D:\下载的文件\学习工作台，不要碰 C:\Users\ATM\WorkBuddy\Worktrees\...
② 老 WebView 内核 Chrome 50~58 上限 ES2017，禁 ?. ?? .replaceAll( {...obj} 等语法；
   node --check 抓不到，必须 Grep 逐项确认 0 命中（/** 与引号内 ** 是误报）
③ app.js(33页) / api.js(30页) / common.css(全站) 是共享文件，同一时刻只允许一条线改
④ 🔴 改文件必须二进制读写、行尾 CRLF 不能变（本项目因此出过生产事故）
⑤ 判断"哪些文件被动过"只能用 mtime，不能用 git（本仓库 HEAD 很旧，会误导）

现状：线上是 20260916O，四项 UI 需求已上线。
先读完文档，然后告诉我你打算按什么顺序做。
```

---

## 【版本 C】如果那个 AI 访问不到本地文件

网页版聊天 AI 只能在对话里看内容，**必须把文档内容一起贴过去**。顺序：

1. **先贴** `更新日志-20260916-第三批续作.md` 的全文（最重要，含落盘实况 + 纠错记录）
2. 再贴 `交接文档-20260916e-第三批续作与对接.md` 的全文
3. 按要做的需求挑着贴：`更新日志-20260916-第三批.md`（上批落盘现场）、`架构-批次九-全线改造-波次计划-20260915.md`
4. 然后发这句：

```
以上是星途项目的对接文档。请先通读，然后：
① 复述你理解的三条最关键的铁律；
② 列出文档里标记为「落盘但未验证」「零产出」「仍纯记录未执行」的项，按你建议的执行顺序排一下；
③ 指出文档里任何你认为矛盾、含糊或信息不足的地方。
先不要写代码。
```

**注意**：这种情况下它**不能真正改代码**，只能产出方案或补丁文本 —— 真正落盘还得交给能读写文件的 AI。

---

## 附：优先让它读的文件清单（都在 `D:\下载的文件\学习工作台\`）

| 优先级 | 文件 | 用途 |
|---|---|---|
| ★★★ | `更新日志-20260916-第三批续作.md` | **先读**：本批落盘 + 两次推送 + 纠错记录 |
| ★★★ | `交接文档-20260916e-第三批续作与对接.md` | **总纲**：现状、铁律、未完成项任务书、部署流程、回滚点 |
| ★★ | `更新日志-20260916-第三批.md` | 上批 429 打停时的落盘现场（含未验证清单） |
| ★★ | `计划书-20260916-第三批执行（分工与波次）.md` | 13 线名册 + 接力链 + Wave 0~7 + 文件独占矩阵 |
| ★★ | `架构-批次九-全线改造-波次计划-20260915.md` | 批次九 23 项 7 波依赖分析 + 冲突矩阵 |
| ★ | `任务清单-待办总览-20260915.md` | A/B/C/D 四组待办 + 已拍板的 8 条决策 |
| ★ | `用户需求与决策总账.md` | 全部需求与拍板记录（**已拍板项照办，不要再问**） |
| ⚠️ 已取代 | ~~`交接文档-20260916d-第三批执行现状与对接.md`~~ | 已过期（说线上是 k、本批未部署），仅 §4 未完成清单仍有参考价值 |
