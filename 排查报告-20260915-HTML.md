# 排查报告 — 侧边栏批量删除「申论刷题」后的 HTML 结构 / 死链复核

- 排查人：sw-html（QA）
- 日期：2026-09-15
- 代码树：`D:\下载的文件\学习工作台`（唯一部署源）
- 性质：**只读排查**，未修改除本报告外的任何文件
- 方法：Read / Grep 逐文件核对（未使用 bash ls/grep/test 判断文件是否存在）

---

## 清单 1 · 侧边栏结构完整性（最高优先级）

**结论：通过（0 处结构性破坏；2 处纯排版瑕疵）**

逐个 Read 出 `<nav class="sidebar">` → `</nav>` 区段，逐行核对 div 开闭配对、孤儿 `</div>`、空行断层、相邻菜单项完整性。

| 文件 | nav 区段行号 | nav-item 数 | div 开/闭 | 孤儿 `</div>` | 空行断层 | 结论 |
|---|---|---|---|---|---|---|
| 更多.html | 16–73 | 11 | 29 / 29 | 无 | 无 | 通过 |
| 学习工作台.html | 16–73 | 11 | 29 / 29 | 无 | 无 | 通过（见 P1） |
| 个人中心.html | 43–44 | 11 | 29 / 29 | 无 | 无 | 通过（见 P2） |
| 工具.html | 35–92 | 11 | 29 / 29 | 无 | 无 | 通过 |
| 设置.html | 16–77 | 12（含时政热点） | 30 / 30 | 无 | 无 | 通过（见 P1） |
| 管理员.html | 17–74 | 11 | 29 / 29 | 无 | 无 | 通过 |
| 动态.html | 65–66 | 12（含动态） | 30 / 30 | 无 | 无 | 通过（见 P2） |
| 私聊.html | 76–77 | 11 | 29 / 29 | 无 | 无 | 通过（见 P2） |
| 时政热点.html | 120–180 | 12（含时政热点） | 30 / 30 | 无 | 无 | 通过 |
| 申论刷题.html | 115–172 | 11 | 29 / 29 | 无 | 无 | 通过 |

判定标准全部满足：nav 内 div 开标签数 = 闭标签数；每个 nav-item 均同时具备「跳转标识（data-page 或 onclick）+ icon span + 文字 span + 闭合 `</div>`」四要素；nav-list 以单一 `</div>` 正确收尾，sidebar-footer / user-card 嵌套层级未被截断。

### 问题明细（均为非阻塞的排版瑕疵，不影响渲染与点击）

- **P1 缩进错乱**（疑似正则替换时吞掉了换行前的缩进）
  - `学习工作台.html:56`——`<div class="nav-item" data-page="profile">` 前有 12 个空格，而同级项均为 6 个。
  - `学习工作台.html:59`、`设置.html:63`——`<div class="nav-section">系统</div>` 顶到了行首（0 缩进）。
  - 建议改法：把这三行缩进统一改成 6 个空格，`shownLines` 差异即可消除；纯美化，可下个批次顺手做。
- **P2 侧边栏整段被压成 1–2 行长行**
  - `个人中心.html:43–44`、`动态.html:65–66`、`私聊.html:76–77` 的 `<nav class="sidebar">…</nav>` 全部挤在两行里，其余 7 个文件仍保持逐行展开。
  - 现象推测：正则在保证结构完整的前提下跨行匹配，把中间的 `\n` 一并吃掉了（若当初莫名的偏差会导致裸 `</div>` 残留，实测并未发生）。
  - 影响：可读性与 diff 友好度下降，HTML 解析与点击行为不受影响。
  - 建议改法：这三个文件的 nav 段按 `更多.html` 的版式重新换行展开即可；同样不必紧急处理。

---

## 清单 2 · 死链 · 页面跳转目标是否存在

**结论：通过（0 处死链）**

Grep 全站 `*.html` 的 `location.href='…html'`、`location.href=encodeURI('…html')`、`href="…html"`（含 JS 字符串拼接出的 `<a href="…html?…">`），目标去重后共 20 个，逐个用 Read 确认实体文件存在：

| 跳转目标 | 被引用（举例） | 是否存在 |
|---|---|---|
| 学习工作台.html | AI模拟面试.html:895 | 是 |
| 更多.html | 学习工作台.html:275 | 是 |
| 工具.html | 学习工作台.html:274 | 是 |
| 设置.html | 工具.html:173 | 是 |
| 管理员.html | 更多.html:123 | 是 |
| 个人中心.html | 动态.html:514 | 是 |
| 动态.html | 动态.html:86 | 是 |
| 私聊.html | 好友申请.html:43 | 是 |
| 好友申请.html | （自身页面） | 是 |
| 登录.html | 好友申请.html:130 | 是 |
| 时政热点.html | 设置.html:36 | 是 |
| 申论刷题.html | 央国企笔试.html:120 | 是 |
| 央国企笔试.html | 企业定向库.html:201 | 是 |
| 企业定向库.html | 央国企笔试.html:105 | 是 |
| AI模拟面试.html | 更多.html:187 | 是 |
| PPT版式库.html | 工具.html:127 | 是 |
| 四级经验分享.html | 工具.html:137 | 是 |
| mock_exam.html | mock_exam_run.html:523 | 是 |
| mock_exam_run.html | mock_exam.html:312 | 是 |
| mock_exam_result.html | mock_exam.html:315 | 是 |

补充说明：`备份\` 与 `tools\qa\` 目录下的同名引用不计入本次判定（非线上部署源）；带 query 的跳转（`?cat=`、`?user=`、`?uid=`、`?paper=`）只校验宿主文件存在性，参数由各页自行解析，不在本清单范围。

---

## 清单 3 · 死链 · onclick 函数是否存在

**结论：通过（20/20 抽样全部能追到定义或 window 挂载，0 处死调用）**

抽查覆盖侧边栏、底部导航 bottom-nav-item / bottom-more-item、feature-card、AI 面板四类高频入口：

| # | 调用点 | 函数 | 定义位置 | 结论 |
|---|---|---|---|---|
| 1 | 更多.html:49（侧边栏「好友」） | `gotoChat` | assets/api.js:97 `function gotoChat()` | 通过 |
| 2 | 更多.html:65（侧边栏 user-card） | `openBlogProfile` | assets/app.js:7611 `function openBlogProfile()` | 通过 |
| 3 | 动态.html:78（主题切换） | `toggleTheme` | assets/app.js:2576 `function toggleTheme()` | 通过 |
| 4 | 更多.html:80（全局搜索） | `globalSearch` | assets/app.js:7662 / assets/api.js:826 | 通过 |
| 5 | 个人中心.html:67（子页路由） | `SubpageRouter.navigate` | 对象随 `SubpageRouter` 在本页内联 script 定义 | 通过 |
| 6 | 更多.html:187（工具面板） | `toggleToolsPanel` | assets/app.js:1346 `function toggleToolsPanel()` | 通过 |
| 7 | 更多.html:196（穿越英语） | `openQuest` | assets/quest.js:977 `window.openQuest = function(){}`（**IIFE 内写法，已正确挂 window**，守卫写法 `if(window.openQuest)` 与之一致） | 通过 |
| 8 | 工具.html:143（导入题库） | `openImporter` | assets/importer.js:533 + :543 `window.openImporter = openImporter`（同上，IIFE 已挂载） | 通过 |
| 9 | PPT训练.html:123（feature-card） | `navigateTo` | assets/app.js:1291 `function navigateTo(page)`（顶层，非 IIFE） | 通过 |
| 10 | PPT训练.html:155 | `toggleMorePanel` | assets/app.js:1338 | 通过 |
| 11 | PPT训练.html:161 | `openBlogStats` | assets/app.js:7603 | 通过 |
| 12 | PPT训练.html:164 | `gotoBlogMine` | assets/api.js:105 | 通过 |
| 13 | PPT训练.html:173 | `showAbout` | assets/app.js:8290 + :8316 `window.showAbout = showAbout` | 通过 |
| 14 | PPT训练.html:358 | `clearAiChat` | assets/app.js:3077 | 通过 |
| 15 | PPT训练.html:359 | `toggleAiPanel` | assets/app.js:3049 | 通过 |
| 16 | PPT训练.html:364 | `sendAiMsg` | assets/app.js:3090 / assets/api.js:874 | 通过 |
| 17 | PPT训练.html:264 | `closeCountdownModal` | assets/app.js:2422 | 通过 |
| 18 | PPT训练.html:265 | `saveCountdown` | assets/app.js:2441 | 通过 |
| 19 | mock_exam.html:207（返回） | `goBack` | mock_exam.html:518 内联定义 | 通过 |
| 20 | mock_exam.html:222（切 Tab） | `switchMockTab` | mock_exam.html:371 `window.switchMockTab = function(t){}` | 通过 |

追加核过（一并计入，同样有定义）：`startInterview/sendMessage/restartInterview/goBack`（AI模拟面试.html:644/757/878/895 内联）、`openPptHub/openPptPanel/closePptPanel/pptAddWork`（PPT训练.html:505/482/499/418 均为 `window.xxx =`）、`filterLayouts`（app.js:5240）、`filterQuotes`（app.js:5476）、`editorTool/saveBlogNote/showBlogView`（app.js:7032/7076/6618）、`moOpenUser`（动态.html:514 内联挂载）、`gotoUserMoments`（个人中心.html:831 内联挂载）。

反向核对（页面没加载 app.js 却在调 app.js 里的函数）：全站根目录下**未加载** `assets/app.js` 的页面只有 `AI模拟面试.html`、`PPT素材库.html`、`四级经验分享.html`、`好友申请.html`、`登录.html` 五个；对这五个逐个 Grep 上述 15 个 app.js 函数名，**零命中**——它们只调用自己内联定义的 `goBack` 等，不存在「函数写在 IIFE 但没挂 window」的取值落空。

---

## 清单 4 · 申论入口残留一致性

**结论：通过（0 处残留，与预期完全吻合）**

全站 `*.html` Grep `申论刷题`，命中仅 4 行，全部落在预期的两处：

| 文件:行号 | 内容 | 判定 |
|---|---|---|
| 央国企笔试.html:119 | `<!-- 【2026-09-15 用户指令】申论刷题入口从全站侧边栏移除，并入本页备考卡片 -->` | 合法注释 |
| 央国企笔试.html:120 | `<div class="feature-card" onclick="location.href=encodeURI('申论刷题.html')">` | **合法入口**（唯一跳转） |
| 央国企笔试.html:122 | `<div class="feature-title">申论刷题</div>` | 卡片标题 |
| 申论刷题.html:13 | `/* ===== 申论刷题页专用样式 … ===== */` | 自身注释 |
| 申论刷题.html:356 | `申论刷题.html · 页面内逻辑（全部内联，不新增任何库）` | 自身注释 |

25 个被改动页面的侧边栏均已无申论菜单项（清单 1 抽查的 10 个文件的 nav-item 清单可作旁证）；`备份\`、`tools\qa\` 下也无残留（Grep 无命中）。

---

## 观察项（不在四条清单内，供后续排期参考，非本次改动引入）

1. **侧边栏「时政热点」入口覆盖不全**：全站仅 `设置.html:36` 与 `时政热点.html:148` 两处 nav-item 有它，其余 20+ 页的学习模块分组里没有。按 ADR-3 落地时若希望全站一致，需要补其余页面（可参照「申论刷题」这次的批量做法）。
2. **旧菌落**：根目录并存 `AI模拟面试.html.bak-0913o`、`mock_exam.html.bak-0913o`、`assets/app.js.bak-0913n`、`assets/app.js.backup_20260914` 等备份文件，Grep 时会重复命中，建议清理或统一挪到 `备份\`。

---

## 总体判定

**通过。** 四条清单全部通过，累计 0 处阻塞性问题；另有 3 个文件的侧边栏缩进/换行排版瑕疵（P1/P2，纯外观，不影响结构与点击），以及 2 条与本次改动无关的观察项。批量删除「申论刷题」菜单项没有造成结构破坏，可以放行。
