# 星途学习工作台 · 更新日志（R2A → R2C）

- 更新日期：2026-09-20
- 当前版本：**1.33（versionCode 34）** ／ 上一版：1.32（versionCode 33）
- 覆盖范围：Web 端全量 + Android App（`星途-1.33.apk`）+ GitHub 仓库同步
- 线上地址：http://110.42.134.62 ／ 代码仓库：https://github.com/Li-jiu0/study-workbench
- 前置批次：R129–R130（2026-09-19b 交接）

---

## 一、用户可见更新（对应 App 更新提示 10 条，即 `version.json` 的 v1.33 notes）

| # | 更新内容 | 类型 |
|---|---|---|
| 1 | 图片查看支持双指捏合放大缩小、拖动浏览，并可一键保存到相册 | 新增 |
| 2 | 用户协议与隐私政策独立页面（正式法律文本），登录时可查看 | 新增 |
| 3 | 数据管理页面 —— 存储空间占用、分类清理、备份与检测更新集中在一处 | 新增 |
| 4 | 完善「关于」页面内容（项目定位、核心功能、协议入口、问题反馈与更新入口） | 完善 |
| 5 | 实时位置共享改为双向可见（双方都能看到对方），群聊多人共享同样修复，并修复定位偶发漂移到 (0,0) | 修复 |
| 6 | 3D 模型生成完成后提示「没有返回文件地址」、无法下载模型的问题 | 修复 |
| 7 | App 退到后台或进程被系统关闭后，收到新消息不弹横幅通知的问题 | 修复 |
| 8 | 设置项全面核对 —— 移除 6 个从未生效的开关，学习提醒改为全站生效（需 App 保持打开） | 优化 |
| 9 | AI「流式输出」「上下文记忆」开关真实生效，关闭后按关闭行为执行 | 优化 |
| 10 | 不再可用的 3D 模型 Hyper3D-Gen2（影眸） | 移除 |

---

## 二、逐项明细（现象 → 根因 → 改动 → 验证）

### 1. 图片：捏合缩放 / 拖动 / 保存到相册（需求 1）
- **现象**：私聊/动态/朋友圈里点开图片只能看原图，不能捏合放大；无法保存到本地。
- **改动**：
  - 新建 **`assets/img-viewer.js`**（单例 `window.ImgViewer`）：双指捏合 **0.5×–4×** 且**双指中点锚定**（`t' = m - (m0 - t0)·(s'/s0)`）、捏合期间禁单指拖动（防松手跳变）；双击 1×/2×；单指拖动；多图 `‹ ›` + 横滑翻页（`scale≤1 且 |dx|>50 且 |dx|>|dy|·1.5`）+ `n/N` 计数；加载 spinner + `onerror` 失败态与「时间戳重试」；`⬇` 保存；Android 返回键 `history.pushState({xtViewer:1})` + `popstate` 关闭。
  - **保存到相册**：`window.AndroidBridge.saveImageToGallery(url, filename)`（原生 `MediaStore` 写入 `Pictures/星途`，**免存储权限**）；无桥时降级 `fetch → blob → a[download]`；原生返回 `__UNSUPPORTED__` / `__ERROR__:xx` 时给明确 toast，不静默失败。
  - 接入：`assets/chat-local.js`（`imPreviewImage` 委托，旧 `_IV` 保留为 fallback）、`assets/xt-moments.js`（`XTM.viewer` / `viewerSrc` → `openList`/`open`，`closeViewer` 留兼容空壳）；`私聊.html`、`动态空间.html`、`我的动态.html`、`朋友圈发布.html` 引入脚本。
- **验证**：`node --check` 通过；jsdom 页面校验 `img-viewer` 静态引用在位；线上带戳拉取 `ImgViewer`/`xtSaveImage`/`saveImageToGallery` 三关键字命中。
- **生效范围**：**Web 与 App 都需要本文件**；但手势与「保存到相册」桥只有 App 端完整可用（`file://` 上下文）。

### 2. 用户协议 / 隐私政策重写（需求 2）
- **改动**：新建 **`协议.html`**：`用户服务协议` **九章**（协议接受与修订 / 服务内容与账号 / 用户行为规范 / 用户内容 UGC / AI 生成内容 / 免责与责任限制 / 服务变更与终止 / 未成年人保护 / 法律适用与争议解决）+ `隐私政策` **八章**（收集 / 使用 / 存储 / 安全 / 你的权利 / 未成年人 / 政策变更 / 争议解决与联系），法律文书风格；`?tab=ua|privacy` 双 tab，`?from=login` 显示「同意并继续」并**双写** `xt_privacy_agreed` 与 `study_workbench_agreed`（实读 `登录.html` 真实消费键为前者）后 `history.back()`。
- **验证**：线上 `协议.html` 200，且「用户服务协议 / 隐私政策 / 九、法律适用与争议解决」三处文本命中。

### 3. 「数据与管理」重构为微信存储空间风（需求 3）
- **改动**：新建 **`数据管理.html`**：顶部总用量 + 比例条；分类占用列表（学习记录 / 自建题库 / AI 数据 / 缓存与临时 / 设置与偏好 / 其他数据），点开看明细并可清理；底部维护区（清理缓存 / 重置外观 / 一键备份 / 检测更新）。共 27 个 `dm*` 函数（`dmStatBases`/`dmStudyDetail`/`dmQbankDetail`/`dmAiDetail`/`dmRenderCats`/`dmExportStudy`/`dmExportQbank`/`dmClearAiMemory`/`stClearCache`/`stResetAppearance`/`dmBackup`/`dmOpenUpdate`…）。
- **入口收敛**：`设置.html` 原「数据总览 / 快捷入口 / 维护与重置」**三个 section 删除**（函数一并迁走），瘦身为「存储空间」**单行入口**；`个人中心.html` 新增「数据管理」直达卡。
- **验证**：线上 200；`存储空间`/`dmRenderCats`/`dmBackup` 关键字命中；`设置.html` 内 `数据管理` 命中。

### 4. 「关于」页面完善（需求 4）
- **改动**：`关于.html` 按 **八区块**重写：① 头部（定位语「给上班族的备考搭子 · 学得下去、问得明白」+ 版本徽标）② 项目定位 ③ 核心功能四分区（学习训练 13 chips / AI 能力 7 / 社区互动 7 / 工具与管理 4）④ 协议与条款两行 → `协议.html?tab=ua|privacy` ⑤ 帮助与反馈 → `设置.html` ⑥ 检测更新（`XTUpdate.openUpdatePage()`，缺失回退设置页）⑦ 赞助与联系 ⑧ 免责声明。
- **版本号单一来源**：`assets/xt-update.js` 暴露 `window.XT_VERSION`；`关于.html` 与 `app.js` 的 `showAboutDialog` 三级回退（`XT_VERSION` → `XTUpdate.CURRENT_VERSION` → `'1.33'`），**去掉写死的 v2.3**，消除两处漂移。

### 5. 设置项全面核对（需求 5）
- **新增消费点（原本"写了不读"）**：
  - `aiStream`（流式输出）：`api.js` 的 `sendAiMsg` 现在**真正生效** —— `false` 时不做逐字递增，改为读满整段一次性渲染（同时撤掉光标态）；`true` 保持打字机观感。随请求体下发 `stream` 字段。
  - `aiContext`（上下文记忆）：`app.js` 的 `buildAiContextMessages()` 门控 —— `false` 时只带当轮最新一条（保留用户问题，不吞提问）。
  - `studyRemind`（每日学习提醒）：`applyStudyReminder()` 从 `设置.html` **迁入 `assets/xt-settings.js`**（ES5 重写）并由 `app.js` boot **无条件注册**；原实现只在设置页注册、离开页面定时器即销毁 → 提醒基本永不触发。补文案「需 App 保持打开」如实标注平台限制。
- **删除无效项（全项目无消费点/已被服务端取代）**：`studyLimitWarn`、`reviewRemind`、`cardAutoPlay`、`notesPublic`、`canSearch`、`studyPublic` —— 从 `app.js DEFAULT_SETTINGS` 与 `xt-settings.js DEFAULTS` 两侧删除，设置页 UI 开关同步移除（仅留说明注释）。旧用户 localStorage 残留键无害（`hasOwnProperty` 合并天然忽略），无需迁移脚本。
- **验证**：`escheck_es2017.js` → `DONE 0`；jsdom 页面校验 `数据管理/存储空间/remindTime` 运行时 DOM 命中。

### 6. 实时位置共享改为双向（含群聊）
- **根因**：`server/routers/liveloc.py` 的二级索引是**一对一映射** —— `_LIVE_BY_PEER[(min,max)] = share_id`、`_LIVE_BY_GROUP[gid] = share_id`。A、B 各自 `start` 时**后者的 share_id 直接覆盖前者** → 双方都只看到自己的那个会话，表现为「只能单方向看到对方」。`_end_existing` 又只结本人最新一条，残留会话让覆盖更隐蔽。
- **改动**（`liveloc.py`，本地 387 行 / 线上原 309 行）：
  - 索引改为 **shareId 列表**：`_LIVE_BY_PEER[(a,b)] -> list[str]`、`_LIVE_BY_GROUP[gid] -> list[str]`（append/remove；列表空则整个键 `pop`，防慢泄漏）；
  - `_hydrate()` 只把 **active**（未 ended 且未过期）行写索引，顺带修掉「ended 行覆盖索引」隐患；
  - `_drop_indexes()` 按值 `remove`；
  - `live_start` 追加而非覆盖；`_end_existing()` 改为 `.all()` **清干净本人同一目标上全部 active 旧会话**（原来只结最新一条）；
  - `live_state` 返回该对/该群的**全部** active 会话 `sessions[]`（含 `sharerId / sharerName / lat / lng / hasFix / updatedAt / stale`），并**保留顶层旧字段作兼容层**；`hasFix = lat is not None and lng is not None`（true 才算有定位，杜绝 `Number(null)===0` 造成的 **(0,0) 非洲漂移**)；
  - tick / stop 零改动（契约不变）。
- **验证**：`py_compile` 通过；生产实文件 `sessions=1 / hasFix=2 / sharerId=5 / def _active=1` 全部命中；服务 `active`、`NRestarts=0`。
- **生效范围**：**后端改动，App 用户立即生效**（App 调的就是线上接口）。

### 7. 3D 模型「生成完成但没有返回文件地址」
- **根因**：服务端 `GET /api/ai/3d/task/{id}` 成功时把 zip 地址放在 **`modelUrl`**，而前端 `assets/ai-cap-3d.js` 的 `poll()` 归一化只认 `rj.fileUrl || rj.videoUrl` → 任务 succeeded 仍报「没有返回文件地址」。
- **改动**：取值链插 `rj.modelUrl`；服务端 `ai.py` 的 `_task_view` 内容类型加固（`content` 可能是 dict / list / 其它 → 分别取字典、取首个 dict 元素、或空对象，避免下游按 dict 解引用炸）。
- **补充说明**：本批核查发现**此前几轮的"已修复"全部只落在本地、从未上线**（生产 `img-viewer.js`/`协议.html`/`数据管理.html` MISSING、`liveloc.py` 无 `sessions`、`model_registry.json` 里 `hyper3d-gen2` 仍在）——这正是用户反馈「本地仍可复现 / 修了没好」的成因。

### 8. 删除 Hyper3D-Gen2（影眸）
- **改动**：`server/data/model_registry.json` 与 `model_quota.json` 删 `ark-hyper3d-gen2`；`assets/ai-config.js` 删条目与详情映射；`ai-cap-3d.js` / `ai-service.js` 注释同步。Seed3D / Hitem3D 保留。
- **验证**：生产 `registry`/`quota` 均 `hyper3d-gen2 = False`，registry 键数 106。

### 9. 横幅通知修复（进程终止 / 后台不弹）
- **定位结论**（按嫌疑度）：① **最高** —— Android 13+ `POST_NOTIFICATIONS` 运行时权限被拒且**无引导闭环**：`MsgPollService.notifyMessage` 未授权直接 `return`（照常轮询、就是不弹），`MainActivity` 拒绝分支既无 toast 也不跳系统设置（对比定位权限是有引导的）；用户在首启弹窗点「不允许」后，后台横幅永久静默。② 进程被杀/划掉后台后前台服务恢复不可靠（仅靠 `START_STICKY`，国产 ROM 常常不重启），全项目无 JobScheduler/WorkManager 兜底、电池优化白名单是否开启完全随缘。③ 通知渠道跨版本持久化，`importance` 可能历史性偏低或被用户关渠道且不可自愈。
- **改动**：
  - `MainActivity.java`：新增 `@JavascriptInterface` 桥 `isNotifyGranted()` / `openNotifySettings()`（供前端查询与跳系统设置）；`onRequestPermissionsResult` 的**拒绝分支补 toast + 跳设置引导**；新增 `maybeGuideBatteryOnce()`（首启一次性引导忽略电池优化）；`onDestroy` 补 `appForeground = false`。
  - `MsgPollService.java`：通知渠道 ID `xt_msg` → **`xt_msg_v2`**（换新渠道 = 重置 importance，绕过历史偏低/被关渠道）；新增静态 `sRunning` 与 `scheduleKeepAlive()`。
  - **新建 `PollKeepAliveJobService.java`**（`JobService`，`setPeriodic(15min)` + `setPersisted(true)`）兜底拉活，并在 `AndroidManifest.xml` 注册（`BIND_JOB_SERVICE`，未加新权限）。
  - `PollLogic.java`：屏幕熄灭轮询间隔 `300_000L → 120_000L`。
- **验证**：5 个文件关键字全 PASS；**`javac` 真编译通过**（exit 0，仅 deprecation 告警）。
- **生效范围**：**必须重打 APK**（前端与 Android 侧改动对 App 用户无效直到重装）。

### 10. 发版 v1.33（R2B）
- **版本号四处同步**：`server/routers/version.json`（1.33 / versionCode 34 / `星途-1.33.apk` / notes / changelog 13 条）、`android/AndroidManifest.xml`（1.33 / 34）、`assets/xt-update.js`（`CURRENT_VERSION = '1.33'`）、APK 实体 `web/static/apk/星途-1.33.apk`；一致性工装 `tools/qa/check_version_consistency.py` → 「版本号一致 ✅ exit 0」。
- **`build_apk.py` 修正认知**：该脚本**早已在 2026-09-17 修好** —— 删 `api.js` 引用的正则已移除，改回纯二进制整页复制，并加了**构建期硬断言**「入口页必须含 `assets/api.js` + `assets/config.js`」→ **APK 本身就是联网版**，无需再问"单机/联网"。本批只在 `REQUIRED_ASSETS` 补了 `img-viewer.js` + `xt-update.js`（**防「代码修好但包里没有」**）。
- **打包结果**：51 个 html 入包；联网断言通过；签名证书 SHA-256 `2b95937e…` **未变**（可覆盖安装）；产物 87,157,631 B。
- **资产戳**：只 bump 本批真改过的 `assets/xt-update.js` → `20260922d`（`20260922c` 已服务过生产，按纪律必须换新值）；全站戳统一为 `20260922c`（app/api/xt-settings/chat-local/xt-moments/img-viewer）。
- **上线校验**：`/api/app/version` 公网 → `status ok / version 1.33 / versionCode 34 / apkReady true / apkUrl http://110.42.134.62/static/apk/星途-1.33.apk`；APK 公网 `HEAD 200`（87,157,631 B）；`changelog` 序列 v1.33 → v1.32 → v1.31 → v2.9…（跳跃升级用户能看到中间各版）。

### 11. 结构破损修复（本批实测发现）
- **现象**：**16 个页面**在首个 `</html>` 之后被追加了一段「自身尾部片段」（起始处常落在半行中间，如 `/script>`、`pt>`、`nImporterView();`），表现为 `</body>`/`</html>` 各出现 **2 次**、`<script>` 与 `</script>` 计数不等 → 白屏级结构破损。
- **涉及页面**：万能金句库 / 个人中心 / 动态空间 / 学习工作台 / 导入题库 / 我的文件 / 时政热点 / 更多 / 申论刷题 / 英语 / 行测 / 行测刷题 / 表达 / 赞助 / 面测 / 面试题库。
- **修法**：备份到 `_repair_bak/` 后**截断到首个 `</html>` 结尾**（补一个换行）；再用「归一化 `?v=` 后 diff」复核只剩本批预期改动。

---

## 三、上线记录

| 批次 | 内容 | 结果 | 备份 |
|---|---|---|---|
| R2A-后端 | 6 文件（`routers/ai.py`、`routers/geo.py`、`routers/liveloc.py`、`ws.py`、`data/model_registry.json`、`data/model_quota.json`） | 6/6 md5 PASS | `backups/r2a-20260920-144905` |
| R2A-前端 | 99 文件（根 HTML + `assets/*`，tar 打包，UTF-8 中文名直传） | 99/99 md5 PASS | `backups/r2a-20260920-145013`（88 个被覆盖文件 + `data.db`） |
| R2B-发版 | 4 页 + `version.json` + `xt-update.js` + `星途-1.33.apk` | 7/7 md5 PASS，`/api/app/version` 在线核验通过 | `backups/r2b-20260920-150729` |
| R2C-GitHub | 净化副本强推 `main` | 远端 `be18396e`，168 提交 / 3717 文件，**通过 secret scanning** | 临时副本已清理 |

- **差异归零门禁**：`本地 md5 vs 生产 md5` 从起始 **98 项 DIFF 降到 4 项**，且 4 项均为有意豁免 —— `server/.env.example`（模板，避免覆盖线上独有键说明）、`server/建表SQL.sql`（SQL 文档）、`web/.codebuddy/settings.local.json`（编辑器本地配置）、`web/_qa_harness.html`（QA 临时页）。
- **健康检查**：站点/`/api/health`/`/api/app/version`/两个新页/`img-viewer.js`/`live-location.html` 全 **200**；`systemctl is-active = active`、`NRestarts=0`；近 10 分钟 `traceback|500` 计数 **0**；`escheck_es2017.js` → `DONE 0`。
- **数据无损**：仅备份 `data.db`（未改），用户表行数未变。
- **本地提交（未 push）**：`410a292`（R2A 全量）、`9118132`（R2A 工装）、`ed547f6`（R2B 发版）、`8a2a749`（密钥清理）。

---

## 四、GitHub 同步（R2C）

- **范围**：远端 `main`：`e34fb3c` → **`be18396e`**（168 提交 / 3717 文件），覆盖 R97 起至今全部本地提交。
- **代理定性（更正既往结论）**：环境变量代理（当次 49690）对 GitHub 已失效（`SSL_read: unexpected eof` / `RPC failed; curl 56 Recv failure: Connection was reset`）；**Clash `127.0.0.1:7897` 稳定可用**（288.6MB / 8816 对象推送 224s 成功）。→ 推 GitHub 走 `7897`。
- **被 push protection 拦 3 次**，逐次收敛：
  1. 载荷过大 + 代理不稳 → 换 Clash，并先量化载荷（288.6MB，首同步固有量）；
  2. 拦下 **火山引擎 Access Key**（`tools/_probe73q_raw.json` 等，仅本地未推送）→ 建隔离副本 `filter_repo --replace-text` 清历史；
  3. 拦下 **GCP/Gemini Key**（**53 字符、无 `AIza` 前缀**，任何前缀正则都抓不到）→ 改为**从被 GitHub 点名文件里在脚本内部提取 literal** 补规则，通过。
- **净化副本（工装 `_r2c_loop.py`）实际剔除**：`*.apk`（6 个发版归档，约 497MB）、`tools/jx_patch/_work/`（约 90MB）、`tools/frontend_*.tar.gz`（约 29MB）、**`server/.env`（整文件从历史删除）**、`_lead_run*.log`。
- **有意差异**：GitHub 版本**不含**上述大件与 `server/.env`；被 GitHub 点名的 6 个文件**保留在仓库**但密钥已替换为 `***REMOVED-BY-R2C***`（复验残留 = 0）。**本地真源完整保留全部内容**。
- **纪律**：**被拦时绝不点 GitHub 给的 `unblock-secret` 链接放行** —— 那等于公开密钥；唯一正确动作是把密钥从历史清掉再推。

---

## 五、已知遗留（按优先级）

1. **🔴 `server/.env` 的密钥早已随 GitHub 历史公开**：该文件由**初始提交**加入、commit `848d642` 移除，两个提交都已是远端祖先 → 其中**全部模型供应商 Key + `JWT_SECRET` + `ADMIN_*`** 应视为已泄漏。强推**删不掉旧对象**，**唯一补救是轮换**（`JWT_SECRET` 一换现有登录态全部失效，需择时）。
2. **🔴 `assets/ai-config.js` 的 `gemini.apiKey` 是真实 Gemini Key 且线上公开**（`keyInQuery: true`）：源自 v1.31「Gemini 前端直连、已内置 Key」的刻意设计，与 R131「前端零密钥、内置平台走服务端中转」主线冲突。→ **R131 并未覆盖 `needVPN` 的海外平台分支**，接手时不要默认前端已无明文 key。处置待用户拍板：轮换 + 改后端中转（需部署 + 重打 APK），或保持前端直连。
3. **🟡 远端 `refs/tags/1.0`** 仍指向旧初始提交 → 旧对象（含 `server/.env`）持续可达；是否删除待拍板。
4. **🟡 本地历史仍含明文密钥** → 今后每次 push 都须继续走 `_r2c_loop.py` 净化流程（或后续把本地历史也治理一遍）。
5. **🟢 小项**：`common.css` 的 `.bottom-nav` 变量/`env()` 未加兜底 + 两处历史 `min()`（行为可接受）；`server/.venv` 的 uvicorn 安装损坏建议 force-reinstall；真源工作区约 500+ 项未跟踪临时物（建议单独一批出清单后归档，**不要顺手删**）。
6. **📌 未验证项**：横幅通知修复是代码级 + `javac` 编译级验证，**建议在真机上实测三态**（前台 / 后台 / 进程被划掉）× 权限矩阵（已授权 / 拒一次 / 拒且不再询问）并截图存档；图片捏合与「保存到相册」同样建议真机复核。
