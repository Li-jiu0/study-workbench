# 架构 · 批次九全线改造波次计划（2026-09-15）

> 本文只做**依赖分析 + 波次拆解**，不含任何代码改动。
> 唯一可编辑代码树 = `D:\下载的文件\学习工作台`。所有 worktree（`main-*`）**禁止写入**。
> 输入源：`交接文档-批次九-20260915.md` 第 10 节（N9-1 ~ N9-24）；实际待办 **23 项**（N9-20 已作废存档）。

---

## 0. 三条决定排期的硬事实（本轮 Grep 实测，优先于交接文档）

| # | 事实 | 数据 | 对排期的直接影响 |
|---|---|---|---|
| F1 | `assets/app.js` 被 **31 个根 HTML** 引用 | 实测引用页 31 个 | 动 app.js = 动全站 |
| F2 | **`assets/api.js` 也是共享文件**（交接文档低估了它） | 实测 **30 个根 HTML** 引用 `api.js?v=20260915c` | N9-15 / N9-21 / N9-22 若经 api.js 收口，同样要 bump 30 页 |
| F3 | **`renderBlogStats` / `renderBlogMine` / `openBlogStats` 在 app.js 与 api.js 里各有一份** | `app.js:6814/7231/7622` vs `api.js:450/761`；`更多.html` 加载序为 `app.js`(L268) → `api.js`(L271)，**api.js 后加载会覆盖 app.js 同名函数** | ⚠️ 交接文档 10.16/10.9 只写了 api.js 落点。**改错文件 = 白改 + 被覆盖**。开工前必须先判明「哪个是活的」 |

> **F3 是本次分析新发现的坑，优先级最高**：N9-15 / N9-22 开工第一步必须先确认生效副本在哪个文件，再动刀。

### 0.1 隐藏的全局互斥锁：版本戳 bump 本身会重写 31 个 HTML

这是最容易被忽略的冲突源。任何「动共享 JS/CSS」的任务，其收尾动作 `bump_versions_safe.py` 会**重写 31 个根 HTML**。若每个任务各自 bump，会与同波任何改 HTML 的任务互踩。

**全局约定（强制）**：
1. 波内任务**只改自己的文件，一律不 bump**；
2. 每波结束后由**波末统一 bump 单点执行**（一次扫全站，含修复 `blog_wechat.html:325` 的畸形戳 `20260915eaaa`）；
3. 改共享 JS 的任务在交付说明里**只声明「需 bump X」，不自己动手**。

---

## 1. 23 项 × 文件归属矩阵

> 「共享」列：★=动 `app.js`(31页) / ▲=动 `api.js`(30页) / ●=动 `common.css`(全站) / ■=动 30+ 个 HTML

| ID | 需求 | 实际改动文件（Grep 核过） | 共享标记 | 风险 | 备注 |
|---|---|---|---|---|---|
| N9-1 | 修常识判断筛选 | `assets/app.js`(filterExamType L5776 / INLINE_CHANGSHI_BANK L494)、`行测刷题.html` | ★ | 中 | 与 N9-2 同文件，必须同 agent 合并 |
| N9-2 | 判断推理 type 匹配 | `assets/app.js`(INLINE_JUDGE_BANK L446)、`行测刷题.html` | ★ | 中 | 同上 |
| N9-3 | 全局消息通知 | 方案定：**新建 `assets/notify.js`** + 30+ HTML 注入 `<script>`（若塞进 app.js 则变 ★ 且与 N9-1/2/24/13 全串行） | ■ | 中 | 与 N9-9 同为「全站 HTML 批量」→ 合并执行 |
| N9-4 | 个人中心作品集 | `个人中心.html`（**限定页内实现**，不碰 app.js/api.js） | — | 中 | 越界碰 `renderProfileOverview` 就会和 N9-22 抢 `个人中心.html`+app.js |
| N9-5 | 工具页填充或隐藏 | `工具.html`（**只填充，不选隐藏**——隐藏要改 30+ 页侧边栏） | — | 低 | 选「隐藏」则升 ■，必须移入 W6 |
| N9-6 | 更多页内容填充 | `更多.html`（**明令避开 L102-106 学习统计卡，留给 N9-22**） | — | 低 | 与 N9-22 串行（非并行） |
| N9-7 | app.js 拆分优化 | `assets/app.js` + 31 HTML + 新建 `assets/app-*.js` | ★ ■ | **极高** | 全站最大改造，**必须最后** |
| N9-8 | 筛选标签交互优化 | `行测刷题.html` + 其它带标签筛选页 | — | 中 | 与 N9-1/2 抢 `行测刷题.html` → 不同波 |
| N9-9 | 全站页头返回补齐 | `assets/page-head.css` + **33 个 HTML** | ■ | 中 | 与 N9-22(28页) / N9-3(30+页) 高度重叠 → **必须合并为一次原子批量** |
| N9-10 | 场景标签条→分类列表 | `assets/voiceplayer.js` | — | 中 | **已被 N9-18 吸收**，随 N9-18 一并交付，不单独派活 |
| N9-11 | 阅读理解布局 bug | `assets/cet-read.js`（单文件，三种题型共用 `.cr-grid`） | — | 中 | 完全独立，**W1 标杆候选** |
| N9-12 | PPT版式库升级 | `PPT版式库.html` + `assets/data-ppt-templates.js` + `assets/tpl-preview.js` + 新增 `assets/images/ppt-tpl/*` | — | 高 | 🔴 **资源阻塞**：15 张预览图 + .pptx 均不存在 |
| N9-13 | PPT案例拆解改进 | **`assets/app.js`**(PPT_CASES L4217 / 渲染 L5327) + 新增 `assets/images/ppt-cases/*` | ★ | 高 | 🔴 **资源阻塞**（真实截图）；又是 app.js 独占项 |
| N9-14 | PPT训练课程改进 | `PPT训练.html` + `assets/data-ppt-tips.js` + `assets/ppt-tips.js` + 新增 `assets/images/ppt-tips/*` | — | 高 | 🟡 部分阻塞：正反例对比图可用占位图先上 |
| N9-15 | 我的文章布局 bug | `assets/common.css`(L1383 `.blog-grid` / L1384 `.note-card`) + `blog_wechat.html` | ● | 中 | ⚠️ 受 F3 影响：先判 `renderBlogMine` 生效副本 |
| N9-16 | 联系管理员只在好友 tab | `私聊.html`（页内 `patchSwitchTab` 加一行显隐，**不动任何 assets**） | — | 低 | 完全自包含，**W1 标杆候选** |
| N9-17 | 长按弹菜单撤回 | `assets/chat-local.js` + `私聊.html`(新增 `.im-msg-menu` CSS) | — | 中 | 与 N9-16 抢 `私聊.html` → 不同波 |
| N9-18 | 听力精听全面改版 | `assets/voiceplayer.js` + 听说训练相关页（**含 N9-10**） | — | 高 | 不可改 `vp-back` 语义（N9-9 基准）与 `#vpMask` id |
| N9-19 | 模拟面试页面重构 | `AI模拟面试.html` + `assets/iv-prep.js` | — | 高 | 🟡 设计稿已在手（PDF+画布），需先出状态机交互文档 |
| N9-21 | 写发帖页面改进 | `学习博客.html` + `blog_wechat.html` + 可能 `assets/api.js`(editorTool/saveBlogNote/editorAiAssist) | ▲ | 中 | 与 N9-15 抢 `blog_wechat.html` → 不同波；与 N9-22 抢 api.js |
| N9-22 | 学习概括页面整合 | 新建 `学习概括.html` + `个人中心.html` + `更多.html` + **28 个 HTML**（删底部导航「学习统计」）+ `app.js`(`openBlogStats` 重定向) | ★ ■ | **高** | 拆成 **A（单文件）/ B（28页批量）** 两段，跨 W3 / W6 |
| N9-23 | 每日一题卡改造 | `商务礼仪面试.html` 纯页内（新增 `ivQuizSubmit()` + localStorage） | — | 低 | **最理想标杆**，零依赖零共享 |
| N9-24 | 万能金句库升级 | **`assets/app.js`**(COMM_QUOTES L4359 / renderQuotes L5493 / renderQuoteCard L5532 / filterQuotes L5485) + `万能金句库.html` + `高情商表达.html`(L143) | ★ | 高 | app.js 独占项；C 项 hitokoto 需真机验 CORS |

**共享文件占用统计（= 关键路径长度）**
- `app.js` 独占任务：**5 个**（N9-1+2、N9-24、N9-13、N9-22B 的 openBlogStats 段、N9-7）→ 这 5 个**天然全串行**，构成本计划的关键路径。
- 30+ HTML 批量任务：**3 个**（N9-9、N9-22B、N9-3）→ **必须合并成一次原子批量**，否则全站被改三遍、第二次覆盖第一次。

---

## 2. 依赖冲突识别

### 2.1 冲突矩阵（X = 文件重叠，绝不可同波并行）

|  | N9-1/2 | N9-3 | N9-4 | N9-6 | N9-7 | N9-8 | N9-9 | N9-13 | N9-15 | N9-16 | N9-17 | N9-21 | N9-22 | N9-24 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **N9-1/2** | — | X★ | | | X★ | Xᵖ | | X★ | | | | | X★ | X★ |
| **N9-3** | X★ | — | | | X★ | | **X■** | X★ | | | | X▲ | **X■** | X★ |
| **N9-4** | | | — | | | | Xᵖ | | | | | | **Xᵖ** | |
| **N9-6** | | | | — | | | Xᵖ | | | | | | **Xᵖ** | |
| **N9-7** | X★ | X★ | | | — | | **X■** | X★ | | | | | X★ | X★ |
| **N9-8** | Xᵖ | | | | | — | Xᵖ | | | | | | | |
| **N9-9** | | **X■** | Xᵖ | Xᵖ | **X■** | Xᵖ | — | | Xᵖ | Xᵖ | Xᵖ | Xᵖ | **X■** | |
| **N9-13** | X★ | X★ | | | X★ | | | — | | | | | X★ | X★ |
| **N9-15** | | | | | | | Xᵖ | | — | | | **Xᵖ** | | |
| **N9-16** | | | | | | | Xᵖ | | | — | **Xᵖ** | | Xᵖ | |
| **N9-17** | | | | | | | Xᵖ | | | **Xᵖ** | — | | Xᵖ | |
| **N9-21** | | X▲ | | | | | Xᵖ | | **Xᵖ** | | | — | X▲ | |
| **N9-22** | X★ | **X■** | **Xᵖ** | **Xᵖ** | X★ | | **X■** | X★ | | Xᵖ | Xᵖ | X▲ | — | X★ |
| **N9-24** | X★ | X★ | | | X★ | | | X★ | | | | | X★ | — |

图例：`★`=`app.js` 冲突（31 页 blast radius）｜`■`= 30+ HTML 批量冲突（最严重）｜`▲`=`api.js` 冲突（30 页）｜`ᵖ`= 单个页面文件冲突

### 2.2 五条「必须先做 / 绝不能并行」的硬判断

| # | 判断 | 依据 |
|---|---|---|
| **D1** | **N9-9 + N9-22B + N9-3 必须合并为一次「全站 HTML 原子批量」** | 三者分别要改 33 / 28 / 30+ 个 HTML，重叠面 ≥28。分三次做 = 全站被改三遍，后写覆盖先写，且每次都要全量回归。合并后只需**一个 Python 脚本 + 一次 bump + 一次部署 + 一次回归** |
| **D2** | **所有动 `app.js` 的任务（N9-1+2 / N9-24 / N9-13 / N9-22B / N9-7）严格串行** | app.js 31 页共用、8761 行。并发写同一文件 = 后提交覆盖先提交。这 5 项构成本计划**关键路径**，只能靠「波内其它任务并行」来摊薄 |
| **D3** | **N9-7（app.js 拆分）必须排在最后** | 拆分会改变文件结构与加载序；任何在它之后动 app.js 的任务都要重定位落点，任何在它之前的共享改动都会被拆分动作二次放大风险 |
| **D4** | **N9-15 必须先于 N9-21**（两者都碰 `blog_wechat.html`） | 且 `blog_wechat.html` 持有全站唯一的畸形戳 `20260915eaaa`，两次改动都要复核该戳 |
| **D5** | **N9-15 / N9-22 开工前必须先解 F3**（同名函数双份） | 改到「死副本」= 全部白做，且不会报错，极难发现 |

### 2.3 逻辑依赖（非文件冲突，但决定顺序）

- `N9-10 ⊂ N9-18`：N9-10 的分类维度疑问已由 N9-18 的「5 大类」定档，**不再单独派活**。
- `N9-9 ← N9-18`：N9-9 的基准样式是 `voiceplayer.js` 的 `vp-back`，N9-18 不得改其语义。二者可并行，但 N9-18 需**只读** `vp-back`。
- `N9-6 → N9-22`：`更多.html` L102-106 由 N9-6 让路、N9-22 改造。
- `N9-22B` 保留 `openBlogStats()` 作兼容别名：**必须等 28 页删按钮全部生效后**才确认无漏网引用，不能在删按钮之前动它。

---

## 3. Mermaid 依赖图

```mermaid
graph TD
    subgraph W1["Wave 1 · 标杆波（自包含·零共享·快速闭环）"]
        A1["N9-23 每日一题卡<br/>商务礼仪面试.html"]
        A2["N9-11 阅读理解布局<br/>cet-read.js"]
        A3["N9-16 联系管理员<br/>私聊.html"]
        A4["N9-5 工具页填充<br/>工具.html"]
        A5["N9-6 更多页填充<br/>更多.html(避开L102-106)"]
    end

    subgraph W2["Wave 2 · 独立模块波（文件集互不相交）"]
        B1["N9-19 模拟面试重构<br/>AI模拟面试.html+iv-prep.js"]
        B2["N9-17 长按菜单撤回<br/>chat-local.js+私聊.html"]
        B3["N9-18 听力精听改版<br/>voiceplayer.js (含N9-10)"]
        B4["N9-14 PPT训练 P0<br/>PPT训练.html+ppt-tips.js"]
    end

    subgraph W3["Wave 3 · 共享文件波 A"]
        C1["N9-1+N9-2 题库筛选修复<br/>★app.js+行测刷题.html"]
        C2["N9-15 我的文章布局<br/>●common.css+blog_wechat.html"]
        C3["N9-22A 学习概括页<br/>新建页+个人中心+更多.html"]
    end

    subgraph W4["Wave 4 · 共享文件波 B"]
        D1["N9-24 金句库升级<br/>★app.js+万能金句库.html"]
        D2["N9-12 PPT版式库<br/>🔴资源阻塞"]
        D3["N9-21 写发帖改进<br/>▲api.js+学习博客+blog_wechat"]
    end

    subgraph W5["Wave 5 · 共享文件波 C"]
        E1["N9-13 PPT案例拆解<br/>★app.js 🔴资源阻塞"]
        E2["N9-4 个人中心作品集"]
        E3["N9-8 筛选标签交互<br/>行测刷题.html+其它"]
    end

    subgraph W6["Wave 6 · 全站 HTML 原子批量波"]
        F1["N9-9 页头返回(33页)"]
        F2["N9-22B 删学习统计(28页)"]
        F3["N9-3 全局通知注入(30+页)"]
        F1 --- F2 --- F3
    end

    subgraph W7["Wave 7 · 收尾波"]
        G1["N9-7 app.js 拆分"]
        G2["统一 bump + 修畸形戳 + 语法自查"]
        G3["部署 + 线上复检"]
        G4["文件清理(铁律:最后一步)"]
    end

    W1 --> W2 --> W3 --> W4 --> W5 --> W6 --> W7
    C1 -.★app.js 串行.-> D1 -.★app.js 串行.-> E1 -.★app.js 串行.-> G1
    C3 -.N9-22 拆两段.-> F2
    A5 -.更多.html 让路.-> C3
    C2 -.blog_wechat.html.-> D3
    A3 -.私聊.html.-> B2
    B3 -.vp-back 基准(只读).-> F1
```

---

## 4. 波次拆解（核心交付）

### Wave 1 · 标杆波
**交付价值**：用 5 个「单文件 / 页内 / 零共享」的改动打通「改→自检→波末 bump→部署→线上复检」整条流水线，建立验证信心与回归基线；同时清掉 3 个用户直接截图反馈的体验问题。
**验证方式**：逐页 `node --check`（内联 JS 抽出后）+ Grep 老 WebView 禁用语法 0 命中 + 5 个页面浏览器实测 + 线上直连复检。

| 子任务 | 需求 | 文件边界（**严格独占**） | 交付物 |
|---|---|---|---|
| W1-T1 | N9-23 | `商务礼仪面试.html`（L101-125 题卡 / L642-687 题库与函数 / 新增 `ivQuizSubmit()`；隐藏孤儿 `#ivAiPanel`） | 「今日一题」卡 + 进度 + localStorage |
| W1-T2 | N9-11 | `assets/cet-read.js`（L135-147 布局 + L258/L330/L402 三题型同步） | 右栏不再空卡片 |
| W1-T3 | N9-16 | `私聊.html`（L532-548 `patchSwitchTab` 加 acEntry 显隐 + L564-568 init 初始调用；**不动任何 assets**） | #acEntry 仅好友 tab 可见 |
| W1-T4 | N9-5 | `工具.html`（填充真实内容，**禁止改侧边栏入口**） | 工具页无空白 |
| W1-T5 | N9-6 | `更多.html`（**禁区：L102-106 学习统计卡留给 N9-22**） | 更多页内容充实 |

**零重叠校验**：5 个文件集合互不相交 ✓
**波末动作**：本波只改 1 个 assets（cet-read.js）→ 波末统一 bump `cet-read.js` 戳（引用页已 Grep 在案）+ 部署 + 复检。

---

### Wave 2 · 独立模块波
**交付价值**：4 条互不相交的业务线同时推进，消化本批最大的几个「单页/单模块」改造；把风险最高的共享文件改造全部推后到 W3 之后。
**验证方式**：各子任务自测 + 波末统一 Grep 语法自查 + 一次性部署复检。

| 子任务 | 需求 | 文件边界（**严格独占**） | 备注 |
|---|---|---|---|
| W2-T1 | N9-19 | `AI模拟面试.html` + `assets/iv-prep.js` | 🟡 需先出「六阶段状态机 + 五段状态条」交互文档 |
| W2-T2 | N9-17 | `assets/chat-local.js` + `私聊.html`（新增 `.im-msg-menu` CSS） | 复用 `imDoRecall`/`imAskRecall`，逻辑零改动 |
| W2-T3 | N9-18（含 N9-10） | `assets/voiceplayer.js` + 听说训练相关页 | **禁改** `vp-back` 语义 / `#vpMask` id / `SCENES` 结构签名 |
| W2-T4 | N9-14 (P0) | `PPT训练.html` + `assets/data-ppt-tips.js` + `assets/ppt-tips.js` | 对比图先用占位，资源到位后回填 |

**零重叠校验**：4 组文件集合互不相交 ✓（N9-16 已在 W1 完成，此处不再碰 `私聊.html` 的同一段）
**波末动作**：统一 bump `chat-local.js` / `voiceplayer.js` / `ppt-tips.js` / `data-ppt-tips.js` 戳 + 部署 + 复检。

---

### Wave 3 · 共享文件波 A
**交付价值**：首次触碰「共享文件」这条关键路径——用风险最低的题库数据修复（N9-1+2）试水 app.js 改动的完整流程；同时并行清掉一个用户截图 bug 和新建 N9-22 的综合页。
**验证方式**：`assets/app.js` 改动后 **Grep 全文件** `?.` / `??` / `replaceAll` / `Object.fromEntries` / `.at(` / 顶层 await / `(?<=` / `(?<!` 全 0；`node --check`；31 页抽样实测；波末统一 bump app.js 到 31 页。

| 子任务 | 需求 | 文件边界（**严格独占**） | 串行约束 |
|---|---|---|---|
| W3-T1 | **N9-1 + N9-2 合并** | `assets/app.js`（L446-449 / L494-497 / L5776）+ `行测刷题.html` | **app.js 本波独占** |
| W3-T2 | N9-15 | `assets/common.css`（L1383/L1384）+ `blog_wechat.html` | ⚠️ 开工先解 F3 |
| W3-T3 | **N9-22 A 段** | 新建 `学习概括.html` + `个人中心.html`（删 L106-108 / L269-276 / 清 L485 / L823 / SubpageRouter）+ `更多.html`（L102-106 改「学习概括」入口、L164-166 删） | 只做单文件段，**28 页批量留到 W6** |

**零重叠校验**：app.js / common.css+blog_wechat.html / 学习概括.html+个人中心.html+更多.html 三组互不相交 ✓
**波末动作**：统一 bump `app.js`（31 页）+ `common.css`（含修 `blog_wechat.html:325` 畸形戳）+ 部署 + 复检。

---

### Wave 4 · 共享文件波 B
**交付价值**：app.js 第二个独占项（金句库 18→100 句）落地；并行推进版式库结构层与写发帖减法。
**验证方式**：同 W3（app.js 语法 Grep 全 0 + 31 页抽样）；金句库额外验「10 分类全 ≥8 句 / 总量 100 / 自定义句 icon 容错 / XHR 超时 toast」。

| 子任务 | 需求 | 文件边界 | 备注 |
|---|---|---|---|
| W4-T1 | N9-24 | `assets/app.js`（L4359 / L5485 / L5493 / L5497 / L5532）+ `万能金句库.html` + `高情商表达.html` L143 | **app.js 本波独占**；XHR 单路径，禁 fetch/async-await |
| W4-T2 | N9-12 | `PPT版式库.html` + `assets/data-ppt-templates.js` + `assets/tpl-preview.js` | 🔴 资源阻塞 → 先做「结构层 + 占位图」，资源到位后回填 |
| W4-T3 | N9-21 | `学习博客.html` + `blog_wechat.html` + `assets/api.js` | ⚠️ 先解 F3 再定 editorTool/saveBlogNote 落在哪个文件 |

**零重叠校验**：三组互不相交 ✓
**波末动作**：统一 bump `app.js` + `api.js`（30 页）+ 版式库相关戳 + 部署 + 复检。

---

### Wave 5 · 共享文件波 C
**交付价值**：app.js 最后一个内容型改动（PPT 案例真实化）落地；并行完成作品集与筛选交互。
**验证方式**：app.js 语法 Grep 全 0 + PPT案例拆解/个人中心/行测刷题三页实测 + 收藏 localStorage 持久化验证。

| 子任务 | 需求 | 文件边界 | 备注 |
|---|---|---|---|
| W5-T1 | N9-13 | `assets/app.js`（PPT_CASES L4217 / 渲染 L5327）+ 新增 `assets/images/ppt-cases/*` | **app.js 本波独占**；🔴 截图未到位先占位 |
| W5-T2 | N9-4 | `个人中心.html`（**限定页内实现**） | 与 W3-T3 的 `个人中心.html` 跨波串行，OK |
| W5-T3 | N9-8 | `行测刷题.html` + 其它带标签筛选页 | 与 W3-T1 的 `行测刷题.html` 跨波串行，OK |

**波末动作**：统一 bump `app.js` + 部署 + 复检。

---

### Wave 6 · 全站 HTML 原子批量波（**最高风险波**）
**交付价值**：一次性完成三件「必须改 30+ 个 HTML」的事——全站补页头返回、删除底部导航「学习统计」、注入全局通知。合并执行把「全站改三遍」降为「全站改一遍」。
**验证方式**：脚本先 `SW_DRY_RUN=1` 空跑出变更清单 → 人工过一遍清单 → 执行 → **36 页全量**逐页打开实测（页头返回可点、底部无「学习统计」、通知脚本已注入且无 ReferenceError）→ 一次 bump → 一次部署 → 线上全站复检。

> ⚠️ **本波内部禁止按「需求」并行**（三个需求覆盖同一批文件），**只允许按「文件批次」并行**。

| 子任务 | 范围 | 文件边界 | 说明 |
|---|---|---|---|
| W6-T0 | **批量脚本编写**（先行） | `tools/` 下新建脚本（不改任何 HTML） | 一个脚本内含三个变换开关：页头返回注入 / 删「学习统计」按钮 / 通知脚本注入 |
| W6-T1 | 第 1 批页面（约 18 页，按 Grep 清单对半切） | 拿到**独占页面清单**，跑全部三个变换 | 页面清单在 T0 阶段固化，两批**不得有交集** |
| W6-T2 | 第 2 批页面（其余约 15-18 页） | 同上 | 同一脚本、同上三变换 |
| W6-T3 | N9-22B 收口（app.js 兼容别名） | `assets/app.js` 的 `openBlogStats()` 改为重定向到 `学习概括.html` | **必须在 W6-T1/T2 全部完成后**执行，确认 28 页删干净再改；函数保留不删 |

**零重叠校验**：T1 / T2 按页面清单对半切，交集为空 ✓；T3 串行在最后，且只碰 app.js ✓
**豁免页**（W6-T0 脚本需显式排除）：`mock_exam_run.html`（考试进行中）、`登录.html`。

---

### Wave 7 · 收尾波
**交付价值**：全站版本一致性收口 + 最高危的 app.js 拆分 + 上线 + 按用户铁律做最后的文件清理。
**验证方式**：`git diff --stat` 复核变更面 → 语法 Grep 全项目 0 命中 → 部署后直连复检 36 页 → 清理前后各跑一次 `git status --short` 留痕。

| 子任务 | 需求 | 顺序 | 说明 |
|---|---|---|---|
| W7-T1 | N9-7 app.js 拆分 | 1 | **最后做**。硬约束：全局符号名不变、`INLINE_*_BANK` 仍在 `examInit` 之前、零现代语法、31 页 script 标签同步 |
| W7-T2 | 统一 bump + 修畸形戳 | 2 | 全站扫一遍；**显式处理 `blog_wechat.html:325` 的 `20260915eaaa`**（词边界匹配会漏） |
| W7-T3 | 全项目语法自查 | 3 | Grep `?.` `??` `replaceAll` `Object.fromEntries` `.at(` 顶层 await `(?<=` `(?<!` —— `node --check` **抓不到** |
| W7-T4 | 部署 + 线上复检 | 4 | 直连 `110.42.134.62`，禁代理；中文文件名 URL quote |
| W7-T5 | 文件清理（休眠 DOM / 旧备份 / 临时脚本） | 5 | **用户铁律：必须所有任务完成后才做**；先 `git status --short` 区分已跟踪/未跟踪，`tools/` 下真实工具不删 |

---

## 5. 资源阻塞清单（不要让人干等）

| ID | 阻塞资源 | 状态 | 处置 |
|---|---|---|---|
| **N9-12** | 15 张版式预览图 + `.pptx` 文件 | 🔴 完全不存在 | W4 先做**结构层 + 占位图**，回填作为独立收尾动作；`.pptx` 若始终无法生产，下载按钮恒显「制作中」，功能仍成立 |
| **N9-13** | 真实 PPT 案例截图（`assets/images/ppt-cases/`） | 🔴 不存在 | W5 先用占位 `<img>`，渲染/收藏/大图遮罩先上线 |
| **N9-14** | 正反例对比图 | 🟡 可降级 | 占位图先上，P0 其余 4 件不受影响 |
| **N9-19** | 画布设计稿 + PDF | 🟢 已在手 | 需先产出「六阶段状态机 + 五段状态条」交互文档再开工 |
| **N9-24-C** | hitokoto 网络金句 | 🟡 验证阻塞 | XHR 单路径上线；APK `file://` CORS 需真机验，失败则 APK 内隐藏该按钮 |
| **N9-1/2** | 缺陷复现 | 🟡 | 需先复现现象（筛选项缺失 or 点选不生效）再定改数据还是改逻辑 |

---

## 6. 全局风险清单与跨波约定

| # | 风险 | 等级 | 跨波约定 / 缓解 |
|---|---|---|---|
| **R1** | **版本戳 bump 重写 31 HTML**（隐藏全局锁） | 🔴 高 | 波内任务**一律不 bump**，波末单点统一执行；改共享文件的任务只在交付说明里声明「需 bump X」 |
| **R2** | **`blog_wechat.html:325` 畸形戳 `20260915eaaa`** | 🔴 高 | 每次 bump 脚本必须**显式单列该页**，不能依赖词边界匹配；该页还同时被 N9-15 / N9-21 改动，双重暴露 |
| **R3** | **F3 同名函数双份**（app.js vs api.js） | 🔴 高 | N9-15 / N9-21 / N9-22 **开工第一步**必须先判明生效副本（建议：浏览器控制台实测 + 加载序分析），再动刀 |
| **R4** | **W6 全站批量改 30+ HTML** | 🔴 高 | 合并成一次原子批量；脚本先 dry-run 出清单人工过目；执行前 tar 备份；一次部署一次全量回归；豁免页显式排除 |
| **R5** | **app.js 五连独占（关键路径）** | 🟠 中高 | N9-1+2 / N9-24 / N9-13 / N9-22B / N9-7 严格串行；靠每波并行 2-3 条独立线摊薄 |
| **R6** | **老 WebView 语法（`node --check` 抓不到）** | 🟠 中高 | 每波结束 Grep：`?.` `??` `replaceAll` `Object.fromEntries` `.at(` 顶层 await `(?<=` `(?<!` → 必须 0 |
| **R7** | **N9-22 删按钮后 `openBlogStats` 漏网引用** | 🟠 中高 | 函数**保留并重定向**，绝不删除；28 页删完 + 全站 Grep 确认无残留调用后，再改函数体 |
| **R8** | **N9-7 拆分改变加载序** | 🟠 中高 | 排最后；拆前 `git tag` 打回滚点；拆后验 `INLINE_*_BANK` 仍在 `examInit` 之前 |
| **R9** | **ADR-3 回归**（改造中引入新弹窗） | 🟡 中 | 每波结束 Grep 原生 `alert/confirm/prompt` = 0；保留原 DOM id 与函数名 |
| **R10** | **并行 Agent 抢文件** | 🟡 中 | 每个子任务在派活时**显式列出独占文件清单**；波末统一 bump 前不允许任何 Agent 碰其它波的文件 |

### 6.1 回滚点策略

| 波次 | 回滚点 | 动作 |
|---|---|---|
| 每波开工前 | `git tag wave-N-start` + 远端 `tar -czf web_bak_<stamp>.tar.gz` | 部署脚本已内置远端备份 |
| W6（全站批量） | 额外全量备份 `web_bak_pre_wave6_full.tar.gz` | 30+ 页改动，必须可一键还原 |
| W7-T1（拆分） | `git tag pre-appjs-split` | 拆分失败直接回退 |

### 6.2 每波统一收口动作（Checklist）

1. Grep 老 WebView 禁用语法 → 必须 0
2. Grep 原生 `alert/confirm/prompt` → 必须 0
3. `node --check` 所有改动过的 `.js` → 0 error
4. 波末**单点**统一 bump（含 `blog_wechat.html:325` 畸形戳特判）
5. 部署（远端先备份）→ 直连线上复检 → 回写 `任务执行文档-20260915-全线自主执行.md`

---

## 7. 波次总览

| 波次 | 需求项 | 子任务数 | 共享文件占用 | 预计风险 |
|---|---|---|---|---|
| **W1 标杆波** | N9-23 / N9-11 / N9-16 / N9-5 / N9-6 | 5 | 无 | 低 |
| **W2 独立模块波** | N9-19 / N9-17 / N9-18(含 N9-10) / N9-14 | 4 | 无 | 中 |
| **W3 共享波 A** | N9-1+N9-2 / N9-15 / N9-22A | 3 | ★app.js ｜ ●common.css | 中高 |
| **W4 共享波 B** | N9-24 / N9-12 / N9-21 | 3 | ★app.js ｜ ▲api.js | 高 |
| **W5 共享波 C** | N9-13 / N9-4 / N9-8 | 3 | ★app.js | 高 |
| **W6 全站原子波** | N9-9 / N9-22B / N9-3 | 3+1(脚本) | ■ 30+ HTML | **最高** |
| **W7 收尾波** | N9-7 + bump + 部署 + 清理 | 5 | ★app.js + 31 HTML | 高 |
| **合计** | **23 项**（N9-20 已作废，N9-10 并入 N9-18） | | | |

---

*文档结束。执行时每完成一波立即回写 `任务执行文档-20260915-全线自主执行.md`。*
