# 任务执行文档-20260915：全线自主执行（批次八+体验修复）

> 开工时间：2026-09-15 00:16。本文档是本轮自主执行的唯一权威进度来源，**每完成一项任务/每次决策/每批部署立即回写进度日志区**。
> 授权记录：用户 00:04 授权自主部署上线；00:09 授权执行中一切决策按执行方最优判断直接做（事后留痕）；00:14 要求不限速时超多线并行、被限自动降档；00:12 定序：申论最前、冗余文件清理最后；00:15 要求本文档实时回写。

---

## 一、任务队列（锁定顺序）

| 序 | 任务 | 线 | 状态 |
|---|---|---|---|
| T1 | 申论 OCR 结构化 → shenlun-questions.json（含抽查验证） | sl-data2 | 🔄 进行中（default 429，已降档 lite 重派） |
| T2 | 申论刷题.html（两 Tab 七子页，风格对齐行测刷题.html，禁止弹窗敷衍） | sl-page | 🔄 进行中 |
| T3 | 行测·常识判断 json 复核 + 言语理解结构化 | xt-data1 | 🔄 进行中 |
| T4 | 行测·资料分析脚本续作 → json | xt-data2 | 🔄 进行中 |
| T5 | 行测·判断推理结构化（含图形切图） | xt-data3 | 🔄 进行中 |
| T6 | bug 硬伤批：③最近学习数据 bug ④薄弱点真实数据 12?user=解析 13学习偏好竖排 | fix-bug2 | 🔄 进行中（default 429，已降档 lite 重派） |
| T7 | 题库注入线：申论+行测各本 INLINE_XXX_BANK 注入 app.js L439 起（T1/T3/T4/T5 完成后，由本线独占 app.js） | inject | ⏳ 等 T1/T3-T5 |
| T8 | 页面合规批：①错题本分类 ⑥好友申请页弹窗卡片 14·B1 文案 | fix-ui | ⏳ 等 T6（app.js 释放） |
| T9 | 大改批：⑤消息通知（微信式顶栏横幅+实时性）②进度卡片重构 | fix-big | ⏳ 等 T8 |
| T10 | 版本 bump ?v= + 全局一致性审查 + QA 证伪回归 | lead+qa | ⏳ 各线完成后 |
| T11 | 部署上线（已授权自主执行，部署后汇报） | lead | ⏳ |
| T12 | 行测·5 本解析册结构化 → 关联 x 字段 | ⏳ T3-T5 后 |
| T13 | 冗余文件清理（**最后一步**，清理前列清单确认无引用，删前备份） | lead | ⏳ 最后 |
| T14 | 需求 20 关闭确认（随 T2/T12 完成自然核销） | — | ⏳ |

不做的：push GitHub（未授权）、APK 打包（永久禁令）。

## 二、执行 SOP（每条线遵守）

1. **代码树**：唯一代码树 = `C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-6599a366`；部署源 = `D:\下载的文件\学习工作台`（只同步，不直接改）。数据线脚本放各自 OCR 目录。
2. **文件零重叠**：本阶段 app.js 归 fix-bug 线独占；申论页线只建 申论刷题.html；注入统一走 T7 线。
3. **环境坑**：bash 缺 coreutils → 用 Read/Glob/Grep/Edit/Write 工具，node/python 用绝对路径（`C:\Users\ATM\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`、`C:\Users\ATM\.workbuddy\binaries\python\versions\3.13.12\python.exe`）；Glob 子目录模式在中文路径会误报空 → path 给到具体子目录。
4. **限速策略**：撞 429 → 换模型重试 → 仍不行转串行兜底，不停摆不请示。
5. **红线**：不删任何现有功能；内容全原创；题库注入不改 EXAM_BANK 数组本身；localStorage 键走 lsKey() 账号隔离；共享 css/js 改动统一由 T10 bump 版本戳（各线不要自行 bump）。
6. **验收**：每线完成后主理人抽查落点与内容（不信汇报），QA 线独立证伪。

## 三、进度日志（实时回写，倒序追加）

- **2026-09-15 00:16** 全线开工。本文档落盘；派出 6 条并行线：sl-data（申论OCR结构化，default）、sl-page（申论刷题.html，lite）、xt-data1（常识复核+言语，lite）、xt-data2（资料分析续作，lite）、xt-data3（判断推理，lite）、fix-bug（bug硬伤批：app.js+个人中心.html 独占，default）。T7-T14 按依赖排队。
- **2026-09-15 00:19 决策记录**：default 模型 429（02:10 重置）。按限速 SOP 降档：sl-data/fix-bug 两线以 lite 重派为 sl-data2/fix-bug2，任务书不变。当前 6 线全 lite 并行。
- **2026-09-15 00:30 决策记录**：lite 配额也打满，sl-page/xt-data1/xt-data2/xt-data3/sl-data2/fix-bug2 全部 429 失败（02:10 重置）。换 reasoning 模型重派为 sl-page2（申论页面）、sl-data3（申论OCR）、xt-data4（常识+言语）、xt-data5（资料分析）、xt-data6（判断推理）、fix-bug3（bug批）；队长同时转主会话串行核实在做（已确认 app.js 的 computeRecentLearning/computeWeakPoints 均为真实数据，非假数据）。
- **2026-09-15 00:35**：6 条 reasoning 线全部重新拉起；02:10 配额恢复后将恢复 lite/default 满线并行。
- **2026-09-15 00:40 决策记录（用户要求成员数拉满）**：成员数无硬上限，瓶颈是文件冲突。按文件零重叠再开 3 线：ui-fix1（⑥好友申请.html 弹窗→页面内，独占该文件）、ui-fix2（14·PPT训练.html B1 文案，独占该文件）、xt-data7（T12 五本解析册结构化，纯数据）。当前 9 线并行：sl-page2 / sl-data3 / xt-data4 / xt-data5 / xt-data6 / xt-data7 / fix-bug3 / ui-fix1 / ui-fix2。
- 暂缓（因文件冲突需排队）：①错题本分类、②进度卡片重构、⑤消息通知（均需 app.js，等 fix-bug4 释放）；全站侧边栏加「申论刷题」入口（等 申论刷题.html 落地后统一批量改，避开 ui-fix3/ui-fix4 独占文件）。
- **2026-09-15 00:45 决策记录**：00:43 一轮 9 线唤醒全部 429（含 reasoning，账号级限流 02:10 重置）。已设一次性自动化「02:20 配额恢复自动拉满并行线」兜底。00:45 再试 reasoning 探针成功，立刻重派 9 线：sl-data4（申论OCR）、sl-page3（申论刷题.html）、fix-bug4（bug批，独占 app.js+个人中心.html）、xt-data8（常识+言语）、xt-data9（资料分析）、xt-data10（判断推理）、xt-data11（解析册）、ui-fix3（好友申请.html）、ui-fix4（PPT文案）。
- **2026-09-15 00:45 队长主会话串行成果**：个人中心.html 第 30 行学习偏好竖排 bug 已修（`.pf-pref-row .form-input { width:auto; flex:0 0 auto; min-width:150px; }`），并加注释说明根因（flex-basis 必须归零）。
- **2026-09-15 00:52 T14（PPT 技巧入口文案）完成并抽查通过**（ui-fix4）：PPT训练.html L211 desc 改为「…5 个专题速览，配 13 道带解析练习与 6 项实战任务」；L227 兜底 tag 由「知识卡片」改「内容合集」。队长独立核实：assets/data-ppt-tips.js L327 meta = { total:5, quiz:13, tasks:6 }，与文案三个数字逐项对齐，无虚标。CATS L455-457 复核无需改。
  - 队长决策（两条开放问题）：① L227 保持「内容合集」（中性、跨分类通用，运行时会被 ppt-tips.js 覆盖写具体数字），不改；② CATS.desc 与 data.intro 文案重复属维护隐患但无功能风险，记为低优先级待办，本批不动；③ 速览 tab 同一时刻只展开一个专题为渲染器既有设计，不改。
- **2026-09-15 00:55 ⑥好友申请页改判（ui-fix3 查证 + 队长裁决）**：好友申请.html 全文无 modal/overlay/popup，核心流程本就页面内闭环，已符合 ADR-3，且它是孤儿后门页（全站仅 私聊.html:243 注释提及，无入口）。用户抱怨的真身是 **私聊.html 的 #imAddFriendModal**（C1/20260913k 改造时 relocateModals 把节点搬进 #imPanelAddFriend，但 .im-overlay>.im-modal 外壳原地保留，视觉上是"侧栏里嵌着弹窗卡片"）。队长裁定：目标改判到 私聊.html（该文件归 ui-fix3 独占），保留 #imAfInput/#imAfResults 两个 id 以免改 chat-local.js，拆掉 modal 标题栏/✕/卡片边框，功能一个不删。
  - 附带发现（记待办）：好友申请.html 是孤儿页，无入口且内容与私聊内加好友重复，后续可与用户确认是否清理（属"冗余清理"最后一步范畴）。
- **2026-09-15 01:05 ⑥弹窗卡片已由队长主会话落地（ui-fix3 撞 429 未执行）**：私聊.html L244-254 的 .im-overlay/.im-modal 外壳 + 标题栏 + ✕ 已拆除，改为 `<div class="im-af-sec" id="imAddFriendModal">`（L246-251），L329-331 新增 .im-af-sec 系列样式（无边框/无圆角/无遮罩，与好友列表同视觉）。**#imAfInput / #imAfResults 与 id imAddFriendModal 全部保留**，chat-local.js 的 imOpenAddFriendModal（L1820-1834，靠 id + display none/flex 控制）、imAfSearch、imRenderFriendRows 零改动照常命中；#imGroupModal / #imGroupSettingsModal 两个真弹窗未受影响。改造前已确认 relocateModals()（L463-468）恒把该节点搬入 #imPanelAddFriend，仅 tab 场景使用，拆壳无副作用。
- **2026-09-15 01:12 T6 bug 硬伤批完成并抽查通过**（fix-bug4，未 bump ?v=）：
  - 【1 首页不刷新】根因 renderHome() 只在顶层 __tryInit 跑一次，bfcache 返回/跨标签学完切回三张卡片仍是旧快照。app.js **L1910 refreshHomeCards()**（先 loadData() 重读 localStorage 再重渲 moduleProgress/weakPoints/recentList，全程 try 兜底）+ **L1922-1931 bindHomeCardsRefresh()**（visibilitychange / pageshow(persisted) / focus，__homeCardsRefreshBound 防重复绑定）+ **L1933** 仅首页执行。抽查确认三处均在位。
  - 【2 题型进度漏统计】**根因定位准确且有价值**：app.js L5075 附近 `if (appData.examTypeProgress[q.type])` 只对默认表内 7 个传统题型累计，常识判断/申论等新题型不在表内被**静默跳过** → 该题型做题数与正确率永不累计，首页「薄弱点提醒」永远算不出它。已改为缺失即补建 `{total:0,correct:0}`（app.js L5078-5081，带注释）。抽查确认。这直接决定了批次八新导入的四个模块能否进统计。
  - 【3 个人中心 ?user=】根因是 SubpageRouter 默认停在 'list' 子页，#profileBox 所在 `[data-subpage="profile"]` 被 display:none，api.js 的 renderUserHome 其实跑了只是藏在隐藏 section。个人中心.html **L787-797** 新增：URLSearchParams 读 user，纯数字则 SubpageRouter.navigate('profile')；不碰 api.js 任何判定。抽查确认。
  - 验证：node --check assets/app.js 通过；验收脚本 tools/verifier/verify_fixbug4_0915.js 15/15 通过。
  - 队长裁决（fix-bug4 提的两点）：① chat-local.js:2240 过期注释「个人中心.html 不解析 ?user=」——本批不改（纯注释，无功能影响），记入待办；② 离线态带 ?user= 仍显示自己资料（需动 api.js 的 renderProfilePage online 分支）——**本批不动**，属低频场景且风险高于收益，记入待办。
- **2026-09-15 01:20 T2 申论刷题.html 交付并抽查通过**（sl-page3，1637 行，新建单文件）：Tab1 题目精练 3 页（列表页 11 卡片 + 无限滚动兜底分页 / 作答页 1581 字上限 80% 橙 100% 红、2s 自动保存、倒计时提醒、离开前二次确认 / 复盘页左右对照、keyExpressions 金色高亮可收藏）；Tab2 阅读积累 4 页（首页两板块 / 材料列表按主题|题型切换 / 材料详情 700px 限宽、选中仅高亮与加积累本 / 规范表达积累本页内表单）。数据源五路兜底：INLINE_SHENLUN_BANK → SHENLUN_BANK → SL_QUESTIONS → SHENLUN_QUESTIONS → shenlun_questions → localStorage（shenlun_questions / shenlun-questions / SHENLUN_BANK）→ fetch assets/data/shenlun-questions.json。副作用已收敛：注入倒计时先算 max 顺延一天再修正为当天；不写 examTypeProgress/skills；自动保存定时器每次重进 _asCleanup 清理。四存储键 shenlun_answers / shenlun_read_progress / shenlun_expressions / shenlun_favorites 齐备。
  - 队长独立抽查：L143-144 侧边栏「申论刷题」入口 active 在位；L491-497 五路兜底在码内；全文件 Grep **无 prompt( / alert(**（仅 #countdownModal 一个表单弹窗）。
  - 队长裁决（sl-page3 提的三点）：① #countdownModal 倒计时表单弹窗——**暂保留**，但记入待办（ADR-3 边界：表单类弹窗是否算轻交互需与用户确认，倒计时亦可做页面内行内编辑）；② 用户须知 3.1 与 L360-362 的红字不一致——属文案微调，顺手统一为「上线后由 app.js 注入/JSON 落盘，注入前为空态」，本批内一并修；③ 全站侧边栏加「申论刷题」入口仍需批量改（等本页稳定后统一做，避开在跑独占文件）。
- **2026-09-15 01:26 ①错题本分类由队长主会话落地**（fix-bug4 派单时撞 429，转串行）：错题本.html 新增页面级 `<style>`（.wb-filter / .wb-chip / .wb-chip.active / .wb-n，不动全站 common.css）+ 挂载点 `<div id="wrongTypeFilter" class="wb-filter">`（L101 附近）；app.js renderWrongBook() 重写：按 EXAM_BANK 还原 q.type 聚合并计数，筛选条「全部 N」+ 各题型按数量降序，chip 用 **data-t + this.dataset.t** 传值（规避题型名含引号破坏 onclick），列表取 wrongQuestions 数组倒序作"最近"近似；当前筛选题型被清空时**自动回退全部**，避免空白页。重新做/移除/清空/空态全部保留。node --check 通过。
  - 已知限制（汇报不含糊）：wrongQuestions 只存 id 无时间戳，故"最近"是数组顺序近似，非真实时间；若要精确到秒需改写入结构，属数据迁移，本批不做，记待办。
- **2026-09-15 01:20 ⑤消息通知完成并抽查通过**（notify-fix1）：chat-local.js L166-194 新增 imTopNotify 顶端横幅（头像+昵称+摘要，点击直达会话，可关闭/自动消失，非全屏遮罩，符合 ADR-3 轻交互）；收消息实时性根因=轮询间隔过大，已缩到 2000ms（L3132）并补 visibilitychange 回前台即时拉取（L3136-3138）。私聊.html L27/37-42 改为不再转发 xtNotify、由本文件渲染。验收脚本 19/19 断言通过。抽查三项落点全部在位。
  - 队长裁定：main-6599a366 是唯一活树兼部署源，main-f0596da4 只是会话工作目录（文档滞后到批次六），**不做同步**，已告知全体。
- **2026-09-15 01:24 行测题解管线根因定位（队长主会话）**：诊断脚本 _diag_gap.py 量化后发现**比"漏题"更严重**——题本考点归属大面积错误（考点3比赛问题 解析 3 题 vs 题本 103 题；考点2概率 31 vs 73；考点5线段法 5 vs 41；考点1代入排除法 15 vs 0），考点名还被截成「考点1」「考点2」空壳。根因三条：① L204-213 原 if/elif 链导致同行含考点+难度时只更新前者（stale difficulty）；② 进入新章/节/考点不清空下级（cur_* 只增不清）；③ 答案侧 L367 兜底「只要考点编号相同就跨章跨节取答案」→ 答案错挂。已修：题本侧改 4 层（章/节/考点/难度）+ 各层级独立判断 + 进入新层级清空下级 + 考点名截断时向后最多拼 2 个 band + is_section_title 补章识别；答案侧同步 4 层 + 兜底收紧为「章+节上下文一致才允许按编号兜底，宁可留空也不错挂」。重跑：**774 → 822 题，答案匹配 813**。
- **2026-09-15 01:26 数据普查（队长主会话，_survey.py）**：推翻 xt-data9「只产出 63 题」的汇报——实际文件是 980 题。真实家底：**言语理解 1010 / 判断推理 981 / 资料分析 980 / 常识判断 878 / 数量关系 822**，合计 **4671 题**；解析册 1009/980/1018/1012/1004。
- **2026-09-15 01:28 上线前答案质检（_verify_answers.py + _probe_fields.py）**：各模块题本/解析字段结构互不相同，跨模块统一匹配键不可行。改用**答案分布吻合度**判据：言语（B262 D255 C252 A241 vs 解析 B261 D255 C252 A241）、判断（C258 D256 B243 A224 vs C258 D258 B241 A223）、资料（题本少 38 题但比例一致）→ **答案提取正确，可信**；常识（少 134 题，比例接近）→ 基本可信；**数量关系 D 项 117 vs 解析 218 偏少 → 仍有问题，暂不扩充**。结论：言语/判断/资料可进下一批注入，常识待补 134 题，数量关系待复检。
- **2026-09-15 01:28 🚀 首批部署上线（20260915a，纯前端不重启）**：新增 申论刷题.html（82KB）+ assets/data/shenlun-questions.json + assets/data/shenlun-inline.js（1.8MB，100 题：单一题 44 / 综合题 28 / 公文题 28，材料与参考答案齐全，node 加载验证通过）；改 app.js（首页刷新+题型补建+错题本分类）、chat-local.js（顶端通知）、私聊.html（拆弹窗壳）、PPT训练.html（文案）、个人中心.html、错题本.html；**23 个页面侧边栏新增「申论刷题」入口**（含首页 学习工作台.html）；版本戳统一 bump 20260915a（98 处 / 33 文件）。部署脚本 tools/deploy_update_20260915a.py（含远端备份，dry-run 支持），远端 BACKUP_OK + EXTRACT_OK，服务器 110.42.134.62 校验通过。
- **当前未完成（下一批）**：① 行测四本注入前端（言语/判断/资料已可信，常识补 134 题，数量关系复检）；② 五本解析册回填 x（解析）字段；③ ②进度卡片重构；④ 申论/行测 OCR 噪声清洗（如「初裹」→「初衷」）；⑤ 冗余清理（最后一步，含好友申请.html 孤儿页处置）；⑥ push GitHub（需用户点头）。
- **2026-09-15 01:32 判断推理交付 + 注入 + 上线**（xt-data10 产 996 题，队长转换注入）：抽查确认 996 题、无答案 0、含图 336、切图 360 张 41.9MB。**首轮注入只进 709 题**——队长查证发现**图形推理 270 题的 options 是空 {}（选项是图形、已含在题干切图里）**，被"选项不足 4"规则误杀 232 题。修正为「options 空 + 有切图 → 用 A/B/C/D 占位」后重注入 **940 题**（图形推理 270 全保住，定义 171/类比 212/逻辑 287），跳过 56 题（OCR 漏抓选项，记待补）。node --check 通过。
- **2026-09-15 01:36 🔴 发现并修复线上既存 bug：行测切图全是死链**。检查发现 app.js 内 **172 处 [IMG:] 引用，assets 下 0 张 png → 已上线的 718 道数量关系题图片 100% 死链**（部署脚本 EXCLUDE_EXTS 一直排除 .png，而切图从未同步过）。根因：切图留在 OCR 工作目录（数量关系在 `数量关系_题本\crops` 756 张仅 1.7MB，判断推理在 `判断推理_题本\images` 360 张 41.9MB），从未进代码树/部署源。已修：注入器统一复制切图到 assets/images/，同步脚本放开 .png，部署脚本不再排除 .png。
- **2026-09-15 01:43 资料分析交付 + 注入 + 上线**（xt-data9 产 1004 题，队长用通用注入器转换）：结构探测发现它有**共享材料字段 material（表格/文字资料）**，若不并入题干则题目无法作答 → 注入器已处理（material + '\n' + stem）。注入 **959 题**（跳过 45 题 OCR 漏选项），切图 790 张 41.2MB。node --check 通过。
- **🚀 部署记录（20260915a，共三次，全部 BACKUP_OK + EXTRACT_OK）**：
  - 01:28 第一批：129 文件 / 2.7MB —— 申论刷题.html + 申论题库 1.8MB + bug 修复批 + 页面合规批 + 23 页侧边栏入口
  - 01:39 第二批：1245 文件 / 43.0MB —— 判断推理 940 题 + 修复死链的 1116 张切图
  - 01:43 第三批：2035 文件 / 82.7MB —— 资料分析 959 题 + 790 张切图
  - 工具：tools/deploy_update_20260915a.py（含远端备份 web_bak_20260915a.tar.gz、关键文件断言、dry-run 支持）；tools/inject_book.py（通用行测题本注入器，参数化 book/tag/id/src/imgdir，可重复运行）；tools/inject_judge_20260915.py（判断推理专项，含图形题占位逻辑）；tools/sync_bump_20260915a.py（worktree→部署源同步 + 版本戳 bump，只同步 html/assets，绝不碰 server/tools）
  - **线上题库现状**：原内置 + 数量关系 718（旧版，待重注 822 修正版）+ 判断推理 940 + 资料分析 959 + 申论 100
- **下一步（按优先级）**：① 言语理解 1010 题注入（等 xt-data8 复核完成）；② 常识判断补 134 题后注入；③ 数量关系用 822 修正版重注入（覆盖旧 718）；④ 五本解析册回填 x 字段；⑤ ②进度卡片重构；⑥ OCR 噪声清洗；⑦ 冗余清理（最后）；⑧ push GitHub（需用户点头）。
- **2026-09-15 02:05 言语理解 + 常识判断注入上线**（xt-data8 交付，队长抽查后注入）：抽查确认言语 **1010 题 options 键数全 4、no_answer=0、无图**，常识 878 题（862 四选项、16 不足、1 无答案、无图）。注入结果：**言语 1010 题零跳过**（片段阅读 365/逻辑填空 360/语句表达 180/篇章阅读 105），**常识 861 题**（跳过 17；人文 296/政治 266/法律 174/地理 85/经济 40）。两本均无切图，注入器走 --subfield chapter + --submap 做章号→章名映射。node --check 通过，第四批部署上线。
- **2026-09-15 02:15 ⚠️ 数量关系答案：无法用解析册校验，暂不替换（重要结论）**：写 tools/fix_quant_answers.py 用 (章, 考点编号, 难度, 题号) 从解析册取答案覆盖题本，结果 **歧义键 705 条、命中率仅 11.6%，且命中的 95 条里与题本一致率只有 53.7%（≈随机）**。说明跨书键匹配在数量关系上根本不可靠，无法判定题本答案与解析册答案孰对孰错。分布对比：题本 A183 B221 C290 D117 vs 解析 A224 B278 C281 D218（题本 D 明显偏少）。
  - **队长裁决：不拿不可靠数据充数**——数量关系维持在线的 718 题不动，822 修正版**暂不上线**，先派线做「原图人工抽检」判定哪个来源可信，再决定是否替换。这是本批唯一被主动卡住的项，宁可慢也不能上线可能错误的答案。
- **2026-09-15 02:22 环境坑（新）**：**子代理没有 Bash 工具**（spawn 后报 `Bash not found in agent general-purpose`），无法跑 python。凡是需要跑脚本的数据线，一律主会话自己做；子代理只能承担 Read/Write/Edit/Grep/Glob 类工作（改页面、改文案可以）。这是本轮 6 条数据线里 4 条空转的根因，务必记住。
- **2026-09-15 02:30-03:00 🚀 解析回填攻坚（新 T1，主会话自研工具链）**：
  - 工具：`tools/jx_match.py`（通用题本↔解析匹配器）+ `tools/apply_jx_patch.py`（独立校验+择优合并+落库）。
  - 三条匹配策略，按章节内容区分度自动选择：① 内容 DP（选项/题干 n-gram 命中率，章内 Needleman-Wunsch 保序）；② **答案序列 DP**（文本零区分度时用，如图形推理）；③ 全书 argmax 兜底。
  - **关键判定：答案一致率不作为打分项**，只作独立校验，避免自证。
  - 逐本结果：言语 1010/1010（99.9%）· 判断 925/940（**100%**，其中图形推理靠「答案序列 DP 找到 256 对全中，随机概率 4⁻²⁵⁶」证明同源）· 资料 959/959（77.5%）· 常识 861（88%）。
  - **独立抽查（不信汇报）**：`apply_jx_patch.py` 用「题干+选项 6-gram 在选中解析里的命中率 vs 同书随机解析基线」复核 —— 选中 0.145~0.256，基线 0.000~0.017，**信噪比 10~100 倍**，配对可信。并把子代理版与队长版逐题打分择优合并。
  - 落库结果：**4488 题中含解析 3765 条，覆盖率 0.4% → 83.9%**（言语100%/常识100%/资料100%/判断98%/数量1%）。node --check 通过。app.js 落库前已备份 `tools/app.js.bak-before-jx`。
- **2026-09-15 03:00 🚀 第五批部署上线（20260915b，纯前端不重启）**：2037 文件 / 80.4MB，远端 BACKUP_OK + EXTRACT_OK。版本戳 96 处 / 34 文件，无旧戳残留。新脚本 `tools/sync_bump_20260915b.py`、`tools/deploy_update_20260915b.py`。
- **🔴 新发现（重要，待用户/下批决策）：资料分析题本答案大面积可疑**。第4章综合资料题本答案与解析册仅 58.7% 一致、第2章图形资料 61.8%。但抽样显示**是题本答案错、解析册对**：id=4543 解析算得 68523÷129≈531=选项A，题本标 B；id=4486 解析算得 (301-203)/203≈48%≈选项D，题本标 C。已产出 187 条「解析自洽且内容配对可信」的答案修正建议（`patch_ziliao.json` 的 `ans_fix` 字段），**本批未动线上答案**（改动面大，先报后改）。
- **2026-09-15 03:05-03:12 ②进度卡片重构（队长主会话落地，子代理撞 429）**：
  - 现状诊断：12 张卡（行测5+申论+四级+内容库5）平铺，`group` 字段定义了却没用在布局上，也没接错题数据。
  - app.js：新增 `buildWrongTypeCounts()`（wrongQuestions 只存 id，一次建 EXAM_BANK 索引再回表统计题型错题，O(n) 而非 O(n·m)）；`computeHomeModules()` 给 5 个行测模块注入 `wrong`；新增 `homeModulesSummaryHtml()`（只汇总同量纲数据，绝不把「题+词+条」混加）；`renderModuleProgress()` 改为**按 group 分区渲染**（行测→申论→英语→内容库，每组带小标题与组内汇总），组内 auto-fill 自适应网格。
  - 学习工作台.html：新增 `.module-summary` / `.module-group` / `.module-group-head` / `.module-group-body` / `.module-wrong` 样式，卡片网格加第 4 行 `wrong` 区；窄屏降单列。node --check 通过。
- **2026-09-15 03:08 ④OCR 噪声清洗落地**：`tools/apply_ocr_and_ansfix.py` 应用 71 条规则（ocr-fix 线产出，均为「非法词→合法词」形近替换）。队长抽查 Top10 规则真实上下文逐条判读无误（`收人→收入` 254、`自已→自己` 138、`进人→进入` 76、`间题→问题` 17、`人境→入境` 12 等）。实际修正 **1543 处**（含解析正文）。
- **2026-09-15 03:10 队长裁决：资料分析 187 题答案已修正上线**。决策依据（三线独立取证）：① 队长人工复核 2/2（id=4543 解析算得 68523÷129≈531=A，题本标 B；id=4486 解析算得 48%≈D，题本标 C）；② 资料分析线人工复核 6/6 全指向「题本错、解析对」；③ 24 种选项置换检验排除「选项顺序错位」；④ 解析册内部自洽 214/215。修正条件极严：内容配对分 ≥0.30 且解析尾句与答案字段自洽。**常识判断 46 条同类建议暂不采纳**（一致率 88%、法律章仅 73.6%，无人工复核支撑，留待下批）。
- **2026-09-15 03:12 🚀 第六批部署上线（20260915c）**：进度卡片重构 + OCR 清洗 1543 处 + 资料分析 187 题答案修正。
  - 发现并修掉一个历史包袱：多轮 bump 叠加出 `?v=20260915caaa` 这类畸形版本戳 + 20260913j/o 残留（会让浏览器拿到旧缓存）。新增 `tools/fix_stamp.py` 用 `\?v=20[0-9]{6}[A-Za-z0-9]*` 全量规范化（398+416 处）。复查：残留只在 `blog_wechat.html`（该文件本就不上线），**线上 36 个页面版本戳全部统一为 20260915c**。
  - 备份：`tools/app.js.bak-before-jx`、`tools/app.js.bak-before-ocr`。
- **下一步（按优先级）**：① 数量关系 718 旧版答案复核（仍卡住，跨书键匹配不可靠）；② 常识判断 46 条答案修正（需人工复核支撑）；③ 资料分析剩余 126 题「配对分不足」的答案待定；④ 冗余清理（最后一步，含好友申请.html 孤儿页与 jx_patch 临时脚本）；⑤ push GitHub（需用户点头）。
- **线上题库现状（第四批后）**：原内置 + 数量关系 718（待验证）+ 言语理解 1010 + 常识判断 861 + 判断推理 940 + 资料分析 959 = **4488 道行测题**，另有申论 100 题。
- **2026-09-15 02:22 恢复执行决策（配额重置）**：自动化任务按 00:16 版清单要求重派 9 线，但队长核查文档后判定 **T1/T2/T3-T6/T8/T9 及 T14 在暂停前已全部完成并部署四批**，重派会造成重复劳动与文件冲突。按 00:09 全权委托，改为**按真实剩余项重排队列**，不重派已完成线。
  - 队长实测抽查（_probe4.js）：app.js 五本 INLINE BANK 在位 —— QUANT 718(id187-935,withX=20,optLT4=59) / JUDGE 940(2905-3844,withX=0) / ZILIAO 959(3905-4863,withX=0) / YANYU 1010(905-1914,withX=0) / CHANGSHI 861(1905-2765,withX=0)。
  - 🔴 **实测结论：4488 道行测题中仅 20 题有解析（x 字段），覆盖率 0.4%** —— 这是当前最大体验短板，升为新 T1。
  - 关键发现：**五本解析册 OCR 结构化其实已完成**（build_book_analysis.py 已产出全部 5 个 `_解析.json`：数量关系1116KB/言语1498KB/判断1547KB/常识1839KB/资料1330KB），缺的只是「解析 → 题本 → 已注入 id」的对齐回填。
- **新任务队列（本轮）**：T1' 四本解析回填（言语/常识/判断/资料，3770 题）→ T2' 数量关系答案可信度裁定+回填 → T3' 进度卡片重构 → T4' OCR 噪声清洗 → T5' 注入+bump+部署 → T6' 冗余清理（最后）→ push GitHub（需点头）。

---

## 二、用户第二轮需求（02:20 提出，本段为新登记）

| 编号 | 需求 | 状态 |
|---|---|---|
| N1 | 时政热点的弹窗改成完整页面（ADR-3 同类：核心内容不得用弹窗承载） | 待定位文件 |
| N2 | 图形推理的题没有图（名实不符，必修） | 排查中 |
| N3 | 真题模拟页面没办法滑动（布局 bug） | 待修 |
| N4 | 动态：在用户个人资料里加按钮，别人可由此看 TA 的动态 | 待修 |
| N5 | 用户发消息增加撤回功能 | 待修 |
| N6 | 导入文件导入题库有 bug，根本导不进；且要支持按导入文件内容自动建自定义题库 | 待修 |
| N1 | 时政热点的弹窗改成完整页面（ADR-3） | ✅ 完成（新建 时政热点.html 1295 行；入口已改 央国企笔试.html / 设置.html） |
| N2 | 图形推理的题没有图（名实不符） | 🔧 修复中（card-fix2，独占 app.js） |
| N3 | 真题模拟页面没办法滑动 | ✅ 完成（根因见下方「全站级根因」） |
| N4 | 动态：个人资料里加按钮看 TA 的动态 | ✅ 完成（feed-btn） |
| N5 | 用户发消息增加撤回功能 | 🔧 主体已落地，收尾中（recall-msg2，lite） |
| N6 | 导入题库根本导不进 + 自动建自定义题库 | 🔧 修复中（imp-fix2，lite） |
| N7 | 全项目排查 bug 并修复 | 待全部需求完成后执行 |
| N8 | **推送到 GitHub**（用户已授权） | 待执行（走 127.0.0.1:7897 代理，须 N7 之后） |

### 用户第三轮需求（本段新登记）

| 编号 | 需求 | 状态 |
|---|---|---|
| P1 | 点击播放提示"朗读服务暂不可用"（TTS 失效） | 🔍 根因已定，补丁落地中（voiceplayer.js 零冲突部分） |
| P2 | 阅读理解和翻译专项页面布局升级 | 🔧 进行中（cet-ui，assets/cet-read.js + cet-translate.js） |
| P3 | i人团伙 和 角色扮演 页面数据显示优化 | ⏸ 排队（本体在 app.js，等 card-fix2 释放） |
| P4 | 把角色扮演加入到 i人团伙页面 | ⏸ 排队（等 P5 抽出 assets/roleplay.js + app.js 释放） |
| P5 | 删除高情商表达页面里的"角色扮演训练"菜单 | 🔧 进行中（gq-fix：先抽成 assets/roleplay.js 再删菜单） |
| P6 | 内置 AI 无法回复，要知识更丰富、更智能 | ⏸ 待定位（疑似 chat-local.js / app.js，等占用释放） |
| P7 | 穿越英语页面假大空，持续优化丰富它 | 🔧 进行中（本体 = assets/quest.js，time-eng2 待重派） |

---

## 四、关键决策与发现（本轮新增）

### 🔴 全站级根因：`html, body { height:100%; overflow:hidden }`
- 位置：`assets/common.css` **L68**。
- 影响：全站所有页面默认禁止滚动。这就是 N3「真题模拟页面没办法滑动」的真正源头，**不止真题模拟一个页面**。
- 处置：mock-fix 采用页面级覆盖（mock_exam.html / mock_exam_run.html / mock_exam_result.html 加 `.mock-scroll-page` 类 + 弹层开合时记录/还原 body 的 overflow 原值），保守且不牵动全站。
- **是否把 L68 改成全局可滚动，列为 N7 全项目排查的重点评估项**（风险：大量页面依赖它做"内部容器滚动"，盲目放开会导致双滚动条/布局塌陷，需要逐页验证）。

### 🔴 P1 TTS 根因（已定性）
- 链路：voiceplayer.js `play()` → app.js `speakUtterance()` → `netSpeak()` → `shouldUseDictTts()` 把英语场景句判为"句子"，直接压到 Web Speech 分支；而该分支踩中 Chrome 三个经典坑：**voices 首次为空（未等 voiceschanged）、未指定 voice、未 resume**，且**没有 onerror**，utterance 被静默丢弃 → 兜底 toast 误报"朗读服务暂不可用"。
- 补丁分两部分：5B（只改 voiceplayer.js，零冲突，已授权落地）+ 5A（重写 app.js `speakFallback`，等 app.js 释放后执行）。
- 硬约束：补丁**禁用正则 lookbehind**（旧 Android WebView 会抛 SyntaxError 直接废掉整个文件）。

### ⚠️ 协作事故：card-fix 改错工作树
- 该成员判定 main-6599a366「不存在」（实为用 bash 探测路径失败——本机 bash 缺 coreutils），把进度卡片重构写进了滞后的 main-f0596da4，**代码不会上线**。
- 处置：已叫停、禁止它再写文件、未回滚（不影响线上）；其 6 点分析（分母写死 / examTypeProgress 丢记录 / 薄弱点阈值 / markAsViewed 不写日志 / 误用 window.lucideIcon / 注册表只覆盖 5 模块）已提炼为验收清单转交 card-fix2 在正确的树上落地。
- 规则重申：**判断文件是否存在只能用 Read/Grep/Glob，禁止用 bash 的 ls/test。**

### 429 限流处置
- tts-diag（3s）、recall-msg（7m）、imp-fix（7m15s）、time-eng 先后撞账号级 429。
- 已用 `node --check` 体检 8 个关键 js（chat-local.js / voiceplayer.js / importer.js / quest.js / app.js / cet-read.js / cet-translate.js / hotnews.js）——**全部语法完好**，无限流造成的半成品污染。
- 按限速 SOP 降档 lite 重启关键线（imp-fix2、recall-msg2、img-fix、ipartner-fix、time-eng2、cet-ui2、gq-fix2）。

### ✅ 数量关系答案修复成功（quant-fix2，长期卡点被攻克）
- 此前卡住的原因：跨书键匹配命中率仅 11.6%、一致率 53.7%（≈随机），无法判定题本与解析册谁对，因此 822 修正版一直不敢上线。
- **真根因**：答案速览是「双栏 + 通栏标题切段」版式，而原脚本按全局 y 排序扫描，导致答案块整体错位（表现：D 选项少 101 个）。
- 修复三处（`_ocr_work/build_book_questions.py`）：① 新增 `ak_reading_order()`（按 x 中心分左右栏 → 栏内按 y 归并成行 → 以「第N章/第N节」通栏标题为切点 → 段内先左栏后右栏）；② 答案写入改 `setdefault` 先到者为准；③ 匹配步骤改「章号+节号+考点号+难度」四元组规范化匹配。
- **结果：一致率 38.0% → 99.5%；报告点名的 17 个干净小节 181 题 27.1% → 100%；D 项倍率 0.54 → 0.83，与 A/B/C（0.78/0.84/0.79）拉平，缺失 101 个 D 的指纹消失。**题本 822 题 / 答案匹配 813。
- 队长裁定（三项）：① 接受「跨栏不重置上下文」（实测右栏顶部大量是左栏续块，重置会整块丢失）——要求把理由写成注释留存；② 加 A–D 白名单，非 A–D 与空答案**置空并打标记留痕，绝不硬猜**；③ **题量缺口 819 vs 1004 本轮不补**——"少"不等于"错"，先换掉线上答案存疑的 718 题，补题（需改 `is_section_title` 认子块标题）单独立项排后。
- 后续动作：A–D 白名单落地后，由队长用 `tools/inject_book.py` 把 819 题注入替换 INLINE_QUANT_BANK（**须等 app.js 上的 img-fix 释放**）。

### ⚠️ 工具陷阱：Glob 在中文路径下会误报"文件不存在"（本轮差点酿成误判）
- 现象：`Glob("assets/roleplay.js")` 与 `Glob("assets/*roleplay*")` 在中文工作树路径下**连续两次返回 No files found**，但用 Read 直接读该路径，文件好端端存在（388 行）。
- 后果：我据此一度判定"角色扮演实现代码已被删除且 roleplay.js 未落盘 → 功能丢失"，差点下令回滚。
- **规则：判断文件是否存在，一律用 Read 直接读路径；Glob 只用于枚举、且其"零结果"必须二次确认。**
- 同源旧坑：本机 bash 缺 coreutils（ls/grep/test 全 not found），用 shell 判断文件存在必然失败。两条加起来：**本项目中"文件不存在"的结论必须由 Read 证实，不能由 Glob 或 shell 证实。**

### 本轮已完成项汇总（截至本批部署前）
| 编号 | 成果 | 关键数字 |
|---|---|---|
| N1 | 时政热点弹窗→独立页 | 新建 时政热点.html 1295 行，验收 25/25 |
| N2 | 图形推理没图 | 根因：app.js L5397 题干用 `textContent = q.q`，`[IMG:xxx]` 当纯文本显示；新增 `xtRenderQText`（L5428）+ 6 处渲染点改 innerHTML；判断推理 940 题中 281 题题干带图，抽查图片真实存在；验收 30/30 |
| N3 | 真题模拟不能滑 | 根因 common.css L68 全站 `overflow:hidden`；三页页面级覆盖 + lockScroll/unlockScroll 计数还原 |
| N4 | 个人资料动态按钮 | 后端 `/api/moments/user/{id}` 服务端过滤，含空态与顶栏 |
| N5 | 消息撤回 | 闭环完整；修掉"本地消息 id 重排导致撤回标记匹配不上"的真 bug；验收 21/21；**跨端撤回需后端接口，当前只本端生效（诚实标注）** |
| P1 | 朗读报"暂不可用" | Chrome 三坑全踩；队长在 voiceplayer.js 落地安全播放层；**禁用 lookbehind**（旧 WebView 会 SyntaxError 废文件） |
| P2 | 阅读/翻译布局升级 | 根因 data-cet-read.js `source` 全为"未知来源"（100% 废数据），改为按篇章推断；104 项断言全过，逻辑层 0 改动 |
| P3 | i人团伙显示优化 | 18 人格 / 9 场景 / 845 条回复；修 5 个真实显示 bug |
| P5 | 角色扮演抽离 + 删菜单 | roleplay.js 388 行（mountRolePlay/openRolePlay），高情商表达.html 菜单已删、div 配平 164/164、jsdom 33/33 |
| P7 | 穿越英语充实 | 36 → 272 条（7.6 倍），16 关，补易错点/文化提示/通关小测，去 emoji |

### 🚀 部署记录（20260915b，第五批）
- 时间：2026-09-15 03:12（远端文件时间戳）；耗时 2m04s。
- 内容：**2039 个文件 / 84,362,956 bytes（84MB）**，含本轮全部前端改动 + 行测切图。
- 校验：关键文件断言全部在位 → 上传 exit 0 → 远端 `BACKUP_OK` + `EXTRACT_OK` → 远端 exit 0。
- 工具链：`tools/sync_bump_20260915b.py`（worktree→部署源，只同步 html + assets，绝不碰 server/tools）+ `tools/deploy_update_20260915b.py`（ROOT 铁律锁定部署源，远端先备份 web 目录再解压覆盖）。
- **本批上线**：N1 时政热点独立页 / N2 图形推理图片 / N3 真题模拟滚动 / N4 资料页动态按钮 / N5 消息撤回 / P1 朗读修复 / P2 阅读翻译布局 / P3 i人团伙显示 / P5 角色扮演抽离+删菜单 / P7 穿越英语扩充。
- 未上线（下一批）：P4 角色扮演并入 i人团伙（进行中）、P6 内置 AI 本地知识库（进行中）、N6 导入题库（待确认）、数量关系 819 题注入（等 A–D 白名单）。

### 本轮已完成项汇总（第二批：P4 / P6 / N6）
| 编号 | 成果 | 关键数字 |
|---|---|---|
| P4 | 角色扮演并入 i人团伙 | 入口在伙伴选择页「进阶实战 · 角色扮演」，走 `mountRolePlay(#ipContent, {backTo,onBack})`，**零复制 roleplay.js 代码**；三重降级（函数守卫 + 3s 轮询 + 可读降级卡）；jsdom 18/18 |
| P6 | 内置 AI 本地知识库 | 7 条规则 → **37 意图 / 101 条回复**；同义词归一 7 组、加权打分、意图优先级、上下文追问（aiLastTopic 定长对象）、候选轮换；26 条输入验证 20/20 + 轮换/上下文断言全过。ai-brain2 补强 3 处：面试意图漏召回、追问判定过宽（"量子力学"被当续写）、"再见我去刷题"被行测意图抢走 |
| N6 | 导入题库 | 根因：`工具.html` 未加载 qbank.js 却裸调 `__qbImportRaw` → ReferenceError 被吞 → 点导入无反应；且前任写的 `upsertImportBank` 从未被调用。现按文件名自动建「自定义·xxx」库，localStorage `study_workbench_imports` |
| 附加 | APK 白名单 | `roleplay.js` 补进 build_apk.py / build-apk.ps1 / build-apk.sh 三处 REQUIRED_ASSETS |

### 🚀 部署记录（20260915c，第六批 / 收尾批）
- 内容：**2039 个文件 / 84,391,516 bytes**，含 N7 排查修复 + P4/P6 成果。
- 校验：上传 exit 0 → 远端 `BACKUP_OK` + `EXTRACT_OK` → 远端 exit 0。
- 本批上线：P4 角色扮演并入 i人团伙、P6 内置 AI 知识库、N6 导入题库、N7 全项目排查修复。

### 🔍 N7 全项目排查结果（用户点名的最后一道工序）
| 类别 | 结论 |
|---|---|
| 原生弹窗 | **53 处 → 0 处**（html 24 + js 29）。统一复用项目已有的 `uiConfirm/uiAlert/uiPrompt`（app.js），未另造轮子 |
| 页面滑不动 | **新发现并修复 4 页**：AI模拟面试（注释写着"恢复滚动"却只加了 overflow-x）、学途、好友申请、登录。累计这条 common.css 全局锁共坑了 7 页 |
| 正则 lookbehind | app.js 2 处清零。**node --check 查不出**（Node 22 支持），必须靠 grep——旧版 Android WebView 遇到会整文件抛 SyntaxError，属全站白屏级隐患 |
| 悬空引用 | 61 个 assets 引用逐个校验，真实悬空 **0**（唯一报缺失的 mock-exam.js 在注释里） |
| 图片 404 | 591 个 `[IMG:]` 标记，**真实缺失 0**（6 个"缺失"全是 app.js 自己的注释与正则字面量） |
| 死链 | onclick 未定义函数 **0**；location.href 指向不存在页面 **0**；IIFE 未挂 window **0** |
| 窄屏溢出 | 0 处需修 |
| 并发写残留 | xtRenderQText / computeHomeModules / 题干渲染点 3 处关键改动全部在位 |

### 🛠 N7 之后队长亲自修的两个真 bug
1. **`editorAiAssist` 重复定义**：app.js（支持服务商直连 + 本地演示兜底）与 api.js（仅后端）同名。`学习博客.html` L428 app.js → L430 api.js，**后加载的 api.js 覆盖了更好的那版**，导致无后端/无密钥时「AI 辅助写发贴」直接失败。已将 api.js 版改名为 `editorAiAssistViaApi`，不再抢占全局名。
2. **穿越英语入口长期失效**：`blog_wechat` / `个人中心` / `更多` / `管理员` 4 页都写了 `if(window.openQuest){openQuest()}else{跳转}`，但**都没引入 quest.js** → 永远走降级跳转，从没真正打开过面板。已给 4 页补引 `assets/quest.js?v=20260915c`（置于 app.js 之后）。

### 🧹 冗余文件清理（用户指定：全部任务完成后才做）
- 工具：`tools/cleanup_temp_20260915c.py`，**硬约束：只删 git 未跟踪的文件**（已入库的一律不碰）。
- 结果：两棵树共删除 **47 个临时文件**，另 3 个 84MB 部署包 tar.gz 已不在（约 250MB 空间）。
- 保留：`tools/jx_*.py`、`tools/apply_*.py`、`tools/inject_*.py` 等真实工具脚本；以及 22 个**已被 git 跟踪**的 `_` 开头文件（如 `_r01_test.py`、`_gen_exam.py`），因有版本管理不删。

### 📦 N8 推送 GitHub（已完成）
- 仓库：`github.com/Li-jiu0/study-workbench`，分支 `main`。
- 提交 1：**`d629946`**「批次八收尾：12 项需求交付 + 全项目 bug 排查修复」—— 1971 files changed（含 时政热点.html / 申论刷题.html / assets/roleplay.js / assets/data/shenlun-* / **assets/images 1906 张切图**）。
- 提交 2：**`000bbba`**「chore: 补充批次八使用的部署/同步/推送/清理工具脚本」。
- 网络：`git push` 走 `http://127.0.0.1:7897`（Clash）——默认 env 代理 62083 推大传输必断，直连被墙。
- 复核：远端 `refs/heads/main` = 本地 HEAD = `000bbba`，一致。
- **未推送**：`server/routers/groups.py`、`server/routers/uploads.py`、`server/schemas.py` 3 个后端改动只存在于 worktree 分支（`workbuddy/main-6599a366`），`main` 分支 `git status -- server/` 为空，故未纳入本次推送，留待用户决定。
- **坑**：PowerShell 捕获 git 输出会乱码（"AI模拟面试.html" → "AI妯℃嫙闈㈣瘯.html"），据此判断文件是否入库会误判成 MISS。一律改用 Python subprocess 取 git 输出。

### 🏁 批次八状态：全部需求交付完毕
12 项需求（N1-N6 + P1-P7 去重后）+ N7 全项目排查 + N8 推送，均已闭环。剩余可选项：数量关系 819 题注入（答案一致率已由 quant-fix2 修到 99.5%，但题干仍缺 185 题，需动 `is_section_title` 补题，未经用户确认未做）。


## 2026-09-15 08:40 申论入口迁移 + 行测筛选下拉化（20260915d 已上线已推送）
- 用户指令①：侧边栏「申论刷题」菜单删除，入口并入央国企笔试备考页 → 24 个 HTML 侧边栏菜单块移除（正则批量，24/24 命中），央国企笔试.html feature-grid 在时政热点卡后新增申论刷题卡。
- 用户指令②：行测刷题.html 题型筛选两行标签 → 单个下拉列表（ecTypeSelect），「我的题库/随机刷题」按钮保留；filterExamType（app.js L5763）未改动，页内 DOMContentLoaded 包装实现下拉框与筛选状态双向同步，免 bump ?v=。
- 部署 tools/deploy_update_20260915d.py：25 个 HTML 增量包 192KB，BACKUP_OK/EXTRACT_OK；线上直连复检 5/5 通过（下拉框 YES、央国企卡片 YES、更多.html 侧边栏残留 NO）。
- 提交 05ef596（26 文件 +130/-81）已推送 GitHub，远端 main=05ef596。代理端口再次漂移（58239→52588），自动探测脚本生效。
- quant-fix2 曾因网络 502（ENOTFOUND copilot.tencent.com）中断，已重新唤醒继续数量关系 A 子任务（白名单+空答案处理），截至本条未回报。
- 环境补充：线上复检 110.42.134.62 是国内 IP，直连即可，不要走代理（走代理反而会被断连的本地代理坑）。


## 2026-09-15 09:12 全项目 bug 排查与修复（20260915e 已上线已推送）
三份只读排查报告：排查报告-20260915-JS.md / -HTML.md / -UI.md（已随包提交）。

【主会话亲自修复 · 阻断级】
1. app.js 14 处可选链 ?.（5121/5127/5188/5194/5269/5275/5381/5388/5506/5512/5646/5651/5676/5707）→ 改写为 (X || []).includes(...)/.length。老 Android WebView 解析期 SyntaxError 会让 APK 全站白屏。剩余 0，node --check 通过。
2. app.js 5 处未判空 DOM 取值：topbarTitle(L1312)、toggleMorePanel/closeMorePanel/toggleToolsPanel(#morePanel+.more-overlay)、greetingText(L1389，27 页无此节点会打断 renderHome)、switchExamMode 三个 examMode*、loadBlogEditor/updateEditorPreview(beCat/blogEditorInput/blogEditorPreview)。app.js 全站共用，缺节点页面抛错会打断后续全部脚本。

【5 条并行 ADR-3 改造线（adr-1~5，均经验证）】
- 学习工作台.html：#countdownModal、#xtTaskModal → 页面内展开区块（div 183/183，11 个 id 全保留）
- 个人中心.html：#profileEditModal → .pe-inline 页面内区块（div 257/257，31 个 id 全保留，logoutConfirmModal 未动）
- PPT训练.html：#pptWorkModal、#pptWorkView → 页面内展开 + 视图切换（div 182/182，17 个 id 全保留，拦截 openAppModal 只切 .active）
- 设置.html：#stPwdModal → .st-pwd-panel（div 489/489，5 id + 3 函数 + 2 校验函数全保留，验证器 A6 八项全 PASS）
- 私聊.html：#imGroupSettingsModal、#acPanel → 页面内视图（div 164/164，id/函数全保留，imGroupModal/imAddFriendModal 未动）

【排查通过项】48/48 js 语法、lookbehind 0、原生弹窗 0、7 页滚动锁仍在、591 图片标记 0 真实断链、窄屏无溢出、20 个跳转目标全存在、20 个 onclick 全有定义。

【发布】app.js 版本戳 31 页 bump 至 20260915e；部署 32 文件（235KB→实际 2.35MB 含 app.js），BACKUP_OK/EXTRACT_OK，线上 6 项校验全中（可选链 0、五页新区块均在）；提交 789ec80（36 文件 +1263/-345）已推送。

【遗留待办】
- 25 个页面残留休眠 #countdownModal DOM（无用户入口，sw-ui 判低风险）→ 留到最后清理阶段处置
- assets/ 下 3 个 .bak 备份文件含 lookbehind（不在打包白名单、无引用）→ 清理阶段移出 assets/
- 根目录 .bak 旧文件、tools 临时文件 → 清理阶段（用户铁律：最后一步）
- 数量关系 819 题（quant-fix3 进行中）；common.css L68 全局滚动锁仍未动（需浏览器实测 36 页）


## 2026-09-15 17:36 批次九需求登记（**未开工**）
- 用户下达 8 项新需求：N9-1 修常识判断筛选 / N9-2 判断推理 type 匹配 / N9-3 全局消息通知（轮询+toast+跳转）/ N9-4 个人中心作品集展示 / N9-5 工具页填充或隐藏 / N9-6 更多页内容填充 / N9-7 app.js 拆分优化 / N9-8 筛选标签交互优化。
- 用户明确要求「**我不说开工你就不要自己执行**」→ 本会话仅做只读摸底 + 登记，未改任何业务代码、未部署。
- 登记位置：`交接文档-批次九-20260915.md` 第 10 节（含每项落点、风险、待确认项）。
- 同时回写三处实测校正：① P0 题本「元数据全空」描述不准（实为 `point` 字段被 OCR 括号噪声污染）② **漏报选项残缺 —— 153/1051（14.6%）含空选项、完全可用题仅 842 条** ③ `blog_wechat.html:325` 版本戳畸形（`20260915eaaa`）。
- 补充：12 个 worktree 全部指向 D: 的 .git；两树 app.js 差 8761 字节纯属 CRLF/LF；执行文档 09:12 后无日志（8 小时断档）。
- 基线未变：HEAD `789ec80`，线上 `20260915e`，GitHub `main` 已对齐。


## 2026-09-15 17:50 新增 N9-9：全站页头返回按钮补齐（**未开工**）
- 用户看截图反馈「很多页面没有回退方式」→ 只读盘点全站：36 个 HTML 中**仅 3 个**有 `xt-page-back`（企业定向库=固定跳央国企笔试【标准范式】、mock_exam / mock_exam_result=goBack()），**33 个无页头返回**（A 类带侧边栏子页 26 个 + B 类无侧边栏独立页 7 个）。
- 部分页面靠页内散装「返回四级 / 返回题库」按钮补偿，位置不统一。
- 详细清单与实现要点已写入 `交接文档-批次九-20260915.md` 第 10.2 节。登记为主，未改代码。


## 2026-09-15 17:56 用户指定返回基准 + 新增 N9-10（**未开工**）
- 用户指定：以「听说训练 · 听力精听」面板（`assets/voiceplayer.js` 注入的 `#vpMask`，从四级备考页进入）左上角的 **`← 返回` 顶部栏**（L234 `vp-back`）作为全站返回按钮的基准样式 → N9-9 的实现要点已更新（视觉照搬 vp-back；行为上，面板类=`__close()` 关面板，普通 HTML 页=固定 `location.href` 跳上级，禁 history.back）。
- 新增 **N9-10**：听力精听顶部横向滚动的场景标签条（`.vp-scene`，L172-174/231/241，数据源 `SCENES` L33 起）改为**分类列表**（分组平铺、不再横滚）；改 JS 需 bump `voiceplayer.js` 版本戳；保持 `SCENES` 结构与 `__scene(k)` 回调签名不变。
- 详细落点见 `交接文档-批次九-20260915.md` 第 10.4 节。仅登记，未改代码。


## 2026-09-15 18:00 新增 N9-11：四级阅读理解页布局 bug（**未开工**）
- 用户截图反馈「阅读理解」页（cet-read.js 渲染面板，选词填空 tab）布局异常：正文窄条 + 右栏大面积空白卡片 + 正文栏内滚动截断。
- 只读定位根因：`assets/cet-read.js` L135–147 —— 宽屏 media query 双栏 `1fr/1.05fr`，右栏 `.cr-col-quiz` 作答前只有空 `#crRes`；左栏 sticky+max-height 内滚。
- 修复方向三选一（右栏放答题卡 / 调断点栏宽 / 去 sticky 单栏化）已写入交接文档 10.5 节；三种题型（L258/L330/L402）共用结构需一起改；改 JS 需 bump cet-read.js。仅登记，未改代码。


## 2026-09-15 18:04 新增 N9-12：PPT版式库升级（豆包需求文档，**未开工**）
- 用户提供豆包撰写的《PPT版式库升级需求文档（详细执行版）》，要求记录。全文已原样落盘：`需求文档-PPT版式库升级-豆包-20260915.md`（部署源根目录）。
- 需求概要：卡片网格 + 真实预览图（`<img>`）+ 收藏（localStorage `ppt_favorites`）/ 预览遮罩（✕/点空白/ESC 三关闭）/ 下载（`<a download>`，无文件显「制作中」）三按钮 + 分类标签筛选 + 实时搜索 + 空状态；学习进度移右上角小字。验收 12 条。
- 差距核实：现渲染器 `tpl-preview.js` 用 CSS 画骨架无真图；`previewImage`/`pptFile` 字段与 15 张预览图、.pptx 文件**均不存在** → 资源生产是前置依赖。
- 适配注意：emoji 图标 → 全站 `data-icon` 体系；遮罩复用 `openAppModal`；保持 ADR-1 后置覆盖约定；老 WebView 语法禁令 + bump 版本戳；file:// 下 404 探测不可靠需定策略。
- 详见 `交接文档-批次九-20260915.md` 第 10.6 节。仅登记，未改代码。


## 2026-09-15 18:08 新增 N9-13：PPT案例拆解改进（豆包锐评，**未开工**）
- 用户转交豆包《PPT案例拆解改进》锐评：与 N9-12 同病（配图是 CSS 假线框图，非真实案例）。原文落盘：`需求文档-PPT案例拆解改进-豆包-20260915.md`。
- 只做 3 件：① 假图换真实截图（`<img>`，占位「案例图待补充」，目录 assets/images/ppt-cases/）② 加「查看大图」遮罩（✕/点空白/ESC）③ 加「收藏案例」⭐（localStorage，顶部「我的收藏」筛选）。不做下载/应用到作品；文字拆解保留。
- 关键风险：本页渲染**直接写在共享 `assets/app.js`**（L4217 数据 + L5323 渲染），不像版式库有独立 tpl-preview.js → blast radius 大，必须零现代语法 + DOM 判空 + bump app.js 戳。
- 详见 `交接文档-批次九-20260915.md` 第 10.7 节。仅登记，未改代码。


## 2026-09-15 18:15 新增 N9-14：PPT训练课程改进（豆包+GPT 综合，**未开工**）
- 用户综合豆包+GPT 给《PPT训练改进需求（综合版）》，原文落盘：`需求文档-PPT训练改进-综合-20260915.md`。
- 定位：现 80% 看课，往"训练"升级。分三期：P0×5（正反例对比图/标记已学+进度条/小测正反例做成入口/正文卡片化/导航栏去橙色）、P1×2（找茬实操题/评分解析）、P2×1（能力雷达图闭环）。
- 改造对象：`PPT训练.html` + `assets/data-ppt-tips.js`（四原则课程数据）+ `assets/ppt-tips.js`（渲染器）。现数据已含每节选择题。
- 注意：对比图/正反例图前置资源；emoji→data-icon；P1 找茬/P2 雷达图受老 WebView 语法禁令（禁 ?./??/replaceAll 等，不引第三方库）；改 JS bump 戳；文字保留。
- 详见 `交接文档-批次九-20260915.md` 第 10.8 节。仅登记，未改代码。

### 2026-09-15（续记·批次九接手会话）
- **N9-15 登记（仅登记、未执行）**：广场·我的文章页布局 bug。根因 `blog_wechat.html #blogViewMine` 的 `#blogMineGrid` 复用 `.blog-grid`（`assets/common.css` L1383 `repeat(auto-fill,minmax(300px,1fr))`），且 `.note-card`（L1384）无 `max-width`；个人页通常仅 1–3 篇，单卡被拉伸到容器满宽，观感失衡。已写入 `交接文档-批次九-20260915.md` 第 10 节表（N9-15 行）+ 10.9 小节，待用户「开工」再修。

### 2026-09-15（续记·批次九接手会话）
- **N9-16 登记（仅登记、未执行）**：「联系管理员」入口只在「好友」tab 显示。实测 `私聊.html` 的 `#acEntry`（L123）为静态节点、刻意置于 `#imList` 之外，**在全部 5 个 tab 下常驻可见**（截图即其出现在加好友 tab 之上）；页内 `patchSwitchTab()`/`showPanel()`/`hidePanels()` 均未控制 acEntry。已写入 `交接文档-批次九-20260915.md` 第 10 节表（N9-16 行）+ 10.10 小节。风险：隐藏整行会连带藏掉 R43 管理员来信未读角标（admin-contact.js L211-224）。待用户「开工」再改。

### 2026-09-15（续记·批次九接手会话）
- **N9-17 登记（仅登记、未执行）**：消息撤回改为微信式「长按弹菜单」。实测现状：撤回功能已存在（批次八），但形态是「桌面 hover 撤回按钮 + 移动端 500ms 长按→直接确认气泡」，没有微信那种长按弹出操作菜单。改造=新增 `imShowMsgMenu` 浮层（非模态，ADR-3 合规），移动端长按 / 桌面右键触发，菜单含撤回(复用 imDoRecall)+复制/转发/删除本端/收藏；复制走 navigator.clipboard + execCommand 兜底。已写入 `交接文档-批次九-20260915.md` 第 10 节表（N9-17 行）+ 10.11 小节。改 chat-local.js 需 bump 私聊.html L341 戳 20260915c。

### 2026-09-15（续记·批次九接手会话）
- **N9-18 登记（仅登记、未执行）**：听说训练·听力精听全面改版（综合版，用户综合 GPT 思路）。需求文档全文落盘 `需求文档-听力精听改进-综合-20260915.md`（部署源根目录）。分三期：P0×5（场景两级分类/卡片放大去模糊/按钮一行+原文中文开关/进度条+查看全文/AI助教去重挂钩）、P1×3（三阶段训练闭环听→理解→说/训练状态反馈/精听挑战双模式）、P2（听写/辨义/综合训练类型）。**吸收原 N9-10 场景分类需求**。改造落点 `assets/voiceplayer.js`（SCENES/`.vp-scene`/vp-back 基准）+ 听说训练页；开工前需补语音播放器内部精读。已写入 `交接文档-批次九-20260915.md` 第 10 节表（N9-18 行）+ 10.12 小节。

### 2026-09-15（续记·批次九接手会话）
- **N9-19 登记（仅登记、未执行）**：模拟面试页面重构（设计稿驱动）。用户提供画布设计稿（Ardot 两屏：回答中/已点评）+ PDF `C:\Users\ATM\Documents\模拟面试页面重构.pdf`。用户判断：页面信息架构失败（三主操作平级/双AI人格打架/提交无反馈）。设计稿落地 6 件事（单主按钮/语音文字Tab/猫头鹰降级40px/删AI美女/删演示球/提交反馈三维评分）+ 两点补充（五段状态条 读题→准备→回答→提交→点评 / 查看示范回答降级为题目卡次级按钮）。砍掉居中猫头鹰/助手/演示球/音频样式条。改造对象 `AI模拟面试.html`（仅引 config.js+icon-map.js，逻辑页内）+ 关联 `assets/iv-prep.js`。已写入 `交接文档-批次九-20260915.md` 第 10 节表（N9-19 行）+ 10.13 小节。

### 2026-09-15（续记·批次九接手会话）
- **N9-20 登记（仅登记、未执行）**：学习数据页面调整：「本机学习数据」→「学习报告」。用户提供实用方案，需求原文落盘 `需求文档-学习数据页面调整-20260915.md`。核心：P0=改名「学习报告」+空状态(不显0)+移除3按钮(错题本/四级词汇/偏好设置)；5区块=核心指标卡(P0)/近7天时长柱状图(P1)/行测各模块表格(P1)/各模块时长环形图(P2)/最近学习记录(P1)；数据=activityLog+EXAM_BANK；不做的=AI分析师/目标完成度/能力雷达图/导出PDF。落点 `个人中心.html` L266-274(`#localStats`/`renderProfileOverview`)+`assets/api.js` L1297/1377「学习数据」卡+L1315-1321「学习模块」入口按钮。已写入 `交接文档-批次九-20260915.md` 第 10 节表（N9-20 行）+ 10.14 小节。待办共 20 项（N9-1~N9-20），全部未执行。

### 2026-09-15（续记·批次九接手会话）
- **N9-21 登记（仅登记、未执行）**：写发帖页面改进（综合版·做减法）。用户综合 GPT 评价提供「聚焦做减法」方案，需求原文落盘 `需求文档-写发帖页面改进-综合-20260915.md`。定位：三页面里完成度最高但功能堆太多，反向砍/藏/简化。落点 `学习博客.html`（快速模板 L204-212 / 基本信息6字段 L214-248 / 富文本工具栏10按钮 L255-272 / AI辅助写发贴 L290 / 草稿恢复条 L197-202 部分已实现 / 顶部4tab L166-169）+ `blog_wechat.html`（封面emoji或图片URL L137 / 截图插图base64 L148）。P0=渐进式填写+删技术细节+顶部导航简化(返回+发布)+编辑器简化(工具栏10→5-6)+写作区放大；P1=快速模板收起+AI辅助写作场景绑定+发布前预览；P2=视觉层级+草稿自动保存(复用 ed-draft-bar)。已写入 `交接文档-批次九-20260915.md` 第10节表（N9-21行）+ 10.15 小节。

### 2026-09-15（续记·批次九接手会话）
- **N9-22 登记（仅登记、未执行）**：学习概括页面整合。用户提出删掉散落多页的「学习概括」卡片、做 ONE 综合实时分析页、个人中心加跳转按钮，需求原文落盘 `需求文档-学习概括页面整合-20260915.md`。⚠️ 关键歧义：代码无「学习概括」字面量(Grep 0 命中)，疑=散落「学习统计/学习数据/数据总览」卡——30+页底部导航 `openBlogStats()` 按钮 / 学途.html L536 / 更多.html L104 / 设置.html L655 数据总览 / 个人中心 L272 本机学习数据；现有聚合入口 `openBlogStats()`→学习博客.html#stats、`renderBlogStats()`(api.js L761)。与 N9-20 关系需定(合并/并存，倾向 B)。已写入 `交接文档-批次九-20260915.md` 第10节表（N9-22行）+ 10.16 小节。待办共 22 项（N9-1~N9-22），全部未执行。

### N9-22 修订登记（2026-09-15 18:57 · 合并终版，取代 N9-20）
- 用户截图澄清后修正 N9-22 口径：把「学习概括」综合页 + 个人中心「本机数据」合并成 ONE 综合页；删「本机数据」菜单和页面；新建页入口加到「更多」页；把「学习统计」功能并入该页并删「学习统计」菜单。
- 已只读核实落点：个人中心.html L106-108(本机数据菜单卡)/L269-276(subpage local-data)/L485/L823/SubpageRouter；更多.html L102-106(学习统计卡)/L164-166(旧弹层)；30+页底部导航 openBlogStats() 学习统计按钮。
- 需求文档已重写为「需求文档-学习概括页面整合-20260915.md」合并终版；交接文档 10.16 已重写、10.14 标 N9-20 仅存档、表格 N9-20/N9-22 行已更新；表头注明实际待办 21 项（N9-20 仅存档）。
- 待确认：综合页承载(独立HTML倾向A/复用博客B)、30+页底部统计按钮删vs改指向、#morePanel去留、renderProfileOverview弃用否。
- 全部仅登记，未动代码（用户铁律「不说开工不要自己执行」）。

### N9-23 登记（2026-09-15 19:05 · 每日一题卡改造·豆包方案A·实用版）
- 用户转发豆包整改建议（方案A），要求记录。改造对象：商务礼仪面试.html 的「面试题库·四分类」题卡 #ivQuizCard(L101-125)。
- 方案：题卡改「今日一题·{分类}」+右上进度「已答 X/Y」+题下 textarea 答题框+三按钮(换一题/提交回答/查看要点)；删「开始模拟面试」按钮；提交存 localStorage(interview_daily_answers)+进度+1+置灰。
- 已只读核实落点：IV_BANK(L642-655,12题 {cat,q,tip})、IV_CATS(L656)、ivQuizNext/ivQuizShowTip/ivQuizCat/pickQuestion(L666-687)、#ivAiPanel(L113-124)。全部本页内联，未分离assets。
- 落点核实发现2处冲突(待确认)：①删开始模拟面试按钮后 #ivAiPanel 面板成孤儿→一并隐藏；②实际要点是单段 tip 字符串非 keyPoints 数组→直接用 tip。
- 需求文档已落盘：需求文档-每日一题卡片改造-豆包-20260915.md；交接10.17小节+表格N9-23行已加；表头改23项(实际待办22项)。
- 全部仅登记，未动代码（用户铁律「不说开工不要自己执行」）。

### N9-24 登记（2026-09-15 19:09 · 万能金句库升级·豆包 A+B+C）
- 用户转发豆包两份建议（加功能+扩内容 / 网络金句API），要求记录。改造对象：万能金句库.html(L95-96) + 共享 assets/app.js。
- A 加功能：遮罩表单加自定义金句→localStorage custom_quotes+可删可改；B 扩 18→80+句(68句新增)；C 🌐网络金句(hitokoto.cn fetch+收藏)。
- 落点核实发现7处差异/风险：①共享app.js高风险(31页共用,必bump戳)；②自定义缺icon/id字段(commIconHtml会空)；③分类名不符(实际10分类:表达不同意见/被甩锅/万能开场,豆包写8分类且"不同意见"应为"表达不同意见")；④来自网络新分类；⑤老WebView无fetch须XHR兜底；⑥CORS/离线toast；⑦L96静态"18句直接套用"须改。
- 需求文档已落盘：需求文档-万能金句库升级-豆包-20260915.md；交接10.18小节+表格N9-24行已加；表头改24项(实际待办23项)。
- 全部仅登记，未动代码（用户铁律「不说开工不要自己执行」）。

### 开工阻塞项决策定稿（2026-09-15 19:27 · 用户授权主理人拍板）
- N9-24 分类方案：沿用实际 10 分类（不删已上线内容）。豆包68句「不同意见」→重映射「表达不同意见」；被甩锅+7、万能开场+7 → 各8句；总量 18→100 句。理由：删分类=删已上线内容且破坏用户已学进度数据(appData存内置id)。
- N9-24 fetch 兜底：XMLHttpRequest 单路径，不用 fetch/async-await（老WebView API<21无fetch且CORS不稳）；try/catch+onerror+ontimeout(8s)→toast不崩；换一句1s防抖；不走后端。遗留L3真机验证 file:// 下CORS，失败则APK内隐藏按钮。
- N9-22 30+页底部学习统计按钮：删除（方案①），但 openBlogStats() 保留并重定向到新综合页作兼容别名（防漏网引用ReferenceError白屏=最大风险点）；更多.html L102-106改「学习概括」入口、L164-166删；renderBlogStats()不动。
- N9-22 综合页承载：定为独立新HTML（方案A），入口在更多.html L102-106。
- 决策已同步至两份需求文档 + 交接文档10.16/10.18小节。仍未开工，等用户指令。

---

## N9-25 · 个人中心精简 + 博客统计入口修复（用户 4 截图反馈）

**登记**：2026-09-15 17:35 ｜ **执行**：全线开工后并入 Wave 1 收尾

### 用户 4 条要求与定位
1. 个人中心删「本机数据」卡 → `个人中心.html:106-110`
2. ~~个人中心删「发贴统计」卡~~ → **用户 17:38 确认误判，不处理、保留**（该卡实为 `navigate('posts')` 正常入口）
   - 附：「好友」页截图确认为误发，忽略
3. 广场「统计」按钮橙色空白 → `学习博客.html:169`；**根因**：`renderBlogStats` 双份定义（app.js:7231 / api.js:761），api.js 后加载覆盖生效，而 app.js:7248 找 `#blogStatsBox`、HTML 里是 `#blogViewStats` → id 不匹配渲染落空
4. 模拟面试「提交回答」按钮通栏过长 → `商务礼仪面试.html` `#ivQuizSubmitBtn`

### 关键决策
- **③ 只改 HTML 容器 id 对齐 api.js 生效版**，不动 app.js/api.js 函数结构（最小侵入）
- **`#toolsPanel` 判定纠正**：非死代码，五页成对存在 `toolsPanel`+`toolsOverlay`，`toggleToolsPanel` 每页引 5-6 次 → **禁止删除**
- **`posts` / `local-data` 底层保留**：只删「本机数据」入口卡，路由 key、`#localStats`、`#profileBoxPostsHost` 全保留，避免 ReferenceError 白屏
- **需求收窄（17:38）**：删除范围从 2 张卡缩减为 **1 张**（仅「本机数据」），`subpage-group-card` 预期 5→4

### Wave 1 收尾结果（已完成，全绿）
- 配对校验 6/6 平衡（`商务礼仪面试/私聊/工具/更多/四级备考/cet-read.js`）
- `node --check` 3/3 rc=0（cet-read.js / app.js / api.js）
- `assets/cet-read.js` 版本戳 **单点 bump → `20260915f`**（`四级备考.html` 内 `cet-read.js` 与 `data-cet-read.js` 同步）
- 修正 `私聊.html:377` 注释中裸写的字面 `<style>`（会持续污染自动配对扫描，改为「样式块」）
- Wave 1 改动面：7 文件，+513 / -40

---

## 批次九 Wave 1 收尾 + N9-25 补充包 · 已交付上线（commit `a60c42f` / 版本戳 `20260915f`）

**执行时间**：2026-09-15 下午（批次九第二次 apps 独占收口）

### 交付清单（11 个子任务全部完成）

| 子任务 | 需求 | 文件 | 结果 |
|---|---|---|---|
| W1-T1 | N9-23 每日一题卡改造 | `商务礼仪面试.html` | 「今日一题·{分类}」+ 右上「已答 X/Y」+ 题下 textarea + 三按钮（换一题/提交回答/查看要点）；删「开始模拟面试」，孤儿 `#ivAiPanel` 一并隐藏；`ivQuizSubmit()` + localStorage `interview_daily_answers` |
| W1-T2 | N9-11 阅读理解布局 bug | `assets/cet-read.js` | 宽屏右栏不再空卡片（选词填空/长篇匹配/策略浏览三题型同步）；去 sticky 内滚截断 |
| W1-T3 | N9-16 联系管理员全 tab 可见 | `私聊.html` | `syncAcEntry()` 收窄到「好友」tab + `#acTabBadge` 未读角标镜像（不改 admin-contact.js）；未改任何 assets |
| W1-T4 | N9-5 工具页内容填充 | `工具.html` | 消除空壳区块与死链，侧边栏入口未动 |
| W1-T5 | N9-6 更多页内容填充 | `更多.html` | 补 5 个站内真实页面直达入口（时政热点/企业定向库/动态/PPT素材库/PPT案例拆解），避开 L102-106 学习统计卡 |
| 补充 | 「统计」橙色空白 bug | `assets/api.js` | 新增 `blogLocalStats()` 本机兜底；`renderBlogStats` 去掉 `!CURRENT_USER` 直接 return —— 未登录/离线也能出真实数据 |
| 补充 | N9-25① 个人中心卡片精简 | `个人中心.html` | 删「本机数据」入口卡（5→4）；`local-data` section / `#localStats` / SubpageRouter 全保留；「发贴统计」按用户确认保留 |
| 补充 | N9-25④ 提交按钮过长 | `商务礼仪面试.html` | 「提交回答」改内容宽 |
| 补充 | 更多页分组折叠 + 工具箱入口 | `更多.html` | 常用/内容直达/系统 三组可点击收放，状态存 localStorage；补「工具箱」入口卡 |
| 补充 | 缺返回按钮补齐 | 7 页 | AI模拟面试 / PPT素材库 / 四级经验分享 / 好友申请 / 工具 / 更多 / 管理员 —— 统一「智能返回」（同项 history → 兜底首页） |
| 补充 | 首页各模块进度卡列表化 | `assets/app.js` + `学习工作台.html` | 明细由「·」拼接一行改为 `.module-meta-item` 独立成行，不再被 nowrap 截断；错题胶囊作为列表最后一行 |

### QA 与上线结果（一次跑完，全绿）

- 老 WebView 禁用语法扫描 37 文件：**0 命中**（`?.` / `??` / `replaceAll` / `Object.fromEntries` / `.at()` / 正则 lookbehind）
- 原生 `alert/confirm/prompt`：**0 命中**（ADR-3 保持）
- `node --check`：`assets/app.js` / `assets/api.js` / `assets/cet-read.js` **3/3 rc=0**
- 版本戳：`app.js` 30 页 → `20260915f`；`api.js` 26 页 → `20260915f`；`cet-read.js`/`data-cet-read.js` → `20260915f`
- 部署：37 文件 / 2.31 MB，远端 `BACKUP_OK` + `EXTRACT_OK`，8 项 grep 断言全中
- 线上直连复检（禁代理）：9 个页面/资源全部 HTTP 200 且新标记命中；线上 `app.js` 可选链 0 / replaceAll 0 / lookbehind 0 / 原生 alert 0
- GitHub：代理端口 `63926` 返回 CONNECT 502（不可用），改 `7897` 一次通 → **`789ec80..a60c42f main -> main`**，远端 HEAD 复核一致

### 遗留（排入后续波次，**不属本轮**）

1. `blog_wechat.html` 版本戳仍滞后：`app.js=20260915eaaa`（畸形戳）/ `api.js=20260913j` / `icon-map.js`、`xt-toast.js`、`error-boundary.js` = `20260913j`。本轮未改该页，**交给 N9-15（W3）一并处理并修畸形戳**。
2. 数量关系题库回滚重做（P0，第 5.1 节）仍未启动 —— 独立于本波，资源已在案。
3. `common.css` L68 全局滚动锁（P1）、文件清理（P2，**必须全批次最后一步**）均按住未动。

---

## 热修 1 · 工具页移除「考试倒计时」区块（commit `65a5673`，2026-09-15 已上线）

**起因**：用户截图反馈工具页首屏挂着一张「考试倒计时」卡，要求删除。

**处理**（`工具.html`，单文件热修，无需 bump 版本戳 —— HTML 无缓存后缀）：
- 删除分组标题 + `.countdown-row` 宿主的可见区块；
- 删除该区块专属的渲染逻辑（宿主判空 + `DOMContentLoaded` 二次渲染监听），避免空转；
- **保留 `#countdownModal` 共用节点** —— `assets/app.js` 在顶层**无守卫**地监听它，删掉会直接中断整页初始化（历史上多页因这个节点缺失翻过车）；
- 首页「重要倒计时」（`学习工作台.html` L97-105）**不受影响**，仍是倒计时的唯一入口。

**验证**：远端 `BACKUP_OK` + `EXTRACT_OK`；线上直连拉取 `工具.html` HTTP 200 / 22735 chars —— 可见区块 False、宿主 False、`#countdownModal` 保留 True、`app.js?v=20260915f` 引用仍在。

**经验**：改动留下的注释里**不要写 id / 函数名本体**（`#countdownRow`、`renderToolsCountdowns` 等），否则会污染全站的自动化符号扫描（远端 grep 命中数出现「假的 2」）。已把注释改成不带标识符的描述，这个只是注释级变更，随 Wave 2 批次一起上线。
