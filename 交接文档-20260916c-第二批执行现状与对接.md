# 星途（学习工作台）· 对接文档（交接给下一个 AI）

> 生成时间：2026-09-16 13:20
> 交接原因：本批执行的 3 名成员被 **429 频率限制**打停（额度 2026-09-17 11:09 重置），另有 3 名成员在此前已零产出被中断
> **本文目标：让你在不读任何历史对话的前提下，安全接手并完成剩余工作，且不把站点改白屏**
> 配套阅读：`星途-更新日志-20260916.md`（本批已发生的全部变更）

---

## 0. 一句话现状

**已完成并各自通过验证的有 6 项**（需求 A 弹窗、需求 B 改名重命名、倒计时卡缩小、头像修复、关于独立页、QA 脚本 P1 修复）；**未完成的有 3 项大活**（更多页白屏破损修复、需求 D 的 ai-service 全量优化、需求 C 输入框 + 模型按钮）；**`app.js` 上有 +405 行半成品**（首页小助手，语法安全但功能完整度未验）。
**本批所有改动都未部署**，线上仍是 `20260916k`（完好版）。**更多页破损只在本地产，线上未受污染**。

---

## 1. 🔴 三条最关键的警告（先看这个）

### 1.1 唯一可写代码树是 `D:\下载的文件\学习工作台`，绝对不要碰 C 盘
`C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-ff3f678e` 是**同一仓库的废弃副本**（无 `.git`、没有 `AI.html` 和 `ai-service.js`、`更多.html` 只有 17959 B）。**改这里等于全部白干。** 所有读写一律用 `D:\` 绝对路径。

### 1.2 `更多.html` 当前是白屏级破损，**一旦部署就会把破损发上线**
项目自检脚本 `node tools/qa/escheck_es2017.js` 输出 **`DONE 3`（应为 `DONE 0`）**，唯一那条失败就是：
```
*** 更多.html [内联#2] :: ES2017 解析失败（老内核会整文件报废）
     需要 ES>2020  |  Unexpected token (3:0)
     L3: </head>
```
详见 §4.1 的**精确修复方案（我已定位到删除哪几行）**。

### 1.3 文档说的"可直接回滚"的那个备份**不可用**
`更多.html.bak-pre-cfix-20260916` 是**破损之后**打的快照（与当前文件 difflib 差异块 = 0，结构指标完全一致）。
**唯一干净的结构基准 = 线上 `20260916k` 版**（304 行、`<script>` 14/14、`<div>` 155/155、注释 20/20、`#importerView` 0）。用 python `urllib` + `urllib.parse.quote` 抓下来做**结构对照**（它没有「导入题库」功能，只能当参照，不能直接替换）。

---

## 2. 项目坐标与环境

| 项 | 值 |
|---|---|
| **唯一可写代码树** | `D:\下载的文件\学习工作台` |
| ⛔ **禁改** | `C:\Users\ATM\WorkBuddy\Worktrees\...`（废弃副本） |
| 线上静态站 | `http://110.42.134.62/`（nginx，远端根 `/opt/study-workbench/web`）——**当前 `20260916k`** |
| 线上后端 | `http://110.42.134.62:8000`（FastAPI / uvicorn，远端根 `/opt/study-workbench`） |
| 部署脚本模板 | `tools/deploy_update_20260916k.py`（照抄改 3 处） |
| 部署工具 | `tools/plink.exe`、`tools/pscp.exe`（已就位，hostkey 已固定） |
| 部署凭据 | 脚本自动从 `upload_v23.ps1` 正则解析 → **不要打印或外传** |
| node | `C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe` |
| python | `C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe` |
| jsdom + acorn | `tools/verifier/node_modules/`（已装好，路径硬编码在 QA 脚本里） |

### 2.1 技术形态
- 前端：**多页静态 HTML + `assets/*.js` 原生 JS**。**无框架、无 CDN、无打包、无构建步骤**。改完直接传文件。
- 后端：FastAPI + SQLite（`server/`），**本地 `server/` 是旧副本，不要以它为准**。
- 运行环境：**安卓 APK 里的老 WebView，内核 ≈ Chrome 50~58（只支持到 ES2017）**，同时也要在桌面浏览器正常运行。

### 2.2 环境坑（会让你白浪费大量时间）
1. **bash 缺 coreutils**：`ls` / `grep` / `head` / `tail` / `wc` / `dirname` **全部不存在** → 一律写 python 脚本，或用 Read/Grep/Glob 工具；命令一律写绝对路径。
2. **Windows 中文路径 + python 控制台**：`print` 中文会 GBK 崩溃 → 结果一律**写 UTF-8 文件**，再用 Read 工具读。
3. 用 python 写 `-c` 单行命令时，含 `["']` 的正则极易被 shell 吃掉引号导致 `SyntaxError` → **正则类脚本写成 `.py` 文件再执行**。
4. `curl -w '%{size_download}'` 本机**恒返回 0**，要量体积请用 python `urllib`。
5. **禁 git 操作**（本机 push 需动态代理，不值得折腾）。文件重命名用 `os.rename`，**不要用 `git mv`**。
6. **换行符（改文件必须保持，否则全文件 diff）**：
   - **CRLF**：全部根 HTML、`app.js`、`api.js`、`common.css`、`icon-map.js`、`ai-config.js`、`xt-content.js`、`ppt-works.js` 以外的多数 assets
   - **LF**：`assets/ai-service.js`、`assets/ai-page.js`、`assets/ppt-works.js`
   - 改前先测：`open(p,'rb').read().count(b'\r\n')` vs `.count(b'\n')`

---

## 3. 铁律（违反 = 整页白屏 / 白干。本项目已因此出过两次全站事故）

### 3.1 老 WebView 语法禁令（内核约 Chrome 50~58）
以下语法**一律禁止**：
```
?.        ??        .replaceAll(        Object.fromEntries
.at(      顶层 await      正则后行断言 (?<= (?<!
{...obj}  ← 对象展开（ES2018）★历史全站事故元凶
const {a, ...rest} = x     ← 对象剩余
**        ← 指数运算符
catch {}  ← 可选 catch 绑定
```
**允许**：`let/const`、箭头函数、模板字符串、`class`、`async/await`（非顶层）、数组展开 `[...arr]`、`Math.max(1, ...arr)`。内核 ≥ ES2017。
⚠️ **`node --check` 抓不到这些**，必须 Grep 逐项确认 0 命中。

### 3.2 顶层声明冲突禁令（"AI 失效"的真凶）
**禁止**在任何 js 里用 `var/const/let` 重复声明别的文件已存在的全局名。浏览器遇到重复声明会判该文件 **SyntaxError 并整文件拒绝执行**（该文件所有功能静默失效）。
新增全局一律写成：
```js
if (typeof window.xxx !== 'function') window.xxx = function () { ... };
```

### 3.3 版本戳（`?v=`）—— 头号"修好了但用户看到老样子"的原因
- **改任何 `assets/*.js` 或 CSS 后，必须 bump 引用它的 `?v=` 版本戳**，否则用户设备永远加载旧文件。
- **现状**：全站 **551 处**全部为 `20260916L`，本批**未 bump**，目标新戳 **`20260916M`**。
- 补裸路径戳请用**单行内**匹配：`(src|href)="(assets/[^"?]+\.(?:js|css))"` → `\1="\2?v=20260916M"`
- ⚠️ **严禁跨行正则**（跨行会吞掉 `-->` 和标签 = 整页白屏，已踩两次）。
- 项目约定：**波内任务一律不 bump，波末单点统一执行**（bump 会重写 30+ 个 HTML，是个全局互斥锁）。

### 3.4 其它
- **禁原生 `alert/confirm/prompt`**（ADR-3），用页面内 toast（`window.xtToast` / `window.showToast`）或页面内区块。
- 改完必须做**配对校验**：`<!--` 与 `-->` 数量相等、剔除 script/style 后 `<div` 与 `</div>` 数量相等、`<script` 与 `</script>` 数量相等、无损坏特征 `.js\?v=xxx"[^>\s]`。
- 改文件前**先备份**（命名含 `bak`，部署脚本会自动跳过含 bak 的文件）。
- **不删现有功能、保留既有 DOM id 与全局函数名**（外部页面可能引用）。
- **工作方式（用户最高优先级）**：命令/脚本**各跑一次即判定**，不要反复回跑、不要多轮回读自审。发现问题直接给结论。

---

## 4. 未完成工作（按优先级，含可直接执行的完整任务书）

### 4.1 🔴 P0：修复 `更多.html` 白屏级破损（**只删不加**）

**当前结构指标（实测）**：573 行 / 35295 字节
| 指标 | 当前 | 目标 |
|---|---|---|
| `<script` vs `</script>` | 16 / **15** | 相等 |
| `</head>` | **2**（L34、L200） | 1 |
| `<body` | **2**（L35、L201） | 1 |
| `id="importerView"` | 2（L187、L342） | 1 |
| `id="impBody"` / `id="impLibs"` | 各 2 | 各 1 |
| `class="morepage-list"` | 2（L128、L294） | 1 |
| `<!--` vs `-->` | 30 / **31** | 相等 |
| 卡片标题 | **13 条** | **8 条** |
| `.mpc-icon` 裸 `data-icon` 写法 | **5 处** | 0 |

**根因（已逐行定位）**：误搬时把**整份工具页的骨架**插进了更多页自己的内联脚本中间。

**精确手术方案 —— 删除 L200 到 L352（含首尾，共 153 行）**

| 行号 | 内容 | 处置 |
|---|---|---|
| L198 | `<script>` | **保留** |
| L199 | `(function(){` | **保留**（关键！L436 的 `})();` 靠它配对） |
| **L200** | `</head>` ← 多余的第二份 | **开始删除** |
| L201 | `<body class="theme-home">` ← 多余 | 删 |
| L203 / L204 | `<div class="app">` / `<nav class="sidebar">` ← 工具页骨架 | 删 |
| L291–L341 | 第二个卡片列表（误搬的 5 张卡：导入题库 / PPT版式库 / 四级经验分享 / AI模拟面试 / 穿越英语） | 删 |
| L342–L351 | 第二个 `#importerView` 视图块 | 删 |
| **L352** | `<script>` ← 误搬夹带的第二个脚本开标签 | **最后一行删除** |
| L353 | `/* 打开导入题库全屏视图：... */` | **保留**（直接接在 L199 的 `(function(){` 后） |
| L353–L436 | `openImporterView / closeImporterView / renderImportLibs / removeImportLibs / escImp / impItemText` 函数定义 | **保留**（这是**唯一一份**，更多页的「导入题库」卡片 L160 依赖它） |
| L436 / L437 | `})();` / `</script>` | **保留** |

**为什么是这个区间（判断依据）**：L436 的 `})();` 必须与 L199 的 `(function(){` 配对，所以 L198–L199 必须保留；误搬块从「多余的第二份 `</head>`」L200 开始，到「误搬夹带的 `<script>` 标签」L352 结束。

**额外要删/改的**：
1. 删掉孤立的 `-->`（`<!--`30 vs `-->`31；若落在 L200–L352 内则随块删除，否则单独定位）。
2. 5 处裸 `<div class="mpc-icon" data-icon="xxx" data-icon-size="20">` → 改成标准写法 `<div class="mpc-icon"><span class="nav-icon" data-icon="xxx" data-icon-size="20"></span></div>`（正确块 L122–L185 里已有 8 处标准写法可对照）。**注意**：这 5 处很可能就在误搬块 L291–L341 里，随块删除即自动归零。
3. `<head>` 里重复的 `.importer-view` 相关 `<style>` 只留一份。
4. 删除标注「**工具页**」归属的残留注释。

**必须保留（别删错）**：
- 更多页自己的「导入题库」功能（DOM `#importerView`/`#impBody`/`#impLibs` 各 1 份 + 上述 6 个函数）
- 共用 DOM：`#adminEntryCard`、`#countdownModal`、`#morePanel`、`#toolsPanel`、`#aiFab`、`#aiPanel`、`#toast`、`.more-overlay`
- 底部导航顺序 `首页→互动→我的→AI→更多`
- **已完成的改名文案不要回退**：侧栏/底栏必须是「**社区**」不是「广场」（需求 B 改过 13 行）；「**互动广场**」（L132 卡片 + 底栏）必须保留（那是「我的发贴」功能，`onclick="gotoBlogMine()"`）；注释里的「表达页」保留

**⚠️ 施工方式**：
- **禁止整文件重写（Write 覆盖）** —— 会冲掉需求 B 的文案改动。
- 只做**定向删除**：用 Edit 精确替换，或写 python 脚本**按内容特征**定位（如「从第二个 `</head>` 起到误搬块内最后一个 `#importerView` 的 `</div></div>` 后紧跟的 `<script>`」），**不要硬编码行号**（行号会随删除漂移）。
- 文件是 **CRLF**，改完保持。

**修复后必须全部通过**：
| 断言 | 期望 |
|---|---|
| `tools/qa/escheck_es2017.js` | **`DONE 0`** ← 最重要 |
| `<script` vs `</script>` | 相等 |
| `id="importerView"` / `id="impBody"` / `id="impLibs"` / `class="morepage-list"` | 各 1 |
| 卡片标题序列 | 严格等于 `互动广场 / 错题本 / 设置 / 管理后台 / 关于 / 导入题库 / AI面试 / 穿越英语` |
| `.mpc-icon` | span 嵌套 = 8，裸 `data-icon` = 0 |
| `<!--` vs `-->` | 相等 |
| 剔除 script/style 后 `<div` vs `</div>` | 相等 |
| 共用 DOM | 上列 8 个各 ≥1 |
| 文案 | 侧栏/底栏为「社区」；「互动广场」仍在 |

---

### 4.2 🔴 P0：需求 D —— AI 接口 400 修复 + 模型降级与超时优化
**文件**：`assets/ai-service.js`（602 行，**LF 换行**，**当前与备份逐字节一致 = 零改动**）
**依据文档**：`AI模型测试报告与修复需求-20260916.md`
**不在范围**：不换模型、不新增模型、不改 Key、**不改 `ai-config.js` 的模型列表和参数**、不碰后端 `/api/ai/chat` 中转逻辑。

**现场事实**：
- 核心请求函数 `:384` `async function requestModel(modelConfig, messages, onChunk, signal)`
- 报 400 的 fetch `:411`；请求体构造 `:393-399` `{ model, messages, temperature, max_tokens, stream: true }`
- 错误归一化 `:412-418` → `makeError("API错误:" + resp.status + " " + emsg, resp.status, ecode)`，**错误对象已带 `status`**，可直接用于判定降级
- 流式解析 `:420+`（`resp.body.getReader()` + `TextDecoder` 按 `data:` 行解析 SSE）
- **另一处流式** `:350` 也有 `resp.body.getReader()`，`:375-381` 是 `lastErr` / "服务端中转全部失败" 降级循环 —— **两处都要覆盖**
- **400 头号根因**：`:77` 与 `:91` 硬编码 JPEG：
  `{ type: "image_url", image_url: { url: "data:image/jpeg;base64," + image } }`
  用户传 PNG/WebP/HEIC 时声明 jpeg、字节是别的格式 → 视觉模型（GLM-4.6V-Flash / GLM-4V-Flash）因 MIME 不符拒绝 → **400**
- 现状 `stream: true` 已开（`:398`），SSE 逐字解析已实现 → **流式不是从零做，是加固**

**⚠️ 报告里有一条修法是错的，不要照抄**：报告建议"base64 需带 `data:image/jpeg;base64,` 前缀" —— 而项目**现在就是这么写的**，照抄等于不改。**真修法是按图片真实类型推导 MIME**（PNG `\x89PNG`、JPEG `\xFF\xD8`、WebP `RIFF....WEBP`、GIF `GIF8`），先 Read `assets/ai-page.js` 的 `handleFile` 确认上游传的是纯 base64 还是已带 dataURL 前缀。

**六项要求**：
1. **修 400**：按真实类型构造 MIME 前缀，绝不写死 jpeg；确认显示名（`GLM-4V-Flash`）与实际 `model` 字段（`glm-4v-flash`）是分开的；失败路径把完整请求体关键信息打 `console.warn`。
2. **降级条件**：**只有 `429` / `5xx` / 网络超时**触发降级；**`400` 不降级**，直接把后端 `error.message` 抛给上层展示（现状出现"提示已切换到 GLM-4V-Flash 但切完还是 400"的误判）。按 `ai-config.js` 的 `fallback` 链依次切换，提示「已切换到 XX 模型」。
3. **15 秒未出首字即切换**：所有 AI 请求超时 15 秒；15 秒内未收到流式首 token → 按 fallback 链切下一个**快**模型，提示「响应较慢，已切换模型」。慢模型（Qwen3-8B、Qwen3.5-4B）**仅用户手动选择时使用**。
4. **限流备选**：某模型连续 2 次 429 → **本次会话内跳过它**（内存态即可），改用 GLM-4.5-Flash 或 Qwen2.5-7B 作首选。
5. **错误提示到用户侧**：在对话区显示可读错误（如「图片格式不支持」「当前模型繁忙，已切换」）；400 必须带 `error.message`。渲染逻辑在 `ai-page.js`（不在你范围），你只需把 `status`/`message`/`code` 带足抛出。
6. **流式加固**：`:350` 与 `:420` 两处 `getReader()` **都加守卫**：`if (resp.body && resp.body.getReader) { ...流式... } else { ...非流式兜底... }`；非流式兜底改发 `stream:false` 或 `resp.text()` 整段解析，保证老内核**不抛错、仍能出答案**。

**⚠️ 老内核硬约束**：APK 的 WebView 无 `ReadableStream`/`body.getReader()`，**"2 秒内逐字输出"在老内核上物理不可能**。已定案**双口径**：现代浏览器按「2 秒内首字逐字输出」为硬指标；老内核按「显示『AI 正在思考…』加载态 + 15 秒未出首字自动切换 + 整段返回后正确渲染」验收，**不要求逐字**。
另外实测：`assets/net-compat.js`（664 行、26 KB、**顶层声明 0 个**、专为此场景写）**全站零引用** —— 若想彻底解决老内核流式，需在 37 页注入它，属 Wave 6 全站批量级动作，**建议先做上面的守卫+兜底，注入留到后续统一波**。

**验收**：11 个模型逐一手动发文字无 400；发图片视觉模型能识别无 400；人为构造 400 **不触发**自动切换且显示明确错误；模拟 429 按 fallback 链切换并提示；现代浏览器下流式 2 秒内可见文字；15 秒未出首字自动切换；等待时有「AI 正在思考…」；控制台无连续 400。

---

### 4.3 P1：需求 C（AI 页输入框"变脸"）+ 侧栏「模型」按钮无反应
**文件**：`AI.html`（874 行，**CRLF**）、`assets/ai-page.js`（1188 行，**LF**）
**当前状态**：**零改动**。已实测确认：`AI.html:313-318` 的 `.ai-model-panel{z-index:99}` **未提**；`:163` `max-width:760px`、`:232` `max-width:768px`、`:491` `max-width:none` **三条规则原样未动**；`ai-page.js` 无 `closeSidebar` 调用。**`AI.html` 里已有的 6 处改动全属需求 B 的侧栏文案改名，与本任务无关。**

**需求 C 机制（已摸清）**：
- 全页**只有一份**输入框 DOM：`AI.html:638-677` `<div class="ai-input-box" id="aiInputBox">`，内含 `#aiImgPreview`、`textarea#aiInput`、`.ai-input-foot`（左 `.ai-foot-left` = 深度思考 `#aiDeepThinkChip` + 模型 `#aiModelBtn`/`#aiModelLabel`；右 `.ai-foot-right` = 附件 `#aiAttachBtn` + 发送 `#aiSendBtn`）、`#aiFileInput`、`#aiModelPanel`。
- 它被 JS 在两个槽位间搬动（`ai-page.js:249-254` `mountInput`）：空态槽 `#aiWelcomeInputSlot`（`AI.html:637`）、对话态槽 `#aiDockInputSlot`（`AI.html:685`）。状态由 `#aiChat` 上的 `.in-conversation` 类控制（`AI.html:167-168`）。
- `.ai-input-box`（`AI.html:235-242`）**本来就是一体化大圆角容器**，控件位置与目标形态一致。**差异全在两个槽位的外层规则 + `.focus` 态**：

| 位置 | 规则 | 差异 |
|---|---|---|
| `AI.html:163` | `.ai-welcome .ai-input-wrap{width:100%;max-width:760px;margin-top:8px;}` | 空态 760px |
| `AI.html:232` | `.ai-input-dock .ai-input-wrap{max-width:768px;margin:0 auto;}` | 对话态 768px |
| `AI.html:491`（媒体查询内） | `.ai-welcome .ai-input-wrap{max-width:none;}` | **窄屏空态变全宽** → 跳变最明显 |
| `AI.html:158-161` | `.ai-welcome{flex:1;...align-items:center;justify-content:center;}` | 空态垂直居中 |
| `AI.html:231` | `.ai-input-dock{flex-shrink:0;padding:10px 16px 14px;}` | 对话态贴底 |
| `AI.html:235-242` | `.ai-input-box{background:var(--ai-bg);border:1px solid transparent;...}` vs `.ai-input-box.focus{background:var(--ai-card);border-color:rgba(0,0,0,.06);box-shadow:...}` | **未聚焦时容器背景=页面底色、边框透明 → 容器"隐形"、控件看起来散开** ← **很可能是"变脸"主因** |

**要求**：不管有没有对话，输入区**从头到尾长一个样**，统一成"大圆角方框包住所有按钮"。**只统一外观，不改任何交互逻辑**（`enterConversation/enterWelcome/mountInput`、发送、上传、模型面板、深度思考互斥都不动）。**保留双槽位机制**（不要删），最稳做法是让两个槽位外层规则**完全一致**，或让 `.ai-input-box` 自身决定外观不依赖父级，并改掉 `:491` 的窄屏覆盖。必须验 375px + 1280px 两端。

**侧栏「模型」按钮（已定位根因）**：
- 按钮 `AI.html:609` `#aiModelInfoBtn`（文案「模型」）、`:613` `#aiSettingsBtn`、`:617` `#aiClearBtn`，都在 `.ai-hist-foot` 内
- 绑定**是好的**：`ai-page.js:1054-1056` `bindById('aiModelInfoBtn','click',toggleModelPanel)` 等
- 函数**也是好的**：`ai-page.js:772-775` `toggleModelPanel()` 判 `.open` 后调 `openModelPanel/closeModelPanel`
- **真因是 z-index 层级冲突**：`#aiModelPanel` 挂在 `.ai-input-box` 内（`AI.html:665`），CSS `:313-318` 是 `position:absolute; bottom:calc(100% + 10px); z-index:99`；而窄屏遮罩 `.ai-sidebar-overlay{z-index:250}`（`:474`）、抽屉 `.ai-history{z-index:260}`（`:479`）→ **面板被遮罩整个盖住 = "点了没反应"**
- 桌面端无遮罩，面板会开在输入框上方、与点击位置隔半屏，**很可能是"打开了但没看见"**

**修法**：点侧栏「模型」时**先关抽屉 + 关遮罩、再开面板**（抽屉开关是 `toggleSidebar`）；把 `.ai-model-panel` 的 `z-index` 提到**≥300**；侧栏按钮文案**跟随选中模型**（复用 `updateModelLabel`/`getSelectedModelId`，别把「模型」两字写死）；顺带排查「设置」（`#aiSettingsBtn` → `openSettings`）。**两处入口必须复用同一个面板，不要各写一套。**

---

### 4.4 🟡 核验 `app.js` 的首页小助手半成品（+405 行）
工程师在做到一半时被 429 打停。`escheck` 通过 → **语法安全**，不会引发解析级白屏。但功能完整度**未经验证**，请逐项核验：

**已确认完成**（可直接采信）：
- `AI_PARTNERS` 第一角色已改：`id:'assistant'`、`name:'小助手'`、`icon:'sparkles'`、`emoji:'✨'`、`tag:'全能学习助手'`、`desc`/`systemPrompt`/`greeting` 已全部改写为通用学习助手
- **旧 id 兼容映射已做**：`app.js:3092` `{ gongkao:'assistant', xiaotu:'assistant', coach:'assistant', ... }`（老用户 localStorage 里存的是旧 id，不做映射会失效）
- 第二角色「暖心学伴」（id `warm`）保留；两份 `demoStyle` 都保留作降级兜底
- `callAI` 引用从 3 处 → **11 处**（已接真实 AI 底座）
- `app.js:3265` 加了注释「卡片副标题：真实 AI 状态（不再出现"本地演示模式"）」

**必须逐项核验的功能**（工单原文要求）：
1. 卡片头部：左侧助手图标 + 名称 + tag；右侧「切换角色」+ 关闭/清空
2. 对话区：用户/助手分气泡、**流式**、加载态、复制、重新生成、空态显示问候语
3. 输入框保留 + 「在 AI 页打开 / 展开完整对话」入口跳 `AI.html`
4. 快捷功能（生成学习计划/分析错题/批改作文/讲解知识点/复习提纲/模拟面试）**精简为一行可横滑**、点击填 prompt、**发送走真实 AI**
5. 「换个伙伴」弹窗只放 2 张卡、图标风格统一、「使用中/使用」状态、**切换记住（localStorage）且不丢历史**、遮罩与关闭按钮均可退出
6. 与 `AI.html` 的模型/历史共享口径是否落地
7. **`app.js` 中仍有 7 处「本地演示模式」文案**（`L2964/L3075/L3938/L3953/L7861/L8609` 等）——需甄别：**卡片副标题**必须去掉；**设置页/AI 自述的既有功能描述**可保留（避免超出需求范围）

**注意**：工单原文写"统一调用后端 `/api/ai/chat`"，**这句是错的**（与项目现状矛盾）—— 全站 AI 底座是 `ai-service.js` 的 `callAI`（前端直连），手册亦明确"不涉及后端 `/api/ai/chat` 中转逻辑"。**已定案走 `callAI`**，不要改回去。
另：`AI.html` 侧**没有"伙伴角色"概念**（只有推荐问题 preset chips），"角色互通"需新定口径——建议"模型+历史共用同一 localStorage 键，角色以首页 `AI_PARTNERS` 为准"，**不要改 `AI.html` 页面结构**（工单明确）。
另：公共 Key 有 **10 次/分钟限频** → 卡片**不能自动发问**，只在用户点发送时调 AI。

---

### 4.5 P2/P3 两个小修
- **P2**：`app.js:6776` `recentMap` 有 2 条死条目：`'社区.html': 'blog'` 应为 `'plaza'`、`'面试题库.html': 'iv'` 应为 `'interview'`（`HOME_DEF` 的 `k` 只有 `plaza/cet/exam/comm/interview/ppt`）。判据是 `HOME_DEF.find(d => d.k === recentMap[name])`，value 对不上就永不记入「最近打开」。（`'私聊.html':'chat'` 和 `'设置.html'/'个人中心.html':'settings'` 无对应 `k`，属历史遗留，删或留自行判断）
- **P3**：`assets/common.css` 注释旧名残留 2 处 —— `L1353` `/* ===== 学习博客（笔记系统）样式 ===== */`、`L2102` `（学习博客.html:236）` → 改为「社区」。**只改注释，不要动任何 CSS 属性值。**

---

## 5. 自检与验收工具

| 目的 | 脚本 | 用法 |
|---|---|---|
| **老内核解析级体检（最重要）** | `tools/qa/escheck_es2017.js` | `node tools/qa/escheck_es2017.js` → 必须输出 **`DONE 0`**（当前 `DONE 3`）。详情写入 `C:\Users\ATM\_escheck_out.txt` |
| 单页运行时体检 | `tools/qa/page_check.js` | `node tools/qa/page_check.js "页面名.html" "关键词1,关键词2"` |
| 多页导航体检 | `tools/qa/multi_check.js` | 无参，跑 8 个页面 |
| AI 端到端（真页面） | `tools/qa/ai_jsdom_check.js` | jsdom 加载 AI.html → 输入 → 点发送 → 断言气泡**不含**「本地参考」 |
| 机器人球点击/拖动 | `tools/qa/ball_verify.js` | 断言球存在 + 触摸拖动后位置变化 |
| 拉线上文件对比本地 | `tools/qa/fetch_srv_files.py` | 下载线上文件与本地做 md5 对比 |

**⚠️ P1 已修（本批）**：`page_check.js` / `multi_check.js` / `ball_verify.js` 三个脚本原先**硬编码 `ORIGIN = 'http://110.42.134.62'`（线上旧版）**，跑出来的"通过"验的是旧代码。现已改为：
```
const ORIGIN = process.env.QA_ORIGIN || 'http://127.0.0.1:<本地端口>';
```
**默认指向本地**。用法：
```
# 先起本地静态服务
python -m http.server 8899 --directory "D:\下载的文件\学习工作台"
# 验本地（默认）
node tools/qa/page_check.js "社区.html" "社区"
# 验线上（部署后用）
QA_ORIGIN=http://110.42.134.62 node tools/qa/page_check.js "社区.html"
```
`escheck_es2017.js` 是**本地 acorn 扫描**，不受此影响。

**每批必跑的自查清单**：
- [ ] 改动过的 js 跑 `node --check`
- [ ] `<!--`/`-->`、`<div`/`</div>`、`<script`/`</script>` 配对平衡
- [ ] 无新增顶层重复全局声明
- [ ] 无 §3.1 禁用语法（`node --check` 抓不到，必须 Grep）
- [ ] 原生 `alert(`/`confirm(`/`prompt(` = 0
- [ ] 改动过的资源 `?v=` 已 bump
- [ ] 全站「不带 `?v=` 的 assets 引用」= 0

**⚠️ 诊断经验（务必传承）**：
1. **只测服务层会假绿**。曾用 vm 只加载 `ai-config.js`/`ai-service.js` 得到"真模型回答"，但没加载 `app.js`，掩盖了全局声明冲突。→ 验证前端必须用 **jsdom 加载真实完整页面**。
2. **"某个模块集体失效"优先怀疑该 js 解析失败**（跑 `escheck_es2017.js` 最快），而不是逐个按钮排查。
3. jsdom 报 `Not implemented: navigation to another Document` 是 **jsdom 自身限制，不是 bug**，忽略。
4. 视觉类需求（弹窗、输入框、倒计时卡）**必须真机/浏览器截图对比**，375px 与 1280px 都要出图 —— jsdom 没有排版引擎，验不了"看起来小一档"。

---

## 6. 部署流程

**照抄 `tools/deploy_update_20260916k.py` 改 3 处：**
1. `STAMP = '20260916M'`（新版本戳）
2. `files = [...]`（要上传的 `assets/` 文件列表；纯 HTML 改动则留空 `[]`）
3. 远端校验段的 grep 关键词

脚本会自动：bump 全部根 HTML 的 `?v=` → 打包 `tar.gz` → `pscp` 上传 → `plink` 远端解压 → `curl` 状态码 + `grep` 内容校验。
**先跑 `SW_DRY_RUN=1` 预演确认打包正常，再真跑。**

### 6.1 💡 重要建议：可以**排除 `更多.html` 单独部署**其余改动
部署脚本按 `files` / 文件列表上传，可以**不把 `更多.html` 放进本次上传清单**。这样：
- 已通过 QA 的 5 项改动（需求 A/B、倒计时卡、头像、关于页）**可以先安全上线**，不必等更多页破损修完；
- 线上 `更多.html` 保持当前完好版（18297 B、结构 155/155 平衡）；
- **前提**：改名后线上会出现 6 个新页 + 6 个旧页并存（旧页是改名前的版本，仍可访问但内容与导航是旧的）。若希望彻底干净，还是先修更多页再整体部署。

### 6.2 部署脚本的坑（血泪清单）
1. **凭据必须是 `root@ip`**：正则若只抓到裸 IP，plink/pscp 会用本地用户名认证 → `Access denied`，报错信息是极具迷惑性的「password not accepted」。模板脚本已处理。
2. **远端备份命令必须容错**：`tar -czf 备份 <filelist>` 在文件不存在时会让 `&&` 链断掉 → **根本不解压**（现象：上传成功但线上 grep 报 No such file）→ 备份命令后加 `|| echo BACKUP_SKIP_NEW`。
3. **部署后一定实测**：`curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1/xxx.html` + 对关键字符串 `grep`，不要只看上传 exit code。
4. **版本戳**：新上的 js 必须带 `?v=` 且 bump，否则用户拿到旧文件。
5. 别用 `sw` 当变量名（与 shell 冲突）。
6. 用 `curl -s -D -` 看响应头，不要用 `-I`；期望 `Cache-Control: no-cache`。
7. 中文文件名 URL 要 quote。
8. **重命名后线上存量链接会 404**（书签/外链指向 `学习博客.html` 等旧名）。可选在 nginx 加 301 旧→新跳转兜住 —— **属生产操作，需用户点头**。已装 APK 内是旧快照、页面自包含，不受影响。

---

## 7. 回滚点与备份

### 7.1 开工前备份（`*.bak-pre-batch2-20260916`，部署脚本自动跳过含 bak 的文件）
根目录 16 个：`更多.html`、`工具.html`、`PPT训练.html`、`AI.html`、`学习工作台.html`、`学习博客.html`、`四级备考.html`、`央国企笔试.html`、`高情商表达.html`、`商务礼仪面试.html`、`设置.html`、`个人中心.html`、`blog_wechat.html`、`动态.html`、`行测刷题.html`、`商务礼仪.html`
`assets/` 8 个：`app.js`、`api.js`、`ai-service.js`、`ai-page.js`、`common.css`、`icon-map.js`、`ai-config.js`、`xt-content.js`

⚠️ **注意**：`更多.html.bak-pre-cfix-20260916` 与 `工具.html.bak-pre-cfix-20260916` **是破损后的快照，不能用于回滚**（见 §1.3）。

### 7.2 开工前 md5 基线（可用于事后校验哪些文件被动过）
| 文件 | 字节 | md5 |
|---|---|---|
| `更多.html` | 35382 | `8fb1a09995ac51aa59b2d2417a670844` |
| `工具.html` | 24696 | `a0bd6fe938999203187fe17c9b731ccb` |
| `PPT训练.html`→`演示.html` | 48307 | `25184a2ece9271f8491198f23c9877cd` |
| `AI.html` | 48737 | `58567c6592fad11eb8b3ad248fcb505b` |
| `学习工作台.html` | 51962 | `5dc5b53940e1fb5b458ed47b90deb87c` |
| `assets/app.js` | 7052711 | `385ccf7b1e653facc482ec199da03436` |
| `assets/api.js` | 108440 | `5d2e616f550ffa9196955b82ca3e5072` |
| `assets/ai-service.js` | 21518 | `7bbc836adc043d752a181dca5b863525` |
| `assets/ai-page.js` | 60089 | `c7427d932143234253df9fe4ab054052` |
| `assets/common.css` | 120789 | `d7cbc48f10d33796002f3b7348029462` |

### 7.3 当前工作区状态速查（"哪些文件被动过"）
**已改动（14）**：`AI.html`、`blog_wechat.html`、`个人中心.html`、`动态.html`、`商务礼仪.html`、`学习工作台.html`、`工具.html`、`更多.html`、`行测刷题.html`、`设置.html`、`assets/ai-page.js`、`assets/api.js`、`assets/app.js`、`assets/common.css`
**未改动（4）**：`assets/ai-config.js`（正确，不该改）、**`assets/ai-service.js`（需求 D 零产出）**、`assets/icon-map.js`、`assets/xt-content.js`
**新建**：`关于.html`、6 个改名后的页面（`社区/英语/行测/表达/面测/演示.html`）

---

## 8. 需求原文索引（追溯需求时读这些）

| 文档 | 内容 |
|---|---|
| `D:\下载的文件\学习工作台\交接文档-20260916b-第二批需求执行手册.md` | **本批总纲**：项目坐标/铁律/在途破损/需求 A~D 逐项要求与验收 |
| `C:\Users\ATM\Desktop\需求文档.txt` | 需求 A/B/C 的**用户亲笔原文** |
| `D:\下载的文件\学习工作台\AI模型测试报告与修复需求-20260916.md` | 需求 D 依据（11 模型全可用；⚠️ 其第 61 行的修法建议本身有误，见 §4.2） |
| `D:\doubao\Download\需求-首页AI小助手重构-20260916.md` | 首页小助手工单（⚠️ 其中"调用后端 `/api/ai/chat`"与项目现状冲突，见 §4.4） |
| `D:\doubao\Download\需求-AI页与首页等4项修复-20260916.md` | 4 项修复工单（模型按钮 / 头像 / 倒计时卡 / 关于独立页） |
| `D:\下载的文件\学习工作台\交接文档-20260916-全站修复收口与APK.md` | 上一轮全站修复收口（含 APK 打包 6 项必查） |
| `D:\下载的文件\学习工作台\架构-批次九-全线改造-波次计划-20260915.md` | 批次九 23 项 7 波依赖分析（**Wave 3~7 共 10 项未开工**） |
| `D:\下载的文件\学习工作台\用户需求与决策总账.md` | 用户全部需求与拍板记录（**已拍板项照办，不要再问用户**） |
| `D:\下载的文件\学习工作台\星途平台改进-需求清单-状态盘点-2026-09-13.md` | 原始 30 条需求状态 |
| `deliverables/星途-更新日志-20260916.md` | **本批已发生的全部变更**（同目录） |
| `deliverables/星途第二批-执行计划书-20260916.md` | 本批开工前的执行计划（含波次编排与文件独占矩阵） |
| `deliverables/星途全量需求进度看板-20260916.md` | 六层需求体系的全量进度盘点 |
| `deliverables/星途项目全景简报-20260916.md` | 项目全景（技术形态、目录职责、工具链） |

---

## 9. 工作方式偏好（用户多次强调，务必遵守）

1. **「直接给出结论，不用反复验证」** —— 命令/脚本各跑一次即判定；不做多轮回读、不做多轮自审。发现问题直接给结论 + 建议。
2. **多线并行** —— 文件零重叠的前提下尽量并行，不要串行单人作业。
3. **文件独占原则（血泪教训）** —— `app.js` 被 **31 个页面**引用、`api.js` **30 个**、`common.css` 全站共享。**同一文件同一时刻只允许一条线改**。本批实际发生过一次并发写风险：需求 B 的文案替换与更多页破损修复同时命中 `更多.html`（最终靠"只做定向删除、禁止整文件重写"化解）。**派活时必须显式列出独占文件清单。**
4. **版本戳 bump 是全局互斥锁** —— 波内任务一律不 bump，波末单点统一（bump 会重写 30+ 个 HTML）。
5. **生产操作先问** —— 删用户数据、改 nginx、部署上线，三件必须用户点头。
6. **数据质量红线** —— 内容全部原创（不抄真题原文）；宁少而准，严禁臆造；新增数据文件用 **ASCII 文件名**、UTF-8 无 BOM（中文内容可以，中文文件名不行 —— pscp GBK 坑）。
7. **不删现有功能、保留原有 DOM**（铁律，违约即打回）。

---

## 10. 建议的接手顺序

1. **先修 `更多.html` 破损**（§4.1，已有精确方案）→ 跑 `escheck` 验到 **`DONE 0`**。这一步不做完不要部署整体。
2. **并行推 §4.2 需求 D**（`ai-service.js`）与 **§4.3 需求 C + 模型按钮**（`AI.html` + `ai-page.js`）—— 两者文件零重叠。
3. **核验 §4.4 `app.js` 半成品**（这一步要读代码，不是改代码；`app.js` 此刻无人占用）。
4. 补 §4.5 两个小修（`app.js` 的 `recentMap` + `common.css` 两处注释）。
5. 全员自检 → **统一 bump 到 `20260916M`** → `SW_DRY_RUN=1` 预演 → 部署 → 线上复检（用 `QA_ORIGIN=http://110.42.134.62`）。
6. 部署后提示用户**强制刷新**（清缓存/重开），否则设备上仍是旧文件。

---

*交接人：主理人 齐活林 · 2026-09-16 13:20。所有基线数据均为本机实测（逐文件 md5 对比 + 结构指标 + difflib 差异定位），非文档转述。*
