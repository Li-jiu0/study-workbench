# 星途（学习工作台）· 交接文档（交接给下一个 AI）

> ⚠️⚠️ **本文已被 `交接文档-20260916e-第三批续作与对接.md` 取代（2026-09-16 17:50）** ⚠️⚠️
> **过期原因**：本文写于 14:25，当时线上是 `20260916k`、本批改动**未部署**。
> **现状已变**：四项 UI 需求已全部实现并上线，**线上版本戳 = `20260916O`**；行尾曾被污染为 LF 并已修复回 CRLF；部署脚本模板应改用 `tools/deploy_update_20260916o.py`（含 3 道安全闸）。
> **请改读 `交接文档-20260916e-第三批续作与对接.md`。** 本文仅作历史存档（§4 的未完成工作清单仍有参考价值）。

> 生成时间：2026-09-16 14:25
> 交接原因：第三批的 13 条工程线被 **429 频率限制**打停（配额 **2026-09-17 13:32:39 UTC+8** 重置），QA 线已完成
> **本文目标：让你在不读任何历史对话的前提下，安全接手并完成剩余工作，且不把站点改白屏**
> 配套阅读：`更新日志-20260916-第三批.md`（本批**实际落盘**的全部变更）｜ `计划书-20260916-第三批执行（分工与波次）.md`（13 线名册 + 接力链 + 波次排期）

---

## 0. 一句话现状

**开工前的 P0 白屏破损（`更多.html`）已修好并验收（`escheck` 从 `DONE 3` → `DONE 0`）**；本批 13 条线里 **2 条完整交付**（`app.js` 小助手核验+P2、`工具.html`+`common.css`），**12 条落盘但未验证**，**5 条零产出**；**全站 `escheck` 仍为 `DONE 0`（没有解析级破损）**；**本批全部改动未部署**，线上仍是完好的 `20260916k`。

🔴 **接手第一件事不是写代码，是读 `更新日志-20260916-第三批.md` 的 §二「落盘实况」表** —— 那张表用 mtime 精确标出了哪些文件是本批动过的，以及哪些"看着像做了其实没做"。

---

## 1. 🔴 四条最关键的警告（先看这个）

### 1.1 唯一可写代码树是 `D:\下载的文件\学习工作台`，绝对不要碰 C 盘
`C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-*` 是**同一仓库的废弃副本**（无完整 `.git`、缺文件）。**改这里等于全部白干。** 所有读写一律用 `D:\` 绝对路径。

### 1.2 判断「哪些文件被动过」只能用 mtime，不能用 git
本仓库的 git HEAD 很旧（很多文件是 `?? 未跟踪`），`git status` 会列出几十个**更早批次**改的文件，极具误导性。
**本批开工时刻 = 2026-09-16 13:30。mtime ≥ 13:30 的业务文件才是本批动过的。**
- 反例：`演示.html`、`PPT版式库.html`、`assets/tpl-preview.js`、`assets/api.js` 相对 HEAD **有** diff，但 mtime 早于 13:30 → **本批没碰过它们**，别当成本批成果，也别当成本批欠账。

### 1.3 12 个"落盘但未验证"的文件**不能当作已完成**
它们是 429 打停时留下的一手现场：语法安全（escheck 0）、但**没有交付报告、没有功能验证**。
⚠️ 特别点名 **`assets/ai-service.js`**：601 → **1080 行**，`'429'`×20、`'15000'`×2、`getReader`×2→×7，说明降级/超时/流式守卫**大概率已写**；但 **`data:image/jpeg;base64,` 仍有 2 处** —— 图片 MIME 硬编码（400 的头号根因）**可能没修**。这是最需要你复核的一处。

### 1.4 有两条"故意没做"的，不是 bug
- **`common.css` L68 全局滚动锁（D-2）** —— 已决策**冻结**，理由与三步走方案见 `更新日志` §5.1。**别顺手改**，动了会让 12 个无 `.app` 外壳的页面出双滚动条。
- **版本戳本批未 bump** —— 项目约定「波内不 bump、波末单点统一」。现状 `20260916L` 共 567 处，目标 `20260916M`。

---

## 2. 项目坐标与环境

| 项 | 值 |
|---|---|
| **唯一可写代码树** | `D:\下载的文件\学习工作台` |
| ⛔ **禁改** | `C:\Users\ATM\WorkBuddy\Worktrees\...`（废弃副本） |
| 线上静态站 | `http://110.42.134.62/`（nginx，远端根 `/opt/study-workbench/web`）——**当前 `20260916k`** |
| 线上后端 | `http://110.42.134.62:8000`（FastAPI / uvicorn） |
| 部署脚本模板 | `tools/deploy_update_20260916k.py`（照抄改 3 处） |
| 部署工具 | `tools/plink.exe`、`tools/pscp.exe`（已就位，hostkey 已固定） |
| 部署凭据 | 脚本自动从 `upload_v23.ps1` 正则解析 → **不要打印或外传** |
| node | `C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe` |
| python | `C:/Users/ATM/.workbuddy/binaries/python/versions/3.13.12/python.exe` |
| jsdom + acorn | `tools/verifier/node_modules/`（已装好，路径硬编码在 QA 脚本里） |
| 根 HTML 数 | **39 个**（含本批新建 `学习概括.html`） |
| 引 `app.js` 的页面 | **33 个**（旧文档写 31，实测 33）｜引 `api.js` **30 个** |

### 2.1 技术形态
- 前端：**多页静态 HTML + `assets/*.js` 原生 JS**。**无框架、无 CDN、无打包、无构建步骤**。改完直接传文件。
- 后端：FastAPI + SQLite（`server/`），**本地 `server/` 是旧副本，不要以它为准**。
- 运行环境：**安卓 APK 里的老 WebView，内核 ≈ Chrome 50~58（只支持到 ES2017）**，同时也要在桌面浏览器正常运行。

### 2.2 🔴 429 配额（**影响你马上能不能开工**）
- 本批 13 条线在 12~21 分钟之间陆续被 429 打停，提示：**配额 `2026-09-17 13:32:39 UTC+8` 重置**。
- **在配额耗尽期间**：派子代理时**在 spawn 时指定 `model: "lite"` 可立刻继续**（用户实测：默认模型耗尽时换 `lite` 立刻可用）。
- ⚠️ **UI 右上角切模型对子代理无效** —— 子代理的 model 只能在 `Agent` 工具调用时指定、中途不可切换，所以**按「批次粒度」固定**，一个批次内不要混用。
- 主会话模型由 UI 手动选择，无法按任务自动切换。

### 2.3 ⚠️ 子代理类型与 Bash 可用性（2026-09-16 实测，别一刀切）
| `subagent_type` | Bash | 结论 |
|---|---|---|
| `software-engineer` | ✅ **有** | 工程师线用它（能跑 `node --check` / escheck / python） |
| `software-qa-engineer` | ❌ **没有** | 拉起后 **6 秒即挂**，报 `Bash not found in agent software-qa-engineer` |
| `general-purpose` | ✅ 有 | **QA 线必须用它**（`name` 仍可写 `software-qa-engineer`） |
> 重名时系统会自动重命名为 `software-qa-engineer-2`，后续用新名字 `SendMessage`。
> 派 QA 活时可在指令里补一句「不要调用 TaskList/TaskUpdate 等工具，直接干活」。

### 2.4 环境坑（会让你白浪费大量时间）
1. **bash 缺 coreutils**：`ls` / `grep` / `head` / `tail` / `wc` / `dirname` / `env` / `rm` **全部不存在** → 一律**写 python 脚本文件再执行**（不要用 `python -c` 单行，正则里的引号会被 shell 吃掉导致 `SyntaxError`），或用 Read/Grep/Glob 工具；命令一律写绝对路径。
2. **Windows 中文路径 + python 控制台**：`print` 中文会 GBK 崩溃 → 结果一律**写 UTF-8 文件**，再用 Read 工具读。
3. `curl -w '%{size_download}'` 本机**恒返回 0**，要量体积请用 python `urllib`。
4. **禁 git 写操作**（本机 push 需动态代理，不值得折腾）。文件重命名用 `os.rename`，**不要用 `git mv`**。`git status` / `git diff` 只读可用，但见 §1.2 的误导性。
5. **换行符（改文件必须保持，否则全文件 diff）**：
   - **CRLF**：全部根 HTML、`app.js`、`api.js`、`common.css`、`icon-map.js`、`ai-config.js`、`xt-content.js`
   - **LF**：`assets/ai-service.js`、`assets/ai-page.js`、`assets/ppt-works.js`
   - 改前先测：`open(p,'rb').read().count(b'\r\n')` vs `.count(b'\n')`
6. **常识性坑**：`len(str)` 是**字符数**不是字节数（中文 1 字符 ≠ 1 字节）—— 本批就有人因此误判"文件正在变化"。量字节一律用 `len(open(p,'rb').read())`。

---

## 3. 铁律（违反 = 整页白屏 / 白干。本项目已因此出过两次全站事故）

### 3.1 老 WebView 语法禁令（内核约 Chrome 50~58）
以下语法**一律禁止**：
```
?.        ??        .replaceAll(        Object.fromEntries
.at(      顶层 await      正则后行断言 (?<= (?<!
{...obj}  ← 对象展开（ES2018）★历史全站事故元凶
const {a, ...rest} = x     ← 对象剩余
**        ← 指数运算符
catch {}  ← 可选 catch 绑定
```
**允许**：`let/const`、箭头函数、模板字符串、`class`、`async/await`（非顶层）、数组展开 `[...arr]`、`Math.max(1, ...arr)`。内核 ≥ ES2017。
⚠️ **`node --check` 抓不到这些**，必须 Grep 逐项确认 0 命中。
⚠️ **两个已验证的误报**（不要当 bug 改）：
- `**` 若出现在**引号内**就是 markdown 字符串（如 `'**一、核心考点**'`），不是指数运算
- `...` 出现在 `[` 之后是**合法的数组展开**；只有出现在 `{` 之后才是禁用的对象展开

### 3.2 顶层声明冲突禁令（"AI 功能集体失效"的真凶）
**禁止**在任何 js 里用 `var/const/let` 重复声明别的文件已存在的全局名。浏览器遇到重复声明会判该文件 **SyntaxError 并整文件拒绝执行**（该文件所有功能静默失效）。
新增全局一律写成：
```js
if (typeof window.xxx !== 'function') window.xxx = function () { ... };
```
**现状基线（本批 QA 实测）**：FATAL **0** ／ RISK **4**（`AUTH_KEY`、`INTERVIEW_QUESTIONS`、`USERS_KEY`、`toastTimer`）／ WARN 46（主要是 `api.js` 与 `app.js` 39 处 `function` 重名）。**守卫写法覆盖率仅 32.9%**。波末门槛：**FATAL = 0 且 RISK = 0**。

### 3.3 版本戳（`?v=`）—— 头号"修好了但用户看到老样子"的原因
- **改任何 `assets/*.js` 或 CSS 后，必须 bump 引用它的 `?v=` 版本戳**，否则用户设备永远加载旧文件。
- **现状**：全站 **567 处，100% 为 `20260916L`**，无杂质戳。目标新戳 **`20260916M`**。
- 补裸路径戳请用**单行内**匹配：`(src|href)="(assets/[^"?]+\.(?:js|css))"` → `\1="\2?v=20260916M"`
- ⚠️ **严禁跨行正则**（跨行会吞掉 `-->` 和标签 = 整页白屏，已踩两次）。
- 项目约定：**波内任务一律不 bump，波末单点统一执行**（bump 会重写 39 个 HTML，是个全局互斥锁）。
- 🔴 **历史遗漏**：**`live_chat.html` 仍是 `?v=20260911c`**，落后 5 天，bump 时必须一并拉齐（正则词边界匹配会漏它）。

### 3.4 共享文件与并行铁律
- **`assets/app.js`（33 页）、`assets/api.js`（30 页）、`assets/common.css`（全站）是共享文件 —— 同一时刻只允许一条线改**，并行写必然后写覆盖先写。
- 本批曾发生一次并发写风险：`app.js` 的核验线与后续接力线同时命中，最终靠"独占 + 交付即交棒"化解。
- 派活时**必须显式列出独占文件清单**。

### 3.5 其它
- **禁原生 `alert/confirm/prompt`**（ADR-3），用页面内 toast（`window.xtToast` / `window.showToast`）或页面内区块。
- 改完必须做**配对校验**：`<!--`/`-->` 相等、剔除 script/style 后 `<div`/`</div>` 相等、`<script`/`</script>` 相等、无损坏特征 `.js\?v=xxx"[^>\s]`。
- 改文件前**先备份**（命名含 `bak`，部署脚本会自动跳过含 bak 的文件）。
- **不删现有功能、保留既有 DOM id 与全局函数名**（外部页面可能引用）。
- **`<head>` 计数别用字面量 `'<head>'`** —— 很多页是 `<head data-page-node-id="...">`，会误判为 0；且 `'<head'` 会误命中 `<header class="topbar">`。另外 **`个人中心.html` 与 `mock_exam*.html` 天然缺 `</head>` 或注释里含 `<body>`**，属历史遗留，不是破损（已用 `git show HEAD:` 回溯确认）。
- **文件清理（铁律：永远最后一步）** —— 根目录现有 **40 个 `_*.py`/`_*.js`/`_*.txt` 临时文件 + `_tmp_l3_qa/`、`_w2t1_img/` 两个目录 + `_w2t1_design.pdf`**，都别急着删。
- **生产操作先问**：删用户数据、改 nginx、部署上线，三件必须用户点头。
- **数据质量红线**：内容全部原创（不抄真题原文）；宁少而准，严禁臆造；新增**数据文件**用 **ASCII 文件名**、UTF-8 无 BOM（中文内容可以，中文文件名不行 —— pscp 有 GBK 坑）。

---

## 4. 未完成工作（按优先级，含可直接执行的完整任务书）

### 4.1 🔴 P0-A：复核并收尾 `assets/ai-service.js`（需求 D）
**文件**：`assets/ai-service.js`（**当前 42,170 B / 1080 行，LF 换行**；基线 21,518 B / 601 行）
**不在范围**：不换模型、不新增模型、不改 Key、**不改 `ai-config.js` 的模型列表和参数**、不碰后端 `/api/ai/chat`。

**已知线索**（本批半成品）：`'429'`×20、`'15000'`×2、`getReader`×7（原 2）、`'timeout'`×0、**`data:image/jpeg;base64,` 仍 2 处**。
→ **降级 / 15 秒超时 / 流式守卫大概率已写；图片 MIME 硬编码可能没修。**

**六项验收要求**（逐项对照，缺哪补哪）：
1. **修 400**：按**真实图片类型**推导 MIME（PNG `\x89PNG`、JPEG `\xFF\xD8`、WebP `RIFF....WEBP`、GIF `GIF8`），**绝不写死 jpeg**。⚠️ 依据文档建议的"base64 需带 `data:image/jpeg;base64,` 前缀"**是错的**（项目现在就是这么写的），别照抄。
2. **降级条件**：**只有 `429` / `5xx` / 网络超时**触发降级；**`400` 不降级**，直接把后端 `error.message` 抛给上层（曾有"提示已切换但切完还是 400"的误判）。按 `ai-config.js` 的 `fallback` 链切换。
3. **15 秒未出首字即切换**：所有请求超时 15 秒；15 秒内无流式首 token → 切下一个**快**模型并提示。慢模型（Qwen3-8B、Qwen3.5-4B）仅手动选择时用。
4. **限流备选**：某模型连续 2 次 429 → 本次会话内跳过（内存态），改用 GLM-4.5-Flash 或 Qwen2.5-7B 作首选。
5. **错误提示到用户侧**：带足 `status`/`message`/`code` 抛出（渲染在 `ai-page.js`）。
6. **流式加固**：`:350` 与 `:420` 两处 `getReader()` **都**要有 `if (resp.body && resp.body.getReader) {...} else {...非流式兜底...}`。

**老内核硬约束（已定案双口径）**：APK WebView **无 `ReadableStream`**，"逐字输出"物理不可能 → 现代浏览器按「2 秒内首字逐字输出」；老内核按「加载态 + 15 秒未出首字自动切换 + 整段返回后正确渲染」验收，**不要求逐字**。
（`assets/net-compat.js` 664 行、顶层声明 0 个、专为此场景写，但**全站零引用**；要彻底解决需在 37 页注入它，属 W4 批量级动作，**先做守卫+兜底，注入留后续**。）

**验收**：11 个模型逐一手动发文字无 400；发图片视觉模型能识别无 400；人为构造 400 **不触发**切换且显示明确错误；模拟 429 按 fallback 链切换；15 秒未出首字自动切换；等待时有「AI 正在思考…」。

### 4.2 🔴 P0-B：收尾需求 C + 侧栏「模型」按钮
**文件**：`AI.html`（49,770 B，**CRLF**）、`assets/ai-page.js`（61,710 B，**LF**）
**已知线索**：`max-width:760px` **已删**、`.ai-model-panel` `z-index: 99 → 330`；但 **`closeSidebar` = 0**、`max-width:none`×1 仍在。

**还差的部分**：
1. **点侧栏「模型」时先关抽屉 + 关遮罩、再开面板**（复用 `toggleSidebar`）—— 这是"点了没反应"的根因（面板被 `z-index:250` 遮罩整个盖住）
2. `.ai-welcome .ai-input-wrap` 的窄屏覆盖 `max-width:none`（`AI.html:491` 媒体查询内）→ 改掉，否则窄屏空态仍会跳变
3. 侧栏按钮文案**跟随选中模型**（复用 `updateModelLabel`/`getSelectedModelId`，别写死「模型」）
4. 顺带排查「设置」（`#aiSettingsBtn` → `openSettings`）；**两处入口必须复用同一个面板**
5. **375px + 1280px 双端截图**（jsdom 无排版引擎，验不了外观 —— 视觉类需求必须出图）

> 机制备忘：全页只有一份输入框 DOM `#aiInputBox`（`AI.html:638-677`），被 `ai-page.js:249-254` `mountInput` 在空态槽 `#aiWelcomeInputSlot`（`:637`）与对话态槽 `#aiDockInputSlot`（`:685`）之间搬动，状态由 `#aiChat` 的 `.in-conversation` 类控制。**保留双槽位机制，只统一外观，不改交互逻辑。**

### 4.3 🟠 P1：复核本批"落盘但未验证"的 11 个文件
按 `更新日志-20260916-第三批.md` §四 的表逐项复核。要点：
- **`assets/cet-read.js`**（L5，31,276→35,497 B）：三种题型在 `.cr-grid` 下右栏不再出现空卡片
- **`AI模拟面试.html` + `assets/iv-prep.js`**（L7，48,684→70,853 / 25,669→31,944 B）：**六阶段状态机交互文档未产出**；六阶段需可完整走通。备份在 `*.bak-pre-l7-20260916`
- **`assets/ppt-tips.js`**（L8，51,565→60,259 B）：确认 P0 五件完成度；`演示.html` / `data-ppt-tips.js` **仍未动**，资源 `assets/images/ppt-tips` 目录**不存在**
- **`错题本.html`**（L11）：`callAI` 0→4，需验降级不白屏、空错题本不报错
- **`个人中心.html`**（L12，+847/-31 行）：收藏持久化实测；确认**未越界改 app.js/api.js**
- **`学习概括.html`（新建 44,186 B）+ `assets/notify.js`（新建 21,408 B）**（L13）：结构配对 + 共用 DOM 七件（`#aiFab`/`#aiPanel`/`#morePanel`/`#toolsPanel`/`#toast`/`#countdownModal`/`.more-overlay`）核验；`notify.js` 顶层声明是否为 0；**页面入口与 30+ 页注入都还没做**

### 4.4 🟠 P1：本批零产出的 5 项（**原封不动，从头做**）
| 线 | 任务 | 目标文件（未被触碰） |
|---|---|---|
| L6 | N9-18 听力精听全面改版（含 N9-10） | `assets/voiceplayer.js`（44,252 B）、`英语.html` |
| L9 | N9-12 PPT 版式库升级 | `PPT版式库.html`、`assets/data-ppt-templates.js`、`assets/tpl-preview.js` |
| L10 | C12 申论批改（接 AI 底座） | `申论刷题.html`（`callAI` 仍 **0**） |
| — | N9-15 我的文章布局 | `blog_wechat.html`、`assets/common.css` |
| L8 余 | N9-14 剩余部分 | `演示.html`、`assets/data-ppt-tips.js` |

**N9-18 硬约束（不可违反）**：**禁改 `vp-back` 语义**（它是 W4「N9-9 全站页头返回补齐」的基准样式，影响 33 页）、**禁改 `#vpMask` id**、**禁改 `SCENES` 结构签名**。

**N9-15 开工第一步必须解 F3**：`renderBlogStats`/`renderBlogMine`/`openBlogStats` 在 `app.js`（约 L6814/L7231/L7622）与 `api.js`（约 L450/L761）**各有一份**，加载序 `app.js` → `api.js`（api.js 后加载会覆盖同名函数）→ **先判明生效副本再动刀**，改死副本 = 白做且不报错。

**C12/C18 的 AI 底座口径（已定案，不要改回去）**：全站 AI 底座是 `assets/ai-service.js` 的 **`window.callAI(funcType, messages, opts)`**，**前端直连**。工单原文写的"统一调用后端 `/api/ai/chat`"**是错的**。公共 Key 有 **10 次/分钟限频** → **不能自动发问**，只在用户点按钮时调。

### 4.5 🟡 P2：批次九 Wave 3~7 未开工项（10 项 + 收尾）
完整波次拆解、文件独占矩阵、依赖冲突矩阵见 `架构-批次九-全线改造-波次计划-20260915.md`。摘要：
- **W3**：N9-1+N9-2 题库筛选（`app.js` + `行测刷题.html`，缺陷复现型，先复现再定改数据还是改逻辑）｜N9-15 我的文章布局｜N9-22A 学习概括页
- **W4**：N9-24 金句库升级（`app.js` + `万能金句库.html` + `表达.html`）｜N9-12 PPT 版式库｜N9-21 写发帖（`社区.html` + `blog_wechat.html` + `api.js`）
- **W5**：N9-13 PPT 案例拆解（`app.js`）｜N9-4 个人中心作品集｜N9-8 筛选标签交互（`行测刷题.html`）
- **W6（最高风险）**：N9-9 页头返回(33 页) + N9-22B 删「学习统计」(28 页) + N9-3 注入 `notify.js`(30+ 页) → **必须合并为一次原子批量**
- **W7**：N9-7 `app.js` 拆分（**最后做**）+ bump + 部署 + 清理
> **`app.js` 接力链**：L1 ✅ → D-1 → N9-1+2 → N9-24 → N9-13 → N9-22B → C8 → C20 → N9-7
> **`api.js` 接力链**：B2 资料卡重构 → N9-21

### 4.6 🟡 P2：D-1 数量关系题库回滚重做（P0 遗留）
2026-09-15 待办总览标注的 P0 遗留，**范围待用户拍板**（回滚到哪个版本 / 重做范围多大）。**开工前需向用户确认，不要自行决定。**

### 4.7 🟡 P3：全站 AI 升级 C-P1~P3（13 项）
**前置**：**C8 —— `app.js` 的 `fetchAiReply` 仍直连旧后端 `APP_AI_DEMO_CONFIG.apiUrl`，未按决策 D2 委托 `window.callAI`**。不做这步后面全是两套底座。
实测**全部目标页 `callAI` = 0**，只有 `aiFab` 悬浮助手统一入口 → 页面级 AI 基本未做。
- **C-P1**：C10 面试真升级、C11 行测 AI 讲解、C12 申论批改
- **C-P2**：C13 四级/词汇、C14 PPT 系列、C15 发帖辅助、C16 金句库生成、C17 高情商/话术
- **C-P3**：C18 错题本 AI 分析、C19 学习数据诊断、C20 悬浮助手上下文感知、C21 拍题贯穿
**准入硬门槛（决策 D8）**：不填 Key 返回真实模型内容 + 断网降级不白屏 + 结果就地渲染 → 真机点一遍才进下一子波。

### 4.8 🟢 P3：更早遗留
| ID | 任务 | 文件 |
|---|---|---|
| B1 | 「我的动态」feed-btn 挪进资料卡操作区 | `个人中心.html`（页内） |
| B2 | 个人资料卡布局重构 | `assets/api.js` `_renderProfileOnline` |
| D-2 | `common.css` L68 全局滚动锁 | **已决策冻结**，见 §1.4 |
| D-3 | 「工具页导入题库跑到博客页」悬案 | ⏸ **等用户补截图，不空转** |
| D-5 | 文件清理（40 个临时文件 + 2 目录 + PDF） | **全批次最后一步** |

---

## 5. 自检与验收工具

| 目的 | 脚本 | 用法 |
|---|---|---|
| **老内核解析级体检（最重要总闸）** | `tools/qa/escheck_es2017.js` | `node tools/qa/escheck_es2017.js` → 必须 **`DONE 0`** |
| **一波多线统一体检（本批新建）** | `tools/qa/wave_check_20260916m.py` | `"$PY" ".../wave_check_20260916m.py" "文件1,文件2,..."` → escheck + 结构配对 + 禁用语法 + 原生弹窗 + 版本戳 + 换行符 |
| **跨文件全局名唯一性扫描（本批新建）** | `tools/qa/global_name_scan_20260916m.py` | 无参扫全工程 → 门槛 **FATAL=0 且 RISK=0** |
| 跨线文件冲突检测（本批新建） | `tools/qa/crossline_conflict_20260916m.py` | 无参用内置 13 线清单自检两两交集 |
| 单页运行时体检 | `tools/qa/page_check.js` | `QA_PORT=8911 node tools/qa/page_check.js "页面.html" "关键词1,关键词2"` |
| 多页导航体检 | `tools/qa/multi_check.js` | 无参跑默认页面集 |
| AI 端到端（真页面） | `tools/qa/ai_jsdom_check.js` | ⚠️ 它**硬编码线上 ORIGIN**，只适合验线上 |
| 机器人球点击/拖动 | `tools/qa/ball_verify.js` | 断言球存在 + 拖动后位置变化 |
| 拉线上文件对比本地 | `tools/qa/fetch_srv_files.py` | 下载线上文件与本地做 md5 对比 |

**⚠️ 工具坑（本批实测，会误导你）**：`tools/qa/_qa_origin.js` 会先探测本地端口上有没有「自己人」服务。若复用了你手工起的 `python -m http.server`，jsdom 会把**所有** `assets/*.js?v=` 报成 `Could not load script` → `typeof callAI = undefined`，**看着像全站 js 崩了，其实是噪声**。
**正确用法**：显式指定一个**没被占用的** `QA_PORT`（8911~8920），逼脚本自启内置 Node 服务；输出头必须显示「**本脚本自启的本地静态服务**」才可信。

**每批必跑的自查清单**：
- [ ] 改动过的 js 跑 `node --check`
- [ ] `escheck_es2017.js` = `DONE 0`
- [ ] `<!--`/`-->`、`<div`/`</div>`、`<script`/`</script>` 配对平衡
- [ ] 跨文件全局名 **FATAL = 0 且 RISK = 0**
- [ ] 无 §3.1 禁用语法（`node --check` 抓不到，必须 Grep）
- [ ] 原生 `alert(`/`confirm(`/`prompt(` = 0
- [ ] 改动过的资源 `?v=` 已 bump；全站「不带 `?v=` 的 assets 引用」= 0
- [ ] 换行符未被改混（CRLF/LF 按 §2.4 第 5 条）

**⚠️ 诊断经验（务必传承）**：
1. **只测服务层会假绿** —— 必须用 **jsdom 加载真实完整页面**（历史曾用 vm 只加载 `ai-config.js`/`ai-service.js` 得到"真模型回答"，掩盖了全局声明冲突）。
2. **"某个模块集体失效"优先怀疑该 js 解析失败**（跑 `escheck_es2017.js` 最快），而不是逐个按钮排查。
3. jsdom 报 `Not implemented: navigation to another Document` 是 **jsdom 自身限制，不是 bug**，忽略。
4. 视觉类需求（弹窗、输入框、倒计时卡）**必须真机/浏览器截图对比**，375px 与 1280px 都要出图 —— jsdom 没有排版引擎，验不了"看起来小一档"。
5. **判断"文件是否被改"用 mtime，不要用 git**（见 §1.2）。

---

## 6. 部署流程

**照抄 `tools/deploy_update_20260916k.py` 改 3 处：**
1. `STAMP = '20260916M'`（新版本戳）
2. `files = [...]`（要上传的 `assets/` 文件列表；纯 HTML 改动则留空 `[]`）
3. 远端校验段的 grep 关键词

脚本会自动：bump 全部根 HTML 的 `?v=` → 打包 `tar.gz` → `pscp` 上传 → `plink` 远端解压 → `curl` 状态码 + `grep` 内容校验。
**先跑 `SW_DRY_RUN=1` 预演确认打包正常，再真跑。**

### 6.1 部署脚本的坑（血泪清单）
1. **凭据必须是 `root@ip`**：正则若只抓到裸 IP，plink/pscp 会用本地用户名认证 → `Access denied`，报错信息是极具迷惑性的「password not accepted」。模板脚本已处理。
2. **远端备份命令必须容错**：`tar -czf 备份 <filelist>` 在文件不存在时会让 `&&` 链断掉 → **根本不解压**（现象：上传成功但线上 grep 报 No such file）→ 备份命令后加 `|| echo BACKUP_SKIP_NEW`。
3. **部署后一定实测**：`curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1/xxx.html` + 对关键字符串 `grep`，不要只看上传 exit code。
4. **版本戳**：新上的 js 必须带 `?v=` 且 bump，否则用户拿到旧文件。
5. 别用 `sw` 当变量名（与 shell 冲突）。
6. 用 `curl -s -D -` 看响应头，不要用 `-I`；期望 `Cache-Control: no-cache`。
7. 中文文件名 URL 要 quote。
8. **重命名后线上存量链接会 404**（书签/外链指向 `学习博客.html` 等旧名）。可选在 nginx 加 301 旧→新跳转兜住 —— **属生产操作，需用户点头**。已装 APK 内是旧快照、页面自包含，不受影响。
9. **部署后可先排除 `更多.html`**（本批已修好，但若整体未验证完，可只部署已验收部分）；改名后线上会短暂出现「6 新页 + 6 旧页」并存，旧页仍可访问。

---

## 7. 回滚点与备份

### 7.1 本批新建的备份（部署脚本会自动跳过含 bak 的文件）
| 文件 | 字节 | 用途 |
|---|---|---|
| `更多.html.bak-pre-morefix-20260916` | 35,295 | 破损前快照（⚠️ **不能当干净基准** —— 它是破损后的状态；干净结构基准=线上 `20260916k`） |
| `AI模拟面试.html.bak-pre-l7-20260916` | 48,684 | L7 改动前 |
| `assets/iv-prep.js.bak-pre-l7-20260916` | 27,428 | L7 改动前 |

### 7.2 上一批的开工前备份
`*.bak-pre-batch2-20260916`：根目录 16 个 + `assets/` 8 个（详见 `交接文档-20260916c` §7.1）。
⚠️ `更多.html.bak-pre-cfix-20260916` 与 `工具.html.bak-pre-cfix-20260916` **是破损后的快照，不能用于回滚**。

### 7.3 干净结构基准
**线上 `20260916k` 版**（304 行、`<script>` 14/14、`<div>` 155/155、注释 20/20、`#importerView` 0）。
用 python `urllib` + `urllib.parse.quote` 抓下来做**结构对照**（它没有「导入题库」功能，只能当参照，不能直接替换）。

### 7.4 当前 md5 快照（本批存档时，可用于事后校验）
| 文件 | 字节 | md5 |
|---|---|---|
| `更多.html` | 25,758 | `bb6b80bf1b58709c791cc088b6cbd1a4` |
| `工具.html` | 28,247 | `48ffd55ea2aea9f9bc1fd729d665463b` |
| `AI.html` | 49,770 | `cef4147b0cb4a30ee455d6e49d8a67ff` |
| `演示.html` | 52,359 | `32ec38e5d406d96e0e726acbd99c34e4` |
| `学习工作台.html` | 52,029 | `a24c6b9139172ee214da150ffa7f702f` |
| `个人中心.html` | 102,718 | `b25a95e2b94fc00d8959c3ded287f282` |
| `错题本.html` | 36,868 | `0e0f7eacb956193478278a368639e839` |
| `AI模拟面试.html` | 70,853 | `cd3e33caee4ca013555a834c65cdf835` |
| `学习概括.html` | 44,186 | `e70798f19a0f96c1f1fb42d8c76ac3fb` |
| `assets/app.js` | 7,073,398 | `b0f452c8485fd9f4c5e4354d75e26706` |
| `assets/api.js` | 111,147 | `5cb5ef2517e24b4d27f346737cbe63d4` |
| `assets/ai-service.js` | 42,170 | `395a1453aba10c8cbf9e244c68c2916a` |
| `assets/ai-page.js` | 61,710 | `e9dfec6081f5021ac8ef080fe520fa92` |
| `assets/common.css` | 121,032 | `39ccb89cb1d0a046b3ee17f5e8caf5b6` |
| `assets/cet-read.js` | 35,497 | `b5f307e400b89ea419142dee1efa57de` |
| `assets/iv-prep.js` | 31,944 | `22d34cf4aa432200688bdd8613aa59c9` |
| `assets/ppt-tips.js` | 60,259 | `8b054de321930f9f46493ad8061c5a8d` |
| `assets/notify.js` | 21,408 | `3eb9b7b6934bcd1b3e1bf1dad187d262` |

---

## 8. 需求原文索引（追溯需求时读这些）

| 文档 | 内容 |
|---|---|
| `更新日志-20260916-第三批.md` | **本批实际落盘的全部变更**（mtime 表 + 签名证据 + 未验证清单） |
| `计划书-20260916-第三批执行（分工与波次）.md` | 13 线名册 + 接力链 + Wave 0~7 排期 + 文件独占矩阵 |
| `交接文档-20260916c-第二批执行现状与对接.md` | 上一份总纲（含 20 项实测校正表） |
| `交接文档-20260916b-第二批需求执行手册.md` | 更早一份总纲（需求 A~D 逐项要求与验收） |
| `架构-批次九-全线改造-波次计划-20260915.md` | 批次九 23 项 7 波依赖分析 + 冲突矩阵 |
| `任务清单-待办总览-20260915.md` | A/B/C/D 四组待办 + 主理人已拍板的 8 条决策记录 |
| `AI模型测试报告与修复需求-20260916.md` | 需求 D 依据（⚠️ 其第 61 行的修法建议**本身有误**） |
| `需求文档-*.md`（共 11 份） | PPT 系列 / 金句库 / 听力精听 / 写发帖 / 学习概括 / 每日一题 等单项需求原文 |
| `用户需求与决策总账.md` | 用户全部需求与拍板记录（**已拍板项照办，不要再问用户**） |
| `星途平台改进-需求清单-状态盘点-2026-09-13.md` | 原始 30 条需求状态 |

---

## 9. 工作方式偏好（用户多次强调，务必遵守）

1. **「直接给出结论，不用反复验证」** —— 命令/脚本**各跑一次即判定**；不做多轮回读、不做多轮自审。发现问题直接给结论 + 建议，不要为了"再确认一下"多跑几轮。**这条适用于调研/核实类工作**（外部文档、方案、代码行号只做**一次**抽验即下判断）。
2. **多线并行** —— 文件零重叠的前提下尽量并行，不要串行单人作业。
3. **文件独占原则（血泪教训）** —— `app.js`(33 页) / `api.js`(30 页) / `common.css`(全站) **同一文件同一时刻只允许一条线改**。派活时必须显式列出独占文件清单。
4. **版本戳 bump 是全局互斥锁** —— 波内任务一律不 bump，波末单点统一。
5. **生产操作先问** —— 删用户数据、改 nginx、部署上线，三件必须用户点头。
6. **数据质量红线** —— 内容全部原创；宁少而准，严禁臆造；新增数据文件用 ASCII 名 + UTF-8 无 BOM。
7. **不删现有功能、保留原有 DOM**（铁律，违约即打回）。
8. **交付即交棒** —— 共享文件线做完立刻把独占权交给下一棒，关键路径不能空转。

---

## 10. 建议的接手顺序

1. **先读** `更新日志-20260916-第三批.md` **§二「落盘实况」表** —— 建立"哪些是真的、哪些是半成品"的地图。**这一步不读完不要动代码。**
2. **跑三把闸确认基线**：`escheck_es2017.js`（期望 `DONE 0`）、`wave_check_20260916m.py`（对 §4.3 的 12 个文件）、`global_name_scan_20260916m.py`（期望 FATAL/RISK 不新增）。
3. **收尾 `assets/ai-service.js`（§4.1）** —— 它是 P0 且最有价值（AI 400 是用户直接痛点）；复核 `data:image/jpeg;base64,` 那 2 处是否真按魔数推导了 MIME。
4. **收尾需求 C（§4.2）** —— 补 `closeSidebar` 调用 + 窄屏覆盖 + 双端截图。与 §4.1 文件零重叠，可并行。
5. **并行启动本批零产出的 5 项（§4.4）** —— `voiceplayer.js`/`英语.html`、`PPT版式库`系列、`申论刷题.html`、`blog_wechat.html`、`演示.html`，文件互不重叠。
6. **复核 §4.3 的落盘半成品**，判定完成度：缺的补、错的改、超范围的记录。
7. **批次九 Wave 3~5**（§4.5）→ **Wave 6 全站原子批量**（最高风险）→ **Wave 7 收尾**。
8. **波末**：统一 bump `20260916L` → `20260916M`（567 处，含 `live_chat.html` 的 `20260911c`）→ `SW_DRY_RUN=1` 预演 → 部署 → 线上复检（`QA_ORIGIN=http://110.42.134.62`）。
9. 部署后提示用户**强制刷新**（清缓存/重开），否则设备上仍是旧文件。
10. **文件清理放在最后**（用户铁律）。

---

## 11. 如果需要拉子代理（本批的编制与踩坑）

| 角色 | `name` | `subagent_type` |
|---|---|---|
| 工程师 | `software-engineer-任务一` … | `software-engineer`（**有 Bash**） |
| QA | `software-qa-engineer` | **`general-purpose`**（`software-qa-engineer` **没有 Bash**，6 秒即挂） |

- **429 期间**在 spawn 时指定 `model: "lite"` 立刻可用；**UI 切模型对子代理无效**。
- 子代理看不到本对话 → **每条指令必须自带完整上下文**（项目铁律 + 独占文件 + 落点 + 验收口径 + 环境坑）。
- 要求子代理完成后 **`SendMessage` 回报**（它在对话里直接输出，主理人看不到）。
- 本批的有效做法：把「唯一可写树 / ES2017 禁用语法清单 / 全局声明守卫 / 禁原生弹窗 / 波内不 bump / 换行符保持 / bash 无 coreutils 的绕法 / python+node 绝对路径 / `QA_PORT` 坑 / 命令各跑一次即判定」写成**固定前言**贴进每条派活指令。

---

*交接人：主理人 齐活林 · 2026-09-16 14:25。所有基线数据均为本机实测（mtime 采样 + md5 对照 + git numstat + 签名核验 + escheck 实测），非文档转述。*
