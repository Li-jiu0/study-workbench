# 交接文档・学习工作台整改批次三

> 生成：2026-09-13 00:55 ｜ 交接方：WorkBuddy 软件开发团队（software-workbench-b3）
> 读者：接手的 AI 助手或开发者。本文档自包含，无需原始对话即可继续工作。



***

## 一、一句话现状

批次三整改（依据 `整改处理方案-批次三.md`）**代码主体已完成且语法验证通过，数据框架已就位**；剩余为 3 处代码小修 + 2 项数据补产 + 1 处历史数据清洗 + 最终 QA 回归。



***

## 二、项目速览



* 项目根：本文件所在目录（`学习工作台`，亦称「星途」）

* 形态：32 个纯 HTML 页面 + `assets/`（app.js 6659 行等 27 个 JS）+ `server/` FastAPI 后端（本批不碰）+ `android/` APK（本批不碰）

* 入口页 `学习工作台.html`，首页由 `assets/app.js` 渲染

* 本批需求来源：2026-09-11 豆包体验报告（4.7 分）遗留整改

### 铁律（违反即打回，接手者必须遵守）



1. 扩展必三件套：DOM 容器 + CSS + 空函数并标注【后续扩展点】

2. 不删原功能、保留原有 DOM；旧数据字段保留兼容旧存档

3. 改 `app.js`（6600+ 行）前必须备份 + 改完 `node --check` + div 配平

4. 新增数据文件一律 ASCII 文件名、UTF-8 内容

5. 图标必须内联 SVG（`fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"`，禁 mask/filter/symbol use），**全站零外链零 CDN**

6. 主题走全局 `isDarkMode`，禁止重写 `body.className`

7. 版本号统一 bump（当前已到 `20260913a`），工具 `tools/bump_versions_0913a.py`

8. 合并数据**不调用 saveData**（用户进度零改动）

9. 验证：`node --check` + jsdom 断言「零未捕获异常 + showToast 可用」

### 实测基线（文档转述不可信，以下为实测，接手者以此为准）



| 项                            | 实测值                                                                |
| ---------------------------- | ------------------------------------------------------------------ |
| 内置题库 `EXAM_BANK`（app.js）     | **60 题**，id 1–60，字段 `id/type/sub/diff/q/o/a/x/tip`（简写体系）           |
| 内置词库 `CET_VOCAB`（app.js）     | **466 词**，首字母**仅 a–f**，9 字段全填充（含 example）                          |
| `assets/data/exam-bank.json` | 仅 14 题，非全量；与内置存在 id 重叠（双源问题）                                       |
| 假数据位置                        | 原 `app.js:250-262`，渲染入口 1241/1267/1285（已改造）                        |
| 真题模考                         | **已做实**（`mock_exam*.html` + `data/mock-papers.js`），方案 6a「改名」已作废    |
| 图标库                          | `assets/icon-map.js` 已存在（内联 lucide SVG + data-icon 扫描），**不得新建第二套** |



***

## 三、已完成（已验证）



| 项                                                                         | 文件                                                                               | 状态                                                   |
| ------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ---------------------------------------------------- |
| 首页真实化：`computeModuleProgress` / `computeWeakPoints` 实时计算 + 渲染改造 + 「未开始」灰态 | `assets/app.js`                                                                  | ✅ 语法过                                                |
| 空态三件套：`renderEmptyState` + `.empty-hint` CSS                              | `assets/app.js` + `assets/common.css`                                            | ✅                                                    |
| 弹窗统一：`.app-modal`（CSS 8 处）+ `modal-lock` + `openAppModal`                 | `assets/app.js` + `assets/common.css`                                            | ✅                                                    |
| 底部导航去 emoji                                                               | `学习工作台.html`                                                                     | ✅ 已清零                                                |
| 版本号 bump → `20260913a`（12 处）                                              | `学习工作台.html`                                                                     | ✅                                                    |
| 词库增量合并 `loadVocabExt`（键 = word 追加不覆盖）                                     | `assets/app.js`                                                                  | ✅                                                    |
| 题库增量分片 7 个（每片 20 题共 140 题，id 101–240）                                     | `assets/data/exam-bank-ext-{figure,define,analogy,logic,verbal,quant,data}.json` | ✅ 质检过：0 重复 / 0 坏题 / 0 泄题                             |
| 分片索引                                                                      | `assets/data/exam-bank-ext-index.json`                                           | ✅（结构 `{version,comment,shards:[{file,type,count}]}`） |
| 听力 3 篇数据（短篇新闻 / 长对话 / 篇章理解，结构 `{version,comment,scenes}`）                 | `assets/data/listening-ext.json`                                                 | ✅ 数据在，**未接线**                                        |
| 泄题扫描脚本（可跑，退出码语义正确）                                                        | `tools/qa/scan_question_leak.py`                                                 | ✅                                                    |
| 备份                                                                        | `assets/app.js.bak-0913`                                                         | ✅                                                    |

设计文档：`deliverables/software-company/batch3-incremental-design.md`（T01–T05 任务表）；增量 PRD：`deliverables/software-company/batch3-incremental-PRD.md`。



***

## 四、待办（按优先级，接手者按序执行）

### P0-1 修正 interviewDone 偏差（唯一已知代码 bug）

`assets/app.js` 中 3 处 `interviewDone`：前工程师误新增了该字段并用 `appData.interviewDone || 0` 计算面试进度 ——**全仓不存在面试场次记录入口，进度会恒为 0%**。

改法：



```
// computeModuleProgress 内，替换 interview 分支：

res.interview = Math.min(100, Math.round((((appData.viewedContent || {}).ivQuestions) || \[]).length / MODULE\_PROGRESS\_TARGET.interview \* 100));
```

`appData.interviewDone: 0` 字段保留并加注释「已废弃不再使用，兼容旧档」。改完 `node --check`。

### P0-2 题库分片加载（代码侧未写）

在 `assets/app.js` 已有的 `exam-bank.json` fetch 覆盖逻辑（约 T02 注释附近）之后，仿照其模式新增：



* fetch `assets/data/exam-bank-ext-index.json`（feature-detect：失败即跳过，离线兜底）

* 按 `shards[].file` 逐片 fetch，`questions` 数组中 **id≥101 只 push 不覆盖内置**，不调 saveData

* 合并完成后如 `typeof renderExamQuestion === 'function'` 则重渲染

### P0-3 听力接线（数据已备，代码未写）

`assets/voiceplayer.js`：仿同样 fetch 模式加载 `assets/data/listening-ext.json` 的 `scenes`，合并进内置 6 场景（失败回退内置）。注意 voiceplayer.js 已有备份要求。

### P1-4 activityLog（最近学习的相对时间）

现状：`wrongQuestions/favoriteQuestions` 只存题 id 无时间戳，`vocabRecords/examRecords` 只到日期，「3 分钟前」这类相对时间做不出来。

按设计文档 §3 新增 `activityLog` 环形日志（上限 200 条，写入点：做题 / 背词 / 看内容 3 处），渲染 `recentLearning` 时优先用 activityLog，旧档无日志回退「今天 / 昨天 / N 天前」。

### P1-5 历史泄题清洗（3 处）

`assets/data/exam-bank.json` id 2/8/41 题干含「旋转 / 移动」等规律描述（scan 已定位原文）。把规律描述从题干移到 `x` 解析字段。改完重跑 `python tools/qa/scan_question_leak.py` 须全通过。另：内置 `EXAM_BANK`（app.js 内 60 题）建议同规则过一遍。

### P2-6 词库补产（第二批，不阻塞上线）

`assets/data/vocab-cet4-ext.json` 现状：600 词仅覆盖 **g–s**，且 `example/example2/collocation/synonym/antonym/root` 全空（内置 466 词全字段、全带例句，反差过大）。



* 补 t–z 段高频词（+150\~200 词，与现有 600 及内置 466 按 word 去重）

* 字段补全：至少 300 条做到全字段（底线：每条 1 个 example）；其余标注第二批

* 注意版权：自撰释义，不抄词典原文

### P2-7 QA 回归（全部做完后统一做，用户已确认此节奏）



* 4 个 JS `node --check`（app.js / icon-map.js / voiceplayer.js / mini.js）

* jsdom 断言：零未捕获异常 + showToast 可用 + navigateTo 正常 + `#countdownModal` 完好

* 手工冒烟：全新 localStorage 首页应显示「未开始」灰态而非假数据；做题后薄弱点出现真实条目

* 老存档回归：灌库后 `vocabLearned` / `wrongQuestions` 不丢失



***

## 五、明确不做（本批范围外）



* 问题 5 首屏压缩（用户拍板维持现状）

* 真题模考改名（前提失效，已作废）

* `server/`、`android/`、`data/mock-papers.js` 一概不碰

* APK 重新打包（等全部批次完成后再说）



***

## 六、常用命令



```
cd <项目根>

node --check assets/app.js          # 语法检查

python tools/qa/scan\_question\_leak.py   # 泄题扫描（退出码 1=有命中）

python tools/bump\_versions\_0913a.py     # 版本 bump（如需再 bump 用 20260913b）
```

启动后端（如需联调）：`cd server && python -m uvicorn main:app --host 0.0.0.0 --port 8000`，健康检查 `http://localhost:8000/api/health`。



***

## 七、续作记录（2026-09-13 01:00–02:30）

> 记录人：寇豆码（工程师）・配合：高见远（架构）/ QA 线
> 本章为
>
> **续作**
>
> 记录：§ 一～§ 六 保持不变（仍有参考价值），被本轮推翻的条目在 §7.3 用「已过时」标注，未直接删除。
> 当前线上版本：
>
> `20260913b`
>
> ；当前题库总量：
>
> **300 题**
>
> ；当前词库总量：
>
> **1066 + 增量 777**
>
> （内置 466 + 增量 777）。

### 7.1 本轮已完成并验证



| # | 项                         | 落地位置                                         | 实测结果                                                                                                                                                                                                                                                                                                                          |
| - | ------------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | **P0-1 面试进度恒 0%**         | `assets/app.js` `computeModuleProgress`      | 改用 `viewedContent.ivQuestions.length / INTERVIEW_QUESTIONS.length`（**实测分母 8**）。新增 `pct(n,d)`：分母 ≤0 一律返回 0，杜绝 NaN/Infinity。`appData.interviewDone` **保留为废弃字段**（仅兼容旧档，不再参与计算）                                                                                                                                                   |
| 2 | **P0-2 题库分片加载**           | `assets/app.js:400-430`                      | ⚠️ **原代码是坏的**：`EXAM_BANK_EXT_SHARDS` 硬编码中文文件名（`exam-bank-ext-图形推理.json` 等），磁盘上实际是 ASCII 名（figure/define/analogy/logic/verbal/quant/data），**140 题一条都没加载过**。已改为先 fetch `exam-bank-ext-index.json`，再按 `shards[].file` 逐片 `loadExamBankShard()`：id<101 跳过、已存在 id 跳过（只 push 不覆盖）、**全程不调 saveData**、合并后 `renderExamQuestion` 存在才重渲染 |
| 3 | **P0-3 听力接线**             | `assets/voiceplayer.js:111-124`              | 前任**已实现**（`loadListeningExt()`，按 key 合并 `listening-ext.json` 的 news/longconv/passage，已存在 key 不覆盖，失败 `.catch` 回退内置），本轮**未改码**，仅修正注释（内置实为 **9 个**：6 个日常情景 + 3 个题型）                                                                                                                                                              |
| 4 | **P1-4 activityLog 环形日志** | `assets/app.js`                              | 新增 `activityLog`（上限 **200** 条）+ `writeActivity()` / `formatRelTime()` / `formatDateRel()` / `dateToTs()` / `activityToItem()`；`computeRecentLearning()` **三级回退**（activityLog → 旧档 `lastReview` 日期 → 空态）。写入点 3 处：`recordVocabLearn` / `recordExamQuestion` / `toggleFavorite`（均在既有 `saveData()` 之前，不新增落盘）。老存档缺省 `[]`，零异常       |
| 5 | **P1-5 泄题清洗**             | `assets/data/exam-bank.json` + `app.js` 内置   | id **2/8/41** 的规律描述从 `q` 移入 `x`（只扫 `q`，`x` 里出现「顺时针」等允许且必要）；顺手修掉 id 8 解析自相矛盾（原文先「不重合」后「重合」）。两套数据已同步。`scan_question_leak.py` **零命中，退出码 0**                                                                                                                                                                                      |
| 6 | **P2-6 词库补产**             | `assets/data/vocab-cet4-ext.json`            | 600 → **777 词**（t–z 新增 **177**，去重后重复 **0**；分布 t:74 u:24 v:39 w:32 y:6 z:2）；`example` 非空 **330 条**；字段严格 9 字段、无新字段；`version`→`20260913b`。代码零改动                                                                                                                                                                                  |
| 7 | **题库第二批**                 | `assets/data/exam-bank-ext-2-*.json` + index | 新增 7 片 **100 题**（id 301–400），index 的 `shards` **7 → 14**，总量 **200 → 300 题**。**代码零改动即生效**（印证「第二批只改 JSON + 索引」的设计）                                                                                                                                                                                                              |
| 8 | **版本 bump**               | `tools/bump_versions_0913b.py`               | `20260913a` → `20260913b`，**27 页 299 处**。5 个不加载 app.js 的页面保持 a：AI 模拟面试 / PPT 素材库 / 四级经验分享 / 好友申请 / 登录                                                                                                                                                                                                                         |
| 9 | **QA 回归**                 | `tools/verifier/`                            | `verify_batch3_0913_p0p1.js` **43/43**、新增 `verify_batch3_0913b.js` **33/33**、`verify_versions_0913b.js` **11/11**；`python tools/qa/scan_question_leak.py` **零命中**                                                                                                                                                             |

### 7.2 QA 查出并修复的缺陷（重点，避免后人重踩）（→ 第 1、2 条的最终结论已更新，见 7.7）



1. **题库答案 100% 集中在 A** —— `app.js:4603` 渲染时选项**不做乱序**，导致 `a=0` 等价于「永远选 A 全对」，会污染正确率与薄弱点统计。

   修复脚本：`tools/verifier/fix_answer_distribution_0913b.js`（`--all --apply`），确定性重排 + **三层校验**（往返校验 / 正确项文本不变 / 结构不变），备份在 `tools/verifier/_backup_0913b/`。

2. **bump 脚本正则漏匹配** —— `bump_versions_0913b.py` 的 `RE_SCRIPT` 只匹配 `src="assets/...`，漏掉 `data/mock-papers.js` 这类**无&#x20;**`assets/`**&#x20;前缀**的资源，导致 mock\_exam 三页残留 `20260912c`（已修）。

   👉 **下批 bump 脚本必须修这个正则**（放宽为 `src="(assets|data)/...` 或按扩展名匹配）。

3. **验证脚本断言写死** —— `verify_batch3_0913_p0p1.js` 有 2 条断言写死题库总数 200，第二批后**已改为 300**。

### 7.3 旧章节状态对照（§ 一～§ 六 未改，此处仅标注）



| 旧条目                                                              | 现状                                                       |
| ---------------------------------------------------------------- | -------------------------------------------------------- |
| § 四 待办 P0-1 / P0-2 / P0-3 / P1-4 / P1-5 / P2-6                   | **全部已完成**（见 §7.1）                                        |
| § 四 待办 P2-7 QA 回归                                                | **已完成**（见 §7.1 第 9 项）                                    |
| ~~§ 二 铁律 7：版本号当前已到 ~~`20260913a`~~，工具 ~~`bump_versions_0913a.py` | **已过时**：现为 `20260913b`，工具 `tools/bump_versions_0913b.py` |
| ~~§ 二 实测基线：题库分片 7 个共 140 题 id 101–240~~                          | **已过时**：现 14 片 240 题（101–240 + 301–400），总量 300           |
| ~~§ 二 实测基线：~~`vocab-cet4-ext.json`~~ 600 词 /example 全空~~         | **已过时**：现 777 词，example 非空 330 条                         |
| ~~§ 二 实测基线：内置听力 6 场景~~                                           | **已过时**：内置 6 情景 + 合并 3 题型 = 9                            |
| § 二 铁律 3（改 app.js 前备份 + `node --check` + div 配平）                 | **继续有效**，本轮已执行，备份见 §7.5                                  |
| § 二 铁律 8（合并数据不调 saveData）                                        | **继续有效**，本轮专项断言覆盖（断言 27/40/41/42 全 PASS）                 |

### 7.4 仍存在的遗留（不阻塞上线）



1. **双源重叠**：内置 `EXAM_BANK` 与 `assets/data/exam-bank.json` 在 id 1-10 / 41-44 上重叠 —— 既有设计：同 id 以 JSON 覆盖内置，内置仅离线兜底。

2. **四个废弃字段仍在默认存档**：`interviewDone` / `moduleProgress` / `weakPoints` / `recentLearning`。建议下批清理（需先确认无老存档依赖）。

3. **词库**：x 段 0 词（`xylophone` 等不属四级高频，为保质量未灌低频词）；距 PRD 目标 2000 词还差约 **1200 词**，属第二批长尾。

4. **全站答案分布与解析文案的两处细节由 QA 线处理中**：id=301 解析文案、第一批答案均衡化。

5. **泄题扫描的两个观察项（非缺陷）**：id=122 题干含「递减」是术语固有组成；id=307 含「序列」为观察项。

### 7.5 本轮新增 / 备份文件清单（便于下批定位）

**数据**



* `assets/data/exam-bank-ext-2-{figure,define,analogy,logic,verbal,quant,data}.json`（第二批 100 题）

* `assets/data/exam-bank-ext-index.json`（已更新：shards 7 → 14，version 20260913b）

* `assets/data/vocab-cet4-ext.json`（已更新：777 词，version 20260913b）

**工具 / 验证**



* `tools/bump_versions_0913b.py`（版本 bump，注意 §7.2 第 2 条的正则问题）

* `tools/gen_exam_bank_batch2.py`（第二批题库生成）

* `tools/verifier/verify_batch3_0913b.js`（33 断言）

* `tools/verifier/verify_versions_0913b.js`（11 断言）

* `tools/verifier/fix_answer_distribution_0913b.js`（答案分布修复）

**备份**



* `assets/app.js.bak-0914`、`assets/voiceplayer.js.bak-0914`

* `tools/verifier/_backup_0913b/`

### 7.6 下批开工提示



1. 先读 §7（本轮）再读 § 一～§ 六；**§7.3 标注了所有已过时的旧基线**，不要照抄旧数字。

2. 新建 bump 脚本时**务必先修&#x20;**`RE_SCRIPT`**&#x20;正则**（否则 `data/` 前缀资源会漏 bump）。

3. 第二批内容（词汇 →2000 / 题库 →400 / 听力全量）只需往 `assets/data/` 丢 JSON + 改 index + bump，**代码零改动**。

4. 任何入库题先过 `python tools/qa/scan_question_leak.py`（退出码 1 = 有命中）。

### 7.7 最终定稿数据（QA 二轮后）

> 本节为
>
> **最终定稿**
>
> ：7.1–7.6 中与本节冲突之处
>
> **以本节为准**
>
> （主要是 7.1 第 9 项的验收数字、7.2 第 1/2 条）。
> 定稿时间：2026-09-13（QA 二轮后）・线上版本 
>
> `20260913b`
>
> ・题库 300 题・词库增量 777 词

**① 题库答案分布（本轮最重要的修复）**



* **根因**：`app.js:4603` `renderExamQuestion()` 渲染时**选项不做乱序**，而增量题答案原本 100% 是 `a=0` ⇒ 用户「永远选 A 全对」，正确率与薄弱点统计被彻底污染。

* **修复**：确定性重排 `t = (3 * id + 1) % 4`（可复现、可回归），脚本 `tools/verifier/fix_answer_distribution_0913b.js`（`--all --apply`），三层校验：**往返校验 / 正确项文本不变 / JSON 结构不变**。

* **修复后分布（回退 3 题后的最终定稿值）**：


  * 增量 **240 题 → A59 / B63 / C58 / D60**

  * 全站 **300 题 → A70 / B96 / C68 / D66**

  * 说明：全站 B 偏高（96）来自**内置 60 题本身 B=33**，该部分不在本批授权范围、未改动；回退的 3 题原始答案**都是 B（不是 A）**，所以回退是往 B 方向回。增量分布仍在均衡区间（最大 63 / 最小 58）。

* **已回退 3 题**：id=**103 / 109 / 115** —— 这 3 题的**选项文本本身就是字母 A/B/C/D**，重排会造成「按钮标签 C、选项文本 B」的错位，已恢复原始顺序。**下批改题时遇到「选项文本是字母」的题必须跳过重排。**

**② 修复过程中引入又修掉的 4 处内容损坏（重要教训，必须记住）**



* **教训**：字母置换正则**区分不了「选项字母」与「领域实体 / 占位符字母」**；**往返校验只能证明置换自洽，证明不了该字母确实是选项引用** —— 校验过了 ≠ 语义正确。

* **误置换 4 处（全部已回退）**：



| id  | 字段    | 原文性质 | 误伤内容        |
| --- | ----- | ---- | ----------- |
| 108 | `x`   | 领域实体 | 「元素 A 与 B」  |
| 155 | `x`   | 人名   | 「推 A 没去」    |
| 227 | `tip` | 占位符  | 「A 是 B 的一种」 |
| 384 | `tip` | 集合名  | 「\|A∪B\|」   |



* **处置**：`tip`**&#x20;字段今后完全不参与字母置换**；`verify_batch3_0913b.js` 新增 **E8 / E9 两条回归锁**锁住这 4 处，防止复发。

**③ bump 脚本缺陷修复（7.2 第 2 条的最终结论）**



* `RE_SCRIPT` / `RE_LINK` 原写死 `assets/` 前缀，漏掉 `data/mock-papers.js` 这类无前缀资源 ⇒ mock\_exam 三页残留 `20260912c`。

* 已改为**任意相对路径 + 排除外链 / 锚点**，行过滤器与正则**同源**，并新增**跨行 HTML 注释状态机**（防「正则吞注释」事故复发）。命中数 **311 → 314**，**误匹配 0**；本轮**未执行&#x20;**`--write`**，HTML 零改动**。

* ⚠️ **险情（下批必须立为硬门禁）**：新正则分组一旦写错，会产出 `src="?v=xxx"`，**抹掉全站 302 处资源 URL**。是靠「`--check` 必须为 0 改动」这一**幂等信号**抓到的。

  👉 **下批铁律：bump 脚本先&#x20;**`--check`**，改动数必须为 0（幂等）才允许&#x20;**`--write`**；否则一律拒绝落盘。**

* **遗留**：`PAGES` 仍是硬编码 27 页名单，新增页面需手工补；`<img>`、CSS `url()` 不在版本号体系内。

**④ 最终验收数字（全绿）**



| 验证                                          | 结果                          |
| ------------------------------------------- | --------------------------- |
| `tools/verifier/verify_batch3_0913b.js`     | **35 / 35**（含新增回归锁 E8 / E9） |
| `tools/verifier/verify_batch3_0913_p0p1.js` | **43 / 43**                 |
| `tools/verifier/verify_versions_0913b.js`   | **11 / 11**                 |
| `python tools/qa/scan_question_leak.py`     | **0 命中，exit 0**             |



* 备份：`tools/verifier/_backup_0913b/` 存有 **14 个分片的最初原始版本**（脚本已加「已存在备份不覆盖」保护，二次执行不会把第一次的原件冲掉）。

**⑤ 词库第三批 + 发布前检查线（2026-09-13 02:50 收口，最终定稿）**



1. **词库第三批 ✅ 已完成**：`assets/data/vocab-cet4-ext.json` 增量 **777 → 1240 词**（超过任务区间上界 1227，实际 +463），`example` 非空 **896 条（≥700 达标）**；字母覆盖 a–z（b–f 段归内置 466 词）。距 PRD 2000 词目标还差 **约 760 词**，属第四批长尾，**不阻塞上线**。

2. **发布前检查线 ✅ 四项全过**：

* **全站零外链**：32 页 HTML + assets 全部 JS 扫描，外部资源引用 **0**（API 基址走 `assets/config.js`，属功能依赖非资源外链）。

* **老存档回归**：`verify_batch3_0913_p0p1.js` 专项断言覆盖（旧存档加载零异常、`activityLog` 补成数组、合并路径 `saveData` 0 调用）43/43 全绿。

* **全新存档空态冒烟**：空数据下 `computeModuleProgress` 五模块全 0 无 NaN、`weakPoints`/`recentLearning` 返回 `[]`、showToast/navigateTo 正常、零未捕获异常。

* **APK 离线资源说明**：三份打包脚本均**递归复制&#x20;**`assets/` → `assets/data/`（题库分片 / 词库 / 听力）自动进包 ✅；但 **根目录&#x20;**`data/mock-papers.js`**&#x20;不在三份脚本复制范围** ⚠️（本副本自 `a53d218` 检出，未带主项目 2026-09-12 22:55 的 data/ 管线修复）。下批打包前必须给脚本补 `data/` 复制 + `REQUIRED_DATA=mock-papers.js` 白名单，否则 **APK 内模考三页静默 404**。

1. **双源重叠未消除**（沿用既有设计：同 id 以 JSON 覆盖内置，内置仅离线兜底，不阻塞）。



***

## 八、批次四记录（2026-09-13 03:30–04:30）

> 记录人：寇豆码（工程师）・配合：词库线 / 听力线 / 归置线 / QA 线
> 本章为
>
> **批次四续作**
>
> 记录：§ 一～§ 七 原文保持不动；被本轮推翻 / 更新的旧结论，在
>
> **本章内**
>
> 用「→ 已更新见 § 八 …」标注指向（不改旧章节正文）。
> 当前线上版本：
>
> `20260913c`
>
> ・题库总量：
>
> **300 题**
>
> ・词库合并后总量：
>
> **2702 词**
>
> （内置 466 + 增量 2236）。

### 8.1 批次四三项代码改动（已 QA 独立证伪通过）



1. **消除题库双源** → 已更新见 § 八，推翻 §7.4 遗留 1 / §7.7⑤ 第 3 条「双源重叠未消除」。

* 删除 `assets/app.js` 原 **380–394 行**那段独立的 `exam-bank.json` fetch 覆盖块；改为**统一入口&#x20;**`loadExamBankExt()` 两级合并：


  * ① 覆盖层 `allowOverride=true`（同 id 覆盖内置、新 id 追加，**id<101 覆盖能力保留**）；

  * ② 增量分片（同 id 跳过、新 id 追加）。

* 两级共用 `mergeExamBankQuestions()` / `loadExamBankShard(url, allowOverride)`（合并全程不调 `saveData`）。

* 收编方式：`assets/data/exam-bank-ext-index.json` 新增 `legacy`**&#x20;字段**指向 `exam-bank.json`（覆盖层分片）。

* **重要经验（没有改名）**：曾考虑把 `exam-bank.json` 改名为 `exam-bank-ext-0-legacy.json`，但离线桩 `verify_batch3_0913b.js` 只屏蔽 `/exam-bank-ext/` 前缀 ⇒ 改名后覆盖层会被**一起屏蔽**、离线基线丢失覆盖、**B5 断言失败**。故改用「index 声明式收编」，文件名保持不变。

1. **清理 4 个废弃存档字段** → 已更新见 § 八，取代 §7.4 遗留 2。

* 从默认 `appData` 移除 `interviewDone` / `moduleProgress` / `weakPoints` / `recentLearning`（含写死的 **45/30/20/15/10%** 假数据、三条假薄弱点、三条假最近学习）。

* `loadData()` 对老存档残留键**静默&#x20;**`delete` → `saveData()` 不再回写；读到不报错、不渲染；进度四字段（`vocabLearned`/`wrongQuestions`/`favoriteQuestions`/`examTypeProgress`）**零丢失**。

1. **bump 脚本改自动发现** → 已更新见 § 八，解决 §7.7③ 遗留「`PAGES` 仍是硬编码 27 页」。

* 新建 `tools/bump_versions_0913c.py`，`PAGES` **不再硬编码**，改为扫全站「凡真实资源行引用了本批改动资源即入选」。

* **新增硬门禁：**`--check`**&#x20;有任何改动即&#x20;**`exit 3`（落地 §7.7③ 提出的「下批铁律」）。

* 验证：自动发现 **27 页**（与既有 27 页一致）、`--check` **0 改动、exit 0**；注释行 / 行内注释 / 跨行注释块里的引用均**不误判**。

### 8.2 数据产出



* **词库**（`assets/data/vocab-cet4-ext.json`）：1573 → **2236 词**（本批 +663），合并内置 466 后**总量 2702**；`example` 与 `example2` 均 **2236/2236（100%）**；与内置冲突 **0**、内部重复 **0**、字段严格 9 个。

* **听力**（`assets/data/listening-ext.json`）：新增 9 篇，`news`/`longconv`/`passage` **各 4 篇**（每篇 6–8 句，en+zh）。


  * **重要发现（批次三遗留的隐性失效点）**：`voiceplayer.js` 的 `SCENES` 内置**已自带** `news`/`longconv`/`passage` 三个 key，而 `loadListeningExt()` 合并规则是「已存在 key 不覆盖」⇒ JSON 里同名的 3 篇**运行时不会生效**。本批新增一律用 `_N` 后缀（`news_1` 等）绕开；原三篇保留未动（无功能收益，且受「不删原功能」约束）。

* **版本**：bump 到 `20260913c` → 已更新见 § 八，取代 §7.7 定稿版本 `20260913b`（批次四改了 app.js 与数据，必须 bump 才能击穿缓存）；5 个不加载 app.js 的页面保持 `20260913a`。

### 8.3 QA 独立验证（新眼睛证伪，非复跑）



* **代码线**：22/22 + 10/10 + 16/16。关键证伪点：


  * 构造**假覆盖层 id=5** 验证 `<101` 覆盖能力；

  * 构造 \*\*「注释里引用 app.js」的页面**验证自动发现**不误判 \*\*；

  * 用**临时副本**验证 `exit 3` 硬门禁；

  * 验证删旧 fetch 块后 `exam-bank.json` **仍被请求**。

* **词库线**：静态 21 项 + 运行时 10 项；**QA 直接修掉 19 条&#x20;**`example2`**&#x20;与&#x20;**`example`**&#x20;完全相同的硬缺陷**（词条：identify /influence/injure /invent/manner /method/mirror /native/negotiate /obey/obvious /unable/universal /vary/violence /quartz/resemble /transit/parent）。

* **回归（全绿）**：`verify_batch3_0913b` **35/35**、`_p0p1` **43/43**、`verify_vocab_0913b` **17/17**、`verify_versions_0913b` **11/11**、`verify_release_precheck_0913b` **23/23**、泄题扫描 **0 命中**。

* **新增验证脚本**：`verify_batch4_qa_a_exam.js`、`verify_batch4_qa_b_fields.js`、`verify_batch4_qa_c_bump.py`、`verify_vocab_batch4_static.js`、`verify_vocab_batch4_runtime.js`。

### 8.4 遗留（下批处理）



* **词库分片阈值已触发**：2702 词 > 2500 阈值（体积 550KB < 600KB）。本批**未做分片**（需改 `loadVocabExt()` 加载逻辑，属架构改动，避免临近上线引入风险）；建议批次五先做「按首字母切片 + `loadVocabExt()` 支持 index 多文件」，或先开服务端 gzip。

* `root` 仍全空、`collocation` 仅 134 条非空（字段补产成本最高，独立排期）。

* 词库本批 **+663 词**超出计划的 460–500 区间（多 163）；QA 抽查最生僻 10 词均属四级常用，判定为「正常多补覆盖」非注水。另发现 19 个 len≤3 基础词（bee /toy/toe /dry/dot /tip/tax /ton/bay /era/fig /foe/owl /bug/dim /dye/fan /tow/tug），判定**保留**（属合法四级词，对基础薄弱用户有价值）。

* **APK 未重新打包**（用户明确决定跳过），安卓离线端仍回退内置 60 题 / 466 词 / 6 情景。

* 部署仍未执行（等用户决定）：git 有 **35+ 已改文件未提交**。

### 8.5 环境坑（本批新增，务必写）



* **Git Bash 缺 coreutils，连管道&#x20;**`|`**&#x20;都会失败**（如 `tail: command not found`）；且**捕获 node stdout 有时失效（连&#x20;**`echo`**&#x20;都无回显，仅 exit code 正常）** ⇒ 一律让脚本**写文件再用 Read 工具读**，不要依赖 stdout。

* 子代理遇 **429 频率限制**时，主代理直接接手做，不要重复派子代理。



***

## 九、批次四上线与运维记录（2026-09-13）

> 记录人：寇豆码（工程师）・本章只记 
>
> **部署上线 /gzip/ 探针 /git/ 风险**
>
> ；开发与 QA 见 § 八。
> 部署源 = 本项目根・线上版本：
>
> `20260913c`
>
> ・部署脚本：
>
> `tools/deploy_update_20260913c.py`
> 硬约束：§ 一～§ 八 原文
>
> **零改动**
>
> ；本批 
>
> `server/`
>
> **&#x20;零改动**
>
> （未部署任何 server 文件、未重启后端）。

### 9.1 部署内容与结果



* 新建部署脚本 `tools/deploy_update_20260913c.py`（照 `tools/deploy_update_20260911i.py` 模板）。

* 打包 **82 个文件 / 734036 bytes**：



| 类别                         | 数量     | 说明                                                |
| -------------------------- | ------ | ------------------------------------------------- |
| HTML                       | 32     | 27 页版本 `20260913c` + 5 页不加载 app.js 保持 `20260913a` |
| assets（JS+CSS）             | 30     | 26 JS + 4 CSS                                     |
| `assets/data/*.json`       | 18     | 题库分片 index / 词库 / 听力等                             |
| `assets/emoji/manifest.js` | 1      | emoji 清单                                          |
| `data/mock-papers.js`      | 1      | mock\_exam 三页引用                                   |
| **合计**                     | **82** | **734036 bytes**                                  |



* **逐文件 MD5 全量比对 82/82 一致**（非抽查）。

* 排除项 `*.bak-*`、`mock-exam.js.removed`、`*.tar.gz`（及 `tools/_artifacts/`）均**未上传**。

* 关键文件**本地 = 远端**：`app.js` `bde28af1…`、`vocab-cet4-ext.json` `f569d1cf…`、`listening-ext.json` `41da8963…`。

* `server/` **零改动** ⇒ 未重启后端；依据：server 无改动 + `systemctl is-active study-workbench` = **active**（`NRestarts` = 0）+ `/api/health` = **200**。Nginx 直读静态目录，**tar 解压即生效**。

### 9.2 校验四件套（全绿）



| 校验项  | 结果                                                 |
| ---- | -------------------------------------------------- |
| 服务状态 | `systemctl is-active study-workbench` = **active** |
| 站点首页 | `http://127.0.0.1/` = **200**                      |
| 后端健康 | `http://127.0.0.1:8000/api/health` = **200**       |
| 数据库  | **22 张表完好**                                        |



* **内容在线核对**（非仅状态码）：


  * 首页 URL 编码后取到，且含版本号 `20260913c`；

  * `vocab-cet4-ext.json` 远端 **MD5 与本地一致**；

  * `exam-bank-ext-index.json` 的 `shards` = **14**，且 `legacy = { file: assets/data/exam-bank.json, mode: override }`；

  * `app.js` 远端 **MD5 与本地一致**。

### 9.3 真实探针与清场



* 真实链路探针：`register` **200** → `login` **200** → `/api/auth/me` **200**。

* `GET /api/users/{id}` = **200**，返回键**不含** `phone` / `gender` / `birthday`（隐私边界守住，**LEAK = \[]**）。

* **清场**：删临时探针账号 `id=10`，用户数 **8 → 8** 复原。

### 9.4 gzip（本轮第二件事：开启 JSON 压缩）



* **现状**：宿主 `nginx/1.28.3`，`http` 段 `gzip on;` 已开，但 `gzip_types` **被注释** ⇒ 默认只压 `text/html`，**JSON 不压缩**（实测 RAW 564318 = GZIP 564318，未压缩）。

* **改动**：站点配置 `server{}` 段（`server_name` 后）**最小追加 3 行**：


  * `gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;`

  * `gzip_comp_level 6;`

  * `gzip_vary on;`

* **备份**：改前 `cp -a` → `/etc/nginx/sites-available/study-workbench.bak-20260913c`。

* **校验**：`nginx -t` = **syntax ok**；`reload` **exit 0**。

* **压缩实测**：



| 资源                         | 原始 bytes | gzip 后 bytes | 压缩比                |
| -------------------------- | -------- | ------------ | ------------------ |
| `vocab-cet4-ext.json`      | 564318   | **150276**   | **3.76×（省 73.4%）** |
| `app.js`                   | 475493   | 145623       | ≈ 3.27×            |
| `exam-bank-ext-index.json` | 2232     | 630          | ≈ 3.54×            |



* 首页响应头出现 `Content-Encoding: gzip` + `Vary: Accept-Encoding`。

* **未改动 nginx 其他任何指令**。

### 9.5 日志与提交



* `journalctl -u study-workbench --since -10min` 匹配 `traceback|exception|500 internal` 计数 = **0**。

* 本地 git commit `77eabb27d07b904be5595808cb3932fe9927fdeb`（父 `5c5b2da`），新增 `tools/deploy_update_20260913c.py` + `tools/run_remote_0913c.py`（**2 files, +230**）。

* **未推 GitHub**；凭据自查 `git ls-files` **363 条**，`data.db` / `.env` / `node_modules` / `*.tar.gz` 命中 **0**。

### 9.6 遗留与风险



* **用户数基线不符**：SOP 记为 **6**，实测 **8**。本批未碰 `server/` 与 DB，**非本次引入**；疑为 2 个历史测试 / 探针账号残留，**未执行任何删除**（删生产用户数据需用户拍板）。

* `blog_wechat.html` 已随 32 页口径部署（SOP 建议将该废弃草稿页列入**排除清单**；该页无引用、无功能影响）。

* 词库仍为**单文件 564KB**（gzip 后 150KB），**分片列入批次五**。

* **已知操作约束**：部署期间**必须冻结所有文件写入**（并行改文件会打包到中间态）。

### 9.7 环境坑（本批新增，务必记下）



* **本机 git 的 ref 写入「假成功」**：`git commit` 报成功且对象已落盘，但分支 ref **未写盘**，导致 worktree 一度显示 "No commits yet" + 全部文件暂存。修复方式 = 直接写 `refs/heads/workbuddy/main-8d0a1649` 指向该 commit。**下批提交需留意同一问题**。

* 本机 Git Bash 缺 coreutils（`grep` / `head` / `tail` 全 `command not found`，连管道 `|` 都失败）；**捕获 node stdout 有时整体失效** ⇒ 一律「**脚本写文件 + Read 读取**」。



***

## 十、批次五交接快照（2026-09-13 04:45・主代理写・**交接给下一个 AI 继续工作**）

> 本节是
>
> **进行中批次的状态快照 + 接手任务书**
>
> 。所有结论均为主代理亲自读盘 / 跑命令核实，不是转述子代理汇报。
> **需求维度**
>
> 的「做了什么 / 没做什么 / 难易评估」见 
>
> `deliverables/software-company/batch3-incremental-PRD.md`
>
>  
>
> **§9 需求进度总览**
>
> （逐条 R1–R7 + 批次五新增项，含难易与剩余工作量）。
> **用户的全部需求、偏好与拍板记录**
>
> 见项目根 
>
> `用户需求与决策总账.md`
>
> （含「已拍板照办 / 待用户拍板 / 工作方式偏好 / 数据质量红线」—— 接手者不需要重新向用户询问任何需求）。

### 10.1 批次五目标与完成度



| 任务                    | 完成度                            | 说明                                        |
| --------------------- | ------------------------------ | ----------------------------------------- |
| 词库分片改造                | ✅ **100%**（已验证）                | 8 片 + 索引 + `loadVocabExt()` 并发改造，等价性 0 差异 |
| 缓存版本体系（4 步流程）         | ✅ **①②③④ 全部完成**，**差最后一步 bump** | 只扫不改→保留名单→注入→回归，全绿                        |
| root/collocation 补产   | 🔴 **41.6%**（930/2236，覆盖 a–i）  | 剩 j–z **1306 条**                          |
| 0913d 部署脚本            | ✅ 已备好 + dry-run                | **未部署**（等 bump 与用户拍板）                     |
| 全局 bump 到 `20260913d` | ❌ **未做**（下一步第 1 项）             | 预演已通过                                     |

### 10.2 已落盘且验证过的事实（接手即可信，不必重验）



1. **词库分片**：`assets/data/vocab-cet4-ext-{a-c,d-f,g-i,j-l,m-o,p-r,s-u,v-z}.json`（365/281/284/110/306/326/418/146 = **2236**）+ `vocab-cet4-ext-index.json`（`shards[].file` 带 `?v=20260913c`）。**旧单文件&#x20;**`vocab-cet4-ext.json`**&#x20;已删除**，备份在 `assets/data/vocab-cet4-ext.json.bak-shard`。主代理已做**集合级等价复核：0 差异**（无丢失 / 无多出 / 无改动 / 无重复；注意分片是按字母重排的，与备份的**序列**不同属预期）。

2. `app.js`：`loadVocabExt()` 已重写（读索引 → `Promise.all` 并发拉 8 片 → 同一 `mergeVocabWords()`；单片失败跳过 + `console.warn`；索引失败静默回退内置 466 词；**不调 saveData ()**；幂等）。`:444` `const VOCAB_EXT_INDEX = 'assets/data/vocab-cet4-ext-index.json?v=20260913c'`，启动点 `:505`。`node --check` 通过。

3. **缓存版本注入（全部&#x20;**`?v=20260913c`**，统一待 bump）**，共三类注入点：

* HTML 32 页的 `<script src>` / `<link href>`（302 处 `c` + 12 处 `a`）

* JS fetch 字面量 4 处：`app.js:377` `EXAM_BANK_LEGACY`、`app.js:378` `EXAM_BANK_EXT_INDEX`、`app.js:444` `VOCAB_EXT_INDEX`、`voiceplayer.js:114`（listening）

* 两个索引 JSON 的 `file` 字段：`exam-bank-ext-index.json`（1 legacy + 14 分片）、`vocab-cet4-ext-index.json`（8 分片）

1. `:431`**&#x20;去重比较已修**：`var legacyPath = String(EXAM_BANK_LEGACY).split('?')[0];` —— 不修它 legacy 会被加载两遍（这是版本线发现的真坑）。

2. **bump 脚本&#x20;**`tools/bump_versions_0913d.py`（新建，未改 0913c 版）：统一管理上述三类注入点；`--check`（目标 d）= **36 文件 / 341 点，问题 0**，报告在 `tools/qa/_bump_d_pre.txt`；**尚未&#x20;**`--write`。

3. **部署脚本&#x20;**`tools/deploy_update_20260913d.py`（新建，未改 0913c 版）：`--dry-run` = **91 文件**（3.31MB → gz 886KB）；**显式排除** `vocab-ext-fields-patch.json`；**部署守卫**：旧单文件若存在直接 SystemExit；`--dry-run` 永不取凭据。报告 `tools/qa/_deploy_0913d_manifest.txt`。**未 SSH、未部署**。

4. **验证体系**：新 `tools/verifier/verify_version_system_0913d.js` **9/9 PASS**（jsdom 真跑 fetch 录制 URL）；5 套既有回归全绿：`verify_batch3_0913b` 35/35、`p0p1` 43/43、`verify_vocab_0913b` 18/18、`verify_versions_0913b` 11/11、`release_precheck` 23/23；泄题扫描 0 命中。`verify_batch3_0913b.js` 已修过一处（索引 `file` 字段带 `?v=` 后 fs 路径读不到 → `split('?')[0]`）。

5. **字段补产**：`assets/data/vocab-ext-fields-patch.json` **930 条**（a–i 段；`collocation` 与 `root` 双 100% 非空；校验过：无重复词、无词库外词、无空搭配数组）。**剩 j–z 1306 条**。该文件是中间产物，**已被部署脚本排除**，最终由任务 4 合并进分片。

6. **只读核实结论**（供决策）：生产用户 **8 = 6 真实 + 2 测试号**（id=8 `testuser01`/ 测试号甲、id=9 `testuser02`/ 测试号乙，**未删，等用户拍板**）；git 主仓库 = `D:\下载的文件\学习工作台`（`main-8d0a1649` 是其 linked worktree，写 ref 到主仓库 `.git/refs/heads/workbuddy/` 属正常路径）；**nginx 对全部静态资源无&#x20;**`Cache-Control`**/**`Expires` → 浏览器启发式缓存（这就是做版本体系的根因）。

### 10.3 接手任务清单（按序执行）



1. **全局 bump**：`python tools/bump_versions_0913d.py 20260913d --write`（预演已过，341 点：HTML 314 / JS 4 / JSON 23）。落盘后应无 `20260913c` 残留于 HTML。

2. **同步两个 verifier 的版本常量**（bump 后必须做，否则回归假红）：

* `tools/verifier/verify_versions_0913b.js`：`CUR` `'20260913c'` → `'20260913d'`

* `tools/verifier/verify_version_system_0913d.js`：`L24 const VER = '20260913c'` → `'20260913d'`（B1 注释一并改）

1. **总回归**（各跑一次）：35 / 43 / 18 / 11 / 23 / 9 六套 + `scan_question_leak.py` 0 命中 + `node --check app.js/voiceplayer.js`。

2. **字段线续作**：`vocab-ext-fields-patch.json` 续 j–z **1306 条**（先 collocation 后 root；每～200 条落盘；**严禁臆造搭配**；同词不重复；只写该 patch 文件）。做完后**合并进 8 分片**（按 word 匹配补字段，词数必须仍 2236，合并后逐条 JSON 比对分片备份）。

3. **⚠️ 合并后必须再 bump 一次（如&#x20;**`20260913e`**）+ 重新部署** —— 分片内容变了而 URL 没变的话，用户端启发式缓存里的旧分片不会更新。**这一步不能省。**

4. **部署 0913d**：照 § 九 流程（凭据从 `D:\下载的文件\学习工作台\upload_v23.ps1` 正则提取，**绝不打印明文**；先 `--dry-run`；部署期间冻结所有文件写入；四件套 + 探针 + 清场 + journalctl + **本地 commit 指定路径、绝不 push**）。

5. **两个待用户拍板**（不要自作主张）：① 删生产用户 id=8/9 两个测试号（删后用户数回基线 6）；② C 方案 —— nginx 对 `/assets/data/` 加 `Cache-Control: no-cache`（需单独授权，超出 gzip 授权范围）。

6. **收尾**：交接文档补 § 十一（部署记录）+ 打 `.bak-b5end` 快照（8 分片 + 2 索引 + patch + app.js + voiceplayer.js）。

### 10.4 接手必读：红线与环境坑



* **429 大限**：2026-09-14 01:32（UTC+8）前**所有子代理不可用**（本批 3 条线全部死于 429）→ 要么主代理自己做，要么等重置。团队 `software-b5` 成员已全部停止，接手者按需重新拉线。

* **git**：本 worktree 的分支 ref 物理存放在主仓库 `D:\下载的文件\学习工作台\.git\refs\heads\workbuddy\`；**多个 worktree 并发 git 操作会触发 ref「假成功」** → 每次 commit 后必须 `git log --oneline -3` 确认落盘。

* **改&#x20;**`app.js`**&#x20;前必须备份** + 改完 `node --check`；**合并数据不得调&#x20;**`saveData()`；**读索引&#x20;**`file`**&#x20;字段做磁盘路径必须&#x20;**`split('?')[0]`。

* 部署四件套与用户数基线见 § 九；删生产用户数据必须用户点头。

* 本机 Git Bash 缺 coreutils、管道会失败、node stdout 捕获有时失效 → 一律「脚本写文件 + Read 读取」。

* Node：`C:\Users\ATM\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`；Python：`C:\Users\ATM\.workbuddy\binaries\python\versions\3.13.12\python.exe`；jsdom 需 `NODE_PATH=C:\Users\ATM\node_modules`。

### 10.5 本批新增产物清单



* 数据：`assets/data/vocab-cet4-ext-{a-c…v-z}.json`（8）、`vocab-cet4-ext-index.json`、`vocab-ext-fields-patch.json`（中间产物）

* 代码：`assets/app.js`（loadVocabExt 重写 + 3 常量注入 + :431 修）、`assets/voiceplayer.js`（:114 注入）

* 工具：`tools/bump_versions_0913d.py`、`tools/deploy_update_20260913d.py`、`tools/verifier/verify_version_system_0913d.js`、`tools/qa/scan_version_candidates_0913d.js`

* 报告：`tools/qa/_version_candidates_0913d{,.summary}.txt`、`_version_keep_list_0913d.md`、`_bump_d_pre.txt`、`_deploy_0913d_manifest.txt`、`_verify_version_system_0913d.txt`

* 备份：`assets/app.js.bak-shard`、`assets/voiceplayer.js.bak-v3`、`assets/data/vocab-cet4-ext.json.bak-shard`

***

## 十一、批次五接手上线与运维记录（2026-09-13 05:50・主代理接手后续作）

> 承接 §十（批次五快照 + 8 步接手任务书）。本章记录 **接手后执行的任务书第 1–8 步**（除第 8 步的文档即为本章本身）。
> 线上版本：`20260913c` → **`20260913e`**；题库 **300 题**；词库 **2236 词（8 分片）**、**root/collocation 100% 覆盖**。

### 11.1 第 1–3 步（bump d + 常量同步 + 总回归）

* `python tools/bump_versions_0913d.py 20260913d --write` → **36 文件 / 341 点**（html 314 / js 4 / json 23），问题 0。
* 同步 verifier 常量：`verify_versions_0913b.js`(CUR)、`verify_version_system_0913d.js`(VER)、`verify_vocab_batch4_runtime.js` 由 c→d。
  索引 JSON 顶层 `version` 字段**不在** bump 范围（bump 只改 `file` 字段），故 `verify_vocab_0913b.js` / `verify_vocab_batch4_static.js` 的该断言保持 c、不动。
* **回归全绿**：35 / 43 / 18 / 11 / 23 / 9 六套 + 泄题 0 命中 + `node --check` 通过。

### 11.2 第 4 步（词库 root/collocation 全量补产）

* patch 续补 **j–z 729 条** → `vocab-ext-fields-patch.json` 达 **2236/2236 全覆盖**（a–i 930 + j–z 1306）。
* 分批产物 `tools/qa/_pb1..17.json`；合并脚本 `tools/qa/_merge_patch.py`；分片应用脚本 `tools/qa/_apply_patch_to_shards.py`。
* 合并进 8 分片后**逐条比对**：word 数仍 **2236**、无重复、`root`/`collocation` 零空缺、**除这两个字段外零改动**（备份 `assets/data/_bak_shard_0913e/`）。

### 11.3 第 5 步（合并后再 bump → `20260913e`）

* **修 0913d 脚本缺陷**：0913d 相比 0913c 丢了「不加载 app.js 的 5 页保持 `20260913a`」豁免，会无差别全 bump。
  已在 `tools/bump_versions_0913d.py` 补回 `KEEP_OLD`（AI模拟面试 / PPT素材库 / 四级经验分享 / 好友申请 / 登录）。
* `python tools/bump_versions_0913d.py 20260913e --write` → **31 文件 / 329 点**（html 302 / js 4 / json 23），5 页正确跳过，问题 0。
* 常量再同步到 e；**回归再次全绿** 35/43/18/11/23/9 + 泄题 0 命中。
* 本地 commit **`edc0f8a`**（父 `77eabb2`，54 files changed）；**未 push**。

### 11.4 第 6 步（部署 0913e）

* 部署脚本 `tools/deploy_update_20260913d.py`（STAMP 改 `20260913e`）。`--dry-run` = **90 文件**，源 2969547 bytes → gz 825284 bytes；显式排除 `vocab-ext-fields-patch.json`；旧单文件守卫通过。
* 上传 1 次成功；远端解压 + **全量 MD5 90/90 一致**；DB 已备份 `backups/data.db.before-20260913e`。
* **校验四件套**：`systemctl is-active study-workbench` = active、`SITE` = 200、`HEALTH` = 200、`NRestarts` = 0、DB **22 张表**。
* **内容在线核对**：首页 12 处 `?v=20260913e`；8 个词库分片全 200 且字节与本地一致（129126 / 96783 / 101795 / 39427 / 108714 / 119086 / 148441 / 51080）；`app.js` 远端 MD5 `97d52a40683e53f5d688fbffdd37e98a`。
* **真实链路探针**：`register` 200 → `login` 200 → `/api/auth/me` 200（昵称回显正确）；隐私边界 `GET /api/users/1` **LEAK = []**。
* `journalctl -u study-workbench --since -10min` 异常计数 = **0**。

### 11.5 第 7 步（用户拍板后执行）

1. **删生产测试号**：`id=8`(testuser01/测试号甲)、`id=9`(testuser02/测试号乙) + 清场 `id=10`(b5probe01 探针)。
   按 26 组关联列全表清理，**合计删除 36 行**（study_logs 25 / friend_requests 4 / messages 2 / friends 1 / notes 1 / users 3）。
   **用户数 8 → 6（回 SOP 基线）**；删除前已备份 `backups/data.db.before-purge-20260913e`。
2. **C 方案**：nginx 站点配置新增 `location /assets/data/ { add_header Cache-Control "no-cache"; try_files $uri =404; }`
   （备份 `study-workbench.bak-20260913e`，`nginx -t` syntax ok，reload OK）。
   **实测生效**：`/assets/data/*.json` 响应头含 `Cache-Control: no-cache`；带 `Accept-Encoding: gzip` 时 `Content-Encoding: gzip`。
3. **APK 永久跳过**（用户拍板）——安卓离线端仍回退内置 60 题 / 466 词 / 6 情景，**不再重打包**。

### 11.6 第 8 步（收尾）

* 交接文档补本章；`.bak-b5end` 快照 **13 个**（app.js / voiceplayer.js / 2 索引 / patch / 8 分片）。
* 未 push GitHub。

### 11.7 本批两个真坑（务必记住）

1. **bump 脚本丢豁免**：新建 bump 脚本时除「正则放宽 / 自动发现」外，**必须显式保留 5 页豁免**，否则 5 页被误 bump、`verify_versions_0913b` 的 V2-b/V2-c 直接红。
2. **git ref「假成功」复发**（§9.7 同源）：`.git/refs/heads/workbuddy/` 目录不存在时 `git update-ref` 会**静默失败**（返回 0 但不落盘）。
   修复 = 用脚本 `os.makedirs` 建目录后直接写 ref 文件（内容为新 commit 完整 SHA）；**每次 commit 后必须 `git log --oneline -3` 复核**。

### 11.8 环境坑（本批补充）

* Git Bash 缺 coreutils 更严重：`cat` / `head` / `tail` / `tee` / `ls` 全无；**`curl -sI`(HEAD) 取不到 nginx 新增头**，须改用 `curl -s -D - -o /dev/null` 验证。
* 远程操作统一走 `tools/plink.exe` + `tools/qa/_b5_*.py`（凭据惰性读取，绝不打印明文）。

***

## 十二、批次 20260913h 交接记录（2026-09-13 13:40・主代理写・**交接给下一个 AI**）

> 本章承接 §十一。记录的是一次**完整批次**：线上 bug 修复 → 两副本合并 → 数据隔离修复 → 功能删减 → 页面重构 → 图标全站升级 → 统一上线。
> 所有结论均为主代理亲自读盘/跑命令/远端探针核实。

### 12.1 一句话现状 + 最重要的事实变更

**批次 20260913h 全部完成并已上线（线上版本 `20260913h`，本地 commit `c1bb3f7` + `2564559`）。**

⚠️ **最重要的变更：两份工作副本已合并，worktree 不再是独立交付线。**
- 来源：worktree `C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-8d0a1649`（分支 workbuddy/main-8d0a1649，含批次三四五成果 153 文件）已 `git merge` 进主仓库 `D:\下载的文件\学习工作台`（main）。
- 合并点 HEAD `8ef7f49`；合并后本批改动 commit `c1bb3f7`（43 files, +3170/-745）+ 文档 commit `2564559`。
- **§十 / §11 中所有「worktree 是工作目录 / ref 写主仓库」相关的操作约定均已失效**——今后一律在主项目 `D:\下载的文件\学习工作台` 工作。
- 备份：`D:\git-backup-20260913-premerge`（合并前）、`D:\git-backup-20260913-postbatch`（本批提交后）。

### 12.2 本批完成清单（全部已上线并探针验证）

| 编号 | 内容 | 涉及文件 |
|---|---|---|
| 线上 bug | 真题模考三页返回按钮 `history.back()` 乒乓 → `goBack()` 按 cat 显式跳转 | mock_exam{,_run,_result}.html |
| A1 | 两副本合并（含需求24 演示模式移除：chat-local.js） | 153 files |
| A2 | **数据隔离黑洞修复**：api.js `syncAiFromServer` 写 `ai_chat` 裸键、study-stats.js `STORE_KEY`/`QUEUE_KEY`/`tool_*` 共 5 处改走 `lsKey()` 账号前缀键 | assets/api.js:1060、assets/study-stats.js |
| A3/A4 | 需求18 计数器验收（答对即时+1/正确率实时/打卡同源）；需求09 个人中心路由验收 + **穿越英语死链修复**（个人中心.html:408、blog_wechat.html:344 守卫兜底） | 个人中心.html、blog_wechat.html |
| B1-B4 | 四级备考删「写作提升」；央国企删「数量关系/判断推理/资料分析」+「题型正确率」；高情商删「情景选择实战」；工具页删「万能金句/场景话术」重复入口 | 四级备考/央国企笔试/高情商表达/工具.html |
| C1-C3 | 好友页（私聊.html）菜单重排 `会话→好友→群聊→申请→加好友` + 令牌化；广场页删顶端说明卡+主导航提级；设置页三菜单（阅读与界面/语音与朗读/帮助与反馈）提级一级菜单 | 私聊/学习博客/设置.html + subpage-router.js |
| E1 | 图标全站升级 **15 页 149 处** emoji→`data-icon`；23 处无注册图标保留 emoji | 15 个 HTML |
| 版本 | 全站 **27 页统一 `20260913h`**；5 个仅引 config.js 的页面保持 `20260913a`（AI模拟面试/PPT素材库/四级经验分享/好友申请/登录） | 27 HTML |
| 文档 | 需求清单状态盘点更新（30 条：20 ✅ / 3 🟡 / 6 ❌） | 星途平台改进-需求清单-状态盘点-2026-09-13.md |

**A2 的 bug 链（重要）**：app.js 启动时 `migrateLegacyKeys()` 会把裸键复制到账号前缀键后**删除裸键**；而 api.js/study-stats.js 运行时仍往裸键写 → 数据进黑洞 + 跨账号串数据。修复即让这两个文件的键全部走 `lsKey()`。

### 12.3 线上状态（探针实测）

- 上传 1 次成功；**全量 MD5 89/89 一致**；DB 备份 `backups/data.db.before-20260913h`；**未重启服务**（server 零改动）。
- 删减探针 6 项全 0 命中；模考三页 `goBack=1`/`history.back 可执行=0`；四级备考 `20260913h`×14；首页 `20260913h`×12、`data-icon`×29；`USERS=6`（基线）；`TRACEBACK=0`。
- 线上遗留死文件 `/assets/data/vocab-cet4-ext.json` **已删除**（404 验证通过），现役分片 index 仍 200。
- ⚠️ 未做（用户未要求）：APK 重打包。前端大改后 `bash android/build-apk.sh` 才会让安卓端吃到新前端（§11.5 记载用户曾拍板「APK 永久跳过」，接手者先向用户确认）。

### 12.4 本批新增工具与验证基建（接手直接用）

- **部署脚本 `tools/deploy_update_20260913h.py`**：
  - 🔴 **ROOT 铁律**：ROOT 必须是主项目 `D:\下载的文件\学习工作台`。历史事故：`deploy_update_20260913d.py` 的 ROOT 曾指向 **worktree**，直接跑会把已删功能复活 + E1 图标全部回退。**新建部署脚本第一件事：校验 ROOT 不是 worktree 路径。**
  - STAMP=20260913h；三条资源硬断言（icon-map.js / subpage-router.js / data/mock-papers.js）；探针先**剥离 HTML 注释**再计数（消除 mock 页 `javascript:history.back()` 文档注释误报）；删减探针断言 **onclick 层调用为 0**（避开注释里保留的功能名）。
  - 用法：`python tools/deploy_update_20260913h.py`（正式）；`SW_DRY_RUN=1 SW_SKIP_UPLOAD=1` 本地自检（不联网不读凭据）；凭据沿用 upload_v23.ps1 正则惰性提取。
- **终验 runner `tools/qa/qa_batch_0913h_final.js`**：`node ...` 跑 6 脚本；`--with-c1` 跑全 7 脚本；含 **8 条硬断言**（3 条防覆盖 + 5 条 C1 菜单）。**以后每批改动后重跑它作回归门。**
- 单页验证器：`qa_a2a3a4_final_0913.js`(59) / `qa_b12_trim_0913h.js` / `qa_b3_b4_smoke.js` / `qa_c23_0913h.js` / `qa_e1_icon_smoke.js`(15页) / `qa_blogwechat_quest_0913.js` / `qa_c1_0913h.js`。
- E1 迁移脚本留档 `tools/e1_icon_upgrade.js`（含 7 文件禁改保护）、运行日志 `tools/e1_run_log.txt`。

### 12.5 被本批推翻/更新的旧结论（读旧章节时注意）

| 旧条目（§十/§十一） | 现状 |
|---|---|
| worktree 为工作目录、分支 ref 写主仓库 | **已合并，worktree 弃用**（见 12.1） |
| 用户数基线 6；曾有 id=8/9 测试号 | 本批实测 **USERS=6**，已回基线 ✔ |
| `vocab-cet4-ext.json` 旧单文件 | 本地已删（分片化）、线上已清理 404 |
| 线上版本 `20260913e` | 现为 **`20260913h`** |
| §10.4「429 大限」、团队 software-b5 | 已过期；本批团队 `xingtu-p0a-batch` 已解散 |
| 「bump 脚本必须保留 5 页豁免」 | 仍然有效；本批版本戳更严：**凡引用本批变更 assets 的页面必须 bump**（首页曾引 `api.js?v=g` 却停 g → 已补 bump，否则老用户命中旧 JS 缓存） |

### 12.6 待办（接手者按需排期，勿擅自开工需用户拍板的项）

**未做需求**（详见 `星途平台改进-需求清单-状态盘点-2026-09-13.md`）：
- 需求07 PPT 训练页重构（新建综合功能模块入口，保留版式库/案例拆解独立）— 中
- 需求13 APP 端好友功能真机验证 — 中（依赖 APK 重打）
- 需求14 私聊/群聊图片+视频消息（语音已有）— 大
- 需求01 管理员超级账号（单向可见，动后端）— 中
- 需求20 同质化选择题库差异化或合并 — 中
- 需求26 商务礼仪 SVG 插画修正、需求28 i人专区/企业库内容补全 — 中
- E1 剩余 23 处图标（需先给 `assets/icon-map.js` 注册新图标，禁 mask/filter/symbol/use）
- `blog_wechat.html` 已是死草稿页但仍在部署包内（可选：列入排除清单）

**需用户拍板**：APK 是否重打包（§11.5 曾拍板跳过，但本批前端大改）。

### 12.7 本批新发现的坑（务必记住）

1. **并行写覆盖**：多个子代理并行时，改共享/大文件前必须重新 Read 盘上最新版，严禁整文件写回（一律精确 Edit）——本批一天内发生 **3 次覆盖事故**（study-stats.js 修复被回滚、blog_wechat.html 图标替换被覆盖）。主理人终验必须加「防覆盖硬断言」（直接断言盘上关键行仍在）。
2. **部署脚本 ROOT 指向旧 worktree** 是静默致命坑（见 12.4）。
3. **版本戳漏 bump 的缓存风险**：Nginx 对 `/assets/*.js|css` 无 Cache-Control（仅 `/assets/data/` 有 no-cache），缓存击穿完全依赖 `?v=`。判定规则：**引用「本批变更 assets」的页面必须 bump**。
4. **PowerShell 在本机中文路径下不可靠**：stdout 常被吞；`Test-Path` / `Set-Content` 曾假失败（返回不存在/未写入）。**删文件用 managed python 的 `os.remove`；需回读的输出用 bash `>` 重定向到项目内 ASCII 名文件再 Read。** bash 仍缺 coreutils（连 `rm`/`tail` 都无）。
5. **git add 中文路径**：命令行传中文路径会静默 no-op → 用 `git add -A`（先清临时文件）或 `git add -u`；commit message 走 `-F <UTF8文件>`（文件放仓库外 ASCII 路径最稳）。
6. **SIGTERM 损坏 .git**：长前台 git 命令（merge 等）被中断会造成 `.git/refs`、`.git/worktrees`、objects pack 丢失 → **长 git 操作必须后台跑，且合并前先备份 .git 到仓库外**。

### 12.8 接手三步

1. 读本章（§十二）+ `星途平台改进-需求清单-状态盘点-2026-09-13.md`（需求真相）+ `用户需求与决策总账.md`（用户全部拍板记录，**接手者不需要重新向用户询问需求**）。
2. 跑一次回归门确认基线：`node tools/qa/qa_batch_0913h_final.js --with-c1`（应全 PASS / exit 0）。
3. 按 §12.6 待办向用户确认下一步；任何改动走「改 → 单页验证器 → 全量 runner → 部署脚本 dry-run → 正式部署 → 远端探针」流程。
