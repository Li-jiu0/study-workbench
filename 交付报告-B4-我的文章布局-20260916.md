# 交付报告 · B4「我的文章」布局（N9-15）

- 任务编号：**B4**
- 页面：`blog_wechat.html` · 视图：`#blogViewMine`（子导航 tab「我的文章」）
- 日期：2026-09-16
- 状态：**DONE**
- 工作树：`D:\下载的文件\学习工作台`（未触碰任何 `C:\Users\ATM\WorkBuddy\Worktrees\...` 副本）

---

## 一、F3 生效副本判定（关键前置）

### 结论

| 函数 | 运行时生效副本 |
|---|---|
| `renderBlogMine` | **`assets/api.js`**（第 498 行） |
| `renderBlogStats` | **`assets/api.js`**（第 826 行） |
| `renderBlogMineTabs` | `assets/app.js`（第 7474 行）——**无重名，无覆盖问题** |
| `openBlogStats` | `assets/app.js`（第 8323 行）——**无重名，无覆盖问题** |

即：`app.js` 里的 `renderBlogMine`（L7515）/ `renderBlogStats`（L7932）是**死副本**，
被后加载的 `api.js` 同名函数覆盖，运行时永不执行。

### 证据（可复现）

方法：用 jsdom 加载真实页面，读取运行时 `window.renderBlogMine.toString()`，
按「函数体独有的字符串指纹」判定来源。脚本：`tools/qa/_b4_f3.js`，
输出：`C:\Users\ATM\_f3_out.txt`。指纹选取：

- `app.js` 版 `renderBlogMine` 独有：函数体首行 `renderBlogMineTabs();`
- `api.js` 版 `renderBlogMine` 独有：`scope=mine&page_size=0`
- `app.js` 版 `renderBlogStats` 独有：`const pub = notes.filter(n => n.status ===`
- `api.js` 版 `renderBlogStats` 独有：`CURRENT_USER.stats || {}`

实测输出（原文摘录）：

```
=== F3 生效副本判定 ===
renderBlogMine: 来源=api.js  长度=2554
  head= async function renderBlogMine() { var tabs = document.getElementById('blogMineTabs');
  指纹: appMineTabs=false  apiScopeMine=true
renderBlogMineTabs: 存在=true  长度=2654
renderBlogStats: 来源=api.js  长度=2784
  head= function renderBlogStats() { var box = document.getElementById('blogStatsBox'); if (!bo
  指纹: appStats=false  apiStats=true
```

**判定**：`renderBlogMine` / `renderBlogStats` 的 `.toString()` **同时命中 api.js 指纹、且不命中 app.js 指纹**
→ 生效副本 = **api.js**。加载顺序 `app.js`(L330) → `api.js`(L331) 随后覆盖，与预期一致。

### 对 B4 的影响（重要）

`api.js` 版 `renderBlogMine` 的数据源是**服务端接口**（`/api/notes?scope=mine` 等），
**它完全不读 `blogMineOpen` / `appData`**（实测 `typeof window.appData === 'undefined'`、
`typeof window.blogMineOpen === 'undefined'`——那两个是 app.js 的 `const/let`，不挂 window）。

因此：
- `blog_wechat.html` 打开后点「我的文章」→ 执行 api.js 版 → 渲染出**扁平的分类 chip 行**（非 app.js 版的竖排菜单页）。
- 本次 B4 的布局改进**针对的就是当前真正生效的那一份渲染输出的 DOM**（chip 行 + `.blog-grid` 卡片网格），
  **改到了生效副本对应的 DOM 上，不是死的。**

> 本波**未修改** `app.js` / `api.js`（两者均不在 B4 独占范围，且分属其它任务线独占）。
> 关于两份重名函数本身的一致性隐患，见第六节「待主理人转派」。

---

## 二、现状梳理（改前）

「我的文章」视图 DOM（`blog_wechat.html` 改前 L113-L119）：

```html
<div class="blog-wrap" id="blogViewMine" style="display:none">
  <div class="blog-toolbar">
    <div class="blog-filters" id="blogMineTabs"></div>   <!-- 分类 chip 行，由 renderBlogMine 填充 -->
  </div>
  <div class="blog-grid" id="blogMineGrid"></div>        <!-- 文章卡片网格，由 renderBlogMine 填充 -->
</div>
```

渲染链路：
`showBlogView('mine')`(app.js L7332) → `renderBlogMine()`(**api.js L498**，生效) →
填 `#blogMineTabs`（`<span class="chip">` 若干：全部/已发布/草稿/归档/收藏/回收站）+ `#blogMineGrid`（`noteCardHtml()` 卡片）。

改前依赖的既有全局样式（`common.css`）：

| 行 | 规则 | 影响 |
|---|---|---|
| L1354 | `.blog-wrap { display:flex; flex-direction:column; gap:16px; }` | 容器纵向布局 |
| L1368 | `.blog-toolbar { display:flex; flex-direction:column; gap:12px; }` | 工具条 |
| L1375 | `.blog-filters { display:flex; gap:8px; flex-wrap:wrap; }` | **chip 行会换行、无横向滚动** |
| L1376-1382 | `.blog-filters .chip {...}` | chip 外观 |
| L1385 | `.blog-grid { grid-template-columns:repeat(auto-fill,minmax(300px,1fr)); gap:16px; }` | 卡片网格 |
| L1386-1407 | `.note-card` / `.note-body` / `.nc-*` | 卡片内部 |

**识别到的布局问题**：

1. **P1（窄屏碎裂）**：`.blog-grid` 最小列宽 `300px`，而 375px 机型内容区（375 − 侧边栏 − padding）不足 300px
   → `auto-fill` 退化为单列但列宽被 300px 撑破，卡片与容器边缘错位/横向溢出。
   在**我的文章**里 6 个分类 chip 还带计数，chip 行一换行进一步挤占高度，首屏卡片被推到折叠线以下。
2. **P2（宽屏空洞）**：1280px 下 300px 最小列 → 单卡被拉宽、列数偏少，网格留白大、卡片显"虚"。
3. **P3（卡片高矮不齐 + 按钮不贴底）**：卡片内含「编辑 / 归档 / 删除」按钮行，
   标题/摘要长度不一时卡片高度参差，同一行卡片底部按钮不对齐，视觉锯齿。

---

## 三、实现内容（含行号）

### 3.1 `blog_wechat.html`（21,962 B → 23,734 B）

| 行号 | 改动 |
|---|---|
| **L17-L36**（新增） | 页内作用域 `<style>` 块（4 组规则，全部以 `.blog-mine-scope` 打头）+ 设计取舍注释 |
| **L38** | `<div class="app">` 原样保留（未动） |
| **L137** | `<div class="blog-wrap" id="blogViewMine" style="display:none">` → `<div class="blog-wrap blog-mine-scope" id="blogViewMine" style="display:none">`（仅新增 1 个 scope class） |

新增样式（全文）：

```css
/* B4-1 统计芯片行：独立成行、可横向滚动不换行（超 6 类时不被挤成多行） */
.blog-mine-scope .blog-filters { flex-wrap: nowrap; overflow-x: auto; -webkit-overflow-scrolling: touch; padding-bottom: 2px; }
.blog-mine-scope .blog-filters .chip { flex: 0 0 auto; white-space: nowrap; }
/* B4-2 「我的文章」卡片网格：自适应列宽（窄屏 1 列 / 宽屏多列） */
.blog-mine-scope .blog-grid { grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 14px; }
/* B4-3 卡片：等高拉伸 + 删除/归档按钮行贴底对齐 */
.blog-mine-scope .blog-grid .note-card { height: 100%; }
.blog-mine-scope .note-body { justify-content: space-between; }
/* B4-4 窄屏（≤480px）：单列铺满，避免卡片过窄 */
@media (max-width: 480px) {
  .blog-mine-scope .blog-grid { grid-template-columns: 1fr; gap: 12px; }
}
```

> 为什么优先页内：`common.css` 是**全站共享文件**、本波仅 B4 持有。页内作用域（前缀类）
> 做到**零全站副作用、易回滚**，风险最低。`.blog-grid` 的覆盖之所以能生效，是因为
> 页内 `<style>` 位于 `<head>` 之后、在 body 解析前生效；即使同为 `0,2,0` 特异性，
> 也会与 `common.css` 的 `.blog-filters`/`.blog-grid` 规则**在「后声明」处取胜**。
> 另加了两道保险：`common.css` 末尾的 `#blogViewMine .blog-grid`（id 选择器，特异性 1,1,0 > 0,2,0），
> 以及 L1385 基础规则本身未动、其它页面不受影响。

### 3.2 `assets/common.css`（121,032 B → 121,877 B，**+845 B**）

**只追加**，位置在**文件最末尾**（既有最后一行 `body.reduce-motion .msg-toast { transition: none; }` 之后），未改动、未重排任何既有字节：

```css
/* ===== B4（20260916）「我的文章」视图布局（作用域限定 · 全站零副作用） ===== */
/* 只命中 #blogViewMine（及其 .blog-mine-scope 类），不影响社区/统计等其它 .blog-grid。
   规则与 blog_wechat.html 页内 <style> 等价，供其它复用同一 id 的页面兜底。 */
#blogViewMine .blog-filters { flex-wrap: nowrap; overflow-x: auto; -webkit-overflow-scrolling: touch; padding-bottom: 2px; }
#blogViewMine .blog-filters .chip { flex: 0 0 auto; white-space: nowrap; }
#blogViewMine .blog-grid { grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 14px; }
#blogViewMine .blog-grid .note-card { height: 100%; }
#blogViewMine .note-body { justify-content: space-between; }
@media (max-width: 480px) {
  #blogViewMine .blog-grid { grid-template-columns: 1fr; gap: 12px; }
}
```

选择 `#blogViewMine` 而非裸 `.blog-grid`：id 选择器只可能命中该视图容器，
**全站零副作用**（其它页即便复用 `.blog-grid` 也不受影响），且特异性 1,1,0 稳压基础规则，无需 `!important`。

### 3.3 版本戳

**本波未 bump 任何 `?v=`**（铁律 4）。`blog_wechat.html` 仍引用 `?v=20260916L`。

---

## 四、`common.css` 改动说明（含「未动 L68」证明）

脚本：`tools/qa/_b4_css_append.py`，结果：`tools/qa/_b4_css_result.json` / `_b4_selfcheck.json`。

| 校验项 | 改前 | 改后 | 判定 |
|---|---|---|---|
| 字节数 | 121,032 | 121,877 | +845（仅追加） |
| CRLF 数 | 2803 | 2814 | 纯 CRLF 保持（+11 行） |
| 裸 LF 数 | 0 | **0** | **PASS** |
| 裸 CR 数 | 0 | 0 | PASS |
| `{` 数量 | 1007 | 1014 | +7（新增 6 条规则 + 1 个 `@media`） |
| `}` 数量 | 1007 | 1014 | 同增 7，**配对未破** |
| 旧文件是改后文件的**严格前缀** | — | `css_starts_with_bak = true` | **追加式修改已证** |

### 「未动 L68」证明

L68 是全局滚动锁 `html, body { height: 100%; overflow: hidden; }`（**已决策冻结**）。

- 改前 L68 = `html, body { height: 100%; overflow: hidden; }`
- 改后 L68 = `html, body { height: 100%; overflow: hidden; }`
- SHA1(改前 L68) = `cd182b329f0897e569fdec67ca5218e266c287c9`
- SHA1(改后 L68) = `cd182b329f0897e569fdec67ca5218e266c287c9`

**两处 SHA1 完全一致 → L68 逐字节未变。** 又因「旧文件是改后文件的严格前缀」成立，
可进一步推出：**改前的每一行（含 L68）都原封不动保留在改后文件相同行号上**，我的改动 100% 落在文件尾部新增段。

### 外壳模型未被破坏

`blog_wechat.html` 保留 `<div class="app">` 外壳（L38，`app_shell_present = true`），
未触碰滚动锁 → 该页仍是「固定外壳 + 内部滚动」模型，**不会出现双滚动条**。

---

## 五、实测结果

### 5.1 ES2017 语法检查

```
node "D:/下载的文件/学习工作台/tools/qa/escheck_es2017.js"
→ DONE 0
```

**PASS**（0 个违规）。

### 5.2 标签配对（`blog_wechat.html`）

| 项 | 读法 | 结果 |
|---|---|---|
| `<!--` / `-->` | 直接计数 | 25 / 25 ✅ |
| `<div` / `</div>` | **剥除 HTML 注释后**计数 | **177 / 177 ✅** |
| `<script` / `</script>` | 直接计数 | 10 / 10 ✅ |

> 说明：不剥注释时 `<div` 计为 178 —— 是因为**我自己的注释文字里写了 `<div class="app">`**（L22），
> 被字符计数误读为一个开标签。剥除注释后为 **177 / 177**，与改前备份**完全一致**（改前 177 / 177），
> 证明**真实 DOM 结构零增删**。这是计数器的假阳性，非 HTML 缺陷。

### 5.3 禁用语法 Grep

| 禁用项 | 命中 |
|---|---|
| `?.` 可选链 | 0 |
| `??` 空值合并 | 0 |
| `.replaceAll(` | 0 |
| `Object.fromEntries` | 0 |
| `.at(` | 0 |
| `(?<=` / `(?<!` 后行断言 | 0 / 0 |
| `window.alert(` / `confirm(` / `prompt(` | 0 / 0 / 0 |

`**` 命中 **1 处**：L200 `<textarea id="blogEditorInput" placeholder="…**加粗**…">`
—— 这是**引号内的 markdown 示例字符串**，属任务书明示的**误报，不改**（且该行非本次改动）。

### 5.4 页面体检（jsdom）

```
QA_PORT=8911 node tools/qa/page_check.js blog_wechat.html "我的文章,互动广场,社区,统计"
```

- 输出头：`数据源 ORIGIN = http://127.0.0.1:8911` / `来源 = 复用已存在的本地静态服务（端口 8911）`
  / **`是否本地 = 是（验的是本地工作区代码）`** ✅
- 关键内容：4 个关键词 **静态 HTML=true 且 运行时 DOM=true**
- `body text len = 4564`；`.card` 类元素 15 个；`button` 15 个
- 运行时错误：**仅 1 条** `JSDOM_ERR: Not implemented: navigation to another Document`
  —— 由页内 `onclick="location.href='AI.html'"` 这类跳转标签在 jsdom 中触发，**非本页代码缺陷，非未捕获异常**
- 渲染证据（页面文本尾部）：`全部 0 已发布 0 草稿 0 归档 0 收藏 0`（chip 行已渲染），
  `还没有公开的发贴…`（社区空态）

**判定：页面正常加载，「我的文章」区域渲染出分类 chip 条目，无未捕获异常 → PASS。**

### 5.5 越界检查（mtime 判据）

| 文件 | 大小 | mtime | 是否被我改动 |
|---|---|---|---|
| `assets/app.js` | 7,073,398 | 2026-09-16 **13:41:42** | **否**（早于我 14:33 的改动） |
| `assets/api.js` | 111,147 | 2026-09-16 **12:18:13** | **否** |
| `blog_wechat.html` | 23,734 | 2026-09-16 **14:33:51** | 是（本任务独占） |
| `assets/common.css` | 121,877 | 2026-09-16 **14:34:04** | 是（本任务独占） |

**未越界改动 `app.js` / `api.js`。**

---

## 六、未验证项与人工核验步骤

### ⚠️ 未做视觉验证（jsdom 无法验证"布局好不好看"）

jsdom **不做布局计算**（无 `getComputedStyle` 的几何结果、无 reflow、无 scrollWidth），
且**不支持 `@media` 生效判定**。以下三项**必须人工在真实浏览器/WebView 核验**：

**核验 A — 375px（安卓手机典型宽度）**
1. Chrome DevTools → 设备工具栏 → 375×812（iPhone X 尺寸近似），或真机 APK。
2. 打开 `blog_wechat.html`，点子导航「我的文章」。
3. **应看到**：分类 chip 行**单行显示**，超出部分可**横向滑动**（不换行、不挤压下方）；卡片**单列铺满**，左右边缘与工具条对齐，**无横向溢出**（页面不出现左右滚动条）。
4. 快速判断横向溢出：DevTools Console 执行
   `document.documentElement.scrollWidth <= document.documentElement.clientWidth` → 应为 `true`。

**核验 B — 1280px（桌面）**
1. 视口设 1280×800，打开同一视图。
2. **应看到**：卡片网格**多列自适应**（每列 ≥260px），卡片高度**等高**，同一行内「编辑 / 归档 / 删除」按钮**底边对齐**（不参差）。类别为空时的空态文案居中。

**核验 C — 回归（防止改坏其它视图）**
1. 同页依次点「社区 / 统计 / 写发贴」三个子 tab。
2. **应看到**：**社区**视图 `.blog-grid` 仍是原 300px 最小列宽（未被我的规则影响）；**统计**视图正常；**写发贴**编辑器正常。
3. 因我的规则全部限定在 `#blogViewMine` / `.blog-mine-scope` 之下，**预期零影响**，但需实测确认。

**核验 D — 深色模式**
切换右上角主题按钮，确认「我的文章」下 chip 与卡片配色正常（仅覆盖了几何属性，未碰颜色，预期无影响）。

---

## 七、待主理人转派的跨文件修改建议（B4 无权改）

> B4 独占文件仅 `blog_wechat.html` + `assets/common.css`；以下根因在**其它文件**，
> **我未做任何改动**，仅给精确建议供主理人转派对应任务线。

### 建议 1（高优先级）· F3 死副本：`app.js` 的 `renderBlogMine` / `renderBlogStats` 永不执行

- **文件 / 行号**：
  - `assets/app.js` **L7474** `function renderBlogMineTabs()`（唯一，仍生效，**保留**）
  - `assets/app.js` **L7515-L7525** `function renderBlogMine()`（**死副本**，被 `api.js` L498 覆盖）
  - `assets/app.js` **L7932-L7965** `function renderBlogStats()`（**死副本**，被 `api.js` L826 覆盖）
- **现象**：`app.js` L7338-7339 `showBlogView()` 调 `renderBlogMine()` / `renderBlogStats()`，
  实际执行的是 `api.js` 版。两版**共存且语义不同**：
  - `renderBlogMine`：app.js 版读本地 `appData.notes`、支持 `blogMineOpen` 菜单/列表两态；
    api.js 版走 `/api/notes` 接口、扁平 chip 行。**两版 UI 不一致**（菜单页 vs chip 行）。
  - `renderBlogStats`：app.js 版只读 `appData.notes`；api.js 版在 `CURRENT_USER` 为空时退回 `blogLocalStats()`。
- **风险**：改 `app.js` 那份**不报错、静默无效**（正是本项目已知的 R3 风险）。
- **建议（二选一，由主理人裁决）**：
  1. **删除 app.js 的死副本**（L7515-L7525、L7932-L7965），只留 api.js 一份 → 消除歧义；
     注意 L7474 `renderBlogMineTabs()` 若无人调用也应一并清理（**改为前须先 Grep 全站调用点**）。
  2. **保留双份但在 app.js 死副本函数首行加注释** `/* ⚠️ 已被 api.js 同名函数覆盖，运行时失效；改此份无效 */`。
- **注意**：`app.js` 由 **B6 线独占**，本波 B4 不碰。

### 建议 2（中优先级）· `renderBlogMine` 的数据源一致性

- **文件 / 行号**：`assets/api.js` **L498-L532**
- **现象**：生效版 `renderBlogMine` 无条件走服务端接口。当**离线 / 未登录 / 未起 `ai-server`** 时
  `api()` 抛错 → 落入 L531 `catch` → 只弹 toast「我的文章加载失败」，`#blogMineGrid` 保持空白，
  **没有像 `blogLocalStats()`（api.js L811）那样的本地兜底**。
- **建议**：参照 `renderBlogStats` 的 `blogLocalStats()` 兜底模式，为「我的文章」补一个
  `blogLocalMine()`（读 `appData.notes` / `appData.favoriteNotes`）作为 catch 分支的降级渲染。
- **是否属 B4**：**否** —— 这是 `api.js` 的行为，且 B4 目标是**布局**，不是数据兜底。列此仅作记录。

---

## 八、遗留问题

1. **视觉未验证**：见第六节，需人工跑核验 A/B/C/D。这是本次唯一的"未闭环"项，属已知限制（工具能力边界）。
2. **F3 双份函数仍在**：本波未消除（越界），仅作了判定与建议转派。**后续任何任务改 `renderBlogMine`/`renderBlogStats` 都必须先看第一节的判定结论**，否则会改到死副本。
3. **`blogMineOpen` 语义在当前页失效**：因生效版 `renderBlogMine`（api.js）不读 `blogMineOpen`，
  `app.js` L8328/L8344 里 `blogMineOpen = true` 的赋值在当前页**无实际作用**（不报错、无副作用）。
  不影响 B4 布局，但属同一 F3 根因，一并记录。
4. **`common.css` 现含 B4 追加段**：后续若有人重排/格式化 `common.css`，会被判为"动了共享文件"。
  已在追加段首行加 `B4（20260916）` 标记便于识别与回滚。
5. **备份文件**（回滚用，命名含 `bak-pre-b4-20260916`）：
   - `blog_wechat.html.bak-pre-b4-20260916`（21,962 B）
   - `assets/common.css.bak-pre-b4-20260916`（121,032 B）

---

## 九、自检清单逐项结果

| # | 检查项 | 结果 |
|---|---|---|
| 1 | `escheck_es2017.js` = `DONE 0` | **PASS** |
| 2 | `<!--`/`-->`、`<div`/`</div>`、`<script`/`</script>` 配对相等 | **PASS**（25/25；177/177（剥注释）；10/10） |
| 3 | 禁用语法 Grep = 0；原生 `alert(`/`confirm(`/`prompt(` = 0 | **PASS**（`**` 1 处为引号内 markdown 字符串，任务书明示误报不改） |
| 4 | `common.css` 仍纯 CRLF（裸 LF=0）、花括号配对未破 | **PASS**（CRLF 2814 / 裸 LF 0；`{`=1014 `}`=1014） |
| 5 | **`common.css` L68 未被修改** | **PASS**（改前后 SHA1 均 `cd182b3…`，逐字节一致；且旧文件是改后文件的严格前缀） |
| 6 | 未越界改动 `assets/app.js` / `assets/api.js` | **PASS**（mtime 13:41:42 / 12:18:13，均早于本次改动时间） |
| 7 | 保留既有 DOM id 与全局函数名 | **PASS**（`blogViewMine`/`blogMineTabs`/`blogMineGrid`/`blogTabMine`/`blogViewList`/`blogGrid` 全在） |
| 8 | 未 bump 版本戳 | **PASS**（仍 `?v=20260916L`） |
| 9 | 未触碰 `C:\Users\ATM\WorkBuddy\Worktrees\...` | **PASS**（全程 `D:\` 绝对路径） |
| 10 | `blog_wechat.html` 保留 `<div class="app">` 外壳 | **PASS**（L38） |
