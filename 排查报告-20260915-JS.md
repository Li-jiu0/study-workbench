# 星途学习工作台 — JS 只读排查报告

- 日期：2026-09-15
- 排查人：sw-js（QA）
- 代码树：D:\下载的文件\学习工作台
- 性质：**只读排查，未修改任何文件**
- 工具：Node v22.22.2-3（`node --check`）、ripgrep（Grep）
- 覆盖范围：`assets/**/*.js` 共 **48 个文件**（含 `assets/emoji/manifest.js`、`assets/data/shenlun-inline.js`）

> 方法学说明：本机 bash 缺 coreutils（`ls/grep/test/dirname` 均不可用），所有文件枚举与
> 内容定位一律走 Glob/Grep/Read 工具或 node 内置 `fs`，避免"误报不存在"造成漏检。
> `node --check` 结果先重定向到 `C:\Users\ATM\_sw_check.txt` 再用 Read 读取。
> **负向对照已做**：构造含真实语法错误的样例文件跑 `--check`，确认其能正确报
> `SyntaxError` 并返回码 1（证明下面的"0 报错"不是因为命令静默失败）。

---

## 清单 1：语法体检（node --check）

**结论：通过。48/48 全部无语法错误，0 报错。**

明细：

| 项 | 值 |
| --- | --- |
| 检查文件数 | 48 |
| 语法报错文件数 | **0** |

被检文件（按 `--check` 顺序，均无输出＝通过）：

```
assets/admin-contact.js      assets/api.js                assets/app.js
assets/admin.js              assets/cet-read.js           assets/cet-translate.js
assets/chat-local.js         assets/comm-cases.js         assets/company-lib.js
assets/config.js             assets/data-cet-read.js      assets/data-cet-translate.js
assets/data-comm-cases.js    assets/data-exam-company.js  assets/data-iv-after.js
assets/data-iv-prep.js       assets/data-ppt-class.js     assets/data-ppt-templates.js
assets/data-ppt-tips.js      assets/data/shenlun-inline.js assets/design-class.js
assets/emoji/manifest.js     assets/error-boundary.js     assets/group-discussion.js
assets/hotnews.js            assets/i-partner.js          assets/icon-map.js
assets/importer.js           assets/iv-after.js           assets/iv-prep.js
assets/mini-cet.js           assets/mini-comm.js          assets/mini-exam.js
assets/mini-interview.js     assets/mini-ppt.js           assets/mini.js
assets/mock-engine.js        assets/mock-result.js        assets/ppt-tips.js
assets/ppt-works.js          assets/qbank.js              assets/quest.js
assets/roleplay.js           assets/study-stats.js        assets/subpage-router.js
assets/topic-express.js      assets/tpl-preview.js        assets/voiceplayer.js
assets/xt-content.js         assets/xt-toast.js
```

建议：无。此清单无需整改。

---

## 清单 2：老 WebView 杀手 —— 正则 lookbehind `(?<=` / `(?<!`

**结论：通过。正式 `.js` 文件中 0 处，上一轮清理结果无回潮。**

明细：

- `assets/**/*.js` 中 `(?<=` / `(?<!` 命中数：**0**。

**但发现 3 个备份文件残留（非阻断，建议清理）**：

| 文件 | 行号 | 现象 |
| --- | --- | --- |
| `assets/app.js.bak-0913o` | 3703 | `var sentences = t.split(/(?<=[\.\!\?\;])\s+/);` |
| `assets/app.js.bak-0913o` | 3710 | `var parts = s.split(/(?<=[\,\，\、])\s+/);` |
| `assets/app.js.backup_20260914` | 3730 | `var sentences = t.split(/(?<=[\.\!\?\;])\s+/);` |
| `assets/app.js.backup_20260914` | 3737 | `var parts = s.split(/(?<=[\,\，\、])\s+/);` |
| `assets/app.js.bak-0913n` | 3698 | `var sentences = t.split(/(?<=[\.\!\?\;])\s+/);` |
| `assets/app.js.bak-0913n` | 3705 | `var parts = s.split(/(?<=[\,\，\、])\s+/);` |

风险评估（**已降级为"低"**）：

1. 这三个文件后缀分别是 `.bak-0913o` / `.backup_20260914` / `.bak-0913n`，**不是 `.js`**，
   不会被 `<script src>` 加载；
2. 全仓 `*.html` 检索 `app.js.bak|app.js.backup` → **0 命中**，无任何页面引用；
3. `android/build_apk.py:61` 的 `REQUIRED_ASSETS` 是白名单制，这三个文件不在名单内，
   **不会打进 APK**；
4. 但 `assets/app.js` 的同名逻辑（3703/3710 对应处）已确认为干净版本。

建议（非阻断，交 fix-bug 组排期）：
- 把这 3 个备份移出 `assets/`（如挪到 `备份/` 或 `.bak/`），彻底消除"某天手滑被
  `<script>` 引到 / 被 `cp -r assets` 打进包"的隐患；
- 或直接删除（内容为 9/13~9/14 旧版，已被现网 app.js 取代）。

---

## 清单 3：其他老 WebView 高危语法（顺带扫）

打包清单（取自 `android/build_apk.py:61-88` `REQUIRED_ASSETS`）中的 **js 文件**：
`icon-map.js`、`subpage-router.js`、`app.js`、`config.js`、`chat-local.js`、`qbank.js`、
`study-stats.js`、`voiceplayer.js`、`mini.js`、`importer.js`、`quest.js`、`hotnews.js`、
`group-discussion.js`、`i-partner.js`、`topic-express.js`、`mini-cet.js`、`mini-comm.js`、
`mini-exam.js`、`mini-interview.js`、`mini-ppt.js`、`emoji/manifest.js`。
（另 `REQUIRED_DATA_ASSETS` 含 `data/mock-papers.js`，不在 `assets/` 内，本轮未扫。）

| 语法 | 命中总数 | 命中文件:行号 | 是否在 Android 打包清单 | 判定 |
| --- | --- | --- | --- | --- |
| `?.` 可选链 | **14** | 见下表（全部在 `assets/app.js`） | **是（app.js 在清单内）** | **不通过** |
| `??` 空值合并 | 0（真实用法） | `assets/data/shenlun-inline.js:4` 为**字符串字面量内**的 OCR 噪声 `"要点①④6??简单叙述铺垫"`，非运算符 | 否（不在清单内） | 通过 |
| `Array.prototype.at(` | 0 | — | — | 通过 |
| `Object.fromEntries` | 0 | — | — | 通过 |
| `String.replaceAll(` | 0 | — | — | 通过 |
| 顶层 `await` | 0 | 全部 `await` 均在 `async` 函数体内（api.js 10 处、app.js 6 处，均有缩进）；且 `node --check` 以 CommonJS 脚本解析，真有顶层 await 必报 SyntaxError，清单 1 已证明 0 报错 | — | 通过 |

### 3.1 可选链 `?.` 明细（14 处，全部在 `assets/app.js`）

| 行号 | 代码片段 |
| --- | --- |
| 5121 | `!appData.viewedContent.etiquette?.includes(e.id)` |
| 5127 | `appData.viewedContent.etiquette?.includes(e.id)` |
| 5188 | `!appData.viewedContent.ivQuestions?.includes(q.id)` |
| 5194 | `appData.viewedContent.ivQuestions?.includes(q.id)` |
| 5269 | `!appData.viewedContent.pptLayouts?.includes(l.id)` |
| 5275 | `appData.viewedContent.pptLayouts?.includes(l.id)` |
| 5381 | `!appData.viewedContent.commScenes?.includes(s.id)` |
| 5388 | `appData.viewedContent.commScenes?.includes(s.id)` |
| 5506 | `!appData.viewedContent.commQuotes?.includes(q.id)` |
| 5512 | `appData.viewedContent.commQuotes?.includes(q.id)` |
| 5646 | `!appData.viewedContent[contentKey]?.includes(id)` |
| 5651 | `!appData.viewedContent[contentKey]?.includes(item.id)` |
| 5676 | `!appData.viewedContent[contentKey]?.includes(id)` |
| 5707 | `appData.viewedContent[contentKey]?.length \|\| 0` |

**风险判定：高（本轮唯一阻断级问题）**

- 可选链是 ES2020 语法，需要 Chromium 80+。老 Android WebView（Chrome 60~70 系内核）
  解析到 `?.` 直接抛 `SyntaxError: Unexpected token '.'`；
- 关键：这是**解析期（Parse）错误**，不是运行期错误 —— 整个 `assets/app.js`
  **一行都不会执行**，且 `app.js` 在 `REQUIRED_ASSETS` 里（build_apk.py:68），
  意味着 APK 里**全站白屏 / 所有交互失效**；
- 同一批代码里已大量使用 `\|\|` / `&&` 兜底写法（如 1326 行
  `const __ct = document.querySelector('.content'); if (__ct) __ct.scrollTop = 0;`），
  说明项目本身是有"老内核兼容"意识的，这 14 处属于**新写入时漏守约定**；
- 14 处的语义全部是"取数组再 `includes`/`length`"，改写成本极低。

**建议改法（交 fix-bug 组，不改语义、纯 ES5 化）**：

```js
// 模式 A：取数组 + includes（5121/5127/5188/5194/5269/5275/5381/5388/5506/5512/5646/5651/5676）
var _v = appData.viewedContent.etiquette || [];   // 或 appData.viewedContent[contentKey] || []
... !_v.includes(id)

// 模式 B：取 length（5707）
var totalViewed = (appData.viewedContent[contentKey] || []).length;
```

补充：`app.js` 里 `Array.prototype.includes` 本身已在别处使用（如 5646 行同一表达式里
的 `!queue.ids.includes(id)`），说明项目已接受 `includes`（ES2016，Chrome 47+），
因此只需消除 `?.` 即可，不必连 `includes` 一起改。

---

## 清单 4：原生弹窗残留 `alert(` / `confirm(` / `prompt(`

**结论：通过。0 处。**

- 检索范围：`assets/**/*.js`
- 检索正则：`\b(alert|confirm|prompt)\s*\(`（用 `\b` 词边界自动排除 `uiAlert` /
  `uiConfirm` / `uiPrompt` / `showToast` / `editorAiAssist` 等合规替代，
  因为它们的 `alert/confirm/prompt` 前一位是字母 `i`，词边界不成立）
- 命中数：**0**

项目铁律"全站禁用原生弹窗"在 `assets/` 侧已完全落实。

---

## 清单 5：明显运行时隐患 —— `document.getElementById(...).xxx` 未判空（抽查）

**结论：不通过。存在高频未判空直取属性的写法。**

- `assets/app.js` 中形如 `getElementById(...).属性/方法` 的**直接链式调用共 139 处**
  （已排除 `var el = getElementById(...); if (el) ...` 这类已判空写法）。
- 全站引用 `app.js` 的 HTML 页面 **33 个**（根目录，排除 `备份/`、`tools/`、`.qa/`）。
- 实测：其中 **6 个页面完全没有 `#topbarTitle` 和 `#morePanel`**：
  `mock_exam.html`、`mock_exam_result.html`、`mock_exam_run.html`、
  `好友申请.html`、`学途.html`、`登录.html`；
  另有 **27 个页面**没有 `#greetingText`（仅首页 `学习工作台.html` 有）。

### 最典型的 5 处

| # | 文件:行号 | 代码 | 现象与影响 |
| --- | --- | --- | --- |
| 1 | `assets/app.js:1312` | `document.getElementById('topbarTitle').textContent = pageTitles[page] \|\| '首页';` | 位于 `navigateTo()` 内，**每次切页必执行**。在无 `#topbarTitle` 的 6 个页面上抛 `TypeError: Cannot set property 'textContent' of null`，并**中断 `navigateTo()` 后续全部逻辑**——包括 1314~1321 导航高亮、1324 主题色切换、1326 滚动置顶、1328 `closeMorePanel()`、`return true`。后果是"页切了但主题色/高亮/状态全错"。这是全清单里**影响面最大**的一处。 |
| 2 | `assets/app.js:1343`、`1348`（另 `1339`、`1340`） | `document.getElementById('morePanel').classList.remove('active');` / `document.querySelector('.more-overlay').classList.remove('active');` | `closeMorePanel()` **被 `navigateTo()` 第 1328 行调用**，同样是"每次切页必执行"，且 `morePanel` 与 `.more-overlay` **两个都未判空**。同文件 1352~1361 的 `toggleToolsPanel()` / `closeToolsPanel()` 却写了 `if (panel && overlay)` 判空——**同一文件内风格不一致**，说明是遗漏而非有意。 |
| 3 | `assets/app.js:1382` | `document.getElementById('greetingText').textContent = greeting;` | `renderGreeting()` 被 `renderHome()` 第 1366 行**第一个**调用。在 27 个无 `#greetingText` 的页面上抛错，会**连带打断 1367~1373 的 `renderCountdowns / renderTasks / renderStats / renderModuleProgress / renderWeakPoints / renderRecentLearning / updateTopbarStats` 全部首页渲染**——一处 null 干掉整屏。历史 bug（#15 首页卡片不刷新）很可能与此类中断同源。 |
| 4 | `assets/app.js:7018`、`7020`、`7025` | `sel.innerHTML = ...`（7018）、`sel.value = ...`（7020）、`document.getElementById('blogEditorInput').addEventListener('input', updateEditorPreview);`（7025） | 位于 `loadBlogEditor()`。同一函数内 7021~7024 **已经**对 `bePrivacy`/`beTags` 写了 `if (prv)` / `if (tg && ...)` 判空，却对 7017 的 `beCat`（`sel`）和 7025 的 `blogEditorInput` 不判空——**同一个函数里两种写法并存**，是最典型的"漏判空"。`blogEditorInput` 在 `学习博客.html` 之外均不存在。 |
| 5 | `assets/app.js:5739-5741`、`6115-6117` | `document.getElementById('examModeNew').className = ...`（5739）、`examModeReview`（5740）、`examModeInfo`（5741） | 行测/词汇模式切换入口，三个元素连续三行直取 `.className` / `.textContent`，**无一个判空**。`mock_exam*.html` 三页（同样引用 app.js）无这些元素，一旦调用即抛错。同类还有 5854~5958（`ec*` 系列，行测刷题页专用）、6177~6259（`vocab*` 系列，四级词汇页专用）共 40+ 行连续未判空。 |

**共性根因**：`app.js` 是全站共用的单文件（约 8400+ 行），里面混装了首页、行测、词汇、
博客、面试、PPT、私聊等**所有页面**的渲染逻辑，但绝大多数取元素处没有做
"当前页面是否有这个元素"的守卫。只要被调用的页面不对口，就是 `TypeError`。

**建议（交 fix-bug 组，按优先级）**：

1. **P0 — 先修 1/2/3 三处（1312、1339-1348、1382）**。它们在"每次切页/每次首页渲染"
   路径上，是全站级影响面。改法最省事：
   ```js
   var _tt = document.getElementById('topbarTitle');
   if (_tt) _tt.textContent = pageTitles[page] || '首页';

   function closeMorePanel() {
     var p = document.getElementById('morePanel');
     var o = document.querySelector('.more-overlay');
     if (p) p.classList.remove('active');
     if (o) o.classList.remove('active');
   }
   ```
2. **P1** — 引入一个统一的 `$id(id)` / `setTxt(id, v)` / `setDisp(id, v)` 小工具
   （内部判空后 no-op），批量替换 139 处直取，成本可控且一劳永逸。
3. **P2** — 或者换思路：在各渲染函数开头加**页面守卫**
   `if (!document.getElementById('page-exam')) return;`，比逐行判空更贴合
   "单文件多页面"的现状。

---

## 总体判定

| 清单 | 结论 | 问题数 |
| --- | --- | --- |
| 1. node --check 语法体检 | **通过** | 0 |
| 2. 正则 lookbehind | **通过**（正式 js 0 处；3 个备份文件残留，非阻断） | 0 阻断 / 3 文件待清理 |
| 3. 其他老 WebView 高危语法 | **不通过** | **14**（`app.js` 可选链 `?.`，且在 APK 打包清单内） |
| 4. 原生弹窗残留 | **通过** | 0 |
| 5. getElementById 未判空 | **不通过** | **5 处典型**（全文件共 139 处直接链式调用） |
| **合计** | — | **阻断级 19 处（14 + 5）**，另 3 个备份文件建议清理 |

**能否上线/打包：**

- **Web 端（现代浏览器）**：可以。清单 1/2/4 全绿，清单 3 的 `?.` 与清单 5 的判空问题
  在 Chrome 80+ 上都不会表现为致命错误（清单 5 仍会在"页面不对口"时抛 TypeError，
  只是被 error-boundary 或控制台吞掉，表现为局部功能失效）。
- **Android APK（老 WebView）**：**不建议直接打包**。清单 3 的 14 处可选链是
  **解析期 SyntaxError**，会令 `assets/app.js` 整体失效 → **APK 内全站白屏**。
  这是本轮唯一的"一票否决"项，必须先清掉再打包。

**修复优先级建议：**

1. **P0（打包前置）**：清 `assets/app.js` 14 处 `?.`（清单 3）—— 否则 APK 白屏。
2. **P0（打包前置）**：补 `app.js` 1312 / 1339-1348 / 1382 三处判空（清单 5 的 #1/#2/#3）。
3. **P1**：清单 5 剩余 2 处典型 + 统一 `$id()` 工具批量收敛 139 处。
4. **P2**：把 `assets/app.js.bak-0913o`、`app.js.backup_20260914`、`app.js.bak-0913n`
   移出 `assets/`（清单 2 遗留项）。

**与历史结论对照**：上一轮"正则 lookbehind 从 2 处清到 0"的成果**已确认保持**，
正式代码无回潮（剩余 6 处全在未被引用的备份文件里）。本轮新增发现的是
**可选链 `?.`**——它和 lookbehind 属于同一类"Node 22 能过 `--check`、老 WebView 会炸"
的语法，建议把它一并写进团队的《老 WebView 语法黑名单》，与 lookbehind 同等对待。

---

*本报告为只读排查产物，排查过程中未对代码树做任何修改。*
*辅助临时文件（非项目文件）：`C:\Users\ATM\_sw_check.txt`、`_sw_dom.txt`、`_sw_qq.txt`、
`_sw_neg.js`、`_sw_neg.txt`（负向对照用，可自行删除）。*
