# 交接文档 · 星途学习工作台（2026-09-14 凌晨 · 交接给下一个 AI）

> 写给接手的 AI：假设你从零接手，读完即可安全开工。
> 前序文档《交接文档-批次三.md》《交接文档-批次四-20260913m.md》《用户需求与决策总账.md》《星途平台改进-需求清单-状态盘点-2026-09-13.md》仍然有效，本文是其增量 + 最新快照。

---

## 一、项目速览（2026-09-14 02:05 最新快照）

- **是什么**：多页 HTML 学习 Web 应用「星途学习工作台」，**32 个页面**（新增 `管理员.html`）+ `assets/app.js`（约 6600 行）+ FastAPI 后端（`server/`），多用户登录 + 管理员超级账号。
- **主项目路径（铁律）**：`D:\下载的文件\学习工作台`。**严禁在 worktree 目录干活**（部署脚本有 ROOT 校验会拒绝 worktree 路径）。
- **线上状态**：线上版本 **`20260914b`**，本地/远端 commit **`102ab2c`**。
  - 服务 `study-workbench` **active**；首页 / `/api/health` / `icon-map.js` / `subpage-router.js` / `data/mock-papers.js` 全 200。
  - 部署校验：**92/92 文件 MD5 全量一致**；本批 9 项内容探针命中；`TRACEBACK_10MIN=0`。
  - **USERS=8**（历史基线 6；今日用户新注册 1 个 + 测试账号，>基线属正常增长）。
  - 服务器：`110.42.134.62`，远端根 `/opt/study-workbench`，服务名 `study-workbench`。
- **Git/GitHub**：`origin = https://github.com/Li-jiu0/study-workbench.git`，**已推送**：`9c270e8..102ab2c  main -> main`。
  - ⚠️ 本沙箱**本地没有 `refs/remotes/origin/main`**（远端跟踪 ref 不落盘），`git log origin/main` 必然报 `Needed a single revision` —— **判断是否推过一律看 push 输出里的 `<old>..<new>  main -> main`**。
- **本批是纯前端改动**：`server/` 一行未动，部署时**不重启后端服务**。

## 二、铁律（违反 = 事故）

1. **APK 永久不打包**（用户拍板；除非用户主动提出，不得再问）。安卓端不追新前端。
2. **不删任何现有功能、保留原有 DOM** —— 例外仅限用户明确拍板的删除。
3. **词库/题库/文案全部自撰**，不得抄袭外部内容（版权红线）。
4. **未经用户明确要求不 push GitHub**；生产操作（删数据 / 改 nginx / 重启服务 / 部署上线）需用户点头。
5. **管理员对普通用户保持隐形**（硬需求，不可回退）：搜索 / 好友 / 在线列表 / 公开主页都不出现管理员；**只有 `GET /api/admin/contact` 这一条通道**能让普通用户拿到管理员 id 并发私信。
6. **`assets/icon-map.js` 单写者规则**：多线并行时只允许唯一一条线改它。
7. **并行子代理改共享大文件（app.js / chat-local.js）前必须重新 Read 最新版**，只做精确 Edit；文件零重叠分区，否则互相覆盖回滚。
8. **凭据红线**：服务器密码 / JWT / API Key 一律走环境变量或 `.gitignore` 掉的 `upload_v23.ps1`，**绝不入库**。

## 三、标准工作流（每批照此走）

```
改（多线并行，文件零重叠）
→ node --check <改动的 js>
→ 页面配对自检（<!-- -->、<div>、<style>/<script> 计数平衡）
→ 版本戳 bump（改了 assets 必 bump，本期 = 20260914b）
→ 独立 QA 证伪回归（脚本写报告文件再读，不信 stdout）
→ git add <指定路径>（勿用 -A）→ git commit
→ SW_DRY_RUN=1 python tools/deploy_update_20260914b.py     （本地清单+打包，不联网）
→ python tools/deploy_update_20260914b.py                  （上传+全量 MD5+DB 备份+探针）
→ git push（用户明确要求时）
```

**部署脚本模板**：`tools/deploy_update_20260914b.py`（由 `..._20260913o.py` 派生）。
新批次复制后改 4 处即可：`STAMP`、`MANIFEST_PATH`、`PROBE_PY` 里的本批内容探针、`judge_probes()` 里对应的断言块。
脚本自带：blog_wechat.html 排除、ROOT 铁律校验、资源硬断言、ASCII 包名中转、远端逐文件 MD5 全量比对、DB 自动备份、探针剥注释。

## 四、本批（批次五）交付清单

### 4.1 代码改动（本轮上线）

| 编号 | 内容 | 改动文件 |
|---|---|---|
| **R31** | 今日任务单条删除：新增 `removeTodayItem(key,id)`，5 张学习卡片今日区加「移除」按钮（仅未学且今日） | `assets/app.js` |
| **R32** | 场景/金句库 emoji → `data-icon` 升级（`COMM_EMOJI_ICON_MAP` + `commIconHtml`；仅 🦋 无匹配图标显式回退 emoji） | `assets/app.js`、`assets/icon-map.js`（+9 图标） |
| **R33** | 私聊页 tab 栏样式统一（`.im-tab.active` 改 `var(--primary)`，去掉旧渐变；「加好友」按钮与其他按钮同步） | `私聊.html` |
| **R34** | 设置页「账号与安全」检测不到账号：`stInitAccount` 从解析期移出 → `DOMContentLoaded` 后就绪执行 + 进入子页刷新 + `ST_ACCT_RETRIES` 600ms×2 重试加固 | `设置.html` |
| **R35** | `fetchJSONAnywhere`：词库加载链 fetch → XHR 回退（file:// 可直开） | `assets/*.js` |
| **R36** | 四级词汇每日复习队列（`dailyReview` 默认 20、clamp 1~100、跨日重建）+ 设置页「每日复习量」 | `assets/*.js`、`设置.html` |
| **R37** | 首页内置任务条移除（`tasks: []` 默认空、`renderTasks` 空列表提前返回、`setStatText` null-safe、loadData 清旧档），修掉点击内置任务的白屏/崩溃 | `assets/app.js` |
| **R39** | **非好友来信（如管理员私信）动态生成会话**：`getFriend` 补 `S.chats` 回退、`imEnsureServerChat`、`loadChats` 回填持久化 → 可点开可回复、已读后保留、刷新不丢 | `assets/chat-local.js` |
| **R40** | **管理员好友 tab 展示全部注册用户**（数据源 `GET /api/admin/users`）+ 最近活跃时间徽标（在线=绿点 / `imFmtLastActive` 格式化）；403 时降级回 `/api/friends` | `assets/chat-local.js` |
| **R41** | `imOpenChatWithUser`：管理员对任意用户点「发消息」即时进会话（原 `imOpenChat` 查不到好友就静默 return） | `assets/chat-local.js` |
| **R42** | 用户行显示 `@账号`；新增页内资料卡 `imShowUserProfile`（账号 / 注册时间 / 最近活跃 / 笔记数 / 学习事件），替代会跳到本人资料的 `openUserHome` | `assets/chat-local.js` |
| **R43** | 未读角标挂到「联系管理员」入口（`refreshEntryBadge` + `.ac-badge`，5s 轮询、`document.hidden` 跳过、打开面板清零）；普通用户侧不再为管理员来信重复建会话行 | `assets/admin-contact.js`、`私聊.html` |

### 4.2 同批已完成的需求（更早提交）

| 需求 | 内容 | 关键 commit |
|---|---|---|
| 需求01 管理员超级账号 | 后端 `GET /api/admin/{overview,users,users/{id},online,feedback,contact}` + 反馈回复；前端 `管理员.html` + `assets/admin.js/css`（概览三卡 / 三 tab / 用户详情 / 反馈回复 / 403 提示 / 30s 自刷新）；`更多.html` 入口（仅管理员可见）；`登录.html` 落 `is_admin`；私聊页「联系管理员」固定入口 | `f803f4f`、`1a8e077`、`28266cf`、`a467cfe`、`9f3d747` |
| 需求02 图标全站升级 | 21 个页面剩余 emoji → `data-icon`；`icon-map` 补 20 个语义图标 | `abd0143`、`b8cd890` |
| 需求06/07/26/28 | 状态回写 ✅（明细见状态盘点文档） | `17857b7`、`ff1c184` |
| 设置页「我的反馈」 | 走 `GET /api/feedback/mine`，有回复显著展示「管理员回复」 | `f803f4f` |

### 4.3 本批 commit 序列（09-14 00:10 → 02:02）

```
f803f4f 需求01 超级管理员账号（前后端）+ 用户反馈管理 + 需求28 i人沟通镜像
b8cd890 feat(icons): icon-map 补齐 20 个语义图标
a1ca9fd / de98d4c chore(server): 反馈运行数据加入 .gitignore 并停止跟踪
28266cf 需求01：私聊页新增「联系管理员」固定入口
abd0143 需求02 收尾：全站剩余 emoji 统一升级为 data-icon（21 页面）
45cbcbf 需求01：联系管理员——乐观消息去重与失败回滚
1a8e077 feat(admin): GET /api/admin/contact
2efe83a / a467cfe 需求01：联系管理员改用 /api/admin/contact 解析管理员 id
9f3d747 需求01：admin.js 用户行归一化补 isOnline camelCase 别名
ff1c184 需求28 收尾：i人心法两卡镜像进 高情商表达.html
17857b7 台账回写：需求 01/06/07/26/28 状态 ❌→✅
102ab2c R39-R43：私聊与管理员体验修复上线（20260914b）   ← 最新，已推送
```

### 4.4 QA 结果（全部由独立 QA 证伪式回归）

| 范围 | 结果 | 证据 |
|---|---|---|
| R31–R36 | **147/147 PASS** | `tools/verifier/verify_feedback_r31_r34.js`、`tools/qa/_qa_feedback_r31_r34.txt` |
| R37 修复回归 | 4/4 + 边界 8/8 PASS | `tools/qa/_r37_verify.txt`、`_r37_qa_edge.txt` |
| R39–R43 | **15/15 PASS**（另修 1 处非阻断瑕疵） | `tools/qa/_r3943_*.js/.txt/.md` |

## 五、图标体系（全站规范，不变）

- 写法 `<span class="nav-icon" data-icon="名称" data-icon-size="14"></span>`，由 `assets/icon-map.js` 渲染（lucide 风格内联 SVG，现注册 100+；只准 path/rect/circle，禁 mask/filter/symbol/use）。
- 动态 `innerHTML` 插入含 `data-icon` 的 DOM 后**必须补调** `window.lucideAutoRender && window.lucideAutoRender()`。
- 白名单（不判残留）：placeholder 文本、select/option、聊天/AI 回复正文、头像字段、内容型 badge、主题按钮文案。

## 六、本机环境坑（全是实锤）

- **bash 缺 coreutils**：无 `ls / grep / tail / head / sleep / env / mkdir`。读写一律用 Read/Edit/Grep/Glob + node/python；长输出**重定向到 ASCII 名文件再 Read**。
- **`env` 命令不存在** → SOP 里 `env -u HTTP_PROXY ... git push` 直接 `rc=127`（**假失败**，实际没执行）。推 GitHub 用 Python 过滤环境变量：
  ```python
  env = {k:v for k, v in os.environ.items() if k.lower() not in ('http_proxy','https_proxy')}
  subprocess.run(['git','-c','http.proxy=http://127.0.0.1:7897','-c','https.proxy=http://127.0.0.1:7897','push','origin','main'], env=env, ...)
  ```
  可用代理端口 **7897**（会话里 `HTTP_PROXY=127.0.0.1:57065` 是死代理）。成功标志在 **stderr**：`<old>..<new>  main -> main`（stdout 为空是正常的）。
- **中文路径 `git add`**：Bash 下 `git add <中文路径>` 有**静默 no-op**风险；用 PowerShell `git add -- "中文路径"`，或 Python `subprocess.run(['git','add','--',f])` 后**核对 `git status --porcelain` 确实 staged**再 commit。
- **并行 Edit 同一文件必然互相覆盖** → 文件零重叠分区或串行。
- **服务器无 `sqlite3` CLI**，查库用 `python3 -c` + sqlite3 模块。
- **pscp 中文名会按 GBK 编码落成乱码** → 打包 tar 用 ASCII 包名上传（现成脚本已处理）；远端 grep 中文不可靠，用 MD5 定位。

## 七、待办与已知遗留（按优先级）

1. **`个人中心.html` 不解析 `?user=`**（已 grep 确认无 `URLSearchParams`）→ 站内所有 `openUserHome(uid)`「看 TA 主页」入口**实际展示的是登录者自己的资料页**。本批只在管理员用户列表旁路（改用页内资料卡），**动态、留言板、群成员头像等位置仍走老路径**。建议后续统一给 `个人中心.html` 补 `?user=` 支持。
2. `私聊.html` `.ac-entry` 图标 span 内遗留 emoji `🛡️`（运行时被 `lucideAutoRender` 覆盖，无害，未在授权范围故未动）。
3. 仓库根有 **21 个未跟踪文件**（`_r01_*`、`_r37_bak.txt`、`_r39.txt`、`tools/deploy_update_20260913m.py`、`tools/local_verify_app.py`、`交接文档-批次四-20260913m.md` 等）—— 清理前先确认无引用。
4. 管理员用户列表为**全量渲染、未分页**（当前 8 用户无压力）；用户量上来后需给 `/api/admin/users` 加分页。
5. 需求总账：30 条需求 **25✅ / 3🟡 / 1❌（仅需求 20）**，明细见《星途平台改进-需求清单-状态盘点-2026-09-13.md》。
6. **额度风险**：本轮有一条工程师线因账号 **429 额度耗尽**中断（代码已落地，后由另一工程师接手补齐）。多线并行时注意额度，遇 429 不要反复重试。

## 八、QA 与运维工具索引（主项目 `tools/` 下）

| 命令 / 文件 | 作用 |
|---|---|
| `python tools/deploy_update_20260914b.py`（`SW_DRY_RUN=1` 可 dry） | 部署（上传 + 全量 MD5 + DB 备份 + 探针） |
| `tools/qa/_deploy_0914b_manifest.txt` | 最近一次部署清单（92 条） |
| `tools/qa/_r3943_REPORT.md` | R39–R43 独立回归汇总报告 |
| `tools/qa/_qa_feedback_r31_r34.txt` | R31–R36 回归 147 项明细 |
| `tools/local_verify_app.py` | 本机起 127.0.0.1:8000 全栈验证服务（前端+后端同源） |
| `node tools/verifier/verify_*.js` | 各类 jsdom 前端行为断言（`NODE_PATH=C:\Users\ATM\node_modules`） |
| `tools/plink.exe` / `tools/pscp.exe` | 服务器操作（**不在 PATH，写全路径**，带 `-hostkey`） |

## 九、给接手 AI 的三句话

1. 改任何东西前先看本文第一章的线上快照与第四章的改动清单，别在已验证的成果上重复施工；**收工前跑一遍相关 QA 脚本**，红了你就是事故第一责任人。
2. 用户的截图需求要「全页审计」而不是只改红框；但 placeholder/option/正文/头像里的 emoji 是白名单，别越界。
3. 生产操作永远等用户点头；其余事「按你的意见排序执行，中间不用再问」。
