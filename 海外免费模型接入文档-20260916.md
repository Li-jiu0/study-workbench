# 海外免费模型接入文档（OpenRouter + Google Gemini）

> 测试时间：2026-09-16
> 测试范围：OpenRouter 全部20个免费模型 + Google Gemini 7个模型，逐个实测
> 定位：**梯子分组**——需代理才能访问的海外模型，作为降级备选，非主力

---

## 一、结论

两个海外平台共测试27个模型，**真正可用的只有10个**。全部归入模型选择列表的「梯子」分组，国内模型（智谱+硅基11个）直接平铺。

| 平台 | 测试数 | 可用数 | 国内直连 | 定位 |
|------|--------|--------|---------|------|
| OpenRouter | 20 | 8（推荐接4个） | ❌ 需梯子 | 降级备选 |
| Google Gemini | 7 | 2 | ❌ 需梯子 | 降级备选 |

---

## 二、OpenRouter 可用模型（推荐接4个）

| 显示名称 | 模型 ID | 响应时间 | 用途 |
|---------|---------|---------|------|
| OR-Auto | `openrouter/free` | 1.9s | 自动路由，最稳 |
| Nemotron-Super | `nvidia/nemotron-3-super-120b-a12b:free` | 1.9s | 快速通用 |
| Gemma-4-31B | `google/gemma-4-31b-it:free` | 2.1s | 快速通用 |
| Nemotron-Ultra | `nvidia/nemotron-3-ultra-550b-a55b:free` | 10.9s | 深度推理 |

> 其余4个可用模型（Ling-VL、Nex-N2.5-Pro、Cohere-Code、Nemotron-Lightning）不接，避免列表过长。

---

## 三、Google Gemini 可用模型（接2个）

| 显示名称 | 模型 ID | 响应时间 | 用途 |
|---------|---------|---------|------|
| Gemini-3.5-Flash | `gemini-3.5-flash` | 6.4s | 通用，Google旗舰 |
| Gemini-3.5-Flash-Lite | `gemini-3.5-flash-lite` | 1.1s | 超快轻量 |

> 旧版 gemini-2.5 系列已对新用户关闭（404）；gemini-flash-latest 高峰期503；gemma-4 系列500内部错误；新 Interactions API 国内连接不稳定，**只用旧的 generateContent 格式**。

### ⚠️ Gemini 格式适配说明（重要）

Gemini **不是 OpenAI 兼容格式**，ai-service.js 需要单独适配：

- **端点**：`https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent?key={apiKey}`
- **请求体**：
  ```json
  { "contents": [{ "parts": [{ "text": "用户问题" }] }] }
  ```
- **响应解析**：`response.candidates[0].content.parts[0].text`
- **Key 传递方式**：URL 参数 `?key=xxx`，不是 Authorization header

---

## 四、ai-config.js 配置参考

### 1. providers 新增两个平台

```javascript
// OpenRouter（OpenAI兼容格式）
openrouter: {
  name: "OpenRouter",
  apiUrl: "https://openrouter.ai/api/v1/chat/completions",
  apiKey: "***REMOVED-BY-R2C***",
  extraHeaders: {
    "HTTP-Referer": "http://110.42.134.62",
    "X-Title": "Xingtu Learning"
  }
},

// Google Gemini（非OpenAI兼容，需单独适配请求/响应格式）
gemini: {
  name: "Google Gemini",
  apiUrl: "https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent",
  apiKey: "AQ.Ab8RN6IO7WLjJiQmQsgs3dk473fqX3kngQNt-6KAFO38U_S3tw",
  apiFormat: "gemini",  // 标记格式类型，ai-service.js据此适配
  keyInQuery: true       // key通过URL参数传递，非header
}
```

### 2. builtinModels 新增6个（接在硅基模型后面，全部 tag:"梯子"）

```javascript
// OpenRouter 4个
{ id: "or-auto", name: "OR-Auto", provider: "openrouter",
  model: "openrouter/free", types: ["general"], tag: "梯子", rate: "1x",
  temperature: 0.7, maxTokens: 1000, fallback: "or-nemotron-super" },
{ id: "or-nemotron-super", name: "Nemotron-Super", provider: "openrouter",
  model: "nvidia/nemotron-3-super-120b-a12b:free", types: ["general"], tag: "梯子", rate: "1x",
  temperature: 0.7, maxTokens: 1000, fallback: "or-gemma-31b" },
{ id: "or-gemma-31b", name: "Gemma-4-31B", provider: "openrouter",
  model: "google/gemma-4-31b-it:free", types: ["general"], tag: "梯子", rate: "1x",
  temperature: 0.7, maxTokens: 1000, fallback: "gemini-flash-lite" },
{ id: "or-nemotron-ultra", name: "Nemotron-Ultra", provider: "openrouter",
  model: "nvidia/nemotron-3-ultra-550b-a55b:free", types: ["general","reasoning"], tag: "梯子", rate: "0.5x",
  temperature: 0.3, maxTokens: 2000, fallback: "deepseek-r1-8b" },

// Google Gemini 2个
{ id: "gemini-flash", name: "Gemini-3.5-Flash", provider: "gemini",
  model: "gemini-3.5-flash", types: ["general"], tag: "梯子", rate: "1x",
  temperature: 0.7, maxTokens: 1000, fallback: "gemini-flash-lite" },
{ id: "gemini-flash-lite", name: "Gemini-3.5-Lite", provider: "gemini",
  model: "gemini-3.5-flash-lite", types: ["general"], tag: "梯子", rate: "1x",
  temperature: 0.7, maxTokens: 1000, fallback: "glm-4-flash" }
```

---

## 五、国内访问提醒

**OpenRouter（openrouter.ai）和 Google Gemini（googleapis.com）均为海外服务，国内普通网络无法直连。**

- 用户开了代理/梯子时可用；
- 没梯子的用户调用会超时或连接被关，**降级链必须能自动切回智谱/硅基**；
- 定位：**降级备选**，不是主力。现有智谱（5个）+ 硅基（6个）= 11个国内直连免费模型作为主力；
- 超时时间建议设为15秒，超时后自动降级，不要让用户干等。

---

## 六、模型选择列表分组要求

AI 页面的模型选择下拉列表：

- **国内模型（智谱+硅基共11个）直接平铺**，不设分组标题；
- 一条分隔线；
- **「梯子」分组标题**（灰色小字，不可点击）；
- 「梯子」分组下共6个模型：OpenRouter 4个 + Gemini 2个；
- 分隔线 + 配置自定义模型。

```
┌─────────────────────────┐
│  G GLM-4.7-Flash    1x  │  ← 国内模型直接平铺
│  G GLM-4.5-Flash    1x  │
│  G GLM-4-Flash      0.8x│
│  G GLM-4.6V-Flash   1.2x│
│  G GLM-4V-Flash     1x  │
│  Q Qwen2.5-7B       1x  │
│  Q Qwen3-8B         1.2x│
│  Q Qwen3.5-4B       0.5x│
│  D DeepSeek-R1-8B   2x  │
│  G GLM-4-9B         1.2x│
│  H Hunyuan-MT-7B    1x  │
│  ─────────────────────  │  ← 分隔线
│  梯子                     │  ← 分组标题（灰色小字）
│  O OR-Auto          1x  │
│  N Nemotron-Super   1x  │
│  G Gemma-4-31B      1x  │
│  N Nemotron-Ultra   0.5x│
│  G Gemini-3.5-Flash 1x  │
│  G Gemini-3.5-Lite  1x  │
│  ─────────────────────  │
│  + 配置自定义模型         │
└─────────────────────────┘
```

- 不可用的模型（限流、403、超时、空回复、已关闭）**一律不显示、不配置**；
- 「梯子」分组的模型 tag 统一显示"梯子"。
