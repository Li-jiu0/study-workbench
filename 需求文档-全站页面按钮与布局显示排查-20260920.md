# 需求文档：全站页面按钮与布局显示排查（第四批）

> 日期：2026-09-20
> 范围：项目根目录下全部 
>
> **52 个真实 HTML 页面**
>
>  \+ 
>
> `assets/`
>
>  公共资源（common.css/app.js/ 各 xt-*.js/page-head.css 等）
> 方式：自动化脚本逐页扫描（资源引用、失效按钮、视口、固定定位与安全区、表单、图片、字号、伪按钮、层级、双头部、栅格）+ 关键页面人工读源码核对
> 原则：
>
> **只发现问题、不改业务代码**
>
> ；每条给出 文件：行号、现象、改法、验收标准，供 workbuddy 直接动手
> 说明：本批是「显示 / 按钮 / 交互」专项；消息横幅、实时位置、聊天键盘、顶部黑边见文档①，登录分享 / 设备统计 / 安装列表见文档②，稳定性与体验杂项（含 U4 高度、U9 长按引用）见文档③，不重复。



***

## 〇、先说结论

经过多轮统一化，全站页面的「骨架」已经比较规范，**没有大面积错位、资源丢失或批量死按钮**：52 个页面没有任何 JS/CSS 引用缺失，视口、响应式栅格、底栏安全区、表单防误提交、弹窗锁滚动都已覆盖（详见第三章「已核查无问题」）。

本批真正要处理的问题收敛为：



* **P2（真机上点不动 / 被遮挡，建议本批修）3 项**：更新页 1 个死按钮；2 个独立全屏页底部条没让手势安全区；弹窗 / 抽屉打开后安卓返回键直接退出页面而不是先关弹窗。

* **P3（一致性 / 规范，建议批量收口）7 项**：按压反馈不统一、Toast 各写各的、字号偏小、图标按钮热区偏小、弹窗高度没跟 dvh、z-index 层级混乱、列表缺「加载失败」态。



***

## 一、P2 问题（真机可见，优先修）

### P2-1　「更新」页更多面板里的「我的发贴」是死按钮（点了没反应）



* **位置**：`更新.html:286`



```
\<div class="bottom-more-item" onclick="gotoBlogMine()"> … 我的发贴/互动广场 … \</div>
```



* **根因**：`gotoBlogMine` 只定义在 `assets/api.js:263`（`function gotoBlogMine(){ location.href='社区.html#mine'; }`），但 `更新.html` 底部只引了 `xt-polyfill / icon-map / xt-toast / error-boundary / app.js / config / xt-update`（见 `更新.html:379-384`），**没有引 api.js，app.js 里也没有这个函数** → 点击报 `gotoBlogMine is not defined`，无任何反应。

* 对照：同面板 `更新.html:312` 的 `openQuest` 写了 `if(window.openQuest){…}else{location.href='工具.html'}` 兜底，所以它不是死按钮；唯独 `gotoBlogMine` 是裸调用。`blog_wechat.html:82/278`、`演示.html:160` 也调 `gotoBlogMine`，但这两页能取到定义（演示引了 api.js），不受影响。

* **改法（二选一，推荐第 1 种，改动最小、不引新脚本）**：

1. 把 `更新.html:286` 的 onclick 改成与函数本体等价的跳转并加兜底：

   `onclick="if(window.gotoBlogMine){gotoBlogMine()}else{location.href='社区.html#mine'}"`

2. 或在 `更新.html` 补引 `assets/api.js`（注意 api.js 会带来登录态 / 轮询逻辑，需回归更新页无副作用）。

* **验收**：在「更新」页打开底部「更多」面板，点「我的发贴 / 互动广场」能正常跳到 `社区.html#mine`，控制台无报错；断网 / 未登录场景也不卡死。

### P2-2　两个独立全屏页的底部固定条没有让出手势安全区（全面屏手势条机型贴边 / 被压）

公共底部导航 `.bottom-nav` 已在 `common.css:1374` 加了 `padding-bottom: env(safe-area-inset-bottom)`，所以带底栏的页面没问题；问题出在**没有公共底栏、自己在页面底部 fixed 一条**的独立页面：



1. **AI 设置页底部说明条**

* 位置：`ai-settings.html:435`



```
.xt-set-foot{ position:fixed;left:0;right:0;bottom:0;z-index:15; padding:9px 14px; … }
```



* 现象：该页是全屏独立页（无公共底栏），底部说明条 `bottom:0` 且 padding 没有 `env(safe-area-inset-bottom)`，在手势条全面屏上文字贴最底边、被横条压住。

* 改法：`padding:9px 14px;` 改为 `padding:9px 14px calc(9px + env(safe-area-inset-bottom,0px));`（或 padding-bottom 单独写一行）。

1. **模拟考试答题页的「答题卡」常驻把手**

* 位置：`mock_exam_run.html:206`（`.mock-ac-pane{position:fixed;…bottom:0;…}`）、`:210`（`.mock-ac-toggle{…padding:14px 16px;…}`，无安全区）；页面主体容器 `:202-203` `padding-bottom:70px` 也没把安全区算进去。

* 现象：答题卡面板平时露出一条「答题卡 0/N」把手（HTML 在 `:522-526`）常驻屏幕最底，手势条机型上把手贴底、最后一题 / 底部内容可能被面板和手势条一起挡住。

* 改法：


  * `.mock-ac-pane` 的把手区加 `padding-bottom: env(safe-area-inset-bottom,0px)`（或把手按钮 padding-bottom 叠加安全区）；

  * 主体 `padding-bottom:70px` 改为 `calc(70px + env(safe-area-inset-bottom,0px))`，保证最后一题不被面板 + 手势条遮挡。

1. **（顺带，低频页）赞助页收款码大图预览**

* 位置：`赞助.html` 内 `.sp-lb`（大图 lightbox，fixed bottom 且无安全区）。

* 现象：全屏看收款码时，底部「左右切换 / 保存到相册」按钮在手势条机型贴底。

* 改法：lightbox 底部按钮容器 `padding-bottom` 叠加 `env(safe-area-inset-bottom,0px)`。

- **统一验收**：在开启「全面屏手势」的安卓机（或开了手势导航的模拟器）上，分别打开 AI 设置、模拟考试答题、赞助大图页，底部说明条 / 答题卡把手 / 大图按钮与手势横条之间有正常间距，不贴边、不被压；三键导航机型显示不变。

### P2-3　弹窗 / 抽屉 / 更多面板打开时，安卓返回键没有「先关弹窗」，而是直接退页面甚至退 App



* **现状（已做对的）**：图片查看器 `img-viewer.js:405-476`、个人资料弹层 `xt-profile.js:1087-1099`、子页路由 `subpage-router.js:101-134` 已经用 `history.pushState` + `popstate` 实现了「返回键先关弹层」，可作为标准范例。

* **问题**：全站其余大量弹层是直接切 `display/.active` 打开、**没有压入历史**，包括但不限于：


  * 通用模态 `common.css .modal-overlay/.modal` 体系（各页设置弹窗、确认框）；

  * 底部「更多 / 工具」面板（`toggleMorePanel / toggleToolsPanel`，首页、更新、演示、blog\_wechat 等）；

  * AI 侧滑 / 设置面板（`AI.html:533` 抽屉、模型选择 / 快捷指令弹层）；

  * 申论 `.sl-overlay`、时政 `.hn-overlay`、AI 设置 `.xt-modal-mask`、设置页各弹层、赞助大图等。

    安卓壳 `MainActivity` 的返回键走的是网页 `history`（canGoBack 则 goBack）。这些弹层打开时历史栈里没有记录，用户按返回键期望「关掉这层」，实际却是**返回上一页 / 退出整个 App**，与微信等主流 App 行为不一致。

* **改法（建议做成统一能力，不要每页各写）**：

1. 在公共脚本里提供一对「弹层压栈 / 出栈」工具（参考 img-viewer 的实现）：弹层打开时 `history.pushState({xtLayer:id},'')`，监听 `popstate` 时关闭弹层；点遮罩 / 右上角 × 关闭时若栈是自己压的则 `history.back()` 收尾，避免历史残留。

2. 所有 modal / 抽屉 / 更多面板 /lightbox 打开统一走这对工具。

3. 配套统一「点遮罩关闭 + 右上角 × 关闭」，三件套行为一致。

* **验收**：任意页面打开 弹窗 / 更多面板 / AI 抽屉 / 大图，按安卓返回键**只关闭弹层、停留在当前页**；连续打开两层弹层时返回键逐层关闭；弹层关闭后再按返回键才是正常的页面返回 / 退出；不会出现历史栈残留（关了弹层还要多按一次返回）。



***

## 二、P3 问题（体验一致性 / 规范，建议批量收口）

### C1　可点卡片「按下去没反应」的视觉反馈不统一



* 现状：`common.css` 只给部分元素做了按压态 ——`.btn:active`（`:521/:1862`）、`.nav-item:active`（`:1879`）、`.bottom-nav-item:active`（`:1893`）、`.im-sess/.task-item/.note-item:active`（`:1905`）、`.morepage-card:active`（`:2224`）、`.subpage-group-card:active`（`:2419`）、AI 伙伴卡 / 芯片等。

* 问题：扫描发现全站每页有 **18～40 个**用 `div/span/li` 直接挂 `onclick` 的「伪按钮」（设置 37、关于 40、工具 / 学习概括 35、社区 / 学途 / 更新 34 左右、blog\_wechat 39……），其中大量内容页自定义卡片（首页模块卡、学途 `.xt-mod/.xt-q`、工具 / 更多宫格 `.bm-*`、PPT 库 / 万能金句 / 商务礼仪 / 场景话术 / 企业定向库的列表项、我的文件 `.mf-*`、面测 / 英语 / 行测的选项卡等）**只有 PC 端&#x20;**`:hover`**、没有移动端&#x20;**`:active`。触屏上点按没有任何下沉 / 变色反馈，像没点中。

* 改法：在 common.css 定一个统一按压规范，让所有可点卡片复用，二选一：


  * 推荐：约定可点卡片统一加 class（如 `.tap`），公共样式 `.tap:active{transform:scale(.98);}`（深色背景的卡片用 `filter:brightness(.96)`），各页把伪按钮补上该 class；

  * 或加一条受限的通用规则（注意别误伤纯展示的 div）：对明确的可点容器类统一 `:active` 反馈。

* 验收：所有能点的卡片 / 宫格 / 列表项 / 选项，手指按下时有轻微缩放或变暗、抬起恢复；纯展示内容不受影响。

### C2　Toast 轻提示各页各写一套，位置 / 时长 / 层级不统一



* 现状：项目已有标准 `assets/xt-toast.js`（`window.xtToast(success/error/warning/info)`，同时只显示 1 条、1.6s 自动消失、`role=status`，样式类 `.xt-toast`）。

* 问题：很多老页面没用它，而是内联了自己的提示：


  * 顶部、`z-index:2000`：`学习工作台.html:404`、`个人中心.html:483`、`更多.html:269`、`设置.html:931`、`我的文件.html:267`、`导入题库.html:247`、`朋友圈发布.html:145`（各自一个 `#toast`）；

  * 底部 `bottom:40px、z-index:9999`：`好友申请.html:81`（afToast）；

  * 底部 `bottom:74px、z-index:400`：`ai-settings.html:445`（setToast）；

  * 私聊、IM 等又是另一套。

    后果：提示位置忽上忽下、时长不一；尤其 `setToast` 层级只有 400，而该页弹窗遮罩是 900（见 C6），**弹窗里触发的提示会被遮罩盖住、用户看不到**。

* 改法：统一收口到 `xtToast()`，删除各页内联 `#toast / afToast / setToast`（或让它们内部转调 xtToast）；确需底部提示时给 xtToast 增加位置参数，而不是另写一套。

* 验收：全站成功 / 失败 / 警告提示样式、位置、时长一致；弹窗内触发的提示一定显示在遮罩之上；同一时刻不叠加多条。

### C3　字号偏小，部分辅助 / 可点文字低于可读下限



* 扫描结果：大量页面存在 11～13px 小字，`学途.html`、`私聊.html` 甚至出现 **10px**；`PPT素材库.html` 报出 4～10px（经核对多为图标字体 `data-icon` 的字号，不是文字，可忽略）。

* 建议下限（自用安卓 WebView，定一个能落地的规范）：


  * 正文 ≥ **14px**；辅助说明 ≥ **12px**；**可点击的文字（按钮 / 链接 / 标签）≥ 13px**；10px 及以下不用于文字。

  * 重点先处理：学途、私聊里的 10px 文字；ai-settings 底部说明条 11.5px（`ai-settings.html:436`）；登录页 11～12px 的强度 / 协议提示可保留但不要再小。

* 验收：真机正常握持距离下所有可读文字清晰；不存在 10px 及以下的文字（图标字体除外）。

### C4　图标按钮热区偏小，手指难点、易误触



* 典型：登录页密码可见按钮 `.eye{width:32px;height:32px}`（`登录.html:72`），小于推荐的 40～44px 触控热区，且紧贴输入框右缘。

* 同类风险：各页右上角关闭 ×、返回、播放 / 暂停、语音、加号、图片查看器翻页等「只有图标」的小按钮，部分视觉尺寸 24～32px、padding 不足。

* 改法：**图标视觉大小可不变，用 padding / 伪元素把可点热区扩到 ≥40×40px**（图标在热区内居中）；密集排列时用间距保证不串键。

* 验收：登录页眼睛按钮、各弹窗关闭 ×、播放 / 翻页等图标按钮，手指点按命中率高、不易误触相邻元素。

### C5　弹窗 / 面板 / 全屏视频高度仍写 100vh，键盘弹起或手势条下偏高



* 全局 `.app` 已在 `common.css:93-96` 做 `100vh → 100dvh` 渐进增强（文档③ U4 已落地），但页面级还有残留：


  * `ai-settings.html:363` 排序弹层 `max-height:calc(100vh - 48px)`；

  * `mock_exam_run.html:141` 面板 `max-height:calc(100vh - var(--xt-space9,16px) - 100px)`；

  * `申论刷题.html:43` 材料区 `max-height:calc(100vh - 270px)`、`:76` 答案区 `calc(100vh - 300px)`；

  * `面测.html:299` AI 面板 `#gdAiPane{height:calc(100vh - 260px)}`；

  * `AI.html:249` 全屏视频 `height:100vh`。

* 现象：软键盘弹起 / 手势条占位时，这些容器按「整屏高度」计算，底部按钮或内容会被压、超出部分够不到。

* 改法：沿用 U4 的写法做渐进兜底 —— 先写 `100vh`，紧跟一条用 `100dvh` 覆盖（例如 `max-height:calc(100vh - 270px); @supports(height:100dvh){ max-height:calc(100dvh - 270px);}`），不支持 dvh 的旧机自动回退。

* 备注：`AI模拟面试.html:15`、`PPT素材库.html:12`、`四级经验分享.html:12`、`好友申请.html:12` 的 `body{min-height:100vh}` 是「最小高度」，内容超出会自动增高，**基本无害，可不改**。

* 验收：面测 AI 面板、申论材料 / 答案区、AI 设置排序弹层、模考面板在键盘弹起时底部操作区仍可见可点；全屏视频在手势条机型不被横条挡关键内容。

### C6　z-index 层级各页各定，存在「提示被遮罩盖住 / 谁盖谁」隐患



* 现状（扫描）：私聊最高 `10001`、实时位置 `9999`、好友申请 toast `9999`、申论遮罩 `2000`、内联 toast `2000`、AI 遮罩 `250/300`、AI 设置遮罩 `900`、AI 设置 toast `400`、通用弹层几十到几百不等。

* 已发现的实际冲突：`ai-settings` 遮罩 `z-index:900`，而该页 `#setToast` 只有 `400` → 弹窗里的提示被遮罩盖住（与 C2 同源）。

* 改法：在 `common.css :root` 定义一套分层令牌，各页引用、不再随手写数字，建议：



```
\--z-sticky:10;        /\* 吸顶 \*/

\--z-nav:100;          /\* 底部导航 \*/

\--z-fab:200;          /\* 悬浮球 \*/

\--z-drawer:900;       /\* 抽屉/更多面板 \*/

\--z-modal-mask:1000;  /\* 弹窗遮罩 \*/

\--z-modal:1001;       /\* 弹窗本体 \*/

\--z-toast:2000;       /\* 轻提示（永远在最上） \*/

\--z-pop:3000;         /\* 极少数引导/系统级 \*/
```

全屏独立页（私聊 IM、实时位置）可在本页内整体抬高，但页内相对顺序仍遵循上面这套。



* 验收：任意页面，遮罩一定盖住页面内容、弹窗一定在遮罩之上、toast 永远在最上层；不再出现提示 / 下拉被遮罩盖住。

### C7　数据列表缺统一的「加载中 / 空数据 / 加载失败（可重试）」三态



* 做得好的：`好友申请.html:64/137`（加载中…/ 暂无收到的好友申请）、`我的文件.html:162/338/761/799`（加载中 / 暂无图片 / 暂无内容）、`设置.html:775/1910/1992`（加载中 / 暂无法读取隐私设置）等，已有加载态与空态。

* 不足：多数列表的**失败态只弹一个 toast，列表区永久停在「加载中…」**，弱网 / 接口报错时用户面对转圈不知怎么办，也没有「点击重试」。涉及动态渲染的列表页：错题本、行测刷题、面试题库、万能金句库、PPT 素材库、四级词汇、管理员、面测、表达、好友申请、我的文件、社区 / 动态等。

* 改法：抽一个公共列表状态组件 / 约定：请求中显示骨架或「加载中…」；成功但无数据显示空态插画 + 文案；失败显示「加载失败，点击重试」并绑定重新加载；三态都要在**弱网、断网、后端 500、未登录**下验证。

* 验收：断网 / 停后端打开任一列表页，能看到失败态且点重试可恢复（不是一直转圈）；新账号无数据时显示友好空态。

### C8　进页面 / 切 tab 的一瞬间，图标先显示成一排浅色空方块，再变成图标（图标闪烁 FOUC）

* 现象（真机和电脑浏览器都能复现）：点击底部「我的」进入 `个人中心.html` 的一瞬间，「个人资料 / 发贴统计 / 动态空间 / 学习偏好 / 我的文件 / 数据管理」六个入口左侧**先出现一排浅蓝空方块**，片刻后才变成对应图标，肉眼可见一闪；底部「我的动态」等异步加载的卡片同样先空块。个人中心一屏多个大图标方块最显眼，**全站任何带图标的独立页首次加载都有，只是小图标处不明显**。

* 根因（已读源码定位）：
  * 图标不是写死在 HTML 里的，静态元素只写了空壳，例如 `个人中心.html:163` `<div class="sgc-icon" data-icon="user"></div>`，由 `assets/icon-map.js` 的 `autoRender()` 扫描所有 `[data-icon]` 并用 `innerHTML` 注入 SVG（`icon-map.js:821-831`，选择器是 `[data-icon]`）。
  * 空壳在 CSS 里本身就有固定尺寸 + 浅色底：`common.css:2458` `.sgc-icon{width:36px;height:36px;border-radius:10px;background:rgba(91,141,239,.12)…}`；同类带底图标壳还有 `.title-icon / .bm-icon / .adm-ov-icon` 等。
  * `icon-map.js` 以 `defer` 引入（`个人中心.html:519`），在文档解析完之后才执行；浏览器在它执行前已经按 HTML+CSS 画了首帧，于是**先画出「带浅底色的空壳」，JS 注入 SVG 后才变成图标**。底部 tab 是跳到独立页、整页重新加载，所以每次点「我的」都会重放一次。
  * 说明：icon-map.js 体积很小（不在 JS 体积前 18 名，小于 42KB），且 `readyState` 非 loading 时会立即渲染（`icon-map.js:836-843`），**不是文件加载慢，而是「首帧绘制早于图标注入 + 空壳带醒目底色」的时序问题**。

* 改法（推荐 1+2，改动集中在 icon-map.js 和一份公共 CSS）：
  1. **防闪烁（首选）**：图标注入完成前不显示空壳。`icon-map.js` 在首次 `autoRender()` 跑完后给 `document.documentElement` 加就绪 class（如 `icons-ready`）；公共 CSS 默认 `html:not(.icons-ready) [data-icon]{visibility:hidden}`（元素仍占着原来的尺寸，不会引起布局跳动），就绪后一并显现。之后动态插入的图标因 html 已有该 class 不受影响，仍走现有的手动 `lucideAutoRender()`，不会被永久隐藏。
  2. **缩短空窗**：把各页 `<script src="assets/icon-map.js" defer>` 提到 `<head>` 并排在所有 defer 脚本最前（defer 不阻塞解析，能更早开始下载、解析完第一时间执行）。
  3. **可选增强**：`autoRender()` 加一个 `MutationObserver`，自动渲染后续插入 DOM 的 `[data-icon]` 空节点，省掉全站几十处手动 `lucideAutoRender()`；并给已注入 SVG 的节点打标记（如 `data-icon-done`）跳过重复注入，避免重复 `innerHTML`（也顺带缓解第五批 I2「搜索每敲一个字就全量重绘图标」）。
  4. **兜底**：`autoRender()` 遇到 `LUCIDE_ICONS` 里不存在的图标名时 `console.warn` 一次，避免某个图标静默缺失、只剩空块。

* 验收：低端机冷启动进入「我的 / 首页 / 各模块」，不再出现一排浅色空方块闪烁，图标与文字基本同时出现且无布局位移；动态加载出来的卡片图标正常；就绪 class 加上后所有图标都能显现，没有图标被一直隐藏。



***

## 三、已核查「无问题 / 已实现」，请勿重复劳动

以下维度本批已逐页核对通过，**不需要再改**（列出以免重复排查）：



1. **资源引用**：52 个页面引用的外部 JS/CSS 全部存在，无 404、无错误路径。

2. **失效按钮**：除 P2-1 的 `gotoBlogMine` 外，所有静态 `onclick` 调用的函数都能在该页引入的脚本中找到定义；`openQuest`（blog\_wechat / 更新 / 演示三处）均带 `if(window.openQuest)` 兜底，不是死按钮。

3. **视口与安全区**：全部页面都有 `viewport-fit=cover`；公共底栏 `.bottom-nav`、内容区留白、AI 悬浮球 / 面板、IM 输入区都已处理 `safe-area-inset`；顶部 `.xt-page-head`/ 外壳顶栏已处理顶部安全区。

4. **双头部**：未发现「外壳顶栏 + xt-page-head」双层顶栏并存（聊天页的双层头是特例，已在文档①处理）。

5. **高度**：`.app` 已做 100dvh 渐进增强（U4 已落地）。

6. **表单**：所有 `<form>` 内的按钮都显式 `type="button"`，不存在误触提交 / 刷新页面。

7. **固定超宽元素**：未发现写死 ≥520px 导致窄屏横向溢出的元素（768/560/640 等均为媒体查询断点或弹窗 max-width）。

8. **图片**：图片基本都有 alt；列表图片无滥用、不存在单页 >5 张图缺懒加载。

9. **响应式栅格**：首页模块、个人中心、我的文件等宫格 / 卡片均用 `repeat(auto-fill,minmax(…,1fr))` + 560/768px 断点降级，窄屏会自动换列 / 单列，适配良好；登录页对 ≤360px 也做了验证码按钮换行处理（`登录.html:134`）。

10. **弹窗滚动**：通用 modal 已有 `body.modal-lock` 锁背景滚动（common.css:2860-2862）。

11. **U9 长按引用已完整实现**：经核对，前端 `chat-local.js`（引用草稿 323、菜单「引用」846-847、气泡引用块、点击跳转高亮、发送携带 reply）、样式 `common.css .im-reply/.im-quotebar`（3035、3042）、后端 `database.py`（messages 表 reply 四列 228-231 + 守卫式迁移 577-588）、`chat.py`（resolve\_reply 107、私聊落库 242-246）、`groups.py`（群聊 390-393）以及两个测试 `server/_r3bC_mig_test.py / _r3bC_e2e_test.py` 都已在仓库。**该项不是待开发，只需真机走查验收**（私聊 + 群聊：长按消息→引用→输入框上方出现引用条→发送后气泡显示引用块→点引用块跳到原消息并高亮；撤回 / 删除被引用消息后显示兜底文案）。



***

## 四、建议真机回归清单（修完后按页过一遍）



1. **手势条全面屏**：AI 设置底部说明条、模考答题卡把手、赞助大图底部按钮不贴边 / 不被压（P2-2）。

2. **死按钮**：更新页 → 更多面板 →「我的发贴」可跳转（P2-1）。

3. **返回键**：通用弹窗、更多 / 工具面板、AI 抽屉、大图、申论 / 时政 overlay 打开时，返回键逐层关闭而非退页面（P2-3）。

4. **键盘弹起**：登录、设置、个人资料、意见反馈、朋友圈发布、群公告、面测 AI 面板、申论材料区 —— 输入框与提交 / 发送按钮不被键盘遮挡，页面可滚到对应位置（结合 C5）。

5. **窄屏 320～360px**：各宫格不挤压、长昵称 / 长标题省略号不溢出、按钮组正常换行（已基本自适应，抽查首页 / 更多 / 个人中心 / 我的文件 / 工具即可）。

6. **弱网 / 断网**：各数据列表三态正确、失败可重试（C7）；弹窗里的 toast 显示在遮罩之上（C2/C6）。

7. **按压反馈**：卡片 / 宫格 / 列表项 / 选项按下有反馈（C1）；图标按钮好点（C4）。

8. **图标防闪烁**：低端机冷启动进入「我的 / 首页 / 各模块」，图标与文字同时呈现、无浅色空方块一闪、无布局位移（C8）。

9. **U9 引用**：私聊 + 群聊完整走查（见第三章第 11 条）。



***

## 五、实施顺序建议



* **第一批（真机硬伤，改动小）**：P2-1 死按钮 → P2-2 三处底部安全区。

* **第二批（交互一致性，需要抽公共能力）**：P2-3 弹层返回键统一压栈（顺带统一点遮罩 /× 关闭）→ C2 toast 收口 → C6 z-index 令牌（这两项建议一起做，顺手解决「提示被遮罩盖」）。

* **第三批（视觉规范，批量打磨）**：C8 图标防闪烁（建议优先，改动集中、首屏观感提升明显）→ C1 按压反馈 → C4 热区 → C3 字号 → C5 dvh 兜底 → C7 列表三态。

* 全程不涉及上架合规，均按自用 + 朋友测试的标准即可。