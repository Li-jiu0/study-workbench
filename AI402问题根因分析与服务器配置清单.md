# AI 402 欠费问题根因分析与服务器配置清单

> 日期：2026-09-18
> 问题：无论选哪个模型，AI对话都返回402"余额不足"

---

## 一、问题现象

用户在"问AI"页面选择任意模型发送消息，都返回：
```
⚠️ 模型服务返回 402：{"code":30001,"message":"Sorry, your account balance is insufficient","data":null}
```

错误码30001是**硅基流动（SiliconFlow）**的欠费特征。

---

## 二、根因分析

### 2.1 项目存在两条独立的 AI 调用链路

| 链路 | 用的配置文件 | Key来源 | 用在哪些功能 |
|------|------------|---------|------------|
| **前端直连** | `assets/ai-config.js` | 前端内置Key | 设置页"测试"按钮、错题本AI、部分页面AI功能 |
| **服务器中转** | `server/.env` | 服务器环境变量 | "问AI"页面对话 |

### 2.2 为什么"选哪个模型都报硅基欠费"

服务器 `server/.env` 里的Key配置情况：

| 平台 | 服务器 .env 状态 |
|------|----------------|
| 硅基流动 | ✅ 已配置（但账户欠费） |
| 火山方舟 | ❌ 空 |
| 智谱AI | ❌ 空 |
| 百度千帆 | ❌ 空 |
| OpenRouter | ❌ 空 |
| Gemini | ❌ 空 |
| DeepSeek/通义/Kimi/OpenAI | ❌ 空 |

**服务器 .env 里只有硅基流动的Key，其他平台全是空的。** 所以"问AI"页面不管前端选什么模型，请求走到服务器中转后，最终都只能打到硅基流动 → 硅基账户欠费 → 全部返回402。

### 2.3 为什么设置页"测试连接"能通过

设置页的"检测"按钮走的是**前端直连链路**，用的是 `ai-config.js` 里的内置Key（火山方舟、智谱等都已验证可用），所以测试能通过。

但"问AI"页面的实际对话走的是**服务器中转链路**，用的是 `server/.env` 里的Key，两套Key是独立的，前端通不代表服务器那把钥匙有钱。

---

## 三、已完成的修复

`server/routers/ai.py` 已增加错误码友好化映射：
- 402 → "该模型平台账户余额不足，请充值后重试，或切换其他模型/平台"
- 401 → "API Key无效，请检查配置"
- 404 → "模型不存在，请检查模型ID"
- 429 → "请求过于频繁，请稍后再试"
- 5xx → "服务器异常，请稍后重试"

用户不再看到裸JSON报错。

---

## 四、需要做的配置（核心）

把 `ai-config.js` 里已验证可用的平台Key，同步配置到服务器 `server/.env` 中。

### 4.1 必须配置的平台（国内可直连、免费可用）

| 平台 | .env 变量名（参考） | Key 值 | 说明 |
|------|-------------------|--------|------|
| **火山方舟** | `ARK_API_KEY` | `ark-e725e1de-7d62-4b4a-aebb-a5def4f05ba7-c22bf` | 主力，18个文本模型+3个图片模型，每天200万token免费 |
| **智谱AI** | `ZHIPU_API_KEY` | `339ab396568541d0b7c0be4a577e5e53.VM5HxadQcgew1JdB` | 2个免费模型（glm-4.7文本、glm-4v-flash视觉） |
| **百度千帆** | `QIANFAN_API_KEY` | `bce-v3/ALTAK-bddJz30wA3jwpUXr3yDbZ/be743c4cbfafd762fa42c4fc10b8308d574c7a5b` | 2个免费模型（ernie-4.5-turbo 32k/128k） |

### 4.2 可选配置的平台（需梯子/代理）

| 平台 | .env 变量名（参考） | Key 值 | 说明 |
|------|-------------------|--------|------|
| OpenRouter | `OPENROUTER_API_KEY` | `***REMOVED-BY-R2C***` | 3个免费模型，需梯子，有CORS问题 |
| Gemini | `GEMINI_API_KEY` | `***REMOVED-BY-R2C***` | 2个免费模型，需梯子 |

### 4.3 暂时不配置的平台

| 平台 | 原因 |
|------|------|
| 硅基流动 | 账户欠费（402），等充值或换新Key后再配 |

---

## 五、服务器 .env 配置示例

```env
# 火山方舟（主力，必须配）
ARK_API_KEY=ark-e725e1de-7d62-4b4a-aebb-a5def4f05ba7-c22bf
ARK_API_URL=https://ark.cn-beijing.volces.com/api/v3/chat/completions

# 智谱AI
ZHIPU_API_KEY=339ab396568541d0b7c0be4a577e5e53.VM5HxadQcgew1JdB
ZHIPU_API_URL=https://open.bigmodel.cn/api/paas/v4/chat/completions

# 百度千帆
QIANFAN_API_KEY=bce-v3/ALTAK-bddJz30wA3jwpUXr3yDbZ/be743c4cbfafd762fa42c4fc10b8308d574c7a5b
QIANFAN_API_URL=https://qianfan.baidubce.com/v2/chat/completions

# OpenRouter（可选，需梯子）
# OPENROUTER_API_KEY=***REMOVED-BY-R2C***
# OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions

# Gemini（可选，需梯子）
# GEMINI_API_KEY=***REMOVED-BY-R2C***
# GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent

# 硅基流动（欠费，不要配置，等充值或换新Key后再启用）
# SILICONFLOW_API_KEY=（欠费已停用，请勿配置）
# SILICONFLOW_API_URL=https://api.siliconflow.cn/v1/chat/completions
```

---

## 六、配置后验证

1. 配置完成后重启服务器
2. 进入"问AI"页面，选择火山方舟的模型（如 DeepSeek-V4-Flash）发送消息
3. 应该能正常回复，不再报402
4. 依次测试智谱、百度千帆的模型，确认都能正常使用
5. 服务器日志确认请求打到了对应平台，而不是硅基

---

## 七、后续注意事项

1. **前端直连和服务器中转是两套独立配置**，以后新增平台或换Key时，两边都要同步更新
2. 硅基流动充值或换新Key后，再取消 `.env` 里的注释启用
3. OpenRouter 和 Gemini 需梯子，服务器如果在国内且没有代理，先不配，避免调用超时
4. 建议在服务器端也做模型可用性检测，欠费/不可用的平台自动从下拉列表中隐藏
5. 建议服务器端增加降级逻辑：当前选中的平台调用失败时，自动降级到其他可用平台
