# 万能金句库升级需求（批次九 · N9-24，2026-09-15 · 豆包方案 A+B+C）

> 登记来源：用户转发**豆包**两份整改建议（① 万能金句库升级：加功能+扩内容；② 获取网络金句功能），要求「记录一下」。
> 本文 = 豆包方案原文登记 + 主理人只读落点核实 / **重大差异注记**。**未开工**——用户铁律「不说开工不要自己执行」。
> 改造对象：页面 `万能金句库.html`（L92–96）+ 共享数据/逻辑 `assets/app.js`（`COMM_QUOTES` L4359 / `renderQuotes` L5493 / `renderQuoteCard` L5532 / `filterQuotes` L5485）。

---

## 0. 一句话定位

给「万能金句库」三件事：① 让用户自己加金句（遮罩表单 + localStorage + 可删可改）；② 内置金句从 18 句扩到 80+ 句；③ 加「🌐 网络金句」按钮，调一言 API 一键收藏。

---

## 1. 功能A · 用户自己添加金句（豆包方案原文）

- **入口**：`万能金句库.html` 标题行右侧（`高情商万能金句库` L95 旁、`18句直接套用` L96 同行）加橙色按钮 `+ 添加金句`。
- **表单**：遮罩层（**非 prompt**，符合 ADR-3），字段：金句内容*(必)* / 分类*(必,下拉)* / 使用场景 / 为什么有效 / 示例对话。
- **存储**：`localStorage` key `custom_quotes`，结构：`{ id:'custom_1', category, quote, scene, why, example, isCustom:true }`；保存后与内置合并，对应分类显示。
- **删除**：用户金句卡片多「🗑 删除」按钮 → 确认后从 localStorage 移除（内置无删除，仅「移除」当日，见 L5537 `removeBtn`）。
- **编辑（豆包方案第三步补充）**：用户金句「编辑」按钮 → 同表单预填 → 保存覆盖。

---

## 2. 功能B · 扩充金句库 18→80+ 句（豆包方案原文 + 实测差异❗）

### 2.1 ✅ 决策定稿（2026-09-15 19:27 主理人拍板）：**沿用实际 10 分类，不删已上线内容**
- **实际 `COMM_QUOTES`（L4359–4378，共 18 条，10 个分类）**：
  `拒绝`(2) / `赞美`(2) / `道歉`(2) / `表达不同意见`(2) / `催进度`(2) / `安慰人`(2) / `向上沟通`(2) / `被甩锅`(1) / `i人专属`(2) / `万能开场`(1)
- **豆包假设的 8 分类**：拒绝/赞美/道歉/**不同意见**/催进度/安慰人/向上沟通/i人专属。

**决策 = 沿用 10 分类（方案 A）**，四条落地要求：
1. **一个分类不删**：保留 `被甩锅`、`万能开场`（它们本身是有独立价值的职场场景）。
2. **标签重映射**：豆包 68 句里的「不同意见」→ 全部改用实际分类名「**表达不同意见**」。
3. **补齐两类短板**：`被甩锅` +7 句、`万能开场` +7 句 → 各达 8 句（内容在实施阶段撰写，须与内置条目质量一致：含 `scene`/`why`/`example`）。
4. **最终总量 18 → 100 句**（现有 18 + 豆包 68 + 本决策新增 14），10 个分类全部 ≥8 句。

**为什么不砍成 8 分类**：① 删分类等于删掉已上线内容，并破坏真实用户数据（`appData.viewedContent` / `dailyQuotes` 存的是内置条目 id，删 litres 会让已学进度失效）；② 「做减法」减的是冗余入口，不是已有数据资产；③ 补 14 句的成本远低于回退功能的代价。

### 2.2 豆包给出的 68 句（按分类，原文待加数据）
**拒绝（+10）**：①这个需求我理解，但当前排期确实满了，我们看看能不能调整优先级？②谢谢你想到我，不过这个领域我不太擅长，怕做不好耽误你，我推荐XX给你？③这个时间点我手头有个紧急任务，能不能改到周四？④我理解你的着急，但这个事需要XX部门审批，我先帮你问一下流程，好吗？⑤这个方案我觉得方向可以，但细节上我有不同看法，我们聊聊？⑥不好意思，这个忙我可能帮不上，我对这块不熟，怕给你帮倒忙。⑦这个我真的来不了，已经有安排了，下次一定！⑧我理解你希望今天完成，但质量和速度只能保一个，你看优先哪个？⑨这个请求我需要和领导确认一下，确认后给你答复，可以吗？⑩谢谢你的信任，但这个超出了我的能力范围，我不想随便应付你。
**赞美（+10）**：①你这个思路很清晰，尤其是第二点，我之前都没想到。②这个方案做得很细致，一看就是花了心思的，辛苦了！③你今天这个发言很有条理，逻辑特别清楚，学到了。④这个数据整理得太清楚了，省了我好多时间，感谢！⑤你总是能注意到别人忽略的细节，这个能力太厉害了。⑥跟你合作特别省心，你总能提前想到下一步。⑦你这个PPT做得真好看，配色和排版都很舒服。⑧你刚才那个问题问得特别好，一下就点到关键了。⑨你处理这个问题的方式很成熟，既解决了问题又没得罪人。⑩这个项目能这么顺利，你功不可没，真的。
**道歉（+10）**：①这个是我的问题，我马上改，半小时内给你新的版本。②不好意思，我刚才没说清楚，重新跟你解释一下。③这个失误是我造成的，给你添麻烦了，我来承担后果。④抱歉让你等这么久，我这边刚处理完，现在马上弄你的。⑤我刚才语气不太好，不是针对你，是我自己着急了，对不起。⑥这个数据我搞错了，已经更正了，感谢你指出来。⑦是我考虑不周，没考虑到这个情况，下次我会注意。⑧对不起，我忘记回复你了，这个事现在什么情况？我来跟进。⑨这个锅我背，是我没交代清楚，不怪执行的人。⑩抱歉打扰你了，这个事我自己再想想办法。
**不同意见/表达不同意见（+10）**：①我理解你的想法，不过我有个不同角度，我们可以讨论一下。②这个方向我同意，但具体执行上我有点担心，我们聊聊风险？③你说的有道理，不过我之前试过类似的，效果不太好，原因是…④我补充一个视角，可能不一定对，供你参考。⑤这个方案整体不错，只有一个地方我觉得可以再优化一下。⑥我明白你的意思，但从用户角度看，可能会有另一种反应。⑦我们先别急着定，我提个反方观点，大家看看有没有道理。⑧这个数据我看了，结论可能和你不太一样，我们一起对一下？⑨我不是反对，只是觉得时机可能不太对，我们再等等？⑩你这个思路很新颖，不过落地的时候可能会遇到XX问题，我们提前想想怎么解。
**催进度（+8）**：①这个事现在到哪一步了？有什么我能帮忙的吗？②客户那边在催了，我们今天能不能先给个初稿？③这个项目截止时间快到了，我们看看哪里还能提速。④上次说的那个事，现在进展怎么样？需要我协调什么吗？⑤这个比较急，能不能今天下班前给我？辛苦你了。⑥我这边后续工作都在等这个，方便的话优先处理一下？⑦这个事拖了几天了，我们定个明确的时间点吧，什么时候能好？⑧不是催你，就是想确认一下进度，好安排后面的事。
**安慰人（+8）**：①已经做得很好了，这种情况换谁都难免出错，别太自责。②没事，问题已经发生了，我们一起想办法解决，不是你一个人的事。③我理解你现在的感受，换我我也会难过，慢慢来。④你已经尽力了，结果不是你一个人能决定的，别把责任都揽自己身上。⑤先别想那么多，休息一下，明天再说，天塌不下来。⑥这种事谁遇到都会郁闷，但你已经处理得很好了，真的。⑦别给自己太大压力，大不了重来，没什么大不了的。⑧我在呢，有什么想吐槽的随时说，我听着。
**向上沟通（+8）**：①领导，这个事我想跟您汇报一下进展，您现在方便吗？②这个方案我做了两版，各有优劣，想请您帮我定个方向。③领导，我遇到一个问题，想了两个解决方案，您看哪个更合适？④这个目标我评估了一下，可能有点挑战，我需要XX资源支持，您看可以吗？⑤领导，上次您说的那个事，我按您的意思做了，这是结果，您看看。⑥这个决定我拿不准，想听听您的意见，您觉得应该怎么处理？⑦领导，我想申请一下，这个项目能不能多给两天时间？原因是…⑧感谢领导指点，我明白了，马上按您说的调整。
**i人专属（+6）**：①我在微信上跟你说吧，现在有点不太方便说话。②这个事我想想再回复你，可以吗？③谢谢邀请，我今天有点累，就不去了，你们玩得开心。④我先观察一下，等熟悉了再参与，可以吗？⑤这个会我就不发言了，我在旁边听着学习。⑥不好意思，我社恐，这个场合我可能不太适应，先撤了。

> ⚠️ 上述 68 句用的是豆包分类标签（如「不同意见」），落地时按 2.1 决策重映射到实际分类名；每句需补 `icon`(emoji，参考内置) + 数字 `id`（接续 19 起）以匹配实际结构（见第 4 节）。

---

## 3. 功能C · 获取网络金句（豆包方案第二步原文）

- **入口**：标题行「+ 添加金句」旁加 `🌐 网络金句` 按钮。
- **接口**：`https://v1.hitokoto.cn/?c=d`（免费、无需密钥、CORS 支持；`c=d`=来自网络分类）。
- **交互**：点→按钮「加载中…」置灰→`fetch` 取一句→弹窗显示「句子 + 出处」+ 三按钮（换一句 / 收藏到我的金句库 / 关闭）。
- **收藏**：存 `custom_quotes`，`category:'来自网络'`（新增分类），`isNetwork:true`，与用户金句合并显示。
- **容错**：`catch`→`showToast('获取失败，请检查网络')`；「换一句」加防抖（1 秒 1 次）。
- 豆包示例代码用了 `async/await` + `fetch` + `localStorage` —— ❗ 老 WebView 见第 4 节风险。

---

## 4. ❗ 落点核实 + 重大差异注记（主理人只读核实，开工前必读）

**已核实落点（Grep + Read `assets/app.js`）**：
- 数据 `COMM_QUOTES`：L4359–4378（18 条，结构 `{id:number, category, icon:emoji, quote, scene, example, why}`）。
- 渲染 `renderQuotes()`：L5493；卡片 `renderQuoteCard(q, viewed, isToday)`：L5532（用 `commIconHtml(q.icon,20)` L5542、`q.category`/`q.quote`/`q.scene`/`q.why`/`q.example`）。
- 过滤 `filterQuotes(cat)`：L5485；状态 `quotesFiltered`/`quotesCat`：L5482；合并点 `catFiltered = quotesCat==='全部'?COMM_QUOTES:COMM_QUOTES.filter(...)`：L5497。
- 队列 `getDailyQueue('commQuotes', catFiltered)`：L5498（若上游合并 custom，队列自动含用户句，满足「今日待学含用户加的」）。
- 页面静态 tag `18句直接套用`：`万能金句库.html` L96（**硬编码，须改**）；动态「剩余」计数在 renderQuotes 内用 `COMM_QUOTES.length` 自动更新（L5502）。
- 已有「移除」按钮（仅今日未学，`removeBtn` L5537）= 豆包说的「从今日学习移除」，与「删除(永久)」不同，新增删除按钮须区分。

**❗ 差异/风险（开工前定）**：
1. **共享文件高风险**：`COMM_QUOTES` + 渲染逻辑全在 `assets/app.js`（8761 行、31 页共用）。改数据 + 改 `renderQuotes`/`renderQuoteCard` 增 custom 合并/删除/编辑 + 新增表单 HTML/CSS + hitokoto fetch，**全部落在共享文件** → 改完必 bump 全站 `app.js` 版本戳（6.4）+ WebView 语法合规 + `node --check`。
2. **字段不符**：内置每条有 `icon`(emoji) + 数字 `id`；豆包 custom 结构缺 `icon` → `commIconHtml(q.icon)` 对自定义句会空/报错，须补 `icon` 默认值或函数容错。
3. **分类名不符**：见 2.1（不同意见 vs 表达不同意见；被甩锅/万能开场 未覆盖）。
4. **「来自网络」新分类**：功能C 收藏用 `category:'来自网络'`，但现有 filter tab 由分类生成（L5487 按 `dataset.cat`），须把「来自网络」纳入分类集合（custom 合并时动态补类别）。
5. **✅ 决策定稿（19:27）：`XMLHttpRequest` 单路径，不依赖 fetch / async-await**
   - 老 Android WebView（API<21）**无 `fetch`**，且部分 WebView 的 fetch + CORS 行为异常 → 直接**统一用 `XMLHttpRequest` 实现**，回调式写法，`async/await` 虽未被禁但一并回避，最大化兼容。
   - 必须带三层容错：`try/catch` + `onerror` + `ontimeout`（建议 8s）→ 统一 `showToast('获取失败，请检查网络')`，**页面绝不崩溃**。
   - 「换一句」必须加 **1 秒防抖**（点击后置灰），避免疯狂请求。
   - **不走后端代理 / 不加 SSR**：保持纯前端、零后端改动（豆包原方案即是如此，保留）。
   - **遗留验证项（纳入 L3 真机验收）**：APK 若以 `file://` 加载页面，跨域请求在 null origin 下需真机确认 `Access-Control-Allow-Origin:*` 是否放行；若 file:// 下失败 → 降级为「仅 http(s) 站点可用，APK 内隐藏该按钮」。
6. **CORS/离线**：hitokoto 支持 CORS，但老 WebView fetch CORS 不稳；离线必须 catch→toast，不崩页（豆包已写，保留）。
7. **静态 tag 更新**：`万能金句库.html` L96「18句直接套用」→ 改动态或「86句直接套用」；`高情商表达.html` L143「18句万能金句」描述可一并更新（非必）。

---

## 5. 实现步骤（合并豆包 A/B/C，顺序可独立验证）

1. **A-表单**：`万能金句库.html` 加「+ 添加金句」按钮 + 遮罩表单（HTML/CSS，非 prompt）；`assets/app.js` 加 `openAddQuote()/saveCustomQuote()/loadCustomQuotes()`；合并点 L5497 改 `COMM_QUOTES.concat(loadCustomQuotes())`；卡片 `renderQuoteCard` 对 `isCustom` 加「删除」「编辑」按钮。
2. **A-删除/编辑**：`deleteCustomQuote(id)`（localStorage 移除 + `renderQuotes()`）；`editCustomQuote(id)`（预填表单）。
3. **B-数据**：按 2.1 决策把 68 句（补 `icon`+`id`+正确 `category`）并入 `COMM_QUOTES`（L4378 前插入）；更新 L96 静态 tag。
4. **C-网络金句**：加「🌐 网络金句」按钮 + `fetchNetworkQuote()`（带 XHR 兜底）+ 展示弹窗（换一句/收藏/关闭）+ 防抖 + 收藏存 `custom_quotes`(`category:'来自网络'`)。

**验收（合并豆包）**：① 有「+ 添加金句」按钮；② 弹窗表单 5 字段（非 prompt）；③ 保存刷新不丢；④ 用户句在对应分类显示+有删除；⑤ 总数≥80；⑥ 每分类≥8（被甩锅/万能开场见 2.1）；⑦ 句数/剩余同步；⑧ 今日待学含用户句；⑨ 有「🌐 网络金句」；⑩ 有网可取、无网 toast 不崩、换一句有防抖。

---

## 6. 不做的（豆包砍掉 / 默认）

- 不做 AI 生成金句；不做金句社区/分享；不做答题式练习（仅「标记已看/移除」现有机制保留）。

---

*文档结束。豆包 A+B+C 三方案已登记；落点经只读核实，发现 7 处差异/风险（共享文件高风险、字段 icon/id、分类名不符、来自网络新分类、老 WebView 无 fetch、CORS、静态 tag），开工前须先按第 2.1 / 第 4 节决策对齐。待用户「开工」后推进。*
