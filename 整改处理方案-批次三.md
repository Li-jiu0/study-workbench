# 整改处理方案 · 批次三（豆包体验报告遗留项 1/3/4/5/6）

> 依据：2026-09-11 豆包全功能深度体验报告（综合 4.7 分）+ 同日代码核查结论。
> 本批范围 = 报告遗留问题中的第 1、3、4、5、6 项。第 2 项（HTTPS + 域名）需购置域名与证书、配置 Nginx，属运维动作不在代码整改范围，单独见文末。
> 执行约定（项目铁律）：扩展必三件套（DOM 容器 + CSS + 空函数标【后续扩展点】）；不写死、不删原功能、保留 DOM；改大文件先备份 + `node --check` + div 配平；完成后版本号统一 bump（建议 `20260911i`，用 tools/bump_versions_safe.py，只作用于真实 script/link 行）；验证一律用 tools/verifier jsdom（含"零未捕获异常 + showToast 可用"断言）。

---

## 一、问题 1：首页假数据（P0，信任底线）

### 现状（已核实）
- `assets/app.js:188-200` 仍是硬编码：
  - `moduleProgress: { cet:45, exam:30, comm:20, interview:15, ppt:10 }`
  - `weakPoints`：3 条（"数量关系-工程问题 正确率仅40%"等）
  - `recentLearning`：3 条（"图形推理-位置类 第3题"等）
- 渲染入口：`app.js:1112`（moduleProgressGrid）、`app.js:1136`（weakList）、最近学习卡片同页。
- 本周柱图已由 study-stats.js 真实数据驱动（app.js:185-187 注释），本次对齐同样思路。

### 方案：无真实记录一律空态，有记录按真实数据算
1. **各模块进度**：改为实时计算，不再读 moduleProgress 固定值：
   - 四级：`vocabLearned.length / 词库总数`；笔试：`examTypeProgress 总做题数` 映射到 0-100（如 min(100, 做题数/3)）；
   - 表达/面试/PPT：分别以话术已看数、面试完成场数、版式浏览数计算；
   - 某模块 0 记录 → 显示 0% 并附"未开始"灰态，**绝不编造**。
2. **薄弱点提醒**：由 `examTypeProgress` 派生——做过 ≥5 题且正确率 <60% 的题型才列出，最多 3 条；无达标项 → 空态。
3. **最近学习**：从错题本/收藏/词汇学习记录取最近 3 条真实行为（含时间）；无 → 空态。
4. **空态样式统一**：`<div class="empty-hint">暂无数据，去开始学习吧 →</div>`，点击跳对应模块；CSS 新增 `.empty-hint`（三件套）。
5. 旧字段 `moduleProgress/weakPoints/recentLearning` 保留在 appData（兼容旧存档），代码注释标明"已废弃，仅兼容旧档，不再渲染"。

### 验收
- 全新 localStorage 首页：进度全 0、薄弱点/最近学习为空态，页面无 "45%"/"正确率仅40%" 字样；
- jsdom 断言：无未捕获异常 + 空态节点存在 + 做题后薄弱点能出现真实条目。

---

## 二、问题 3：内容量不足（P0-P1，数据工程为主）

### 现状（已核实）
- 四级词汇 466 词（要求 4500+，目标先补到 2000 高频）；行测 60 题（目标先补到 300+）；听力仅 6 个日常对话场景。
- 行测部分图形推理题题干自带规律文字描述（泄题），需随数据清洗一并处理。

### 方案
1. **词库扩充**：
   - 数据源：四级高频词表（按词频排序 2000 词），字段沿用现有结构（word/meaning/detail/synonym/antonym 等，缺字段可空）；
   - 落地：新词以增量 JSON 打包，启动时与现有 466 词合并去重（键=word），不覆盖用户已学标记（vocabLearned 按 word 存，天然兼容）；
   - 数据文件放 assets/data/（新增目录，ASCII 文件名，中文内容 UTF-8）。
2. **行测题库扩充到 300+**：
   - 每题必含：type/sub/difficulty/options/answer/**explanation（解析）**/tip；
   - 题干清洗硬规则：图形推理题干**删除一切规律描述文字**，只保留图形（字符画或 SVG）与选项，规律只出现在 explanation；写校验脚本 `tools/qa/scan_question_leak.py`（正则扫描"依次/规律/顺时针在题干"）入库前把关；
   - 分布建议：图形推理/定义判断/类比推理/逻辑判断/言语理解/数量关系/资料分析 各 40-50 题。
3. **听力内容补题型**（与问题 6 的听力项合并执行）：
   - 新增"短篇新闻 / 长对话 / 篇章理解"三类，各 3-5 篇，沿用 voiceplayer.js 播放结构（文本次要，先保证题型名实相符）。

### 验收
- 词汇页显示总词数 ≥2000；行测中心 ≥300 题且每题有解析；scan_question_leak.py 零命中；
- 用户已学进度、错题本不受灌库影响（回归：老存档升级后 vocabLearned/wrongQuestions 不丢）。

---

## 三、问题 4：UI 去 emoji 化（P2，渐进式）

### 现状
模块图标、按钮、分类标签大量 emoji，跨系统渲染不一致、观感不专业。

### 方案（渐进三步，不搞一刀切）
1. 新建 `assets/icons.js`：`icon(name)` 返回内联 SVG 字符串（**不用 CDN**，符合全站无外链铁律）；首批图标 20 个以内（首页/统计/任务/笔记/博客/私信/好友/错题/词汇/听力/笔试/表达/面试/PPT/设置/工具/更多/返回/关闭/收藏）。首行注释标【后续扩展点：继续加图标】。
2. 第一批替换（收益最大）：底部导航 5 项、侧边栏分组图标、卡片标题 `.title-icon`、更多面板。替换用 `icon('xxx')` 注入，**原 DOM 结构与 class 保留**，仅改内部内容节点。
3. 第二批（可延后）：各模块页内的分类标签、按钮内 emoji。
- emoji 的功能型字符（✅/⏭ 等状态指示）暂保留，属内容不属于图标。

### 验收
- 底部导航/侧边栏/卡片标题无 emoji，SVG 在亮/暗主题下均可见（stroke 用 currentColor）；
- jsdom：icons.js 加载无异常、替换节点存在、navigateTo 正常。

---

## 四、问题 5：首页信息过载（P2）

### 现状
首页一屏 12 个区块（功能中心/倒计时/学习数据/本周时长/完成率/成就/任务/进度/薄弱点/最近学习/更多工具/留言板）。

### 方案：12 → 6，不删功能只收层级
1. **保留在首页（6）**：欢迎头 + 倒计时（合并为头部条）、今日目标+今日任务（合并一卡）、各模块进度（问题1真实化后）、继续学习（薄弱点+最近学习二合一卡）、在线互动入口、留言板。
2. **收进二级**：学习数据/本周时长/成就徽章 → 全部归入"学习统计"（study-stats 已有底座，首页只留一个入口 chip）；功能中心 → 保留但压缩成一行 6-8 个小图标入口，长尾入口收进"更多"。
3. "更多工具"卡维持已隐藏状态（DOM 保留，20260911 上午已处理）。
4. 布局规矩：模块页在 .content 内；改布局时确认 navigateTo() 空 .content 隐藏逻辑不被破坏；主题用全局 isDarkMode（防 body.className 重写清 dark 类）。

### 验收
- 首页可见主区块 ≤6；被收编的数据在"学习统计"内可达；
- jsdom：零未捕获异常、#countdownModal 等共用 DOM 完好、各入口 onclick 可达。

---

## 五、问题 6：真题模考名不副实 / 弹窗混乱 / 听力题型（三项合并）

### 6a. 真题模考
- 短期（本批）：入口文案改"考试指南"，弹窗标题同步，杜绝名不副实；
- 做实排期（下一数据批次）：至少 2 套完整真题（写作提纲+听力文本+阅读+翻译），复用 qbank/importer 机制 + 倒计时交卷判分，入口改回"真题模考"。

### 6b. 弹窗统一
- 新增公共类 `.app-modal`（三件套）：固定右上角 ✕、支持 ESC 关闭与点击遮罩关闭、打开时锁 body 滚动；
- 本批先改造 3 个重灾区：听力全屏播放器（加统一 ✕，替代"必须点 .vp-x"）、数量关系弹窗（修复空白 + 加载失败给 toast 而非白板）、各类型选择弹窗；
- 存量弹窗渐进迁移，已上线的 #countdownModal 结构不动。

### 6c. 听力"下一句"与题型
- "下一句"按钮已接线 `openVoiceTrain.__next()`（voiceplayer.js:103/109），本批回归确认即可；
- 内容题型匹配在问题 3 第 3 条执行。

### 验收
- 文案：全站搜"真题模考"仅在做实后出现；jsdom：ESC/遮罩关闭生效、数量关系弹窗无空白路径、零未捕获异常。

---

## 六、执行顺序与工作量预估

| 顺序 | 事项 | 性质 | 预估 |
|------|------|------|------|
| 1 | 问题1 首页真实化 | 纯代码 | 半天 |
| 2 | 问题6b 弹窗统一 + 6a 改名 | 纯代码 | 半天 |
| 3 | 问题4 图标库+第一批替换 | 纯代码 | 半天 |
| 4 | 问题5 首页压缩 | 纯代码 | 半天 |
| 5 | 问题3 数据灌库（词/题/听力）+ 题干清洗脚本 | 数据工程 | 1-2 天（可并行外包给数据脚本） |

总代码量约 2 个工作日；数据量是长尾，可先上代码空态、数据分批补。

## 七、上线检查单（沿用既有 SOP）
1. 版本号 bump → tools/bump_versions_safe.py；grep 损坏特征 `\.js\?v=...["][^>]`；
2. jsdom 全量回归（tools/verifier + tools/qa）+ node --check；
3. 前端页面 pscp 走 ASCII 中转 + 映射 JSON + urlopen 实读验证；涉及 server/ 的先做差异预检；
4. MD5 三重核验 → systemctl restart study-workbench → journalctl → 真实探针（临时账号用完清理，用户数基线=6）；
5. 改动落库 git 指定路径提交，默认不推 GitHub。

## 八、范围外：HTTPS + 域名（报告问题 2，需用户决策）
1. 购域名（国内需备案）→ 云解析到 110.42.134.62；
2. 签发证书（Let's Encrypt 免费即可）→ Nginx 配 443 + http 跳转；
3. 全站资源无硬编码 http:// 绝对地址才可切（config.js 的 file: 场景兜底地址保留，服务器地址改用相对/同源）；
4. APK 端 WebView 混合内容已允许（MixedContentMode），切换后需回归一遍 App。

---
*生成：2026-09-11 21:10 · 依据外部评测报告核查后编制，执行方：后续开发会话（当前 QA/部署收尾完成后接手）。*
