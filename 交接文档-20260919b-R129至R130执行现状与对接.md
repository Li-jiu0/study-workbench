# 交接文档 · R129–R130 执行现状与对接（2026-09-19b）

> 面向下一位接手者（人或 AI）。看完这页即可接手，无需回溯对话。

---

## 一、一句话现状

**R129 + R130 两个批次已全部上线（Web 全量 + Android APK 1.30），"仓库 == 线上"成立（两个本地提交 `2891e91`、`nd185665` 未 push）；唯一的高优先待办是 `assets/ai-config.js` 的明文 key 泄露风险。**

| 项 | 值 |
|---|---|
| 线上站点 | http://110.42.134.62 |
| 当前版本 | 1.30 / versionCode 31（上一版 1.29 / 30） |
| APK | `星途-1.30.apk` 87,083,351 B，md5 `cfe3addba01892b50b60730f79decf25` |
| 版本接口 | `/api/app/version` → `{version:"1.30", versionCode:31, apkReady:true}` |
| 本地提交（未 push） | `2891e91`（R130 收尾）、`nd185665`（R130b 增量） |
| 更新日志 | `更新日志-R129至R130-20260919.md`（同目录） |

---

## 二、🚨 安全待办（最高优先级，建议立刻处理）

**问题**：`assets/ai-config.js` 内含**明文 API key**（硅基流动）。该文件是**公开静态资源**：`curl http://110.42.134.62/assets/ai-config.js` 即可读到 → 与 2026-09-19 早先那次「额度被第三方盗刷」是同一泄密路径（历史记录见根目录 `_SECURITY_REPORT_20260919.md`、`_KEY_ROTATION_CHECKLIST.md`）。

**处置方案（两步，缺一不可）**：
1. **前端 AI 调用改走服务端中转**：后端 `/api/ai/chat` 已支持按 `model_registry.json` 的 `type` 分流（chat/embedding/rerank）与 `/v1/*` 端点派生，`.env` 已有 `SILICONFLOW_API_KEY`；把 `ai-cap-embed.js`（向量/重排，现为浏览器直连）、以及 ai-config 内置 key 的调用路径统一改成走中转，前端即不再需要任何 key。（注：此线用户曾于 22:00 前后口头取消，**因安全原因建议重启**。）
2. **轮换 key**：在硅基流动控制台重新生成，只写入生产 `server/.env`（用 `pscp` 追加，**绝不用本地覆盖**，两边内容不保证一致）；随后清理仓库与该前端文件中的旧 key。

**纪律（不可再犯）**：任何 provider key **只准进服务器 `.env`**，不准进前端、不准进 git。

---

## 三、代码树与生产环境事实

- **唯一可写代码树**：`D:\下载的文件\学习工作台`（分支 `main`）。
  ⚠️ 会话工作区 `C:\Users\ATM\WorkBuddy\Worktrees\学习工作台\main-aae0a44f` 是**停在旧提交的过期快照**（HEAD 停在 R102 收口），里面的同名源码比真源旧十几个提交，mtime 却很新 → **最容易被误当最新版**。任何 md5/grep/编辑只认真源绝对路径；判据 `git -C <dir> log --oneline -1`。
- **生产**：`110.42.134.62`
  - Nginx root：`/opt/study-workbench/web/`（前端资源在 **`/opt/study-workbench/web/assets/`**，不是 `/opt/study-workbench/assets/`，写错会得假失败）
  - 后端：`/opt/study-workbench/server/`；systemd 单元 `study-workbench`（uvicorn，**单进程**；改后端需 `systemctl restart study-workbench`）
  - `/api/` 反代 → `127.0.0.1:8000`；APK 静态目录 `/opt/study-workbench/web/static/apk/`
  - 备份目录：`/opt/study-workbench/backups/<批次>-<时间戳>/`
- **凭据取法**：从 `.gitignore` 忽略的 `upload_v23.ps1` 正则取（`pscp.exe" -pw "([^"]+)" ... root@([0-9.]+):`）→ **绝不 print 明文、绝不写进任何入库文件**；`tools/pscp.exe` / `tools/plink.exe` 写全路径；首次连主机需 `-hostkey SHA256:i2DNTi3iEPauuTxvJEPlZ4jLf8K0LdDsaijQxArUU1M`。
- **禁止上线**的运行时数据：`server/data/model_usage.json`、`model_quota.json`、`geo_place_count.json`、`geo_staticmap_count.json`、`feedback.json`。

---

## 四、架构红线（碰之前先读）

1. **站点是纯 HTTP，不上 HTTPS**（用户明确决策）。直接后果：浏览器安全策略恒拒 Web 端 geolocation → 「距你约 X 公里」、精确位置发送、实时共享等**只能在 App（`file://` 安全上下文）成立**，且**禁止用 IP 粗定位冒充**。
2. **App 是 `file://` 壳**：`android/java/com/study/workbench/MainActivity.java` 用 `web.loadUrl("file:///android_asset/学习工作台.html")`，页面与 assets **打进 APK** → **后端改动 App 用户立即生效；前端改动必须重打 APK 才能到达 App**。Web 部署对 App 无效。
3. **模型解析链**（决定"选哪个跑哪个"）：`body.modelId` → `resolve_model_name(provider, quota_key)` 查 `server/data/model_registry.json`（79 条，含前端 44 个 id 全映射、硅基流动 12 个免费模型）→ 失败用 `body.modelName` → 再失败回退 `.env` 默认模型（响应头 `X-Ai-Model-Fallback` 提示）。新增模型请补 registry，不要靠猜。
4. **腾讯地图配额红线**：`geocoder/v1` 6000/日（可自由用，含 `get_poi=1`）；`place/v1/*` **仅 200/日**，只允许走 `/api/geo/place` 代理且受 `GEO_PLACE_DAILY_CAP=150` 熔断（**`place/v1/explore` 全站禁调**）；`staticmap/v2` 约 6000/日，新增 `GEO_STATICMAP_DAILY_CAP`（缺省 3000）。Key 是 **IP 白名单制** → 本机直连腾讯恒返 `status=112`（不是配额问题），探测必须落服务器。前端**绝不直连** `apis.map.qq.com`。
5. **限流桶**：`rate_limit("geo")` = 30/分/IP **且缓存命中也计数** → 运营商 NAT 下易 429；**新业务不要挂这个桶**，用默认桶。
6. **CSS / 前端红线**：禁 `clamp()/min()/max()` **函数**（老 WebView），但 `min-height/max-height` 属性可用；`vh` 在 fixed 层安全；ES2017 上限（`node tools/qa/escheck_es2017.js` 必须 `DONE 0`）；禁 `prompt/alert/confirm`。
7. **编辑纪律**：改前二进制检测行尾并保持（多数 html 为 CRLF、`ai-page.js`/`xt-region.js` 为 LF）→ 改前备份 `.bak-pre-*` → 不做无关重排。**`?v=` 版本戳由 lead 统一 bump，且只前进不回落**（内容变了必须前进，否则老用户命中旧缓存 = 白改）。

---

## 五、部署 SOP 摘要（完整版见 skill `study-workbench-deploy`）

1. **差量 + 依赖闭包**：列出改动文件 + 其引用资源（如新增 `assets/leaflet/` 必须整目录带上）。
2. **过境**：`pscp` 上传（`.env` 只在服务器追加，绝不覆盖）。
3. **差异归零门禁**：**本地 md5 == 生产 md5 逐文件判**（不能只数总数、不能比 size；生产侧用 `plink md5sum` 拉清单）。
4. **重启与健康检查**（仅后端变更）：`systemctl is-active`、`/api/health`、`/api/ai/models`、目标新路由（如 `POST /api/ai/model3d/preview` 空体应得 422 而非 404）。
5. **页面抽验**：`curl` 公开 URL 检查新戳与新特性关键字命中。
6. **发版（含 APK）**：三处 bump（`server/routers/version.json`、`android/AndroidManifest.xml`、`assets/xt-update.js`）+ 打包 + aapt2 校验 + 上传 APK（md5 闸门通过后才删旧包）+ **最后**传 version.json + 轮询 `/api/app/version` 到 `apkReady=true`。**版本号与 APK 必须一起动**（只推版本号会让 App 提示更新却下载不到包）。
7. **收尾**：备份路径与逐文件 md5 写报告；本地 git 提交（**push 需用户单独拍板**）。

---

## 六、验证工具与口径

| 工具 | 用途 | 要点 |
|---|---|---|
| `tools/verifier`（jsdom） | 前端行为断言 | 必须 `runScripts:'outside-only'` 才能 `window.eval` |
| `tools/qa/escheck_es2017.js` | 语法红线 | 在工作树根跑，必须 `DONE 0` |
| Chromium headless（裸 CDP） | 真链路验证（真瓦片、真接口） | `agent-browser` 对**中文路径 `file://` 会丢页** → 用 ASCII 路径副本 |
| 服务器侧探针 | 腾讯接口/线上接口 | 必须落服务器（IP 白名单） |
| 独立 QA 线 | 复核实施方自证 | 实施方与复核方**文件零重叠**，复核方只读 |

---

## 七、本轮改动文件与 md5（关键 12 个）

| 文件 | md5 | 说明 |
|---|---|---|
| `assets/ai-page.js` | `dc7b7de4…` | 思维链自动开 + 3D 预览 + ASR 直发 + 头像归位 |
| `assets/ai-service.js` | `dc923b6e…` | reasoning 分帧透传（R107） |
| `AI.html` | （戳 ai-page.js?v=20260919q / icon-map.js?v=20260919l） | nginx no-cache 已生效 |
| `assets/chat-local.js` | `54a03cc4…` | 语音 SVG 图标 + IM 先发后转 + ASR 自动选型 |
| `assets/icon-map.js` | `17dace92…` | play-solid / pause-solid |
| `assets/xt-region.js` | `ecb0df09…` | 选择器六点打磨 + 地图 32vh |
| `assets/xt-moments.css` | `abf99a4c…` | padding 绑底栏高 + 安全区 |
| `assets/common.css` | `aef2b05c…` | `.main>.page` / `.content` 补 env |
| `server/routers/ai.py` | `005aadfe…` | embeddings/rerank 分流 + model3d 预览路由 |
| `server/data/model_registry.json` | `f0352303…` | 4 条 vector/rerank 补 `type` |
| `server/routers/version.json` | `ba864cbd…` | 1.30 / 31 / 星途-1.30.apk |
| `assets/xt-update.js` | `11021c1b…` | CURRENT_VERSION=1.30 |

---

## 八、遗留与下一步

1. **🔴 安全**：`ai-config.js` 明文 key → 前端切中转 + 轮换 key（见第二节）。
2. **🟡 git push 未做**：`origin` 是**公开仓库**（`github.com/Li-jiu0/study-workbench`）→ **push 前**必须先解决第 1 条（否则等于把 key 推到公开仓库），并执行 `git diff --cached | grep -iE 'sk-|apiKey|_API_KEY|SECRET|PASSWORD'` 自查；本机 push 需走代理（优先读环境变量 `HTTPS_PROXY`，备选 `127.0.0.1:7897`）。
3. **🟢 小项**：`common.css` 的 `.bottom-nav` 变量/env 未加兜底 + 两处历史 `min()`（行为可接受）；IM ASR 模型想去设置页可配（现自动选型已够用）；`server/.venv` uvicorn 安装损坏建议 force-reinstall；本地 `model_usage.json` 有 3 条 E2E 测试残留。
4. **🧹 仓库清洁**：真源工作区当前约 800+ 项未跟踪临时物（`_a*.py`、`_b*.txt`、`_audit_*`、`_tmp_*`、`_r104*_prod` 等），建议单独一批做归档/清理（**不要顺手删，先出清单**）。
5. **📌 未验证项**：本轮的"思维链可见性"是链路级验证（jsdom + 服务端行为），**建议在真机上用一个支持 reasoning 的模型（如 DeepSeek-R1 系列）实测一次**并截图存档。

---

## 九、给接手者的一句话建议

先读本节 + 第二节（安全），再执行任何部署前**先跑一次"本地 md5 vs 生产 md5"差异归零预检**——本轮所有踩坑（假失败路径、缓存戳未 bump、漂移未察觉、App 与 Web 生效范围不同）都可以被这一步拦住。
