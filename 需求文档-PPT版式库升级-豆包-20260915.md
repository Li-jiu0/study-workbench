# PPT版式库升级需求文档（详细执行版）

> 来源：豆包（用户提供，2026-09-15 18:04）。**仅登记，未开工。**
> 登记编号：N9-12（见 `交接文档-批次九-20260915.md` 第 10.6 节）。
> 用户原话：「这是豆包的建议，你记录一下」。

## 一、页面整体布局

```
┌─────────────────────────────────────────────────────┐
│  📐 PPT版式库                    [搜索框...........]  │  ← 第一行：标题+搜索
├─────────────────────────────────────────────────────┤
│  [全部] [封面页] [目录页] [过渡页] [内容页] [⭐我的收藏]  │  ← 第二行：分类标签
│                                          已学 3/15 ▾ │  ← 右上角：学习进度（小字）
├─────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │  [预览图] │  │  [预览图] │  │  [预览图] │  ...    │  ← 卡片网格
│  │  封面页   │  │  目录页   │  │  过渡页   │         │
│  │  适用场景 │  │  适用场景 │  │  适用场景 │         │
│  │ [⭐][👁][⬇]│  │ [⭐][👁][⬇]│  │ [⭐][👁][⬇]│         │
│  └──────────┘  └──────────┘  └──────────┘         │
└─────────────────────────────────────────────────────┘
```

---

## 二、每个卡片的结构（从上到下）

### 第1层：预览图区域

- **元素**：`<img>` 标签，不是 CSS 画的 div
- **尺寸**：16:9 比例，宽度撑满卡片，高度自适应
- **图片路径**：从数据的 `previewImage` 字段取
- **加载失败**：如果图片加载失败，显示统一占位图（灰色背景 + 居中显示版式名称），不要显示裂图图标
- **点击行为**：点击预览图本身 = 点击"👁 预览"按钮，弹出大图

### 第2层：版式名称

- **元素**：文字，加粗
- **内容**：数据的 `name` 字段，如"封面页·商务蓝"
- **位置**：预览图下方，左对齐

### 第3层：适用场景

- **元素**：文字，灰色小字，最多显示 2 行，超出省略号
- **内容**：数据的 `useCase` 字段，如"汇报/答辩/发布会的第一页"
- **位置**：版式名称下方

### 第4层：操作按钮区（三个按钮并排）

| 按钮 | 图标+文字 | 位置 | 说明 |
| --- | --- | --- | --- |
| 收藏 | ⭐ | 最左 | 空心=未收藏，实心黄色=已收藏 |
| 预览 | 👁 预览 | 中间 | 点击弹出大图遮罩层 |
| 下载 | ⬇ 下载 | 最右 | 有文件可点击，没文件置灰显示"制作中" |

---

## 三、每个按钮的详细交互

### 按钮1：⭐ 收藏按钮

**外观**：

- 未收藏状态：空心星星 ⭐（灰色边框）
- 已收藏状态：实心星星 ⭐（黄色填充）
- 鼠标悬停：放大 1.1 倍

**点击交互**：

1. 点击后立即切换状态（空心↔实心），不需要等服务器响应
2. 同时更新 localStorage
3. 如果当前筛选的是"⭐我的收藏"，取消收藏后该卡片立即从列表消失

**数据存储**：

- localStorage key：`ppt_favorites`
- 值：版式 ID 数组，如 `["cover-01", "timeline-02"]`
- 页面加载时读取这个数组，给对应卡片的星星设为实心

**异常处理**：

- localStorage 读取失败时，默认所有卡片未收藏，不报错

---

### 按钮2：👁 预览按钮

**外观**：

- 文字按钮，灰色背景，hover 变深
- 文字："预览"，前面加 👁 图标

**点击交互**：

1. 点击后弹出**全屏遮罩层**（半透明黑色背景，透明度 0.7）
2. 遮罩层中间显示一个白色内容面板，宽度占屏幕 80%，最大宽度 1000px
3. 内容面板布局：

```
┌──────────────────────────────────────────┐
│  封面页·商务蓝                        ✕   │  ← 标题+关闭按钮
├──────────────────────┬───────────────────┤
│                      │  适用场景：        │
│                      │  汇报/答辩第一页   │
│      [大图]          │                   │
│   (真实PPT截图)      │  设计要点：        │
│                      │  · 大标题居中      │
│                      │  · 背景用主题色    │
│                      │  · 可加Logo        │
├──────────────────────┴───────────────────┤
│  [⬇ 下载模板]  [⭐ 收藏]  [← 返回]       │  ← 底部按钮
└──────────────────────────────────────────┘
```

4. 左侧大图：点击可放大查看（可选，先做基础版）
5. 右侧文字：适用场景 + 设计要点列表（保留现在卡片里的文字内容）
6. 底部三个按钮：
   - "⬇ 下载模板"：和卡片上的下载按钮功能一样
   - "⭐ 收藏"：和卡片上的收藏按钮功能一样，状态同步
   - "← 返回"：关闭遮罩层

**关闭方式**（三种都要支持）：

1. 点击右上角 ✕ 按钮
2. 点击遮罩层的黑色空白区域（不要点白色内容面板也关闭）
3. 按键盘 ESC 键

**禁止事项**：

- ❌ 禁止用 `alert()` 或 `prompt()` 弹窗实现
- ❌ 禁止只显示文字不显示大图
- ❌ 禁止遮罩层点哪里都关闭（必须点空白区域才关）

---

### 按钮3：⬇ 下载按钮

**外观（两种状态）**：

**状态A：有真实PPT文件**（数据的 `pptFile` 字段不为 null）

- 蓝色背景，白色文字
- 文字："⬇ 下载"
- 可点击，鼠标悬停变手型

**状态B：没有真实PPT文件**（`pptFile` 为 null）

- 灰色背景，灰色文字
- 文字："制作中"
- 不可点击，鼠标悬停不变手型
- **不要弹 alert 说"功能开发中"**，直接在按钮上显示状态

**点击交互（仅状态A可点击）**：

1. 点击后触发浏览器下载
2. 下载的文件是数据 `pptFile` 字段指向的 .pptx 文件
3. 下载的文件名保持原始文件名（如"封面页-商务蓝.pptx"）
4. 下载后不跳转页面，不弹提示（浏览器自己会显示下载完成）

**实现方式**：

- 用 `<a href="文件路径" download="文件名">` 标签实现，不要用 JS 手动创建 blob
- 如果文件路径是相对路径，确保相对于网站根目录

**异常处理**：

- 文件不存在（404）时，按钮自动切换为"制作中"状态，不要让用户点了下载404页面

---

## 四、分类筛选标签

**位置**：页面第二行，横向排列

**标签列表**（从左到右）：

1. `全部` - 默认选中，显示所有版式
2. `封面页` - 只显示 category = "封面页" 的
3. `目录页` - 只显示 category = "目录页" 的
4. `过渡页` - 只显示 category = "过渡页" 的
5. `内容页` - 只显示 category = "内容页" 的
6. `⭐ 我的收藏` - 只显示在 ppt_favorites 数组里的

**交互**：

- 点击哪个标签，哪个高亮（蓝色背景白色文字），其他恢复默认
- 点击后卡片网格立即刷新，只显示符合条件的卡片
- 筛选时保留当前的收藏状态（星星状态不变）

**空状态**：

- 如果筛选后没有卡片（比如"我的收藏"里还没收藏任何版式），显示空状态：
  - 居中显示一个图标（📭）
  - 文字："还没有收藏的版式，去逛逛吧"
  - 下面一个按钮："查看全部版式"，点击切换到"全部"标签

---

## 五、搜索框

**位置**：页面右上角，第一行

**外观**：

- 输入框，placeholder："搜索版式名称/关键词"
- 宽度 200px，圆角

**交互**：

- 输入文字后，实时过滤卡片（不需要点搜索按钮）
- 匹配范围：版式名称 + 适用场景 + 设计要点的文字
- 不区分大小写
- 清空搜索框后，恢复显示当前分类筛选的全部卡片
- 搜索和分类筛选同时生效（比如选了"封面页"分类，又搜了"商务"，只显示封面页里含"商务"的）

**空状态**：

- 搜索无结果时显示："未找到匹配的版式，换个关键词试试"

---

## 六、学习进度（保留现状，不做大改）

**位置**：页面右上角，分类标签行的最右侧

**外观**：

- 小字显示："已学 3/15 ▾"
- 点击展开下拉面板，显示今日待学列表

**交互**：

- 保留现在的逻辑，不需要改
- 只是位置从顶部整行移到右上角，不要占主视觉

---

## 七、数据结构

每个版式的数据（存在 `data-ppt-templates.js` 或类似文件里）：

```
const PPT_TEMPLATES = [
  {
    id: 'cover-01',              // 唯一ID，用于收藏和筛选
    name: '封面页·商务蓝',        // 卡片显示的名称
    category: '封面页',            // 分类：封面页/目录页/过渡页/内容页
    previewImage: 'assets/images/ppt-templates/cover-01.png',  // 预览图路径
    pptFile: 'assets/ppt-files/cover-01.pptx',                  // PPT文件路径，没有则填 null
    useCase: '汇报/答辩/发布会的第一页，奠定整体风格',  // 适用场景
    designPoints: [                 // 设计要点列表
      '大标题居中，副标题在下',
      '背景用主题色或深色渐变',
      '可加公司Logo或装饰元素'
    ]
  },
  // 没有真实文件的版式示例：
  {
    id: 'timeline-02',
    name: '时间轴·简约',
    category: '内容页',
    previewImage: 'assets/images/ppt-templates/timeline-02.png',
    pptFile: null,                  // null = 没有文件，下载按钮显示"制作中"
    useCase: '展示发展历程/流程步骤/项目计划',
    designPoints: [
      '横向或纵向时间线',
      '节点用圆点/图标标记',
      '每个节点：时间+标题+简短说明'
    ]
  }
  // ... 共15个版式
]
```

---

## 八、文件存放目录

```
assets/
├── images/
│   └── ppt-templates/          ← 预览图放这里
│       ├── cover-01.png
│       ├── cover-02.png
│       └── ...
├── ppt-files/                   ← 真实PPT文件放这里（可以先空着）
│   ├── cover-01.pptx
│   └── ...
└── data-ppt-templates.js       ← 版式数据文件
```

---

## 九、验收标准（逐条检查）

1. ✅ 打开版式库，每个卡片里显示的是**图片**（`<img>` 标签），不是 CSS 画的灰色矩形
2. ✅ 图片加载失败时显示统一占位图，不显示裂图
3. ✅ 点击"👁 预览"按钮，弹出全屏遮罩层，左侧显示大图，右侧显示适用场景和设计要点
4. ✅ 预览遮罩层可以通过：点 ✕、点空白区域、按 ESC 三种方式关闭
5. ✅ 点击"⭐"收藏按钮，星星从空心变实心，刷新页面后状态保留
6. ✅ 顶部分类栏点"⭐ 我的收藏"，只显示已收藏的版式
7. ✅ 下载按钮：有文件的显示"⬇ 下载"可点击，点击后真实下载 .pptx 文件
8. ✅ 下载按钮：没文件的显示"制作中"置灰，不可点击，不弹 alert
9. ✅ 分类筛选标签点击后卡片实时过滤，空状态有友好提示
10. ✅ 搜索框输入后实时过滤，搜索和分类筛选同时生效
11. ✅ 整个页面没有 `alert()` 或 `prompt()` 弹窗
12. ✅ 学习进度在右上角，不占顶部整行

---

这份文档每个按钮、每个交互、每个异常情况都写清楚了，直接发给 workbuddy，他按这个做就行。
