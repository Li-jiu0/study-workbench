# 交付报告 · A7 —— 个人中心 作品集 + B1 feed-btn 挪位 · 验证收尾

- **任务编号**：A7（N9-4 个人中心作品集 + B1 feed-btn 挪位 —— 验证收尾）
- **执行人**：software-engineer-任务七
- **日期**：2026-09-16
- **状态**：**DONE**（含 1 处必要缺陷修复）
- **可写代码树**：`D:\下载的文件\学习工作台`
- **独占文件**：`D:\下载的文件\学习工作台\个人中心.html`

---

## ① 改动文件清单

| 文件 | 改动前 | 改动后 | mtime（改动后） | 说明 |
|---|---|---|---|---|
| `个人中心.html` | 102,718 B | **104,035 B** | 2026-09-16 14:37:30 | 唯一被本任务修改的文件 |
| `个人中心.html.bak-a7-pre-fix-20260916` | — | 102,718 B（副本） | 2026-09-16 13:52:21（继承源） | 改动前备份 |
| `tools/qa/_a7_persist_test.js` | 新增 | — | — | jsdom 收藏持久化 + feed-btn 验证脚本（仅测试工具，非站点代码） |

**零改动的共享文件（重点声明）**：

| 文件 | 实测 mtime | 是否本任务改动 |
|---|---|---|
| `assets/app.js` | 2026-09-16 **13:41:42** | ❌ 否（L1 线改动） |
| `assets/api.js` | 2026-09-16 **12:18:13** | ❌ 否（更早批次） |

本任务开工基线 = 2026-09-16 13:30。两个共享文件的 mtime 均不落在本任务写入窗口内，
且本任务全程**只写 `个人中心.html` 一个文件**（+ 测试脚本），未触碰 `assets/app.js` / `assets/api.js`。

---

## ② 完工自检清单（逐项）

| # | 检查项 | 结果 | 证据 |
|---|---|---|---|
| 1 | `node tools/qa/escheck_es2017.js` = `DONE 0` | **PASS** | 实测输出 `DONE 0` |
| 2 | `<!--`/`-->` 配对 | **PASS** | 26 / 26 |
| 3 | `<div`/`</div>` 配对 | **PASS** | 275 / 275 |
| 4 | `<script`/`</script>` 配对 | **PASS** | 17 / 17 |
| 5 | `<body`/`</body>` 配对 | **PASS** | 1 / 1 |
| 6 | `<section`/`</section>` 配对 | **PASS** | 6 / 6 |
| 7 | `<style`/`</style>` 配对 | **PASS** | 1 / 1 |
| 8 | `<button`/`</button>` 配对 | **PASS** | 24 / 24 |
| 9 | `<span`/`</span>` 配对 | **PASS** | 89 / 89 |
| 10 | 禁用语法 Grep = 0 | **PASS** | 可选链/空值合并/replaceAll/Object.fromEntries/.at(/正则后行断言/`**`/对象展开/可选 catch **全部 0 命中** |
| 11 | 原生 `alert(`/`confirm(`/`prompt(` = 0 | **PASS** | 0 命中 |
| 12 | 换行符仍纯 CRLF | **PASS** | CRLF=1777 / LF_total=1777 / **bare_LF=0** |
| 13 | `app.js` / `api.js` mtime 未被本任务改动 | **PASS** | 见 ① 表 |

> 说明：配对统计已**剔除注释区与字符串字面量**后再计数，避免「注释里写 `<body>` / `<style>`」造成误报。
> `<head>`/`</head>` 本页实测 1 / 0 —— 属**历史遗留，非本次破损**（任务书已确认，未"顺手修"）。

---

## ③ 收藏持久化实测结论（N9-4 核心）

**方法**：jsdom 加载**真实页面文件**，只执行页面自身 8 段内联脚本；`#profileBox` 用与真实
`app.js renderProfilePage()` / `api.js` **同构**的动作条填充以复现真实归位目标。
脚本：`tools/qa/_a7_persist_test.js` → 日志 `_tmp_a7_jsdom_out.txt`。

**结果：41 项断言，PASS 41 / FAIL 0 → `VERDICT: ALL_PASS`**

### 关键输出摘录

```
键名口径（项目约定：裸 key + window.lsKey() 账号后缀）
  lsKey("xtc:lib:pf:folio:fav") = xtc:lib:pf:folio:fav@shared        [PASS]

>> 模拟用户收藏：点击 id=f_test_2 卡片右上角 ☆
  toast 提示 = ["已收藏，刷新后仍在"]
  localStorage["xtc:lib:pf:folio:fav@shared"] = {"f_test_2":1}        [PASS]
  [PASS] 收藏表内容正确：{"f_test_2":1}

>> 再收藏演示页聚合作品 id=ppt_test_1
  收藏表 = {"f_test_2":1,"ppt_test_1":1}                              [PASS] 两来源均生效
  [PASS] 只读聚合键 xtc:lib:pptw:works 未被本页改写（仍 1 件）

>> 打开查看弹窗并收藏 id=f_test_1
  弹窗收藏后 收藏表 = {"f_test_2":1,"ppt_test_1":1,"f_test_1":1}       [PASS]
  弹窗收藏按钮文案（收藏后）= "★ 已收藏"                                [PASS]

>> 切到「★ 收藏」筛选 tab
  收藏筛选下卡片数 = 3                                                [PASS]

============ 重新加载页面 → 收藏状态恢复断言 ============
搬迁 localStorage 键 4 个（含 fav@shared = {"f_test_2":1,"ppt_test_1":1,"f_test_1":1}）
重载后渲染：卡片数 = 3，收藏态（★ on）卡片数 = 3                        [PASS]
逐卡收藏态: {"f_test_2":true,"f_test_1":true,"ppt_test_1":true}         [PASS] 渲染回原位、未错位

>> 重载后取消收藏 id=f_test_2，再重载一次
取消后收藏表 = {"ppt_test_1":1,"f_test_1":1}                            [PASS] 移除生效
第三次加载后收藏态: {"f_test_2":false,"f_test_1":true,"ppt_test_1":true}  [PASS] 取消同样持久化

>> 多账号隔离回归：CURRENT_ACCOUNT=u777
u777 视角卡片 id = ["u777_1"]  ★数=1                                   [PASS] 不串号
```

### 结论

1. **写入**：收藏动作（卡片 ☆ / 查看弹窗按钮）**确实写入 localStorage**，键 = `xtc:lib:pf:folio:fav`（经 `lsKey()` 前缀化为 `…@<account>`）。
2. **恢复**：**重新加载页面后收藏状态被正确恢复**，★ 高亮**渲染回原卡片、位置未串**。
3. **取消也持久化**：取消收藏同样落库，第三次加载仍保持未收藏。
4. **键名与其它页一致**：使用**裸 key**，经项目统一 `window.lsKey()` 做账号后缀（与 `app.js:15`、`assets/ppt-works.js:32/79`、`xt-content.js lsKeySafe` 同名同口径），**未自创前缀**。
5. **只读边界正确**：演示页聚合键 `xtc:lib:pptw:works` **仅读未写**。
6. **多账号隔离正确**：不同 `CURRENT_ACCOUNT` 互不可见，无串号。

---

## ④ B1 feed-btn 状态

**结论：B1 挪位已完成且达标 —— 但发现并修复了 1 处与挪位直接相关的真实缺陷。**

### 已达标的既有实现（未改动）

| 项 | 实测 | 判定 |
|---|---|---|
| ① 节点已从资料卡**上方**移到 `#profileBox` **之后** | `[data-subpage="profile"]` 子节点顺序 = `profileBox , profileFeedBtnRow , profileEditModal` | PASS |
| ② `btn-primary` → `btn-outline` 降级 | 静态 class = `"btn btn-outline"`；运行时仍保持 | PASS |
| ③ 运行时搬进「编辑资料」所在动作条 | 归位后 `row.parentNode === 「编辑资料」按钮的父节点` → `display:block` | PASS |
| ④ 自/他人视角文案 | 自视角 = `我的动态` | PASS |
| ⑤ 去重（动作区已有同义「动态」按钮时隐藏） | `display = none` | PASS |

### ⚠️ 发现并修复的缺陷（唯一改动原因）

**问题**：feed-btn 在资料卡重渲染后会**永久消失**。

- **根因**：B1 把 `#profileFeedBtnRow` 挪进了资料卡操作条（位于 `#profileBox` **内部**）。
  而 `renderProfilePage()` 执行 `box.innerHTML = \`...\``，会把该行**连同其父动作条一起销毁**。
  原 `MutationObserver` 回调首行是 `if (!row) return;` —— 节点已被连根删除，`getElementById` 返回 `null`
   → **直接早退，永远无法归位**（原注释自称"被冲掉后自动重新归位"，与代码自相矛盾）。
- **可达路径**：首次进入 profile 子页顺序是安全的（`renderProfilePage()` → `updateProfileFeedBtn()`，见 HTML L947-951）；
  但**已在 profile 子页时的二次渲染**会踩中 —— 即 `assets/api.js:1591` 的
  `closeProfileEditor(); renderProfilePage();`（**保存资料 / 更换头像后**）等流程。
  表现为：保存资料后「我的动态」按钮凭空消失。
- **回归性质**：这是 B1 挪位**引入**的（挪位前该行在 `#profileBox` **外部**，`innerHTML` 重写不会波及）。

**修复（最小变更，仅本页内联 JS，不碰 assets）**：
1. 首次归位时用 `cloneNode(true)` 在内存保留一份「原始行」模板；
2. 新增 `ensureRow()`：若 `#profileFeedBtnRow` 已不存在（被 `innerHTML` 冲掉），按模板重建并挂回文档待归位；
3. `placeFeedBtn()` 与 `MutationObserver` 回调改用 `ensureRow()`，不再因 `!row` 早退；
4. `updateProfileFeedBtn()` 开头同样走 `ensureRow()`（被冲掉时仍能自愈）；
5. 重建节点会清掉残留 `onclick` 句柄，随后由 `updateProfileFeedBtn()` 统一重绑 —— 避免跨节点句柄复用。

其余行为（`btn-outline` 降级、自/他人视角文案、同义按钮去重、`gotoUserMoments` 跳转口径）**完全不变**。

**修复后回归验证**（同脚本）：

```
模拟 renderProfilePage() 整块重写 #profileBox.innerHTML
  重渲染瞬间 #profileFeedBtnRow 是否仍取到 = false（节点已随 innerHTML 一起消失）
  重渲染后 #profileFeedBtnRow 重新出现 = true
  重渲染后 row.parentNode === 新动作条 = true
  重渲染后 display = block
  [PASS] 资料卡重渲染后 feed-btn 自动重新归位到新动作条
  [PASS] 重渲染后 feed-btn 仍可见
  [PASS] 检测到同义按钮时自动隐藏本按钮，避免重复（回归未被破坏）
  [PASS] 重复调用 updateProfileFeedBtn() 不抛异常（回归未被破坏）
```

**改动量**：`+29` 行（26 新增 / 2 替换），102,718 B → 104,035 B，**换行符仍纯 CRLF**，
`escheck_es2017.js` 仍 `DONE 0`。改动前后已备份 `个人中心.html.bak-a7-pre-fix-20260916`。

---

## ⑤ 越界自检结论

- 本任务**仅写入** `个人中心.html`（+ 测试脚本 `tools/qa/_a7_persist_test.js`）。
- `assets/app.js` mtime = **13:41:42**（L1 线，非本任务）；`assets/api.js` mtime = **12:18:13**（更早批次）。
- 两者 mtime **均未被本任务改动**，本页改动**未越界**去修改共享文件。
- 未新增任何 `assets/*` 文件；作品集为**纯页内实现**（DOM + `.xtf-` CSS + 内联 JS）。
- 未 bump 任何版本戳（波内不 bump）。
- 页面既有 DOM id 与全局函数名**全部保留**；新增全局一律经 `typeof window.xxx !== 'function'` 守卫赋值。

---

## ⑥ 遗留问题 / 需要主理人裁决的点

1. **`<head>` / `</head>` 缺失（历史遗留，非本次引入）**
   本页 `<head>` 1 处、`</head>` 0 处，浏览器靠容错解析（`<body>` 自动闭合 head）。
   已按任务书要求**未修改**。**建议单独立项**统一处理（`mock_exam*.html` 等页面同病），
   避免在多任务并行期间产生全文件级 diff 与冲突。

2. **本页 `lsKey()` 依赖 `assets/app.js` 先加载**
   本页内联脚本不定义 `lsKey`，`wrapKey()` 在缺失时**退回裸 key**。
   正常加载顺序下 `app.js` 先执行（`window.lsKey` 已就位）无影响；
   若将来某个页面不引 `app.js`，作品集数据会落在**无账号后缀**的裸键上。
   属既有项目约定（`company-lib.js` / `mini.js` / `ppt-works.js` 等同样兜底），**不单独修改**，仅记录。

3. **`app.js` 渲染的资料卡（本地模式）不含「编辑资料」所在动作条**
   本地模式 `app.js renderProfilePage()` 把「编辑资料」放在 `.pp-profile` 内的独立按钮
   （无 `button[onclick="editProfile()"]` 的 DIV 兄弟行），此时 `actionHost()` 退回 `box.querySelector('.card')`，
   feed-btn 会落在**第一个 `.card` 开头**而非操作条。
   在线模式（`api.js`）与 `?user=` 视角均正常命中动作条。
   **不影响功能**（按钮仍可见、可点、跳转正确），仅视觉位置在本地模式下略靠上。
   建议主理人确认是否需要在后续批次微调（**本次未改**，因属"是否要改"的产品判断，且改动会扩大 diff）。

4. **`#profileFeedBtnRow` 内联 `style="display:none"` 依赖运行时点亮**
   属既定设计（先隐藏、由 `updateProfileFeedBtn()` 决定是否显形），未改动。
   若 `assets/api.js` / `app.js` 均未加载（极端降级），按钮保持隐藏 —— 与挪位前的降级行为一致。

---

## 附：验证脚本与日志

| 用途 | 路径 |
|---|---|
| jsdom 收藏持久化 + feed-btn 验证脚本 | `tools/qa/_a7_persist_test.js` |
| 上脚本完整日志（41 断言逐条） | `_tmp_a7_jsdom_out.txt` |
| 完工自检输出（结构/语法/弹窗/CRLF/mtime） | `_tmp_a7_selfcheck_out.txt` |
| 改动前备份 | `个人中心.html.bak-a7-pre-fix-20260916` |

**复现命令**（本机路径）：

```
# ES2017 语法门
C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe \
  "D:/下载的文件/学习工作台/tools/qa/escheck_es2017.js"        -> DONE 0

# 收藏持久化 + feed-btn 回归
C:/Users/ATM/.workbuddy/binaries/node/versions/22.22.2-3/node.exe \
  "D:/下载的文件/学习工作台/tools/qa/_a7_persist_test.js"      -> pass=41 fail=0
```
