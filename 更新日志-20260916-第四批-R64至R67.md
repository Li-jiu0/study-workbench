# 更新日志 · 2026-09-16 第四批（R64 → R67）

> 批次时间：2026-09-16 19:50 ~ 22:25
> 需求来源：用户桌面 4 份文档 —— 《模型升级.txt》(R64)、《AI继续升级.txt》(R65)、《AI再次优化.txt》(R66)、《持续升级优化.txt》(R67)
> 交付形态：仅本地代码树 `D:\下载的文件\学习工作台`，**未部署上线**（部署需用户点头）
> 相关交接文档：`交接文档-20260916f-第四批执行现状与对接.md`、`给接手AI的开场指令-20260916f.txt`（同日生成）

> 🛑 **收工声明（22:23 用户指示停止推进）**：本轮主理人在 R66 三线交付、R67 线3 交付后停止。
> **未做**：R66 独立 QA、R67 线1/线2 编码、打戳 `20260916R`、部署。**已成**：R64/R65（含 QA + 打戳 Q）、R66 三线代码、R67 线3 数据、三份交接文档。
> 接手者请从「R66 独立回归」开始（详见交接文档 §4 与 §9）。

---

## 一、批次总览

| 编号 | 主题 | 改动文件 | 开发自测 | 独立 QA |
|---|---|---|---|---|
| **R64** | 模型设置页升级（14 条需求） | `ai-settings.html`、`assets/ai-settings.js`、`assets/ai-service.js`、`assets/ai-config.js`、`assets/ai-page.js` | 线1 13/13、线2 35/35、线3 39/39 | ✅ 14/14 PASS，0 BUG |
| **R65** | 三模式 + 记忆 + MAX 升级 | 同上（并波交付） | 同上 | ✅ 同上（并入 R64 QA） |
| **R66** | AI 第三轮优化（9 组：密钥安全/下拉瘦身/用量显示/检测按钮化/表单/关于页） | `ai-settings.html`、`assets/ai-settings.js`、`assets/ai-page.js`、`AI.html`、`assets/ai-service.js` | 线1 70/70、线2 34/34、线3 37/37 | ⏳ **未出报告**（QA 被中断两次，成员处于 paused，可唤醒继续） |
| **R67** | 上下文格式 + 添加模型表单再优化（3 组） | `assets/ai-config.js`（线3 已交付）；`ai-settings.*`、`ai-page.js`、`AI.html`（线1/线2 **未开工**） | 线3 15/15 | ⏳ 待线1/线2 完成后统一验 |

---

## 二、R64 + R65（模型设置页升级 + 三模式/记忆/MAX）

**来源**：《模型升级.txt》14 条 + 《AI继续升级.txt》3 条

**R64 落地要点**
- 设置页 `ai-settings.html` + `assets/ai-settings.js` 重构为 **5 个平级 Tab**：模型列表 / 功能分类 / 模型说明 / 添加模型 / 关于；移动端 Tab 横滑。
- 模型列表：点行选中写 `ai_selected_model` + 橙框「当前使用」高亮；✏️ 编辑（内置写 `overrides`、自定义改 `ai_custom_models`）；🗑 删除（仅自定义）；拖拽 + ↑↓ 兜底排序写 `order`；「恢复默认」清 `disabled/order/overrides/stars`（不动自定义模型与分类）；星级可点击修改写 `stars`。
- 功能分类：只按**模型能力**分 4 类（文本/推理/视觉·识图/翻译），场景字样全部清除；分类可增删排序（`categories`）；每类多选模型按优先级（`catModels[funcType]`）。
- 模型说明：24 个模型卡片（星级 + 响应速度 + 说明）。
- 关于页：模型总数/已启用/自定义数/不可用数。
- 滚动 bug 根因：`common.css` 第 68 行 `html,body{height:100%;overflow:hidden}` 锁死独立全页 → 设置页内 `!important` 解锁恢复自然滚动。
- 健康检查队列：加载后 ~1.5s 启动、每 ~6.5s 测一个（**守 10 次/分钟限频铁律**）、结果缓存 30 分钟写 `health[id]`；失败行标灰不隐藏；`cors` 单独提示「跨域受限（浏览器直连）」。
- 底座 `ai-service.js`：修 `findModel` 只查内置模型的 latent bug（自定义模型被静默忽略）；`requestModel` 支持模型自带 `apiUrl/apiKey/apiFormat/extraHeaders`（无 provider 也能直连）；`buildChain` 读 `catModels`、跳过 `disabled`、未注册 funcType 先查 catModels 再回退 general（顺带做通 R63 遗留的「自定义分类不生效」）；新增 `window.aiGetModelSettings` / `window.aiSaveModelSettings` / `window.aiHealthCheck`。
- 配置 `ai-config.js`：24 模型 `modelDetails` 补 `stars`（1-5 整数）与 `speed`（快/中/慢/待检测；13 个新增模型用 2026-09-16 实测毫秒换算，11 个旧模型标「待检测」不臆造）；`FUNC_TYPES` 7 个 desc 能力化（零场景词）。

**R65 落地要点**
- 三模式：`ai_model_mode`（`''`/fast/balanced/ultimate）+ `AI_CONFIG.modelModes` 三链（名单按实测速度/星级挑，Gemini 需自备网络不入链）；下拉顶部三模式行 + 分隔线；模式内模型全挂**自动降下一模式**（fast→balanced→ultimate→general 默认链）；点模式关深度思考、手动选模型清模式；模式生效时输入框标签显示模式名，气泡上仍标实际使用模型。
- 记忆：`ai_memory`（字符串数组，单条 ≤200 字、上限 50 条 FIFO）；`askAI` 头部注入 system；消息操作「存入记忆」；**不做自动提炼**（守限频铁律）。
- MAX 模式：`ai_max_mode='1'` 时自动开深度思考 + `max_tokens` 上限提到 8000 + `callAI` 头部注入「深度教学」system 提示词（逐步推导/讲透/易错点/举一反三/允许长回答）。
- 发现并处理：`AI.html` 原设置弹窗（MAX 开关 / 上下文长度 chips）自 R63 把侧栏按钮改跳设置页后**已无入口可达**；R65 期间在模型面板注入过「记忆管理」入口，R66 又按要求移除（详见 R66）。

**R64/R65 独立 QA 结论：14/14 PASS、0 BUG**（含密钥/数据/行为/引用四组；跨线契约、模式降级、健康检查不烧限频、R63 五项回归全部通过）。
**波末已打戳：`?v=20260916Q`**（32 页 / 65 处，仅这 4 个 AI 资产；裸路径 0、旧戳 P 残留 0、行尾零变化、`escheck DONE 0`）。

---

## 三、R66（AI 第三轮优化，9 组）

**来源**：《AI再次优化.txt》

| 组 | 内容 | 状态 |
|---|---|---|
| A 密钥安全 | 编辑模型**不回填平台密钥**、Key 框改 `type=password` + 眼睛按钮、保存语义改「空=保持原值」、新增「清除密钥覆盖」、底座幂等清洗存量副本 | 已交付（自测 8/8 + 底座 5/5） |
| B 模式项 | 三模式行删副标题小字、行高压缩至 ~34px | 已交付 |
| C 菜单改名 | 入口「模型设置」→「添加模型」（侧栏 + 面板两处） | 已交付 |
| D 记忆管理挪位 | 下拉里的「记忆管理」入口移除；记忆管理区块挪进设置页 Tab5 关于区 | 已交付 |
| E 下拉溢出 | 面板 `max-height:70vh` + 内部滚动 + `max-width:calc(100vw - 24px)`；`layoutModelPanel()` 处理右溢贴右、上方不足翻转 | 已交付 |
| F 对话框 | 模型下拉移到发送按钮左边；新增灰字上下文用量「已用 1.2k / 8k」；>80% 橙 / >95% 红；上限随 模式链首个模型 / MAX 8000 / 选中模型 动态 | 已交付 |
| G 连通性检测 | 「待检测」→ 可点三态按钮（⏳/✅/❌）+ 弹窗原因映射（密钥无效/地址错误/限流/服务端错误/跨域受限/网络不可达/超时/无响应）+ 批量检测（串行 6.5s）+ 表单【测试连接】用未保存值 + 关于页【重新检测全部模型】 | 已交付 |
| H 添加模型表单 | 名称改预设下拉（datalist）、选预设自动回填端点/格式/模型 ID、【自定义】清空 | 已交付（R67 还要再改成服务商分组） |
| I 关于页 | 删重复「设置保存在本机」+ 新增【重新检测全部模型】 | 已交付 |

**底座同步（`ai-service.js`）**
- `aiGetModelSettings` 首次调用时**幂等清洗**：`overrides[id].apiKey` 与 `AI_CONFIG.providers[provider].apiKey` **严格相等**者删除并写回；用户自填 Key、自定义模型 Key 一律不动；日志只输出计数，**不打印任何 Key 明文**。
- `aiHealthCheck(modelId, cfgOverride)` 新增第二参数（临时配置，不读不写已存配置、不落盘）；新增 err 值 `no_endpoint` / `no_key`。

**⚠️ 重要边界（必须如实传递给用户/接手者）**：平台密钥本身**明文硬编码在 `assets/ai-config.js`**（L15/20/26/31/36/45），前端直连架构下**无法真正保密**。本批交付的是「UI 不回填 + 不复制进 localStorage + 清洗存量副本」，**不等于密钥保密**；要真保密只能把 Key 挪到后端转发（R63 方案 B 已否决）。

**R66 独立 QA：未执行**（22:23 用户指示停止推进，QA 一次都没跑完 → **不等于通过**）。开发线自测：线1 70/70、线2 34/34、线3 37/37。
**接手者必须补的验证项**：① 密钥专项（localStorage 全量 dump 不含平台密钥、编辑不回填、留空保存不丢覆盖 key、清除覆盖、清洗幂等且 8 字段无损）② 因 `ai-settings.js` 被**全量重写**，须逐项行为断言 R64/R65 特性未丢（选中/禁用/编辑/删除/排序/恢复默认/星级/分类/健康队列）③ R66 新功能（检测三态与原因映射、批量串行 ≥6.5s、测试连接第二参数、记忆管理、datalist、AI 页用量与面板限高）④ 跨文件集成（设置页写的 settings 被底座与 AI 页正确消费）。
**主理人抽验通过**：备份为真·改动前原样；R64 特性存活（`order` 29/29、`drag` 15/15、`moveModel` 3/3、上移下移 6/6 与备份计数逐一相等）；新文件平台密钥 0 命中；行尾零污染。
**已知取舍**：375px 窄屏输入行右侧组会折行（发送按钮仍右对齐），主理人拍板**接受**，不强行单行；上下文用量为**估算值**（中文 1 字≈1 token、非中文 4 字符≈1 token），非精确 tokenizer。

---

## 四、R67（上下文格式 + 添加模型表单再优化）

**来源**：《持续升级优化.txt》（首读为空文件，用户补写后重读有效）

| 组 | 内容 | 状态 |
|---|---|---|
| A 上下文用量 | 格式改 `0.0% · 0 / 1000.0K 上下文已使用`（百分比 1 位小数 + 上限 K 单位 1 位小数）；hover 悬浮说明；阈值变色沿用；排版紧凑 | ⏳ 线2 **未开工**（等 R66 QA） |
| B 删重复入口 | 删除设置页模型列表「＋ 添加自定义模型」按钮（`ai-settings.html:423`）+ 同步空态文案（`ai-settings.js:1065`） | ⏳ 线1 **未开工** |
| C 添加模型表单 | 服务商分组下拉 + 端点回填 + 模型 ID 下拉联动 + 能力标签预填 + API 格式默认隐藏（高级设置可展开）+ 检测连接按钮 | ⏳ 线1 **未开工** |
| D 服务商数据 | `AI_CONFIG.providerGroups` 13 组 | ✅ 线3 **已交付**（15/15 PASS） |

**`AI_CONFIG.providerGroups` 结构契约（线1 消费，一字不改）**
```js
{ key, label, builtin, apiUrl, apiFormat, needKey, note, models: [{ id, name, types }] }
```
- 第一层 `builtin:true` 6 家（智谱AI / 硅基流动 / 火山方舟·豆包 / 百度千帆·文心一言 / OpenRouter / Google Gemini），`needKey:false`，模型候选与现有 `builtinModels` 逐条一致。
- 第二层 `builtin:false` 6 家主流公开模板（DeepSeek / 通义千问 / Kimi / OpenAI / Claude / 讯飞星火），全部 `needKey:true` + note 含「需自备 Key，端点未经本项目实测」；Claude 为 `apiFormat:'custom'` + 「非 OpenAI 兼容」标注 + `apiUrl:''`。
- 第三层 `custom 自定义/兼容接口`（`models: []`）。
- 主理人抽验：`providerGroups` 单块（1 处定义 + 1 处注释）、段内 13 key 无重复、备份为真原样、`node --check` 通过、行尾 `CRLF=719 / loneLF=0`。

**⚠️ 待对齐小项**：第二层厂商模型的 `name` 展示名由线3 按通用命名补全，线1 做下拉 UI 时如需统一文案再微调。

---

## 五、本批新增/变更的数据契约（接手必读）

| 键 / 契约 | 结构 | 说明 |
|---|---|---|
| `ai_selected_model` | string | 当前选中模型 id（设置页写、AI 页读） |
| `ai_model_mode` | `''` \| `'fast'` \| `'balanced'` \| `'ultimate'` | 三模式；非空时 mode 优先于手动选中 |
| `ai_memory` | string[] （单条 ≤200 字、≤50 条 FIFO） | 长期记忆 |
| `ai_custom_models` | `[{id,name,apiUrl,apiKey,apiFormat,extraHeaders,types,stars,rate,temperature,maxTokens,fallback,desc,speed,needVPN}]` | 自定义模型 |
| `ai_model_settings` | `{disabled:{id:true}, order:[], overrides:{id:{...}}, catModels:{ft:[id]}, categories:[], health:{id:{ok,ms,err,at}}, stars:{id:1-5}}` | 设置页单键配置 |
| `AI_CONFIG.modelDetails[id]` | 新增 `stars`(1-5) / `speed`(快\|中\|慢\|待检测) | 参考默认值，可被 overrides 覆盖 |
| `AI_CONFIG.modelModes` | `{fast/balanced/ultimate:{label, chain:[modelId]}}` | 三模式链 |
| `AI_CONFIG.providerGroups` | 见 R67 章节 | 服务商分组数据 |
| `window.aiHealthCheck(modelId, cfgOverride?)` | → `Promise<{ok, ms, err}>`；err ∈ `http_XXX`/`cors`/`network`/`timeout`/`empty`/`no_endpoint`/`no_key` | 不 recordCall、不烧限频、12s 超时、maxTokens=1 |
| `window.aiGetModelSettings()` / `aiSaveModelSettings(obj)` | — | 统一读写 + 内置平台密钥副本清洗 |

---

## 六、踩坑记录（本批新增）

1. **打戳双后缀（主理人自踩，已修复）**：bump 脚本 TARGETS 元素带 `.js`、替换串又拼 `.js` → 65 处写成 `assets/xxx.js.js?v=…`。防法：① 捕获组不带后缀 ② **终态扫描必须用与替换不同的独立 pattern** ③ 显式扫 `\.js\.js\?v=` 必须 0。已写入部署技能 `study-workbench-deploy` §2.0.1。
2. **日志被 bash 吞字（已修复）**：把含反引号的 Python 代码内联给 bash，反引号内容被当命令替换执行 → 文件名丢失。**含中文/反引号的一律 Write 落盘再执行**。
3. **成员自述 ≠ 事实**：线3 自述「补丁脚本曾误执行两次导致重复插入」，主理人独立抽验确认最终态为单块、备份已恢复为真原样。
4. **全量重写的风险**：R66 线1 对 `ai-settings.js` 做了全量重写（1760→2257 行）。主理人以「签名计数对照备份」+ 要求 QA 做**逐项行为断言**双重兜底——接手者遇到同类重写务必照此办理。

---

## 七、未完成事项（⚠️ **22:25 停工时点的历史快照**——第 1/2/3/4 条已于当晚收尾完成，实测状态见 §八）

1. 🔴 **R66 独立回归：未执行**（必做第一件事；被测 4 文件 `ai-settings.js`/`ai-settings.html`/`ai-page.js`/`AI.html`；验证项见 §三末段）。
2. 🔴 **R67 线1 / 线2 未开工（一行代码未改）**：线1 = 设置页删 `#setAddCustom` + 模型名称改服务商分组下拉（消费 `AI_CONFIG.providerGroups`）+ 模型 ID 下拉联动 + 能力标签预填 + API 格式折叠 + 检测连接；线2 = 上下文用量改 `0.0% · 0 / 1000.0K 上下文已使用` + hover 说明 + 阈值色 + 紧凑排版。设计上被 R66 QA 阻塞（保归因）。
3. ⏳ **版本戳未打新值** —— R66/R67 改动后 4 个 AI 资产内容已变，仍是 `?v=20260916Q`（**与内容不符**）；验证全绿后应打 **`20260916R`**（属性锚定 + 最小变更 + 二进制读写 + **独立终态复验**）。
4. ⏳ **未部署上线** —— 全部改动仅在本地，部署需用户明确点头。
5. 🧹 **交付树仍有 ~96 个他批 `.bak-*`** —— 按项目约定全批次最后一步统一清理；本批 r64/r66/r67 备份**保留**。
6. 📌 `providerGroups` 第二层厂商模型展示名与线1 下拉文案对齐（线1 开工时顺手做）。
---

## 八、批次外收尾实录（R66 收口 + R67 线1/线2 + R68 + R70 · 2026-09-16 深夜 → 09-17 凌晨）

> 本节由收尾轮主理人补记，**§七 所列第 1/2/3/4 条已全部完成**。以下均为 2026-09-17 01:40 实测复核。

### 8.1 完成清单

| 事项 | 结果 | 证据 |
|---|---|---|
| R66 独立回归 + R67 线1/线2 + R68 + 打戳 `20260916R` + 部署上线 | ✅ | commit `1abfc88`；全站戳分布实测 `20260916R`×66、`20260916Q` 残留 0（`20260916O` 516 处未动） |
| R67 线1（删重复入口 + 服务商分组下拉 + 检测连接） | ✅ | `ai-settings.js` providerGroups×8、检测按钮在位 |
| R67 线2（上下文用量新格式） | ✅ | `ai-page.js` 新格式特征在位（toFixed(1)/上下文已使用） |
| R68 移动端 AI 对话框布局（输入区单行化/控件缩小/隐藏圆环） | ✅ | commits `64c7945`→`f748547`→`d7d8f76`→`98cdac0` |
| R70-① 设置页连通性检测修复 | ✅ | commit `f34939d`，真浏览器改前/改后对照通过 |
| R70-② APK 恢复联网能力重打包 | ✅ | versionCode 18 / 87.3MB，apksigner 验证通过 |

### 8.2 R70-① 连通性检测：根因与修复

- 根因：`ai-settings.html` 的 script 清单**从未包含 `assets/ai-service.js`**（R64 起即如此），而
  `window.aiHealthCheck` 只在该底座导出（`ai-service.js:1645-1647`，typeof 守卫）。设置页对设置读写有本地兜底、
  **唯独检测没有** → `typeof window.aiHealthCheck !== 'function'` → 弹「当前环境不支持连通性检测」。
  **不是浏览器/环境/部署环境问题。**
- 修复：在 `ai-config.js` 之后、`ai-settings.js` 之前补 `<script src="assets/ai-service.js?v=20260916R" defer></script>`。
- ⚠️ 新坑（已踩已修）：首次插标签**漏写 `</script>`**，底座标签把后续 ai-settings.js 吞掉 → 页面静默全空白、
  无任何控制台报错。**改 script 清单后必须核对 count(`<script`) == count(`</script>`)（现 9/9）**。
- 验证：Chrome 无头改前/改后同环境对照——改前点击无任何反应；改后弹「连通性检测 · GLM-4.7-Flash 检测成功，
  接口可用 / 耗时 0.0s」，按钮转「正常」。

### 8.3 R70-② APK 静态壳：根因与修复

- 根因：`android/build_apk.py` 曾用正则**剔除每页 `<script src="assets/api.js">`** → APK 失去全部联网能力
  （好友私信/留言板/社区互动/语音/题库全废），仅 673KB 空壳；另文本模式复制会把 LF 改成 CRLF。
- 修复：① 改**纯二进制整页复制**（保行尾）② 资源白名单补 `api.js` + AI 五件 +
  `xt-polyfill/xt-toast/error-boundary/page-head.css/states.css` ③ **打包期硬断言**：入口页必须含
  `api.js`+`config.js` 否则中止 ④ versionCode 17→18。
- 现状：新包 87.3MB（40 页 + 60 JS + 28 题库 JSON + mock-papers.js）；manifest 含 INTERNET +
  usesCleartextTraffic + allowUniversalAccessFromFileURLs；外网实测 `110.42.134.62` 的 80/8000 端口均可达；
  `config.js` 在 file:// 下返回绝对地址。
- **待验收**：真机安装（versionCode 18 可覆盖升级）验证联网功能——这是唯一硬性遗留，见开场指令任务 1。

### 8.4 交接状态更新（覆盖 §0/§4/§9 中过时结论）

- git HEAD 已推进到 `f34939d`；工作树仍有 ~44 M + 6 D 的更早批次未提交漂移（含 `server/` 3 个文件），
  属常态——**判断改动用 mtime/内容对照备份，不要用 git status**。
- `.gitignore` 已加 APK 但其仍被 git 跟踪（M 状态）——下次提交代码前先 `git rm --cached 学习工作台-安卓App.apk`。
- R68/R70 备忘尚未写入总账（`用户需求与决策总账.md` 止于 R67，638 行）——留给接手者补记。
- 旧开场指令已加过期横幅，最新版 = `给接手AI的开场指令-20260917a.txt`。
