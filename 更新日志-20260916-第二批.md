# 星途 · 更新日志（本批次 2026-09-16）

> 记录本批次**实际发生**的全部代码变更。以工作区实测为准，非计划。
> 代码树：`D:\下载的文件\学习工作台` ｜ 线上仍为 `20260916k`（**本批全部改动未部署**）

---

## 一、变更总览

| 编号 | 需求 | 状态 | 改动文件 | 验证 |
|---|---|---|---|---|
| — | **更多页白屏破损修复** | ❌ **未做**（原定 P0 前置） | — | 破损仍在（`escheck` DONE 3 的唯一来源） |
| 需求 A | 演示页「添加作品」改弹窗 | ✅ 完成 | `演示.html`、`assets/ppt-works.js` | 58/58 jsdom 冒烟通过 |
| 需求 B | 首页 6 卡更名 + 6 HTML 重命名 | ✅ 完成 | 6 个 HTML 重命名 + 52 个文件文案/引用 | QA 独立验收通过 |
| 需求 C | AI 页输入框"变脸" | ❌ **未做**（工程师零产出） | — | `AI.html` 的 760/768/`max-width:none` 规则原样未动 |
| — | AI 侧栏「模型」按钮无反应 | ❌ **未做**（工程师零产出） | — | `z-index:99` 未提，无 `closeSidebar` 调用 |
| 需求 D | AI 400 修复 + 降级/超时 | ❌ **未做**（工程师零产出） | — | `ai-service.js` **与备份完全一致** |
| — | 首页 AI 小助手重构 | 🟡 **半成品** | `assets/app.js`（+405 行）、`学习工作台.html` | `escheck` 通过（语法安全），功能完整度未验 |
| — | 更多页「关于」改独立页 | ✅ 完成 | 新建 `关于.html`、`assets/app.js` | 结构配对全绿（14/14、160/160、15/15） |
| — | 首页倒计时卡缩小 | ✅ 完成 | `assets/common.css` | QA 独立验收通过（diff 全落 countdown 内） |
| — | 用户头像加载不出来 | ✅ 完成 | `assets/api.js`、`assets/app.js` | QA 独立验收 57/57（含 XSS 边界） |
| P1 | QA 脚本硬编码线上 | ✅ 完成 | `tools/qa/page_check.js`、`multi_check.js`、`ball_verify.js` | 已加 `QA_ORIGIN` 开关 |
| P3 | `common.css` 注释旧名残留 | ❌ 未做 | — | 两处仍在（L1353、L2102） |
| P2 | `recentMap` 5 条死条目 | ❌ 未做 | — | `'社区.html':'blog'` 等仍错 |

**版本戳**：全站 **551 处**全部为 `20260916L`，**本批未 bump**（按项目约定由收尾波单点统一，目标 `20260916M`）。

---

## 二、逐项变更明细

### ✅ 需求 B · 首页功能中心 6 卡更名 + 6 个 HTML 重命名

**文件重命名（6 个，`os.rename`，未用 git）**

| 新名 | 旧名 |
|---|---|
| `社区.html` | `学习博客.html` |
| `英语.html` | `四级备考.html` |
| `行测.html` | `央国企笔试.html` |
| `表达.html` | `高情商表达.html` |
| `面测.html` | `商务礼仪面试.html` |
| `演示.html` | `PPT训练.html` |

**替换量**：文件名引用 **70 处**、展示文案 **301 处**，落地 **52 个文件**（根 HTML 30 + assets js 22）。

**`app.js` 的 `HOME_DEF`（现约 L6719）** —— 只改 `t` 与 `url`，`k`/`ic`/`dc`/`d` 原样：
```js
{ k: 'plaza', ic: '🌍', dc: 'globe',          t: '社区', d: '看大家的',     url: '社区.html' },
{ k: 'cet',   ic: '📖', dc: 'book-open',      t: '英语', d: '词汇听力阅读', url: '英语.html' },
{ k: 'exam',  ic: '📝', dc: 'pencil',         t: '行测', d: '行测刷题',     url: '行测.html' },
{ k: 'comm',  ic: '💬', dc: 'message-square', t: '表达', d: '场景话术',     url: '表达.html' },
{ k: 'interview', ic: '🤝', dc: 'handshake',  t: '面测', d: '面试题库',     url: '面测.html' },
{ k: 'ppt',   ic: '🎨', dc: 'palette',        t: '演示', d: '版式案例',     url: '演示.html' }
```

**⚠️ 替换口径的三处主动纠偏（重要，接手者必须沿用）**
原始口径"广场改 70 / 保留 36"会**损坏数据**，工程师实际执行时改为**61 改 / 45 保留**：

| 保护项 | 位置 | 原因 |
|---|---|---|
| 题库正文 8 处「广场」 | `app.js` 数量关系题「矩形文化广场」「广场的一边」「现沿广场」「文化广场」、言语理解「大广场、大草坪」、常识判断「社区广场」「某市中心广场」×2 | 改成「矩形文化社区」会直接损坏试题 |
| 「互动广场」36 处 | `更多.html`、`blog_wechat.html` 的 title/侧栏/顶栏/hero/底栏、各页 `bm-label` | 这是**另一个功能**（我的发贴/草稿/收藏，`gotoBlogMine()`），不是首页「广场」模块 |
| 「笔记广场」1 处 | `学途.html:547` | 该页自有命名，不在需求范围 |
| 「英语四级备考」2 处 | `四级经验分享.html` 帖子标题 | 否则变成「英语英语全攻略」 |
| AI 语料 1 处 | `assets/ai-presets.js` 正文「四级备考四步法」 | 属纯 AI 语料，改动会削弱输出质量（其 `keywords` 已加「英语」） |

**两处重复词预处理**：`设置.html:857`「央国企笔试(行测/企业定向库/)」→「行测(企业定向库/)」；`chat-local.js:236`「备考四级、行测、央国企笔试等」→「备考四级、行测等」。

**`recentMap`（`app.js:6776`）** 现状：
```js
var recentMap = {
  '社区.html': 'blog', '私聊.html': 'chat', '行测刷题.html': 'exam', '面试题库.html': 'iv',
  '四级词汇.html': 'cet', '英语.html': 'cet', '设置.html': 'settings', '个人中心.html': 'settings'
};
```
已修：`'英语.html'` 的 value 从 `'listen'` 改为 `'cet'`（`listen` 不是有效的 `k`，导致英语页永不记入「最近打开」）。
**未修（P2）**：`'社区.html':'blog'`（应为 `plaza`）、`'面试题库.html':'iv'`（应为 `interview`）。

### ✅ 需求 A · 演示页「添加作品」改带遮罩弹窗
- `#pptWorkModal` 从作品集卡片内的页面内区块**整块搬到页尾**，外壳换成站内既有 `.modal-overlay` + `.modal` 体系（与 `更多.html` 的 `#countdownModal` 同构），**未自创样式**。
- 遮罩关闭判据：`onclick="if(event.target.id==='pptWorkModal') pptWorksCloseAdd()"` —— 点内部不关闭。
- **ADR-3 的「id 与控件全部保留」严格执行**：`#pptWorkModal` 及内部 `pwTitle/pwType/pwFiles/pwThumbs/pwDesc/pwTags` 一个没删没改名；`#pptWorkView`（作品详情/大图）整个保留原样。
- 新增 4 个 id（纯增量）：`pwLink`、`pwFileList`、`pwTitleErr`、`pwVLink`。
- 4 条关闭路径：取消 / ✕ / ESC / 点遮罩，全部走同一个 `closeAdd()`；取消**先清 `pending`/`pendingFiles` 再关**，不落库。
- 保存：标题 `trim()` 非空校验（红字 + 描红 + focus + toast），通过后 `add()` → `closeAdd()` → `render()` 实时刷新。
- **localStorage 键 `xtc:lib:pptw:works` 与数据结构未变**，仅新增两个可选字段 `files` / `link`，旧数据读回自动补默认值。
- 本页仅补 3 处细节样式：`.pw-modal{max-width:520px}`、`.pw-modal-x`、`.pw-req/.pw-err/.pw-invalid`、`.pw-files*`；加 `@media(max-width:420px)` 断点。
- **ADR-3 注释已更新**：说明 `#pptWorkView` 仍按 ADR-3 保持页面内区块、`#pptWorkModal` 按需求 A 回滚为遮罩弹窗，并写明理由，便于回溯。

### ✅ 首页「重要倒计时」卡片缩小一档（`assets/common.css`，共 12 处）
`.countdown-row` gap 12→10｜`.countdown-card` min-width 160→140、padding 16→12｜`::before` height 4→3｜`-name` 13→12 + margin 8→5｜`-days` **32→26**｜`-days .unit` 14→12｜`-date` 11→10 + margin 8→5｜`-actions` top/right 8→6｜`-btn` 24×24→20×20 + font 12→11｜**`-add` min-width 120→140（对齐坑修复）** + gap 8→6 + font 13→12｜`-add .add-icon` 28→20｜`@media(max-width:480px)` 新增 `.countdown-card,.countdown-add{min-width:120px}`（**同选择器同值**）+ `.countdown-card{padding:10px}` + `-days{22px}` + `-add .add-icon{18px}`。

**对齐坑根因**：原来基础态 `.countdown-add` 自带 `min-width:120px` 覆盖了 `.countdown-card` 的 160px，窄屏又只给 `.countdown-card` 下调 → 虚线卡永远比正式卡窄。现两处都改为同选择器同值。

### ✅ 用户头像加载不出来（`assets/api.js` + `assets/app.js`）
- **根因**：在线模式头像早已改为文件上传 URL、不再 base64 入库（`api.js:196` 注释原文），但 6 处判据仍强制 `p.avatarImg && /^data:image\//` → 判据必然失败 → 回退首字头像。
- **修法**：`api.js` 新增 4 个工具（全部 `if (typeof window.x !== 'function')` 守卫）：
  - `window.apiAvatarSrcOf(obj)` —— 先 `avatarUrl` 再 `avatarImg`，拦 `javascript:`/`vbscript:`，结果一律过 `apiFileUrl()`
  - `window.apiAvatarHtml(obj, fallbackText, extraAttr)` —— 生成 `<img>` 时内嵌 `onerror="window.apiAvatarFallback(this,'首字')"`，letter 内嵌前剥掉 `' " \ CR LF` 防属性写穿
  - `window.apiAvatarFallback(img, letter)` —— 404 时还原为昵称首字
  - `window.apiAvatarText(s)` —— 复用 app.js 的 `esc()`，缺失时自带等价实现
- 6 处硬判据全部改造（`api.js` 4 处 + `app.js` 3 处，另收拢 6 个在线渲染点补 `onerror`）。`app.js` 三处加了 `window.apiAvatarHtml ? ... : 原逻辑` 真值守卫 —— **因登录页不注入 api.js，避免 TypeError 打挂全局脚本**。
- **`app.js` 行数 9070 → 9070（当时的头像改动零行位移）**。

### ✅ 更多页「关于」改独立页面
- 新建 `关于.html`（394 行 / 25128 B）：`<script>` 14/14、`<!--` 15/15、`<div` 160/160 全配对；含 `.topbar` + `.morepage-back` 返回 + `.content`/`.page` + 底部导航（顺序 `首页→互动→我的→AI→更多`）。
- **共用 DOM 全部保留**：`#aiFab`、`#aiPanel`、`#morePanel`、`#toolsPanel`、`#toast`、`#countdownModal`、`.more-overlay` 各就位。
- `app.js` 的 `showAbout()`（现 L9004）改为**跳转 `关于.html`**，并加了「已在关于页则忽略」的防自跳转判据；**旧页内弹窗保留为 `showAboutDialog()`**（`app.js:9013`，含免责声明等完整文案）作兜底。
- 全站 `showAbout()` 调用点（更多页菜单、底部更多面板）无需改动即生效（因为函数本身改了实现）。

### 🟡 首页 AI 小助手重构（半成品，`assets/app.js` +405 行）
**已确认完成的部分**：
- `AI_PARTNERS` 第一个角色已改：`id: 'assistant'`、`name: '小助手'`、`icon: 'sparkles'`、`emoji: '✨'`、`tag: '全能学习助手'`、`desc` 已改为全站覆盖口径、`systemPrompt` 已改为通用学习助手人设、`greeting` 已重写。
- **旧 id 兼容映射已做**（`app.js:3092`）：`gongkao / xiaotu / coach / mentor / interviewer / buddy → assistant`。
- 第二个角色「暖心学伴」（id `warm`）**保留**，`demoStyle` 两份**都保留作降级兜底**。
- `callAI` 引用从 **3 处 → 11 处**（已接真实 AI 底座）。
- 卡片副标题加了注释「不再出现"本地演示模式"」（`app.js:3265`）。

**未确认完成的部分**（接手者必须逐项核验）：
- 卡片头部布局（图标/名称/tag + 切换角色 + 关闭/清空）
- 对话区分气泡、流式、加载态、复制、重新生成、空态问候语
- 输入框 + 「在 AI 页打开」入口
- 6 个快捷功能是否精简为一行横滑、是否走真实 AI
- 「换个伙伴」弹窗 2 卡是否风格统一、切换是否记住（localStorage）、是否丢历史
- 与 `AI.html` 的模型/历史共享口径是否落地
- `app.js` 中仍有 7 处「本地演示模式」文案（L2964/L3075/L3938/L3953/L7861/L8609 等），**需甄别哪些是必须去掉的卡片副标题、哪些是设置页既有功能描述（后者可保留）**

### ✅ P1 · QA 脚本硬编码线上（QA 工程师自修）
`tools/qa/page_check.js`、`multi_check.js`、`ball_verify.js` 三处 `ORIGIN` 改为支持 `QA_ORIGIN` 环境变量，默认指向本地，注释里写明用法：
```
python -m http.server 8899 --directory "D:\下载的文件\学习工作台"
node tools/qa/page_check.js "社区.html" "社区"
QA_ORIGIN=http://110.42.134.62 node tools/qa/page_check.js "社区.html"   # 验线上
```

---

## 三、已知未做（详见对接文档）

| 优先级 | 项 | 说明 |
|---|---|---|
| **P0** | `更多.html` 白屏级破损修复 | 结构指标：`<script>` 16 / `</script>` **15**、`</head>` **2**、`<body` **2**、`#importerView` 2、`.morepage-list` 2、卡片标题 13 条 |
| **P0** | 需求 D（`ai-service.js` 零改动） | 400 修复、降级条件、15 秒超时、429 跳过、流式守卫 全部未做 |
| **P1** | 需求 C + 模型按钮（`AI.html` 零改动） | 输入框双槽位外观统一 + 面板被遮罩盖住 |
| **P2** | `recentMap` 2 条死条目 | `blog`→`plaza`、`iv`→`interview` |
| **P3** | `common.css` 注释旧名 2 处 | L1353、L2102 |
| — | 全站 bump → `20260916M` | 551 处仍是 `20260916L` |
| — | 部署 + 线上复检 | 本批改动全部未上线 |

---

## 四、团队执行记录（谁做了什么）

| 成员 | 任务 | 结果 |
|---|---|---|
| 寇豆码（`software-engineer-任务四`） | 倒计时卡 + 头像 | ✅ 交付，QA 验收通过 |
| 寇豆码（`software-engineer-任务五`） | 需求 B 改名重命名 | ✅ 交付（含 3 处口径纠偏 + `listen→cet` 补修） |
| 寇豆码（`software-engineer-任务六`） | 需求 A 改弹窗 | ✅ 交付，58/58 冒烟 |
| 寇豆码（`software-engineer-任务七`） | 首页小助手 + 关于页 | 🟡 **429 限流中断**，留下 +405 行半成品（语法安全） |
| 严过关（`software-qa-engineer`） | 需求B + 倒计时/头像 独立验收 | ✅ 出具完整验收报告，并修完 P1 |
| w0（`w0-more-fix`） | 更多页破损修复 | ❌ **零产出**（文件与备份逐字节一致） |
| w1（`w1-ai-page`） | 需求 C + 模型按钮 | ❌ **零产出**（`AI.html` 的 6 处改动全属需求 B 改名） |
| 寇豆码（`software-engineer`） | 需求 D | ❌ **零产出**（`ai-service.js` 与备份完全一致） |

**失败原因**：三名成员均被 **429 频率限制**打停（额度 2026-09-17 11:09 重置）。三名"零产出"成员在被打停前未落盘任何改动。

---

*生成时间：2026-09-16 13:15 · 依据：对 `D:\下载的文件\学习工作台` 的逐文件 md5 对比 + 结构指标实测 + difflib 差异定位*
