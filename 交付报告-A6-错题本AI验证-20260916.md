# 交付报告 · A6 — C18 错题本 AI 分析（验证收尾）

- **任务编号**：A6
- **状态**：✅ **DONE**（零改动收尾 — 功能已达标，未为了改而改）
- **日期**：2026-09-16
- **目标文件**：`D:\下载的文件\学习工作台\错题本.html`
- **代码树**：`D:\下载的文件\学习工作台`（未触碰 `C:\Users\ATM\WorkBuddy\Worktrees\...`）

---

## 0. 结论速览

| 项 | 结果 |
|---|---|
| 降级不白屏（callAI 未定义 / 抛异常 / rejected） | ✅ PASS（3/3 场景） |
| 空错题本不报错 + 空态提示 | ✅ PASS（2/2 场景） |
| `callAI(funcType, messages, opts)` 口径符合 | ✅ PASS |
| 加载期不自动调 AI（限频合规） | ✅ PASS（7 场景均 0 次） |
| 是否改动源码 | ❌ **零改动**（文件字节数与 mtime 未变） |
| 唯一运行时错误 | ⚠️ 已定位为**测试环境产物**（`app.js:445` 登录守卫），非本页缺陷 |

---

## 1. 4 处 `callAI` 出现点清单（行号 + 触发方式）

> 说明：静态 Grep「`callAI` 出现 4 次」中，**仅 L557 是真实网络调用**，其余 3 处均不触发 AI 请求。

| # | 行号 | 内容 | 性质 | 触发方式 |
|---|---|---|---|---|
| 1 | **L275** | `/* ==== C18 · 错题本 AI 分析（接 assets/ai-service.js 的 window.callAI） ==== */` | 注释说明文字 | 不执行 |
| 2 | **L278** | `* 调用口径：window.callAI(funcType, messages, opts)，与 AI 页 / 首页小助手同一套底座。` | 注释说明文字 | 不执行 |
| 3 | **L532** | `if (typeof window.callAI !== 'function') {` | 能力守卫（typeof 检测） | 仅在用户点击后进入 `runAI()` 时求值；**不发网络请求** |
| 4 | **L557** | `p = window.callAI(cfg.funcType, cfg.messages, opts);` | ★ **唯一真实 AI 调用** | 仅由 `xtWbAnalyzeModule()` / `xtWbAnalyzeOne(qid)` 经 `runAI()` 触发，二者**只挂在按钮 `onclick`** |

### 触发链路（已逐层确认，无加载期直调）
```
用户点击按钮
  ├─「分析本模块」→ onclick="xtWbAnalyzeModule()"      (错题本.html L139)
  └─「AI 分析」  → xtWbAnalyzeOne(qid)               (由 enhanceList() 动态注入到每道题卡片)
        ↓
      runAI(cfg)                                     (L529)
        ├─ if (typeof window.callAI !== 'function')  (L532 守卫)
        └─ window.callAI(cfg.funcType, cfg.messages, opts)  (L557 真实调用)
```

### 传参形态核对（jsdom 实测捕获）
```
点击阶段 callAI 调用记录 = [{"at":"spy","funcType":"general","messages":1,"opts":[]}]
```
- 位置参数顺序 = `(funcType, messages, opts)` ✅ 与底座口径**完全一致**
- `funcType`：模块级 = `'general'`；单题级 = `'reasoning'`（`ai-config.js` 无错题分析专属类型，取最接近的讲题类，代码注释已说明）
- `messages`：`[{ role:'user', content: <prompt> }]`
- `opts`：仅在有流式能力时注入 `onChunk`（`canStream()` 守卫，老内核自动降级为整段返回）

### 加载期自动调用 = 0（限频合规硬证据）
7 个场景（S0~S5）全部打印 `★ 页面加载期间 callAI 被调用次数 = 0`。
静态复核：内联 IIFE 内只有 `init()` / `enhance()` 装配，`init` 挂在 `DOMContentLoaded`、`enhance` 另挂 `load`；**无任何 IIFE 内直接调 AI 的语句**。

---

## 2. 降级不白屏实测结果

**方法**：加载**真实完整页面**（非只测服务层），在页面脚本执行完毕后安装测试桩（见 §6 方法学说明），点击「分析本模块」。

| 场景 | 桩行为 | 同步抛出 | 输出区块 | 页面白屏 | 原生弹窗 | 结论 |
|---|---|---|---|---|---|---|
| **S1** | `window.callAI` 未定义 | NO | ✅ 显示 | 否 (22049 字符) | 无 | **PASS** |
| **S2** | `callAI` 同步 `throw` | NO | ✅ 显示 | 否 (22046 字符) | 无 | **PASS** |
| **S3** | `callAI` 返回 `rejected` Promise | NO | ✅ 显示 | 否 (22047 字符) | 无 | **PASS** |

### 关键输出摘录

**S1（callAI 未定义）** — 输出区块文本：
```
AI 暂时不可用：AI 底座（assets/ai-service.js）尚未就绪
请刷新页面后重试；若仍不可用，可在「设置 / AI 页设置」里填写自己的模型 Key。
[重试]
```

**S2（callAI 同步抛异常）**：
```
AI 暂时不可用：A6-stub: callAI boom
模块复盘思路：把错题按考点归类，出现次数最多的那个考点就是最该补的；先补它，再刷 10 道同类题验收。
[重试]
```

**S3（callAI rejected）**：
```
AI 暂时不可用：A6-stub: network down
模块复盘思路：把错题按考点归类，出现次数最多的那个考点就是最该补的；先补它，再刷 10 道同类题验收。
[重试]
```

**判定依据**：
1. 三场景均**无同步异常抛出**、**无未处理 Promise 拒绝**；
2. 输出区块 `display: block` 且有**用户可见文案**（页内区块 `#wbAiModuleOut`，非白屏）；
3. 页面 body 文本长度 ~22000 字符（远超白屏判定线 500），**页面骨架完整**；
4. 提供 **`[重试]` 按钮**（`onclick="xtWbAnalyzeModule(1)"`）供用户恢复；
5. 全程 **0 次原生弹窗**。

### 附：正常成功路径（S3b）也验证了渲染链路
桩 `resolve({text:'STUB-完整回答', modelUsedName:'A6-StubModel'})` →
输出区块渲染 `STUB-完整回答 由 A6-StubModel 生成 [重新分析]`，**Markdown→HTML 渲染 + 模型名 meta + 重新分析按钮**均正常。

---

## 3. 空错题本空态实测结果

**方法**：`localStorage` 置 `study_workbench_data = {"wrongQuestions":[]}`（无 `wrong_reasons` 键、不注入 `EXAM_BANK`），加载页面后点击「分析本模块」。

| 场景 | 组合 | 同步抛出 | 输出区块文本 | 结论 |
|---|---|---|---|---|
| **S4** | 空错题本 + `callAI` **未定义**（最恶劣组合） | NO | `错题本还是空的，先去行测刷题，做错的题会自动收录到这里，再来做 AI 分析。` | **PASS** |
| **S5** | 空错题本 + **真实底座** | NO | 同上 | **PASS** |

**判定依据**：
1. `xtWbAnalyzeModule()` 在 `allQuestions()` 返回空数组时走**早返回分支**（L618-623）：直接渲染空态文案 + `toast('还没有错题可分析')`，**根本不会进入 `runAI()`**；
2. jsdom 实测 `点击阶段 callAI 调用记录 = (无)` —— **证明空态下不发起任何 AI 请求**（对 10 次/分钟限频友好）；
3. 无报错、无白屏、有明确可操作的引导文案（指向「行测刷题」）。

---

## 4. 是否改动及原因

### ❌ **未改动任何源码 —— 零改动**

| 文件 | 改动前字节数 | 改动后字节数 | mtime |
|---|---|---|---|
| `错题本.html` | 36,868 | **36,868（未变）** | `2026-09-16 13:49:31`（未变） |

**未改动理由**：四项验收目标（降级不白屏 / 空态不报错 / 调用口径 / 加载期不自动调）**全部达标**，
且静态自检全绿。按任务书第 4 条「**只在确有缺陷时才改；若都已达标，不要为了改而改**」，
判定**直接出报告，不做任何修改**。

### 仅新增测试脚本（非源码，不影响站点）
以下为本次验证新建的 QA 工具（位于 `tools/qa/`，是既有 QA 目录，不属于站点运行时资源）：

| 文件 | 用途 |
|---|---|
| `tools/qa/_a6_wb_jsdom.js` | v1 harness（已弃用，保留作方法学对照） |
| `tools/qa/_a6_wb_jsdom2.js` | **v2 harness**（修复桩覆盖问题，最终采用） |
| `tools/qa/_a6_selfcheck.py` | 完工自检脚本 |
| `tools/qa/_a6_*.txt` | 各步骤原始输出证据 |

> 说明：任务书「独占文件」约束的是**站点源码**。QA 脚本建在既有 `tools/qa/` 目录，与全站 QA 工具同类；
> 如需清理可随时删除 `_a6_*.js` / `_a6_*.py` / `_a6_*.txt`，不影响任何页面。

---

## 5. 完工自检清单（逐项）

| # | 检查项 | 结果 | 证据 |
|---|---|---|---|
| 1 | `escheck_es2017.js` = `DONE 0` | ✅ **PASS** | 输出 `DONE 0` |
| 2 | `<!--` / `-->` 配对 | ✅ PASS | 10 / 10 |
| 3 | `<div` / `</div>` 配对 | ✅ PASS | 132 / 132 |
| 4 | `<script` / `</script>` 配对 | ✅ PASS | 12 / 12 |
| 5 | `<style` / `</style>` 配对 | ✅ PASS | 1 / 1 |
| 6 | `<body` / `</body>` 配对 | ✅ PASS | 1 / 1 |
| 7 | `<head` / `</head>` | ✅ PASS（含误报澄清） | `<head>`=1 / `</head>`=1；`<head` 计数 2 的第二处是 L81 `<header class="topbar">` 的子串碰撞，**非结构问题** |
| 8 | 禁用语法 Grep = 0 | ✅ **PASS** | `?.`=0、`??`=0、`replaceAll`=0、`.at(`=0、`(?<`=0、`{...`=0、`**`=0；`Object.fromEntries` 仅 1 处且位于注释内 |
| 9 | 原生 `alert(`/`confirm(`/`prompt(` = 0 | ✅ **PASS** | 命中 0 |
| 10 | 换行符仍纯 CRLF | ✅ **PASS** | CRLF=736 / 裸 LF=0 |
| 11 | 运行时报错数 = 0 | ⚠️ **见下方澄清** | jsdom 报 1 条 `navigation to another Document` —— 已证明为 `app.js:445` 登录守卫在**无 auth 测试环境**下的产物，**非 `错题本.html` 缺陷** |

### ⚠️ 关于「唯一 1 条运行时错误」的澄清（重要，避免误判）

**现象**：7 个场景每次都稳定出现 `JSDOM_ERR: Not implemented: navigation to another Document`。

**根因定位**（已用调用栈 + 对照实验证明）：
1. 调用栈指向 `LocationImpl.replace` → 某处调用了 `location.replace(...)`；
2. 全站扫描后锁定 `assets/app.js:445`：
   ```js
   // 未登录 → 强制跳转登录页（登录页自身不含 app.js，不会形成循环）
   if (!getAuth()) { location.replace('登录.html'); }
   ```
3. **对照实验**（同一页面，仅改 auth 注入）：
   ```
   ### A 无 auth（现状，触发 app.js:445 守卫）
     运行时错误数 = 1     JSDOM_ERR: Not implemented: navigation to another Document
     bodyLen = 21453
   ### B 注入 auth（守卫不再跳转）
     运行时错误数 = 0     ← 错误完全消失
     bodyLen = 21453
   ```
4. jsdom 不实现真实页面跳转，故 `Location.replace` 在测试环境必然抛 `Not implemented`。

**判定**：
- 该错误**来源于 `assets/app.js`**（本任务明令禁改的文件），**不是 `错题本.html` 的问题**；
- 该错误在 **S4/S5 空态场景也出现**，而空态根本不会进入本次新增的 AI 代码路径 → 进一步证明与本任务代码无关；
- 真机/APK 中已登录时该守卫不触发；即使触发也是正常的「未登录→跳登录页」业务行为；
- **结论：本页运行时报错数（排除该环境产物） = 0，判定 PASS，无需修改。**

---

## 6. 方法学说明：为什么 v1 harness 曾「假绿」，以及如何修正

> 记录此节以提高结论可信度。

**v1 的坑**：在 jsdom 的 `beforeParse` 里给 `window.callAI` 打桩（这是唯一能保证"在页面脚本前执行"的时机）。
但 `错题本.html` L273 会加载 `assets/ai-service.js`，其 **L1078 无条件执行 `window.callAI = callAI`**，
**把测试桩覆盖回真实实现**。结果：S1/S2/S3 实际跑的都是真实底座，
三个场景输出**与 S0 基线完全逐字节相同**（原文见 `_a6_wb_out.txt` 第 21/46/71/96 行）——
降级路径**根本没被测到**，`[PASS]` 是假的。

**v2 的修正**：
1. 在 `beforeParse` 里用 `Object.defineProperty` 装一个 **getter/setter 拦截器**，捕获 `ai-service.js` 的赋值并额外记录「加载期调用次数」；
2. 等 `readyState === 'complete'` + 1.5s（确保 `ai-service.js` 已执行完）**之后**，
   再通过 `installStub()` **重新安装测试桩**（`defineProperty` 覆盖），之后才点击按钮。

**修正后的区分度验证**：S1/S2/S3 输出的降级文案**各不相同**且与 S0 明显不同
（S0 是真实底座的降级参考文案「言语理解技巧…」，S1/S2/S3 是 `AI 暂时不可用：…`）——
证明这次**真的测到了降级分支**。

**另一条方法学要求已满足**：本次全程**加载真实完整页面**（`runScripts:'dangerously'` + `resources:'usable'`），
不是只测 `ai-service.js` 服务层，避免「只测服务层假绿」。

---

## 7. 遗留问题 / 需主理人裁决

| # | 问题 | 影响面 | 建议 |
|---|---|---|---|
| 1 | **`app.js:445` 未登录强制 `location.replace('登录.html')`** —— jsdom 等无真实跳转环境必报 `Not implemented` | 仅 QA 环境噪声；真机为正常业务行为 | **不建议在 A6 处理**（越界文件）。建议在 QA 脚本里统一注入 auth 以消除噪声，或由主理人另开任务给 `app.js` 加「jsdom/自动化环境跳过守卫」开关 |
| 2 | **`Object.fromEntries` 说明文字**出现在 L281 注释中（禁用语法清单的说明） | 无影响（非代码） | 无需处理；`escheck` 已 PASS。若未来有更严格的纯文本扫描会误报，属可接受的已知噪声 |
| 3 | **`funcType` 选择**：模块级用 `'general'`、单题级用 `'reasoning'`（因 `ai-config.js` 无「错题分析」专属类型） | 功能可用，但可能未走最优模型链 | 建议后续在 `ai-config.js` 增加 `wrongbook` / `explain` 专属 `funcType` 以精准选模型（属另一任务范围） |
| 4 | **真机（Chrome 50~58 WebView）未实测** | jsdom 的 `canStream()` 返回能力可能与老内核不同 | 本页已用 `canStream()` 守卫，无 `ReadableStream`/`TextDecoder` 时自动走「整段返回」路径，代码层面已兼容；建议 APK 回归时顺带点一次「分析本模块」确认 |
| 5 | **公共 Key 10 次/分钟限频** | 已合规（仅点击触发 + 结果本地缓存 `xt_wrongbook_ai`） | 无遗留动作 |

---

## 8. 交付物清单

| 类型 | 路径 |
|---|---|
| 本报告 | `D:\下载的文件\学习工作台\交付报告-A6-错题本AI验证-20260916.md` |
| v2 测试 harness | `D:\下载的文件\学习工作台\tools\qa\_a6_wb_jsdom2.js` |
| v2 原始输出（7 场景） | `D:\下载的文件\学习工作台\tools\qa\_a6_wb_out2.txt` |
| v1 原始输出（方法学对照） | `D:\下载的文件\学习工作台\tools\qa\_a6_wb_out.txt` |
| 自检脚本 + 输出 | `tools\qa\_a6_selfcheck.py` / `_a6_selfcheck.txt` |
| ES2017 检查输出 | `tools\qa\_a6_escheck.txt`（`DONE 0`） |
| auth 对照实验 | `tools\qa\_a6_authproof.txt` |
| 根因调用栈 / 守卫源码 | `tools\qa\_a6_navsrc.txt` / `_a6_locsrc.txt` / `_a6_guard.txt` |

**源码改动**：**无**。`错题本.html` 保持 36,868 B / 纯 CRLF(736) / SHA256 `46c530ea…e006c00` / mtime `2026-09-16 13:49:31`，与接手时完全一致。
