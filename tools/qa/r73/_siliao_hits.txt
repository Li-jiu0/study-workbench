命中 18 行
6: // toast 提示（私聊页独立实现，不依赖 app.js）
24: - window.currentChatUserId：当前打开的会话对象 userId（私聊=对方 serverId；群聊/无会话=null）
234: 本文件此前直读裸键 → 每次进私聊页本地会话 / AI 配置归零。
327: - threadKey：服务器私聊 'u<serverId>'；本地/AI 会话 'l<id>'；群聊 'g<id>'。
476: - 仅本人发出的私聊消息可撤回；2 分钟时限；轻量确认气泡（非全屏 modal，符合 ADR-3）。
610: 菜单项：撤回（仅本人 + 私聊 + 2 分钟内）/ 复制 / 删除本端；转发、收藏本次不做，故不显露。
1510: '<div style="font-size:14px;line-height:1.7">确定删除好友「<b>' + esc(name) + '</b>」吗？删除后双方互不可见，且无法继续私聊。</div>' +
1874: // 需求24：私聊是真实好友会话，不再挂「演示模式」误导标签；
1886: // T02 增量：私聊会话隐藏「⋯」群设置入口
1937: // 已读角标（T4 增量）：仅服务器私聊消息展示 ✓已发送 / ✓✓已读
1971: // 与私聊好友列表点头像行为一致；非好友主页只有「加为好友」，好友主页有「发消息」；
1996: → 私聊 POST /api/chat/{serverId}/messages {content:url, kind:'image'}
2020: /* 上传成功后把 url 作为一条 kind='image' 消息发出去（群聊 / 私聊各一条分支）。
2244: if (S.group) { toast('语音消息仅支持私聊'); return; }
2476: // 取 #note=ID 深链拼到正文并消费掉（私聊/群聊都走这里）；无转发时 __imConsumeForward 返回 ''。
3166: /* R42（2026-09-14）：管理员在「全部用户」列表点用户头像/昵称 → 在私聊页内弹资料卡，
4012: // R60：未读总数（私聊 + 群聊，同样跳过免打扰/已隐藏）抛给 app.js
4044: /* R60：群聊未读总数（/api/chat/unread 只统计私聊，群未读来自 loadGroups 的 unreadCount）。