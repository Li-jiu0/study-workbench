# -*- coding: utf-8 -*-
import os

P = r"D:\下载的文件\学习工作台\用户需求与决策总账.md"
raw = open(P, "rb").read()
before = (raw.count(b"\r\n"), raw.count(b"\n"))

BLOCK = """
### ⚠️ 勘误：需求 20 主理人误读已纠正（2026-09-17 用户亲口重新下发原文）

**用户重新下发的需求 20 原文（B 部分，逐字）**：

> 同时，请确保应用打包为 APK 并安装到手机后，**项目里各种界面显示文字显示都与布局均能正确适配，不出现错位、串行，溢出、遮挡、字体或元素尺寸异常等问题**。请明确说明以下内容：需要覆盖的页面范围、适配的目标设备与屏幕尺寸、判定适配是否正常的标准，以及验证适配效果的具体方式。

**原裁定作废**（见上文「主理人裁定」第 2 条）：此前把需求里的某个**外部站点名**当成原文，再据此「映射回本项目页面」——**该表述是主理人误读**。需求原文写的是「**项目里**各种界面」，范围本来就是本项目自己的全部页面，**需求从未涉及任何外部站点**。

| 对比项 | 错的那一版 | 纠正后 |
|---|---|---|
| 覆盖对象 | 误把外部站点当原文，再自造「映射」 | **本项目 42 个根目录页面**（= APK 内可访问全部页面） |
| 关注维度 | 只做「布局」 | **「显示文字显示」与「布局」并重** |
| 缺陷清单 | 主理人自行归纳 | 按原文逐字列五类：**错位、串行、溢出、遮挡、字体或元素尺寸异常** |
| 判定断言 | A1–A8 全是几何断言 | **新增文字显示专项 A9–A13**（窜行 / 逐字断行 / 长串可断 / 字号层级 / 系统字体 1.3×） |

**已落地的纠正动作**：
1. `需求20-移动端适配规格与验收标准-20260917.md` **整篇重写**（304 行，CRLF 无裸 LF）：删除全部错误表述；新增「版本修正说明」；新增 **A9–A13 文字显示专项断言**；新增「需求原文五类缺陷 → 断言映射表」（错位→A2/A9、串行→A9/A10、溢出→A1/A4/A11、遮挡→A3/A5、字体或元素尺寸异常→A7/A12/A13/A8）；新增「本项目串行/文字异常高频根因表」；逐页验收表补入文字维度验收点；新增「本批新增页面须纳入验收」小节（朋友圈/个人资料/赞助/好友 4 个新页）。
2. 已把纠正后的需求原文与规格书要点下发给移动适配线（任务八）并唤醒全部暂停线路。

**教训（记入本账，避免复发）**：需求文档里出现与项目无关的专有名词时，**先当作疑似误粘去核对原文，绝不可自行「映射/重新界定范围」后当作需求执行**——一旦在「需求里没有的东西」上发散，整条线的工作方向都会偏。

---

### 主理人裁定（补充第 7 条）

7. **需求 20-B 的验收断言以修正版为准**：A1–A13 共 13 条，其中 A9–A13 为文字显示专项。QA 的 L2 自动化必须跑「7 视口 × A1–A13」+「根字号 130% 复跑 A1–A4/A9」两轮，全部产出 CSV 与截图后才算需求 20-B 通过。
"""

# 保证以换行结尾
if not raw.endswith(b"\n"):
    raw += b"\r\n"
if not raw.endswith(b"\r\n\r\n"):
    raw += b"\r\n"

add = BLOCK.replace("\r\n", "\n").replace("\n", "\r\n").lstrip("\r\n")
raw += add.encode("utf-8")
open(P, "wb").write(raw)

r2 = open(P, "rb").read()
a = (r2.count(b"\r\n"), r2.count(b"\n"))
print("before CRLF=%d LF=%d bare=%d" % (before[0], before[1], before[1] - before[0]))
print("after  CRLF=%d LF=%d bare=%d  (bare must be 0)" % (a[0], a[1], a[1] - a[0]))
